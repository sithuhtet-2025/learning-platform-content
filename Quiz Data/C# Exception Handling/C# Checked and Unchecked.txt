INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Checked and Unchecked', 'What is the primary purpose of the ''checked'' and ''unchecked'' keywords in C#?', 'To verify if a database connection is open or closed before executing a query.', 'To explicitly control the overflow-checking context for integral-type arithmetic operations and conversions.', 'To ensure variables are properly initialized before they are used.', 'To validate user input against a regular expression pattern.', 'B', 'The ''checked'' and ''unchecked'' keywords are used to explicitly enable or disable overflow checking for integral types (like int, long, byte) during arithmetic operations and explicit casting.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Checked and Unchecked', 'By default, what happens in C# if an integer arithmetic operation (like addition or multiplication) results in a value that exceeds the maximum capacity of its data type at runtime?', 'The program immediately crashes and throws an OutOfMemoryException.', 'The compiler automatically upgrades the variable to a larger data type (e.g., int to long).', 'The operation silently "wraps around" (truncates the high-order bits), resulting in a mathematically incorrect value without throwing an error.', 'It throws a DivideByZeroException.', 'C', 'By default, C# runtime arithmetic operations are "unchecked". If an operation overflows, it silently discards the high-order bits that don''t fit in the destination type, resulting in a wrap-around (often to a negative number) without throwing any exceptions.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Checked and Unchecked', 'What specific exception is thrown when an integral arithmetic operation overflows while executing inside a ''checked'' context?', 'System.ArithmeticException', 'System.OverflowException', 'System.InvalidCastException', 'System.IndexOutOfRangeException', 'B', 'When an arithmetic operation or conversion produces a value outside the bounds of the destination type inside a ''checked'' block or expression, the Common Language Runtime (CLR) immediately throws a System.OverflowException.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Checked and Unchecked', 'Which of the following data types are completely unaffected by the ''checked'' and ''unchecked'' keywords?', 'byte and short', 'int and uint', 'long and ulong', 'float and double', 'D', 'Floating-point types (float and double) do not throw OverflowExceptions in C#. If a floating-point operation overflows, it evaluates to special values like PositiveInfinity, NegativeInfinity, or NaN (Not a Number), completely ignoring the checked/unchecked context.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Checked and Unchecked', 'If you write the code `int x = int.MaxValue + 1;` using hardcoded literals, how does the C# compiler handle this by default?', 'It compiles perfectly but silently wraps around to a negative number at runtime.', 'It automatically wraps the statement in an unchecked block.', 'It evaluates the constant expression at compile-time and throws a compiler error because it detects the overflow.', 'It automatically upgrades the ''int'' to a ''long''.', 'C', 'Unlike runtime variables, expressions consisting entirely of constant values are evaluated by the compiler at compile-time. By default, the compiler evaluates constant expressions in a checked context and will refuse to compile if it detects a guaranteed overflow.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Checked and Unchecked', 'Which of the following syntaxes correctly demonstrates how to apply the ''checked'' context to a single, inline expression rather than a whole block of code?', 'int z = [checked] (x + y);', 'int z = checked(x + y);', 'int z = (checked) x + y;', 'checked int z = x + y;', 'B', 'Both ''checked'' and ''unchecked'' can be used as operators for a single expression by wrapping the operation in parentheses (e.g., `checked(x + y)`), or as statements by wrapping multiple lines of code in curly braces `{}`.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Checked and Unchecked', 'If a method call `CalculateSum(a, b)` is placed strictly inside a `checked { }` block, does the overflow-checking apply to the arithmetic happening inside the `CalculateSum` method?', 'Yes, the checked context is dynamically inherited by all methods called within the block.', 'No, the checked context is strictly lexical; it only applies to the literal code written between the curly braces, not to external method invocations.', 'Yes, but only if the method is in the same class.', 'No, checked blocks can only contain variable declarations.', 'B', 'The checked and unchecked contexts are lexically scoped. They do not flow down the call stack at runtime. Any operations hidden inside a method call will use the default context (or their own local context) regardless of where the method was invoked.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Checked and Unchecked', 'How can you globally change the default overflow-checking behavior for an entire C# project so that all operations throw exceptions on overflow without adding `checked` blocks everywhere?', 'By adding `using System.Checked;` to every file.', 'By setting the `<CheckForOverflowUnderflow>true</CheckForOverflowUnderflow>` property in the project''s .csproj file.', 'By modifying the Windows Registry.', 'By inheriting all classes from `System.CheckedObject`.', 'B', 'You can globally alter the compiler''s default behavior by enabling the CheckForOverflowUnderflow build setting in your project configuration (or passing the `/checked+` flag to the compiler). This is often turned on during debugging and turned off for release builds to maximize performance.');