INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# FileInfo Class', 'What is the primary architectural difference between the `FileInfo` class and the `File` class in C#?', '`FileInfo` is exclusively used for text files, while `File` handles binary data.', '`FileInfo` requires instantiation and caches metadata, making it highly optimized for multiple operations on the same file, whereas `File` provides static methods for quick, single operations.', '`File` is deprecated in modern .NET; `FileInfo` must always be used.', '`FileInfo` can only read files, while `File` can create and delete them.', 'B', 'The static `File` class performs security checks and accesses the file system every single time a method is called. `FileInfo` is an instance class that caches file state (like size and permissions), making it significantly faster when executing multiple operations on the exact same file.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# FileInfo Class', 'Which syntax correctly instantiates a `FileInfo` object to inspect a specific file?', 'FileInfo file = File.GetInfo(@"C:\data.txt");', 'FileInfo file = @"C:\data.txt" as FileInfo;', 'FileInfo file = new FileInfo(@"C:\data.txt");', 'FileInfo file = File.Open(@"C:\data.txt");', 'C', 'Because `FileInfo` represents a specific, stateful instance of a file on the file system, you must explicitly instantiate it using the `new` keyword and its constructor, passing the file''s physical path as a string.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# FileInfo Class', 'Which property of an instantiated `FileInfo` object do you access to determine the exact size of the file?', 'Size', 'ByteCount', 'Length', 'Capacity', 'C', 'The `Length` property of a `FileInfo` object queries the file system and returns the total size of the current file in bytes, represented as a 64-bit integer (`long`).');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# FileInfo Class', 'How do you extract just the file extension (e.g., ".json" or ".csv") from a `FileInfo` object named `myFile`?', 'myFile.Extension', 'myFile.GetExtension()', 'myFile.Suffix', 'myFile.FileType', 'A', 'The `Extension` property immediately returns a string representing the extension part of the file name, perfectly preserving the leading period (e.g., ".txt").');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# FileInfo Class', 'Before attempting to open, move, or delete a file using a `FileInfo` object, which property should you evaluate to prevent catastrophic runtime exceptions?', 'IsValid', 'IsAccessible', 'HasData', 'Exists', 'D', 'Checking the `Exists` property (a boolean) guarantees that the file physically exists at the specified path at that exact moment in execution, safely preventing `FileNotFoundException` crashes during subsequent operations.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# FileInfo Class', 'If you want to copy a file using `FileInfo.CopyTo(string destFileName)`, how do you instruct the CLR to overwrite an existing file at the destination?', 'It overwrites automatically by default.', 'Pass `overwrite: true` as the second parameter (e.g., `CopyTo(destPath, true)`).', 'Call `Delete()` on the destination path first.', 'Pass `FileMode.Overwrite` as the second parameter.', 'B', 'By default, `CopyTo()` will aggressively throw an `IOException` if the destination file already exists to prevent accidental data loss. You must explicitly pass the boolean `true` as the second argument to authorize the method to overwrite the existing file safely.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# FileInfo Class', 'What happens if you call `myFile.Delete()` on a `FileInfo` object, but the file does not actually exist on the hard drive?', 'It throws a `FileNotFoundException`.', 'It throws an `IOException`.', 'It creates a zero-byte file and immediately deletes it.', 'It safely does nothing and execution continues normally without throwing an exception.', 'D', 'Unlike many strict file operations, the `FileInfo.Delete()` method is architecturally robust. If the file does not exist when the method is called, it simply returns silently, avoiding unnecessary `try-catch` boilerplate code.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# FileInfo Class', 'Which property on a `FileInfo` object allows you to easily traverse upward and obtain the parent folder where the file resides?', 'ParentFolder', 'Directory', 'Root', 'Location', 'B', 'The `Directory` property returns a fully instantiated `DirectoryInfo` object representing the parent directory of the file. This allows developers to seamlessly transition from file manipulation directly into directory traversal in a single fluid motion.');