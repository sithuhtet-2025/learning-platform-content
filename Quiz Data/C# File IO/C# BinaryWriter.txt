INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# BinaryWriter', 'What is the primary architectural purpose of the `BinaryWriter` class in C#?', 'To convert C# objects into JSON format for network transfer.', 'To write primitive data types (like int, double, and string) in binary format directly to an underlying stream.', 'To encrypt data streams in memory securely.', 'To write human-readable UTF-8 text to a log file.', 'B', 'The `BinaryWriter` class provides a simplified, highly optimized interface for writing raw primitive C# data types directly to an underlying stream (like a FileStream or MemoryStream) in a compact binary format.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# BinaryWriter', 'Unlike the `BinaryReader`, which has specifically named methods like `ReadInt32()` and `ReadBoolean()`, how does `BinaryWriter` handle writing different primitive types?', 'It requires the developer to manually convert everything to a byte array first.', 'It uses generic methods like `Write<T>()`.', 'It uses a single, heavily overloaded `Write()` method that accepts almost all primitive data types.', 'It uses specifically named methods like `WriteInt32()` and `WriteBoolean()`.', 'C', 'The `BinaryWriter` class elegantly relies on method overloading. It exposes dozens of variations of the `Write()` method (e.g., `Write(int)`, `Write(bool)`, `Write(string)`), allowing the compiler to automatically select the correct binary conversion based on the argument''s data type.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# BinaryWriter', 'How does the `BinaryWriter.Write(string)` method encode and format a string into the underlying binary stream?', 'It writes a null-terminator (0x00) at the end of the text bytes.', 'It prefixes the string with a 7-bit encoded integer (LEB128) representing the length of the string in bytes, followed by the text data.', 'It pads all strings to exactly 256 bytes.', 'It converts the string to a fixed-size char array.', 'B', 'To ensure the `BinaryReader` knows exactly how many bytes to read later, the `BinaryWriter` uses a length-prefixed format. It automatically calculates the byte length of the string and writes that length as a variable-length LEB128 integer immediately before writing the actual string characters.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# BinaryWriter', 'By default, what happens to the underlying stream (like a `FileStream`) when the `BinaryWriter` is disposed?', 'The underlying stream is left open and active.', 'The underlying stream is automatically closed and disposed alongside the writer, unless the `leaveOpen` flag was set to true during instantiation.', 'The underlying stream is reset to position 0.', 'The underlying stream is locked permanently in memory.', 'B', 'By default, the `BinaryWriter` takes full ownership of the stream it wraps. When the writer''s `Dispose()` method is called (usually at the end of a `using` block), it automatically flushes and closes the underlying stream. You can override this using the `leaveOpen` constructor parameter in modern .NET.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# BinaryWriter', 'If you have written data using a `BinaryWriter` but need to ensure it is immediately pushed to the underlying physical storage without closing the writer, which method should you call?', 'Close()', 'Push()', 'Commit()', 'Flush()', 'D', 'The `Flush()` method forces the `BinaryWriter` to immediately push any buffered data down into the underlying stream and clears its internal buffers. This ensures that data is actually written to the destination (like a hard drive) without destroying the writer object.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# BinaryWriter', 'If you need to overwrite a specific byte sequence previously written by the `BinaryWriter` (for example, updating a file header size), how do you change the writer''s position?', 'By calling `writer.Reset()`', 'By creating a new `BinaryWriter` instance.', 'By using the `writer.Seek()` method to move the pointer within the underlying stream.', 'It is impossible; `BinaryWriter` is strictly append-only.', 'C', 'The `BinaryWriter.Seek(int offset, SeekOrigin origin)` method allows you to move the write pointer to a specific byte location within the stream. This allows you to go back and overwrite specific binary data, such as a file header or a length placeholder.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# BinaryWriter', 'What is the default byte order (endianness) used by the `BinaryWriter` when converting multibyte primitive types (like an `int` or `double`) into binary?', 'Big-endian (most significant byte first)', 'Network byte order', 'Little-endian (least significant byte first)', 'Middle-endian', 'C', 'By default, `BinaryWriter` writes multibyte primitives in little-endian format, aligning perfectly with the standard architecture of Intel and AMD processors and its `BinaryReader` counterpart. If you need to interface with a big-endian system, you must manually reverse the bytes before writing.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# BinaryWriter', 'Which core .NET namespace must be imported to utilize the `BinaryWriter` class?', 'System.IO', 'System.Binary', 'System.Text', 'System.Data', 'A', 'The `BinaryWriter` class, exactly like the `BinaryReader` and the fundamental stream classes it wraps, resides entirely within the `System.IO` (Input/Output) namespace.');