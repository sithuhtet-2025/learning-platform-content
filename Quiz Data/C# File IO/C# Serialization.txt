INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Serialization', 'What is the fundamental definition of serialization in C#?', 'The process of converting an in-memory object into a stream of bytes, JSON text, or XML for storage or network transmission.', 'The process of extracting a C# object from a database row.', 'The process of converting a byte stream back into a strongly-typed C# object.', 'The process of encrypting an object''s properties in RAM.', 'A', 'Serialization is the process of translating data structures or object state into a format that can be stored (for example, in a file or memory buffer) or transmitted (across a network connection link) and reconstructed later.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Serialization', 'Which class is the modern, highly-optimized, built-in standard for serializing C# objects to JSON in .NET Core and .NET 5+?', 'System.Runtime.Serialization.Formatters.Binary.BinaryFormatter', 'Newtonsoft.Json.JsonConvert', 'System.Text.Json.JsonSerializer', 'System.Xml.Serialization.XmlSerializer', 'C', 'While Newtonsoft.Json was the historical third-party standard, Microsoft engineered `System.Text.Json.JsonSerializer` from the ground up to be the native, high-performance, low-allocation standard for modern .NET architecture.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Serialization', 'What is the official Microsoft architectural guidance regarding the use of the `BinaryFormatter` class for serialization?', 'It is the recommended approach for deep-cloning objects.', 'It is the fastest serializer and should be used for internal microservices.', 'It is inherently dangerous, officially deprecated, and strictly prohibited for untrusted data due to severe Remote Code Execution (RCE) vulnerabilities.', 'It should only be used to serialize Entity Framework models.', 'C', '`BinaryFormatter` is profoundly insecure. Because it includes type metadata in the payload and instantiates those types during deserialization, attackers can craft malicious payloads that execute arbitrary code on the server. It is effectively banned in modern .NET.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Serialization', 'If you have a `PasswordHash` property on your user model that you absolutely do NOT want included in the serialized JSON output, which attribute should you apply?', '[NonSerialized]', '[JsonIgnore]', '[Obsolete]', '[ExcludeFromSerialization]', 'B', 'The `[JsonIgnore]` attribute explicitly instructs the `System.Text.Json` serializer to completely skip the decorated property when generating the JSON payload, ensuring sensitive data or unnecessary navigation properties are excluded.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Serialization', 'If your C# property is named `FirstName`, but the external API requires the JSON key to be strictly formatted as `first_name`, which attribute must you apply?', '[DataMember(Name = "first_name")]', '[JsonPropertyName("first_name")]', '[JsonMap("first_name")]', '[XmlElement("first_name")]', 'B', 'The `[JsonPropertyName("...")]` attribute explicitly overrides the default property naming behavior, forcing the `System.Text.Json` serializer to output the exact string provided when writing the JSON key.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Serialization', 'By default, what happens if the `System.Text.Json.JsonSerializer` encounters a circular reference (e.g., a Parent object references a Child object, which references the Parent back) during serialization?', 'It silently ignores the circular reference and outputs null.', 'It serializes the objects infinitely until the application crashes with an OutOfMemoryException.', 'It detects the loop and immediately throws a `JsonException`.', 'It automatically assigns numeric IDs to map the references.', 'C', 'By default, `System.Text.Json` strictly prevents infinite loops by throwing a `JsonException` upon detecting a circular reference. Developers must explicitly configure `ReferenceHandler.Preserve` or `ReferenceHandler.IgnoreCycles` in the `JsonSerializerOptions` to handle them safely.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Serialization', 'When using the traditional `System.Xml.Serialization.XmlSerializer` to serialize an object to XML, what are the strict architectural limitations on the target class?', 'It serializes all private and protected fields automatically.', 'It requires a public, parameterless constructor and only serializes public fields and properties.', 'It cannot serialize arrays or collections.', 'It requires the class to implement the `ISerializable` interface.', 'B', 'The legacy `XmlSerializer` has strict constraints: the target class must have a default (parameterless) constructor so it can be reconstructed later, and it will only serialize explicitly public fields and properties, ignoring private state completely.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Serialization', 'By default, `System.Text.Json` only serializes public properties. If you explicitly need to serialize a private field or a property with a private setter, which attribute can you apply to it?', '[JsonInclude]', '[SerializePrivate]', '[DataMember]', '[Serializable]', 'A', 'Introduced in later versions of .NET, the `[JsonInclude]` attribute can be applied to non-public properties or even private fields to explicitly force the `System.Text.Json` serializer to include them in the JSON output, bypassing standard encapsulation boundaries for data transfer.');