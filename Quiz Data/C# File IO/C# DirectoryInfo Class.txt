INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# DirectoryInfo Class', 'What is the primary architectural difference between the `DirectoryInfo` class and the `Directory` class in the `System.IO` namespace?', '`DirectoryInfo` is for network drives, while `Directory` is for local drives.', '`DirectoryInfo` requires instantiation to represent a specific directory state, whereas `Directory` provides static methods requiring a path argument for every call.', '`DirectoryInfo` can only read data, while `Directory` can modify data.', 'There is no difference; they are aliases.', 'B', 'The `Directory` class is entirely static and ideal for single, quick operations. The `DirectoryInfo` class is instantiated for a specific path and is highly optimized when performing multiple operations on the same directory, as it caches security and state information.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# DirectoryInfo Class', 'Which of the following demonstrates the correct syntax to instantiate a `DirectoryInfo` object for a specific path?', 'DirectoryInfo dir = Directory.GetInfo(@"C:\Logs");', 'DirectoryInfo dir = new DirectoryInfo(@"C:\Logs");', 'DirectoryInfo dir = @"C:\Logs" as DirectoryInfo;', 'DirectoryInfo dir = File.OpenDirectory(@"C:\Logs");', 'B', 'Because `DirectoryInfo` represents an instance of a directory, it must be explicitly instantiated using the `new` keyword and its constructor, passing the target file path as a string.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# DirectoryInfo Class', 'Before attempting to read files from a directory, which `DirectoryInfo` property should you evaluate to guarantee the physical directory actually exists on the file system?', 'IsAvailable', 'HasContents', 'Exists', 'IsValid', 'C', 'The `Exists` property returns a boolean indicating whether the directory physically exists at the specified path. Checking this prevents `DirectoryNotFoundException` errors during runtime.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# DirectoryInfo Class', 'If you instantiate a `DirectoryInfo` object for a path that does not currently exist, which method do you call on that object to physically create the folder on the disk?', 'dir.MakeDirectory();', 'dir.Create();', 'dir.Build();', 'dir.Initialize();', 'B', 'The `Create()` method physically creates the directory on the file system. If the directory already exists, this method safely does nothing, making it highly robust for initialization logic.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# DirectoryInfo Class', 'When you call `dir.GetFiles()` on a `DirectoryInfo` object, what specific data type is returned?', 'An array of strings (`string[]`) containing the absolute file paths.', 'A generic `List<File>` object.', 'An array of `FileInfo` objects (`FileInfo[]`), providing deep access to each file''s metadata.', 'A single binary stream of all file contents.', 'C', 'Unlike the static `Directory.GetFiles()` method which simply returns an array of strings (paths), the `DirectoryInfo.GetFiles()` method returns an array of heavily detailed `FileInfo` objects, allowing immediate programmatic access to file sizes, extensions, and timestamps.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# DirectoryInfo Class', 'If a directory contains nested subdirectories and files, what is the required syntax to successfully delete the entire directory tree using a `DirectoryInfo` object?', 'dir.DeleteAll();', 'dir.Delete(recursive: true);', 'dir.Remove(force: true);', 'dir.Delete();', 'B', 'Calling `dir.Delete()` on a non-empty directory will immediately throw an `IOException`. To securely wipe a directory and all of its recursive contents (files and subfolders), you must explicitly pass `true` to the `Delete` method.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# DirectoryInfo Class', 'Which property of the `DirectoryInfo` class allows you to navigate upward in the file system hierarchy by returning the immediate parent directory?', 'dir.ParentDirectory', 'dir.Root', 'dir.Parent', 'dir.Up', 'C', 'The `Parent` property returns a new `DirectoryInfo` object representing the immediate parent folder of the current directory. If the current directory is a root volume (like "C:\"), this property returns null.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# DirectoryInfo Class', 'Which method should you use on a `DirectoryInfo` object to retrieve only the subdirectories that match a specific wildcard naming pattern (e.g., all folders starting with "Archive")?', 'dir.FilterDirectories("Archive*");', 'dir.GetDirectories("Archive*");', 'dir.Search("Archive*");', 'dir.FindFolders("Archive*");', 'B', 'The `GetDirectories(string searchPattern)` method acts as a highly optimized filter. It queries the file system directly to return an array of `DirectoryInfo` objects that strictly match the provided wildcard search pattern.');