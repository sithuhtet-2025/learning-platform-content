INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# StreamWriter', 'What is the primary architectural purpose of the `StreamWriter` class in C#?', 'To serialize complex C# objects into JSON format.', 'To write human-readable characters to a byte stream in a specific encoding, abstracting away the complex character-to-byte translation.', 'To write raw binary data (bytes) directly to a physical hard drive.', 'To compress and encrypt text files securely.', 'B', 'The `StreamWriter` is a high-level wrapper around a byte stream (like a `FileStream`). It takes standard C# strings and automatically encodes them into raw bytes using a specified text encoding format before passing them to the underlying stream.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# StreamWriter', 'What is the exact functional difference between the `Write()` and `WriteLine()` methods in the `StreamWriter` class?', '`Write()` only accepts primitive characters, while `WriteLine()` only accepts string objects.', '`Write()` appends text to the current line, whereas `WriteLine()` appends the text and immediately follows it with a line terminator.', '`WriteLine()` forces an immediate flush to the disk, while `Write()` does not.', 'There is no functional difference; they are interchangeable aliases.', 'B', '`WriteLine()` automatically appends the environment-specific line terminator string (defined by `Environment.NewLine`, usually `\r\n` on Windows) to the end of the provided text, ensuring the next output strictly begins on a new line.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# StreamWriter', 'How do you correctly instantiate a `StreamWriter` so that it adds new text to the absolute end of an existing file without wiping out its current contents?', 'new StreamWriter("log.txt", overwrite: false);', 'new StreamWriter("log.txt", append: true);', 'new StreamWriter("log.txt", FileMode.Append);', 'writer.SeekToEnd();', 'B', 'The `StreamWriter` constructor includes a boolean parameter specifically designed for appending. Passing `true` as the second argument (e.g., `new StreamWriter(path, true)`) instructs the writer to preserve existing data and safely start writing at the end of the file.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# StreamWriter', 'Because physical disk I/O operations are incredibly slow, `StreamWriter` temporarily holds your text in RAM. Which method forces the writer to immediately push this held text to the underlying physical stream?', 'Commit()', 'Push()', 'Flush()', 'Save()', 'C', '`StreamWriter` utilizes an internal memory buffer to drastically optimize performance. The `Flush()` method forcefully drains this internal buffer, transmitting all pending characters down to the underlying byte stream (and ultimately the physical disk).');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# StreamWriter', 'Which property can you explicitly set to `true` on a `StreamWriter` instance to ensure that every single write operation is immediately pushed to the underlying stream without manually calling the flush method?', 'ImmediateWrite', 'AutoFlush', 'SyncMode', 'BypassBuffer', 'B', 'Setting `writer.AutoFlush = true` forces the `StreamWriter` to call `Flush()` automatically after every single write operation. While this is much safer for preventing data loss during unexpected crashes, it severely degrades overall application performance for high-volume writing.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# StreamWriter', 'What critical architectural issue occurs if you fail to explicitly `Dispose()` or wrap a `StreamWriter` in a `using` block after finishing your write operations?', 'The target file is automatically deleted by the CLR.', 'The text encoding defaults back to ASCII, corrupting special characters.', 'Data sitting in the internal memory buffer may never be flushed to the disk (resulting in silent data loss), and the underlying OS file handle remains permanently locked.', 'The application immediately throws a `MemoryLeakException`.', 'C', 'Disposing a `StreamWriter` automatically calls `Flush()` to save any pending data, and then gracefully closes the underlying `FileStream`. Failing to dispose of it causes silent data loss and leaves the file locked by the OS ("File is in use by another process").');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# StreamWriter', 'If you instantiate a `StreamWriter` without explicitly specifying a text encoding format, which standard encoding does it use by default in modern .NET?', 'UTF-16', 'UTF-8 (without a Byte Order Mark)', 'ASCII', 'ANSI', 'B', 'By default, the `StreamWriter` class targets UTF-8 encoding. In modern .NET Core and .NET 5+, it specifically defaults to UTF-8 without a BOM (Byte Order Mark), which is the absolute standard for maximum cross-platform compatibility.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# StreamWriter', 'To prevent the UI thread from freezing when writing massive amounts of text to a log file, which modern `StreamWriter` method should you utilize?', 'WriteLineAsync()', 'BeginWrite()', 'WriteThreaded()', 'WriteInBackground()', 'A', 'Modern C# architecture heavily emphasizes asynchronous I/O. Using `await writer.WriteLineAsync(text)` offloads the blocking disk operation, freeing up the calling thread to handle other tasks (like rendering the UI) while the OS writes the data.');