INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# BinaryReader', 'What is the primary architectural purpose of the `BinaryReader` class in C#?', 'To read standard, human-readable UTF-8 text files line by line.', 'To securely decrypt binary files using AES encryption.', 'To read primitive data types (like int, bool, double, and strings) as raw binary values from an underlying stream.', 'To deserialize JSON or XML objects directly from a network socket.', 'C', 'The `BinaryReader` class provides a simplified interface for reading raw, strictly typed primitive data directly from an underlying stream (like a `FileStream` or `MemoryStream`) in binary format, rather than parsing plain text.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# BinaryReader', 'Which .NET namespace must be imported to utilize the `BinaryReader` class?', 'System.Data', 'System.Binary', 'System.Text', 'System.IO', 'D', 'The `BinaryReader` class, along with its counterpart `BinaryWriter` and all foundational stream classes, resides in the `System.IO` (Input/Output) namespace.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# BinaryReader', 'How does the `BinaryReader.ReadString()` method fundamentally know how many bytes to read to form the string?', 'It reads until it encounters a null-terminator (0x00) character.', 'It reads until it reaches the end of the entire file.', 'It expects the string to be prefixed with a 7-bit encoded integer (LEB128) that explicitly dictates the length of the string in bytes.', 'It assumes all strings are exactly 255 characters long.', 'C', 'When writing and reading strings, `BinaryWriter` and `BinaryReader` use a length-prefixed format. The string''s length is stored immediately before the text characters as a LEB128 variable-length integer, allowing the reader to allocate exactly the right amount of memory.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# BinaryReader', 'By default, what happens to the underlying stream (e.g., a `FileStream`) when you call `.Dispose()` or exit a `using` block on the `BinaryReader` that wraps it?', 'The underlying stream remains open and fully active.', 'The underlying stream is automatically closed and disposed alongside the reader, unless the `leaveOpen` parameter was set to true during instantiation.', 'The underlying stream is reset back to position 0.', 'The underlying stream is locked permanently in memory.', 'B', 'By default, a `BinaryReader` takes ownership of the stream it wraps. Disposing the reader cascades the disposal to the underlying stream. In modern .NET, you can pass `leaveOpen: true` in the constructor to prevent this behavior if you need to keep the stream alive for other operations.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# BinaryReader', 'If you need to read a 32-bit signed integer from a binary file, which specific method of the `BinaryReader` class must you call?', 'ReadInteger()', 'ReadNumber()', 'ReadInt32()', 'ReadBinaryInt()', 'C', '`BinaryReader` exposes explicitly typed methods for all primitives to ensure exact byte-length reading. `ReadInt32()` reads exactly 4 bytes from the stream and converts them into an `int`.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# BinaryReader', 'Which property of the `BinaryReader` class grants you direct access to the underlying stream so you can manually check its current position or length?', 'UnderlyingStream', 'SourceStream', 'InnerStream', 'BaseStream', 'D', 'The `BaseStream` property exposes the underlying `Stream` object (like the `FileStream`). This is heavily utilized to check `reader.BaseStream.Position < reader.BaseStream.Length` to safely loop through a file without throwing exceptions.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# BinaryReader', 'What is the default byte order (endianness) used by the `BinaryReader` class when reading primitive types like integers or doubles from a stream?', 'Little-endian (least significant byte first)', 'Big-endian (most significant byte first)', 'Network byte order', 'Middle-endian', 'A', 'The `BinaryReader` explicitly expects binary data to be in little-endian format by default, which matches the architecture of standard Intel/AMD processors. If you need to read big-endian data, you must manually reverse the bytes after reading them.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# BinaryReader', 'What specific exception is thrown by primitive read methods (like `ReadInt64()`) if they attempt to read bytes but reach the end of the file before retrieving enough data?', 'System.IndexOutOfRangeException', 'System.IO.EndOfStreamException', 'System.NullReferenceException', 'System.OverflowException', 'B', 'If a method expects to read a specific number of bytes (e.g., 8 bytes for an Int64) but the underlying stream runs out of data before that requirement is met, the CLR immediately throws an `EndOfStreamException`.');