INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Deserialization', 'What is the fundamental definition of deserialization in C#?', 'It is the process of extracting methods and metadata from a compiled DLL.', 'It is the process of converting a stream of bytes, JSON text, or XML back into an active, strongly-typed, in-memory C# object.', 'It is the process of converting a C# object into a byte stream or text format for storage and transmission.', 'It is the process of explicitly destroying an object to free managed memory.', 'B', 'Deserialization is the reverse of serialization. It takes formatted data (like a JSON payload from an API or bytes from a file) and reconstructs the corresponding C# object in memory so the application can interact with it programmatically.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Deserialization', 'Which class is the modern, highly-optimized, and built-in standard for deserializing JSON data in .NET Core and .NET 5+?', 'Newtonsoft.Json.JsonConvert', 'System.Runtime.Serialization.Formatters.Binary.BinaryFormatter', 'System.Text.Json.JsonSerializer', 'System.Xml.Serialization.XmlSerializer', 'C', 'While Newtonsoft.Json (Json.NET) was the historical third-party standard, Microsoft introduced `System.Text.Json.JsonSerializer` as the native, ultra-high-performance, and secure standard for JSON serialization and deserialization in modern .NET architecture.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Deserialization', 'What is the official Microsoft architectural guidance regarding the use of `BinaryFormatter` for deserialization in modern .NET applications?', 'It should be used exclusively for highly secure network payloads.', 'It is the recommended approach for deep-cloning objects in memory.', 'It is inherently dangerous, officially deprecated, and strictly prohibited for untrusted data because it can lead to Remote Code Execution (RCE) attacks.', 'It is the fastest deserializer available and should replace JSON in web APIs.', 'C', '`BinaryFormatter` is profoundly insecure. It allows the payload to dictate which object types are instantiated, allowing an attacker to craft a malicious byte stream that instantiates dangerous framework classes, leading directly to Remote Code Execution. It is effectively banned in modern .NET.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Deserialization', 'By default, how does the `System.Text.Json.JsonSerializer` handle property name matching when deserializing a JSON payload into a C# object?', 'It strictly matches names in a case-sensitive manner.', 'It is entirely case-insensitive by default.', 'It automatically converts all JSON keys to lowercase before matching.', 'It ignores property names completely and maps strictly by the order of fields in the JSON.', 'A', 'For maximum performance, the modern `System.Text.Json` deserializer is strictly case-sensitive by default (e.g., "Name" will not match "name"). You must explicitly configure `PropertyNameCaseInsensitive = true` in the `JsonSerializerOptions` if you need case-insensitive matching.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Deserialization', 'If an incoming JSON payload contains a property named "first_name", but your C# class property is named `FirstName`, which attribute must you apply to the C# property to correctly map it during deserialization with `System.Text.Json`?', '[JsonMap("first_name")]', '[JsonPropertyName("first_name")]', '[DataMember(Name = "first_name")]', '[XmlElement("first_name")]', 'B', 'The `[JsonPropertyName("...")]` attribute explicitly tells the `System.Text.Json` deserializer to map a specific JSON key to the decorated C# property, bridging the gap between JSON snake_case conventions and C# PascalCase conventions.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Deserialization', 'By default, what happens if the `System.Text.Json` deserializer encounters a JSON property in the payload that does NOT have a corresponding matching property on the target C# class?', 'It throws a `JsonException` immediately, halting deserialization.', 'It automatically creates a new dynamic property on the C# object.', 'It safely ignores the unrecognized JSON property and continues deserializing the rest of the recognized data.', 'It returns a completely null object.', 'C', 'By default, `System.Text.Json` is resilient to extra data. It simply ignores JSON properties that do not map to the target C# class. This behavior can be strictly overridden by setting `JsonSerializerOptions.UnmappedMemberHandling` to throw an exception if strict payload validation is required.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Deserialization', 'When using the traditional `XmlSerializer` class to deserialize an XML document into a C# object, what strict structural requirement is imposed on the target C# class?', 'The class must implement the `ICloneable` interface.', 'The class must have a public, parameterless (default) constructor.', 'All properties in the class must be marked as `readonly`.', 'The class must inherit from `System.Xml.XmlDocument`.', 'B', 'The legacy `XmlSerializer` architecture requires the target class to possess a public, parameterless constructor. The deserializer instantiates the object using this default constructor first, and then sequentially sets the properties based on the XML nodes.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Deserialization', 'If your C# class is designed with an immutable architecture and requires a parameterized constructor to instantiate, which attribute can you apply to the constructor to allow `System.Text.Json` to successfully deserialize data into it?', '[JsonConstructor]', '[DeserializeHere]', '[Serializable]', '[DataContract]', 'A', 'Modern C# encourages immutable data structures (like Records). By decorating a specific constructor with the `[JsonConstructor]` attribute, you instruct the `System.Text.Json` deserializer to pass the parsed JSON values directly into that constructor''s parameters to instantiate the object.');