INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Properties', 'Which of the following best describes a Property in C#?', 'A public data field that stores information directly.', 'A method that strictly returns a boolean value.', 'A member that provides a flexible mechanism to read, write, or compute the value of a private field.', 'A specialized constructor used for initializing classes.', 'C', 'Properties act as smart fields. They look like data fields to the outside world but internally use special methods called accessors (get and set) to protect and manage the underlying data.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Properties', 'What is the correct syntax for declaring an auto-implemented property in C#?', 'public int Age;', 'public int Age { get(); set(); }', 'public int Age { get; set; }', 'public Property<int> Age;', 'C', 'Auto-implemented properties (e.g., public int Age { get; set; }) allow you to quickly define a property without manually declaring a private backing field. The C# compiler creates a hidden backing field automatically.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Properties', 'If you want to allow external classes to read a property but only allow the declaring class to modify it, how should you declare it?', 'public string Name { get; protected set; }', 'public string Name { get; private set; }', 'public string Name { readonly get; set; }', 'private string Name { get; set; }', 'B', 'By applying the ''private'' access modifier to the set accessor, the property becomes read-only to external consumers, while methods inside the defining class can still mutate its internal value.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Properties', 'Where is data validation logic typically implemented when using a full property (one with a manually defined backing field)?', 'Inside the class constructor.', 'Inside the get accessor.', 'Inside the set accessor before assigning the ''value'' keyword to the backing field.', 'Properties cannot contain logic; a separate method must be used.', 'C', 'The set accessor provides a perfect encapsulation boundary to validate incoming data (represented by the implicit ''value'' parameter) before it is assigned to the private backing field, preventing an invalid state.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Properties', 'Which syntax correctly represents a read-only expression-bodied property in modern C#?', 'public string FullName => FirstName + " " + LastName;', 'public string FullName() => FirstName + " " + LastName;', 'public string FullName { get => return FirstName; }', 'public string FullName -> FirstName + LastName;', 'A', 'Expression-bodied members allow concise syntax for read-only properties using the lambda arrow (=>) directly after the property name, returning the evaluated expression without needing a ''get'' block or ''return'' keyword.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Properties', 'Introduced in C# 9.0, what is the purpose of the ''init'' accessor in a property?', 'It automatically initializes the property to its default data type value.', 'It ensures the property is only accessible from the class constructor.', 'It allows the property to be assigned only during object initialization (e.g., via an object initializer), making it immutable thereafter.', 'It replaces the ''get'' accessor for asynchronous property reads.', 'C', 'The ''init'' accessor is a variant of the ''set'' accessor that restricts mutation. It allows clients to set the property''s value during the object''s creation phase using an object initializer, but strictly enforces immutability once initialization is complete.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Properties', 'What happens if you define an auto-implemented property with only a ''get'' accessor and no ''set'' or ''init'' accessor?', 'A compile-time error occurs.', 'The property is completely read-only from the outside, and can only be assigned a value in the class constructor or inline declaration.', 'The compiler automatically generates a public ''set'' accessor.', 'The property throws a runtime exception if read before being initialized.', 'B', 'A getter-only auto-property represents strict immutability. Its backing field is implicitly declared as ''readonly'' by the compiler, meaning it can only be assigned a value inline where it is declared, or inside the class constructor.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Properties', 'Can C# interfaces define properties, and if so, how?', 'No, interfaces can only define methods and events.', 'Yes, by defining the property signature including the ''get'' and/or ''set'' accessors without any implementation body.', 'Yes, but they must provide a default backing field.', 'Yes, but they can only define read-only properties.', 'B', 'Interfaces can declare properties. The interface simply specifies whether the property requires a get accessor, a set accessor, or both, forcing the implementing class to provide the actual logic or auto-property implementation.');