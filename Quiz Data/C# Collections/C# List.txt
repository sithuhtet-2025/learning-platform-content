INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# List', 'How is the System.Collections.Generic.List<T> class internally implemented in C#?', 'As a doubly linked list.', 'As a dynamically resizing single-dimensional array.', 'As a hash table mapping indices to values.', 'As a fixed-size contiguous memory block that throws an exception when full.', 'B', 'List<T> uses an internal array to store elements. When its Count exceeds its Capacity, it automatically allocates a new, larger array (usually double the size) and copies the existing elements over.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# List', 'What is the time complexity (Big O notation) of retrieving an element by its index (e.g., `myList[5]`) in a List<T>?', 'O(1)', 'O(log n)', 'O(n)', 'O(n^2)', 'A', 'Because List<T> is backed by a contiguous array in memory, accessing an element via its zero-based index involves a direct mathematical memory address calculation, making it an extremely fast O(1) operation.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# List', 'If you instantiate a new list using `List<int> numbers = new List<int>(50);` and immediately call `numbers.Add(5);`, what are the values of `numbers.Count` and `numbers.Capacity` respectively?', 'Count = 1, Capacity = 1', 'Count = 50, Capacity = 50', 'Count = 1, Capacity = 50', 'Count = 0, Capacity = 50', 'C', 'The constructor List<int>(50) pre-allocates the internal array to a Capacity of 50, avoiding reallocation penalties until the 51st item is added. The Count property strictly reflects the actual number of elements inserted, which is 1.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# List', 'What happens internally when you use `myList.Insert(0, item)` to insert an element at the beginning of a List<T> that currently contains 10,000 items?', 'The new item is placed in a new memory node and linked to the previous first item.', 'The internal array is immediately doubled in size to accommodate the new item.', 'All 10,000 existing elements are shifted one position to the right in memory to make room for the new element at index 0.', 'An InvalidOperationException is thrown unless the list was explicitly initialized with extra capacity at index 0.', 'C', 'Inserting or removing elements anywhere other than the end of a List<T> is an O(n) operation. The underlying array must physically shift all subsequent elements to maintain the correct sequential index order.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# List', 'Which method should you use to find and return ALL elements in a List<Employee> that match a specific condition (e.g., Salary > 50000)?', 'Find()', 'FindAll()', 'Contains()', 'BinarySearch()', 'B', 'The FindAll() method takes a Predicate<T> delegate and iterates through the collection, returning a completely new List<T> containing every element that evaluates to true. The Find() method only returns the first matching element.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# List', 'What is the most efficient and exception-safe way to remove all items from a List<T> that meet a specific condition, without throwing a ''Collection was modified'' exception?', 'Iterate forward using a standard foreach loop and call list.Remove().', 'Iterate forward using a standard for loop and call list.RemoveAt().', 'Use the list.RemoveAll(Predicate<T>) method.', 'Copy the list to an array, delete the items, and create a new list.', 'C', 'The RemoveAll() method is highly optimized for this exact scenario. It efficiently removes all matching elements in a single pass without throwing enumeration exceptions or suffering from the O(n^2) performance degradation of repeated, manual Remove() calls.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# List', 'If you have a List<string> with a Capacity of 1,000,000, but you have removed elements until the Count is only 10, which method can you call to release the massive unused memory block back to the system?', 'GC.Collect()', 'myList.Clear()', 'myList.TrimExcess()', 'myList.Compact()', 'C', 'The TrimExcess() method evaluates the list''s current Count. If the capacity is significantly larger than the count, it allocates a new, smaller internal array perfectly sized for the current elements, allowing the garbage collector to reclaim the old, large array.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# List', 'When converting a List<int> to an array using the `myList.ToArray()` method, what exactly occurs in memory?', 'The list is safely cast to an array without any new memory allocation.', 'A new array is allocated and all elements from the list are copied into it.', 'A read-only Span<int> is returned pointing to the list''s internal array.', 'The list is destroyed and replaced by the array in memory.', 'B', 'The ToArray() method allocates a brand-new array object of the exact appropriate size and performs a shallow copy of all elements from the List<T> into this new array. This is an O(n) operation.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# List', 'If you call `myList.Sort()` on a List<CustomObject>, what is absolutely required for the method to successfully sort the list without throwing an InvalidOperationException at runtime?', 'The list must contain at least 2 elements.', 'The CustomObject class must implement the IComparable<T> interface, or an IComparer<T> must be provided as an argument to the Sort method.', 'The CustomObject class must override the Equals() and GetHashCode() methods.', 'The list must be implicitly cast to an array first.', 'B', 'The default, parameterless Sort() method relies on the list elements natively implementing IComparable<T> to determine their relative ordering (greater than, less than, or equal). Without this interface or a custom IComparer, the CLR doesn''t know how to sort custom types.');