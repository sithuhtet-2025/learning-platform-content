INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# HashSet', 'Which of the following best describes the primary characteristic of the System.Collections.Generic.HashSet<T> class in C#?', 'It allows duplicate elements and maintains insertion order.', 'It is an unordered collection that strictly contains unique elements.', 'It is a sorted collection backed by a binary search tree.', 'It stores elements as key-value pairs.', 'B', 'A HashSet<T> is an unordered collection of unique elements backed by a hash table. It is designed for high-performance set operations and automatically prevents duplicate values from being stored.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# HashSet', 'What is the return type and behavior of the HashSet<T>.Add(T item) method when attempting to add an item that already exists in the set?', 'It returns void and silently overwrites the existing item.', 'It returns void and throws an ArgumentException.', 'It returns bool (false) and leaves the set unchanged.', 'It returns an int representing the index of the duplicate item.', 'C', 'Unlike the Add method of a Dictionary (which throws an exception for duplicate keys) or a List (which allows duplicates), HashSet<T>.Add safely returns false if the item is already present, without modifying the collection or throwing an error.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# HashSet', 'Which method modifies the current HashSet<T> so that it contains all elements that are present in either the current set, a specified collection, or both?', 'IntersectWith()', 'ExceptWith()', 'SymmetricExceptWith()', 'UnionWith()', 'D', 'The UnionWith(IEnumerable<T>) method performs a mathematical union operation. It modifies the current HashSet to include all elements from itself and the specified collection, automatically discarding any resulting duplicates.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# HashSet', 'If you want to determine whether all elements of a HashSet<T> are also present in another collection, which method should you use?', 'IsSubsetOf()', 'IsSupersetOf()', 'Overlaps()', 'SetEquals()', 'A', 'The IsSubsetOf(IEnumerable<T>) method returns a boolean indicating whether the current HashSet is a strict subset of (i.e., all its elements are completely contained within) the specified collection.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# HashSet', 'What is the average time complexity (Big O notation) for the Contains, Add, and Remove operations in a standard HashSet<T>?', 'O(1)', 'O(log n)', 'O(n)', 'O(n log n)', 'A', 'Because HashSet<T> is implemented using a hash table, the average time complexity for basic operations like Add, Remove, and Contains is an extremely fast O(1), assuming a good hash function with minimal collisions.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# HashSet', 'When using a custom class as the type parameter T in a HashSet<T>, what must the custom class correctly override to ensure the set can accurately identify and reject duplicate objects?', 'ToString() and Clone()', 'CompareTo() and GetType()', 'GetHashCode() and Equals()', 'Dispose() and Finalize()', 'C', 'A hash-based collection relies first on GetHashCode() to find the correct memory bucket, and then uses Equals() to verify actual object equality if multiple items share the same bucket (a collision). Both must be properly overridden for custom types to enforce set uniqueness.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# HashSet', 'What happens if you iterate through a standard HashSet<T> using a foreach loop?', 'The elements are guaranteed to be returned in alphabetical or numeric sorted order.', 'The elements are guaranteed to be returned in the exact chronological order they were added.', 'The elements are returned in an undefined order that may change as items are added or removed.', 'A compile-time error occurs because HashSet<T> does not implement IEnumerable<T>.', 'C', 'HashSet<T> makes absolutely no guarantees regarding the enumeration order of its elements. The internal hash buckets dictate memory placement, which appears random and changes dynamically as the collection resizes and rehashes.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# HashSet', 'Which method should you use to modify a HashSet<T> so that it only retains elements that are NOT present in a specified second collection?', 'RemoveAll()', 'ExceptWith()', 'IntersectWith()', 'SymmetricExceptWith()', 'B', 'The ExceptWith(IEnumerable<T>) method performs a mathematical set difference. It iterates through the specified collection and removes any matching elements from the current HashSet.');