INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Collections', 'What is the primary performance advantage of using the generic `System.Collections.Generic.List<T>` over the older non-generic `System.Collections.ArrayList` for value types like `int`?', 'It avoids the performance overhead of boxing and unboxing.', 'It is automatically thread-safe and lock-free.', 'It allocates memory exclusively on the stack instead of the managed heap.', 'It uses a linked list internally, making insertions faster.', 'A', '`ArrayList` stores elements as `object`, requiring value types (like `int`) to be boxed when added and unboxed when retrieved, which degrades performance. `List<T>` is strongly typed, eliminating boxing completely.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Collections', 'When retrieving a value from a `Dictionary<TKey, TValue>`, what happens if you attempt to access a key that does not exist using the indexer (e.g., `var val = myDict["MissingKey"];`)?', 'It returns `null` or the default value for `TValue`.', 'It throws a `KeyNotFoundException`.', 'It automatically adds the key to the dictionary with a default value.', 'It throws an `IndexOutOfRangeException`.', 'B', 'Using the indexer to access a non-existent key in a `Dictionary<TKey, TValue>` throws a `KeyNotFoundException`. To safely check for and retrieve a value without exceptions, the `TryGetValue` method should be used.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Collections', 'In a `List<T>`, what is the precise difference between the `Count` property and the `Capacity` property?', '`Count` is the maximum number of items the list can hold; `Capacity` is the current number of items.', 'They are synonymous and always return the exact same integer value.', '`Count` is the actual number of elements currently in the list; `Capacity` is the total number of elements the internal array can hold without resizing.', '`Count` includes `null` elements; `Capacity` only counts non-null, valid elements.', 'C', '`Capacity` represents the size of the underlying internal array allocated in memory. `Count` represents the actual number of elements currently added to the list. When `Count` equals `Capacity` and a new item is added, the `List<T>` automatically resizes its internal array, typically doubling it.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Collections', 'Which collection type is fundamentally designed to store a collection of unique elements and provides highly optimized mathematical set operations like Union, Intersection, and Difference?', '`LinkedList<T>`', '`List<T>`', '`HashSet<T>`', '`ObservableCollection<T>`', 'C', '`HashSet<T>` is an unordered collection of unique elements backed by a hash table. It provides O(1) performance for lookups and additions, and natively includes mathematical set operations like `UnionWith` and `IntersectWith`.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Collections', 'If you need to process items strictly in a Last-In, First-Out (LIFO) order, which collection class should you use, and which method adds an item to it?', '`Queue<T>`, using the `Enqueue()` method.', '`Stack<T>`, using the `Push()` method.', '`List<T>`, using the `Add()` method.', '`LinkedList<T>`, using the `AddLast()` method.', 'B', 'A `Stack<T>` operates on a Last-In, First-Out (LIFO) principle. You add items to the top of the stack using the `Push()` method and remove/read them from the top using the `Pop()` or `Peek()` methods.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Collections', 'Which interface serves as the foundational base interface for all collections in C# and is strictly required for an object to support iteration using the `foreach` statement?', '`ICollection`', '`IList`', '`IEnumerable`', '`IQueryable`', 'C', '`IEnumerable` (and its generic counterpart `IEnumerable<T>`) exposes an enumerator that provides simple iteration over a collection. The C# compiler translates a `foreach` loop into calls to `GetEnumerator()` and `MoveNext()`.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Collections', 'When implementing a custom iterator block in C# that returns an `IEnumerable<T>`, what keyword is used to return each element one at a time while automatically preserving the state of the iteration?', '`yield return`', '`return next`', '`continue`', '`await return`', 'A', 'The `yield return` statement is used in an iterator method to provide the next value in the sequence. It pauses the method''s execution, preserves its local state variables, and resumes from that exact point upon the next iteration request.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Collections', 'Which of the following generic dictionary implementations uses a tree-based internal structure to maintain its keys in a sorted order, resulting in O(log n) retrieval time?', '`Dictionary<TKey, TValue>`', '`SortedList<TKey, TValue>`', '`SortedDictionary<TKey, TValue>`', '`ConcurrentDictionary<TKey, TValue>`', 'C', '`SortedDictionary<TKey, TValue>` is implemented as a binary search tree (typically a red-black tree), which keeps the keys strictly sorted but has slower O(log n) insertion and retrieval times compared to the O(1) time of a hash-based `Dictionary<TKey, TValue>`.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Collections', 'What is the main structural characteristic of the `System.Collections.Generic.LinkedList<T>` class in C#?', 'It is a singly linked list where each node only points to the next node.', 'It is a doubly linked list where each node contains references to both the previous and next nodes.', 'It is a dynamic array that automatically resizes itself when capacity is reached.', 'It is a thread-safe wrapper around a standard `List<T>`.', 'B', '`LinkedList<T>` in C# is a general-purpose doubly linked list. Each `LinkedListNode<T>` object contains a `Value` property, a `Next` reference, and a `Previous` reference, allowing fast O(1) insertions and deletions at both ends or around known nodes.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Collections', 'Which of the following syntax examples demonstrates a valid and standard Collection Initializer in C# for a `List<int>`?', '`List<int> numbers = new List<int>(1, 2, 3);`', '`List<int> numbers = new List<int> { 1, 2, 3 };`', '`List<int> numbers = { 1, 2, 3 };`', '`List<int> numbers = new List<int>[1, 2, 3];`', 'B', 'A collection initializer uses curly braces `{ }` immediately after the constructor call (e.g., `new List<int> { 1, 2, 3 }`). Behind the scenes, the C# compiler automatically translates this block into consecutive calls to the collection''s `Add` method.');