INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Call by Reference', 'Which C# keyword is used to pass an argument by reference, strictly requiring that the variable be initialized before it is passed to the method?', 'out', 'in', 'ref', 'pass', 'C', 'The ''ref'' keyword passes a variable by reference. It explicitly requires that the variable be initialized before being passed into the method, allowing the method to both read and modify its existing value.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Call by Reference', 'What is the primary purpose of the ''out'' parameter modifier in C#?', 'To pass an initialized variable by reference so it can be read.', 'To pass a variable by reference with the strict requirement that the called method must assign a value to it before returning.', 'To prevent a method from modifying the passed variable.', 'To allow returning multiple values only from asynchronous methods.', 'B', 'The ''out'' keyword is used when a method needs to return multiple values or initialize a variable. The calling code does not need to initialize the variable, but the called method is strictly forced by the compiler to assign a value to it before exiting.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Call by Reference', 'Introduced in C# 7.2, what does the ''in'' parameter modifier achieve?', 'It passes an argument by reference but makes it strictly read-only inside the called method.', 'It passes an argument by value to ensure thread safety.', 'It allows an inner function to access an outer function''s variables.', 'It forces the variable to be allocated on the heap instead of the stack.', 'A', 'The ''in'' modifier passes arguments by reference (avoiding the performance overhead of copying large value types like structs) but enforces immutability, preventing the called method from modifying the original variable.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Call by Reference', 'What happens if you pass a reference type (like a class instance) to a method by value (without using ref or out)?', 'A completely cloned copy of the object is created in memory.', 'You cannot modify the object''s properties or fields from within the method.', 'A copy of the reference is passed; modifying the object''s internal properties alters the original, but reassigning the parameter to a completely new object does not affect the original variable.', 'A compiler error occurs because reference types must always be passed with the ''ref'' keyword.', 'C', 'When a reference type is passed by value, a copy of the pointer (reference) is passed. You can modify the data the pointer points to (the object''s properties), but if you assign the parameter to a `new` object, the original caller''s pointer remains unchanged.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Call by Reference', 'Which of the following is true regarding the syntax when calling a method that takes a ''ref'' parameter?', 'The ''ref'' keyword is only required in the method signature, not in the method call.', 'The ''ref'' keyword must be explicitly used in both the method signature and the method call.', 'The ''ref'' keyword is optional in the method call if the variable is already initialized.', 'The compiler automatically infers the ''ref'' keyword based on the method signature.', 'B', 'C# design requires explicit intent. To prevent accidental state mutation, the ''ref'' keyword must be explicitly written in both the method declaration and every time the method is invoked by the caller.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Call by Reference', 'Starting in C# 7.0, how can you declare an ''out'' variable seamlessly at the exact point it is passed to a method?', 'int.TryParse("123", out int result);', 'int.TryParse("123", int result out);', 'var result = int.TryParse("123", out);', 'int.TryParse("123", result = new out int());', 'A', 'C# 7.0 introduced inline ''out'' variable declarations. Instead of declaring the variable on a separate line before the method call, you can declare it directly inside the argument list (e.g., `out int result`), making the code cleaner and more concise.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Call by Reference', 'Are you permitted to use the ''ref'', ''out'', or ''in'' keywords on parameters of methods marked with the ''async'' modifier?', 'Yes, but only for value types (structs).', 'Yes, they work exactly the same as in synchronous methods.', 'No, compiler errors will occur because asynchronous methods cannot have by-reference parameters.', 'Yes, but you must use the ''await ref'' syntax.', 'C', 'Asynchronous methods (using the `async` keyword) and iterator blocks (using `yield return`) are compiled into state machines by the C# compiler. Because local variables may be hoisted onto the heap to survive across awaits, passing parameters by reference (which strictly point to stack memory locations) is not permitted and results in a compiler error.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Call by Reference', 'If a method signature is `public void UpdateData(ref int number)`, which of the following is a valid way to call it?', 'UpdateData(ref 5);', 'const int x = 10; UpdateData(ref x);', 'int x; UpdateData(ref x);', 'int x = 10; UpdateData(ref x);', 'D', 'You cannot pass a literal value (like 5) or a constant to a ''ref'' parameter because they cannot be modified. Additionally, the variable must be initialized before being passed. Therefore, declaring `int x = 10;` and passing `ref x` is the only valid option.');