INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Namespaces', 'What is the primary architectural purpose of utilizing namespaces in a C# application?', 'To improve the runtime execution speed of the compiled application.', 'To logically organize code and prevent naming collisions between types with the same name.', 'To enforce strict memory management and garbage collection rules.', 'To encrypt the source code against reverse engineering.', 'B', 'Namespaces are designed to organize large code projects logically and to create boundaries that prevent name conflicts (e.g., distinguishing between ''System.Windows.Point'' and ''MyGraphics.Point'').');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Namespaces', 'Introduced in C# 10, which syntax correctly declares a file-scoped namespace, applying the namespace to all types in the file without requiring curly braces?', 'namespace MyProject.Core;', 'using namespace MyProject.Core;', 'global namespace MyProject.Core;', '#pragma namespace MyProject.Core', 'A', 'C# 10 introduced file-scoped namespaces (e.g., ''namespace MyProject.Core;''). This eliminates a level of indentation by applying the namespace to all types declared subsequently in that specific file.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Namespaces', 'Which of the following is the correct, concise syntax for declaring a nested namespace in C#?', 'namespace Outer::Inner { }', 'namespace Outer -> Inner { }', 'namespace Outer.Inner { }', 'namespace Outer { inner Inner { } }', 'C', 'C# uses dot notation to define nested namespaces directly (e.g., ''namespace Outer.Inner { }''), which is equivalent to nesting the ''Inner'' namespace block physically inside the ''Outer'' namespace block.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Namespaces', 'What does the ''using static'' directive allow a developer to do in a C# file?', 'It makes the entire namespace statically allocated in memory.', 'It allows the developer to access static members of a specific class directly without qualifying them with the class name.', 'It prevents the classes in the namespace from being instantiated.', 'It imports all namespaces dynamically at runtime.', 'B', 'The ''using static'' directive (e.g., ''using static System.Console;'') imports the static members of a single class, allowing you to call methods like ''WriteLine()'' directly instead of ''Console.WriteLine()''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Namespaces', 'If two imported namespaces both contain a class named ''Widget'', how can you resolve the ambiguity without typing the fully qualified name every time?', 'Use the ''override'' keyword on the namespace.', 'Create a namespace alias directive (e.g., ''using UIWidget = ProjectA.UI.Widget;'').', 'Use the ''shadows'' keyword in the class declaration.', 'It is impossible; one of the namespaces must be removed.', 'B', 'A namespace alias directive allows you to assign an identifier to a namespace or a specific type, resolving naming collisions cleanly and keeping the code concise.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Namespaces', 'Introduced in C# 10, how can you apply a ''using'' directive so that it automatically applies to every single C# file in your entire project?', 'Prefix the directive with the ''global'' keyword (e.g., ''global using System.Linq;'').', 'Place the directive inside the ''Program.cs'' main method.', 'Prefix the directive with the ''public'' keyword.', 'Add it to the appsettings.json file.', 'A', 'The ''global using'' directive tells the compiler to apply the namespace import to the entire compilation unit (project), drastically reducing boilerplate ''using'' statements at the top of every file.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Namespaces', 'What is the purpose of the ''global::'' contextual keyword alias in C# (e.g., ''global::System.Console'')?', 'To declare a variable that is globally accessible across the network.', 'To force the compiler to search for the type starting from the absolute root namespace, bypassing any local namespace shadowing.', 'To import all namespaces available in the .NET framework at once.', 'To mark a class as an entry point for the application.', 'B', 'If you accidentally create a local namespace or class that shares a name with a system namespace (like ''System''), the ''global::'' alias explicitly forces the compiler to resolve the type from the root global namespace.');