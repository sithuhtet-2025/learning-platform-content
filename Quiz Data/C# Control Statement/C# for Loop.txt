INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# for Loop', 'What are the three distinct, comma-separated sections that make up the signature of a standard C# `for` loop?', 'Declaration, Execution, Termination', 'Initializer, Condition, Iterator', 'Variable, Boolean, Increment', 'Start, Stop, Step', 'B', 'A standard `for` loop signature consists of three parts separated by semicolons: the Initializer (executed once at the start), the Condition (evaluated before every iteration), and the Iterator (executed after every iteration).');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# for Loop', 'In a standard `for` loop, at what exact point during the execution cycle does the iterator statement (e.g., `i++`) run?', 'At the very beginning of the loop, before the condition is evaluated.', 'Simultaneously with the condition evaluation.', 'Immediately after the loop body finishes executing, right before the condition is checked for the next iteration.', 'It executes only when the loop terminates.', 'C', 'The execution flow of a `for` loop is strictly ordered: Initializer -> Condition -> Loop Body -> Iterator. The iterator modifies the loop variable only after the body has completed its run for that specific cycle.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# for Loop', 'If you declare an iterator variable directly inside the `for` loop signature (e.g., `for (int i = 0; i < 10; i++)`), what is the scope of that variable?', 'It is globally accessible throughout the entire class.', 'It is accessible anywhere within the method that contains the loop.', 'It is strictly scoped to the `for` loop itself and ceases to exist once the loop terminates.', 'It remains in memory until the Garbage Collector is manually called.', 'C', 'Variables declared inside the initializer section of a `for` loop are block-scoped to the loop. Attempting to access `i` outside the closing curly brace of the loop will result in a compile-time "does not exist in the current context" error.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# for Loop', 'Which of the following is perfectly valid C# syntax for creating an intentional, infinite `for` loop?', 'for (infinite)', 'for (true)', 'for (;;)', 'for (int i = 0; ; )', 'C', 'All three sections (initializer, condition, and iterator) of a `for` loop are completely optional. By omitting all three but keeping the two mandatory semicolons `for (;;) `, you create a valid infinite loop equivalent to `while (true)`.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# for Loop', 'Is it possible to declare and modify multiple variables simultaneously within the signature of a single `for` loop?', 'No, a `for` loop can strictly only manage one iterator variable.', 'Yes, but only if they are of different data types.', 'Yes, by separating the declarations and iterations with commas (e.g., `for (int i = 0, j = 10; i < j; i++, j--)`).', 'Yes, but it requires nested `for` loops.', 'C', 'C# allows you to declare multiple variables of the same type in the initializer, and execute multiple statements in the iterator section, simply by separating them with commas. This is highly useful for algorithms converging from two ends of an array.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# for Loop', 'If a `continue` statement is triggered inside the body of a `for` loop, where does the execution control immediately jump?', 'Directly to the condition evaluation, skipping the iterator.', 'To the very beginning of the loop, re-running the initializer.', 'Straight out of the loop, identical to a `break` statement.', 'Directly to the loop''s iterator statement (e.g., `i++`), and then to the condition evaluation.', 'D', 'When `continue` is called inside a `for` loop, it skips the rest of the body but meticulously ensures the iterator block is executed. This prevents the infinite loop traps commonly associated with using `continue` improperly in a `while` loop.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# for Loop', 'From an architectural perspective, when is a `for` loop generally preferred over a `while` loop?', 'When iterating over data streams of unknown length.', 'When the exact number of required iterations is known in advance or easily computable (e.g., iterating through an array by its length).', 'When waiting for a user input event.', 'When the loop needs to guarantee at least one execution pass.', 'B', 'The `for` loop is the standard structure for deterministic iteration—meaning the developer knows exactly how many times the loop should run based on a collection size or a mathematical counter.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# for Loop', 'What happens if you leave the condition section of a `for` loop completely blank (e.g., `for (int i = 0; ; i++)`)?', 'The loop skips execution entirely.', 'The compiler throws a syntax error.', 'The C# compiler implicitly treats the blank condition as `false`.', 'The C# compiler implicitly treats the blank condition as `true`, resulting in an infinite loop unless explicitly broken.', 'D', 'If the condition expression is omitted in a `for` loop, the compiler defaults it to `true`. The loop will run infinitely until a `break`, `return`, or exception disrupts the execution flow.');