INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Do-While Loop', 'What is the defining architectural characteristic of a `do-while` loop compared to a standard `while` loop in C#?', 'It requires an explicit counter variable to be declared in its signature.', 'It is a pre-test loop; it evaluates its condition before executing the loop body.', 'It is a post-test loop; it evaluates its condition after executing the loop body.', 'It cannot be terminated using a `break` statement.', 'C', 'A standard `while` loop evaluates its condition at the top (pre-test). A `do-while` loop evaluates its condition at the bottom (post-test), fundamentally changing how and when the loop decides to repeat.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Do-While Loop', 'Because of its post-test architecture, what is the absolute execution guarantee of a `do-while` loop?', 'It will execute infinitely unless a `break` statement is encountered.', 'It is guaranteed to execute the code inside its block at least one time, even if the `while` condition is initially false.', 'It will skip execution entirely if the initial condition is false.', 'It will execute exactly as many times as specified by its iterator.', 'B', 'Because the condition check is located at the very end of the loop structure, the CLR must execute the `do` block first before it can even evaluate whether a second iteration is necessary. This guarantees at least one execution pass.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Do-While Loop', 'Which of the following represents the strictly correct syntax for a `do-while` loop in C#?', 'do { ... } while (condition)', 'do { ... } while (condition);', 'do (condition) { ... } while;', 'while (condition) do { ... };', 'B', 'Unlike a standard `while` loop, the `while (condition)` clause at the end of a `do-while` loop is considered the termination of a complete statement in C#. Therefore, it must strictly end with a semicolon (;).');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Do-While Loop', 'Regarding variable scoping, what happens if you declare an integer variable `int x = 0;` directly inside the `do { }` block and try to evaluate it in the `while(x < 5);` condition?', 'The loop executes normally.', 'The compiler automatically moves the variable declaration outside the loop.', 'A compile-time error occurs because the variable `x` is out of scope when the `while` condition is evaluated.', 'A runtime NullReferenceException is thrown.', 'C', 'Variables declared inside the curly braces `{}` of the `do` block are strictly block-scoped. The `while` condition sits outside those braces. To evaluate a variable in the condition, it must be declared before the `do` keyword.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Do-While Loop', 'In what practical software engineering scenario is a `do-while` loop typically preferred over a standard `for` or `while` loop?', 'When iterating over a dynamically sizing `List<T>`.', 'When executing a complex mathematical algorithm that requires exactly 100 iterations.', 'When prompting a user for console input or displaying a menu, where the action must occur at least once before validating the input to determine if it should repeat.', 'When querying a database for a specific record.', 'C', 'Because the `do-while` loop guarantees at least one execution, it is the perfect architectural pattern for interactive menus and input validation. You must show the menu/prompt first (`do`), and then check if the user''s input was invalid (`while`) to prompt them again.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Do-While Loop', 'If a `continue` statement is executed inside the body of a `do-while` loop, where does the control flow immediately jump?', 'To the very top of the `do` block, completely skipping the `while` condition.', 'Directly to the `while` condition evaluation at the bottom of the loop to determine if the next iteration should occur.', 'To the first line of code following the entire loop structure.', 'It throws a runtime exception because `continue` is invalid in a `do-while` loop.', 'B', 'Just like a standard `while` loop, the `continue` statement skips the rest of the current iteration''s code body and jumps straight to the loop''s conditional expression to evaluate whether it should loop again.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Do-While Loop', 'What happens if you forget to include the semicolon (`;`) at the very end of the `while (condition)` statement in a `do-while` loop?', 'The compiler automatically inserts it during the build process.', 'The loop becomes an infinite loop.', 'The C# compiler throws a syntax error (CS1002: ; expected).', 'The loop executes only once and terminates.', 'C', 'The semicolon is a strict syntactic requirement for the `do-while` structure in C#. Forgetting it breaks the statement termination rules, causing an immediate compilation failure.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Do-While Loop', 'How does the `break` statement behave when executed inside a `do-while` loop?', 'It restarts the loop from the beginning.', 'It immediately terminates the entire loop, bypassing the `while` condition check, and transfers control to the next line of code outside the loop.', 'It pauses the loop until a user input is received.', 'It acts identically to the `continue` statement.', 'B', 'The `break` statement acts as a hard exit. When encountered inside the `do` block, it instantly destroys the loop''s execution cycle, skipping any remaining code in the block and ignoring the `while` condition evaluation completely.');