INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# While Loop', 'What is the defining architectural characteristic of a `while` loop in C#?', 'It executes a block of code a specific, mathematically pre-determined number of times.', 'It is a pre-test loop that repeatedly executes a block of code as long as a specified boolean condition evaluates to true.', 'It executes a block of code at least once before checking any conditions.', 'It iterates specifically over collections or arrays.', 'B', 'The `while` loop is a fundamental pre-test iteration structure. It evaluates a boolean condition before entering the loop body, repeating the execution continuously as long as that condition remains true.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# While Loop', 'Because the `while` loop is a pre-test structure, what is its minimum guaranteed number of executions?', 'Zero. If the condition evaluates to false on the very first check, the loop body is entirely skipped.', 'One. It must execute the body to determine if it should loop again.', 'It depends on the variables initialized outside the loop.', 'Infinite, unless a break statement is explicitly provided.', 'A', 'Since the condition is evaluated before the loop is ever entered, if the initial evaluation yields false, the Common Language Runtime (CLR) completely bypasses the loop block, resulting in zero executions.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# While Loop', 'From an architectural perspective, when is a `while` loop generally preferred over a standard `for` loop?', 'When iterating sequentially through a strongly typed array.', 'When the exact number of iterations is completely unknown in advance and depends on a dynamic runtime condition (e.g., reading lines from a network stream until it closes).', 'When the loop needs to guarantee at least one execution pass.', 'When you need to declare multiple iterator variables inline.', 'B', 'A `while` loop is the standard pattern for non-deterministic iteration. It is used when the termination of the loop relies on an external event or changing state that cannot be calculated mathematically beforehand.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# While Loop', 'Which of the following is the strictly correct C# syntax for declaring an intentional, infinite `while` loop?', 'while (infinite) { ... }', 'while (1) { ... }', 'while (true) { ... }', 'while (;;) { ... }', 'C', 'Because C# requires conditions to evaluate strictly to a boolean type (`bool`), `while (true)` is the correct and standard syntax for creating an infinite loop. Using `1` like in C++ will result in a compile-time error.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# While Loop', 'What is a highly common architectural risk (anti-pattern) when manually managing the state of a `while` loop?', 'Declaring variables outside the loop body.', 'Failing to update the variables involved in the condition check from inside the loop body, resulting in a locked, unintentional infinite loop.', 'Using a `break` statement to exit the loop early.', 'Calling a database operation inside the loop.', 'B', 'Unlike a `for` loop which has a built-in iterator block, a `while` loop relies entirely on the developer to manually mutate the state within the loop body. If the state never changes to make the condition false, the loop runs infinitely, locking the thread.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# While Loop', 'If a `continue` statement is executed inside the body of a `while` loop, where does the execution control immediately jump?', 'It jumps completely out of the loop.', 'It jumps to the first line of the loop body, bypassing the condition check.', 'It jumps directly back to the boolean condition evaluation at the top of the loop to determine if the next iteration should proceed.', 'It throws a runtime exception because `continue` is invalid in a `while` loop.', 'C', 'The `continue` statement halts the current iteration, skipping any remaining code in the block, and transfers control straight back up to the condition evaluation. If the state wasn''t updated before the `continue` call, this instantly causes an infinite loop trap.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# While Loop', 'What happens if you declare a variable (e.g., `int temp = 0;`) strictly inside the curly braces of a `while` loop body?', 'It is globally accessible across the entire class.', 'It retains its value across all iterations of the loop.', 'It is strictly block-scoped; it is logically created and destroyed during every single iteration and cannot be accessed outside the loop.', 'The compiler throws an error preventing variable declarations inside loops.', 'C', 'Variables declared inside the loop body are restricted to that block scope. They cannot be referenced by code outside the loop, nor do they persist their value from one iteration to the next (unless captured by a closure).');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# While Loop', 'How does the `break` statement function when executed inside a `while` loop?', 'It immediately terminates the entire loop, bypassing any remaining code in the current iteration and skipping the condition check entirely, transferring control to the code directly following the loop.', 'It pauses the loop until a background thread finishes execution.', 'It skips the current iteration and evaluates the condition again.', 'It acts exactly like the `return` statement, exiting the entire method.', 'A', 'The `break` statement acts as an emergency exit. It completely destroys the loop''s execution cycle unconditionally, regardless of whether the `while` condition is still true, and moves the program flow forward to the next structural block.');