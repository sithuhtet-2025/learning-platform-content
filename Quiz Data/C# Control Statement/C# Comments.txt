INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Comments', 'What syntax is used to create a standard single-line comment in C#?', '<!-- comment -->', '# comment', '// comment', '-- comment', 'C', 'In C#, two forward slashes (//) instruct the compiler to ignore all subsequent text on that specific line. This is the standard syntax for single-line comments.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Comments', 'Which syntax allows you to create a comment that spans across multiple lines of C# code?', '/* comment */', '<!-- comment -->', '~~ comment ~~', '## comment ##', 'A', 'Multi-line comments (often called block comments) begin with /* and end with */. The compiler ignores everything between these two delimiters, allowing the comment to span multiple lines or be placed inline within a statement.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Comments', 'Which specific comment syntax is parsed by the C# compiler to automatically generate XML documentation files and populate IDE IntelliSense tooltips?', '/// (Triple forward slash)', '/** (Slash double-asterisk)', '<!-- (HTML comment)', '#region (Region directive)', 'A', 'Triple forward slashes (///) denote an XML documentation comment in C#. When placed directly above a class, method, or property, the IDE uses these comments to generate structured documentation and helpful IntelliSense popups for developers.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Comments', 'What physically happens to standard C# comments (// and /* */) when the source code is compiled into Intermediate Language (IL)?', 'They are encrypted and stored in the DLL metadata.', 'They are completely stripped out and ignored by the compiler, having zero impact on the final executable size or performance.', 'They are converted into runtime strings for debugging.', 'They are moved to a separate .pdb file.', 'B', 'Comments are strictly for human readers. During the compilation process, the C# compiler completely discards standard comments. They do not exist in the compiled Intermediate Language (IL) and have absolutely no impact on application performance or binary size.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Comments', 'Within an XML documentation comment (///), which standard XML tag is used to write the primary description of a method or class?', '<description>', '<info>', '<summary>', '<details>', 'C', 'The <summary> tag is the foundational element of an XML documentation comment. It provides a brief, high-level overview of the type or member, which is immediately displayed in IntelliSense when a developer hovers over the code.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Comments', 'What happens if you attempt to nest a multi-line comment inside another multi-line comment in C# (e.g., `/* Outer /* Inner */ Outer */`)?', 'It compiles perfectly and ignores both layers.', 'The compiler throws a syntax error because the first `*/` it encounters closes the entire block, leaving the trailing `Outer */` as uncompilable syntax.', 'The inner comment is ignored, but the outer comment is preserved.', 'It creates a regional code block.', 'B', 'C# does not support nested multi-line comments. The compiler strictly looks for the very first `*/` sequence to terminate the block comment. Any text following that first `*/` will be treated as executable code, usually resulting in a syntax error.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Comments', 'Which standard keyword prefix is heavily recognized by Visual Studio and other C# IDEs inside a comment to automatically populate the "Task List" window?', 'FIX:', 'BUG:', 'TODO:', 'LATER:', 'C', 'Writing a comment like `// TODO: Refactor this method` triggers built-in IDE features. Visual Studio automatically scans for the "TODO" token and populates the Task List window, providing developers with a centralized dashboard of pending work items.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Comments', 'According to modern Clean Code architectural principles, what is the most appropriate use of comments in a professional C# codebase?', 'To explain exactly "what" every line of code is doing.', 'To leave old, unused code commented out just in case it is needed later.', 'To explain the "why" behind complex business logic or architectural decisions when the code itself cannot be made self-documenting.', 'To add the author''s name and date to every method.', 'C', 'Clean Code dictates that code should be as self-documenting as possible through expressive variable and method names. Comments should be reserved for explaining "why" a specific, perhaps non-obvious, approach was taken, rather than stating "what" the code is doing.');