INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# If-else Statement', 'What is the primary architectural purpose of the `if-else` statement in C#?', 'To loop through a collection of items sequentially.', 'To evaluate a boolean expression and branch the program''s execution flow based on whether the result is true or false.', 'To catch and handle runtime exceptions gracefully.', 'To execute a block of code asynchronously on a separate thread.', 'B', 'The `if-else` statement is a fundamental control flow structure. It evaluates a given condition and directs the execution down one of two exclusive paths based on a strict true/false outcome.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# If-else Statement', 'Unlike C++ or JavaScript, what is strictly required regarding the condition expression inside a C# `if` statement?', 'It must be wrapped in square brackets instead of parentheses.', 'It can be any numeric value, where 0 represents false and anything else is true.', 'It must evaluate strictly to a boolean (`bool`) data type.', 'It must be a direct comparison between two variables of the exact same type.', 'C', 'C# is a strongly typed language. The condition evaluated by an `if` statement must mathematically yield a `bool` (true or false). It does not support "truthy" or "falsy" implicit conversions from integers or objects like some other languages do.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# If-else Statement', 'If you need to evaluate multiple, mutually exclusive conditions sequentially, which structure should you use to chain the checks together?', 'else if', 'then if', 'elseif', 'switch if', 'A', 'The `else if` construct allows you to chain multiple conditional checks. The compiler evaluates them strictly top-to-bottom and executes only the block associated with the very first condition that evaluates to true, skipping all subsequent checks.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# If-else Statement', 'What happens if you intentionally omit the curly braces `{}` after an `if` statement''s condition (e.g., `if (x > 10) DoSomething();`)?', 'The code will not compile due to a syntax error.', 'The `if` condition will apply only to the single statement immediately following it.', 'The `if` condition will apply to all subsequent code until an `else` keyword is found.', 'The statement defaults to an infinite loop.', 'B', 'While curly braces are technically optional if the execution block contains only a single statement, omitting them applies the condition strictly to the next line. Clean Code principles highly recommend always using braces to prevent dangerous bugs if new lines of code are later added to the block.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# If-else Statement', 'Introduced in modern C# (7.0+), what powerful feature allows you to combine type checking and variable assignment directly within an `if` condition (e.g., `if (obj is string text)`)?', 'Type Coercion', 'Pattern Matching', 'Implicit Casting', 'Reflection Binding', 'B', 'Pattern matching with the `is` keyword allows you to safely check if an object is compatible with a specific type and, if true, immediately cast it and assign it to a new, block-scoped local variable in one fluid, highly expressive statement.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# If-else Statement', 'When evaluating a complex condition like `if (user != null && user.IsActive)`, why does the C# runtime NOT throw a NullReferenceException when the `user` object happens to be null?', 'The compiler automatically catches the exception inside the `if` block.', 'The conditional logical AND operator (`&&`) performs short-circuit evaluation.', 'The `if` statement automatically ignores null objects during runtime.', 'The `&&` operator evaluates the right side of the expression first.', 'B', 'The `&&` (AND) and `||` (OR) operators perform critical short-circuit evaluation. If `user != null` evaluates to false, the overall `&&` condition can never mathematically be true, so the CLR completely skips evaluating the right side (`user.IsActive`), preventing the crash.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# If-else Statement', 'If you declare a new variable entirely inside an `if` block (e.g., `if (true) { int x = 5; }`), where can that variable be accessed?', 'Strictly only within the curly braces of that specific `if` block.', 'Within the `if` block and any attached `else if` or `else` blocks.', 'Anywhere within the enclosing method scope.', 'Globally across the entire class.', 'A', 'Variables declared inside the curly braces of an `if`, `else if`, or `else` block are strictly block-scoped. They cease to exist in memory the exact moment the execution flow exits those specific braces.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# If-else Statement', 'Which operator serves as a highly concise, inline architectural alternative to a simple `if-else` statement for assigning values based on a single condition?', 'The null-coalescing operator (??)', 'The lambda arrow operator (=>)', 'The ternary conditional operator (?:)', 'The spaceship operator (<=>)', 'C', 'The ternary operator (`condition ? true_value : false_value`) allows developers to write simple conditional assignments on a single line, drastically reducing boilerplate code and visual noise compared to a full multi-line `if-else` block.');