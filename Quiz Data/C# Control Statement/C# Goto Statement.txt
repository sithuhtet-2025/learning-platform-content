INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Goto Statement', 'What is the primary function of the `goto` statement in C#?', 'To immediately terminate the application.', 'To transfer program execution control unconditionally to a labeled statement.', 'To pause execution until a specific thread completes.', 'To call a method recursively.', 'B', 'The `goto` statement performs an unconditional jump, immediately transferring the execution flow to a specific, labeled line of code elsewhere within the same method scope.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Goto Statement', 'What is the correct C# syntax for defining a target label that a `goto` statement can jump to?', '#TargetLabel', '@TargetLabel;', 'TargetLabel:', '[TargetLabel]', 'C', 'A target label is defined by a standard identifier followed immediately by a colon (e.g., `StartLoop:`). The `goto` statement then references this identifier without the colon (e.g., `goto StartLoop;`).');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Goto Statement', 'How is the `goto` keyword uniquely utilized within a C# `switch` statement?', 'It is used to exit the switch block completely without executing further cases.', 'It is used to explicitly jump from one `case` block to another `case` block or the `default` block.', 'It is used to re-evaluate the switch expression dynamically.', 'It is illegal to use `goto` inside a switch statement.', 'B', 'Because C# strictly forbids implicit fall-through between populated `case` blocks, developers must use explicit jump statements. `goto case <value>;` or `goto default;` can be used to intentionally transfer execution flow from the end of one case directly into another.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Goto Statement', 'Which of the following execution jumps is strictly prohibited by the C# compiler when using the `goto` statement?', 'Jumping out of a deeply nested loop to a label outside the loop.', 'Jumping backward to a label executed earlier in the method.', 'Jumping from outside a code block (like a `for` loop or `if` statement) to a label located deep inside that block.', 'Jumping from a `catch` block to a label outside the `try-catch` structure.', 'C', 'The C# compiler explicitly forbids using `goto` to jump *into* a nested code block from the outside. Doing so would bypass the initialization of local variables and loop condition checks, destabilizing the execution state and causing a compile-time error.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Goto Statement', 'What is the strict compiler rule regarding the `goto` statement and `finally` blocks?', '`goto` can only jump into a `finally` block, not out of it.', '`goto` can only jump out of a `finally` block, not into it.', '`goto` is strictly prohibited from jumping either into or out of a `finally` block.', '`goto` works normally with `finally` blocks without any restrictions.', 'C', 'Because the `finally` block has absolute execution guarantees for critical resource cleanup, the C# compiler explicitly prevents any jump statements (`goto`, `break`, `continue`, `return`) from transferring control out of it. It also forbids jumping into it from the outside.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Goto Statement', 'Why is the widespread use of the `goto` statement generally considered a severe architectural anti-pattern in modern C# development?', 'It significantly slows down compilation times.', 'It consumes excessive heap memory.', 'It bypasses the garbage collector.', 'It creates convoluted, unpredictable execution flows commonly referred to as "spaghetti code", making the application incredibly difficult to read and maintain.', 'D', 'The `goto` statement breaks the logical, top-down structured flow of code provided by standard loops and conditionals. Overusing it makes tracing the application state and debugging nearly impossible, directly violating modern Clean Code principles.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Goto Statement', 'Despite its reputation, in which specific architectural scenario is the `goto` statement still considered an acceptable, and sometimes preferred, solution in C#?', 'Handling standard NullReferenceExceptions.', 'Breaking out of multiple, deeply nested loops simultaneously.', 'Replacing the `return` statement in methods for faster execution.', 'Instantiating objects dynamically.', 'B', 'Because C# does not support "labeled breaks" (unlike Java), using a `goto` statement to jump to a label immediately following a massive, multi-layered nested loop structure is often much cleaner and more efficient than maintaining and checking complex boolean escape flags at every loop level.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Goto Statement', 'Can a `goto` statement located in one method jump directly to a label defined in a completely different method?', 'Yes, if both methods belong to the exact same class.', 'Yes, if the target method is marked as `static`.', 'Yes, if the target label is marked as `public`.', 'No, the target label must be strictly within the exact same method scope as the `goto` statement.', 'D', 'The `goto` statement is strictly intra-method. It cannot under any circumstances be used to jump across method boundaries. If you need to transfer control to another method, you must use standard method invocation calls or delegates.');