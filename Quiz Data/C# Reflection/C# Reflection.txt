INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Reflection', 'Which of the following best describes the primary purpose of Reflection in C#?', 'To improve the execution speed of loops at runtime.', 'To inspect metadata, discover types, and dynamically invoke code at runtime.', 'To mirror a database schema into C# classes automatically.', 'To secure the source code from decompilation.', 'B', 'Reflection provides objects that encapsulate assemblies, modules, and types. It allows you to dynamically create instances, bind types, and invoke methods at runtime by inspecting their metadata.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Reflection', 'Which C# namespace must be imported to access the core classes required for Reflection (e.g., MethodInfo, PropertyInfo)?', 'System.Diagnostics', 'System.Runtime', 'System.Reflection', 'System.Metadata', 'C', 'The System.Reflection namespace contains classes and interfaces that provide a managed view of loaded types, methods, and fields, which are essential for reflective programming.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Reflection', 'What is the primary abstract base class in the .NET framework used to represent type declarations (classes, interfaces, arrays) during Reflection?', 'System.Object', 'System.Class', 'System.Type', 'System.Reflection.TypeInfo', 'C', 'System.Type is the root of all reflection operations. It represents type declarations for class types, interface types, array types, value types, and enumeration types, providing access to their underlying metadata.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Reflection', 'If you have a class named ''Customer'' but NO instantiated object of it, how can you obtain its System.Type object?', 'Customer.GetType()', 'typeof(Customer)', 'Reflection.GetType("Customer")', 'new Customer().Type', 'B', 'The typeof() operator is used to obtain the System.Type object for a known, hardcoded type at compile time. The GetType() method, inherited from System.Object, can only be called on an instantiated object at runtime.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Reflection', 'Which class and method are commonly used to dynamically create an instance of a type at runtime using Reflection?', 'Type.CreateNew()', 'Reflection.Instantiate()', 'Activator.CreateInstance()', 'ObjectGenerator.Create()', 'C', 'Activator.CreateInstance() is heavily used in Reflection to create an instance of a specified type dynamically at runtime, using the type''s default or parameterized constructor.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Reflection', 'Once you have obtained a ''MethodInfo'' object representing a specific method, how do you dynamically execute that method?', 'Call MethodInfo.Execute()', 'Call MethodInfo.Run()', 'Call MethodInfo.Invoke()', 'Call MethodInfo.Start()', 'C', 'The MethodInfo.Invoke() method is used to execute the dynamically discovered method. You pass the object instance to invoke it on (or null for static methods) along with an array of required parameters.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Reflection', 'What is a significant architectural drawback or risk of using Reflection extensively in a C# application?', 'It prevents the application from being compiled into a DLL.', 'It enforces strict access modifiers, crashing if private fields exist.', 'It incurs a significant performance overhead and can bypass compile-time type safety and encapsulation.', 'It consumes excessive hard drive space during compilation.', 'C', 'Reflection operations (like dynamic invocation and type discovery) are significantly slower than direct code execution. Furthermore, Reflection can be used to access private members, intentionally breaking encapsulation and bypassing compiler type safety checks at runtime.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Reflection', 'How can you retrieve all the public properties of a specific class using Reflection?', 'typeof(MyClass).GetProperties()', 'typeof(MyClass).FindProperties(BindingFlags.Public)', 'Reflection.GetClassProperties("MyClass")', 'typeof(MyClass).Attributes.Properties', 'A', 'The Type.GetProperties() method returns an array of PropertyInfo objects representing all the public properties of the current Type. You can also pass BindingFlags to specify exactly which properties (e.g., non-public or static) to retrieve.');