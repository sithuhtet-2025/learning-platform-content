INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Method Overloading', 'What is the fundamental definition of method overloading in C#?', 'Creating methods with different names but the same parameters.', 'Defining multiple methods within the same class that share the same name but have different parameter lists.', 'Overriding a base class method in a derived class.', 'Passing more arguments to a method than it was designed to handle.', 'B', 'Method overloading allows a class to have multiple methods with the exact same name, provided their parameter lists (signatures) are distinct.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Method Overloading', 'Which of the following is explicitly EXCLUDED from a method''s signature when the C# compiler resolves overloaded methods?', 'The number of parameters.', 'The data types of the parameters.', 'The return type of the method.', 'The order of the parameters.', 'C', 'In C#, a method''s return type is not part of its signature for overloading purposes. You cannot overload two methods if they only differ by what they return; they must differ by their input parameters.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Method Overloading', 'What specific type of polymorphism does method overloading represent in object-oriented programming?', 'Runtime Polymorphism', 'Dynamic Polymorphism', 'Compile-time (Static) Polymorphism', 'Interface Polymorphism', 'C', 'Method overloading is resolved by the C# compiler at compile-time based on the arguments passed, making it a form of static or compile-time polymorphism. Overriding (using virtual/override) represents runtime polymorphism.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Method Overloading', 'How does the C# compiler handle overloading two methods where the ONLY difference in their signature is that one uses a ''ref'' parameter and the other uses an ''out'' parameter?', 'It compiles successfully and calls the appropriate method at runtime.', 'It compiles successfully but always defaults to the ''ref'' version.', 'It throws a compile-time error because the CLR considers ''ref'' and ''out'' to be identical for signature matching.', 'It automatically converts the ''out'' parameter to a ''ref'' parameter.', 'C', 'While you can overload a method based on by-value vs. by-reference (e.g., Print(int x) and Print(ref int x)), you cannot overload purely between ''ref'' and ''out''. The Common Language Runtime (CLR) treats them identically when differentiating method signatures.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Method Overloading', 'If a class contains overloaded methods `Calculate(double x)` and `Calculate(int x)`, what happens when you call `Calculate(5)`?', 'The compiler throws an ambiguous invocation error.', '`Calculate(double x)` is called because double is more precise.', '`Calculate(int x)` is called because it is an exact type match.', 'Both methods are executed sequentially.', 'C', 'The C# compiler always chooses the most specific, exact match first. Since 5 is an integer literal, `Calculate(int x)` is the perfect match and will be executed over the implicit conversion to double.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Method Overloading', 'What occurs if the C# compiler encounters a method call that matches multiple overloaded methods through implicit conversions, but none of them is definitively "better" than the others?', 'It arbitrarily picks the first one defined in the source file.', 'It defaults to the method with the fewest parameters.', 'It throws a compile-time error for "ambiguous invocation".', 'It throws a runtime exception.', 'C', 'If the compiler cannot determine a single, clearly superior match among the overloads after applying implicit type conversion rules, it halts compilation and throws an ambiguous invocation error to prevent unpredictable behavior.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Method Overloading', 'Can you overload a C# method based purely on the names of its parameters (e.g., `void Save(int id)` vs `void Save(int index)`)?', 'Yes, provided you call them using named arguments.', 'No, parameter names are entirely ignored when evaluating method signatures for overloading.', 'Yes, but only if they are in different namespaces.', 'Yes, but the compiler will issue a warning.', 'B', 'Method overloading relies strictly on the number, types, and order of parameters. The actual names given to the parameters have no impact on the method signature, so differing only by parameter name results in a duplicate definition error.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Method Overloading', 'How does the ''params'' keyword interact with overload resolution if a class has `Log(string message)` and `Log(params string[] messages)`?', 'A call to `Log("Error")` will execute `Log(params string[] messages)`.', 'A call to `Log("Error")` will execute `Log(string message)`.', 'The compiler will block the inclusion of both methods.', 'A call to `Log("Error")` will execute both methods.', 'B', 'When resolving overloads, the C# compiler always prefers a method with standard parameters over a method utilizing the ''params'' array if both are applicable matches for the provided arguments.');