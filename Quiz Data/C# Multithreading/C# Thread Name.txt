INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Thread Name', 'What is the primary architectural benefit of explicitly assigning a custom string to the `Thread.Name` property in C#?', 'It allows the operating system to allocate more CPU priority to the thread.', 'It vastly improves debugging and diagnostics by allowing developers to easily identify specific worker threads in IDEs (like Visual Studio) or log files, rather than relying on arbitrary, rotating numeric Thread IDs.', 'It prevents the thread from being forcefully aborted.', 'It automatically groups threads with the same name into a shared memory pool.', 'B', 'When building complex multithreaded applications, relying on arbitrary numeric Managed Thread IDs makes debugging a nightmare. Assigning a clear, human-readable `Name` (e.g., "DatabasePoller") allows developers to instantly identify the thread''s purpose in the debugger''s Threads window.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Thread Name', 'What critical restriction does the .NET Common Language Runtime (CLR) rigidly enforce regarding the `Thread.Name` property?', 'It must be exactly 8 characters long.', 'It can only be set before the `Start()` method is called.', 'It is strictly a write-once property. Once a name is assigned to a thread, attempting to modify or overwrite it later will instantly throw an `InvalidOperationException`.', 'It cannot contain spaces or special characters.', 'C', 'The `Name` property is designed for permanent identification. The CLR strictly enforces a write-once rule. If you attempt to assign a string to `thread.Name` when it is no longer null, the runtime will violently reject the operation with an `InvalidOperationException`.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Thread Name', 'If a developer instantiates a new `Thread` object but does not explicitly assign a value to its `Name` property, what is its default value?', 'An empty string `""`', 'A generic name like `"Thread1"`', '`null`', 'The numeric `ManagedThreadId` converted to a string.', 'C', 'By default, the `Name` property of a newly instantiated managed thread evaluates to `null`. It will remain `null` for its entire lifecycle unless explicitly assigned a string value by the developer.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Thread Name', 'How can a developer safely assign a custom diagnostic name to the primary UI or Main execution thread of an application?', 'By passing a string parameter to the `static void Main(string[] args)` entry point.', 'By using `Thread.CurrentThread.Name = "MainUIThread";` early in the application''s startup sequence, provided it hasn''t already been named by the underlying framework.', 'The Main thread is permanently locked and cannot be named.', 'By modifying the application''s `app.config` file.', 'B', 'The Main Thread is just a standard managed thread. You can capture it using `Thread.CurrentThread` and assign its `Name` property just like any other thread, which is a highly recommended practice for clean log files.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Thread Name', 'From a software architecture perspective, why is it considered a severe anti-pattern to manually assign a `Name` to a thread that was retrieved from the `ThreadPool` (or via the Task Parallel Library)?', 'ThreadPool threads do not have a `Name` property.', 'Naming a ThreadPool thread causes a memory leak.', 'Because ThreadPool threads are constantly recycled and reused for entirely different tasks. Since `Name` is strictly write-once, naming a pool thread permanently brands it, causing massive diagnostic confusion when it is reused for a different operation later.', 'It forces the ThreadPool to permanently retire the thread.', 'C', 'The `ThreadPool` reuses execution threads to save resources. If you name a pool thread "DownloadWorker", and it finishes downloading and is recycled to execute "SaveToDatabase", it will still show up in the debugger and logs as "DownloadWorker", destroying your diagnostic integrity.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Thread Name', 'Inside a deeply nested, independent helper method executed by a background worker, how can you programmatically retrieve the name of the thread currently executing that specific line of code for a log entry?', '`System.Diagnostics.Process.GetCurrentProcess().ThreadName`', '`Thread.CurrentThread.Name`', '`this.Thread.Name`', '`Environment.ThreadName`', 'B', 'The static `Thread.CurrentThread` property retrieves the object reference for the exact thread actively executing the current code block, allowing you to seamlessly pull its `Name` property for localized logging contexts.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Thread Name', 'At what exact point in a thread''s lifecycle can you safely assign a value to its `Name` property without the CLR throwing an exception?', 'Strictly when the thread is in the `Unstarted` state.', 'Strictly when the thread is in the `Running` state.', 'You can assign it at any time during its lifecycle (Unstarted, Running, or even Stopped), provided that the `Name` property has never been previously assigned a value.', 'Only inside the thread''s executing method itself.', 'C', 'The CLR does not restrict *when* you name the thread based on its `ThreadState`. The only strict structural limitation is that the assignment must be the very first time the property is set for that specific object instance.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Thread Name', 'Does setting the managed `Thread.Name` property in C# also rename the physical, unmanaged operating system thread visible in external performance monitors (like Windows Task Manager or Linux top)?', 'Yes, it has always directly renamed the OS-level thread since .NET 1.0.', 'No, the managed `Name` property is purely for internal .NET debugging tools (like Visual Studio). However, modern .NET (Core/5+) actively attempts to forward this name to the underlying OS if the host platform supports it natively.', 'No, the OS thread name is strictly read-only and managed by the CPU.', 'Yes, but it requires the application to run with Administrator privileges.', 'B', 'Historically in the legacy .NET Framework, the `Thread.Name` was purely a managed CLR construct invisible to the OS. Modern .NET architecture closes this gap, making cross-platform system-level diagnostics significantly more powerful by forwarding the name to the native OS thread where supported.');