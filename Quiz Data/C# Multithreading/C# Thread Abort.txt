INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Thread Abort', 'What was the historical primary function of the `Thread.Abort()` method in the early .NET framework?', 'To temporarily pause a thread until data became available.', 'To gracefully ask a thread to stop working at its next convenience.', 'To forcefully and asynchronously terminate a thread''s execution immediately from another thread.', 'To reset a thread''s internal memory state.', 'C', 'Historically, `Thread.Abort()` was used to forcefully terminate a thread from the outside. It was an aggressive, asynchronous command that stopped the thread wherever it was currently executing in its instruction set.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Thread Abort', 'How does modern .NET architecture (.NET Core, .NET 5 and later) handle calls to `Thread.Abort()`?', 'It executes normally but logs a severe warning to the Windows Event Viewer.', 'It strictly throws a `PlatformNotSupportedException` because forced thread abortion is officially deprecated and completely unsupported.', 'It converts the forced abort into a safe, background cancellation request.', 'It prompts the user for administrator privileges before aborting.', 'B', 'Because forced thread abortion causes catastrophic state corruption, Microsoft completely removed support for it in modern .NET. Calling `Thread.Abort()` in .NET 5+ will instantly throw a `PlatformNotSupportedException`.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Thread Abort', 'When `Thread.Abort()` is called on a target thread in the legacy .NET Framework, how does the CLR actually attempt to stop the thread?', 'By terminating the entire application process.', 'By freezing the CPU core the thread is running on.', 'By injecting and throwing a special `ThreadAbortException` directly into the target thread''s execution path.', 'By changing the thread''s execution priority to zero.', 'C', 'The CLR halts the thread asynchronously by abruptly injecting a `ThreadAbortException` into it. This causes the thread to instantly jump out of its normal execution flow and begin unwinding its stack.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Thread Abort', 'From a software architecture perspective, why is using `Thread.Abort()` considered a catastrophic anti-pattern?', 'It consumes massive amounts of RAM to generate the exception.', 'It terminates execution abruptly, potentially leaving static variables corrupted, unmanaged resources open, and synchronization locks (like `lock` statements) permanently locked, leading to application-wide deadlocks.', 'It bypasses the Garbage Collector, causing memory leaks.', 'It causes the thread to permanently enter a sleep state instead of dying.', 'B', 'Forced abortion is violently unpredictable. If a thread is aborted while holding a lock or halfway through updating a shared data structure, that structure remains corrupted and the lock is never released, instantly destabilizing the entire application process.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Thread Abort', 'What is completely unique about the behavior of the `ThreadAbortException` compared to almost all other standard exceptions in C#?', 'It cannot be caught by any `try-catch` block.', 'It does not contain a stack trace.', 'Even if it is successfully caught and handled in a `catch` block, the CLR automatically and silently re-throws it at the end of the `catch` block to ensure the thread still terminates.', 'It can only be caught by the Main Thread.', 'C', 'To ensure the abort request is honored, the CLR implements a unique safety mechanism. If a thread catches a `ThreadAbortException`, the CLR automatically re-throws it at the end of the `catch` block, preventing the thread from simply swallowing the error and continuing execution.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Thread Abort', 'If a thread catches a `ThreadAbortException`, what specific method must be called to cancel the abort sequence and prevent the CLR from automatically re-throwing the exception?', 'Thread.CancelAbort()', 'Thread.ResetAbort()', 'Exception.Clear()', 'Thread.Resume()', 'B', 'If a thread must absolutely prevent its own termination after being aborted, it can catch the exception and call `Thread.ResetAbort()`. This clears the abort request flag in the CLR and prevents the automatic re-throw, allowing the thread to survive.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Thread Abort', 'What happens if `Thread.Abort()` is called on a thread while it is currently executing inside a `finally` block or a static constructor?', 'The `finally` block is instantly aborted, leaving resources unreleased.', 'The application immediately crashes with a fatal ExecutionEngineException.', 'The CLR defers the abort; it waits patiently until the thread successfully completes the `finally` block or static constructor before injecting the `ThreadAbortException`.', 'The abort request is permanently ignored and deleted.', 'C', 'The CLR recognizes that `finally` blocks and static constructors are hyper-critical for application stability. It deliberately delays the `ThreadAbortException` until control exits these critical regions to prevent severe state corruption.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Thread Abort', 'Since `Thread.Abort()` is banned in modern .NET architecture, what is the standard, safe pattern for stopping a background task or thread?', 'Using the `Process.Kill()` method to terminate the application.', 'Cooperative cancellation using `CancellationTokenSource` and passing a `CancellationToken` that the worker thread voluntarily checks at safe intervals.', 'Calling `Thread.Suspend()` followed by `Thread.Resume()`.', 'Forcing the Garbage Collector to destroy the thread object.', 'B', 'Modern concurrency strictly relies on "cooperative cancellation". The parent passes a `CancellationToken` to the worker. The worker thread periodically checks `token.IsCancellationRequested` at safe points in its logic and gracefully exits on its own terms, preventing all state corruption.');