INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# ThreadPriority', 'What is the primary architectural purpose of the `Thread.Priority` property in C#?', 'To strictly force the CPU to execute threads in a sequential, predictable order.', 'To provide a hint to the operating system''s thread scheduler regarding the relative importance of a thread''s execution time compared to other active threads.', 'To determine how much RAM is allocated to the thread''s execution stack.', 'To bypass the Garbage Collector for critical threads.', 'B', 'The `ThreadPriority` enumeration (Lowest, BelowNormal, Normal, AboveNormal, Highest) does not guarantee execution order. It acts as a hint to the OS scheduler, requesting that the thread be granted larger or more frequent CPU time slices relative to other threads.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# ThreadPriority', 'What is the default `ThreadPriority` assigned to a newly instantiated managed thread in C#?', 'Lowest', 'Normal', 'AboveNormal', 'Highest', 'B', 'By default, the Common Language Runtime (CLR) assigns `ThreadPriority.Normal` to all newly created threads, ensuring an equitable distribution of CPU cycles across the application''s baseline tasks.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# ThreadPriority', 'From a software architecture perspective, what is the most severe risk of arbitrarily setting a background worker thread''s priority to `ThreadPriority.Highest`?', 'It causes an immediate memory leak.', 'It forcefully aborts all other threads in the application.', 'It can cause "Thread Starvation," where the OS continuously allocates CPU time to the high-priority thread, freezing the UI thread and starving other critical system processes.', 'It limits the thread to executing on a single CPU core.', 'C', 'Elevating a thread to `Highest` priority tells the OS to favor it aggressively. If this thread executes a heavy CPU-bound loop, it will effectively steal time slices from `Normal` threads (like the Main UI thread or the Garbage Collector), causing application freezing and severe starvation.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# ThreadPriority', 'Does setting a thread''s priority to `Highest` absolutely guarantee that it will finish executing before a thread with `Normal` priority?', 'Yes, the OS scheduler guarantees strict sequential execution based on priority.', 'Yes, but only on single-core processors.', 'No. Priority is merely a scheduling hint indicating how frequently a thread should receive a CPU time slice; context switching still occurs, and exact execution order remains highly unpredictable.', 'No, the CLR randomly ignores priority settings.', 'C', 'Thread priority never guarantees execution order. A `Highest` priority thread will just get more CPU time slices than a `Normal` one. Because the OS continuously preempts and swaps threads, they will still execute concurrently in an unpredictable overlapping sequence.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# ThreadPriority', 'How does a managed thread''s `ThreadPriority` interact with the underlying operating system''s Process Priority Class?', 'They are completely independent and do not interact.', 'The managed `ThreadPriority` overrides the OS process priority completely.', 'The managed `ThreadPriority` is relative to the overall priority class of the application''s process. A `Highest` priority thread in a `BelowNormal` process will still yield to a `Normal` thread in a `High` priority process.', 'Setting `ThreadPriority.Highest` automatically elevates the entire application process to Administrator level.', 'C', 'Managed thread priorities are localized to the application. The operating system calculates the thread''s *actual* base priority by combining the application''s overarching Process Priority Class with the specific thread''s relative `ThreadPriority`.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# ThreadPriority', 'What is the official architectural guidance regarding changing the `ThreadPriority` of threads obtained from the standard C# `ThreadPool` (or via `Task.Run`)?', 'It is highly recommended to set them to `Highest` to maximize Task Parallel Library performance.', 'It is considered a severe anti-pattern; ThreadPool threads are designed to run strictly at `Normal` priority, and modifying their priority disrupts the pool''s highly tuned internal scheduling algorithms.', 'ThreadPool threads default to `Lowest` and must manually be set to `Normal`.', 'You must use `TaskPriority` instead of `ThreadPriority`.', 'B', 'The `ThreadPool` heavily optimizes its worker threads based on the assumption that they are all running at `Normal` priority. Manually altering the priority of a pool thread destabilizes the global queue. Furthermore, if the priority isn''t manually reset, the modified thread pollutes the pool when it is recycled.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# ThreadPriority', 'If two threads (Thread A and Thread B) share the exact same `ThreadPriority`, how does the operating system typically schedule them?', 'It uses a Round-Robin scheduling algorithm, rapidly context-switching and allocating roughly equal CPU time slices to both threads.', 'Thread A will execute entirely to completion before Thread B is allowed to start.', 'The CLR throws an exception requiring distinct priorities.', 'It merges them into a single thread.', 'A', 'When threads possess the same priority level, modern OS schedulers (like Windows) employ a preemptive, round-robin time-slicing approach. They rapidly rotate through the threads in the ready queue, ensuring equitable execution progress without manual developer intervention.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# ThreadPriority', 'Can assigning different `ThreadPriority` levels to different threads be used as a reliable mechanism to prevent race conditions and synchronize data access?', 'Yes, because a higher priority thread will always lock the data first.', 'Yes, priority acts as a built-in mutex.', 'No, priority strictly controls time-slicing frequency, not execution atomicity. Race conditions will still occur, and explicit synchronization constructs (like `lock` or `Mutex`) must be used to protect shared data.', 'Yes, but only on Windows operating systems.', 'C', 'Relying on thread priority for synchronization is a catastrophic architectural flaw. The OS scheduler can pause a `Highest` priority thread at any millisecond to service hardware interrupts or other processes, allowing a `Lowest` priority thread to execute and corrupt shared memory. Strict locking mechanisms are mandatory.');