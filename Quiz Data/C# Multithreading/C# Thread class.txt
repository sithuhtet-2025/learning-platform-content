INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Thread class', 'What is the primary architectural role of the `System.Threading.Thread` class in .NET?', 'To execute asynchronous HTTP requests exclusively.', 'To provide the lowest-level, managed API required to create, control, prioritize, and monitor operating system threads within a C# application.', 'To act as a garbage collector for unused memory.', 'To automatically parse JSON payloads in the background.', 'B', 'The `Thread` class is the foundational component of multithreading in C#. It encapsulates an actual operating system thread, giving developers granular, low-level control over its lifecycle, priority, and execution state.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Thread class', 'When instantiating a new `Thread` object to execute a method that takes absolutely zero parameters, which delegate does the `Thread` constructor implicitly use?', 'Action', 'Func', 'ParameterizedThreadStart', 'ThreadStart', 'D', 'The `ThreadStart` delegate is specifically designed to represent a method that takes no arguments and returns void. When you pass a parameterless method to the `Thread` constructor (e.g., `new Thread(DoWork)`), the compiler implicitly wraps it in a `ThreadStart` delegate.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Thread class', 'If you must pass a single argument to a method executing on a newly spawned thread, which delegate and method combination must be utilized?', 'The `Action<T>` delegate, passed via `Thread.Run()`.', 'The `ParameterizedThreadStart` delegate, passing the argument via the `Thread.Start(object)` method.', 'The `ThreadStart` delegate, passing the argument via the constructor.', 'You cannot pass arguments to a `Thread`; you must use the `ThreadPool`.', 'B', 'To pass data to a thread, the target method must accept a single parameter of type `object`. The `Thread` constructor uses the `ParameterizedThreadStart` delegate to bind the method, and the data is actually injected when you call `thread.Start(myData)`.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Thread class', 'What happens to the execution flow of the calling thread (e.g., the Main Thread) when it invokes `targetThread.Join();`?', 'It executes concurrently alongside the target thread.', 'It immediately terminates the target thread.', 'It synchronously blocks (pauses execution) and waits indefinitely until the `targetThread` completely finishes its execution.', 'It yields its CPU priority to the target thread but continues executing.', 'C', 'The `Join()` method is a critical synchronization mechanism. It forces the calling thread to halt its execution flow completely until the thread it is joining on has safely terminated, ensuring sequential dependency resolution.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Thread class', 'Which property must you explicitly set to `true` to ensure a newly created worker thread does NOT prevent the CLR from terminating the application process once the Main Thread finishes?', 'IsBackground', 'IsAsync', 'IsDaemon', 'Priority', 'A', 'By default, raw `Thread` objects are created as foreground threads, which keep the application process alive until they finish. Setting `thread.IsBackground = true` converts it to a background thread, allowing the CLR to safely terminate it when all foreground threads exit.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Thread class', 'What is the architectural purpose of calling the static `Thread.Sleep(int millisecondsTimeout)` method?', 'To hibernate the entire host operating system.', 'To force the currently executing thread to suspend its execution, yielding its CPU time slice back to the OS scheduler for the specified duration.', 'To pause the Garbage Collector.', 'To permanently terminate the thread gracefully.', 'B', '`Thread.Sleep()` temporarily suspends the current thread. During this sleep period, the thread consumes zero CPU cycles, allowing the operating system to schedule and execute other threads until the timeout expires.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Thread class', 'How can you dynamically obtain a reference to the specific thread that is currently executing the active block of code?', 'Process.GetCurrentThread()', 'Thread.Active', 'Thread.CurrentThread', 'this.Thread', 'C', 'The static property `Thread.CurrentThread` returns an object reference to the thread currently executing the code. This is heavily utilized for debugging, checking thread IDs, or verifying if the code is running on the Main UI thread.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Thread class', 'What specific runtime exception is immediately thrown if you attempt to call `Start()` on a `Thread` instance that has already been started or has previously terminated?', 'InvalidOperationException', 'ThreadStateException', 'AccessViolationException', 'StackOverflowException', 'B', 'A `Thread` object in C# is a single-use entity. Once its execution has begun, or if it has already completed its lifecycle, attempting to call `Start()` again violates the thread state machine, triggering a strict `ThreadStateException`.');