INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Threading Example static method', 'What is the strictly correct C# syntax to instantiate a new `Thread` object that executes a static method named `DoWork` located inside a static class named `Worker`?', 'new Thread(Worker::DoWork);', 'new Thread(new Worker().DoWork);', 'new Thread(Worker.DoWork());', 'new Thread(Worker.DoWork);', 'D', 'Because the method is static, it belongs to the type itself, not an instance. You pass the method group directly using the class name and method name (without parentheses, as you are referencing the method, not invoking it immediately).');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Threading Example static method', 'When you pass a parameterless static method directly to the `Thread` constructor (e.g., `new Thread(Worker.DoWork)`), which delegate does the C# compiler implicitly wrap the method in?', 'Action', 'Func', 'ThreadStart', 'ParameterizedThreadStart', 'C', 'The `ThreadStart` delegate is the foundational signature for a thread entry point that takes zero parameters and returns void. The compiler automatically infers and wraps the static method in this delegate.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Threading Example static method', 'If your static method is designed to process external data (e.g., `static void ProcessData(object data)`), how is that data passed to the thread at runtime?', 'By passing the data into the `Thread` constructor: `new Thread(Worker.ProcessData, myData);`', 'By passing the data into the `Start` method: `thread.Start(myData);`', 'By setting a global static variable before calling `Start()`.', 'You cannot pass data to static methods running on threads.', 'B', 'For methods accepting an `object` parameter, the compiler implicitly uses the `ParameterizedThreadStart` delegate. The specific data payload is then injected safely when you explicitly call `thread.Start(payloadData);`.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Threading Example static method', 'From a strict concurrency perspective, are local variables declared directly inside the body of a static method safe from race conditions if 100 threads execute that exact same method simultaneously?', 'Yes, because every single thread allocates its own completely independent call stack in memory, ensuring each thread gets its own private, isolated copy of those local variables.', 'No, because static methods inherently share all internal variables across threads.', 'No, local variables must be locked using a `Monitor` to prevent corruption.', 'Yes, but only if the variables are marked as `readonly`.', 'A', 'Local variables inside a method are stored on the thread''s execution stack. Because each of the 100 threads has its own physical stack, there is absolutely zero memory sharing of local variables, making them perfectly thread-safe by default.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Threading Example static method', 'If multiple threads are executing the same static method simultaneously, what is the catastrophic architectural risk regarding class-level `static` fields accessed by that method?', 'They cause memory leaks because the Garbage Collector cannot clean them up.', 'Static fields are globally shared in the AppDomain; concurrent read/write access by multiple threads without a `lock` will instantly cause race conditions and severe data corruption.', 'The CLR automatically locks static fields, causing massive performance bottlenecks.', 'They force the threads to run synchronously on a single CPU core.', 'B', 'Unlike local stack variables, class-level `static` fields are stored in a single, globally shared memory location on the High Frequency Heap. If multiple threads mutate them simultaneously, the data will be hopelessly corrupted unless explicit synchronization (like `lock` or `Interlocked`) is used.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Threading Example static method', 'If you require a class-level static field, but architecturally need that field to maintain a separate, completely independent value for every individual thread that accesses it, what attribute must you apply?', '[Isolated]', '[ThreadStatic]', '[Volatile]', '[ThreadLocal]', 'B', 'Decorating a static field with the `[ThreadStatic]` attribute instructs the CLR to allocate a unique, independent memory slot for that specific static field on every single thread that references it, cleanly preventing cross-thread state corruption.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Threading Example static method', 'Why might a software architect explicitly choose to execute a static method on a background thread rather than an instance method?', 'Static methods execute purely on the GPU.', 'Static methods bypass the CLR security checks.', 'Static methods do not require the instantiation of a class object on the heap, making them highly memory-efficient and perfectly suited for stateless, pure-function utility processing.', 'Static methods automatically implement the `IDisposable` interface.', 'C', 'Static methods are bound to the type, not an instance. If a background task is a pure function (input goes in, processing happens, result comes out) that doesn''t rely on an object''s state, using a static method completely eliminates the overhead of instantiating and garbage-collecting a worker object.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Threading Example static method', 'What fundamental object-oriented restriction applies when executing a static method as a thread entry point?', 'The method cannot catch exceptions.', 'The method cannot instantiate new objects inside its body.', 'The method cannot use the `this` keyword or access any non-static instance fields of its parent class, because it executes without an object instance context.', 'The method cannot be marked as `private`.', 'C', 'Because the method is static, it executes independently of any instantiated object. Therefore, there is no `this` pointer. The executing thread cannot read or write to normal instance properties or fields belonging to that class unless an explicit object instance is passed into the method.');