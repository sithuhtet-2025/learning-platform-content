INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Thread Join', 'What is the primary architectural function of the `Thread.Join()` method in C#?', 'To merge two running threads into a single shared execution path.', 'To synchronously block the calling thread until the target thread completely terminates.', 'To force the target thread to pause its execution temporarily.', 'To start the execution of a thread that is currently in the Unstarted state.', 'B', '`Thread.Join()` is a blocking synchronization mechanism. When the Main Thread calls `workerThread.Join()`, the Main Thread completely suspends its own execution flow and waits indefinitely until `workerThread` safely finishes its work and terminates.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Thread Join', 'What does the overloaded method `Thread.Join(int millisecondsTimeout)` return to the caller?', 'An integer representing the thread''s exit code.', 'A boolean value: `true` if the thread successfully terminated before the timeout, and `false` if the timeout elapsed while the thread was still running.', 'A string containing the thread''s final state.', 'It returns `void` and simply throws a `TimeoutException` if the time expires.', 'B', 'Providing a timeout is a critical safety pattern. Instead of blocking forever if the worker thread hangs, `Join(timeout)` waits up to the specified limit. It returns `true` if the thread finished in time, or `false` if the caller gave up waiting, allowing the caller to handle the hanging thread gracefully.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Thread Join', 'What specific runtime exception is immediately thrown if you attempt to call `Join()` on a thread that is still in the `Unstarted` state?', 'System.NullReferenceException', 'System.InvalidOperationException', 'System.Threading.ThreadStateException', 'System.Threading.ThreadInterruptedException', 'C', 'A thread must actually be running (or have already run and finished) to be joined. Calling `Join()` before calling `Start()` on a thread violates the internal state machine of the CLR, resulting in a strict `ThreadStateException`.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Thread Join', 'From a CPU performance perspective, why is calling `workerThread.Join()` architecturally superior to using a continuous `while(workerThread.IsAlive) { }` loop (a spin-wait)?', '`Join()` executes the worker thread''s code on the GPU.', 'A spin-wait loop burns massive amounts of CPU cycles continuously checking the boolean flag, whereas `Join()` puts the calling thread into an efficient `WaitSleepJoin` state that consumes zero CPU time until the OS wakes it up.', '`Join()` automatically garbage collects the thread''s memory.', '`IsAlive` is strictly private and cannot be accessed externally.', 'B', 'Spin-waiting (empty while loops) creates a "busy wait" that maxes out a CPU core doing absolutely nothing. `Thread.Join()` signals the OS thread scheduler to put the calling thread to sleep, preserving CPU resources until the target thread signals its termination.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Thread Join', 'What catastrophic architectural failure occurs if a thread executes `Thread.CurrentThread.Join();` upon itself?', 'The thread instantly restarts from the beginning of its method.', 'The compiler throws a syntax error.', 'The thread enters a permanent, unbreakable deadlock, freezing its execution infinitely because it is asleep waiting for itself to terminate.', 'The thread''s priority is elevated to ''Highest''.', 'C', '`Join()` instructs a thread to wait for a target thread to finish. If a thread targets itself, it goes to sleep waiting for itself to finish. Since it is asleep, it can never execute further to finish, instantly creating a permanent deadlock on that execution path.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Thread Join', 'When the Main Thread is blocked by a `Join()` call, what happens to its `ThreadState` property?', 'It becomes `Running`.', 'It becomes `Suspended`.', 'It becomes `WaitSleepJoin`.', 'It becomes `Stopped`.', 'C', 'Whenever a thread is blocked by calling `Join()`, `Sleep()`, or waiting on a `Monitor` lock, the CLR transitions its internal state to `WaitSleepJoin`. This explicitly tells the runtime that the thread is blocked and yielding its execution time to others.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Thread Join', 'If a worker thread is currently stuck blocked on a `Join()` call, how can another external thread forcefully break that block and wake it up?', 'By calling `Thread.Resume()` on the blocked thread.', 'By calling `Thread.Interrupt()` on the blocked thread.', 'By calling `Thread.Start()` again.', 'It is impossible to break a `Join()` block externally.', 'B', 'If a thread is stuck in a `WaitSleepJoin` state (like waiting indefinitely on a `Join()`), another thread can call `Interrupt()` on it. This forcefully awakens the blocked thread by injecting a `ThreadInterruptedException` into it, breaking the deadlock and forcing the thread to handle the exception.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Thread Join', 'While `Thread.Join()` successfully synchronizes execution, what is a major architectural limitation of this legacy method compared to modern `Task.WhenAll()` or `await`?', '`Join()` can only wait for a maximum of 3 seconds.', '`Join()` does not support returning data/results from the thread, nor does it efficiently propagate exceptions thrown inside the worker thread back to the caller.', '`Join()` can only be used on background threads.', '`Join()` requires the `unsafe` code context.', 'B', 'In the raw `Thread` API, threads return `void` and swallow unhandled exceptions. `Join()` only waits for the thread to die; it cannot retrieve a result payload or safely bubble up exceptions. Modern `Task` architecture solves this entirely by returning strongly-typed `Task<T>` objects that capture results and exceptions seamlessly.');