INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Interface', 'By default, what is the access modifier for members declared within a C# interface (prior to the introduction of default interface methods in C# 8.0)?', 'private', 'protected', 'public', 'internal', 'C', 'By default, all members of an interface are inherently public and abstract. Explicitly specifying the public modifier was not even allowed on standard interface members before C# 8.0.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Interface', 'Which statement is true regarding classes and interfaces in C#?', 'A class can inherit from multiple abstract classes.', 'A class can implement multiple interfaces but inherit from only one base class.', 'A class can implement only one interface.', 'Interfaces can inherit directly from classes.', 'B', 'C# supports multiple inheritance for interfaces but strictly single inheritance for classes. A single class can implement any number of interfaces while inheriting from at most one base class.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Interface', 'If a class implements two distinct interfaces that both contain a method with the exact same signature, how can the class provide a separate implementation for each interface''s method?', 'By using the override keyword for one and virtual for the other.', 'By changing the return type of one of the methods in the class.', 'By using explicit interface implementation, prefixing the method name with the interface name.', 'It is impossible; the code will result in a compile-time error.', 'C', 'Explicit interface implementation (e.g., void IFirst.MethodName()) allows a class to implement methods with identical signatures from multiple interfaces without naming conflicts. These methods are only accessible when the object is explicitly cast to the specific interface type.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Interface', 'Can you instantiate an interface directly using the new keyword in C# (e.g., IMyInterface obj = new IMyInterface();)?', 'Yes, if the interface contains default method implementations.', 'Yes, but only inside the assembly where it is defined.', 'No, interfaces can never be instantiated directly; they must be implemented by a class or struct.', 'No, unless the interface is marked with the static keyword.', 'C', 'Interfaces act as abstract contracts and cannot be instantiated directly. You must instantiate a concrete class or struct that implements the interface, and then assign that instance to an interface reference variable.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Interface', 'Introduced in C# 8.0, what does a "default interface method" allow a developer to do?', 'Define stateful fields and properties with backing stores inside an interface.', 'Provide a concrete implementation for a method within the interface itself, allowing safe addition of new methods to existing interfaces.', 'Force all classes implementing the interface to be declared as abstract classes.', 'Prevent implementing classes from overriding the method implementation.', 'B', 'C# 8.0 introduced default interface methods, allowing interfaces to contain actual method bodies. This prevents breaking changes to existing implementations when a new method is added to a widely used, already-published interface.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Interface', 'Can an interface inherit from another interface in C#?', 'No, inheritance is strictly limited to classes.', 'Yes, but an interface can only inherit from one other interface.', 'Yes, an interface can inherit from one or multiple other interfaces.', 'Yes, but only if the base interface contains no methods.', 'C', 'Interfaces can inherit from multiple other interfaces. A class that implements the derived interface must provide implementations for all members defined in the derived interface as well as all of its base interfaces.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Interface', 'Which of the following is a valid abstract property declaration inside a C# interface?', 'int Id;', 'int Id = 5;', 'int Id { get; set; }', 'int Id => 0;', 'C', 'Interfaces can define properties by specifying the get and/or set accessors without bodies. They cannot contain instance fields (like Option A and Option B).');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Interface', 'Which of the following statements regarding structs and interfaces is true in C#?', 'Structs cannot implement interfaces because they are value types.', 'Structs can implement multiple interfaces, exactly like classes.', 'Structs can implement interfaces, but doing so forces them to become reference types.', 'Structs can only implement a single interface.', 'B', 'In C#, structs (which are value types) can implement one or more interfaces, exactly like classes. However, it is important to note that boxing occurs when a struct instance is cast to an interface type.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Interface', 'If you have an object reference ''obj'' of type object, how can you safely check if it implements the ''IDisposable'' interface and cast it in one step without throwing an exception?', 'IDisposable d = (IDisposable)obj;', 'if (obj.Implements(IDisposable)) { ... }', 'if (obj is IDisposable d) { d.Dispose(); }', 'IDisposable d = obj.As<IDisposable>();', 'C', 'Using the ''is'' operator with pattern matching (introduced in C# 7.0) is the safest and most concise way to check if an object implements an interface and simultaneously assign it to a newly scoped, strongly-typed variable.');