INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Abstraction', 'Which of the following best defines the concept of Abstraction in C# object-oriented programming?', 'Hiding internal implementation details and showing only essential features of an object.', 'Wrapping data and methods into a single unit to prevent external access.', 'Creating a new class from an existing class to promote code reuse.', 'Allowing a single function name to have multiple implementations based on parameter types.', 'A', 'Abstraction is the process of hiding the complex implementation details and exposing only the necessary functionalities to the user. Option B describes Encapsulation, C describes Inheritance, and D describes Polymorphism.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Abstraction', 'In C#, which two primary language constructs are used to achieve abstraction?', 'Structs and Enums', 'Virtual methods and Sealed classes', 'Abstract classes and Interfaces', 'Partial classes and Extension methods', 'C', 'C# achieves abstraction primarily through abstract classes (which can provide partial implementation) and interfaces (which establish a strict contract with no implementation prior to C# 8.0 default interface methods).');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Abstraction', 'If you are modeling a car in C#, and you expose a ''StartEngine()'' method while hiding the complex fuel injection and ignition processes, which OOP principle are you primarily applying?', 'Polymorphism', 'Abstraction', 'Inheritance', 'Type Safety', 'B', 'Exposing the simple ''StartEngine()'' method to the consumer while hiding the complex, underlying mechanical processes is a classic example of abstraction, focusing on *what* the object does rather than *how* it does it.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Abstraction', 'When designing an application, when is it generally more appropriate to use an Interface rather than an Abstract Class for abstraction?', 'When you need to provide a default implementation for some methods.', 'When you want to define a contract for classes that may not be related by a class hierarchy.', 'When you need to declare non-static fields and access modifiers.', 'When you want to restrict a class to single inheritance only.', 'B', 'Interfaces are ideal for defining cross-cutting capabilities (like IComparable or IDisposable) across completely unrelated class hierarchies, as classes can implement multiple interfaces but inherit from only one abstract class.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Abstraction', 'Consider an interface ''IDatabase'' with a method ''Connect()''. A class ''SqlDatabase'' implements ''IDatabase''. How does abstraction benefit the client code that uses this database?', 'The client code can directly modify the fields of ''SqlDatabase''.', 'The client code only needs to interact with the ''IDatabase'' reference, remaining decoupled from the specific ''SqlDatabase'' implementation.', 'The client code must instantiate the ''IDatabase'' directly using the new keyword.', 'The client code runs faster because interfaces bypass compile-time type checking.', 'B', 'Abstraction allows the client to program against the interface (''IDatabase''). This decouples the client from specific implementations, making the code more modular, testable, and easier to maintain.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Abstraction', 'While often confused, Abstraction and Encapsulation serve different primary purposes. Which statement accurately distinguishes them?', 'Abstraction hides complexity by exposing a high-level interface, while Encapsulation hides internal state and restricts direct access to data.', 'Encapsulation is achieved using interfaces, while Abstraction is achieved using access modifiers like private and protected.', 'Abstraction applies only to methods, while Encapsulation applies only to properties and fields.', 'There is no difference; they are synonymous terms in C# design patterns.', 'A', 'Abstraction solves design-level problems by hiding implementation complexity (focusing on the outside view), whereas Encapsulation solves implementation-level problems by hiding internal data and state (focusing on the inside view).');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Abstraction', 'In modern C# (8.0 and later), how has the concept of pure abstraction in interfaces evolved?', 'Interfaces can now be instantiated directly.', 'Interfaces can now define constructors and destructors.', 'Interfaces can now provide default implementations for their methods.', 'Interfaces can now inherit from standard base classes.', 'C', 'Starting with C# 8.0, interfaces can include default method implementations. This slightly blurs the line of pure abstraction but allows developers to add new methods to existing interfaces without breaking the classes that currently implement them.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Abstraction', 'How can you enforce abstraction of a property using an interface in C#?', 'By declaring a field inside the interface.', 'By specifying the property signature with get and/or set accessors without a body.', 'By using the abstract keyword on the property inside the interface.', 'Properties cannot be defined in interfaces; only methods can be abstracted.', 'B', 'Interfaces can declare properties by defining the signature with get and/or set accessors (e.g., ''string Name { get; set; }''). The implementing class is then forced to provide the concrete implementation of those accessors.');