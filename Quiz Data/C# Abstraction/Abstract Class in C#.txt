INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Abstract Class in C#', 'Which of the following statements regarding the instantiation of an abstract class in C# is true?', 'It can be instantiated directly using the new keyword.', 'It cannot be instantiated directly, but can be instantiated if it contains only non-abstract methods.', 'It cannot be instantiated directly; you must instantiate a non-abstract derived class.', 'It can be instantiated only within the assembly where it is defined.', 'C', 'Abstract classes are designed to be base classes and cannot be instantiated directly using the new operator, regardless of whether they contain abstract methods or not.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Abstract Class in C#', 'What is the correct syntax to declare an abstract method inside an abstract class?', 'public abstract void DoWork() {}', 'public abstract void DoWork();', 'public virtual void DoWork();', 'public void abstract DoWork() => throw new NotImplementedException();', 'B', 'Abstract methods have no implementation (no body) in the abstract class and must end with a semicolon. They implicitly act as virtual methods that must be overridden in derived classes.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Abstract Class in C#', 'When a non-abstract class inherits from an abstract class, which keyword must be used to implement the abstract methods?', 'virtual', 'new', 'override', 'implement', 'C', 'The override keyword is required to extend or modify the abstract or virtual implementation of an inherited method, property, indexer, or event.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Abstract Class in C#', 'Can an abstract class in C# define constructors?', 'No, because abstract classes cannot be instantiated.', 'Yes, but they must be declared as static.', 'Yes, they can have constructors which are called when a derived class is instantiated.', 'Yes, but only parameterless constructors are allowed.', 'C', 'Abstract classes can have constructors. Even though the abstract class itself cannot be instantiated directly, its constructors are invoked from the constructors of derived classes during the instantiation of a derived type.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Abstract Class in C#', 'Which of the following is a key limitation of abstract classes compared to interfaces in C#?', 'A class can inherit from only one abstract class, but can implement multiple interfaces.', 'Abstract classes cannot contain fully implemented methods, whereas interfaces can.', 'Abstract classes cannot declare properties, whereas interfaces can.', 'Abstract classes cannot have access modifiers on their members.', 'A', 'C# does not support multiple class inheritance. A derived class can only inherit from a single base class (abstract or concrete), but it can implement any number of interfaces.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Abstract Class in C#', 'What happens if you apply the sealed modifier to an abstract class?', 'The class can be instantiated directly.', 'The abstract methods become virtual automatically.', 'It compiles successfully, but prevents any further inheritance.', 'It causes a compile-time error.', 'D', 'The sealed and abstract modifiers are mutually exclusive. An abstract class is meant to be inherited, while a sealed class explicitly prevents inheritance, resulting in a compiler error if both are used together.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Abstract Class in C#', 'What is the primary difference between a virtual method and an abstract method in an abstract base class?', 'Virtual methods must be overridden, abstract methods are optional.', 'Virtual methods provide a default implementation, abstract methods cannot have an implementation and must be overridden.', 'Abstract methods can only be called from the base class, virtual methods from the derived class.', 'There is no difference; they are interchangeable keywords in C#.', 'B', 'Virtual methods include a method body (implementation) that derived classes can optionally override. Abstract methods do not contain a body and force non-abstract derived classes to provide an implementation.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Abstract Class in C#', 'Can an abstract method be marked as private?', 'Yes, if it is only meant to be overridden by nested classes.', 'Yes, private abstract is a valid combination in C#.', 'No, abstract methods cannot be private because derived classes must be able to access and override them.', 'No, abstract methods must always be strictly public.', 'C', 'Abstract methods are intended to be overridden by derived classes. If they were private, derived classes could not see them to override them. They can be public, protected, or internal, but not private.');