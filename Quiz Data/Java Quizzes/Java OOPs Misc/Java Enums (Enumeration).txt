INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Enums (Enumeration)', 'What is the primary architectural purpose of declaring an ''enum'' in Java rather than using standard integer or string constants?', 'To encrypt sensitive configuration data.', 'To create a specialized class that enforces absolute type safety, guaranteeing a variable can only ever hold one of a predefined, fixed set of constant values.', 'To allow a class to inherit from multiple parent classes.', 'To dynamically generate new constants at runtime based on user input.', 'B', 'Enums (enumerations) replace legacy patterns like ''public static final int RED = 1''. They provide compile-time type safety. If a method requires a ''DayOfWeek'' enum, it is impossible for a developer to accidentally pass an invalid integer or a misspelled string.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Enums (Enumeration)', 'Which class does every Java ''enum'' implicitly extend at the compiler level, preventing it from extending any other custom classes?', 'java.lang.Object', 'java.lang.Class', 'java.lang.Enum', 'java.util.Enumeration', 'C', 'When you declare an ''enum'', the Java compiler automatically translates it into a class extending ''java.lang.Enum''. Because Java does not support multiple inheritance for classes, an enum cannot extend any other superclass.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Enums (Enumeration)', 'What are the implicit, automatic modifiers applied to every single constant declared inside a Java ''enum'' (e.g., the ''RED'' in ''enum Color { RED, BLUE }'')?', 'private and transient', 'public static final', 'protected and volatile', 'default and abstract', 'B', 'Enum constants represent singleton instances of the enum class itself. The compiler implicitly makes them ''public'' (accessible everywhere), ''static'' (belonging to the class, not an instance), and ''final'' (cannot be reassigned or modified).');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Enums (Enumeration)', 'Can a Java ''enum'' contain custom fields, methods, and constructors?', 'No, enums can only contain a comma-separated list of constant names.', 'Yes, but constructors must be explicitly declared as ''public'' so the user can instantiate them.', 'Yes, an enum is a fully featured class. It can have fields and methods. However, its constructors are strictly enforced as ''private'' or package-private to prevent external instantiation.', 'Yes, but only static methods are allowed.', 'C', 'Enums in Java are extremely powerful because they are full classes. You can attach data (fields) to each constant and define methods. The JVM ensures that enum constructors are only ever invoked internally when the constants are initialized upon class loading; you cannot use ''new'' on an enum.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Enums (Enumeration)', 'Which static compiler-generated method allows you to retrieve an array containing all the constants defined within a specific enum, in the exact order they were declared?', 'getAll()', 'values()', 'getConstants()', 'list()', 'B', 'The compiler automatically inserts a static ''values()'' method into every enum. This method returns an array containing all of the enum''s instances, which is incredibly useful for iterating over the available options in a ''for-each'' loop.');
