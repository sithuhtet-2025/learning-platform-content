INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Abstraction in Java', 'What is the primary architectural goal of Abstraction in Object-Oriented Programming?', 'To bundle data and methods together into a single unit to prevent unauthorized access.', 'To hide complex background implementation details and expose only the essential features and functionalities to the user.', 'To allow a class to inherit from multiple parent classes simultaneously.', 'To create multiple methods with the exact same name but different parameters.', 'B', 'Abstraction focuses on design-level simplification ("what" an object does). It hides the complex, low-level implementation details ("how" it does it) from the user, exposing only a clean, high-level interface.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Abstraction in Java', 'Which two mechanisms are strictly utilized in Java to achieve abstraction?', 'Encapsulation and Inheritance.', 'Static blocks and Final variables.', 'Abstract Classes (0 to 100% abstraction) and Interfaces (100% abstraction).', 'Try-catch blocks and Multithreading.', 'C', 'Java achieves abstraction through abstract classes (which can contain a mix of abstract and concrete methods) and interfaces (which historically contained strictly abstract methods, though default methods were added in Java 8).');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Abstraction in Java', 'If a Java class contains at least one ''abstract'' method, what strict rule applies to the class declaration itself?', 'The class must implement the Serializable interface.', 'The class must also be declared as ''abstract''.', 'The class must be declared as ''final''.', 'The class must provide a default constructor.', 'B', 'If a class contains an abstract method (a method without a body), the compiler mandates that the entire class must be declared as ''abstract''. An abstract class serves as an incomplete template that cannot be directly instantiated.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Abstraction in Java', 'What happens if you attempt to use the ''new'' keyword to instantiate an object directly from an ''abstract'' class?', 'The JVM dynamically implements the missing methods at runtime.', 'The compiler throws an InstantiationError, preventing the creation of the object.', 'An object is created, but calling the abstract methods throws a NullPointerException.', 'The JVM delegates instantiation to the nearest concrete subclass.', 'B', 'Abstract classes are intentionally incomplete blueprints. The Java compiler strictly forbids their direct instantiation. You must instantiate a concrete subclass that has provided valid implementations for all the inherited abstract methods.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Abstraction in Java', 'When a concrete subclass extends an ''abstract'' parent class, what is its exact architectural obligation?', 'It must overload all the methods of the parent class.', 'It must declare itself as abstract as well.', 'It must provide concrete implementations (method bodies) for every single abstract method defined in the parent class.', 'It must use the ''super'' keyword in every method.', 'C', 'To become a concrete, instantiable class, a subclass must fulfill the contract established by its abstract parent. It is obligated to override and implement every inherited abstract method. If it fails to do so, it too must be declared abstract.');
