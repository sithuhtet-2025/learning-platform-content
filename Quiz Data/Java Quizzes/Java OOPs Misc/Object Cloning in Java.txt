INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Object Cloning in Java', 'What specific interface must a Java class explicitly implement to legally utilize the ''Object.clone()'' method without crashing?', 'java.io.Serializable', 'java.lang.Cloneable', 'java.util.Copyable', 'java.lang.Runnable', 'B', 'The ''java.lang.Cloneable'' interface is a "marker interface" (containing no methods). It acts strictly as a permission flag to the JVM. If you invoke ''clone()'' on an object whose class does not implement this interface, the JVM throws a ''CloneNotSupportedException''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Object Cloning in Java', 'What type of copy is generated by the default ''Object.clone()'' native implementation?', 'A Deep Copy: completely duplicating the object and recursively duplicating all nested objects it references.', 'A Shallow Copy: creating a new object instance, but copying only the exact values of primitives and the memory references of nested objects, meaning both the original and clone point to the same nested objects.', 'A Lazy Copy: copying only the metadata.', 'A Reference Copy: simply creating a new pointer to the exact same original object.', 'B', 'The default cloning mechanism is shallow. If your class contains an array or another mutable object (like a ''Date''), the clone will receive a pointer to the original array, not a separate copy of the array. Modifying the array in the clone will corrupt the original.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Object Cloning in Java', 'If a class requires a "Deep Copy" to prevent shared nested state between the original and the clone, how must the developer achieve this?', 'By adding the ''@DeepClone'' annotation to the class.', 'By calling ''super.clone(true)''.', 'By overriding the ''clone()'' method, calling ''super.clone()'' first, and then manually creating independent copies of all mutable internal object references.', 'By using the ''java.util.Collections.copy()'' method.', 'C', 'To execute a deep copy, the developer must override the ''clone()'' method. After obtaining the shallow copy from ''super.clone()'', the developer must explicitly invoke ''clone()'' (or use ''new'') on all mutable reference variables to break the shared memory links.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Object Cloning in Java', 'What is the primary modern architectural alternative recommended over using the flawed and complex ''Object.clone()'' mechanism?', 'Using Java Reflection APIs.', 'Writing a custom "Copy Constructor" (e.g., ''public Employee(Employee original)'') or a static factory method that manually copies the required fields.', 'Using the ''=='' operator.', 'Using the ''finalize()'' method.', 'B', 'Effective Java and modern engineering practices highly discourage using ''Cloneable'' due to its fragile design and exception-handling overhead. A Copy Constructor provides clear, type-safe, and easily maintainable object duplication logic without relying on native JVM magic.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Object Cloning in Java', 'Because the ''clone()'' method in the ''Object'' class is declared as ''protected'', what must you do to make it accessible to external classes?', 'You cannot change its visibility; it must remain protected.', 'You must declare the class as ''public''.', 'You must override the ''clone()'' method in your class and widen its access modifier to ''public''.', 'You must pass the object to a separate cloning thread.', 'C', 'Protected methods can only be called from within the same package or by subclasses. If you want external classes to be able to call ''myObject.clone()'', you must override it in your class, change the modifier to ''public'', and typically cast the return type from ''Object'' to your specific class type.');
