INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Difference Between Method Overloading and Method Overriding in Java', 'Which fundamental types of polymorphism do Method Overloading and Method Overriding represent, respectively?', 'Both represent compile-time polymorphism.', 'Overloading is Run-Time Polymorphism (Dynamic Binding); Overriding is Compile-Time Polymorphism (Static Binding).', 'Overloading is Compile-Time Polymorphism (Static Binding); Overriding is Run-Time Polymorphism (Dynamic Binding).', 'Both represent run-time polymorphism.', 'C', 'Overloading is resolved by the compiler based on the method signature (Static Binding). Overriding is resolved by the JVM at runtime based on the actual object type in memory, regardless of the reference type (Dynamic Binding).');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Difference Between Method Overloading and Method Overriding in Java', 'Where do these two polymorphic mechanisms physically occur within the Java class architecture?', 'Both occur strictly within the exact same class.', 'Overloading occurs between a superclass and a subclass; Overriding occurs within the same class.', 'Overloading occurs within the same class (or across inherited methods); Overriding strictly requires an inheritance relationship between a superclass and a subclass.', 'Both require an interface to function.', 'C', 'Overloading is about having multiple methods with the same name but different parameters within a class''s scope. Overriding is strictly about a child class replacing the specific behavior of a method inherited from its parent class.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Difference Between Method Overloading and Method Overriding in Java', 'What is the absolute, compiler-enforced rule regarding the method signature (name and parameters) for both concepts?', 'Overloading requires the exact same signature; Overriding requires a different signature.', 'Both require the exact same signature.', 'Both require different signatures.', 'Overloading requires the same name but MUST have different parameters; Overriding MUST have the exact same name and the exact same parameters.', 'D', 'For overloading, changing the parameter list is the entire mechanism. For overriding, the subclass must match the parent''s signature perfectly so that it can seamlessly replace the parent''s behavior when called polymorphically.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Difference Between Method Overloading and Method Overriding in Java', 'Can you apply Method Overriding to a method declared as ''static'' or ''private'' in the superclass?', 'Yes, overriding ignores access modifiers.', 'No, but you CAN perfectly overload ''static'' and ''private'' methods within the same class.', 'Yes, but only if you use the @Override annotation.', 'No, neither overriding nor overloading is permitted for static methods.', 'B', 'Method Overriding requires dynamic dispatch at runtime based on object instances. ''static'' methods belong to the class, not the instance, and ''private'' methods are invisible to subclasses. Therefore, they cannot be overridden (though static methods can be "hidden"). You can, however, overload them normally.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Difference Between Method Overloading and Method Overriding in Java', 'What are the restrictions regarding the Return Type for both mechanisms?', 'Overloading: must be the same. Overriding: can be completely different.', 'Overloading: can be completely different as long as parameters change. Overriding: MUST be the exact same type or a covariant return type (a subclass of the original return type).', 'Both require the exact same return type.', 'There are no restrictions on return types for either.', 'B', 'In overloading, the return type doesn''t matter as long as the parameters differ. In overriding, you cannot break the parent''s contract; you must return the same type, or a valid subclass of that type (covariant return), to ensure type safety for callers using the parent reference.');
