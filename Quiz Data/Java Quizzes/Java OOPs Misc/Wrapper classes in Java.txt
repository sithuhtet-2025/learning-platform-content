INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Wrapper classes in Java', 'What is the primary architectural purpose of Wrapper classes (like Integer and Double) in Java?', 'To make mathematical operations execute faster.', 'To encapsulate primitive data types into Objects so they can be utilized in Java''s object-oriented Collections framework (like ArrayList or HashMap).', 'To encrypt primitive data in memory.', 'To convert strings into character arrays.', 'B', 'Java''s Collections framework (Lists, Sets, Maps) strictly requires Objects and cannot store raw primitive data types (like ''int'' or ''char''). Wrapper classes provide an object-oriented capsule around these primitives to bridge this gap.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Wrapper classes in Java', 'What feature, introduced in Java 5, automatically converts a primitive type into its corresponding wrapper class object (e.g., ''Integer num = 10;'')?', 'Typecasting', 'Unboxing', 'Autoboxing', 'Serialization', 'C', 'Autoboxing is the Java compiler''s automatic conversion of primitive types into their corresponding object wrapper classes. The compiler seamlessly injects ''Integer.valueOf(10)'' behind the scenes.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Wrapper classes in Java', 'Consider the code: ''Integer a = 100; Integer b = 100; System.out.println(a == b);''. Assuming standard JVM settings, what is the output and why?', 'false, because they are different objects on the heap.', 'true, because Autoboxing uses an internal IntegerCache for values between -128 and 127, returning the exact same memory reference.', 'true, because == compares the values of wrapper classes.', 'false, because of unboxing overhead.', 'B', 'To save memory, the JVM caches ''Integer'' objects for values ranging from -128 to 127. When autoboxing a value within this range, the JVM returns a reference to the cached object. Thus, both variables point to the exact same memory location.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Wrapper classes in Java', 'What happens during "Unboxing" if the wrapper class reference is currently pointing to null (e.g., ''Integer obj = null; int num = obj;'')?', 'The primitive is assigned a default value of 0.', 'The primitive is assigned a value of -1.', 'The compiler throws a syntax error.', 'The JVM throws a NullPointerException at runtime.', 'D', 'Unboxing automatically invokes the underlying value method (like ''intValue()'') on the wrapper object. If the object reference is ''null'', attempting to implicitly call an instance method on it inherently triggers a ''NullPointerException''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Wrapper classes in Java', 'Are instances of standard Java wrapper classes (like Integer, Boolean, Character) mutable or immutable?', 'They are mutable via setter methods.', 'They are strictly immutable; their internal primitive value cannot be changed once the object is instantiated.', 'They are mutable but only through reflection.', 'Boolean is mutable but Integer is immutable.', 'B', 'All standard wrapper classes in Java are deeply immutable. If you perform an operation like ''Integer x = 5; x++;'', the JVM actually unboxes ''x'', increments the primitive value, autoboxes the new value into a brand NEW ''Integer'' object, and reassigns the reference.');
