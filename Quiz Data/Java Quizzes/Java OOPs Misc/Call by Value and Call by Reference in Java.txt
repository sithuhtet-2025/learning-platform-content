INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Call by Value and Call by Reference in Java', 'Which of the following statements definitively describes Java''s parameter passing mechanism?', 'Java uses Call by Reference for objects and Call by Value for primitives.', 'Java is strictly and exclusively Call by Value for all data types, including object references.', 'Java uses Call by Reference exclusively.', 'Java determines the passing mechanism dynamically at runtime based on object size.', 'B', 'Java is strictly pass-by-value. It never passes the actual memory address of a variable (true call-by-reference like C++). For primitives, a copy of the value is passed. For objects, a copy of the memory reference (the pointer) is passed.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Call by Value and Call by Reference in Java', 'If you pass a primitive integer variable ''x = 10'' to a method that increments its parameter by 5, what is the value of ''x'' in the calling method after the method returns?', '15', '10', '0', 'NullPointerException', 'B', 'Because primitives are passed by value, the method receives a completely independent copy of the integer ''10''. Modifying this local copy inside the method has absolutely zero effect on the original variable ''x'' residing in the calling method''s stack frame.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Call by Value and Call by Reference in Java', 'When passing an object reference (e.g., a ''Car'' object) to a method, why does modifying the object''s internal state (e.g., ''car.setSpeed(100)'') inside the method affect the original object?', 'Because objects are passed by true reference.', 'Because the JVM synchronizes all object instances automatically.', 'Because a copy of the reference is passed, but both the original reference and the copied reference point to the exact same physical object residing on the Heap memory.', 'Because the object is cloned before being passed.', 'C', 'Java passes a copy of the pointer. Since both the original variable and the method parameter hold the exact same memory address, invoking mutator methods on the parameter alters the single, shared object located on the heap.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Call by Value and Call by Reference in Java', 'What happens if a method receives an object reference and then uses the ''new'' keyword to reassign that parameter to a brand new object (e.g., ''carParam = new Car();'')?', 'The original reference variable in the calling method is updated to point to the new object.', 'The new object replaces the old object in the heap memory.', 'The original reference in the calling method remains completely unchanged and continues pointing to the original object.', 'It causes a compilation error because parameters are implicitly final.', 'C', 'Because the method operates on a copy of the reference, reassigning the parameter variable simply changes where the local copy points. It breaks the link to the original heap object, leaving the calling method''s original reference entirely unaffected.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Call by Value and Call by Reference in Java', 'Why did the architects of Java deliberately exclude true "Call by Reference" (like C++ pointers) from the language design?', 'To increase the speed of mathematical computations.', 'To enforce strict encapsulation, prevent unauthorized memory manipulation, and simplify the Garbage Collection process by eliminating complex pointer arithmetic.', 'To reduce the size of the compiled bytecode.', 'Because it is mathematically impossible in a virtual machine.', 'B', 'True call-by-reference requires direct memory pointers. Java abstracts memory management away from the developer to ensure security, prevent pointer-based memory leaks or corruption, and allow the Garbage Collector to safely move objects in the heap without breaking hardcoded pointers.');
