INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Object class in Java', 'What is the defining structural characteristic of the ''java.lang.Object'' class in the Java environment?', 'It is a marker interface.', 'It is the absolute root superclass of the Java class hierarchy; every class inherently extends it, directly or indirectly.', 'It is the parent class strictly for primitive data types.', 'It is an abstract class used to build UI components.', 'B', 'By definition, if a Java class does not explicitly extend another class using the ''extends'' keyword, the Java compiler implicitly injects ''extends Object''. This ensures every object in Java shares a common set of foundational methods.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Object class in Java', 'What does the default implementation of the ''equals(Object obj)'' method provided by the ''Object'' class actually evaluate?', 'It performs a deep comparison of all instance variables to check for logical equality.', 'It checks if the two objects share the same memory hash code.', 'It strictly evaluates reference equality: it returns true ONLY if both reference variables point to the exact same physical object in heap memory (acting identically to the ''=='' operator).', 'It compares the lengths of the objects in bytes.', 'C', 'The default ''equals()'' method does not understand the business logic of your custom classes. It simply checks if ''this == obj''. To compare the actual data contents of two distinct objects, you must override this method.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Object class in Java', 'What is the strict architectural contract between the ''equals()'' and ''hashCode()'' methods inherited from the ''Object'' class?', 'If two objects have the same hashCode, they must be equal according to equals().', 'If two objects are equal according to the equals() method, then calling the hashCode() method on each of the two objects MUST produce the exact same integer result.', 'There is no contract; they operate completely independently.', 'hashCode() must be randomly generated regardless of equals().', 'B', 'This contract is vital for hash-based collections like ''HashMap''. While identical hash codes do not guarantee equality (hash collisions can occur), logical equality MUST guarantee identical hash codes to ensure objects can be found in a hash bucket.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Object class in Java', 'Which method provided by the ''Object'' class returns the runtime class object representing the exact type of the instantiated object?', 'typeof()', 'instanceof()', 'getClass()', 'getClassName()', 'C', 'The ''getClass()'' method is a native final method that returns an instance of ''java.lang.Class''. This is heavily utilized in Java Reflection to analyze an object''s metadata, methods, and fields dynamically at runtime.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Object class in Java', 'Why are the inter-thread communication methods ''wait()'', ''notify()'', and ''notifyAll()'' defined in the ''Object'' class rather than the ''Thread'' class?', 'Because Threads cannot have methods.', 'Because Java threads are not objects.', 'Because locks (monitors) in Java are held on Objects, not on Threads. Any object can act as a synchronization monitor, so the methods to pause and signal threads must be available to every object.', 'Because they are legacy methods from Java 1.0.', 'C', 'In Java''s concurrency model, threads wait *on a specific object''s monitor*. When a thread executes a synchronized block locked on an object, it can call ''wait()'' on that specific object to release the lock and pause until another thread calls ''notify()'' on that exact same object.');
