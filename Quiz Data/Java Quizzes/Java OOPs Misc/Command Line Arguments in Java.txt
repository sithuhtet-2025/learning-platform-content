INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Command Line Arguments in Java', 'What is the specific parameter signature of the Java ''main'' method designed to intercept arguments passed from the operating system''s command line?', 'public static void main(String args)', 'public static void main(Object[] args)', 'public static void main(String[] args)', 'public static void main(char[][] args)', 'C', 'The JVM passes command line arguments to the Java application as an array of Strings. The standard method signature requires ''String[] args'' (or ''String... args'') to receive this data upon application launch.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Command Line Arguments in Java', 'If you execute a Java program from the terminal using the command ''java MyApp 100 200'', what is the exact data type of the values "100" and "200" when they arrive in the ''args'' array?', 'int', 'Integer', 'String', 'Object', 'C', 'Regardless of whether the user types numbers, booleans, or characters in the terminal, the OS and JVM strictly parse all command line arguments as text. They are stored in the ''args'' array as ''java.lang.String'' objects ("100", "200").');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Command Line Arguments in Java', 'Unlike C and C++, what value is stored at index 0 (''args[0]'') of the command line arguments array in Java?', 'The name of the Java executable (e.g., "java").', 'The name of the compiled class file (e.g., "MyApp").', 'The absolute path to the JVM directory.', 'The very first user-provided argument passed after the class name.', 'D', 'In C/C++, the first element is the name of the program itself. In Java, the JVM strips the "java" command and the class name. ''args[0]'' strictly holds the first actual parameter provided by the user.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Command Line Arguments in Java', 'How do you correctly pass a single argument that contains internal spaces (like a file path: C:\\My Documents\\file.txt) via the command line?', 'By replacing spaces with underscores.', 'By escaping the spaces with backslashes.', 'By enclosing the entire argument within double quotes ("C:\\My Documents\\file.txt").', 'It is impossible; spaces always delimit separate arguments.', 'C', 'The terminal shell natively uses spaces as delimiters to separate arguments. To instruct the shell to treat a string containing spaces as a single, contiguous argument, it must be encapsulated in double quotes.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Command Line Arguments in Java', 'If a Java program is executed without providing any arguments (e.g., simply ''java MyApp''), what is the state of the ''args'' parameter inside the main method?', 'It is strictly ''null'', throwing a NullPointerException if accessed.', 'It is an initialized array with a length of 0.', 'It contains a single empty string element ("").', 'It is bypassed, and the JVM calls a no-argument main method instead.', 'B', 'To prevent unexpected NullPointerExceptions, the JVM guarantees that the ''args'' array is always instantiated. If no arguments are provided, it simply creates an empty array where ''args.length == 0''.');
