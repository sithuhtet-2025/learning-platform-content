INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Compile-Time Polymorphism in Java', 'What is the primary mechanism used to achieve compile-time polymorphism in Java?', 'Method Overriding', 'Method Overloading', 'Method Hiding', 'Interface Implementation', 'B', 'Compile-time polymorphism is achieved through Method Overloading, where multiple methods in the same class share the same name but have different parameter lists.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Compile-Time Polymorphism in Java', 'Why is this specific type of polymorphism referred to as "compile-time"?', 'Because the methods execute faster during the compilation phase.', 'Because the Java compiler determines exactly which method to invoke based on the method signature and arguments provided, binding the call to the method body before the program ever runs.', 'Because it only works with primitive data types verified by the compiler.', 'Because the JVM ignores these methods at runtime.', 'B', 'It is called compile-time polymorphism (or early binding/static binding) because the compiler resolves the method call based strictly on the reference type and the arguments passed at compile time, not on the actual object type at runtime.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Compile-Time Polymorphism in Java', 'When the compiler resolves an overloaded method call, which elements constitute the "method signature" it uses for exact matching?', 'The method name and the return type.', 'The method name, the number of parameters, their data types, and their exact order sequence.', 'The method name and the access modifier.', 'The parameter variable names.', 'B', 'The Java compiler strictly uses the method name combined with the parameter list (number, type, and order of arguments) to distinguish between overloaded methods. Return types and access modifiers are not part of the signature for overload resolution.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Compile-Time Polymorphism in Java', 'Can you successfully overload a method in Java by changing ONLY its return type (e.g., ''public int calculate()'' and ''public double calculate()'')?', 'Yes, the compiler will choose the correct one based on the variable it is assigned to.', 'No, attempting this results in a compile-time error indicating the method is already defined.', 'Yes, but only if one is static and the other is an instance method.', 'No, unless you also change the access modifier.', 'B', 'Changing only the return type is strictly forbidden for method overloading. The compiler cannot determine which method to call if the parameter lists are identical, leading to an ambiguity error during compilation.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Compile-Time Polymorphism in Java', 'Is it possible to overload the ''main'' method in a Java application?', 'No, the JVM strictly forbids multiple main methods.', 'Yes, you can have any number of main methods with different parameter lists, but the JVM will solely use the standard ''public static void main(String[] args)'' as the initial entry point.', 'Yes, and the JVM will randomly pick one to execute.', 'No, it causes a runtime exception.', 'B', 'The ''main'' method can be perfectly overloaded like any other static method. However, when starting the application, the JVM exclusively searches for the exact signature ''main(String[] args)'' to launch the program.');
