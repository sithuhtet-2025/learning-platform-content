INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Difference between Object and Class in Java', 'What is the absolute fundamental distinction between a Class and an Object in Object-Oriented Programming?', 'A Class executes code, whereas an Object strictly stores data.', 'A Class is a physical entity in memory, whereas an Object is a logical entity.', 'A Class is a logical template or blueprint that defines state and behavior, whereas an Object is a concrete, physical instance of that blueprint residing in memory.', 'An Object is used for compiling, whereas a Class is used for execution.', 'C', 'A Class is merely a declarative specification (a blueprint). It defines what an object of its type will look like and what it can do. An Object is the actual realization of that class, occupying physical space in the JVM heap and holding specific data.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Difference between Object and Class in Java', 'At what specific point during the Java application lifecycle is memory officially allocated for a Class versus an Object?', 'Memory is allocated for the Class when it is declared, and for the Object when the program terminates.', 'Memory for the Class is allocated in the Heap when instantiated, and Object memory is allocated in the Stack.', 'Memory is never allocated for a Class. Memory is allocated for an Object in the Heap strictly when the ''new'' keyword is executed at runtime.', 'Both are allocated memory at compile time.', 'C', 'Writing a class declaration consumes zero heap memory for instances. The blueprint is simply loaded by the ClassLoader. Memory is only dynamically allocated on the Heap when you explicitly create an Object using the ''new'' keyword.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Difference between Object and Class in Java', 'Regarding their multiplicity within a running JVM, how do Classes and Objects compare?', 'There can be only one Object per Class.', 'A Class is loaded into memory exactly once per ClassLoader, but you can instantiate an infinite number of distinct Objects from that single Class blueprint.', 'You must define a new Class for every Object you wish to create.', 'A single Object can be instantiated from multiple Classes simultaneously.', 'B', 'The ClassLoader reads the class definition (.class file) into the Metaspace once. From that single definition, you can use the ''new'' operator to generate thousands of independent Object instances, each with its own separate state.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Difference between Object and Class in Java', 'Which structural keyword is exclusively used to define a Class, and which keyword is exclusively used to instantiate an Object?', '''struct'' for Class, ''malloc'' for Object', '''define'' for Class, ''create'' for Object', '''class'' for Class, ''new'' for Object', '''template'' for Class, ''instance'' for Object', 'C', 'In Java syntax, the ''class'' keyword declares the blueprint (e.g., ''public class Car''). The ''new'' keyword actively allocates memory and invokes the constructor to create the instance (e.g., ''Car myCar = new Car();'').');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Difference between Object and Class in Java', 'How do Classes and Objects relate to "state" (variables) and "behavior" (methods)?', 'An Object defines the available state and behavior, while the Class executes them.', 'A Class holds the actual variable values, while the Object holds the method definitions.', 'A Class declares the structure of the state (instance variables) and defines the behavior (methods), whereas the Object holds the actual, specific data values assigned to those variables during runtime.', 'Objects cannot have behavior; they only have state.', 'C', 'The Class acts as the dictionary, stating "every Car has a color variable and a drive() method." The Object is the actual Car where color = "Red", completely independent of another Car object where color = "Blue".');
