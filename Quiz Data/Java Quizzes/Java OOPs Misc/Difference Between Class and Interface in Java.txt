INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Difference Between Class and Interface in Java', 'What is the most fundamental architectural distinction regarding instantiation between a concrete Class and an Interface?', 'A class can only be instantiated once, whereas an interface can be instantiated infinitely.', 'A class can be instantiated using the ''new'' keyword to create objects in memory, whereas an interface is purely an abstract contract and can never be directly instantiated.', 'An interface requires the ''implements'' keyword to be instantiated.', 'There is no difference; both can be instantiated directly.', 'B', 'A concrete class provides full implementations and serves as a blueprint for creating objects. An interface is an abstract contract; you cannot create an object directly from it (e.g., ''new MyInterface()'' is illegal).');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Difference Between Class and Interface in Java', 'How do Classes and Interfaces differ significantly in their approach to Multiple Inheritance in Java?', 'A class can extend multiple classes, but an interface can only extend one interface.', 'A class can extend only one superclass to prevent the Diamond Problem, whereas a class can implement multiple interfaces simultaneously to inherit multiple type definitions.', 'Neither classes nor interfaces support any form of multiple inheritance.', 'An interface can implement multiple classes.', 'B', 'Java strictly prohibits multiple inheritance of state (a class extending multiple classes). However, it allows multiple inheritance of type, meaning a single class can implement an unlimited number of interfaces (e.g., ''class A implements B, C, D'').');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Difference Between Class and Interface in Java', 'What are the implicit, automatic modifiers applied by the Java compiler to any variable declared inside an Interface?', 'private and transient', 'protected and volatile', 'public, static, and final', 'default (package-private)', 'C', 'Any field declared in an interface is implicitly ''public static final'', meaning it acts as a global constant. Interfaces cannot hold mutable instance state (instance variables) like a standard class can.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Difference Between Class and Interface in Java', 'Prior to Java 8, what was the strict restriction on methods declared within an Interface compared to a Class?', 'Interface methods had to be native C++ calls.', 'Interface methods could contain implementation logic, but class methods could not.', 'All methods in an interface were implicitly ''public'' and ''abstract'', strictly containing no body (no implementation logic), whereas classes contained concrete method bodies.', 'Interface methods were implicitly private.', 'C', 'Historically, an interface was purely a contract. It declared "what" a class should do, but provided zero implementation of "how" to do it. Classes provided the actual method bodies. (Note: Java 8 introduced ''default'' and ''static'' methods to interfaces).');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Difference Between Class and Interface in Java', 'Which keywords correctly define the inheritance relationships between Classes and Interfaces?', 'A class ''implements'' a class, an interface ''extends'' an interface.', 'A class ''extends'' a class, a class ''implements'' an interface, and an interface ''extends'' another interface.', 'An interface ''implements'' a class.', 'A class ''extends'' an interface.', 'B', 'The ''extends'' keyword is used when entities of the same type inherit from each other (Class -> Class, Interface -> Interface). The ''implements'' keyword crosses the boundary, used specifically when a Class signs the contract to fulfill an Interface.');
