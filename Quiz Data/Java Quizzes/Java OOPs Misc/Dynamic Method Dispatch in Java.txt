INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Dynamic Method Dispatch in Java', 'What is the precise definition of Dynamic Method Dispatch in the Java runtime architecture?', 'The process of the compiler resolving overloaded methods based on parameter signatures.', 'The mechanism by which a call to an overridden instance method is resolved at runtime based on the actual object type, rather than the reference variable type at compile time.', 'The automatic garbage collection of dynamically created methods.', 'The process of converting a child class reference into a parent class reference.', 'B', 'Dynamic Method Dispatch is the core mechanism enabling Runtime Polymorphism. When an overridden method is called through a superclass reference, the JVM looks at the actual object sitting in the heap at runtime to determine which version of the method to execute.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Dynamic Method Dispatch in Java', 'Which two Object-Oriented principles are absolutely mandatory prerequisites for Dynamic Method Dispatch to occur?', 'Encapsulation and Abstraction', 'Method Overloading and Static Binding', 'Inheritance and Method Overriding', 'Interfaces and Abstract Classes', 'C', 'Dynamic dispatch relies entirely on a parent-child relationship (Inheritance) where the child class provides a specific implementation of a method already defined in the parent class (Method Overriding).');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Dynamic Method Dispatch in Java', 'Assume ''Animal'' is a superclass of ''Dog'', and both have an overridden ''makeSound()'' method. If you execute ''Animal myPet = new Dog(); myPet.makeSound();'', which method runs and why?', 'The Animal method runs because the reference variable ''myPet'' is of type Animal.', 'The program crashes due to a ClassCastException.', 'Both methods run sequentially.', 'The Dog method runs because, despite the reference type being Animal, the actual object instantiated in memory is a Dog, and the JVM dynamically dispatches the call to the object''s overridden method.', 'D', 'This is the essence of "Upcasting" and dynamic dispatch. The compiler checks if ''makeSound()'' exists in ''Animal'' to allow compilation, but the JVM executes the ''Dog'' version at runtime because the physical object is a ''Dog''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Dynamic Method Dispatch in Java', 'Does Dynamic Method Dispatch apply to instance variables (data members) in Java?', 'Yes, variables are overridden dynamically just like methods.', 'No, variable access is determined strictly by the reference type at compile time (Static Binding), not by the actual object type at runtime.', 'Yes, but only if the variables are declared as private.', 'No, because variables cannot be inherited.', 'B', 'In Java, methods are overridden, but variables are only "hidden". If a superclass and subclass have a variable with the same name, accessing it via a superclass reference will ALWAYS yield the superclass variable''s value, regardless of the actual object type. Dynamic dispatch applies strictly to instance methods.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Dynamic Method Dispatch in Java', 'Can Dynamic Method Dispatch be used to invoke a method that is defined purely in the subclass but does NOT exist in the superclass, using a superclass reference?', 'Yes, the JVM will dynamically find the method in the subclass.', 'No, the Java compiler will throw an error because the method signature does not exist in the reference type''s class contract.', 'Yes, if you use the @Override annotation.', 'No, it will throw a NoSuchMethodException at runtime.', 'B', 'A superclass reference strictly restricts your access to only the methods declared in the superclass. Even if the actual object is a subclass containing new, unique methods, you cannot call them without explicitly downcasting the reference first.');
