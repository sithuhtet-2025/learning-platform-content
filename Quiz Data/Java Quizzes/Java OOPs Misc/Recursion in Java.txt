INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Recursion in Java', 'What is the fundamental requirement for a recursive method to prevent an infinite loop and an eventual StackOverflowError?', 'A loop counter.', 'A base case.', 'A try-catch block.', 'A static variable.', 'B', 'Every recursive function must have a base case (a terminating condition) that evaluates to true without making further recursive calls. Without it, the method calls itself infinitely, exhausting the thread''s call stack memory.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Recursion in Java', 'When a Java method calls itself recursively, what happens in the JVM memory architecture?', 'A new thread is spawned.', 'The existing local variables are overwritten.', 'A new stack frame containing local variables and the return address is pushed onto the thread''s execution stack.', 'The object is cloned on the heap.', 'C', 'Each recursive call generates a brand new stack frame on the execution stack. This frame holds the specific local variables, parameters, and return address for that specific invocation, ensuring the states remain isolated across deep recursive dives.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Recursion in Java', 'Which of the following problems is classically best suited for a recursive algorithm rather than a flat iterative one?', 'Finding the maximum value in a flat array.', 'Iterating through a standard ArrayList.', 'Traversing a tree data structure or calculating the Fibonacci sequence.', 'Reading a file line by line.', 'C', 'Recursion elegantly models problems that are inherently self-similar or hierarchical, such as exploring branches of a tree structure, navigating directories, or mathematical recurrence relations like factorials and Fibonacci.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Recursion in Java', 'What is "Tail Recursion" and how does the standard Java compiler (javac) handle it?', 'It is when the recursive call is the absolute last operation in the method. Java heavily optimizes it to reuse the same stack frame.', 'It is when recursion starts at the end of an array. Java does not support it.', 'It is when the recursive call is the final statement, but unlike C++, the standard Java compiler does NOT natively optimize tail recursion, still resulting in distinct stack frames.', 'It is recursion using a while loop.', 'C', 'Tail recursion occurs when the recursive call is the final action. While languages like Scala or C++ optimize this to prevent stack growth (Tail Call Optimization), standard Java (JVM) currently does not natively perform this, meaning it still consumes linear stack space.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Recursion in Java', 'What exception is uniquely associated with excessive or infinite recursion in Java?', 'OutOfMemoryError', 'StackOverflowError', 'RecursiveMethodException', 'InfiniteLoopException', 'B', 'Because every recursive call pushes a new frame onto the finite thread stack, failing to reach a base case quickly depletes this specific memory area, causing the JVM to explicitly throw a ''StackOverflowError''.');
