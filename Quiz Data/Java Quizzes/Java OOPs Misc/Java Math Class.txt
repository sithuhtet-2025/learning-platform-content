INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Math Class', 'What architectural pattern does the ''java.lang.Math'' class follow regarding instantiation and method access?', 'It is an abstract class that must be extended to use its methods.', 'It requires instantiation using the ''new'' keyword to access its non-static mathematical methods.', 'Its constructor is private, preventing instantiation, and all its mathematical methods and constants (like PI) are declared as ''public static''.', 'It is an interface that classes must implement.', 'C', 'The ''Math'' class acts strictly as a utility class. To prevent meaningless instantiation (an object of type "Math" makes no logical sense), its constructor is private. All functions are accessed statically (e.g., ''Math.max(a, b)'').');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Math Class', 'What is the precise range of values generated by the ''Math.random()'' method?', 'An integer between 0 and 100.', 'A double value greater than or equal to 0.0 and strictly less than 1.0.', 'A double value greater than 0.0 and less than or equal to 1.0.', 'Any valid positive or negative double value.', 'B', 'The ''Math.random()'' method returns a pseudorandom ''double'' with a positive sign. The value is mathematically defined to be >= 0.0 and < 1.0. If you need a different range, you must multiply and cast the result.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Math Class', 'What are the resulting values of ''Math.ceil(-3.14)'' and ''Math.floor(-3.14)'', respectively?', '-4.0 and -3.0', '-3.14 and -3.14', '-3.0 and -4.0', '3.0 and 4.0', 'C', '''Math.ceil()'' rounds upwards toward positive infinity, making -3.14 become -3.0. ''Math.floor()'' rounds downwards toward negative infinity, pushing -3.14 down to -4.0.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Math Class', 'Which method should be used to calculate a number raised to the power of another number (e.g., x to the power of y)?', 'Math.power(x, y)', 'Math.exp(x, y)', 'Math.pow(x, y)', 'x ^ y', 'C', 'The ''Math.pow(double a, double b)'' method returns the value of the first argument raised to the power of the second argument. The ''^'' operator in Java is the bitwise XOR operator, not an exponentiation operator.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Math Class', 'What is the specific purpose of the ''Math.abs()'' method?', 'To return the absolute (positive) mathematical value of the argument.', 'To calculate the absolute difference between two numbers.', 'To cast a decimal number into an absolute integer.', 'To determine if a number is even or odd.', 'A', 'The ''Math.abs()'' method strips the negative sign from a number if it exists. If the argument is positive or zero, it returns the argument unchanged. If it is negative, it returns its positive negation.');
