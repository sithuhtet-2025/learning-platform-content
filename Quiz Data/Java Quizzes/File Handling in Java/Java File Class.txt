INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java File Class', 'What does the ''file.exists()'' method evaluate?', 'Whether the file is currently open in another program.', 'Whether the abstract pathname represented by the File object points to an actual, physical file or directory on the host OS file system.', 'Whether the Java application has permission to read the file.', 'Whether the file has been modified recently.', 'B', 'Because a ''File'' object simply encapsulates a path string, instantiating it does not guarantee the file exists. The ''exists()'' method queries the OS to verify if the path maps to a real entity on the disk.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java File Class', 'What exact data is returned when you invoke the ''length()'' method on a ''File'' object representing a standard file?', 'The number of lines of text in the file.', 'The total allocated block size on the hard drive.', 'The size of the file in exact bytes.', 'The length of the filename string.', 'C', 'The ''length()'' method returns the physical size of the file in bytes as a ''long''. If the file does not exist, it returns 0. Note: Calling this on a directory returns an unspecified, system-dependent value, not the sum of its contents.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java File Class', 'Which method is used to retrieve an array of ''File'' objects representing all the files and subdirectories immediately contained within a specific directory?', 'file.getContents()', 'file.listFiles()', 'file.readDirectory()', 'file.list()', 'B', 'If the ''File'' object represents a directory, ''listFiles()'' returns an array of ''File'' objects for the items inside it. The related method ''list()'' simply returns an array of String names instead of File objects.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java File Class', 'What is the fundamental difference between the ''getPath()'' and ''getAbsolutePath()'' methods in the ''File'' class?', 'They return the same string, but ''getAbsolutePath()'' guarantees cross-platform compatibility.', '''getPath()'' returns the exact string path used to construct the File object (which may be relative), whereas ''getAbsolutePath()'' resolves the path against the current working directory to return the full, unambiguous path from the filesystem root.', '''getAbsolutePath()'' resolves symbolic links, while ''getPath()'' does not.', '''getPath()'' only returns the filename without directories.', 'B', 'If you construct ''new File("data.txt")'', ''getPath()'' simply returns "data.txt". However, ''getAbsolutePath()'' calculates its true location, returning something like "C:\\Users\\App\\data.txt" depending on where the JVM was launched.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java File Class', 'Can the legacy ''java.io.File'' class be used to directly read the textual contents of a file?', 'Yes, using the ''file.readText()'' method.', 'No, the ''File'' class strictly handles file paths and OS metadata (creation, deletion, size). It has no capability to open streams or read/write file data payloads.', 'Yes, by casting it to a String object.', 'Yes, but only for files under 1MB in size.', 'B', 'The ''File'' class is strictly a metadata and path-manipulation tool. To actually read or write the data inside the file, you must pass the ''File'' object into an I/O stream like ''FileInputStream'' or ''FileReader''.');
