INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('File Handling in Java', 'In the legacy ''java.io'' package, what does an instance of the ''File'' class actually represent in memory?', 'An open stream to read the contents of a file.', 'An abstract representation of a file or directory pathname, regardless of whether it physically exists on the disk.', 'The binary data payload of a file loaded into the heap.', 'A file descriptor tied directly to the operating system.', 'B', 'The ''java.io.File'' class does not represent the file''s contents or an open connection. It merely acts as an object-oriented wrapper around a pathname string, providing methods to query metadata (like size or permissions) or manipulate the path.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('File Handling in Java', 'Which two abstract base classes form the foundation for all Byte Stream classes in Java (used for reading/writing raw binary data like images or compiled code)?', 'Reader and Writer', 'FileInputStream and FileOutputStream', 'InputStream and OutputStream', 'DataInput and DataOutput', 'C', '''InputStream'' and ''OutputStream'' are the absolute root abstract classes for all byte-oriented I/O operations in Java, operating on 8-bit bytes.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('File Handling in Java', 'Why were Character Stream classes (like ''FileReader'' and ''FileWriter'') introduced in Java 1.1?', 'To read files faster than byte streams by bypassing the OS cache.', 'To automatically encrypt text files.', 'To handle 16-bit Unicode characters natively, automatically translating between the JVM''s internal characters and the local machine''s character encoding format.', 'To read primitive data types like int and double directly.', 'C', 'Byte streams only handle 8-bit bytes. Because Java natively uses 16-bit Unicode characters, Character Streams (Reader/Writer) were introduced to seamlessly manage text I/O and handle encoding/decoding translations automatically.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('File Handling in Java', 'What type of exception is predominantly thrown by classes in the ''java.io'' package when a file operation fails (e.g., file not found, access denied)?', 'java.lang.RuntimeException', 'java.io.IOException', 'java.lang.SystemException', 'java.nio.file.FileSystemException', 'B', 'Almost all constructors and methods in the ''java.io'' package that interact with the physical file system throw a checked ''IOException'' (or its subclasses, like ''FileNotFoundException''), forcing developers to explicitly catch and handle potential hardware or OS-level failures.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('File Handling in Java', 'Introduced in Java 7, what is the primary architectural advantage of the ''java.nio.file'' (NIO.2) package over the legacy ''java.io.File'' class?', 'It completely eliminates the need to handle checked exceptions.', 'It only supports reading files, not writing them, to enforce security.', 'It provides comprehensive support for file metadata, symbolic links, directory traversal, and utilizes asynchronous, non-blocking I/O operations.', 'It compresses files automatically in memory.', 'C', 'NIO.2 (New I/O) introduced the ''Path'' interface and ''Files'' utility class, providing a massive upgrade over ''java.io.File''. It natively understands symbolic links, allows granular metadata access, handles large directory trees efficiently, and supports modern non-blocking file operations.');
