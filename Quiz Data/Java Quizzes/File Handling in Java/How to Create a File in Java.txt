INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('How to Create a File in Java', 'When using the legacy ''java.io.File'' API, which method atomically creates a new, empty file on the disk?', 'file.create()', 'file.mkdir()', 'file.createNewFile()', 'file.openNew()', 'C', 'The ''createNewFile()'' method attempts to atomically create a new, empty file. Atomicity guarantees that if multiple threads/processes attempt to create the file simultaneously, only one will succeed and the others will be notified of its existence.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('How to Create a File in Java', 'What does ''file.createNewFile()'' return if a file with the specified name already exists at that exact path?', 'It throws a FileAlreadyExistsException.', 'It overwrites the file and returns true.', 'It returns false, leaving the existing file completely untouched.', 'It appends an integer to the filename and returns true.', 'C', 'The ''createNewFile()'' method is non-destructive. If the file already exists, it simply returns ''false'' without modifying or overwriting the current file''s contents.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('How to Create a File in Java', 'What happens automatically if you instantiate ''new FileOutputStream("output.txt")'' and the file "output.txt" does NOT currently exist on the disk?', 'The JVM throws a FileNotFoundException.', 'The JVM dynamically creates the new file on the disk before opening the stream.', 'It creates the file only in RAM, discarding it upon closure.', 'The compiler throws a syntax error.', 'B', 'Unlike reading from a file (which throws an exception if missing), opening an output stream (like ''FileOutputStream'' or ''FileWriter'') will instruct the operating system to automatically create the physical file if it does not exist, assuming the application has write permissions for the directory.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('How to Create a File in Java', 'Using the modern NIO.2 API, which method is used to create a new file based on a ''Path'' object?', 'Path.createFile()', 'Paths.newFile(Path p)', 'Files.createNewFile(Path p)', 'Files.createFile(Path p)', 'D', 'In the NIO.2 architecture, the ''java.nio.file.Files'' class is a static utility class that handles all physical file operations. You pass the ''Path'' object into ''Files.createFile(path)'' to generate the physical file.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('How to Create a File in Java', 'How does the modern ''Files.createFile(Path)'' method behave differently from the legacy ''File.createNewFile()'' when encountering an existing file?', 'It silently returns false.', 'It automatically overwrites the file with zero bytes.', 'It renames the old file to ".bak".', 'It throws a checked ''java.nio.file.FileAlreadyExistsException''.', 'D', 'NIO.2 uses an exception-driven design for file collisions rather than boolean return values. If you call ''Files.createFile()'' and the target already exists, the JVM explicitly throws a ''FileAlreadyExistsException'', making error handling more robust.');
