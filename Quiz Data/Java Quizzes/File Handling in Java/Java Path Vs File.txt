INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Path Vs File', 'What is the fundamental architectural distinction between ''java.io.File'' and ''java.nio.file.Path''?', '''File'' is a modern interface, while ''Path'' is a legacy abstract class.', '''File'' strictly represents file contents, whereas ''Path'' only represents the string name.', '''File'' is a concrete legacy class tightly coupled to the default OS file system, whereas ''Path'' is a modern, flexible interface that can represent paths in custom file systems (like ZIP archives or memory).', 'They are identical concepts, but ''Path'' is the specific implementation for Windows.', 'C', 'Introduced in Java 7 (NIO.2), ''Path'' is an interface tied to the abstract ''FileSystem'' provider architecture. Unlike ''File'', which only understands the host OS file system, ''Path'' can seamlessly interact with custom, virtual, or network file systems.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Path Vs File', 'How do the ''File'' and ''Path'' APIs fundamentally differ in their approach to error handling during operations?', '''File'' operations throw checked exceptions for every failure, while ''Path'' methods return booleans.', '''File'' methods frequently return ''false'' on failure without explaining why, whereas NIO.2 ''Files'' methods (which consume ''Path'' objects) throw specific, descriptive exceptions (e.g., NoSuchFileException, AccessDeniedException).', 'Both use the exact same error handling mechanisms.', '''Path'' operations cannot fail; they automatically retry.', 'B', 'The legacy ''java.io.File'' class relies heavily on boolean return values (e.g., ''file.delete()'' returns false if it fails). The modern NIO.2 API is exception-driven, providing granular details about exactly why an OS-level file operation failed.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Path Vs File', 'Which method provides the standard, seamless conversion from a legacy ''File'' object to a modern NIO.2 ''Path'' object?', 'File.toPath()', 'Path.fromFile()', 'Files.toPath(File)', 'new Path(File)', 'A', 'To bridge the gap between legacy IO and modern NIO.2, Java added the ''toPath()'' method to the ''java.io.File'' class, instantly converting the legacy path representation into a highly functional ''Path'' interface.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Path Vs File', 'How do ''File'' and ''Path'' differ in their handling of symbolic links (symlinks)?', '''File'' resolves them natively, while ''Path'' ignores them.', 'Both handle them identically.', 'The legacy ''File'' class has virtually no native awareness of symbolic links, often treating them simply as standard files, whereas the NIO.2 ''Path'' and ''Files'' API fully understand, traverse, and manage symbolic links natively.', '''Path'' throws an exception if it encounters a symbolic link.', 'C', 'One of the massive upgrades in NIO.2 is native support for symbolic links. The ''Files'' utility class provides specific methods (like ''Files.isSymbolicLink(Path)'') and options (like ''LinkOption.NOFOLLOW_LINKS'') that the old ''File'' class completely lacked.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Path Vs File', 'Which NIO.2 class serves as the primary utility companion to the ''Path'' interface, containing all the static methods for actually reading, writing, and manipulating the physical files?', 'Paths', 'FileSystems', 'FileChannel', 'Files', 'D', 'The ''Path'' interface only represents the location. The ''java.nio.file.Files'' class acts as the execution engine, housing static utility methods (like ''Files.copy()'', ''Files.delete()'', ''Files.readAllBytes()'') that perform the actual physical I/O operations on the target represented by the ''Path''.');
