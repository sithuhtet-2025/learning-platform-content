INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java FileDescriptor class', 'What exactly does the ''java.io.FileDescriptor'' class represent conceptually within the JVM?', 'An opaque handle or pointer representing a low-level, OS-specific open file, open socket, or byte sink/source.', 'A high-level utility for formatting file names.', 'An encryption key used to lock files.', 'A metadata cache for the ''File'' class.', 'A', 'A ''FileDescriptor'' is a low-level construct. It serves as an opaque handle to the underlying machine-specific structure (like a POSIX file descriptor in Linux or a File Handle in Windows) representing an active, open I/O connection.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java FileDescriptor class', 'How does a developer typically obtain a valid ''FileDescriptor'' object associated with an active file?', 'By calling ''new FileDescriptor("filename.txt")''.', 'By calling ''System.getDescriptor()''.', 'By invoking the ''getFD()'' method on an active ''FileInputStream'', ''FileOutputStream'', or ''RandomAccessFile'' object.', 'By parsing it from the ''java.io.File'' object.', 'C', 'Because the OS manages file handles, you cannot just instantiate a valid one. You must open a stream first. Once the stream successfully connects to the file, you can call ''getFD()'' to retrieve the underlying OS handle.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java FileDescriptor class', 'What critical, hardware-level function does the ''FileDescriptor.sync()'' method perform?', 'It synchronizes the file across a network to a cloud backup.', 'It forces all OS-level memory buffers to flush and synchronize directly with the underlying physical storage device (hard drive), guaranteeing the data is physically committed to the disk.', 'It synchronizes two separate threads trying to write to the same file.', 'It encrypts the file descriptor for safe transfer.', 'B', 'Even when Java calls ''flush()'', the OS might still cache the data in RAM to optimize disk access. Calling ''sync()'' on the ''FileDescriptor'' forces the OS to physically write the bits to the platter/NAND flash, ensuring data survival even in a power loss scenario.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java FileDescriptor class', 'Which standard, pre-defined ''FileDescriptor'' static instance is automatically attached to the console''s standard output stream (System.out)?', 'FileDescriptor.console', 'FileDescriptor.sysout', 'FileDescriptor.out', 'FileDescriptor.stdout', 'C', 'The ''FileDescriptor'' class provides three static fields representing the standard POSIX streams initialized by the JVM: ''in'' (standard input/keyboard), ''out'' (standard output/console), and ''err'' (standard error).');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java FileDescriptor class', 'Can a standard Java application safely instantiate a custom ''FileDescriptor'' using the ''new FileDescriptor()'' constructor to open a new file?', 'Yes, it is the modern way to open files in Java 9+.', 'No, the constructor creates an invalid, unlinked handle. ''FileDescriptor'' objects must be generated and managed internally by the OS and returned via active I/O streams.', 'Yes, but it requires administrative OS privileges.', 'Yes, but only for network sockets.', 'B', 'While the constructor exists, invoking ''new FileDescriptor()'' manually just creates an empty object without a valid OS handle. Valid descriptors are strictly created by the JVM''s native C/C++ code when opening system streams.');
