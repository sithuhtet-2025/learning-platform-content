INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('How to delete a file in Java', 'When using the legacy ''java.io.File'' class, what does the ''delete()'' method return upon execution?', 'It throws an IOException if it fails.', 'It returns the deleted File object.', 'It returns a boolean value: ''true'' if the file was successfully deleted, and ''false'' otherwise.', 'It returns an integer representing the number of bytes freed.', 'C', 'The legacy ''File.delete()'' method does not throw exceptions for standard deletion failures (like the file not existing or lacking permissions). Instead, it quietly returns ''false'', making it the developer''s responsibility to check the boolean result.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('How to delete a file in Java', 'What happens if you invoke ''file.delete()'' on a ''File'' object that represents a directory containing other files or folders?', 'The directory and all its contents are permanently deleted.', 'The JVM moves the directory to the system recycle bin.', 'The method silently returns ''false'', and the directory remains completely untouched.', 'A DirectoryNotEmptyException is immediately thrown.', 'C', 'In Java, a directory cannot be deleted natively unless it is completely empty. Calling ''delete()'' on a populated directory simply fails and returns ''false''. To delete it, you must write a recursive function to delete all nested contents first.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('How to delete a file in Java', 'In the modern NIO.2 API, what happens when ''Files.delete(Path)'' is called but the specified file does not exist on the disk?', 'It silently returns false.', 'It throws a ''NoSuchFileException''.', 'It creates the file and then deletes it.', 'It throws a NullPointerException.', 'B', 'Unlike the legacy API, the modern ''java.nio.file.Files'' utility class uses an exception-driven design. If the target path does not exist, ''Files.delete()'' throws a ''NoSuchFileException'', providing robust error handling and diagnostics.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('How to delete a file in Java', 'Which NIO.2 method should be used to safely attempt a file deletion without throwing an exception if the target file is already missing?', 'Files.deleteSafely(Path)', 'Files.remove(Path)', 'Files.deleteIfExists(Path)', 'Path.destroy()', 'C', 'The ''Files.deleteIfExists(Path)'' method is a convenience utility that checks for the file''s existence before attempting deletion. It returns ''true'' if the file was deleted, and ''false'' if the file didn''t exist, effectively avoiding the ''NoSuchFileException''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('How to delete a file in Java', 'What does the ''file.deleteOnExit()'' method do in the ''java.io'' package?', 'It schedules the file to be deleted by the operating system during the next reboot.', 'It registers the file to be automatically deleted when the JVM terminates normally.', 'It deletes the file immediately and exits the current thread.', 'It deletes the file only if an exception is thrown during execution.', 'B', 'The ''deleteOnExit()'' method requests that the JVM delete the file when the virtual machine terminates normally. This is particularly useful for cleaning up temporary files created during the application''s lifecycle.');
