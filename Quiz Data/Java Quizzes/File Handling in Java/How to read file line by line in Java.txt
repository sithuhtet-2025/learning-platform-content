INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('How to read file line by line in Java', 'Which legacy ''java.io'' class provides the most efficient, buffered approach for reading text from a file line by line?', 'FileReader', 'FileInputStream', 'BufferedReader', 'LineReader', 'C', 'While a ''FileReader'' reads character by character, wrapping it in a ''BufferedReader'' drastically improves performance by loading large chunks of the file into a memory buffer. It provides the ''readLine()'' method for convenient line-by-line extraction.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('How to read file line by line in Java', 'What does the ''BufferedReader.readLine()'' method return when it successfully reads a line, and what does it return when the End of File (EOF) is reached?', 'It returns the string including the newline character, and throws an EOFException at the end.', 'It returns the string without the newline character, and returns ''null'' at the end of the file.', 'It returns the string with the newline character, and returns an empty string ("") at the end.', 'It returns a byte array, and returns null at the end.', 'B', 'The ''readLine()'' method reads characters until it encounters a line terminator (\\n or \\r). It returns the string excluding the terminator characters. When the stream reaches the absolute end of the file, it safely returns ''null''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('How to read file line by line in Java', 'Which modern NIO.2 method introduced in Java 8 is the most memory-efficient way to process a massive, multi-gigabyte log file line by line using functional programming?', 'Files.readAllLines(Path)', 'Files.readString(Path)', 'Files.lines(Path)', 'Paths.stream(Path)', 'C', 'The ''Files.lines(Path)'' method returns a lazily populated ''Stream<String>''. Because it loads lines into memory one at a time on demand as the stream is consumed, it is incredibly memory-efficient for processing massive files without causing OutOfMemoryErrors.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('How to read file line by line in Java', 'What is the critical architectural risk of using ''Files.readAllLines(Path)'' on very large files?', 'It randomly drops lines to save space.', 'It locks the file permanently, preventing other applications from reading it.', 'It reads the entire file synchronously and stores every single line as a separate String object inside a ''List<String>'' in RAM, which can easily trigger an OutOfMemoryError.', 'It automatically converts the file to a byte array.', 'C', 'The ''readAllLines()'' method is a convenience function intended strictly for small files. For large files, loading the entire contents into Heap memory simultaneously as a List of Strings will exhaust the JVM''s memory limit.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('How to read file line by line in Java', 'Which class from the ''java.util'' package can be used to parse a file line by line while natively supporting regular expression delimiters and tokenization?', 'StringTokenizer', 'Scanner', 'PatternReader', 'RegexStream', 'B', 'The ''java.util.Scanner'' class can take a ''File'' object as input. Using ''scanner.hasNextLine()'' and ''scanner.nextLine()'', it reads files line by line, but it also provides advanced capabilities to split those lines into specific primitive data types or tokens using regex.');
