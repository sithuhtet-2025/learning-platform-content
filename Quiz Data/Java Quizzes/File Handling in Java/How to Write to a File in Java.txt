INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('How to Write to a File in Java', 'Which legacy ''java.io'' class is primarily designed to write streams of characters (text) directly to a file system?', 'FileOutputStream', 'FileWriter', 'DataOutputStream', 'StringWriter', 'B', 'The ''FileWriter'' class is specifically designed to write 16-bit Unicode character streams to a file. For writing raw 8-bit byte streams (like images), ''FileOutputStream'' should be used.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('How to Write to a File in Java', 'How do you configure a ''FileWriter'' to append new text to the end of an existing file, rather than completely overwriting its current contents?', 'By calling ''writer.setAppendMode(true)''.', 'By passing the string "append" as the second argument to the constructor.', 'By passing the boolean flag ''true'' as the second argument to the constructor: ''new FileWriter("file.txt", true)''.', 'By using the ''FileAppender'' class instead.', 'C', 'The ''FileWriter'' constructor accepts a boolean ''append'' parameter. If set to ''true'', the OS file pointer is positioned at the end of the file, allowing subsequent write operations to safely append data without destroying existing contents.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('How to Write to a File in Java', 'Why is it considered a critical best practice to wrap a ''FileWriter'' inside a ''BufferedWriter'' (e.g., ''new BufferedWriter(new FileWriter(file))'')?', 'To automatically encrypt the text before saving.', 'To drastically reduce the number of expensive physical disk I/O operations by accumulating characters in a memory array and writing them to the disk in large chunks.', 'To allow the writing of primitive data types like integers directly.', 'To automatically close the file when the thread terminates.', 'B', 'Writing directly to a disk character-by-character is extremely slow. A ''BufferedWriter'' temporarily stores characters in a local RAM buffer. Once the buffer is full (or ''flush()'' is called), the entire block is written to the physical disk at once, vastly improving performance.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('How to Write to a File in Java', 'Introduced in Java 11, which NIO.2 utility method provides the most concise, one-line solution to write a complete String directly to a file?', 'Paths.writeText(Path, String)', 'Files.writeString(Path, CharSequence)', 'File.write(String)', 'Files.writeAllText(Path, String)', 'B', 'The ''Files.writeString()'' method drastically simplifies file I/O. It creates the file, writes the provided CharSequence (String) using UTF-8 encoding by default, and automatically closes the stream, all in a single line of code.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('How to Write to a File in Java', 'If an application needs to write raw binary data (such as a compiled PDF or a PNG image) to a file, which stream class must be utilized?', 'FileWriter', 'PrintWriter', 'FileOutputStream', 'BinaryWriter', 'C', 'Binary data is not strictly character-based and can contain arbitrary byte sequences. ''FileOutputStream'' is required because it handles raw 8-bit bytes without attempting to apply character encoding translations that would corrupt binary files.');
