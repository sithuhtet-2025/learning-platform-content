INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java FilePermission Class', 'What is the primary architectural purpose of the ''java.io.FilePermission'' class?', 'To permanently alter the operating system''s file access rights (like chmod).', 'To represent access control logic for files and directories, utilized by the Java SecurityManager to grant or deny operations at runtime.', 'To check if a file is currently locked by another application.', 'To encrypt files so only authorized users can read them.', 'B', 'The ''FilePermission'' class does not alter OS-level permissions. Instead, it is a core component of Java''s internal security architecture. It represents the permissions (read, write, execute, delete) granted to the executing Java code by the active SecurityManager.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java FilePermission Class', 'When instantiating a ''FilePermission'', which string accurately represents the actions required to allow both reading data and deleting a file?', '"read:delete"', '"r,d"', '"read,delete"', '"read | delete"', 'C', 'The actions string in a ''FilePermission'' constructor is a comma-separated list of keywords. The valid keywords are strictly "read", "write", "execute", and "delete". Thus, "read,delete" is the correct syntax.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java FilePermission Class', 'In the path argument of a ''FilePermission'', what does the wildcard "-" (dash) signify when appended to a directory path (e.g., "/documents/-")?', 'It grants permission only to the directory itself, not its contents.', 'It grants permission to all files located directly in that directory, but not in any subdirectories.', 'It grants permission to all files in that directory, and recursively to all files in all of its subdirectories.', 'It revokes all permissions for that directory.', 'C', 'The "-" wildcard is recursive. It specifies that the permission applies to the specified directory and comprehensively encompasses every file and subdirectory nested within it, at any depth.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java FilePermission Class', 'In contrast to the dash, what does the wildcard "*" (asterisk) signify in a ''FilePermission'' path (e.g., "/documents/*")?', 'It grants access to all files within the specified directory, but strictly does NOT recurse into subdirectories.', 'It grants access to all files ending with a specific extension.', 'It represents all files on the entire hard drive.', 'It grants access exclusively to hidden files.', 'A', 'The "*" wildcard is non-recursive. It applies the specified permissions to all files immediately located within that specific directory, but it stops there and does not extend to files inside any nested subdirectories.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java FilePermission Class', 'What does a ''FilePermission'' with the special path "<<ALL FILES>>" represent?', 'It throws an IllegalArgumentException.', 'It represents all files in the current working directory.', 'It strictly targets the JVM''s installation directory.', 'It grants the specified actions to every single file accessible by the underlying file system.', 'D', 'The special target string "<<ALL FILES>>" is an absolute wildcard. It instructs the SecurityManager to apply the requested permissions (e.g., "read") universally across every file and directory the host operating system can access.');
