INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('How to Read and Print All Files From a Zip File in Java', 'Which stream class is designed to sequentially read the contents and metadata of a ZIP archive?', 'java.util.zip.ZipReader', 'java.util.zip.ZipInputStream', 'java.io.ZipFileInputStream', 'java.util.zip.GZIPInputStream', 'B', 'The ''ZipInputStream'' class acts as a filter over an underlying ''FileInputStream''. It understands the ZIP file format and can iterate through the archive, inflating compressed files on the fly.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('How to Read and Print All Files From a Zip File in Java', 'What method must be repeatedly called on a ''ZipInputStream'' to navigate through the individual files stored inside the ZIP archive?', 'readNext()', 'nextFile()', 'getNextEntry()', 'openEntry()', 'C', 'A ZIP file is composed of multiple entries. The ''getNextEntry()'' method scans the stream for the next local file header, positions the stream pointer at the start of that file''s compressed data, and returns a ''ZipEntry'' object containing its metadata.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('How to Read and Print All Files From a Zip File in Java', 'When using a ''while'' loop with ''ZipInputStream.getNextEntry()'', what return value signals that the entire ZIP archive has been completely read?', 'An EOFException is thrown.', 'The method returns an empty ZipEntry object.', 'The method returns null.', 'The method returns -1.', 'C', 'The ''getNextEntry()'' method returns a valid ''ZipEntry'' object as long as there are files left in the archive. When the end of the ZIP directory is reached, it safely returns ''null'', breaking the iteration loop.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('How to Read and Print All Files From a Zip File in Java', 'How do you extract the string filename of the current file being processed from a ''ZipEntry'' object?', 'entry.getFileName()', 'entry.getName()', 'entry.toString()', 'entry.getPath()', 'B', 'The ''getName()'' method of the ''ZipEntry'' class returns the path and filename of the entry as it is stored within the ZIP archive (e.g., "documents/report.pdf").');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('How to Read and Print All Files From a Zip File in Java', 'When reading the byte data from a ''ZipInputStream'' using the standard ''read(byte[] buffer)'' method, when does it return -1?', 'When the entire ZIP file has been fully read.', 'When an encrypted file is encountered.', 'When the end of the CURRENT ''ZipEntry'' data payload is reached, preventing it from accidentally reading into the next file''s header.', 'It never returns -1; it throws an exception.', 'C', 'The ''ZipInputStream.read()'' method restricts your reading boundary. It reads the inflated data for the currently active ''ZipEntry'' and returns -1 strictly when the end of that specific file''s data is reached. You must then call ''closeEntry()'' and ''getNextEntry()'' to read the next file.');
