INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('How to Create a Zip File in Java', 'Which primary class from the ''java.util.zip'' package is responsible for writing files into a ZIP archive format?', 'ZipWriter', 'ZipArchiveOutputStream', 'ZipOutputStream', 'GZIPOutputStream', 'C', 'The ''java.util.zip.ZipOutputStream'' class implements an output stream filter for writing files in the standard ZIP file format, handling the complex compression and archiving headers automatically.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('How to Create a Zip File in Java', 'When constructing a ZIP file, what must you do immediately before you start writing the actual byte data of a file into the ''ZipOutputStream''?', 'Call ''stream.compress()''.', 'Create and add a new ''ZipEntry'' object using ''putNextEntry()''.', 'Declare the file size using ''stream.setFileSize()''.', 'Encode the file name in Base64.', 'B', 'A ZIP file is a collection of entries. Before you can write a file''s data into the archive, you must explicitly signal the start of a new file by creating a ''ZipEntry'' (representing the file''s path and metadata inside the zip) and calling ''putNextEntry()'' on the stream.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('How to Create a Zip File in Java', 'After you have finished writing the byte data for a specific file into the ''ZipOutputStream'', what critical method must be called before adding the next file?', 'stream.flush()', 'stream.closeEntry()', 'stream.finish()', 'stream.commit()', 'B', 'The ''closeEntry()'' method explicitly tells the ''ZipOutputStream'' that the current file''s data stream is complete. It finalizes the checksums and headers for that specific entry, preparing the stream to accept a new ''putNextEntry()'' call.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('How to Create a Zip File in Java', 'If you want to create a directory structure INSIDE the resulting ZIP file (e.g., placing "data.txt" inside a folder named "docs"), how is this achieved using ''ZipEntry''?', 'By calling ''stream.makeDirectory("docs")'' before adding the file.', 'By passing a string with a forward slash representing the path directly into the ZipEntry constructor, such as ''new ZipEntry("docs/data.txt")''.', 'By creating a ZipEntry for the folder and appending the file to it as a child object.', 'ZIP files do not support internal directory structures in Java.', 'B', 'Directories within a ZIP archive are simply represented by the string path provided to the ''ZipEntry''. Using a forward slash (/) in the entry name (e.g., "folder/file.txt") automatically constructs the logical directory path inside the archive viewer.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('How to Create a Zip File in Java', 'What does the ''ZipOutputStream.setLevel(int level)'' method control?', 'The maximum number of files allowed in the archive.', 'The level of encryption applied to the files (0-9).', 'The compression ratio, ranging from 0 (NO_COMPRESSION) to 9 (BEST_COMPRESSION).', 'The buffer size used for reading the input files.', 'C', 'The ''setLevel()'' method allows developers to balance CPU time versus archive size. Level 0 stores the data without compression, while Level 9 forces the Deflater algorithm to use maximum CPU cycles to achieve the smallest possible file size.');
