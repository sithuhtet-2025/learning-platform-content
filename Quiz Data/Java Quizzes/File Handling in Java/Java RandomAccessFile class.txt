INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java RandomAccessFile class', 'What unique operational capability completely distinguishes ''RandomAccessFile'' from standard stream classes like ''FileInputStream'' and ''FileOutputStream''?', 'It automatically encrypts the data randomly.', 'It generates random numbers and writes them to the disk.', 'It allows non-sequential, bidirectional reading and writing within the same file by manually manipulating an internal file pointer to any exact byte offset.', 'It reads files significantly faster using multi-threading.', 'C', 'Standard streams are strictly sequential; you read or write bytes in a continuous forward flow. ''RandomAccessFile'' behaves like a large array of bytes in memory. You can jump the file pointer backwards or forwards to instantly read or overwrite specific blocks of data anywhere in the file.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java RandomAccessFile class', 'When instantiating a ''RandomAccessFile(String name, String mode)'', which mode string enables both read and write access, while also guaranteeing that every write is synchronously flushed to the underlying physical storage device?', '"rw"', '"rws"', '"rwd"', 'Both "rws" and "rwd"', 'D', 'The "rw" mode allows reading and writing but relies on the OS cache. "rws" (synchronizes file content and metadata) and "rwd" (synchronizes file content only) force the OS to immediately flush every write operation directly to the physical disk hardware, ensuring data survival in a crash.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java RandomAccessFile class', 'Which method is used to move the internal file pointer to a specific absolute byte position within the file?', 'setPointer(long pos)', 'moveTo(long pos)', 'seek(long pos)', 'skipBytes(int n)', 'C', 'The ''seek(long pos)'' method acts as the core navigational tool of the ''RandomAccessFile''. It sets the absolute file-pointer offset, measured in bytes from the absolute beginning of the file, at which the next read or write operation will occur.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java RandomAccessFile class', 'What happens if you invoke ''seek()'' to move the pointer significantly beyond the current physical end of the file, and then immediately perform a ''write()'' operation?', 'The JVM throws an EOFException.', 'The JVM throws an IOException indicating out of bounds.', 'The file simply grows to accommodate the new write. The undefined gap between the old end-of-file and the new data is automatically filled with null bytes (0).', 'The file wraps around and overwrites from the beginning.', 'C', 'Seeking past the end of the file is perfectly legal. It does not instantly change the file size. However, the moment you write data at that distant offset, the OS expands the file size. The unwritten gap is filled with zero-bytes (nulls), creating what is known in operating systems as a "sparse file".');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java RandomAccessFile class', 'Which method should you invoke to determine the exact absolute byte position where the internal file pointer is currently resting?', 'getOffset()', 'position()', 'getFilePointer()', 'current()', 'C', 'The ''getFilePointer()'' method returns the current offset of the internal file pointer, as a ''long'' integer, representing the exact number of bytes from the absolute beginning of the file.');
