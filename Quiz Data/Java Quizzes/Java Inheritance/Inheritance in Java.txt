INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Inheritance in Java', 'Which OOP principle allows a new Java class to acquire the fields and methods of an existing class?', 'Encapsulation', 'Polymorphism', 'Inheritance', 'Abstraction', 'C', 'Inheritance is the mechanism in Java by which one class is allowed to inherit the features (state and behavior) of another class, establishing a parent-child relationship.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Inheritance in Java', 'What specific keyword is used in a Java class signature to establish an inheritance relationship with a parent class?', 'implements', 'inherits', 'super', 'extends', 'D', 'The ''extends'' keyword indicates that a class is derived from a superclass. For example, ''class Dog extends Animal'' establishes that Dog inherits from Animal.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Inheritance in Java', 'Which of the following types of inheritance is strictly NOT supported by Java through classes?', 'Single Inheritance', 'Multilevel Inheritance', 'Hierarchical Inheritance', 'Multiple Inheritance', 'D', 'To prevent ambiguity and the "Diamond Problem," Java classes do not support Multiple Inheritance (where one class directly extends more than one parent class). Java uses interfaces to achieve multiple inheritance of type.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Inheritance in Java', 'Which class serves as the ultimate, implicit root superclass for every single Java class ever created?', 'java.lang.Class', 'java.util.Object', 'java.lang.Object', 'java.lang.System', 'C', 'If a class does not explicitly extend another class, the Java compiler implicitly makes it extend ''java.lang.Object''. Therefore, ''Object'' sits at the very top of the Java class hierarchy.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Inheritance in Java', 'While a subclass inherits the members of its superclass, which specific elements are NEVER inherited by the subclass?', 'public methods', 'Constructors', 'protected variables', 'static methods', 'B', 'Constructors are not considered standard members of a class and are strictly not inherited by subclasses. A subclass must define its own constructors, which internally invoke the parent''s constructor using ''super()''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Inheritance in Java', 'What happens if a subclass attempts to access a ''private'' variable declared in its superclass?', 'It accesses it successfully because inheritance bypasses access modifiers.', 'It causes a compile-time error because private members are strictly hidden from subclasses.', 'The JVM automatically promotes the variable to ''protected''.', 'It accesses the variable, but only for reading, not writing.', 'B', 'Private members are strictly confined to the class in which they are declared. While the subclass object technically contains the memory allocation for that private state, it cannot access it directly and must use public/protected getter/setter methods provided by the parent.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Inheritance in Java', 'In a scenario with Multilevel Inheritance (Class C extends Class B, and Class B extends Class A), what is the exact execution order of constructors when ''new C()'' is invoked?', 'C, then B, then A', 'A, then B, then C', 'C, then A, then B', 'They all execute simultaneously.', 'B', 'Constructor chaining works top-down. The child constructor calls the parent constructor via ''super()'' before executing its own body. Therefore, the highest ancestor (A) completes first, followed by the intermediate parent (B), and finally the child (C).');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Inheritance in Java', 'What is "Cyclic Inheritance", and how does the Java compiler handle it?', 'It occurs when a class extends itself (e.g., ''class A extends A''), and the compiler allows it for recursive objects.', 'It occurs when a class implements multiple interfaces, and the compiler handles it using default methods.', 'It occurs when classes form an inheritance loop (e.g., ''class A extends B'', and ''class B extends A''). The Java compiler strictly forbids this and throws a compilation error.', 'It occurs when a parent class inherits from Object, and it is handled natively.', 'C', 'Cyclic inheritance creates a logical impossibility where a class is its own ancestor. The Java compiler detects cyclical structures during the compilation phase and immediately rejects them with a "cyclic inheritance involving class" error.');
