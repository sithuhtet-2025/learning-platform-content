INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Aggregation in Java', 'In Object-Oriented Design, what type of relationship is established by Aggregation?', 'IS-A relationship', 'HAS-A relationship', 'WAS-A relationship', 'USES-A relationship', 'B', 'Aggregation is a specialized form of object association that represents a "HAS-A" relationship, where a complex object is built by aggregating simpler, component objects (e.g., a Department HAS-A Professor).');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Aggregation in Java', 'What is the defining characteristic of the lifecycle dependency in an Aggregation relationship?', 'The contained object is strictly dependent on the container; if the container is destroyed, the contained object is also destroyed.', 'Both objects share the exact same memory address and are destroyed simultaneously.', 'It represents a weak association. The contained object can exist independently of the container. If the container is destroyed, the contained object continues to exist.', 'The container cannot be destroyed until all contained objects are destroyed first.', 'C', 'Aggregation represents a weak "HAS-A" relationship. For example, if a University closes (container destroyed), the Professors (contained objects) do not cease to exist; they can be employed elsewhere. Their lifecycles are independent.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Aggregation in Java', 'Which of the following code snippets best demonstrates Aggregation?', 'class Car extends Engine { }', 'class Car { Engine e; Car(Engine e) { this.e = e; } }', 'class Car { void start() { Engine e = new Engine(); e.run(); } }', 'class Car implements Engine { }', 'B', 'Passing an existing ''Engine'' object into the ''Car'' constructor via a reference variable correctly demonstrates Aggregation. The ''Engine'' is created externally and passed in, meaning the ''Engine'' can survive even if the ''Car'' object is eventually garbage collected.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Aggregation in Java', 'What is the primary architectural benefit of utilizing Aggregation in a Java application?', 'It promotes Code Reusability by allowing existing classes to be used as components in new classes without the tight coupling and rigid hierarchy of inheritance.', 'It executes code faster than inheritance.', 'It allows multiple inheritance of state.', 'It automatically synchronizes all method calls within the aggregated objects.', 'A', 'Aggregation favors composition over inheritance. It allows developers to assemble complex objects from existing, independent modules, maximizing code reuse while keeping the system loosely coupled and highly maintainable.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Aggregation in Java', 'How does Aggregation fundamentally differ from Composition in Java OOP?', 'Aggregation is a "HAS-A" relationship, while Composition is an "IS-A" relationship.', 'Aggregation implies a strong ownership where the child cannot exist without the parent (Composition), whereas Composition implies a weak relationship.', 'Composition implies strict, exclusive ownership and coincident lifecycles (if the parent dies, the child dies). Aggregation implies a weak relationship with independent lifecycles.', 'They are identical concepts with different terminology.', 'C', 'Composition is a strong "HAS-A" (e.g., a House and a Room; destroy the house, the rooms are destroyed). Aggregation is a weak "HAS-A" (e.g., a Department and a Teacher; destroy the department, the teacher still exists).');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Aggregation in Java', 'If class ''Library'' aggregates a list of ''Book'' objects, how is this typically represented in the Java class definition?', 'By making ''Library'' a subclass of ''Book''.', 'By declaring a field such as ''List<Book> books'' inside the ''Library'' class.', 'By making ''Book'' a static inner class of ''Library''.', 'By implementing the ''Book'' interface in the ''Library'' class.', 'B', 'A one-to-many aggregation is typically modeled by declaring a collection (like a List or Array) of the component object type as an instance variable within the container class.');
