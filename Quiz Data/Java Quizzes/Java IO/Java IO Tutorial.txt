INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java IO Tutorial', 'What is the absolute fundamental, architectural bifurcation that splits the entire ''java.io'' package into two distinct, non-overlapping super-hierarchies?', 'Classes that read files vs. classes that read networks.', 'Classes that operate entirely in RAM vs. classes that hit the hard drive.', 'Classes that are designed to handle unformatted, 8-bit binary data (Byte Streams), versus classes that are exclusively engineered to handle decoded, 16-bit Unicode text (Character Streams).', 'Classes that are thread-safe vs. classes that are not.', 'C', 'This is the golden rule of Java I/O. If you are handling an image, a PDF, or a compiled ''.class'' file, you MUST use the ''InputStream/OutputStream'' family. If you are handling human-readable text (like a ''.txt'' or ''.json'' file), you MUST use the ''Reader/Writer'' family.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java IO Tutorial', 'What highly ubiquitous structural Design Pattern dominates the ''java.io'' package, allowing developers to cleanly chain behaviors together (e.g., new BufferedReader(new InputStreamReader(new FileInputStream(...))))?', 'The Singleton Pattern', 'The Factory Method Pattern', 'The Decorator Pattern', 'The Observer Pattern', 'C', 'Java I/O is the textbook example of the Decorator pattern. You start with a concrete data source (''FileInputStream''), wrap it in a translator (''InputStreamReader''), and wrap that in a performance enhancer (''BufferedReader''). Each layer dynamically adds capabilities without modifying the core stream.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java IO Tutorial', 'What are the three static, globally available standard streams implicitly provided to every JVM by the host operating system, accessible via the ''java.lang.System'' class?', 'System.file, System.network, System.memory', 'System.in (Standard Input), System.out (Standard Output), and System.err (Standard Error).', 'System.read, System.write, System.append', 'System.keyboard, System.monitor, System.printer', 'B', 'These are the OS-level defaults. ''System.in'' is an ''InputStream'' typically bound to the keyboard. ''System.out'' and ''System.err'' are ''PrintStreams'' typically bound to the console/terminal display. They are uniquely always open and do not require instantiation.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java IO Tutorial', 'What happens architecturally if a Java thread successfully writes 500 bytes to an underlying OS file, but then the JVM suddenly crashes before the ''close()'' or ''flush()'' methods are invoked on the stream?', 'The OS automatically flushes the bytes.', 'The bytes are written, but the file is marked as corrupt.', 'Because stream output is typically buffered in volatile RAM for performance, any data residing in those intermediate buffers at the exact moment of the crash is permanently destroyed and never reaches the physical disk.', 'The JVM recovers the data upon reboot.', 'C', 'This highlights the critical importance of I/O lifecycle management. Buffering sacrifices immediate disk persistence for speed. Failing to cleanly close or flush a stream guarantees data loss during abrupt process termination.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java IO Tutorial', 'Introduced in Java 7, what is the modern, architecturally mandated syntax for declaring and initializing streams to absolutely guarantee they are closed and OS resources are released, regardless of Exceptions?', 'The ''finally'' block pattern.', 'The ''Runtime.getRuntime().addShutdownHook()'' pattern.', 'The ''try-with-resources'' statement (e.g., ''try (FileReader fr = new FileReader("f.txt")) { ... }'').', 'The ''AutoCloseable'' interface decorator.', 'C', 'Prior to Java 7, developers had to write messy ''finally'' blocks with nested try-catches just to close a file. ''Try-with-resources'' automatically and deterministically invokes the ''close()'' method on any object implementing ''AutoCloseable'' the exact millisecond the execution block exits.');
