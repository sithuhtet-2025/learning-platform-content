INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java ByteArrayOutputStream Class', 'What is the primary architectural purpose of the ''java.io.ByteArrayOutputStream'' class?', 'To write binary data directly to a physical file on the hard drive.', 'To act as a dynamic, automatically expanding byte buffer in Heap memory, capturing output stream data so it can later be retrieved as a single, contiguous byte array.', 'To compress byte streams in real-time.', 'To convert 16-bit characters into 8-bit bytes.', 'B', 'A ''ByteArrayOutputStream'' does not connect to a file, network, or external device. It writes strictly to an internal ''byte[]'' array. As you push data into it, it dynamically resizes itself (similar to an ArrayList) to accommodate the incoming bytes without overflowing.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java ByteArrayOutputStream Class', 'When a thread has finished writing data to the stream, which precise method is invoked to extract the accumulated binary payload from memory?', 'stream.getArray()', 'stream.flush()', 'stream.toByteArray()', 'stream.readAllBytes()', 'C', 'The ''toByteArray()'' method is the core extraction mechanism. It allocates a brand new ''byte[]'' array perfectly sized to match the exact number of valid bytes written to the stream, copies the data into it, and returns the reference to the calling thread.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java ByteArrayOutputStream Class', 'What highly efficient utility method allows a developer to dump the entire contents of a ''ByteArrayOutputStream'' directly into a completely different, external ''OutputStream'' (like a ''FileOutputStream'')?', 'stream.pipeTo(OutputStream out)', 'stream.copy(OutputStream out)', 'stream.writeTo(OutputStream out)', 'stream.transfer(OutputStream out)', 'C', 'The ''writeTo(OutputStream out)'' method is heavily optimized. It takes the target ''OutputStream'' as a parameter and writes the entire internal byte array directly to it in a single, massive block transfer, avoiding the overhead of extracting the array manually first.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java ByteArrayOutputStream Class', 'What is the operational consequence of invoking the ''close()'' method on a ''ByteArrayOutputStream'' instance?', 'It throws an IOException on any subsequent write attempts.', 'It locks the internal array, making it strictly read-only.', 'It immediately flushes the bytes to the hard drive.', 'It has absolutely zero effect. The class holds no physical OS resources, and methods can still be called on it safely even after ''close()'' has been invoked.', 'D', 'Because the stream only manipulates volatile RAM in the JVM heap, there are no OS-level file descriptors or network sockets to close. The ''close()'' method is empty, existing solely to satisfy the polymorphic contract of the ''java.io.OutputStream'' abstract class.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java ByteArrayOutputStream Class', 'What is the default initial capacity of the internal memory array utilized by a ''ByteArrayOutputStream'' if instantiated using the no-argument constructor?', '16 bytes', '32 bytes', '1024 bytes', '8192 bytes', 'B', 'By default, the JVM initializes the internal ''byte[]'' array to an extremely conservative size of 32 bytes. If a developer expects to write megabytes of data into the stream, they should use the overloaded constructor ''new ByteArrayOutputStream(int size)'' to pre-allocate memory and prevent massive reallocation CPU penalties.');
