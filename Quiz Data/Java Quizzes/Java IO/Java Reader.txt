INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Reader', 'What is the fundamental architectural purpose of the abstract ''java.io.Reader'' class?', 'To read unformatted, raw 8-bit binary data from the file system.', 'To serve as the abstract base class for reading streams of 16-bit Unicode characters.', 'To automatically parse text files into Java Objects.', 'To read serialized objects from a network socket.', 'B', 'The Reader class is the root of the character-stream hierarchy in Java I/O. It provides the architectural blueprint for reading 16-bit text, contrasting with the 8-bit byte-oriented InputStream.'),
('Java Reader', 'When creating a custom subclass of the abstract ''Reader'' class, which two methods are explicitly mandated by the API to be implemented by the developer?', 'read() and close()', 'read(char[] cbuf) and flush()', 'read(char[] cbuf, int off, int len) and close()', 'readAll() and close()', 'C', 'According to the Java API documentation, a subclass must minimally implement only ''read(char[], int, int)'' and ''close()''. All other read methods (like the no-arg ''read()'') are structurally defined in the base class to delegate to this core array-based read method.'),
('Java Reader', 'What is the precise return type and mathematical meaning of the zero-argument ''reader.read()'' method?', 'It returns a ''char'' representing the read character.', 'It returns a ''String'' containing the current line of text.', 'It returns a ''byte'' representing the 8-bit ASCII value.', 'It returns an ''int'' representing the 16-bit Unicode character (0 to 65535), or exactly -1 if the absolute End of File (EOF) is reached.', 'D', 'Because a valid Java character spans from 0x0000 to 0xFFFF (0 to 65535), returning a ''char'' would not leave any mathematical room to cleanly signal EOF. By returning a 32-bit ''int'', it safely reserves -1 as the universal EOF indicator.'),
('Java Reader', 'What is the operational function of the ''reader.ready()'' method?', 'It initializes the stream buffer.', 'It blocks the current thread until the entire file is loaded into RAM.', 'It returns a boolean indicating whether the next ''read()'' operation is guaranteed to execute immediately without blocking the thread for physical I/O.', 'It checks if the file has read permissions.', 'C', 'The ''ready()'' method is a defensive check for non-blocking I/O operations. If it returns true, data is available in the underlying buffer. If false, calling ''read()'' might suspend the thread while it waits for the OS/network to deliver data.'),
('Java Reader', 'Which interface does the ''Reader'' class implement that explicitly allows it to be used gracefully within a modern try-with-resources statement?', 'java.io.Serializable', 'java.lang.AutoCloseable', 'java.lang.Readable', 'java.util.Iterator', 'B', 'The ''Reader'' class implements ''java.io.Closeable'' (which extends ''java.lang.AutoCloseable''). This architectural contract ensures that the JVM will autonomously invoke the ''close()'' method the moment the execution flow exits a try-with-resources block.'),
('Java Reader', 'If a ''Reader'' does not natively support the ''mark()'' and ''reset()'' operations, what exception is strictly thrown when ''mark()'' is invoked?', 'UnsupportedOperationException', 'IOException', 'NotSupportedException', 'IllegalStateException', 'B', 'Unlike the standard Collections framework which relies heavily on ''UnsupportedOperationException'', the I/O framework strictly adheres to throwing ''IOException'' for unsupported stream operations, such as attempting to mark a stream that lacks buffering capabilities.');