INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java - PipedWriter', 'In the context of Java concurrency, what is the absolute defining architectural purpose of the ''java.io.PipedWriter'' class?', 'To compress character data before writing it to a physical file.', 'To provide a secure, synchronized character-based communication conduit between two distinct, concurrently executing threads within the same JVM.', 'To write strings directly to the system console.', 'To establish a network socket connection to a remote server.', 'B', 'Pipes are the classical producer-consumer mechanism in Java I/O. A ''PipedWriter'' allows a "Producer" thread to write characters into a stream, which are instantly buffered and made available to be read by a completely different "Consumer" thread.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java - PipedWriter', 'What strict structural prerequisite must be met before a thread can successfully push character data into a ''PipedWriter''?', 'The writer must be explicitly mapped to a physical disk file.', 'The thread must acquire a global monitor lock.', 'The ''PipedWriter'' MUST be logically connected to a corresponding ''java.io.PipedReader'' object to form a complete, closed-loop pipeline.', 'The data must be converted to raw bytes.', 'C', 'A pipe cannot function openly. Writing into an unconnected ''PipedWriter'' immediately triggers an ''IOException''. It must be paired with a ''PipedReader'' using either the constructor (e.g., ''new PipedWriter(pipedReader)'') or the explicit ''connect()'' method.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java - PipedWriter', 'What happens architecturally if the internal buffer of the connected ''PipedReader'' becomes completely full, but the producer thread continues attempting to write data into the ''PipedWriter''?', 'The JVM throws a BufferOverflowException.', 'The new characters silently overwrite the oldest unread characters.', 'The ''PipedWriter'' gracefully blocks and suspends the execution of the producer thread until the consumer thread reads some data, freeing up space in the buffer.', 'The JVM dynamically doubles the size of the pipe''s buffer.', 'C', 'This is the essence of synchronized thread communication. The pipe inherently manages flow control. If the consumer is slow, the producer is automatically blocked to prevent data loss or memory exhaustion.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java - PipedWriter', 'What specific runtime error occurs if the consumer thread reading from the connected ''PipedReader'' unexpectedly dies or terminates while the producer thread is actively writing to the ''PipedWriter''?', 'The pipeline is automatically rerouted to System.out.', 'The writer safely ignores the dead reader and continues executing.', 'The JVM throws an ''IOException'' stating that the pipe is "broken", immediately terminating the communication channel.', 'The writer throws a NullPointerException.', 'C', 'The JVM continuously monitors the health of the threads operating on both ends of the pipe. If the reading thread dies (loses its JVM thread reference) while the writer is active, the pipe is declared "broken" because the written data can mathematically never be consumed.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java - PipedWriter', 'Is it considered safe and architecturally sound for multiple distinct producer threads to write into a single ''PipedWriter'' instance simultaneously?', 'Yes, the pipe automatically interleaves the characters perfectly.', 'No, the Java architecture explicitly dictates that a single pipe connection should be strictly utilized by one writer thread and one reader thread. Multiple writers will cause unpredictable data interleaving, corruption, and potential deadlocks.', 'Yes, but only if they are in the same ThreadGroup.', 'No, unless the strings are encrypted.', 'B', 'Pipes are strictly 1-to-1 communication channels. While they are thread-safe regarding the buffer, the pipe has no mechanism to distinguish which thread wrote which character. If Thread A writes "Hello" and Thread B writes "World", the reader might receive "HWeolrllod".');
