INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('FileWriter', 'What is the core architectural purpose of the ''java.io.FileWriter'' class?', 'To compress large datasets into zip archives.', 'To write raw binary byte streams (like audio or video) to a physical disk.', 'To act as a convenience class that encodes Java 16-bit characters into bytes and writes them to a physical file on the operating system.', 'To generate automated log files dynamically.', 'C', 'Extending ''OutputStreamWriter'', a ''FileWriter'' takes characters generated by the Java application, applies a character encoding (like UTF-8) to convert them into bytes, and transmits those bytes to an underlying ''FileOutputStream'' pointing to a physical file on the disk.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('FileWriter', 'If you execute the constructor ''new FileWriter("log.txt")'' but the file "log.txt" does NOT currently exist in the directory, what is the default behavior of the JVM?', 'It throws a FileNotFoundException.', 'It throws an IOException.', 'It automatically instructs the operating system to create a brand new, empty file with that specific name, assuming the JVM possesses the necessary directory write permissions.', 'It returns a null FileWriter reference.', 'C', 'Unlike ''FileReader'' which crashes if the file is missing, ''FileWriter'' is designed to create outputs. If the target file doesn''t exist, the OS provisions a new file. If the file DOES exist, the default constructor silently deletes its entire contents and overwrites it from scratch.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('FileWriter', 'How can a developer instruct the ''FileWriter'' to safely ADD new text to the end of a pre-existing file, rather than completely destroying and overwriting the file''s current contents?', 'By calling ''writer.appendMode(true)''.', 'By passing a boolean ''true'' as the second argument to the constructor: ''new FileWriter("log.txt", true)''.', 'By using the ''java.io.FileAppender'' class instead.', 'By seeking to the end of the file using a cursor.', 'B', 'The overloaded constructor ''FileWriter(File file, boolean append)'' controls the OS-level file writing mode. Passing ''true'' opens the file in append mode. The OS file pointer immediately jumps to the absolute end of the file, ensuring new writes are safely concatenated.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('FileWriter', 'When a thread executes ''writer.write("Hello")'', why might the string "Hello" NOT immediately appear in the physical text file on the hard drive?', 'Because FileWriter is asynchronous by default.', 'Because the JVM automatically delays all writes until the program terminates.', 'Because the characters are temporarily held in an internal memory buffer. They are not physically flushed to the OS disk until the buffer fills up, or the developer explicitly invokes the ''flush()'' or ''close()'' methods.', 'Because the file is locked by the antivirus.', 'C', 'Writing to a disk is incredibly slow. To optimize performance, ''OutputStreamWriter'' (the parent of FileWriter) buffers outgoing bytes. If your application crashes before the buffer is flushed, the recent data is permanently lost. Always call ''flush()'' or use try-with-resources to guarantee disk delivery.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('FileWriter', 'Similar to its reader counterpart, what major capability was finally added to the ''FileWriter'' constructors in Java 11, solving a decade-long architectural limitation?', 'The ability to write binary data.', 'The ability to accept a specific ''java.nio.charset.Charset'' as a constructor argument (e.g., ''StandardCharsets.UTF_8''), guaranteeing cross-platform encoding safety without needing to manually wrap a ''FileOutputStream''.', 'The ability to write to remote network drives natively.', 'The ability to compress output streams natively.', 'B', 'Before Java 11, ''FileWriter'' strictly used the host machine''s default encoding, making it dangerous for writing cross-platform UTF-8 files. Java 11 finally introduced constructors like ''FileWriter(String fileName, Charset charset, boolean append)'' to enforce strict encoding standards.');
