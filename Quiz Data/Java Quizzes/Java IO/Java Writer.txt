INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Writer', 'What is the core architectural mandate of the abstract ''java.io.Writer'' class?', 'To serve as the abstract base class for outputting raw 8-bit binary streams.', 'To serve as the foundational abstract class for all character-oriented output streams, handling the transmission of 16-bit Unicode text.', 'To act as a physical buffer for the OS hard drive.', 'To serialize Java Objects into JSON format.', 'B', 'The Writer class is the exact inverse of the Reader class. It forms the root hierarchy for all classes designed to encode and write 16-bit character streams, standing in contrast to the byte-oriented OutputStream hierarchy.'),
('Java Writer', 'When a developer creates a custom concrete subclass extending ''Writer'', which three specific methods must absolutely be implemented to fulfill the minimum structural contract?', 'write(int c), flush(), and close()', 'write(String str), append(), and close()', 'write(char[] cbuf, int off, int len), flush(), and close()', 'print(), println(), and close()', 'C', 'The API documentation states that subclasses must implement only the array-based ''write(char[], int, int)'', ''flush()'', and ''close()''. All other convenience write methods in the base class (like writing Strings or single chars) are internally wired to delegate to this core array method.'),
('Java Writer', 'What significant convenience capability does the ''Writer'' class structurally provide that its byte-oriented counterpart (OutputStream) strictly lacks?', 'It supports automatic GZIP compression.', 'It provides overloaded ''write()'' methods that directly accept standard ''java.lang.String'' objects, seamlessly handling the character extraction internally.', 'It can write data directly to the CPU cache.', 'It operates without throwing IOExceptions.', 'B', 'Because OutputStreams handle raw binary, they cannot comprehend Strings natively (requiring string.getBytes()). Writers operate in the character domain, so the API natively includes ''write(String str)'' and ''write(String str, int off, int len)'', drastically improving developer ergonomics.'),
('Java Writer', 'What is the catastrophic architectural consequence of a Java application writing text to a ''Writer'' buffer but failing to invoke ''flush()'' or ''close()'' before the process terminates?', 'The operating system locks the file permanently.', 'The JVM automatically flushes the buffers during its shutdown hook.', 'Any characters trapped in the volatile internal memory buffers are instantly annihilated and permanently lost, resulting in truncated or completely empty output files.', 'The text is written, but character encoding is corrupted.', 'C', 'Writers (especially buffered implementations) hold data in RAM to optimize disk I/O. If the stream is not instructed to forcefully push (flush) this RAM down to the physical OS descriptor before the JVM exits, the data is lost.'),
('Java Writer', 'Which core interface does the ''Writer'' class implement that explicitly permits method chaining (e.g., ''writer.append("A").append("B")'')?', 'java.io.Serializable', 'java.lang.Appendable', 'java.lang.CharSequence', 'java.util.Collection', 'B', 'The Writer class implements the ''java.lang.Appendable'' interface. This mandates the implementation of ''append(CharSequence csq)'', which structurally returns the Writer instance itself (''this''), allowing for highly readable, fluent API method chaining.'),
('Java Writer', 'To ensure thread safety, the ''Writer'' base class defines a protected instance variable used to synchronize internal operations. What is the type and name of this variable?', 'protected Thread syncThread;', 'protected final Object lock;', 'protected volatile boolean isLocked;', 'protected ReentrantLock bufferLock;', 'B', 'The base Writer class defines ''protected Object lock;''. By default, all synchronized operations within the Writer use this specific object as their monitor lock. Subclasses can leverage this variable to maintain strict synchronization consistency across multiple threads.');