INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Scanner Class in Java', 'What is the absolute primary architectural advantage of utilizing ''java.util.Scanner'' over a standard ''BufferedReader''?', 'Scanner executes disk reads significantly faster than BufferedReader.', 'Scanner automatically decrypts AES-256 encoded text files.', 'Scanner possesses a built-in tokenizer that natively parses incoming text, splitting it by delimiters and seamlessly converting the tokens directly into Java primitives (e.g., int, double).', 'Scanner guarantees thread-safety across multiple CPU cores.', 'C', 'A BufferedReader strictly returns raw Strings. A Scanner does the algorithmic heavy lifting of tokenization and primitive parsing, converting a file containing "10 20.5 true" into an int, a double, and a boolean effortlessly.'),
('Scanner Class in Java', 'By default, what internal evaluation mechanism does the ''Scanner'' class utilize to identify boundaries and separate individual tokens?', 'It splits strictly on comma (,) characters.', 'It splits strictly on the newline character (\\n).', 'It splits based on the ''Character.isWhitespace()'' algorithm, effectively treating all spaces, tabs, and carriage returns as token delimiters.', 'It evaluates based on fixed-width byte limits.', 'C', 'Unless explicitly reconfigured, the Scanner utilizes a regular expression matching any standard whitespace character to define token boundaries, making it highly robust for parsing space-separated or multi-line primitive lists.'),
('Scanner Class in Java', 'What catastrophic parsing bug frequently occurs when a developer sequentially executes ''scanner.nextInt()'' followed immediately by ''scanner.nextLine()''?', 'The Scanner throws an InputMismatchException.', 'The Scanner closes the underlying input stream.', 'The ''nextLine()'' method intercepts the leftover, unconsumed newline (Enter key) character residing in the buffer from the integer input, instantly returning an empty string and appearing to "skip" user input.', 'The JVM completely deadlocks.', 'C', 'This is a notorious Java parsing trap. ''nextInt()'' consumes only the numerical digits. The Enter key (\\n) remains in the input buffer. The subsequent ''nextLine()'' sees the \\n, assumes the user submitted a blank line, and returns immediately.'),
('Scanner Class in Java', 'If a text file contains the exact word "Apple" but the application code executes ''scanner.nextDouble()'', what is the definitive runtime result?', 'It returns 0.0.', 'It returns Double.NaN.', 'The JVM immediately throws a ''java.util.InputMismatchException''.', 'The JVM throws a ''NumberFormatException''.', 'C', 'The Scanner is strongly typed during extraction. If the target token cannot be mathematically translated into the requested primitive type according to the active locale settings, it strictly throws an InputMismatchException.'),
('Scanner Class in Java', 'How does the ''Scanner'' class architecturally handle checked IOExceptions originating from its underlying input source (e.g., a disconnected file)?', 'It propagates them outward, forcing the developer to write try-catch blocks.', 'It crashes the JVM to prevent data corruption.', 'It intentionally suppresses checked IOExceptions to simplify the API. Developers must manually query the ''scanner.ioException()'' method to discover if a silent failure occurred.', 'It automatically retries the read operation 3 times.', 'C', 'Unlike the rigid java.io classes, Scanner was designed for ease of use. It hides IOExceptions behind its boolean methods (like ''hasNext()''). If a physical read failure occurs, ''hasNext()'' simply returns false, and the underlying exception is cached for retrieval via ''ioException()''.'),
('Scanner Class in Java', 'Which method provides the defensive architectural pattern necessary to prevent volatile parsing exceptions before attempting data extraction?', 'scanner.validate()', 'scanner.canRead()', 'scanner.hasNext...() (e.g., hasNextInt(), hasNextDouble())', 'scanner.peek()', 'C', 'Defensive programming with a Scanner mandates checking before reading. Executing ''if(scanner.hasNextInt()) { int val = scanner.nextInt(); }'' mathematically guarantees that the next token is a valid integer, completely eliminating the risk of an InputMismatchException.'),
('Scanner Class in Java', 'How can a developer instruct a ''Scanner'' to parse a Comma-Separated Values (CSV) string instead of relying on default whitespace separation?', 'By invoking ''scanner.useDelimiter(",")'' or passing a specific Regex pattern.', 'By invoking ''scanner.setCSVMode(true)''.', 'By casting the string to a CSVReader object.', 'It is impossible; Scanner strictly processes whitespace.', 'A', 'The ''useDelimiter(String pattern)'' method allows developers to inject custom Regular Expressions into the Scanner''s tokenization engine. Setting it to "," instantly transforms the Scanner into a robust, primitive-aware CSV parser.');
