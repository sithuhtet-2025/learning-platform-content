INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('BufferedReader', 'What is the primary architectural performance advantage of wrapping a standard ''FileReader'' inside a ''java.io.BufferedReader''?', 'It automatically translates 8-bit bytes into 16-bit Unicode characters.', 'It drastically reduces the number of expensive, physical OS-level disk reads by fetching a large chunk of characters into a memory array (buffer) all at once, serving subsequent read requests directly from RAM.', 'It encrypts the text stream using AES-256.', 'It allows the file to be read backwards.', 'B', 'Physical disk I/O is incredibly slow compared to CPU/RAM speeds. A ''FileReader'' reads character-by-character from the OS. A ''BufferedReader'' pulls a massive block of characters (e.g., 8KB) into its internal array in a single disk hit, making subsequent ''read()'' calls instantaneous memory operations.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('BufferedReader', 'Which highly specialized and frequently utilized method is introduced by the ''BufferedReader'' class that is NOT present in the base ''Reader'' abstract class?', 'readAll()', 'nextString()', 'readLine()', 'fetchLine()', 'C', 'The ''readLine()'' method is the hallmark of the ''BufferedReader''. It scans the internal memory buffer for line terminators (\\n or \\r) and extracts entire strings of text line-by-line, vastly simplifying text file processing.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('BufferedReader', 'What is the exact return value of ''BufferedReader.readLine()'' when the stream reaches the absolute End of File (EOF)?', 'An empty string ("").', 'It throws an EOFException.', 'It returns -1.', 'It safely returns ''null''.', 'D', 'While the standard ''read()'' method returns -1 at EOF, the ''readLine()'' method returns a String. To definitively signal that no more lines exist in the file, it evaluates to ''null''. This is typically used as the termination condition in a ''while'' loop.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('BufferedReader', 'When constructing a ''BufferedReader'', what happens if you do not explicitly specify a buffer size (e.g., ''new BufferedReader(new FileReader("file.txt"))'')?', 'The JVM dynamically sizes the buffer based on the file''s total size.', 'It defaults to a single-character buffer, defeating its purpose.', 'It uses the JVM''s default buffer size, which is typically 8192 characters (16KB of memory), sufficient for most standard text processing tasks.', 'It throws an IllegalArgumentException.', 'C', 'The default constructor provisions a standard, hardcoded array size (usually 8KB). If you are processing lines known to be exceptionally long, you can use the overloaded constructor ''new BufferedReader(Reader in, int sz)'' to manually allocate a larger array.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('BufferedReader', 'In a modern Java architecture (Java 7+), what is the most robust way to ensure a ''BufferedReader'' and its underlying streams are safely closed, even if an ''IOException'' occurs during reading?', 'Calling ''reader.close()'' inside a ''finalize()'' method.', 'Using the try-with-resources statement: ''try (BufferedReader br = new BufferedReader(...)) { ... }''', 'Calling ''System.gc()''.', 'Leaving it open; the OS closes it automatically when the thread dies.', 'B', 'The try-with-resources syntax guarantees that the ''close()'' method is deterministically invoked on the ''BufferedReader'' (which internally cascades the ''close()'' command down to the underlying ''FileReader'') the moment the block exits, effectively eliminating resource leaks.');
