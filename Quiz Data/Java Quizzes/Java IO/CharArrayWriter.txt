INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('CharArrayWriter', 'What is the primary architectural function of the ''java.io.CharArrayWriter'' class?', 'To write characters directly to a physical text file.', 'To act as a dynamic, automatically expanding character buffer in Heap memory, capturing stream output so it can later be retrieved as an array or a String.', 'To write binary data to a network socket.', 'To compress text data before writing it.', 'B', 'A ''CharArrayWriter'' does not connect to a file or network. It acts as an internal memory sink. You write characters to it just like any other ''Writer'', but under the hood, it simply appends them to a hidden ''char[]'' array, resizing the array seamlessly when it gets full.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('CharArrayWriter', 'What happens internally when a thread attempts to write 100 characters into a ''CharArrayWriter'' whose internal backing array is currently full?', 'It throws an IOException indicating buffer overflow.', 'It silently drops the new characters to prevent a crash.', 'The JVM automatically allocates a brand new, larger char array in memory, copies the existing data over, and then appends the new characters, preventing any overflow.', 'It blocks the thread until memory is freed.', 'C', 'This is the core feature of the class. It behaves like an ''ArrayList'' for characters. You do not need to know the final size of the data beforehand; the ''CharArrayWriter'' handles all the complex array reallocation and copying mechanics natively.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('CharArrayWriter', 'Once a process has finished writing data to a ''CharArrayWriter'', which two methods are primarily used to extract the accumulated text payload?', 'getArray() and getString()', 'toCharArray() and toString()', 'readAll() and output()', 'flush() and close()', 'B', 'The ''toCharArray()'' method returns a newly allocated ''char[]'' array containing an exact copy of the accumulated data. The ''toString()'' method provides a highly convenient way to instantly convert the entire buffer into a standard ''java.lang.String'' object.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('CharArrayWriter', 'Which unique utility method does ''CharArrayWriter'' provide that allows you to dump its entire internal buffer directly into a completely different, external ''Writer'' stream (like a ''FileWriter'')?', 'writer.pipeTo(Writer out)', 'writer.transfer(Writer out)', 'writer.writeTo(Writer out)', 'writer.copy(Writer out)', 'C', 'The ''writeTo(Writer out)'' method is highly efficient. It takes the target ''Writer'' stream as an argument and directly writes its internal character array into that stream, essentially acting as a fast memory-to-stream data pump.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('CharArrayWriter', 'Similar to its reader counterpart, what is the architectural reality of calling ''flush()'' or ''close()'' on a ''CharArrayWriter''?', 'It physically flushes the RAM to the hard drive.', 'It immediately throws a stream closed exception.', 'It locks the internal array, making it read-only.', 'They execute without throwing errors, but they have absolutely no operational effect because the class only interacts with Heap memory, not OS file descriptors or network buffers.', 'D', 'Because there is no physical OS destination (like a disk platter or a TCP connection), there is nothing to "flush", and no external resource handle to "close". The methods are present purely to satisfy the polymorphic contract of the ''java.io.Writer'' interface.');
