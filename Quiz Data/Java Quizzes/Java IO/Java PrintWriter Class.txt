INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java PrintWriter class', 'What is the primary advantage of PrintWriter over PrintStream for internationalized applications?', 'PrintWriter is faster at writing binary data.', 'PrintWriter is a character stream, making it more robust for handling various Unicode character encodings compared to the byte-oriented PrintStream.', 'PrintWriter automatically translates text into different languages.', 'PrintWriter does not require a destination file.', 'B', 'Because PrintWriter is part of the Reader/Writer (Character) hierarchy, it is superior for handling text across different international charsets.'),
('Java PrintWriter class', 'Does PrintWriter support the auto-flush feature?', 'No, it must be flushed manually every time.', 'Yes, but only when the println(), printf(), or format() methods are invoked.', 'Yes, it flushes after every single character.', 'Only when writing to a network socket.', 'B', 'Unlike PrintStream, PrintWriter''s auto-flush is only triggered by specific methods (println, printf, format), not by simply writing a newline character or a byte array.'),
('Java PrintWriter class', 'How does PrintWriter handle IOExceptions during its print() operations?', 'It throws them as checked exceptions.', 'It crashes the JVM.', 'It suppresses them, requiring the use of checkError() to detect failures.', 'It logs them to System.err automatically.', 'C', 'Like PrintStream, PrintWriter aims for convenience and does not force the developer to handle checked IOExceptions for every print call.'),
('Java PrintWriter class', 'What happens if you use the PrintWriter(OutputStream out) constructor?', 'It creates a writer that uses the default character encoding to bridge to the output stream.', 'It throws a compile-time error because it only accepts Writers.', 'It automatically enables auto-flush.', 'It converts the output stream into a binary file.', 'A', 'This constructor internally creates an OutputStreamWriter to act as the bridge between the character-based PrintWriter and the byte-based OutputStream.'),
('Java PrintWriter class', 'Which method is used to write a single character to a PrintWriter?', 'append()', 'put()', 'write(int c)', 'emit()', 'C', 'While print() is commonly used, PrintWriter inherits the write(int c) method from its parent Writer class to output a single character.'),
('Java PrintWriter class', 'Which of the following is NOT a constructor for PrintWriter?', 'PrintWriter(File file)', 'PrintWriter(String fileName)', 'PrintWriter(Writer out)', 'PrintWriter(URL url)', 'D', 'PrintWriter has constructors for File, String (filename), Writer, and OutputStream, but not directly for a URL.');