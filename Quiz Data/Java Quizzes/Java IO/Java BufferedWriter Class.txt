INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java BufferedWriter Class', 'What is the primary architectural function of the ''java.io.BufferedWriter'' class?', 'To convert binary files into readable text.', 'To provide a highly efficient, in-memory buffer for character streams (text), accumulating characters before passing them to an underlying Writer (like FileWriter) to minimize physical disk encoding operations.', 'To write text to the system console asynchronously.', 'To automatically encrypt text data before saving it.', 'B', 'Exactly analogous to its byte-stream counterpart, the ''BufferedWriter'' prevents the underlying ''FileWriter'' from hitting the hard drive for every single character. It catches the characters in an 8192-character array, writing them to disk in massive, highly efficient blocks.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java BufferedWriter Class', 'Which highly specialized utility method is introduced by the ''BufferedWriter'' class to resolve cross-platform text formatting issues?', 'writer.carriageReturn()', 'writer.enter()', 'writer.nextLine()', 'writer.newLine()', 'D', 'Different operating systems use different characters to represent a line break (e.g., ''\r\n'' on Windows, ''\n'' on Linux/macOS). The ''newLine()'' method queries the JVM''s ''line.separator'' system property and injects the exact, correct line break for the specific host OS, ensuring perfectly formatted text files.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java BufferedWriter Class', 'What is the catastrophic architectural consequence of failing to call ''flush()'' or ''close()'' on a ''BufferedWriter'' before the Java application process terminates?', 'The file becomes permanently locked by the OS.', 'The JVM will automatically flush it during the shutdown sequence.', 'Any characters that were still residing in the internal RAM buffer waiting to be written are instantly destroyed and permanently lost, resulting in truncated or completely empty output files.', 'The text is written, but the file extension is corrupted.', 'C', 'A buffer is purely volatile RAM. If the application finishes its logic and exits without explicitly instructing the ''BufferedWriter'' to drain its RAM to the physical disk, the OS reclaims the memory and the pending text is annihilated.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java BufferedWriter Class', 'Because a ''BufferedWriter'' requires an underlying ''Writer'' to function, which of the following is the standard architectural pattern for instantiating it to write text to a file?', 'new BufferedWriter("output.txt")', 'new BufferedWriter(new File("output.txt"))', 'new BufferedWriter(new FileWriter("output.txt"))', 'new BufferedWriter(new FileOutputStream("output.txt"))', 'C', 'The ''BufferedWriter'' uses the Decorator design pattern. It cannot communicate with the OS file system itself. It must wrap a concrete character stream, specifically a ''FileWriter'', which handles the actual physical byte encoding and file descriptor management.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java BufferedWriter Class', 'What is the default capacity of the internal memory array utilized by a standard ''BufferedWriter''?', '1024 characters (2 KB in memory)', '4096 characters (8 KB in memory)', '8192 characters (16 KB in memory, as each Java char is 2 bytes)', '16384 characters (32 KB in memory)', 'C', 'Like most buffered I/O classes in Java, the default internal array size is 8192. Because the ''BufferedWriter'' stores 16-bit Unicode characters (not 8-bit bytes), an 8192-character buffer actually consumes roughly 16 Kilobytes of JVM Heap space.');
