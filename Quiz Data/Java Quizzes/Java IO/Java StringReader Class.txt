INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java StringReader Class', 'What is the foundational architectural data source that a ''java.io.StringReader'' extracts its characters from?', 'A local physical file system.', 'A dynamically allocated char array.', 'A pre-existing, immutable ''java.lang.String'' object residing in the JVM heap, allowing it to be parsed sequentially using standard Reader APIs.', 'A network socket.', 'C', 'A ''StringReader'' requires no OS I/O. It takes a standard String and wraps it in a stream architecture, allowing libraries or parsers that demand a ''Reader'' input to seamlessly consume a block of text held strictly in RAM.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java StringReader Class', 'What is the exact behavioral outcome of invoking the ''close()'' method on a ''StringReader''?', 'It destroys the underlying String object.', 'It throws an IOException.', 'It releases the OS file descriptor.', 'It has no operational effect whatsoever on the system, as the class holds no physical native resources requiring closure.', 'D', 'Because the data source is just a String in the JVM, there are no OS-level handles to release. The ''close()'' method is practically a no-op, provided strictly to fulfill the abstract ''java.io.Reader'' interface contract.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java StringReader Class', 'Does the ''StringReader'' class inherently support the ''mark(int readAheadLimit)'' and ''reset()'' streaming operations?', 'No, marking is only supported for buffered file streams.', 'No, it throws an IOException.', 'Yes, because the String data is fully present in memory. Marking simply saves the current index position of the internal cursor, allowing ''reset()'' to instantly rewind the stream without penalty.', 'Yes, but it requires wrapping it in a BufferedReader.', 'C', 'Unlike physical streams which might lose data, memory streams are permanent. The ''StringReader'' natively supports marking. Calling ''mark()'' saves the current character index, and ''reset()'' pushes the reading cursor exactly back to that index.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java StringReader Class', 'What specific runtime exception is thrown if a developer instantiates a ''StringReader'' by passing a ''null'' reference (e.g., ''new StringReader(null)'')?', 'IllegalArgumentException', 'IOException', 'NullPointerException', 'No exception; it creates an empty reader.', 'C', 'The constructor explicitly demands a valid ''String'' object. Passing ''null'' immediately triggers a ''NullPointerException'' because the stream has absolutely no data source to initialize its internal length and cursor state.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java StringReader Class', 'How does a ''StringReader'' structurally track which character to return next when its ''read()'' method is called?', 'It deletes characters from the String as they are read.', 'It uses the JVM Garbage Collector.', 'It maintains a private integer variable (acting as an internal index pointer) that increments by 1 every time a character is successfully extracted from the underlying String.', 'It uses a java.util.Iterator.', 'C', 'The architecture relies on an internal cursor (often named ''next''). When ''read()'' is invoked, the stream essentially executes ''str.charAt(next++)'', safely returning the character and advancing the pointer until it equals the string''s length.');
