INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java FilterOutputStream Class', 'What is the absolute structural purpose of the ''java.io.FilterOutputStream'' class within the Java I/O framework?', 'To natively compress outbound binary data into GZIP format.', 'To act as the foundational base class for implementing the Decorator design pattern on byte output streams, allowing developers to dynamically attach new behaviors (like buffering or primitive data translation) to an existing ''OutputStream''.', 'To physically delete filtered bytes from the OS hard drive.', 'To convert 8-bit bytes into 16-bit characters before writing.', 'B', 'The ''FilterOutputStream'' provides the architectural blueprint for wrapping an existing ''OutputStream''. By itself, it simply forwards all ''write()'' requests to the underlying stream. Its power comes from its subclasses (like ''BufferedOutputStream'' or ''DataOutputStream'') which override these methods to add functionality.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java FilterOutputStream Class', 'How does the base ''FilterOutputStream'' class natively handle an invocation of its ''flush()'' method?', 'It instantly throws an ''UnsupportedOperationException''.', 'It clears its own internal 8KB memory buffer.', 'It simply delegates the call, executing ''out.flush()'' to force the underlying, wrapped stream to flush its physical OS-level buffers.', 'It closes the underlying stream automatically.', 'C', 'Because the base ''FilterOutputStream'' is purely a transparent wrapper and possesses no buffering logic of its own, calling ''flush()'' must be passed down the chain to the concrete stream (like a ''FileOutputStream'') to ensure data actually hits the physical disk.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java FilterOutputStream Class', 'Which access modifier is strictly applied to the internal ''OutputStream'' variable (named ''out'') stored within the ''FilterOutputStream'', and why?', 'private, to enforce strict encapsulation and prevent subclass tampering.', 'public, allowing global modification of the stream pipeline.', 'protected, intentionally granting subclasses direct, low-level memory access to the underlying stream object in case their specific filtering algorithms require bypassing standard delegation.', 'default (package-private).', 'C', 'The instance variable is declared as ''protected OutputStream out;''. This is a deliberate architectural choice. Subclasses implementing complex logic might need to directly inspect or invoke specialized methods on the ''out'' stream without going through the overridden wrapper methods.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java FilterOutputStream Class', 'Which of the following highly utilized standard JDK classes is a direct subclass of ''FilterOutputStream''?', 'java.io.FileWriter', 'java.io.ByteArrayOutputStream', 'java.io.PrintStream', 'java.io.ObjectOutputStream', 'C', 'The ''PrintStream'' class (the class that powers ''System.out'') is a highly complex subclass of ''FilterOutputStream''. It wraps an underlying byte stream and adds the capability to format and write various data values conveniently (like ''print()'' and ''println()'').');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java FilterOutputStream Class', 'What is the architectural result of invoking ''close()'' on a ''FilterOutputStream'' instance?', 'It closes only the wrapper, leaving the underlying file descriptor open.', 'It delegates the ''close()'' call to the underlying ''out'' stream, but crucially, it calls ''flush()'' before closing to guarantee no bytes are lost in transit.', 'It throws an IOException to prevent accidental closure.', 'It executes ''System.exit(0)''.', 'B', 'The ''close()'' method in ''FilterOutputStream'' is designed defensively. Before releasing the OS resources by calling ''out.close()'', it explicitly invokes ''flush()'' to ensure any pending data trapped in intermediate buffers is safely written out.');
