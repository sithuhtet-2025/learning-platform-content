INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java DataOutputStream Class', 'What is the absolute defining architectural function of the ''java.io.DataOutputStream'' class?', 'To compress output data using Huffman coding.', 'To take standard Java primitive data types (int, boolean, double) and dynamically decompose them into raw binary bytes, writing them to an underlying output stream in a highly portable, machine-independent format.', 'To write Java objects directly to a database.', 'To encode text files into UTF-16.', 'B', 'The ''DataOutputStream'' is the exact inverse of ''DataInputStream''. If a developer calls ''writeInt(1000)'', the class mathematically splits that 32-bit integer into 4 distinct 8-bit bytes and pushes them down the stream in strict sequential order.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java DataOutputStream Class', 'Which internal metric does the ''DataOutputStream'' continuously track, which can be retrieved at any time by invoking the ''size()'' method?', 'The total physical capacity of the hard drive.', 'The current size of its internal memory buffer.', 'The exact total number of bytes written to the underlying stream so far.', 'The number of primitives written to the stream.', 'C', 'The class maintains a protected integer variable named ''written''. Every time a method like ''writeByte()'' or ''writeLong()'' executes, it increments this counter appropriately (by 1, or by 8), allowing developers to precisely track the binary payload size generated.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java DataOutputStream Class', 'Does the ''DataOutputStream'' natively buffer its own output to prevent excessive OS-level disk writes?', 'Yes, it has a built-in 8KB buffer.', 'Yes, but only for Strings.', 'No, it does not possess any internal buffering mechanism. Every primitive written results in immediate downstream byte transmission, meaning it should almost always be wrapped around a ''BufferedOutputStream'' for performance.', 'Yes, it buffers data until flush() is called.', 'C', 'A ''DataOutputStream'' simply translates primitives to bytes. If wrapped directly around a ''FileOutputStream'', calling ''writeDouble()'' will cause 8 separate, painfully slow physical disk writes. The optimal architecture is: ''new DataOutputStream(new BufferedOutputStream(new FileOutputStream(...)))''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java DataOutputStream Class', 'When executing the ''writeUTF(String str)'' method, what specifically does the ''DataOutputStream'' write to the stream IMMEDIATELY BEFORE it writes the actual encoded text characters?', 'A null termination character.', 'A 32-bit integer representing the CRC checksum of the string.', 'A 16-bit unsigned integer representing the total number of bytes it will take to encode the string.', 'A boolean indicating if the string is empty.', 'C', 'The Modified UTF-8 format used by Java serialization requires knowing the length of the string before reading it. The ''writeUTF()'' method computes the byte length of the string, writes that length as a 2-byte ''short'', and then writes the actual characters, ensuring ''readUTF()'' knows exactly when to stop.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java DataOutputStream Class', 'What is the architectural difference between using ''writeBytes(String s)'' versus ''writeChars(String s)'' in the ''DataOutputStream'' class?', 'There is absolutely no difference.', '''writeBytes()'' encodes the string using UTF-8, while ''writeChars()'' uses ASCII.', '''writeBytes()'' aggressively strips the high 8 bits from every 16-bit Java character, writing only the lower 8 bits (corrupting non-ASCII characters). ''writeChars()'' safely preserves data by writing every character as two full bytes.', '''writeChars()'' adds a length prefix, while ''writeBytes()'' does not.', 'C', 'The ''writeBytes()'' method is extremely dangerous for internationalization. It casts every ''char'' down to a ''byte'', permanently destroying any Unicode data stored in the upper 8 bits. ''writeChars()'' correctly splits each ''char'' into two bytes and writes them sequentially.');
