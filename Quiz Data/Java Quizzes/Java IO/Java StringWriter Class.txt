INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java StringWriter Class', 'What is the primary architectural utility of the ''java.io.StringWriter'' class?', 'To compress output data into a String format.', 'To act as a dynamic, automatically expanding character buffer in memory, capturing Writer stream output so it can be extracted later as a single, concatenated String.', 'To write strings to a physical hard drive.', 'To format strings for console output.', 'B', 'A ''StringWriter'' does not hit a disk. It acts as an internal sink. You write characters to it, and it appends them to a hidden internal buffer, dynamically resizing itself to accommodate the incoming text payload.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java StringWriter Class', 'What specific underlying JDK data structure is secretly utilized by the ''StringWriter'' to manage its dynamically resizing character buffer?', 'java.lang.String', 'java.util.ArrayList', 'java.lang.StringBuffer', 'char[] array', 'C', 'Under the hood, a ''StringWriter'' is essentially a wrapper around a ''java.lang.StringBuffer''. Every time ''write()'' or ''append()'' is called on the stream, it is simply delegating the call to the synchronized ''StringBuffer'' to manage the memory allocation.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java StringWriter Class', 'When a process is finished writing to a ''StringWriter'', which fundamental method is invoked to extract the entirely accumulated text payload?', 'writer.getString()', 'writer.readAll()', 'writer.toString()', 'writer.toCharArray()', 'C', 'The ''toString()'' method is the primary extraction tool. It calls ''toString()'' on its internal ''StringBuffer'', instantly returning a single, fully concatenated ''String'' object containing every character written to the stream.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java StringWriter Class', 'What happens structurally if a developer executes ''close()'' on a ''StringWriter'' and then immediately executes ''writer.write("More Data")''?', 'It throws an IOException ("Stream closed").', 'It writes to standard output instead.', 'The characters are successfully written and appended to the internal buffer. Because the class holds no physical OS resources, the ''close()'' method is architecturally a no-op and does not prevent further writes.', 'It clears the internal buffer.', 'C', 'Unlike a ''FileWriter'' which will aggressively crash if written to after closing, a ''StringWriter'' ignores the ''close()'' command entirely. It is a memory construct, so no state is locked, allowing appending to continue unimpeded.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java StringWriter Class', 'If a developer anticipates writing exactly 50,000 characters into a ''StringWriter'', what is the recommended architectural approach for instantiating the object?', 'new StringWriter(50000)', 'new StringWriter() and manually expanding it.', 'new StringWriter(new char[50000])', 'new StringWriter("50000")', 'A', 'The default constructor provisions a relatively small ''StringBuffer'' (typically 16 characters). If you dump 50,000 characters into it, the JVM must painfully reallocate and copy the buffer memory dozens of times. Using ''new StringWriter(initialSize)'' pre-allocates the memory, eliminating massive reallocation overhead.');
