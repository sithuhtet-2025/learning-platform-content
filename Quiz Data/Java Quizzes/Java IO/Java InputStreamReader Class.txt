INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java InputStreamReader Class', 'What is the absolute defining architectural purpose of the ''java.io.InputStreamReader'' class?', 'To compress 16-bit characters down to 8-bit bytes.', 'To act as a physical buffer in the OS filesystem.', 'To serve as a fundamental structural bridge, continuously reading raw 8-bit bytes from an underlying ''InputStream'' and dynamically decoding them into 16-bit Java characters using a specified character set.', 'To write binary data out to a network socket.', 'C', 'The JVM operates internally on 16-bit characters (Unicode). Operating systems and network protocols typically stream 8-bit bytes. An ''InputStreamReader'' provides the critical translation layer, pulling bytes from a source and decoding them so Java can process them as readable text.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java InputStreamReader Class', 'If a developer instantiates ''new InputStreamReader(System.in)'' without specifying a Charset, which character encoding algorithm does the JVM utilize by default?', 'It strictly enforces UTF-8.', 'It strictly enforces ASCII.', 'It uses the default character encoding provided by the host operating system environment (e.g., Windows-1252 on US Windows, UTF-8 on Linux), which can cause cross-platform character corruption if ignored.', 'It throws an IOException.', 'C', 'Relying on the default charset is an architectural risk. A file written on a system defaulting to UTF-8 might be read incorrectly by an ''InputStreamReader'' running on a system defaulting to an ISO format. It is universally recommended to explicitly declare the Charset.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java InputStreamReader Class', 'Which of the following constructor signatures represents the most architecturally robust method for instantiating an ''InputStreamReader'' in modern Java (Java 7+)?', 'new InputStreamReader(InputStream in)', 'new InputStreamReader(InputStream in, String charsetName)', 'new InputStreamReader(InputStream in, java.nio.charset.Charset cs)', 'new InputStreamReader(InputStream in, int bufferSize)', 'C', 'While the String-based constructor (e.g., "UTF-8") is common, it throws an ''UnsupportedEncodingException'' if typed incorrectly. Passing a predefined ''Charset'' constant (like ''StandardCharsets.UTF_8'') is strictly compile-time safe and mathematically guarantees the encoding will exist.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java InputStreamReader Class', 'Why does the ''read()'' method of an ''InputStreamReader'' sometimes physically block and wait even if one byte has already been retrieved from the underlying stream?', 'Because the stream is synchronized.', 'Because it waits for a newline character.', 'Because if the specified Charset (like UTF-8) defines a character as multi-byte (e.g., a 3-byte Chinese symbol), the Reader must physically wait and read all 3 bytes from the OS before it can successfully construct and return the single 16-bit Java ''char''.', 'Because it flushes the buffer automatically.', 'C', 'Byte-to-character translation is rarely 1:1. UTF-8 uses variable-width encoding. The ''InputStreamReader'' contains an internal decoder that mathematically analyzes incoming bytes to determine if more bytes are required to fulfill a single character payload.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java InputStreamReader Class', 'What is the standard, optimized architectural pattern for utilizing an ''InputStreamReader'' when processing large amounts of text from a file or network?', 'Wrap it immediately in a ''java.io.DataInputStream''.', 'Read character-by-character continuously.', 'Wrap it immediately inside a ''java.io.BufferedReader'' (e.g., ''new BufferedReader(new InputStreamReader(...))'') to minimize expensive physical byte-level decoding calls and gain the crucial ''readLine()'' capability.', 'Use a ''Scanner'' directly without the Reader.', 'C', 'An ''InputStreamReader'' decoding a stream byte-by-byte is highly CPU intensive. Wrapping it in a ''BufferedReader'' pulls a massive block of bytes, decodes them all at once into a character array in RAM, and yields vastly superior execution performance.');
