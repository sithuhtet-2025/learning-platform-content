INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java FileInputStream Class', 'What is the exact architectural role of the ''java.io.FileInputStream'' class within the Java I/O framework?', 'To read and decode 16-bit Unicode text characters from a file.', 'To read raw, unformatted 8-bit binary byte streams (such as compiled classes, images, or audio data) directly from a physical file on the host operating system.', 'To dynamically load Java class files into the ClassLoader.', 'To compress a file into a ZIP archive while reading it.', 'B', 'A ''FileInputStream'' is completely agnostic to text encoding. It interfaces directly with the OS to read physical files byte-by-byte. If you need to read human-readable text, this byte stream must be bridged to a character stream using an ''InputStreamReader'' or ''FileReader''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java FileInputStream Class', 'What specific checked exception is thrown immediately during the instantiation of a ''new FileInputStream("data.bin")'' if the target file does not exist on the disk?', 'IOException', 'NoSuchFileException', 'FileNotFoundException', 'FileReadException', 'C', 'The constructor requests a file descriptor from the operating system. If the OS cannot locate the file at the specified path, or if the application lacks read permissions, the JVM instantly aborts the creation and throws a ''FileNotFoundException''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java FileInputStream Class', 'When executing the fundamental ''stream.read()'' method on a ''FileInputStream'', what does the JVM mathematically return to signal that the absolute End of File (EOF) has been reached?', 'It returns a null byte (0x00).', 'It throws an EOFException.', 'It returns the integer -1.', 'It safely returns a boolean ''false''.', 'C', 'The ''read()'' method returns the next byte of data as an integer ranging from 0 to 255. Because a valid byte can be 0, it cannot use 0 as an EOF marker. Instead, it relies on returning the out-of-bounds integer -1 to distinctly signal that the stream has run dry.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java FileInputStream Class', 'Which highly specific method allows a developer to extract the opaque, OS-dependent handle representing the actual open file connection from the ''FileInputStream''?', 'stream.getFileHandle()', 'stream.getOSPointer()', 'stream.getFD()', 'stream.getDescriptor()', 'C', 'The ''getFD()'' method returns a ''java.io.FileDescriptor'' object. This object is an opaque representation of the underlying machine-specific structure representing an open file, an open socket, or another source/sink of bytes. It is commonly used to force OS-level physical disk synchronization via ''fd.sync()''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java FileInputStream Class', 'Why is reading a 1GB file via a raw ''FileInputStream'' using a ''while(stream.read() != -1)'' loop considered a catastrophic architectural anti-pattern?', 'Because it will inevitably cause a StackOverflowError.', 'Because the JVM will cache the entire 1GB file in RAM, causing an OutOfMemoryError.', 'Because invoking ''read()'' for a single byte forces a physical context switch to the OS and a mechanical hard drive read head movement 1 billion separate times, completely destroying application performance.', 'Because it corrupts binary data.', 'C', 'Raw file streams perform unbuffered I/O. Hitting the physical disk for every single byte is phenomenally slow. The stream must architecturally be wrapped in a ''BufferedInputStream'', or the developer must utilize the overloaded ''read(byte[] b)'' method to fetch massive chunks of bytes in a single OS call.');
