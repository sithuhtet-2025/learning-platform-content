INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java PipedReader Class', 'In a Java concurrency environment, what is the fundamental purpose of the ''java.io.PipedReader'' class?', 'To parse XML data directly from a file.', 'To establish a secure, synchronized character-based communication channel (the consumer end) that directly reads characters provided by a concurrently executing producer thread within the same JVM.', 'To continuously monitor a physical file for changes.', 'To compress incoming characters in real-time.', 'B', 'The ''PipedReader'' represents the reading half of a Java Pipe. It acts as the receiver in a Producer-Consumer multi-threaded pattern, safely consuming 16-bit characters as they flow into its internal buffer from an attached ''PipedWriter''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java PipedReader Class', 'What is the absolute prerequisite operational state before a ''PipedReader'' can successfully execute a ''read()'' operation without crashing?', 'It must be mapped to a physical disk descriptor.', 'It must have its priority set to MAX.', 'It must be logically bound to a corresponding ''java.io.PipedWriter'' instance, forming a fully connected stream pipeline.', 'It must first be flushed.', 'C', 'A ''PipedReader'' is useless in isolation. Attempting to read from an unconnected pipe immediately throws an ''IOException''. The connection is established either via the constructor ''new PipedReader(pipedWriter)'' or by explicitly calling ''reader.connect(pipedWriter)''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java PipedReader Class', 'What occurs at the thread level if a consumer thread invokes ''read()'' on a connected ''PipedReader'', but the internal pipe buffer is currently completely empty?', 'The JVM throws a BufferUnderflowException.', 'The reader automatically returns -1 to indicate EOF.', 'The ''read()'' method automatically blocks (suspends) the consumer thread, placing it in a waiting state until the producer thread writes at least one new character into the pipe.', 'The JVM dynamically shuts down the pipe.', 'C', 'This inherent blocking behavior is the core mechanism of Java pipe synchronization. The consumer thread politely halts execution, preventing CPU-intensive "busy waiting", and is naturally awakened by the JVM when new data arrives from the producer.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java PipedReader Class', 'What is the standard, hardcoded capacity of the internal circular memory buffer (the ''PIPE_SIZE'') within the ''PipedReader'' class if not explicitly specified via the constructor?', '256 characters', '1024 characters', '8192 characters', '65536 characters', 'B', 'Historically, the default buffer size for Java pipes is 1024. If the producer thread fills these 1024 slots before the consumer reads them, the producer thread will block. Developers can adjust this size using the overloaded constructor ''new PipedReader(int pipeSize)'' to tune performance.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java PipedReader Class', 'How does the JVM mathematically determine that an actively connected pipe has become "broken"?', 'If the buffer reaches 100% capacity.', 'If a timeout of 5000 milliseconds is exceeded between writes.', 'If the JVM determines that the specific thread which was actively providing data to the ''PipedWriter'' has terminated (died), effectively making it impossible for any new data to ever arrive.', 'If a non-ASCII character is transmitted.', 'C', 'Pipes are tightly bound to the thread execution context. If the producer thread crashes or completes its run while the consumer thread is still attempting to read, the pipe detects the thread death and throws an ''IOException("Pipe broken")'' to prevent the consumer from blocking indefinitely forever.');
