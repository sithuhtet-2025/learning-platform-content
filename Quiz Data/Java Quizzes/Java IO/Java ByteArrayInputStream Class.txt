INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java ByteArrayInputStream Class', 'What is the fundamental architectural data source that a ''java.io.ByteArrayInputStream'' reads from?', 'A local file on the hard drive.', 'A strictly in-memory ''byte[]'' array residing in the JVM Heap, allowing the array to be treated polymorphically as a standard continuous input stream.', 'A network socket connection.', 'A physical hardware buffer.', 'B', 'A ''ByteArrayInputStream'' contains an internal buffer that contains bytes that may be read from the stream. It does not perform physical I/O. It simply takes an existing array of bytes in memory and provides the standard ''read()'' API to access them sequentially.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java ByteArrayInputStream Class', 'What is the exact architectural outcome of invoking the ''close()'' method on a ''ByteArrayInputStream'' instance?', 'It throws an IOException if the array is currently being read.', 'It permanently deletes the underlying byte array from memory.', 'It has absolutely zero effect. The class holds no physical OS resources (like file descriptors or network ports) that require releasing, so the ''close()'' method is functionally empty.', 'It resets the stream pointer to the beginning of the array.', 'C', 'Because the stream is entirely memory-based, it relies solely on the JVM Garbage Collector. The ''close()'' method exists exclusively to fulfill the interface contract of the ''InputStream'' superclass, but executing it performs no operation.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java ByteArrayInputStream Class', 'How does the ''ByteArrayInputStream'' internally track the next byte to be read during a sequence of ''read()'' invocations?', 'It physically removes bytes from the array as they are read.', 'It maintains an internal integer variable named ''pos'' (position) which acts as an index cursor, incrementing by 1 each time a byte is successfully returned.', 'It relies on the operating system''s file pointer.', 'It uses a java.util.Iterator object.', 'B', 'The class logic is incredibly simple. It maintains a ''buf'' reference to the array and a ''pos'' integer. When ''read()'' is called, it returns ''buf[pos++]'', strictly advancing the cursor until ''pos'' equals the total ''count'' of bytes.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java ByteArrayInputStream Class', 'Does the ''ByteArrayInputStream'' natively support the ''mark()'' and ''reset()'' streaming capabilities without throwing an exception?', 'No, marking is only supported by buffered file streams.', 'Yes, because the data is already safely stored in a RAM array. Calling ''mark()'' simply saves the current integer value of the ''pos'' cursor, and ''reset()'' instantly restores the cursor to that saved index.', 'Yes, but it requires wrapping it in a BufferedInputStream first.', 'No, it throws an IOException if mark() is called.', 'B', 'Unlike a network stream which loses data once it flows past, a memory array is permanent. Therefore, ''ByteArrayInputStream'' inherently supports marking. It simply remembers the index you marked, allowing you to instantly rewind the stream without any complex caching logic.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java ByteArrayInputStream Class', 'What precise mathematical value is calculated and returned by the ''stream.available()'' method in a ''ByteArrayInputStream''?', 'The total physical memory available in the JVM Heap.', 'The total length of the underlying byte array.', 'The exact number of unread bytes remaining in the stream, calculated strictly as the difference between the ''count'' (total valid bytes) and the current ''pos'' (cursor index).', 'A boolean 1 or 0 indicating if the stream is open.', 'C', 'The ''available()'' method is highly deterministic for this specific class. Because the entire stream exists in memory, it mathematically knows exactly how many bytes are left to read (''count - pos'') and returns that exact integer instantly.');
