INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java PushbackInputStream Class', 'What highly specialized architectural capability does the ''java.io.PushbackInputStream'' provide that standard byte streams strictly lack?', 'The ability to dynamically switch the underlying data source.', 'The ability to automatically decompress ZIP bytes.', 'The ability to physically return (push back) one or more bytes into the front of the input stream after they have been read, forcing the stream to serve those specific bytes again on the very next ''read()'' invocation.', 'The ability to write bytes to the hard drive backwards.', 'C', 'Standard streams consume data irreversibly. The ''PushbackInputStream'' (a FilterInputStream Decorator) maintains an internal staging buffer. When ''unread()'' is called, bytes are placed in this buffer. Subsequent ''read()'' calls will pull from this buffer first before requesting new bytes from the underlying OS file descriptor.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java PushbackInputStream Class', 'In what specific type of software architecture is the ''unread()'' capability of a ''PushbackInputStream'' practically indispensable?', 'Cryptographic hashing algorithms.', 'Network load balancers.', 'Building parsers, lexical analyzers, or compilers. The parser needs to "peek" ahead at the next byte to determine the context of a token (e.g., seeing an ''='' after a ''<'' to detect ''<=''), and if it doesn''t belong to the current token, it pushes it back for the next parsing cycle.', 'Automated database backups.', 'C', 'Compilers rely heavily on peeking. If a parser reads a ''-'', it needs to know if the next character is another ''-'' (to form the decrement operator ''--''). It reads the next byte; if it''s a space instead, it pushes the space back so it isn''t lost, and concludes the token was just a minus sign.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java PushbackInputStream Class', 'If you instantiate a ''PushbackInputStream'' using its default constructor (e.g., ''new PushbackInputStream(myStream)''), what is the absolute capacity limit of its pushback buffer?', '1 byte.', '1024 bytes.', '8192 bytes.', 'Unlimited bytes.', 'A', 'By default, the stream is configured with a remarkably tiny 1-byte pushback buffer. This is optimized for simple single-character peeking scenarios. Attempting to ''unread(byte[] b)'' with more than one byte will immediately crash the application.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java PushbackInputStream Class', 'What specific runtime exception is thrown if a developer executes ''unread(data)'' but the internal pushback buffer is already completely full?', 'BufferOverflowException', 'PushbackOverflowException', 'IOException', 'IndexOutOfBoundsException', 'C', 'Because I/O operations strictly conform to the ''IOException'' hierarchy, pushing too much data back into the stream triggers an ''IOException'' with a message typically stating "Push back buffer is full". To push multiple bytes back, you must explicitly provision a larger buffer: ''new PushbackInputStream(in, size)''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java PushbackInputStream Class', 'Does calling the ''unread(int b)'' method physically alter the data stored in the underlying file on the OS hard drive?', 'Yes, it permanently overwrites the file data.', 'Yes, but only if the file is opened in write mode.', 'No, the ''unread()'' operation interacts strictly with a volatile, in-memory array (the pushback buffer) held within the JVM. The physical disk remains completely untouched.', 'Yes, it deletes the last read byte from the disk.', 'C', 'The concept of "pushing back" is a purely logical construct within the JVM stream wrapper. The OS file pointer doesn''t move backwards, and the physical disk file is never modified. The stream simply caches the unread byte in RAM and lies to the application on the next ''read()'' call.');
