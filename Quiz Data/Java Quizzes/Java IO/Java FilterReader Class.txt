INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java FilterReader Class', 'What is the fundamental architectural distinction between a ''FilterInputStream'' and a ''FilterReader''?', '''FilterInputStream'' reads text, while ''FilterReader'' reads binary data.', '''FilterInputStream'' is a concrete class that wraps 8-bit byte streams, whereas ''FilterReader'' is an ABSTRACT class that wraps 16-bit character streams, requiring developers to explicitly subclass it to utilize it.', '''FilterReader'' is thread-safe, while ''FilterInputStream'' is not.', 'There is no distinction; they are aliases.', 'B', 'While they both implement the Decorator pattern, they operate on different data types (bytes vs. characters). Furthermore, the original JDK designers made ''FilterReader'' abstract to explicitly prevent developers from instantiating a useless, transparent wrapper, forcing them to provide an actual implementation.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java FilterReader Class', 'Which specific protected variable does the ''FilterReader'' class maintain to hold the reference to the stream it is decorating?', 'protected InputStream in;', 'protected Reader in;', 'protected CharBuffer buffer;', 'protected FilterReader next;', 'B', 'To function as a Decorator for character streams, the ''FilterReader'' must hold a reference to an object that fulfills the ''java.io.Reader'' contract. This protected variable is named ''in''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java FilterReader Class', 'Which of the following is a standard JDK concrete subclass of ''FilterReader'' that allows a developer to physically push a character back into the input stream after it has been read?', 'java.io.BufferedReader', 'java.io.StringReader', 'java.io.PushbackReader', 'java.io.CharArrayReader', 'C', 'The ''PushbackReader'' is a classic application of the ''FilterReader'' architecture. It maintains an internal buffer that allows a parser to "peek" at a character and then invoke ''unread()'' to push it back into the stream, altering the sequence for the next ''read()'' call.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java FilterReader Class', 'If a developer creates a custom class ''UpperCaseFilterReader extends FilterReader'', which method must be primarily overridden to intercept and capitalize the incoming characters?', 'The ''readLine()'' method.', 'The ''read()'' and ''read(char[] cbuf, int off, int len)'' methods, capturing the integer/characters from ''super.read()'', applying ''Character.toUpperCase()'', and then returning the mutated data.', 'The ''toUpperCase()'' interface method.', 'The ''skip()'' method.', 'B', 'To manipulate the stream data, the subclass must intercept the execution flow. It calls the underlying stream to get the raw characters, executes the custom business logic (capitalization) on those characters in memory, and then returns the modified payload to the calling thread.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java FilterReader Class', 'How does the ''FilterReader'' architecture handle the ''skip(long n)'' method natively?', 'It throws an UnsupportedOperationException.', 'It reads and silently discards ''n'' characters into a temporary array.', 'It natively delegates the exact call to the underlying ''in.skip(n)'' method, trusting the wrapped stream to handle the file pointer manipulation.', 'It resets the stream to the beginning and counts forward.', 'C', 'Because the abstract ''FilterReader'' provides a fully transparent baseline implementation, operations like ''skip()'', ''ready()'', and ''markSupported()'' are simply forwarded to the underlying ''in'' object without any interference.');
