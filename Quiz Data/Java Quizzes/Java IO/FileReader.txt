INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('FileReader', 'In the Java I/O hierarchy, which specific class does ''java.io.FileReader'' directly extend, defining its architectural role as a bridge from byte streams to character streams?', 'java.io.InputStream', 'java.io.Reader', 'java.io.InputStreamReader', 'java.io.BufferedReader', 'C', 'A ''FileReader'' is just a convenience class. It extends ''InputStreamReader'', meaning it internally wraps a raw ''FileInputStream'' (bytes) and automatically decodes those incoming bytes into 16-bit Java characters using a specified or default character set.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('FileReader', 'What is the primary operational distinction between using a ''FileReader'' versus using a raw ''FileInputStream''?', 'FileReader reads faster because it uses a RAM cache.', 'FileInputStream is used for reading raw, unformatted binary data (like images or compiled class files), whereas FileReader is engineered strictly to read streams of text (characters) and decode them properly.', 'FileReader only works on Windows OS.', 'FileInputStream can only read one byte at a time, whereas FileReader reads entire lines.', 'B', 'The ''Reader'' and ''Writer'' hierarchies were introduced in Java 1.1 specifically to handle internationalization and character encoding. A ''FileReader'' interprets bytes as text characters (e.g., UTF-8), whereas a ''FileInputStream'' sees only raw binary 0s and 1s.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('FileReader', 'What mandatory checked exception is explicitly thrown by the ''FileReader'' constructor if the specified file path does not exist on the target file system?', 'IOException', 'NoSuchFileException', 'FileNotFoundException', 'FileReadException', 'C', 'When executing ''new FileReader("data.txt")'', the JVM attempts to open a native OS file descriptor. If the OS reports the file is missing, the constructor immediately throws a ''java.io.FileNotFoundException'' (which is a subclass of ''IOException'').');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('FileReader', 'Historically (prior to Java 11), what was the severe architectural limitation of instantiating a ''FileReader'' using its standard constructors?', 'It could not be wrapped in a BufferedReader.', 'It was strictly hardcoded to use the host operating system''s default character encoding (e.g., Windows-1252), completely prohibiting the developer from safely reading UTF-8 files on a system with a different default encoding.', 'It could only read files smaller than 2GB.', 'It could not be used in multi-threaded applications.', 'B', 'For over a decade, ''FileReader'' was considered a flawed convenience class because you could not specify a Charset. To read UTF-8 on a Windows machine, developers were forced to manually wrap a ''FileInputStream'' in an ''InputStreamReader(stream, StandardCharsets.UTF_8)''. (Note: Java 11 finally added constructors to ''FileReader'' that accept a Charset).');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('FileReader', 'Why is it considered a standard architectural best practice to almost universally wrap a new ''FileReader'' instantiation inside a ''BufferedReader''?', 'To encrypt the character stream.', 'To convert the text to uppercase automatically.', 'To drastically minimize the number of expensive OS-level disk reads. The BufferedReader fetches large chunks of the file into memory at once, and provides the highly critical ''readLine()'' method.', 'To prevent the file from being deleted by other processes.', 'C', 'Calling ''read()'' on a raw ''FileReader'' hits the physical hard drive for every single character. Wrapping it (e.g., ''new BufferedReader(new FileReader("file.txt"))'') buffers the physical I/O and provides line-by-line parsing, vastly improving performance and usability.');
