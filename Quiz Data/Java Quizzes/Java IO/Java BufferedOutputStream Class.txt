INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java BufferedOutputStream Class', 'What is the exact architectural mechanism by which ''java.io.BufferedOutputStream'' dramatically improves writing performance?', 'It compresses the outgoing bytes using a GZIP algorithm.', 'It intercepts individual byte write requests and pools them into an internal memory array (buffer). It only executes a slow, physical OS-level disk write when this array becomes completely full, drastically reducing the number of I/O operations.', 'It delegates the writing process to a separate background thread.', 'It writes directly to the CPU cache instead of the hard drive.', 'B', 'Physical disk writes are computationally expensive and slow. If you write 1,000 bytes individually, a raw ''FileOutputStream'' hits the disk 1,000 times. A ''BufferedOutputStream'' catches those bytes in RAM, allowing it to hit the disk exactly once with a 1,000-byte payload.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java BufferedOutputStream Class', 'If a developer needs to guarantee that a critically important partially-filled buffer is immediately written to the physical disk before the application crashes, which method must be invoked?', 'stream.push()', 'stream.commit()', 'stream.flush()', 'stream.sync()', 'C', 'The ''flush()'' method forcefully drains the internal memory buffer, instructing the JVM to immediately transmit any pending bytes down the stream to the physical OS destination, regardless of whether the array is completely full or not.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java BufferedOutputStream Class', 'What occurs architecturally if a developer calls ''close()'' on a ''BufferedOutputStream'' that currently holds 400 unwritten bytes in its internal buffer?', 'The 400 bytes are permanently lost, and the stream is closed.', 'The JVM throws an ''IOException'' warning of unsaved data.', 'The ''close()'' method automatically invokes ''flush()'' first to safely write the remaining 400 bytes to the destination, and then cascades the close command to the underlying raw output stream.', 'The stream refuses to close until it reaches its 8192-byte capacity.', 'C', 'Resource management in Java is highly defensive. Calling ''close()'' on a buffered stream guarantees that no data is left behind. It gracefully flushes its internal array before securely releasing the OS file descriptors.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java BufferedOutputStream Class', 'Unless explicitly overridden via the constructor ''new BufferedOutputStream(OutputStream out, int size)'', what is the default byte capacity of the internal buffer array?', '1024 bytes (1 KB)', '8192 bytes (8 KB)', '16384 bytes (16 KB)', '65536 bytes (64 KB)', 'B', 'The JDK source code provisions a default internal ''byte[] buf'' array with a size of 8192. This strikes an optimal architectural balance, maximizing disk throughput efficiency without wasting excessive Heap memory.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java BufferedOutputStream Class', 'Can a developer use a ''BufferedOutputStream'' to directly write a standard ''java.lang.String'' object (e.g., ''stream.write("Hello")'')?', 'Yes, it automatically encodes the string using UTF-8.', 'No, because it is strictly an ''OutputStream'' (a byte stream, not a character stream). It only accepts raw ''byte[]'' arrays or integer byte values. Strings must be manually converted to byte arrays first.', 'Yes, but it writes the string''s memory address instead of the text.', 'No, strings can only be written to a BufferedInputStream.', 'B', 'The ''OutputStream'' hierarchy is architecturally blind to text formatting, character sets, and strings. It deals exclusively with raw binary data. To write text efficiently, a ''BufferedWriter'' should be utilized instead.');
