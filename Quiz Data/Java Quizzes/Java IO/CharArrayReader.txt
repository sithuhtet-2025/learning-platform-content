INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('CharArrayReader', 'Unlike ''FileReader'', what is the fundamental data source that a ''java.io.CharArrayReader'' pulls its characters from?', 'A physical text file on the hard drive.', 'A network socket stream.', 'A pre-existing, populated ''char[]'' array residing directly in the JVM''s Heap memory.', 'A standard java.lang.String object.', 'C', 'A ''CharArrayReader'' is a memory-based stream. It takes a standard character array and wraps it in the ''Reader'' API. This allows developers to pass a block of memory into functions that strictly require a ''Reader'' input stream, completely bypassing the file system.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('CharArrayReader', 'What is the architectural effect of invoking the ''close()'' method on a ''CharArrayReader''?', 'It permanently deletes the underlying character array from memory.', 'It throws an IOException.', 'It frees up OS-level file descriptors.', 'It has absolutely zero effect. The method is empty because a CharArrayReader does not hold any physical OS resources (like file handles or network ports) that require releasing.', 'D', 'Because the data source is just an array in RAM, there is no physical stream to close. The ''close()'' method is only present to satisfy the contract of the ''Reader'' abstract superclass. The array itself is simply reclaimed by the Garbage Collector when it falls out of scope.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('CharArrayReader', 'If you instantiate ''new CharArrayReader(myArray, 5, 10)'', what exactly does this specific stream represent?', 'It reads from index 5 to index 10.', 'It skips the first 5 characters and reads the entire rest of the array 10 times.', 'It creates a stream window that starts reading at array index 5 and spans exactly 10 characters in length (ending at index 14).', 'It throws a syntax error.', 'C', 'This overloaded constructor allows you to create a reader against a specific subset (slice) of the array. The second parameter is the ''offset'' (starting index), and the third parameter is the ''length'' (number of characters to expose to the stream).');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('CharArrayReader', 'How does the ''CharArrayReader'' internally keep track of which character to return next when ''read()'' is invoked?', 'It deletes characters from the array as it reads them.', 'It relies on the OS file pointer.', 'It maintains an internal integer index variable (cursor) that increments linearly each time a character is successfully read from the backing array.', 'It generates a random index.', 'C', 'The class maintains internal state variables, specifically ''pos'' (the current index) and ''count'' (the limit). Every call to ''read()'' fetches ''array[pos]'' and then increments ''pos'' by 1.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('CharArrayReader', 'Does the ''CharArrayReader'' class natively support the ''mark()'' and ''reset()'' streaming capabilities?', 'Yes, because the data is safely held in memory, the stream easily remembers a marked index and can instantly jump the internal cursor back to that position upon calling ''reset()''.', 'No, marking is only supported for buffered file streams.', 'Yes, but it requires wrapping it in a BufferedReader first.', 'No, attempting to call mark() throws an IOException.', 'A', 'Memory-based streams inherently support marking. Because the entire array is already in RAM, calling ''mark(int readAheadLimit)'' simply saves the current value of the internal index pointer, and ''reset()'' restores it, allowing you to reread sections of the array seamlessly.');
