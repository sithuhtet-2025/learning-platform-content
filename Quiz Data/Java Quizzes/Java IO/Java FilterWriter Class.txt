INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java FilterWriter Class', 'What is the absolute defining structural state of the ''java.io.FilterWriter'' class in the Java API?', 'It is a ''final'' class that cannot be extended.', 'It is an ''interface'' requiring implementation of the ''write()'' method.', 'It is an ''abstract'' class that provides a transparent Decorator wrapper around a ''Writer'', intentionally requiring developers to subclass it to build custom character-output filters.', 'It is a fully instantiated Singleton object.', 'C', 'Like ''FilterReader'', ''FilterWriter'' is strictly abstract. It exists solely to provide the architectural boilerplate for the Decorator pattern on outgoing character streams, forcing the developer to define the actual mutation logic in a concrete subclass.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java FilterWriter Class', 'What protected instance variable does the ''FilterWriter'' class encapsulate to manage its downstream connection?', 'protected OutputStream out;', 'protected Writer out;', 'protected FileWriter out;', 'protected CharArrayWriter out;', 'B', 'The ''FilterWriter'' holds a protected reference to a generic ''java.io.Writer'' object named ''out''. This allows it to dynamically wrap any type of character output stream, whether it is a ''FileWriter'', a ''StringWriter'', or a ''PipedWriter''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java FilterWriter Class', 'If a developer writes ''out.write("Secret")'' using a standard ''FilterWriter'' subclass, how does the base class handle the ''write(String str)'' invocation?', 'It translates the string into a byte array.', 'It converts the string into a char array and calls ''write(char[] cbuf)''.', 'It directly delegates the call to ''out.write(str, 0, str.length())'', forwarding the string object to the underlying Writer.', 'It throws an IOException.', 'C', 'The transparent delegation model of the base class ensures that higher-level calls like writing an entire String are passed seamlessly down to the underlying ''out'' stream. If a custom filter needs to intercept string writes, it must explicitly override this specific method.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java FilterWriter Class', 'Which of the following is a classic use-case demanding the creation of a custom ''FilterWriter'' subclass?', 'To buffer output characters to minimize disk I/O (use BufferedWriter instead).', 'To convert text characters directly into 8-bit binary audio data.', 'To build an automated log sanitization stream that intercepts all outgoing text, dynamically replacing sensitive keywords (like passwords or SSNs) with asterisks before they are written to the physical log file.', 'To read configuration files into a HashMap.', 'C', 'A custom ''FilterWriter'' is perfect for data sanitization or transformation. By overriding the ''write()'' methods, the developer intercepts the text, applies a Regex replacement to mask sensitive data, and then passes the sanitized text to the underlying ''FileWriter''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java FilterWriter Class', 'Why does the Java API provide extremely few built-in, standard concrete subclasses of ''FilterWriter'' compared to its byte-stream counterpart (''FilterOutputStream'')?', 'Because character streams are considered deprecated in modern Java.', 'Because ''FilterWriter'' is notoriously slow and inefficient.', 'Because most complex output manipulations (like buffering or printing) were architecturally implemented as direct subclasses of ''Writer'' (e.g., ''BufferedWriter'', ''PrintWriter'') rather than strictly utilizing the ''FilterWriter'' hierarchy.', 'Because it is impossible to filter 16-bit characters.', 'C', 'This is a historical quirk in the JDK design. While ''FilterOutputStream'' has many standard subclasses (DataOutputStream, PrintStream), the designers of the Character streams often bypassed ''FilterWriter'' entirely, making classes like ''BufferedWriter'' extend ''Writer'' directly, leaving ''FilterWriter'' primarily as a tool for custom developer implementations.');
