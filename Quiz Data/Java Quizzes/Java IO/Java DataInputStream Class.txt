INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java DataInputStream Class', 'What specific architectural capability does the ''java.io.DataInputStream'' class provide that standard input streams fundamentally lack?', 'It allows concurrent multi-threaded reading without synchronization.', 'It automatically decompresses GZIP files.', 'It provides specialized methods (like ''readInt()'' or ''readDouble()'') to read raw bytes from an underlying stream and automatically reconstruct them into standard Java primitive data types in a machine-independent way.', 'It reads entire files into memory instantaneously.', 'C', 'Standard streams (like ''FileInputStream'') strictly read raw 8-bit bytes. ''DataInputStream'' implements the ''DataInput'' interface, acting as a translator. If you call ''readInt()'', it pulls exactly 4 bytes from the underlying stream, performs the bitwise shift operations, and hands you a fully formed 32-bit Java integer.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java DataInputStream Class', 'What specific runtime exception is thrown if ''DataInputStream.readInt()'' is invoked, but the underlying stream reaches the absolute End of File (EOF) after reading only 2 bytes?', 'It returns -1.', 'It returns a partially constructed, corrupted integer.', 'It throws an ''IOException''.', 'It throws an ''EOFException''.', 'D', 'This is a critical architectural distinction. A raw ''read()'' returns -1 at EOF. However, ''readInt()'' mathematically requires exactly 4 bytes to reconstruct an integer. If the stream dries up prematurely, returning -1 would be interpreted as a valid integer (-1). Therefore, it must forcefully throw an ''EOFException''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java DataInputStream Class', 'When reading multi-byte primitives (like a 64-bit ''long'' or 32-bit ''int''), what Endianness architecture does the ''DataInputStream'' strictly assume the incoming bytes are formatted in?', 'Little-Endian (least significant byte first).', 'Big-Endian (most significant byte first).', 'Middle-Endian.', 'It automatically detects the host OS Endianness.', 'B', 'Java was designed for platform independence. Therefore, the ''DataInputStream'' and ''DataOutputStream'' strictly enforce a universal network byte order standard: Big-Endian. This ensures an integer written by a Windows machine can be flawlessly read by a Linux mainframe.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java DataInputStream Class', 'Which method is exclusively used by the ''DataInputStream'' to read a String that has been encoded using a specialized, length-prefixed format?', 'readString()', 'readLine()', 'readChars()', 'readUTF()', 'D', 'The ''readUTF()'' method reads a String that was previously written using ''DataOutputStream.writeUTF()''. This specific format prepends the string with a 16-bit integer indicating the exact byte length of the text, allowing the stream to read the exact number of bytes required without searching for a termination character.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java DataInputStream Class', 'Because ''DataInputStream'' strictly implements the Decorator pattern, what is the required method for instantiating it?', 'new DataInputStream()', 'new DataInputStream(new File("data.bin"))', 'new DataInputStream(InputStream in)', 'DataInputStream.getInstance()', 'C', 'A ''DataInputStream'' cannot perform physical I/O itself. It must wrap an existing ''InputStream'' (such as a ''FileInputStream'' or a ''Socket.getInputStream()''). It intercepts the raw bytes from that underlying stream and translates them into primitives.');
