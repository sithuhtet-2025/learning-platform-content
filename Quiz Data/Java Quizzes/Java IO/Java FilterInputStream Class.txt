INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java FilterInputStream Class', 'What highly prominent software engineering Design Pattern is the ''java.io.FilterInputStream'' class specifically architected to implement?', 'The Factory Pattern', 'The Singleton Pattern', 'The Observer Pattern', 'The Decorator Pattern', 'D', 'The ''FilterInputStream'' is the classic Java implementation of the Decorator pattern. It allows developers to dynamically attach new responsibilities (like buffering, data translation, or encryption) to an object at runtime by wrapping it, without altering the underlying object''s core structure.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java FilterInputStream Class', 'What is the primary architectural purpose of the ''FilterInputStream'' base class?', 'To compress all incoming data into a ZIP format automatically.', 'To natively connect to SQL databases and filter incoming rows.', 'To serve as a foundational superclass for customized stream wrappers (like ''BufferedInputStream'' or ''DataInputStream''), maintaining a protected reference to the underlying stream and passing requests to it.', 'To physically delete filtered bytes from the hard drive.', 'C', 'The ''FilterInputStream'' itself does nothing special. It simply holds an internal reference (''protected volatile InputStream in'') and delegates all standard ''read()'' calls directly to it. Its true power is unleashed by its subclasses, which override these delegated methods to manipulate the bytes as they pass through.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java FilterInputStream Class', 'How does a raw ''FilterInputStream'' instance handle an invocation of its ''read()'' method if it has not been subclassed or overridden?', 'It returns 0 by default.', 'It throws an UnsupportedOperationException.', 'It simply forwards the exact method call to the underlying, wrapped ''InputStream'' instance (e.g., ''return in.read();'') and returns the raw result without any modification.', 'It buffers the read into a 1KB array.', 'C', 'By default, the base ''FilterInputStream'' is entirely transparent. It intercepts the call and immediately passes it down the chain to the ''in'' stream. Subclasses must intercept this return value and modify it to achieve their specific filtering logic.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java FilterInputStream Class', 'Which visibility modifier is applied to the internal ''InputStream'' variable (named ''in'') stored within the ''FilterInputStream'' class, and why?', 'private, to enforce strict encapsulation preventing any access.', 'public, allowing any external class to manipulate the underlying stream directly.', 'protected, allowing specifically constructed subclasses (like ''PushbackInputStream'') direct, low-level access to the underlying stream state if their complex filtering algorithms require it.', 'default (package-private).', 'C', 'The declaration is ''protected volatile InputStream in;''. The designers intentionally made it ''protected'' so that developers writing custom filter subclasses can directly access and manipulate the downstream connection if they need to bypass standard delegation for optimization or complex state management.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java FilterInputStream Class', 'Is it standard architectural practice to instantiate a pure ''FilterInputStream'' directly (e.g., ''new FilterInputStream(myStream)'') in a production application?', 'Yes, it provides an automatic 8KB memory buffer.', 'No, while it is not declared as an ''abstract'' class, instantiating it directly provides absolutely zero functional value because its default implementation merely delegates calls transparently. It is designed exclusively to be subclassed.', 'Yes, it provides automatic thread-safety synchronization.', 'No, it throws an InstantiationException.', 'B', 'Creating a raw ''FilterInputStream'' is mathematically equivalent to doing nothing. To actually filter or enhance a stream, you must instantiate one of its powerful subclasses, such as ''BufferedInputStream'', ''DataInputStream'', or a custom developer-written subclass.');
