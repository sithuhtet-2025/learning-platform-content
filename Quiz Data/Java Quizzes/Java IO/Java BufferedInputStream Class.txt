INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java BufferedInputStream Class', 'What is the primary architectural performance advantage of wrapping a standard ''FileInputStream'' inside a ''java.io.BufferedInputStream''?', 'It dynamically converts raw bytes into Unicode characters.', 'It drastically reduces the number of highly expensive, physical OS-level disk reads by fetching a massive chunk of raw bytes into an internal RAM buffer all at once, serving subsequent read requests instantly from memory.', 'It allows the byte stream to be parsed line-by-line.', 'It compresses the incoming byte stream to save Heap memory.', 'B', 'Fetching a single byte from a hard drive takes milliseconds; fetching it from RAM takes nanoseconds. A ''BufferedInputStream'' absorbs the penalty of physical I/O by reading thousands of bytes simultaneously into its internal ''byte[]'' array, massively accelerating sequential stream processing.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java BufferedInputStream Class', 'If a developer instantiates a ''BufferedInputStream'' without manually specifying a buffer size, what is the default capacity of the internal memory array allocated by the JVM?', '1024 bytes (1 KB)', '4096 bytes (4 KB)', '8192 bytes (8 KB)', '65536 bytes (64 KB)', 'C', 'The JDK source code defines the ''DEFAULT_BUFFER_SIZE'' as 8192. This 8KB chunk strikes an optimal architectural balance, being large enough to dramatically reduce OS I/O calls without consuming excessive amounts of the JVM heap for multiple concurrent streams.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java BufferedInputStream Class', 'Because it manages an internal memory array, what powerful stream manipulation capabilities does ''BufferedInputStream'' reliably support that a raw ''FileInputStream'' generally does not?', 'The ability to skip backwards to the exact beginning of the file.', 'The ability to natively parse XML tags.', 'The ''mark()'' and ''reset()'' capabilities, allowing a developer to tag a specific position in the byte stream and later jump the reading cursor back to that exact memory index.', 'The ability to write bytes back into the stream.', 'C', 'A raw file stream cannot easily move backward. Because a ''BufferedInputStream'' holds the bytes in RAM, calling ''mark(readlimit)'' saves the current array index. Calling ''reset()'' simply rewinds the internal pointer back to that saved index, allowing the bytes to be processed again.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java BufferedInputStream Class', 'What happens architecturally if you invoke ''mark(100)'' on a ''BufferedInputStream'', but then proceed to read 150 bytes from the stream before calling ''reset()''?', 'The stream flawlessly resets to the marked position.', 'The JVM automatically doubles the mark limit.', 'The stream throws an ''IOException'' stating "Resetting to invalid mark", because the internal buffer had to overwrite or discard the marked memory block to accommodate the new incoming bytes exceeding the specified limit.', 'The JVM crashes with an OutOfMemoryError.', 'C', 'The ''readlimit'' parameter in ''mark()'' is a strict contract. It tells the buffer exactly how many bytes it must preserve in memory. If you read past that limit, the buffer is forced to drop the old data to pull new bytes from the disk, permanently invalidating the mark.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java BufferedInputStream Class', 'When processing is complete, what is the correct architectural method for releasing the OS resources associated with the ''BufferedInputStream'' and its underlying file stream?', 'You must call ''close()'' on the underlying ''FileInputStream'' first, then the ''BufferedInputStream''.', 'You simply invoke ''close()'' directly on the ''BufferedInputStream'' wrapper object. Its internal architecture guarantees that the close command will be perfectly cascaded down to the underlying raw byte stream.', 'You only need to invoke the ''flush()'' method.', 'You rely entirely on the JVM Garbage Collector.', 'B', 'The Decorator pattern used in Java I/O means you only ever need to close the outermost wrapper. Closing the ''BufferedInputStream'' natively propagates the ''close()'' call down the chain, securely shutting down the OS file descriptor without redundant code.');
