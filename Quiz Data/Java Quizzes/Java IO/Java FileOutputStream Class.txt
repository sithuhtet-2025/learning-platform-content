INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java FileOutputStream Class', 'What is the primary architectural function of the ''java.io.FileOutputStream'' class?', 'To format text into XML or JSON structures before saving.', 'To securely encrypt binary data into a physical file.', 'To transmit raw 8-bit binary data (bytes) from the Java application out to a physical destination file residing on the OS file system.', 'To compress a file into a binary stream.', 'C', 'The ''FileOutputStream'' is the fundamental output pipeline for binary data. It takes bytes generated by the JVM and hands them over to the operating system to be permanently written to a physical storage medium.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java FileOutputStream Class', 'If a developer invokes ''new FileOutputStream("archive.zip")'' but the file "archive.zip" already exists and contains 500MB of data, what is the default, immediate action taken by the JVM and OS?', 'It throws an IOException indicating the file is locked.', 'It silently appends new bytes to the end of the existing 500MB file.', 'It immediately truncates the existing file to zero bytes, permanently destroying the old 500MB of data before writing anything new.', 'It renames the old file to "archive.zip.bak".', 'C', 'By default, opening a ''FileOutputStream'' is a destructive operation. The OS creates a new file or overwrites an existing one entirely. If data preservation is required, the developer must explicitly opt into append mode.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java FileOutputStream Class', 'How is a ''FileOutputStream'' architecturally configured to strictly append new bytes to the end of an existing file without destroying its current contents?', 'By calling ''stream.seek(EOF)'' before writing.', 'By utilizing the overloaded constructor: ''new FileOutputStream("file.txt", true)''.', 'By wrapping the stream in a java.io.Appendable stream.', 'By calling ''stream.setAppendMode(true)''.', 'B', 'The second boolean parameter in the constructor dictates the OS file opening mode. Passing ''true'' instructs the operating system to position the internal file pointer at the absolute end of the file, safely appending all subsequent byte writes.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java FileOutputStream Class', 'Can a developer directly write a ''java.lang.String'' to a ''FileOutputStream'' using ''stream.write("Data")''?', 'Yes, the stream automatically converts the string to bytes.', 'Yes, but it writes the JVM memory address of the string.', 'No, the compiler will throw an error because ''FileOutputStream'' only accepts integer byte values or ''byte[]'' arrays. The string must first be manually converted using ''string.getBytes(StandardCharsets.UTF_8)''.', 'No, strings can only be written to DataOutputStreams.', 'C', 'As a pure byte stream, ''FileOutputStream'' has no concept of character encoding. The developer is entirely responsible for decomposing the 16-bit Java characters into a raw 8-bit byte array before passing it to the stream''s ''write()'' method.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java FileOutputStream Class', 'What severe system-level risk is introduced if a developer continuously instantiates new ''FileOutputStream'' objects in a loop without ever calling their ''close()'' methods?', 'The JVM throws a ConcurrentModificationException.', 'The OS will eventually exhaust its limited pool of available File Descriptors, completely preventing the Java application (and potentially the entire host OS) from opening any further files or network sockets.', 'The hard drive will become physically fragmented.', 'The file sizes will continuously double.', 'B', 'Every open ''FileOutputStream'' holds a native OS File Descriptor. These are finite, highly restricted system resources. Failing to call ''close()'' creates a massive resource leak, ultimately resulting in an OS-level crash (often throwing a "Too many open files" IOException).');
