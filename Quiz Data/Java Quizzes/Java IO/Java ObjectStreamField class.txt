INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java ObjectStreamField class', 'What is the absolute core purpose of the ''java.io.ObjectStreamField'' class within the Java Serialization architecture?', 'To dynamically create new fields in a class at runtime via Reflection.', 'To provide a definitive, descriptive metadata blueprint (encapsulating the name, data type, and signature) for a single, individual serializable field within an ObjectStreamClass.', 'To physically compress individual fields into a binary format before saving them to disk.', 'To convert Java primitive data types into their Wrapper equivalents.', 'B', 'While ''ObjectStreamClass'' describes the entire class, ''ObjectStreamField'' drills down to the micro-level. It encapsulates the metadata for one specific field (e.g., ''private int age''), telling the serialization engine exactly what data type it is processing.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java ObjectStreamField class', 'By actively instantiating an array of ''ObjectStreamField'' objects and assigning it to a private static final variable named ''serialPersistentFields'', what precise architectural control does a developer gain?', 'The ability to bypass the serialization of the object''s superclass.', 'The ability to force the JVM to serialize fields explicitly marked as ''transient''.', 'The explicit ability to dictate exactly which fields of the class are legally allowed to be serialized, overriding the JVM''s default behavior of serializing all non-transient, non-static fields.', 'The ability to change field access modifiers to public.', 'C', 'This is a powerful security tool. Instead of using ''transient'' to exclude fields, a developer declares a whitelist. Only the exact fields defined in the ''serialPersistentFields'' array will ever be written to the output stream, regardless of the class''s actual structure.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java ObjectStreamField class', 'Which two required parameters must be provided to the standard ''ObjectStreamField'' constructor to accurately describe a target field?', 'The name of the field as a String, and the Field object from the Reflection API.', 'The name of the field as a String, and the exact ''java.lang.Class'' type of the field (e.g., String.class, Integer.TYPE).', 'The name of the field and its initial default value.', 'The memory address of the field and its transiency flag.', 'B', 'To build a valid metadata descriptor, the JVM needs the field''s identifier (e.g., "employeeName") and its exact structural type. This is achieved via ''new ObjectStreamField("employeeName", String.class)''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java ObjectStreamField class', 'What internal metadata detail does the ''getTypeString()'' method of ''ObjectStreamField'' uniquely provide?', 'The package name of the class.', 'A boolean indicating if the type is primitive.', 'The JVM internal type signature of the field (e.g., "Ljava/lang/String;" for an object reference, or "I" for a primitive int).', 'The access modifier of the field.', 'C', 'The JVM uses a specific internal signature system to compactly represent data types in bytecode. The ''getTypeString()'' method returns this exact signature string, allowing the deserialization engine to verify structural compatibility at the lowest binary level.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java ObjectStreamField class', 'How does an ''ObjectStreamField'' physically differentiate between a primitive data type and an Object reference type internally?', 'By analyzing the string length of the field''s name.', 'By maintaining an internal boolean flag accessible via ''isPrimitive()'', which dictates how the deserialization engine should physically allocate and reconstruct memory for that specific field.', 'By throwing an exception if an Object is encountered.', 'It relies solely on the Object class hierarchy.', 'B', 'This distinction is highly critical for memory reconstruction. Primitives are read directly via stream methods like ''readInt()'', while Objects require invoking the complex ''readObject()'' graph-traversal logic. The ''isPrimitive()'' flag governs this execution path.');
