INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java PushbackReader Class', 'What is the absolute fundamental difference between a ''PushbackInputStream'' and a ''java.io.PushbackReader''?', 'PushbackReader uses a larger default buffer.', 'PushbackReader is intrinsically thread-safe.', 'PushbackInputStream manipulates raw 8-bit binary data (bytes), whereas PushbackReader strictly manipulates decoded 16-bit Unicode text (characters).', 'PushbackReader can only read from Strings, not files.', 'C', 'Like all Reader/Stream parallels, this is a matter of data type. ''PushbackReader'' is designed for parsing human-readable text (like parsing HTML or Java source code), handling the complexities of character encoding automatically, whereas the InputStream version handles raw bytes.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java PushbackReader Class', 'Which base class does the ''PushbackReader'' structurally extend to act as a wrapper around an existing character stream?', 'java.io.Reader', 'java.io.FilterReader', 'java.io.BufferedReader', 'java.io.InputStreamReader', 'B', 'The ''PushbackReader'' is a concrete implementation of the abstract ''FilterReader'' class, utilizing the Decorator pattern to inject its specific LIFO (Last-In-First-Out) unread buffer functionality seamlessly into any existing character stream.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java PushbackReader Class', 'If a developer executes ''reader.unread(''A'')'' followed immediately by ''reader.unread(''B'')'', what will be the exact character returned by the subsequent invocation of ''reader.read()''?', 'The character ''A''.', 'The character ''B''.', 'The characters are merged into a String "AB".', 'An IOException is thrown.', 'B', 'The internal pushback buffer operates architecturally as a strict LIFO (Last-In-First-Out) stack. The most recent character pushed back into the stream is inherently the absolute first character to be read back out.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java PushbackReader Class', 'How is the ''mark()'' and ''reset()'' streaming functionality architecturally handled by the ''PushbackReader'' class?', 'It is fully supported and utilizes the same internal unread buffer.', 'It is supported only if the buffer size exceeds 1024.', 'It is strictly unsupported. Invoking ''mark()'' will immediately throw an ''IOException'' stating that mark/reset operations are not supported by this specific stream wrapper.', 'It delegates the mark operation directly to the OS.', 'C', 'The internal mechanics of a pushback buffer fundamentally clash with the logic required to maintain a persistent mark pointer. Therefore, the JDK designers explicitly disabled mark/reset capabilities for this class. Calling ''markSupported()'' always evaluates to ''false''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java PushbackReader Class', 'If an application requires pushing back entire words or sentences during parsing, how must the ''PushbackReader'' be instantiated?', 'Using ''new PushbackReader(underlyingReader)'' because the default buffer resizes dynamically.', 'Using ''new PushbackReader(underlyingReader, size)'' to explicitly declare a sufficiently large internal ''char[]'' array capacity at initialization.', 'By calling ''reader.setBufferSize(size)'' after instantiation.', 'By wrapping it in a BufferedWriter.', 'B', 'Just like its byte-stream counterpart, the default constructor provisions a rigid 1-character buffer. To execute ''unread(char[] cbuf)'', the developer must mathematically predict the maximum pushback requirement and explicitly size the buffer during object construction to prevent ''IOExceptions''.');
