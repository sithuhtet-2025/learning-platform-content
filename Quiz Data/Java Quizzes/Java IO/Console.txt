INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Console', 'How does a developer architecturally acquire a reference to the singleton ''java.io.Console'' object associated with the current Java Virtual Machine?', 'By calling ''new Console()''.', 'By calling ''System.in.getConsole()''.', 'By invoking the static method ''System.console()''.', 'By instantiating it through the Runtime class.', 'C', 'The ''Console'' class does not have a public constructor. To interact with the character-based console device, a developer must request the singleton instance directly from the ''System'' class via ''System.console()''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Console', 'What is the exact architectural outcome of calling ''System.console()'' when running a Java application entirely in the background, or executing it from within an IDE (like Eclipse or IntelliJ) that redirects standard input/output streams?', 'It throws an IOException.', 'It opens a new native command prompt window.', 'It returns a virtual console object tied to the IDE''s output window.', 'It returns ''null'', causing a NullPointerException if its methods are invoked without a null check.', 'D', 'If the JVM is not launched from an interactive command-line terminal, or if the standard input/output streams have been redirected by a parent background process (or an IDE), no physical console device is attached. In this scenario, ''System.console()'' strictly returns ''null''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Console', 'Which highly specialized method provided by the ''Console'' class is specifically designed to safely read sensitive user input without echoing the typed characters back to the screen?', 'console.readHidden()', 'console.readPassword()', 'console.readSecureString()', 'console.getHiddenInput()', 'B', 'The ''readPassword()'' method suppresses console echoing (typing does not appear on the screen). It is designed exclusively for capturing sensitive data like passwords or cryptographic keys directly from the terminal operator.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Console', 'Why does the ''Console.readPassword()'' method architecturally return a ''char[]'' array rather than a standard ''java.lang.String''?', 'Because char arrays consume less memory than Strings.', 'To support Unicode characters natively.', 'Because Strings in Java are permanently immutable and stored in the String Pool. A ''char[]'' array can be explicitly overwritten with blank characters the absolute millisecond the password is no longer needed, eliminating the data from RAM to prevent memory dump attacks.', 'Because Strings cannot suppress terminal echoing.', 'C', 'This is a critical security architecture. If a password is read into a ''String'', it lingers in the JVM Heap indefinitely until Garbage Collection runs. By using a mutable ''char[]'', the developer can execute ''Arrays.fill(password, '' '')'' to physically wipe the sensitive data from memory instantly.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Console', 'In addition to reading input, what C-style output capability does the ''Console'' object inherently provide via its ''format()'' and ''printf()'' methods?', 'It outputs directly to a printer peripheral.', 'It provides advanced String formatting (e.g., ''%s'', ''%d'') directly to the terminal output stream, similar to ''System.out.printf()'', but specifically bound to the console device.', 'It formats output as valid XML.', 'It encrypts the output text.', 'B', 'The ''Console'' class implements the ''Flushable'' interface and provides built-in formatter utilities. Calling ''console.printf("User %s logged in", username)'' writes formatted, localized strings directly to the console''s native output stream.');
