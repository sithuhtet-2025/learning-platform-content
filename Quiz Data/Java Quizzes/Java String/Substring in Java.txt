INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Substring in Java', 'What is the evaluated result of the expression ''"Programming".substring(3)''?', '"Pro"', '"gramming"', '"gram"', '"Programming"', 'B', 'The ''substring(int beginIndex)'' method extracts all characters starting from the specified ''beginIndex'' (inclusive) straight through to the very end of the string. Since indices are zero-based, index 3 is ''g'', resulting in "gramming".');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Substring in Java', 'What string is returned by the method call ''"Welcome".substring(2, 5)''?', '"lc"', '"lco"', '"com"', '"lcom"', 'B', 'The ''substring(int beginIndex, int endIndex)'' method is inclusive of the ''beginIndex'' and exclusive of the ''endIndex''. Index 2 is ''l'', index 3 is ''c'', and index 4 is ''o''. It stops before index 5 (''m''), returning "lco".');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Substring in Java', 'What occurs at runtime if you call ''"Data".substring(5)''?', 'It returns null.', 'It returns an empty string "".', 'It throws a StringIndexOutOfBoundsException.', 'It throws a NullPointerException.', 'C', 'The ''beginIndex'' must be greater than or equal to 0 and less than or equal to the length of the string. Since the length of "Data" is 4, an index of 5 is out of bounds, triggering a ''StringIndexOutOfBoundsException''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Substring in Java', 'If you invoke ''String part = original.substring(0, original.length());'', what can be said about the resulting object ''part''?', 'It is a brand new String object allocated on the heap containing the same characters.', 'It causes an IndexOutOfBoundsException because length() is exclusive.', 'It evaluates to exactly the same reference as the ''original'' string to optimize memory, returning the object itself.', 'It converts the String into a char array.', 'C', 'As an optimization, if the ''substring()'' arguments explicitly specify the exact boundaries of the entire original string (beginIndex is 0 and endIndex is the full length), the method simply returns a reference to the original ''String'' object rather than allocating a new one.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Substring in Java', 'Historically (prior to Java 7 Update 6), what severe memory issue was commonly associated with heavy use of the ''substring()'' method on very large strings?', 'It caused stack overflows due to deep recursion.', 'The substring returned a new String object but silently maintained a hidden reference to the original, massive backing ''char[]'' array, preventing the large array from being garbage collected even if only a tiny substring was retained.', 'It leaked memory into the native C++ heap via JNI.', 'It caused the String Constant Pool to overflow uncontrollably.', 'B', 'In older Java versions, to save memory and processing time, ''substring()'' did not copy characters. Instead, it created a new String object that pointed to the original string''s massive internal ''char[]'' array with different offset/length variables. This led to massive memory leaks if the small substring was kept alive while the original large text was discarded. Modern Java resolves this by copying the required characters into a brand new, correctly sized array.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Substring in Java', 'Does invoking ''"Hello".substring(1)'' modify the original string literal?', 'Yes, the original string permanently becomes "ello".', 'No, the original string is completely immutable; the method strictly allocates and returns a new String object containing the substring.', 'Yes, but only if the result is assigned back to the same variable.', 'No, it modifies the string in the cache but not the current instance.', 'B', 'Because Java Strings are fundamentally immutable, no method on the ''String'' class can ever alter the underlying character sequence. Operations like ''substring()'', ''replace()'', and ''toUpperCase()'' always generate and return brand new String objects.');
