INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Difference between String and StringBuffer in Java', 'What is the most fundamental architectural difference between the ''String'' class and the ''StringBuffer'' class in Java?', '''String'' is stored on the stack, while ''StringBuffer'' is stored on the heap.', '''StringBuffer'' can only store alphabetic characters, while ''String'' can store alphanumeric characters.', '''String'' objects are strictly immutable (their state cannot change after creation), whereas ''StringBuffer'' objects are mutable (their contents can be modified in place).', '''String'' is a primitive type, whereas ''StringBuffer'' is an object.', 'C', 'The core distinction is mutability. Once a ''String'' object is created, any operation that seems to modify it (like concatenation) actually creates a brand new String object. ''StringBuffer'' modifies the existing sequence of characters without creating new objects.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Difference between String and StringBuffer in Java', 'Why is ''StringBuffer'' considered thread-safe compared to standard mutability classes?', 'It uses the ''volatile'' keyword on its internal character array.', 'Almost all of its core public methods (like ''append()'' and ''insert()'') are declared as ''synchronized''.', 'It operates entirely within the thread-local stack.', 'It is immutable, making it inherently thread-safe.', 'B', 'The ''StringBuffer'' class is designed for multithreaded environments. Its methods are synchronized, ensuring that multiple threads attempting to modify the same ''StringBuffer'' object concurrently will not corrupt the underlying character array.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Difference between String and StringBuffer in Java', 'If you need to perform thousands of string concatenation operations in a loop within a single-threaded environment, what is the most efficient choice?', 'Using the ''+'' operator with ''String''.', 'Using ''String.concat()''.', 'Using ''StringBuffer''.', 'Using ''StringBuilder''.', 'D', 'While ''StringBuffer'' is mutable and avoids creating thousands of new objects, it carries the overhead of synchronization. In a single-threaded loop where thread safety is not required, ''StringBuilder'' (the non-synchronized counterpart to StringBuffer) is significantly faster.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Difference between String and StringBuffer in Java', 'How does the ''equals()'' method behave differently in ''StringBuffer'' compared to ''String''?', '''StringBuffer'' compares the actual character sequence, while ''String'' compares memory references.', 'Both classes compare the character sequences identically.', '''String'' overrides ''equals()'' to compare the actual character contents, but ''StringBuffer'' does not override it, so it falls back to comparing memory references (object identity).', '''StringBuffer'' throws an exception if ''equals()'' is called.', 'C', 'The ''String'' class explicitly overrides the ''equals()'' method to evaluate if two strings contain the exact same characters. ''StringBuffer'' inherits ''equals()'' directly from ''Object'', meaning it will only return true if both variables point to the exact same physical object in memory.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Difference between String and StringBuffer in Java', 'What is the default initial capacity of the internal character array when a ''StringBuffer'' is instantiated using its empty constructor (''new StringBuffer()'')?', '0 characters', '16 characters', '32 characters', 'It dynamically resizes, so it has no initial capacity.', 'B', 'By default, an empty ''StringBuffer'' allocates an internal character array with an initial capacity of 16 characters. This buffer helps minimize the number of times the array needs to be resized and reallocated in memory as characters are appended.');
