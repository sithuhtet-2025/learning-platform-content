INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Why String is Immutable in Java', 'What is the primary architectural reason behind making the ''String'' class immutable in Java regarding memory management?', 'It allows Strings to be dynamically resized without garbage collection overhead.', 'It safely enables the concept of the String Constant Pool, allowing multiple reference variables to share the exact same String object in memory without risk of one reference altering the text for the others.', 'It ensures that Strings are strictly stored on the Thread Stack rather than the Heap.', 'It prevents the JVM from caching Strings.', 'B', 'Immutability is the foundational requirement for the String Pool. If Strings were mutable, one variable changing its value would silently corrupt the data for every other variable pointing to that shared memory location.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Why String is Immutable in Java', 'How does String immutability inherently benefit multithreaded Java applications?', 'It automatically serializes strings being passed between threads.', 'It forces threads to execute string operations synchronously.', 'Immutable objects are intrinsically thread-safe. Multiple threads can safely read and share the same String object concurrently without requiring complex synchronization locks to prevent race conditions.', 'It assigns a unique thread ID to every String created.', 'C', 'Because the internal state of an immutable String cannot be modified after it is instantiated, there is zero risk of data corruption from concurrent writes. This eliminates the need for synchronization overhead when sharing Strings across threads.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Why String is Immutable in Java', 'From a security perspective, why is it critical that ''String'' objects, often used for network connections, database URLs, and file paths, are immutable?', 'It encrypts the string data natively in the JVM.', 'If a String were mutable, a malicious process could pass a valid file path to a security manager for approval, and then mutate the string to point to a restricted file immediately before the OS opens it.', 'It prevents hackers from reading the string from RAM.', 'It ensures that strings are immediately garbage collected after use.', 'B', 'Immutability guarantees that once a string is validated by a security mechanism (like checking if a user has access to a specific file path), that exact text cannot be maliciously altered before the actual file I/O operation occurs.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Why String is Immutable in Java', 'How does String immutability optimize the performance of data structures like ''java.util.HashMap''?', 'It allows HashMaps to store Strings without allocating heap memory.', 'It prevents hash collisions entirely.', 'Because the string cannot change, its hash code is calculated exactly once and cached. Subsequent calls to ''hashCode()'' return the cached value instantly, vastly speeding up hash-based lookups.', 'It forces the HashMap to sort its keys alphabetically.', 'C', 'A String''s hash code is frequently used when it acts as a key in a HashMap. Since the string''s characters can never change, its hash code will never change. The JVM computes it once and caches it, making dictionary lookups extremely fast.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Why String is Immutable in Java', 'What internal mechanism does the ''java.lang.String'' class use to enforce its immutability at the source code level?', 'It declares the class as ''final'' to prevent subclassing, and stores characters in a ''private final'' array (byte[] in modern Java, char[] historically) with no public setter methods.', 'It relies exclusively on the JVM to block write access to the memory sector.', 'It implements the ''Immutable'' interface which locks the object.', 'It marks all of its public methods as ''synchronized''.', 'A', 'Immutability is enforced through strict encapsulation. The class is ''final'' so its behavior cannot be overridden. The backing character array is ''private'' (hidden from the outside) and ''final'' (the reference cannot be reassigned), and no methods are provided that modify the array''s contents.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Why String is Immutable in Java', 'How does the Java ClassLoader rely on String immutability?', 'It requires mutable strings to dynamically rename classes at runtime.', 'The ClassLoader uses Strings to identify classes to load (e.g., "java.lang.Object"). If Strings were mutable, the requested class name could be altered mid-loading, allowing malicious code substitution.', 'It uses strings to store the compiled bytecode.', 'The ClassLoader does not interact with Strings.', 'B', 'The JVM''s core security heavily relies on the ClassLoader loading the exact classes requested. Immutable class name strings ensure that a request for a safe system class cannot be hijacked to load a malicious class instead.');
