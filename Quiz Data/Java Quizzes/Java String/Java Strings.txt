INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Strings', 'Why is the ''String'' class designed to be strictly immutable in Java?', 'To allow Strings to be dynamically resized without memory fragmentation.', 'To make String objects inherently thread-safe, secure for use as classloader arguments or database connections, and to safely enable the String Constant Pool caching mechanism.', 'To prevent garbage collection of frequently used text.', 'Because immutable objects execute methods significantly faster than mutable objects.', 'B', 'Immutability guarantees that a string''s value cannot be maliciously or accidentally changed once created. This ensures security for network/file paths, inherently provides thread safety (no synchronization needed), and allows the JVM to safely share a single string instance across multiple references via the String Pool.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Strings', 'Consider the code: ''String s1 = "Hello"; String s2 = new String("Hello");''. How many total String objects are created in the JVM memory during execution?', 'One object, stored in the String Pool.', 'Two objects: one in the String Pool and a separate, distinct object in standard Heap memory.', 'Three objects: two in the Heap and one in the Pool.', 'Zero objects, because they are evaluated at compile time.', 'B', 'The literal "Hello" forces the creation of an object in the String Constant Pool (if it doesn''t already exist). The ''new'' keyword strictly forces the JVM to allocate a brand new, separate String object in standard Heap memory, even if an identical string already exists in the pool.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Strings', 'Since Java 8, where is the String Constant Pool physically located within the JVM memory architecture?', 'In the Thread Stack.', 'In the Metaspace (formerly PermGen).', 'In the primary Heap space.', 'In the Native Method Stack.', 'C', 'Historically (prior to Java 7), the String Pool was located in the PermGen space, which had a fixed size and caused OutOfMemoryErrors. In modern Java, the String Pool has been relocated to the main Heap space, allowing it to be dynamically garbage collected along with regular objects.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Strings', 'What architectural mechanism does the JVM use to optimize string concatenation using the ''+'' operator in modern Java (Java 9 and later)?', 'It compiles the ''+'' operator directly into ''StringBuffer.append()'' chains.', 'It uses ''StringBuilder.append()'' at compile time.', 'It relies on ''invokedynamic'' and the ''StringConcatFactory'' to dynamically generate highly optimized concatenation strategies at runtime.', 'It uses native C++ code to merge the memory blocks.', 'C', 'While older Java versions compiled ''+'' into ''StringBuilder'' chains, Java 9+ utilizes the ''invokedynamic'' bytecode instruction to defer the concatenation strategy to runtime. The ''StringConcatFactory'' can then use advanced techniques (like pre-sizing byte arrays) to concatenate strings much faster and with less garbage creation.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Strings', 'What does the ''String.intern()'' method functionally guarantee when invoked?', 'It encrypts the String for secure transmission.', 'It forcefully moves a String object from the Heap to the Stack.', 'It returns a canonical representation of the string object: if the String Pool already contains a string equal to this one, the pool reference is returned; otherwise, this string is added to the pool and its reference is returned.', 'It permanently prevents the String from being garbage collected.', 'C', 'The ''intern()'' method is a native call that checks the String Constant Pool. It ensures that all interned strings with identical character sequences point to the exact same memory address in the pool, allowing for lightning-fast ''=='' reference comparisons instead of ''equals()''.');
