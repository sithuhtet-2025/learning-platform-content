INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('StringBuffer Class in Java', 'What is the default initial capacity of the internal character array when a ''StringBuffer'' is instantiated using ''new StringBuffer("Java")''?', '4 (the length of the string)', '16', '20', '32', 'C', 'When instantiated with a String argument, ''StringBuffer'' allocates an internal array with a capacity equal to the length of the provided string plus the default 16 extra characters. So, 4 + 16 = 20.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('StringBuffer Class in Java', 'Which fundamental characteristic guarantees that ''StringBuffer'' is safe to use in a multithreaded environment?', 'It implements the ''java.util.concurrent.locks.Lock'' interface.', 'Its core public methods (like ''append'', ''insert'', ''delete'') are modified with the ''synchronized'' keyword.', 'It stores its character array in thread-local storage.', 'It is completely immutable.', 'B', 'The ''StringBuffer'' class was designed to be thread-safe. It achieves this by synchronizing almost all of its methods, ensuring that only one thread can modify its internal character array at any given time.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('StringBuffer Class in Java', 'If the internal capacity of a ''StringBuffer'' is exceeded by an ''append()'' operation, what is the standard formula the JVM uses to calculate the new increased capacity?', '(oldCapacity * 2)', '(oldCapacity + 16)', '(oldCapacity * 2) + 2', '(oldCapacity * 1.5)', 'C', 'When a ''StringBuffer'' (or StringBuilder) runs out of space, it allocates a new, larger array and copies the old contents over. The standard resizing formula is to double the current capacity and add 2.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('StringBuffer Class in Java', 'What does the ''ensureCapacity(int minimumCapacity)'' method do?', 'It truncates the ''StringBuffer'' to the specified capacity.', 'It throws an exception if the current capacity is lower than the minimum.', 'It guarantees the internal array has at least the specified capacity, reallocating memory immediately if it does not, which helps avoid multiple automatic resizes during large appending operations.', 'It prevents the ''StringBuffer'' from ever growing beyond the specified capacity.', 'C', 'If you know you will be appending a large amount of text, calling ''ensureCapacity()'' upfront optimizes performance by allocating the required memory all at once, preventing the expensive array-copying process that occurs during incremental automatic resizes.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('StringBuffer Class in Java', 'What is the result of calling ''delete(int start, int end)'' on a ''StringBuffer''?', 'It deletes characters from the ''start'' index (inclusive) to the ''end'' index (inclusive).', 'It deletes characters from the ''start'' index (inclusive) to the ''end'' index (exclusive).', 'It clears the entire buffer if the indices are valid.', 'It replaces the specified range with spaces.', 'B', 'Like the ''substring()'' method, the ''delete()'' method in ''StringBuffer'' operates using an inclusive start index and an exclusive end index. The character at the end index is not removed.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('StringBuffer Class in Java', 'Which interface is implemented by both ''String'' and ''StringBuffer'' that allows them to be used interchangeably in certain API methods, like ''String.contains()''?', 'Serializable', 'Appendable', 'CharSequence', 'Comparable', 'C', 'The ''CharSequence'' interface represents a readable sequence of char values. Because ''String'', ''StringBuffer'', and ''StringBuilder'' all implement this interface, many modern Java APIs accept a ''CharSequence'' to allow flexibility in the type of string-like object passed to them.');
