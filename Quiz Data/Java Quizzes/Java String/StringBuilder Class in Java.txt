INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('StringBuilder Class in Java', 'What is the primary architectural justification for using the ''StringBuilder'' class over the standard ''String'' class?', 'To encrypt strings automatically for secure transmission over HTTP.', 'To create highly optimized, immutable string literals in the String Pool.', 'To provide a mutable sequence of characters, allowing for efficient in-place modifications without generating excessive garbage objects on the heap.', 'To allow strings to span multiple lines in the source code.', 'C', 'Unlike the ''String'' class, which is strictly immutable and creates a new object for every modification, ''StringBuilder'' wraps a mutable character array. Appending or inserting characters modifies this array directly, significantly improving performance and reducing memory overhead.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('StringBuilder Class in Java', 'If you initialize a ''StringBuilder'' using the default no-argument constructor (''new StringBuilder()''), what is the initial capacity of its internal character array?', '0', '16', '32', 'It is dynamically sized to exactly match the first appended string.', 'B', 'The default constructor of ''StringBuilder'' creates an empty string builder with an initial capacity of 16 characters. As you append more characters, it will automatically allocate a larger array when this capacity is exceeded.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('StringBuilder Class in Java', 'What is the functional difference between the ''length()'' and ''capacity()'' methods of a ''StringBuilder'' object?', 'They return the exact same value at all times.', '''length()'' returns the number of characters currently stored in the builder, whereas ''capacity()'' returns the total amount of memory currently allocated for the internal character array.', '''capacity()'' is a fixed maximum limit, while ''length()'' is the current size.', '''length()'' counts bytes, while ''capacity()'' counts characters.', 'B', 'The ''length()'' method simply counts the characters currently composing the string sequence. The ''capacity()'' method reveals the size of the underlying backing array, which is often larger than the length to accommodate future appends without immediate reallocation.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('StringBuilder Class in Java', 'How does the ''append()'' method in ''StringBuilder'' facilitate "method chaining" (e.g., ''sb.append("A").append("B");'')?', 'It returns a new String object after each append.', 'It implicitly calls the ''toString()'' method.', 'It throws a runtime exception if chained improperly.', 'It mutates the internal state and then returns a reference to the exact same ''StringBuilder'' instance (''this'').', 'D', 'The ''append()'' methods (and many other modifying methods like ''insert()'' and ''reverse()'') in ''StringBuilder'' return a reference to the current object (''return this;''). This fluent interface design natively supports continuous method chaining on a single line of code.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('StringBuilder Class in Java', 'What exception is thrown if you call ''sb.insert(10, "Java")'' on a ''StringBuilder'' whose current ''length()'' is only 5?', 'StringIndexOutOfBoundsException', 'IllegalArgumentException', 'NullPointerException', 'CapacityExceededException', 'A', 'The ''insert(int offset, String str)'' method requires the offset to be greater than or equal to 0, and strictly less than or equal to the current ''length()'' of the sequence. If the offset is invalid, it throws a ''StringIndexOutOfBoundsException''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('StringBuilder Class in Java', 'Why should you strictly avoid passing a shared ''StringBuilder'' instance to multiple concurrent threads?', 'Because it will throw a ConcurrentModificationException.', 'Because ''StringBuilder'' methods are intentionally not synchronized, leading to race conditions, corrupted character sequences, or ArrayIndexOutOfBoundsExceptions under concurrent access.', 'Because ''StringBuilder'' is inherently immutable across thread boundaries.', 'Because the JVM will automatically convert it into a ''StringBuffer'', slowing down the program silently.', 'B', '''StringBuilder'' was introduced specifically to remove the synchronization overhead present in ''StringBuffer''. Because its methods lack thread-safety mechanisms, simultaneous modifications by multiple threads will corrupt its internal state.');
