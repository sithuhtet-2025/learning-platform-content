INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('How to create Immutable Class in Java', 'What is the crucial first step at the class declaration level to ensure a custom Java class is strictly immutable?', 'Declare the class as ''abstract''.', 'Declare the class as ''static''.', 'Declare the class as ''final''.', 'Implement the ''Immutable'' interface.', 'C', 'Declaring the class as ''final'' prevents it from being subclassed. If it could be subclassed, child classes could potentially override methods and compromise the immutability of the object''s state.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('How to create Immutable Class in Java', 'How must the instance variables (fields) of an immutable class be declared to enforce state protection?', 'public and static', 'private and final', 'protected and volatile', 'default (package-private)', 'B', 'Fields must be ''private'' to prevent direct external access (encapsulation) and ''final'' to ensure their values are assigned exactly once during construction and can never be reassigned.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('How to create Immutable Class in Java', 'If an immutable class must store a reference to a mutable object (e.g., a ''java.util.Date'' object), how should it be handled inside the constructor?', 'Assign the passed reference directly to the instance variable.', 'Throw an exception, as mutable objects are forbidden in immutable classes.', 'Perform a deep copy (or clone) of the passed object and assign the copy to the instance variable.', 'Cast the mutable object to an immutable type.', 'C', 'If you store the direct reference passed by the caller, the caller could later modify that object externally, breaking the class''s immutability. The constructor must create a defensive deep copy of any mutable parameters.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('How to create Immutable Class in Java', 'Similarly, if an immutable class has a getter method that returns a mutable internal object (like an array or a Date), what must the getter return?', 'The direct reference to the internal object.', 'A defensive deep copy (or clone) of the internal object.', 'null', 'A String representation of the object.', 'B', 'To maintain immutability, getters must never return a direct reference to internal mutable state. They must return a defensive copy so that the caller can modify the returned copy without affecting the immutable object''s internal state.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('How to create Immutable Class in Java', 'Which of the following is NOT a required characteristic of a properly designed immutable class?', 'It provides no setter methods.', 'All fields are initialized via a constructor.', 'It must implement the Serializable interface.', 'It does not allow subclasses to override methods.', 'C', 'Implementing ''Serializable'' is not a requirement for immutability. The core requirements focus entirely on restricting state modification: making the class final, fields private/final, omitting setters, and using defensive copying.');
