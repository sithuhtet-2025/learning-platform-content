INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('StringTokenizer Class in Java', 'What is the current official status of the ''java.util.StringTokenizer'' class according to modern Java documentation?', 'It is the recommended tool for complex regex parsing.', 'It is strictly reserved for use with the Java IO streams.', 'It is a legacy class retained for compatibility reasons, and its use is discouraged in new code in favor of the ''String.split()'' method or the ''java.util.regex'' package.', 'It has been completely removed from the JDK.', 'C', 'While still fully functional, ''StringTokenizer'' is considered a legacy class. The Java API documentation explicitly recommends that new applications use the ''split()'' method of the ''String'' class or the ''java.util.regex'' package for string tokenization.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('StringTokenizer Class in Java', 'If you instantiate a ''StringTokenizer'' using the constructor ''new StringTokenizer("Hello World Java")'' without providing a custom delimiter string, what are the default delimiters used?', 'Only the space character (" ").', 'The comma character (",").', 'Space, tab, newline, carriage return, and form-feed characters (" \t\n\r\f").', 'Every non-alphanumeric character.', 'C', 'When the delimiter string is omitted, the ''StringTokenizer'' constructor implicitly falls back to a default set of common whitespace delimiters: the space character, the tab character, the newline character, the carriage-return character, and the form-feed character.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('StringTokenizer Class in Java', 'Which two methods are canonically used together in a ''while'' loop to iterate through all the tokens parsed by a ''StringTokenizer'' object?', 'hasNext() and next()', 'hasMoreTokens() and nextToken()', 'hasMoreElements() and getNext()', 'isFinished() and readToken()', 'B', 'The standard iteration pattern for a ''StringTokenizer'' utilizes the ''hasMoreTokens()'' method as the boolean condition for the loop, and the ''nextToken()'' method inside the loop body to retrieve the next available string token and advance the internal pointer.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('StringTokenizer Class in Java', 'Consider the code: ''StringTokenizer st = new StringTokenizer("a,b;c", ",;");''. How many total tokens will this tokenizer produce?', '1 token: "a,b;c"', '2 tokens: "a" and "b;c"', '3 tokens: "a", "b", and "c"', 'It will throw an IllegalArgumentException.', 'C', 'The ''StringTokenizer'' treats each individual character within the provided delimiter string as a valid separator. Therefore, both the comma '','' and the semicolon '';'' act as independent delimiters, splitting the string into three distinct tokens: "a", "b", and "c".');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('StringTokenizer Class in Java', 'What happens if you invoke the constructor ''new StringTokenizer("math=x+y", "=", true)'' and then iterate through it?', 'It returns 2 tokens: "math" and "x+y".', 'It returns 3 tokens: "math", "=", and "x+y".', 'It skips the "=" delimiter completely.', 'It throws a compilation error because constructors cannot take a boolean flag.', 'B', 'The third parameter of this constructor is the ''returnDelims'' flag. If set to ''true'', the tokenizer treats the delimiter characters themselves as valid tokens and returns them sequentially along with the parsed text tokens.');
