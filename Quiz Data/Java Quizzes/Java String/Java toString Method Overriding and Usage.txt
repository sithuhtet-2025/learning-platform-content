INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java toString Method Overriding and Usage', 'If you print an object to the console using ''System.out.println(myObject)'' without overriding the ''toString()'' method, what is the default output format provided by the ''java.lang.Object'' class?', 'The object''s memory address in binary format.', 'A JSON representation of the object''s public fields.', 'The fully qualified class name, followed by an @ symbol, followed by the object''s unsigned hexadecimal hash code.', 'A blank line, as the default method returns an empty string.', 'C', 'The default implementation of ''toString()'' in the ''Object'' class returns a string consisting of the class name, an @ character, and the hexadecimal representation of the object''s hash code (e.g., ''com.example.Employee@1b6d3586'').');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java toString Method Overriding and Usage', 'What is the strictly required method signature to successfully override the ''toString()'' method in a custom Java class?', 'public void toString()', 'protected String toString()', 'public String toString()', 'static String toString()', 'C', 'Because the ''toString()'' method in the parent ''Object'' class is declared as ''public'' and returns a ''String'', the overriding method in the subclass must perfectly match this signature. Reducing visibility (e.g., to protected) or changing the return type will cause a compile-time error.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java toString Method Overriding and Usage', 'In which of the following scenarios does the Java compiler implicitly (automatically) invoke an object''s ''toString()'' method?', 'When the object is serialized to a file.', 'When the object is concatenated with a String using the ''+'' operator.', 'When the object is cast to another class type.', 'When the object is passed into an ''if'' statement condition.', 'B', 'The Java compiler natively understands string concatenation. If you attempt to concatenate a String with an object (e.g., ''"Data: " + myObject''), the compiler automatically invokes ''myObject.toString()'' to convert the object into a string representation before concatenating.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java toString Method Overriding and Usage', 'Why does printing a Java array directly (e.g., ''System.out.println(myArray)'') yield an unreadable memory reference instead of the array''s contents?', 'Because arrays are primitive types and do not have a ''toString()'' method.', 'Because arrays inherit the default ''toString()'' implementation from the ''Object'' class and do not override it to print their elements.', 'Because printing arrays poses a security risk.', 'Because arrays are stored off-heap in modern JVMs.', 'B', 'Despite being objects, arrays in Java do not override the ''toString()'' method to iterate through their contents. They use the default ''Object'' implementation. To print array contents readable, you must use utility methods like ''java.util.Arrays.toString(myArray)''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java toString Method Overriding and Usage', 'From an architectural and best-practice standpoint, what is the primary purpose of overriding the ''toString()'' method in business/domain objects?', 'To provide a concise, human-readable string representation of the object''s current state, primarily to facilitate robust logging, debugging, and diagnostics.', 'To serialize the object for network transmission.', 'To format data strictly for graphical user interface (GUI) rendering.', 'To generate a unique primary key for database insertion.', 'A', 'Overriding ''toString()'' is a standard engineering practice. It ensures that when an object is logged or inspected in a debugger, developers see a clear snapshot of its internal state (e.g., ''Employee{id=101, name="Alice"}'') rather than a cryptic memory hash code.');
