INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('How to Reverse a String  in Java', 'Why doesn''t the standard ''java.lang.String'' class provide a built-in ''reverse()'' method?', 'Because reversing a string is computationally too expensive for the JVM.', 'Because Strings in Java are strictly immutable; a ''reverse()'' method would imply modifying the internal character sequence.', 'Because the String Pool cannot store reversed strings.', 'Because developers are expected to write their own sorting algorithms.', 'B', 'Java Strings are immutable by design. Providing a ''reverse()'' method directly on the String class could imply in-place modification. Reversing a string always requires the creation of a brand new character sequence.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('How to Reverse a String  in Java', 'What is the most efficient and standard API-driven technique to reverse a String in Java?', 'String.reverse("text");', 'new StringBuilder("text").reverse().toString();', 'Collections.reverse("text");', 'Arrays.reverse("text".toCharArray());', 'B', 'The ''StringBuilder'' (and ''StringBuffer'') class provides a highly optimized, built-in ''reverse()'' method that mutates its internal character array. Wrapping the String in a StringBuilder, reversing it, and converting it back to a String is the industry standard approach.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('How to Reverse a String  in Java', 'If you manually reverse a String by converting it to a character array (''toCharArray()'') and using a ''for'' loop to swap characters from both ends, what is the required number of loop iterations?', 'Exactly the length of the string.', 'The length of the string minus one.', 'Exactly half the length of the string (length / 2).', 'The length of the string squared.', 'C', 'When swapping characters using a two-pointer approach (one at the beginning, one at the end), they meet in the middle. Therefore, you only need to iterate through half of the array (N/2) to completely reverse it in O(N) time.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('How to Reverse a String  in Java', 'What data structure intrinsically possesses a Last-In-First-Out (LIFO) behavior that makes it perfectly suited for reversing a string character by character?', 'Queue', 'Stack', 'LinkedList', 'HashMap', 'B', 'A Stack operates on a LIFO principle. If you push all characters of a string onto a Stack sequentially and then pop them off one by one, they will naturally be retrieved in exact reverse order.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('How to Reverse a String  in Java', 'What is the time and auxiliary space complexity of reversing a String of length N using the ''StringBuilder.reverse()'' method?', 'Time: O(N), Space: O(1)', 'Time: O(log N), Space: O(N)', 'Time: O(N), Space: O(N)', 'Time: O(N^2), Space: O(1)', 'C', 'The reversal algorithm takes O(N) time to iterate and swap characters. Because Strings are immutable, ''StringBuilder'' must allocate a new internal character array of size N to hold the reversed sequence, resulting in O(N) auxiliary space.');
