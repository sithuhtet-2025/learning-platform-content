INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java String Methods', 'When using the ''substring(int beginIndex, int endIndex)'' method on a Java String, how are the boundary indices treated?', 'Both beginIndex and endIndex are inclusive.', 'Both beginIndex and endIndex are exclusive.', 'beginIndex is inclusive, and endIndex is exclusive.', 'beginIndex is exclusive, and endIndex is inclusive.', 'C', 'The ''substring'' method extracts characters starting exactly at the ''beginIndex'' (inclusive) and extending up to the character at ''endIndex - 1''. The character at ''endIndex'' itself is strictly excluded.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java String Methods', 'What is the primary difference between the ''replace(CharSequence target, CharSequence replacement)'' method and the ''replaceAll(String regex, String replacement)'' method?', 'There is no functional difference; they are aliases.', '''replace()'' only replaces the first occurrence, while ''replaceAll()'' replaces all occurrences.', '''replace()'' operates on literal character sequences, while ''replaceAll()'' strictly evaluates the target string as a Regular Expression (Regex).', '''replaceAll()'' modifies the original String in memory, while ''replace()'' returns a new String.', 'C', 'Both methods replace all occurrences of the target. However, ''replaceAll()'' compiles the first argument as a regular expression, making it powerful for pattern matching, whereas ''replace()'' treats the target strictly as a literal sequence of characters.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java String Methods', 'Introduced in Java 11, how does the ''strip()'' method differ from the legacy ''trim()'' method?', '''strip()'' removes whitespace from the middle of the string, while ''trim()'' only removes it from the edges.', '''strip()'' is aware of the full Unicode character set and removes all Unicode whitespace, whereas ''trim()'' only removes ASCII control characters (values <= 32).', '''strip()'' is faster but not thread-safe.', 'There is no difference; ''strip()'' simply replaced the deprecated ''trim()'' method.', 'B', 'The legacy ''trim()'' method indiscriminately removes any character with a Unicode value less than or equal to ''\u0020'' (space). The modern ''strip()'' method intelligently leverages ''Character.isWhitespace()'' to remove true whitespace characters across the entire Unicode standard.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java String Methods', 'What does the ''indexOf(String str)'' method return if the specified substring is not found within the original string?', 'null', 'A StringIndexOutOfBoundsException is thrown.', '0', '-1', 'D', 'If the specified character or substring does not exist within the string, the ''indexOf()'' method safely returns the integer -1, allowing developers to programmatically check for existence without catching exceptions.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java String Methods', 'Which of the following methods, introduced in Java 11, should be used to determine if a string is completely empty OR contains only whitespace characters?', 'isEmpty()', 'isBlank()', 'isNullOrEmpty()', 'hasNoText()', 'B', 'The ''isBlank()'' method returns true if the string is empty or contains strictly white space codepoints. In contrast, ''isEmpty()'' only returns true if the string''s exact length is 0, returning false if it contains even a single space.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java String Methods', 'Given the string ''String s = "apple,banana,cherry,";'', what is the length of the array returned by ''s.split(",")''?', '3', '4', '2', 'It throws an exception due to the trailing comma.', 'A', 'By default, the ''split(String regex)'' method silently discards trailing empty strings from the resulting array. Because there is nothing after the final comma, the resulting array will only contain ["apple", "banana", "cherry"], making its length 3.');
