INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('String Concatenation in Java', 'What is the evaluated output of the following Java expression: ''System.out.println(10 + 20 + "Java" + 10 + 20);''?', '1020Java1020', '30Java30', '30Java1020', 'Compilation error', 'C', 'The ''+'' operator evaluates from left to right. First, the integers 10 and 20 are added to make 30. Then, 30 is concatenated with "Java" to make "30Java". From that point on, the operands are treated as strings, so "30Java" + 10 becomes "30Java10", and finally "30Java1020".');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('String Concatenation in Java', 'How does the ''String.concat(String str)'' method behave differently from the ''+'' operator when dealing with a ''null'' argument?', 'Both throw a NullPointerException.', 'The ''+'' operator treats null as the literal string "null", whereas ''concat()'' throws a NullPointerException.', 'The ''concat()'' method ignores the null and returns the original string, whereas ''+'' throws an exception.', 'There is no difference; both append "null" to the string.', 'B', 'The ''+'' operator is designed to gracefully handle null values by converting them to the string literal "null". The ''concat()'' method, however, expects a valid String object argument and will throw a NullPointerException if passed null.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('String Concatenation in Java', 'In Java 9 and newer, how does the compiler internally optimize String concatenation using the ''+'' operator?', 'It strictly converts all ''+'' operations into ''StringBuffer.append()'' chains.', 'It converts them into ''StringBuilder.append()'' chains.', 'It utilizes the ''invokedynamic'' bytecode instruction and ''StringConcatFactory'' to dynamically generate highly optimized concatenation strategies at runtime.', 'It uses native C JNI calls to manipulate memory directly.', 'C', 'Prior to Java 9, the compiler generated ''StringBuilder'' chains. Java 9 introduced JEP 280, changing the bytecode to use ''invokedynamic'', allowing the JVM runtime to choose the most efficient concatenation strategy (like pre-computing array sizes) without recompiling the code.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('String Concatenation in Java', 'Why is using the ''+'' operator to concatenate strings inside a large loop considered a severe performance anti-pattern?', 'It causes a StackOverflowError.', 'It creates a new String object and a new StringBuilder object in every single iteration, overwhelming the Heap and triggering excessive Garbage Collection.', 'It prevents the JVM from utilizing the String Constant Pool.', 'It causes thread deadlocks in multi-threaded applications.', 'B', 'Because Strings are immutable, using ''+'' in a loop forces the JVM to continuously allocate new memory for intermediary String and StringBuilder objects. For loops, a single external ''StringBuilder'' should be instantiated and appended to instead.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('String Concatenation in Java', 'Which class, introduced in Java 8, provides a fluent API specifically designed to construct a sequence of characters separated by a delimiter, with optional prefix and suffix?', 'StringTokenizer', 'StringBuilder', 'StringJoiner', 'StringBuffer', 'C', 'The ''java.util.StringJoiner'' class allows developers to easily construct delimited strings (e.g., "[apple, banana, cherry]") by specifying a delimiter, prefix, and suffix during instantiation.');
