INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java String Comparison', 'When comparing two String variables using the double equals operator (''==''), what exactly is the JVM evaluating?', 'It compares the actual character sequences for exact equality.', 'It compares the memory addresses (references) to verify if both variables point to the exact same physical object on the heap.', 'It compares the lengths of the two strings.', 'It compares the lexicographical Unicode values of the strings.', 'B', 'The ''=='' operator in Java evaluates reference equality, not content equality. It strictly returns true only if both reference variables point to the exact same memory location.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java String Comparison', 'Which method should you universally use to compare the actual character contents of two String objects for exact, case-sensitive equality?', 'string1.matches(string2)', 'string1.compareTo(string2)', 'string1.contentEquals(string2)', 'string1.equals(string2)', 'D', 'The ''equals()'' method is overridden in the ''String'' class to perform deep content comparison. It iterates through the underlying character arrays to ensure every character matches exactly.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java String Comparison', 'Given the code: ''String s1 = "Java"; String s2 = new String("Java");''. What are the respective results of ''s1 == s2'' and ''s1.equals(s2)''?', 'true, true', 'false, false', 'true, false', 'false, true', 'D', '''s1'' points to a literal in the String Pool, while ''s2'' points to a brand new object instantiated in standard heap memory. Therefore, ''s1 == s2'' is false (different memory addresses). However, ''s1.equals(s2)'' is true because their character contents are identical.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java String Comparison', 'What does the ''String.compareTo(String anotherString)'' method return if the invoking string is lexicographically identical to the argument string?', '-1', '0', '1', 'true', 'B', 'The ''compareTo()'' method returns an integer based on lexicographical (alphabetical Unicode) comparison. It returns a negative number if the string is lexicographically less, a positive number if it is greater, and exactly 0 if both strings are identical.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java String Comparison', 'If you need to compare the contents of two strings while strictly ignoring whether the letters are uppercase or lowercase, which method is the most efficient choice?', 's1.toLowerCase().equals(s2.toLowerCase())', 's1.equalsIgnoreCase(s2)', 's1.matchesIgnoreCase(s2)', 's1.compareToIgnoreCase(s2) == 1', 'B', 'The ''equalsIgnoreCase()'' method is specifically optimized for this task. It compares the characters directly without the memory overhead of creating brand new, fully lowercased String objects as ''toLowerCase().equals()'' would.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java String Comparison', 'What impact does invoking the ''intern()'' method have on String comparison using the ''=='' operator?', 'It causes the ''=='' operator to throw an exception.', 'It places the string into the String Pool (if not already there) and returns the pool reference, allowing strings with identical content to share the exact same memory address and thus evaluate to true using ''==''.', 'It overrides the ''=='' operator to behave exactly like the ''equals()'' method.', 'It encrypts the string, making comparison impossible.', 'B', 'The ''intern()'' method interacts directly with the String Constant Pool. If two separate String objects with the same content are both interned, they will return the same reference from the pool, making ''=='' evaluate to true.');
