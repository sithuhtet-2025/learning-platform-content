INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('StringBuffer vs StringBuilder in Java', 'What is the absolute most critical architectural difference between ''StringBuffer'' and ''StringBuilder''?', '''StringBuilder'' is immutable, while ''StringBuffer'' is mutable.', '''StringBuffer'' methods are synchronized (thread-safe), whereas ''StringBuilder'' methods are not synchronized (not thread-safe).', '''StringBuilder'' has a smaller default capacity than ''StringBuffer''.', '''StringBuffer'' is available only in Java 8+, whereas ''StringBuilder'' is legacy.', 'B', 'The sole reason ''StringBuilder'' was introduced (in Java 1.5) was to provide a drop-in, non-synchronized replacement for ''StringBuffer''. Because it lacks synchronization overhead, ''StringBuilder'' is significantly faster but cannot be safely shared across multiple threads.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('StringBuffer vs StringBuilder in Java', 'In a standard single-threaded Java application, which class should be used for heavy string manipulation and why?', '''String'', because it is immutable and memory-efficient.', '''StringBuffer'', because its synchronization prevents garbage collection.', '''StringBuilder'', because the absence of synchronization lock checks makes it considerably faster than ''StringBuffer''.', 'Both perform exactly the same in a single-threaded environment.', 'C', 'Even in a single-threaded environment, invoking synchronized methods on a ''StringBuffer'' forces the JVM to acquire and release monitor locks. ''StringBuilder'' avoids this completely unnecessary overhead, resulting in faster execution.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('StringBuffer vs StringBuilder in Java', 'What happens if multiple threads concurrently attempt to append data to the exact same ''StringBuilder'' instance?', 'The operations are queued and executed safely.', 'The JVM automatically converts it into a ''StringBuffer''.', 'Race conditions occur, likely resulting in an ''ArrayIndexOutOfBoundsException'' or a corrupted character sequence with missing data.', 'A ''ConcurrentModificationException'' is immediately thrown.', 'C', 'Because ''StringBuilder'' is not thread-safe, concurrent modifications to its internal character array and size variables will lead to race conditions. Threads may overwrite each other''s data or trigger internal array bounds exceptions as they simultaneously trigger reallocation logic.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('StringBuffer vs StringBuilder in Java', 'Which of the following is true regarding the API compatibility between ''StringBuffer'' and ''StringBuilder''?', 'They have completely different method names for appending and inserting.', '''StringBuilder'' lacks the ''reverse()'' method.', 'They possess an identical set of public methods with the exact same signatures, making them easily interchangeable in source code.', '''StringBuffer'' does not support the ''delete()'' operation.', 'C', '''StringBuilder'' was intentionally designed as a direct drop-in replacement for ''StringBuffer''. Therefore, their public APIs are functionally identical, allowing developers to switch between them simply by changing the object instantiation type.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('StringBuffer vs StringBuilder in Java', 'Both ''StringBuffer'' and ''StringBuilder'' inherit their core implementation logic from which package-private abstract superclass in the JDK?', 'AbstractString', 'AbstractStringBuilder', 'CharSequenceBase', 'MutableString', 'B', 'To avoid code duplication, the authors of the JDK placed the actual character array manipulation logic (like expanding capacity and appending) into a package-private superclass named ''java.lang.AbstractStringBuilder''. Both ''StringBuffer'' and ''StringBuilder'' extend this class, with ''StringBuffer'' simply adding ''synchronized'' modifiers to the method overrides.');
