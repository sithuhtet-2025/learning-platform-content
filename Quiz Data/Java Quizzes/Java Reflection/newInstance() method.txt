INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('newInstance() method', 'What was the historical purpose of the ''Class.newInstance()'' method?', 'To duplicate an existing object without using the Cloneable interface.', 'To dynamically instantiate a new object of the class using its no-argument (default) constructor.', 'To reload a class definition into the JVM memory.', 'To invoke a static initialization block.', 'B', 'For many years, ''Class.newInstance()'' was the standard reflective way to create an object, effectively acting as a dynamic equivalent to calling ''new MyClass()'' with no arguments.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('newInstance() method', 'Why was the ''Class.newInstance()'' method officially deprecated in Java 9?', 'It caused memory leaks by bypassing the garbage collector.', 'It could not instantiate classes located in external JAR files.', 'It bypasses compile-time exception checking, allowing checked exceptions thrown by the constructor to propagate as unchecked exceptions.', 'It was too slow compared to the modern alternatives.', 'C', 'A major design flaw of ''Class.newInstance()'' is that if the class''s no-arg constructor throws a checked exception (like IOException), ''newInstance()'' throws it without wrapping it in an InvocationTargetException, violating Java''s strict checked exception rules.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('newInstance() method', 'What is the modern, recommended, and safer alternative to calling ''clazz.newInstance()''?', 'clazz.createObject()', 'clazz.getDeclaredConstructor().newInstance()', 'clazz.clone()', 'Constructor.create(clazz)', 'B', 'The accepted replacement is to first get the specific Constructor object using ''getDeclaredConstructor()'' and then call ''newInstance()'' on that Constructor object. This properly wraps any exceptions thrown by the constructor into an ''InvocationTargetException''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('newInstance() method', 'What exception is thrown by the modern ''Constructor.newInstance()'' if you attempt to instantiate an abstract class or an interface?', 'InstantiationException', 'IllegalArgumentException', 'IllegalAccessException', 'UnsupportedOperationException', 'A', 'Attempting to instantiate an abstract class, an interface, an array class, or a primitive type using reflection will result in an ''InstantiationException''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('newInstance() method', 'Unlike ''Class.newInstance()'', what additional capability does ''Constructor.newInstance()'' provide?', 'It allows bypassing the JVM''s memory limits.', 'It automatically serializes the object upon creation.', 'It can instantiate inner classes without an outer class reference.', 'It can accept an array of arguments to invoke parameterized constructors, rather than being restricted to the no-arg constructor.', 'D', 'Because you can request a specific constructor (e.g., ''getDeclaredConstructor(String.class, int.class)''), the ''Constructor.newInstance(Object... initargs)'' method allows you to pass parameters and use any available constructor, not just the default one.');
