INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Creating your own appletviewer', 'What is the very first technical operation a custom appletviewer must perform when executing an HTML file containing an applet?', 'Compile the Java source code.', 'Parse the HTML file to locate the ''<applet>'' tag and extract its attributes like ''code'', ''width'', and ''height''.', 'Instantiate the ''java.applet.Applet'' base class directly.', 'Connect to the internet to download the latest JRE.', 'B', 'An appletviewer works by scanning a given HTML document for the ''<applet>'' tag. It must parse the file to extract the ''code'' attribute (to know which class to load) and dimensional attributes (to size the viewing window).');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Creating your own appletviewer', 'Which standard Java class is typically utilized by a custom appletviewer to load the target applet''s compiled ''.class'' file from a specified codebase or directory path?', 'FileReader', 'ByteClassLoader', 'URLClassLoader', 'AppletClassLoader', 'C', 'Because applets can be located locally or over a network at a specific URL/codebase, a ''URLClassLoader'' is the standard mechanism used to dynamically load the applet''s bytecode into the JVM.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Creating your own appletviewer', 'After successfully loading the applet class and instantiating it, what is the exact required sequence of lifecycle methods the custom appletviewer must invoke to officially start the applet?', 'start() then init()', 'init() then start()', 'paint() then init()', 'run() then start()', 'B', 'The applet lifecycle dictates that the container (your appletviewer) must first call ''init()'' to allow the applet to initialize its resources, immediately followed by ''start()'' to begin its execution.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Creating your own appletviewer', 'To display the instantiated applet on the screen, which top-level AWT or Swing container is typically created by the custom appletviewer to act as the host window?', 'Canvas', 'Panel', 'Frame or JFrame', 'WindowBox', 'C', 'Because the ''Applet'' class extends ''Panel'', it is not a standalone window. The appletviewer must create a top-level container, such as a ''java.awt.Frame'' or ''javax.swing.JFrame'', and add the applet instance to it for display.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Creating your own appletviewer', 'To fully simulate a browser environment and provide the applet with execution context (like resolving relative URLs or accessing parameters), which interface must the custom appletviewer implement and bind to the applet?', 'AppletContext', 'AppletStub', 'Runnable', 'BrowserEnvironment', 'B', 'The ''AppletStub'' interface serves as the bridge between an applet and its container. By implementing ''AppletStub'' and calling ''applet.setStub()'', the custom viewer provides the applet with access to parameters (''<param>'' tags) and the document/codebase context.');
