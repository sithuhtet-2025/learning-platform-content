INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Creating a program that works as javap tool', 'Which built-in Java API is fundamentally required to build a custom tool that mimics the class-disassembling functionality of the standard ''javap'' utility?', 'Java Native Interface (JNI)', 'Java Compiler API', 'Java Reflection API', 'Java Debug Interface (JDI)', 'C', 'The Java Reflection API (''java.lang.reflect'') allows a Java program to inspect the metadata of classes, interfaces, fields, and methods at runtime, which is exactly how a disassembler like ''javap'' reads class structures.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Creating a program that works as javap tool', 'When building a custom ''javap'' tool, how do you dynamically load a class into memory for inspection using just its fully qualified string name (e.g., "java.util.Scanner")?', 'ClassLoader.load(String name)', 'Class.forName(String className)', 'Runtime.getRuntime().loadClass(String name)', 'new Class(String className)', 'B', 'The static method ''Class.forName("fully.qualified.ClassName")'' dynamically loads the specified class into the JVM and returns the corresponding ''Class'' object used as the entry point for reflection.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Creating a program that works as javap tool', 'To replicate ''javap'' output, you must list a class''s methods. Which method of the ''Class'' object strictly returns only the methods explicitly declared in that specific class, excluding inherited methods?', 'getMethods()', 'getAllMethods()', 'getDeclaredMethods()', 'getConstructors()', 'C', 'The ''getDeclaredMethods()'' method returns an array of ''Method'' objects reflecting all methods declared by the class or interface, including public, protected, default, and private methods, but excludes inherited ones. ''getMethods()'' returns only public methods (including inherited).');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Creating a program that works as javap tool', 'During reflection, methods like ''Method.getModifiers()'' return an integer value representing the access modifiers. How do you decode this integer into a human-readable string like "public static"?', 'By manually masking the bits against integer constants.', 'By using the static method ''Modifier.toString(int mod)''.', 'By calling ''String.valueOf()'' on the integer.', 'The integer cannot be decoded in standard Java.', 'B', 'The ''java.lang.reflect.Modifier'' class provides the static utility method ''toString(int mod)'', which takes the integer bitmask returned by ''getModifiers()'' and translates it into standard Java modifier keywords.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Creating a program that works as javap tool', 'If your custom ''javap'' tool needs to print the constructors of a target class, which reflection method should be invoked on the ''Class'' instance?', 'getDeclaredMethods()', 'getInitializers()', 'getDeclaredConstructors()', 'getConstructorsOnly()', 'C', 'Constructors are distinct from standard methods in the Reflection API. You must use ''getDeclaredConstructors()'' to retrieve an array of ''Constructor'' objects representing all constructors defined within the class.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Creating a program that works as javap tool', 'When extracting field types to display in your custom ''javap'' output, what method on a ''Field'' object returns the data type of that field?', 'field.getType()', 'field.getClass()', 'field.getDataType()', 'field.getReturnType()', 'A', 'The ''getType()'' method on a ''Field'' object returns a ''Class'' object that identifies the declared type for the field (e.g., returning ''int.class'' or ''String.class'').');
