INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java throws Keyword', 'Where is the ''throws'' keyword positioned within Java syntax?', 'Inside the ''try'' block parameters.', 'Appended to the method signature declaration, preceding the method body''s opening curly brace.', 'Immediately after the ''catch'' keyword.', 'At the class declaration level.', 'B', 'The ''throws'' keyword is a declaration modifier. It is placed in the method signature (e.g., ''public void readFile() throws IOException { ... }'') to advertise to callers what checked exceptions the method might propagate.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java throws Keyword', 'What is the primary architectural purpose of the ''throws'' keyword?', 'To actively instantiate and trigger an exception in memory.', 'To automatically convert a checked exception into an unchecked exception.', 'To declare that a method does not handle a specific checked exception internally, thereby delegating the responsibility of catching it to the calling method up the stack.', 'To prevent the garbage collector from destroying exception objects.', 'C', 'The ''throws'' keyword is a mechanism for exception propagation. By declaring it, a method signals that it declines to handle the exception locally and forces the calling method to either catch it or continue throwing it upwards.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java throws Keyword', 'Is it possible to declare multiple different exceptions using the ''throws'' keyword on a single method?', 'No, a method can only declare one exception type to maintain type safety.', 'Yes, by chaining multiple ''throws'' keywords (e.g., ''throws IOException throws SQLException'').', 'Yes, by providing a comma-separated list of exception classes after a single ''throws'' keyword (e.g., ''throws IOException, SQLException'').', 'Yes, but they must all be unchecked exceptions.', 'C', 'A single method might fail in various ways involving different systems (e.g., file I/O and database access). Java allows declaring multiple potential failures using a comma-separated list after the ''throws'' keyword.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java throws Keyword', 'Does the Java compiler strictly mandate the use of the ''throws'' keyword for Unchecked Exceptions (e.g., ''NullPointerException'') if a method propagates them?', 'Yes, the "catch or specify" rule applies equally to all exceptions.', 'No, the compiler does not require Unchecked Exceptions to be declared via ''throws'', as they represent programming errors rather than recoverable external conditions.', 'Yes, but only if the method is marked as public.', 'No, unchecked exceptions cannot be propagated at all.', 'B', 'Unchecked exceptions (subclasses of ''RuntimeException'' and ''Error'') are exempt from the "catch or specify" requirement. While you *can* declare them with ''throws'' for documentation purposes, the compiler will not force you to do so.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java throws Keyword', 'If Method A calls Method B, and Method B has ''throws IOException'' in its signature, what are Method A''s exact obligations according to the Java compiler?', 'Method A can safely ignore it, as Method B is responsible.', 'Method A must wrap the call to Method B in a try-catch block handling IOException, OR Method A must also declare ''throws IOException'' in its own signature to pass it further up.', 'Method A must throw a RuntimeException instead.', 'Method A must declare Method B as abstract.', 'B', 'The compiler enforces the checked exception contract. Since Method B declares it might throw an IOException, Method A must acknowledge this danger. It must either neutralize the danger locally (try-catch) or warn its own callers (by adding ''throws IOException'').');
