INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java try-catch Block', 'What is the primary architectural purpose of the ''try'' block in Java exception handling?', 'To catch exceptions thrown by the JVM.', 'To enclose the "risky" code that might generate an exception during runtime, isolating it from the rest of the application logic.', 'To definitively execute cleanup code regardless of application crashes.', 'To declare which exceptions a method might throw.', 'B', 'The ''try'' block is specifically designed to wrap the section of code where exceptions are anticipated. If an exception occurs within this block, the JVM immediately creates an exception object and hands control to the corresponding ''catch'' block.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java try-catch Block', 'If a ''try'' block contains 5 lines of code, and a critical exception is thrown on line 2, what happens to lines 3, 4, and 5?', 'They are executed immediately after the catch block finishes.', 'The JVM attempts to execute them before jumping to the catch block.', 'They are completely skipped. Control flow immediately exits the try block and jumps to the first matching catch block.', 'They cause a compilation error.', 'C', 'The moment an exception is thrown inside a ''try'' block, normal execution is instantly halted. Any remaining code within that specific ''try'' block is bypassed as the JVM searches for an appropriate exception handler.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java try-catch Block', 'Can a ''catch'' block exist independently without a preceding ''try'' block?', 'Yes, if it is placed at the very end of a method.', 'Yes, if it catches an unchecked exception like NullPointerException.', 'No, a catch block must strictly follow a try block. An isolated catch block will cause a compile-time syntax error.', 'Yes, but it requires the ''throws'' keyword in the method signature.', 'C', 'A ''catch'' block is structurally dependent on a ''try'' block. It acts as the designated handler for the specific code enclosed in the ''try'' block. The Java compiler strictly enforces this pairing.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java try-catch Block', 'What is the scope of a variable declared entirely within a ''try'' block (e.g., ''try { int x = 10; }'')?', 'It is globally accessible throughout the entire class.', 'It is accessible in the ''try'' block and the corresponding ''catch'' block, but nowhere else.', 'It is strictly limited to the ''try'' block itself. It cannot be accessed in the ''catch'' block, the ''finally'' block, or the rest of the method.', 'It is accessible anywhere within the method.', 'C', 'Java utilizes block-level scope. A variable declared inside the curly braces of a ''try'' block is completely invisible to the outside, including its attached ''catch'' and ''finally'' blocks. To share variables across these blocks, they must be declared before the ''try'' block begins.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java try-catch Block', 'What occurs if an exception is thrown inside a ''try'' block, but there is no ''catch'' block provided that matches the specific exception type?', 'The JVM automatically converts it into a generic Exception and handles it.', 'The exception is swallowed and the program continues normally.', 'The JVM ignores the try block and restarts the method.', 'The exception is left unhandled locally and propagates up the call stack to the calling method, potentially crashing the thread if never caught.', 'D', 'If the ''try'' block throws an exception that does not match any of the provided ''catch'' parameters, the local handling fails. The JVM abruptly terminates the method and passes the exception object up the execution stack, searching for a capable handler.');
