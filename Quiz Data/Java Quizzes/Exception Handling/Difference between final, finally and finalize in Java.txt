INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Difference between final, finally and finalize in Java', 'Fundamentally, what are ''final'', ''finally'', and ''finalize'' in the Java language architecture?', '''final'' is a keyword, ''finally'' is a block used in exception handling, and ''finalize'' is a method.', 'They are all reserved keywords.', 'They are all methods of the Object class.', '''final'' is a class, ''finally'' is a method, and ''finalize'' is a garbage collector flag.', 'A', 'Despite sounding similar, they belong to completely different domains: ''final'' is an access modifier keyword, ''finally'' is a structural block tied to ''try-catch'', and ''finalize()'' is a specific method historically used for object destruction.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Difference between final, finally and finalize in Java', 'What is the absolute execution guarantee of the ''finally'' block?', 'It executes only if an exception is caught.', 'It executes only if no exception is thrown.', 'It is guaranteed to execute regardless of whether an exception is thrown or caught within the associated ''try'' block, making it the standard location for closing resources like database connections.', 'It is guaranteed to execute before the JVM starts.', 'C', 'The ''finally'' block contains critical cleanup code. The JVM guarantees its execution after the try/catch blocks complete, ensuring that vital resources (like file streams or network sockets) are closed safely, even if a severe exception occurred.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Difference between final, finally and finalize in Java', 'Under what exact, extreme circumstance will a ''finally'' block completely fail to execute?', 'If a NullPointerException is thrown.', 'If the try block contains a ''return'' statement.', 'If ''System.exit(0)'' is called within the try or catch block, or if the JVM abruptly crashes/loses power.', 'If the catch block throws a new exception.', 'C', 'While ''finally'' is resilient to exceptions and return statements, it cannot survive the termination of the JVM itself. Calling ''System.exit()'' immediately halts the Java process, bypassing the execution of any pending ''finally'' blocks.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Difference between final, finally and finalize in Java', 'What is the historical purpose of the ''finalize()'' method, which was officially deprecated in Java 9?', 'To definitively assign a value to a final variable.', 'To permanently lock a class from being inherited.', 'It was a callback method invoked by the Garbage Collector immediately before reclaiming the object''s memory, intended to allow the object to release non-Java resources (like OS file handles).', 'To manually trigger the Garbage Collector.', 'C', 'The ''finalize()'' method was defined in ''java.lang.Object''. The JVM''s garbage collector would call it before destroying an unreachable object. It was deprecated because its execution timing is highly unpredictable, leading to resource leaks and severe performance penalties.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Difference between final, finally and finalize in Java', 'What is the modern, highly recommended alternative to using both ''finally'' blocks for resource closing and the deprecated ''finalize()'' method for cleanup?', 'The try-with-resources statement and the ''AutoCloseable'' interface.', 'The ''System.gc()'' method.', 'The ''java.lang.Runtime'' class.', 'The ''@PreDestroy'' annotation.', 'A', 'Introduced in Java 7, the try-with-resources statement automatically and deterministically closes any resource that implements the ''AutoCloseable'' interface the moment the try block exits, effectively replacing manual ''finally'' blocks and unreliable ''finalize()'' methods.');
