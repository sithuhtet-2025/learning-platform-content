INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Throw Exception', 'What is the precise functional role of the ''throw'' keyword in Java?', 'To declare that a method might fail in the future.', 'To actively halt current execution and hand an instantiated exception object over to the JVM runtime to initiate the exception propagation process.', 'To catch an exception thrown by a sub-thread.', 'To bypass the compiler''s exception checking mechanisms.', 'B', 'The ''throw'' keyword is an executable statement. It takes an instance of a ''Throwable'' object (like ''new IllegalArgumentException()'') and physically triggers the error state, forcing the JVM to stop normal execution and search the call stack for a handler.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Throw Exception', 'What will the Java compiler do if you place executable code immediately on the line following a ''throw'' statement within the same block?', 'It will compile it, but the code will never be reached at runtime.', 'It will automatically move the code into a ''finally'' block.', 'It will throw a compile-time error explicitly stating "unreachable statement".', 'It will execute the code before throwing the exception.', 'C', 'Because the ''throw'' statement unconditionally interrupts the normal flow of control, any code written immediately after it in the same scope cannot possibly be executed. The Java compiler detects this and rejects it as an "unreachable statement".');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Throw Exception', 'Which of the following syntax examples correctly utilizes the ''throw'' keyword?', 'throw Exception.class;', 'throw "Invalid Input";', 'throw new NullPointerException("Missing data");', 'throws new IOException();', 'C', 'The ''throw'' keyword must be followed by a valid, instantiated object that inherits from ''java.lang.Throwable''. You cannot throw classes, primitive types, or raw strings. You must use the ''new'' keyword to instantiate the exception object.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Throw Exception', 'Can you use the ''throw'' keyword to trigger a custom, user-defined exception class based on your application''s business logic?', 'No, ''throw'' only works with standard JDK exceptions.', 'Yes, provided the custom class extends ''Throwable'' (typically by extending ''Exception'' or ''RuntimeException'').', 'Yes, but the custom exception must be declared as an interface.', 'No, user-defined exceptions are thrown automatically by the JVM.', 'B', 'Custom business exceptions (e.g., ''throw new InsufficientFundsException()'') are one of the primary use cases for the ''throw'' keyword, allowing developers to flag violations of domain-specific rules.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Throw Exception', 'If you use ''throw'' to trigger a Checked Exception (e.g., ''throw new IOException();'') inside a method, what else is strictly required for the code to compile?', 'You must wrap the throw statement in a synchronized block.', 'You must also declare that exception in the method''s signature using the ''throws'' keyword, OR wrap the ''throw'' statement inside a try-catch block.', 'You must instantiate a FileDescriptor.', 'Nothing else is required; throwing it is sufficient.', 'B', 'Because Checked Exceptions are governed by the compiler''s "catch or specify" rule, actively throwing one means the method is now generating a checked exception. You must either handle it locally (try-catch) or specify it in the method signature (throws) to delegate it.');
