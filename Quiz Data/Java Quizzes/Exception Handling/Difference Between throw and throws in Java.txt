INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Difference Between throw and throws in Java', 'Where is the ''throws'' keyword strictly located in Java syntax?', 'Inside a catch block.', 'Inside a finally block.', 'Appended to the method signature, immediately before the opening curly brace of the method body.', 'Inside the try block.', 'C', 'The ''throws'' keyword is a declaration. It is placed in the method signature (e.g., ''public void readFile() throws IOException { ... }'') to warn the compiler and any calling methods that this specific method might propagate the listed checked exceptions.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Difference Between throw and throws in Java', 'What is the exact functional action performed by the ''throw'' keyword?', 'It declares a new exception class.', 'It actively interrupts the normal flow of execution by explicitly tossing an instantiated exception object to the JVM runtime.', 'It catches an exception.', 'It throws a warning to the compiler.', 'B', 'The ''throw'' keyword is an execution statement (e.g., ''throw new IllegalArgumentException();''). It physically creates the error state and hands the exception object directly to the JVM runtime, forcing the method to immediately halt and search for a catch block.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Difference Between throw and throws in Java', 'Regarding plurality, what is the crucial difference between ''throw'' and ''throws''?', 'They both only handle one exception at a time.', '''throws'' can only declare unchecked exceptions, while ''throw'' can handle both.', '''throw'' is followed by a single instance of an exception object, whereas ''throws'' can declare a comma-separated list of multiple exception classes that the method might propagate.', 'There is no difference in plurality.', 'C', 'You can only ''throw'' one specific exception object at the exact moment a failure occurs (e.g., ''throw new Exception();''). However, a complex method might fail in various ways, so ''throws'' can declare a list of potential failures (e.g., ''throws IOException, SQLException'').');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Difference Between throw and throws in Java', 'Can you use the ''throw'' keyword to trigger an unchecked exception (like ''NullPointerException'') inside a method without declaring it with ''throws'' in the method signature?', 'Yes, the compiler does not mandate that unchecked (Runtime) exceptions be declared with the ''throws'' keyword.', 'No, every exception triggered by ''throw'' must be declared by ''throws''.', 'Yes, but it will cause a memory leak.', 'No, the compiler will throw a syntax error.', 'A', 'Unchecked exceptions (subclasses of ''RuntimeException'') represent programming logic errors. The Java compiler allows you to explicitly ''throw'' them anywhere in your code without requiring you to declare them in the method signature using ''throws''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Difference Between throw and throws in Java', 'What is the concept of "Exception Re-throwing", and which keywords does it utilize?', 'It is when a catch block captures an exception, logs it, and uses the ''throw'' keyword to toss the exact same exception object back up the call stack to be handled by a higher-level method. The enclosing method must declare this using ''throws''.', 'It is when the JVM automatically throws an exception twice to ensure it is caught.', 'It uses only the ''throws'' keyword to bypass try blocks entirely.', 'It is the process of converting a checked exception into an Error.', 'A', 'Exception re-throwing is a common pattern where a method catches an exception to perform localized cleanup (like rolling back a database transaction), but then executes ''throw e;'' to pass the exception object up the chain. Because the exception escapes the method, the method signature must still include it in its ''throws'' clause.');
