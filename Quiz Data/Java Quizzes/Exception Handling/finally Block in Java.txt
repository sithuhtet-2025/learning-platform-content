INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('finally Block in Java', 'What is the primary architectural guarantee provided by the ''finally'' block in Java exception handling?', 'It executes only if the try block completes without any exceptions.', 'It guarantees execution only when a checked exception is caught.', 'It executes unconditionally after the try and catch blocks finish, regardless of whether an exception was thrown, caught, or left unhandled, making it ideal for resource cleanup.', 'It prevents the JVM from terminating if an OutOfMemoryError occurs.', 'C', 'The ''finally'' block is designed for critical cleanup operations (like closing file streams or database connections). The JVM ensures it runs regardless of the execution path taken through the preceding try and catch blocks.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('finally Block in Java', 'Under which of the following extreme circumstances will a ''finally'' block definitively FAIL to execute?', 'If the try block contains a ''return'' statement.', 'If an unhandled exception propagates out of the catch block.', 'If ''System.exit(0)'' is called, or if the underlying JVM process crashes or is killed by the OS.', 'If a NullPointerException occurs within the try block.', 'C', 'While ''finally'' survives exceptions and return statements, it cannot survive the death of the JVM itself. Calling ''System.exit()'' halts the Java process immediately, preventing any pending ''finally'' blocks from executing.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('finally Block in Java', 'If a ''try'' block contains a ''return'' statement (e.g., ''return "Success";''), when does the ''finally'' block execute?', 'The ''finally'' block is skipped entirely.', 'It executes immediately after the calling method receives the returned value.', 'It executes immediately before the ''return'' statement evaluates and hands control back to the caller.', 'It throws an IllegalStateException.', 'C', 'The JVM intercepts the ''return'' statement. It evaluates the return expression, suspends the return operation, executes the ''finally'' block, and only then actually returns the value to the calling method.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('finally Block in Java', 'What happens if an exception is thrown inside a ''catch'' block, but the associated ''finally'' block also throws a completely different exception?', 'Both exceptions are propagated up the call stack simultaneously.', 'The exception from the ''catch'' block is propagated, and the ''finally'' exception is ignored.', 'The JVM crashes with an InternalError.', 'The exception thrown by the ''finally'' block completely overrides the original exception, and only the ''finally'' exception propagates up the stack.', 'D', 'If a ''finally'' block completes abruptly by throwing an exception, it masks any previous exceptions that were currently in flight from the ''try'' or ''catch'' blocks. This is a notorious source of lost debugging information, known as "exception swallowing".');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('finally Block in Java', 'Is it syntactically legal to write a ''try'' block and a ''finally'' block without including any ''catch'' blocks?', 'No, a catch block is mandatory for exception handling.', 'Yes, but it will cause a compilation error if checked exceptions are thrown.', 'Yes, a ''try-finally'' structure is perfectly legal and is used when you want to ensure resource cleanup but prefer to let the exception propagate up the call stack.', 'No, unless the finally block is completely empty.', 'C', 'A ''try'' block requires either a ''catch'' block, a ''finally'' block, or both. Using ''try-finally'' without ''catch'' ensures that critical cleanup code runs, while intentionally allowing the exception to bubble up to a higher-level handler.');
