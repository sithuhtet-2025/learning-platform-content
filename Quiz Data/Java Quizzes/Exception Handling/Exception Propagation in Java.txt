INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Exception Propagation in Java', 'What exactly is "Exception Propagation" in the Java runtime environment?', 'The process of an exception automatically cloning itself across all active threads.', 'The process where an unhandled exception is passed backward up the method call stack, from the method where it occurred to the method that called it, until it is caught or reaches the top.', 'The process of converting a checked exception into an unchecked exception.', 'The process of writing an exception to a log file.', 'B', 'When an exception occurs and isn''t caught in the current method, the JVM abruptly terminates that method and drops the exception object down to the caller method. This "bubbling up" continues until a matching catch block is found in the call stack.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Exception Propagation in Java', 'Which category of exceptions is automatically and implicitly propagated up the call stack by the JVM without requiring any special keywords in the method signatures?', 'Checked Exceptions (e.g., IOException)', 'Unchecked Exceptions (e.g., NullPointerException, ArithmeticException)', 'Both Checked and Unchecked Exceptions', 'Errors only (e.g., OutOfMemoryError)', 'B', 'Unchecked exceptions (RuntimeExceptions and Errors) propagate up the call stack automatically. The compiler does not require you to declare them using the ''throws'' keyword to allow them to bubble up.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Exception Propagation in Java', 'If Method A calls Method B, and Method B throws a Checked Exception (like SQLException), what must Method B do to successfully propagate it back to Method A?', 'Method B must catch the exception and swallow it.', 'Method B must declare ''throws SQLException'' in its method signature to explicitly hand it back to the caller.', 'Method B must use the ''volatile'' keyword.', 'Checked exceptions cannot be propagated; they must be caught locally.', 'B', 'Unlike unchecked exceptions, Checked Exceptions do not propagate automatically. If Method B does not catch the exception internally, it must explicitly declare it in its ''throws'' clause to legally pass it back up the call stack to Method A.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Exception Propagation in Java', 'Assume Method1 calls Method2, which calls Method3. An exception is thrown in Method3. If Method2 has no try-catch block, but Method1 has a catch block that matches the exception, what happens?', 'The exception is caught in Method1, and execution resumes normally after Method1''s catch block. The remaining code in Method2 and Method3 is abandoned.', 'The exception is caught in Method1, and then execution jumps back to finish Method3.', 'The JVM crashes because Method2 failed to catch it.', 'The exception is ignored and dropped.', 'A', 'As the exception propagates, any code remaining in the methods it passes through (Method3 and Method2) is abruptly skipped. Once caught in Method1, execution resumes normally from the point immediately following Method1''s catch block.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Exception Propagation in Java', 'During exception propagation, what happens to the ''finally'' blocks belonging to the methods that the exception is passing through on its way up the call stack?', 'They are completely skipped to prioritize fast error handling.', 'They are queued and executed only after the exception is finally caught.', 'They are executed in reverse order as the JVM unwinds the stack frame for each method, guaranteeing resource cleanup even during an unhandled exception flight.', 'They cause the propagation to stop immediately.', 'C', 'As the JVM unwinds the call stack (destroying the execution frame of each method the exception leaves), it definitively executes any ''finally'' blocks attached to those methods before continuing the exception''s journey up to the next caller.');
