INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Custom (User-Defined) Exception in Java', 'If you need to create a custom exception that the compiler absolutely forces the calling method to catch or declare, which class must your custom exception extend?', 'java.lang.Error', 'java.lang.RuntimeException', 'java.lang.Throwable', 'java.lang.Exception', 'D', 'To create a checked exception (one that the compiler enforces handling for), your custom class must inherit from ''java.lang.Exception''. Inheriting from ''RuntimeException'' creates an unchecked exception.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Custom (User-Defined) Exception in Java', 'What is the standard and most crucial best practice when defining the constructors for a custom exception class?', 'To make all constructors private to prevent external instantiation.', 'To provide at least a no-argument constructor and a constructor that accepts a String message, passing that message to the parent class using ''super(message)''.', 'To initialize a new Thread within the constructor to handle the error asynchronously.', 'To override the ''printStackTrace()'' method in the constructor.', 'B', 'A well-designed custom exception should mimic standard exceptions by providing a constructor that accepts a detailed error message. This message must be passed to the superclass (via ''super(message)'') so it can be retrieved later using the ''getMessage()'' method.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Custom (User-Defined) Exception in Java', 'Under what architectural circumstance is creating a User-Defined Exception highly recommended over simply throwing a standard ''IllegalArgumentException'' or ''RuntimeException''?', 'When you want to bypass the JVM''s garbage collector.', 'When the application encounters a specific, domain-level business logic violation (e.g., ''InsufficientFundsException'') that requires distinct, targeted catch blocks for specialized recovery operations.', 'When you need to throw an exception from a static method.', 'When standard exceptions are too slow to instantiate.', 'B', 'Custom exceptions provide semantic meaning to business logic errors. They allow developers to write specific ''catch'' blocks for domain events (like ''InvalidTaxCodeException''), separating critical business error handling from generic programming errors.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Custom (User-Defined) Exception in Java', 'If your custom exception needs to wrap another underlying exception (like an ''IOException'' that caused your business rule to fail), what constructor signature should you implement?', 'MyException(Throwable cause)', 'MyException(String message, Throwable cause)', 'Both A and B', 'MyException(Exception[] causes)', 'C', 'To preserve the stack trace and the original cause of the failure (Exception Chaining), a robust custom exception should provide constructors that accept a ''Throwable cause'', passing it to the superclass constructor.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Custom (User-Defined) Exception in Java', 'Can a custom exception class contain its own unique fields and methods, distinct from the standard ''Exception'' class?', 'Yes, it is a standard Java class and can encapsulate additional context data (e.g., storing an error code or a transaction ID) and provide getter methods for them.', 'No, exception classes are strictly limited to the methods inherited from Throwable.', 'Yes, but only if the fields are declared as static.', 'No, because the JVM strips custom fields during the stack trace generation.', 'A', 'A custom exception is just a regular Java class. It is highly advantageous to add custom fields (like a specific ''errorCode'' or ''userId'') to carry structured diagnostic data directly within the exception object itself.');
