INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Exception Handling in Java', 'Which class sits at the absolute top of the exception hierarchy in Java, acting as the superclass for all errors and exceptions?', 'java.lang.Exception', 'java.lang.Error', 'java.lang.Throwable', 'java.lang.RuntimeException', 'C', 'The ''java.lang.Throwable'' class is the root class of the Java exception hierarchy. Only objects that are instances of this class (or one of its subclasses) are thrown by the Java Virtual Machine or can be thrown by the Java ''throw'' statement.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Exception Handling in Java', 'What is the primary technical distinction between a Checked Exception and an Unchecked Exception in Java?', 'Checked exceptions extend RuntimeException, while unchecked exceptions extend Error.', 'The compiler forces the programmer to handle or declare Checked Exceptions at compile time, whereas Unchecked Exceptions are not checked until runtime.', 'Checked exceptions crash the JVM, while unchecked exceptions only crash the thread.', 'Checked exceptions can only be thrown by the JVM, not by application code.', 'B', 'Checked exceptions (like IOException) represent anticipated external failures and must be explicitly caught or declared in the method signature (throws). Unchecked exceptions (RuntimeExceptions) represent logical programming bugs (like NullPointerException) and are not enforced by the compiler.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Exception Handling in Java', 'Is it syntactically legal to write a ''try'' block in Java without a corresponding ''catch'' block?', 'No, a try block must always be immediately followed by at least one catch block.', 'Yes, but only if the method signature includes a throws clause.', 'Yes, as long as it is immediately followed by a ''finally'' block or utilizes the try-with-resources syntax.', 'No, it causes a compilation error indicating missing exception handling.', 'C', 'A ''try'' block does not strictly require a ''catch'' block. It can be paired exclusively with a ''finally'' block to ensure resource cleanup, or it can stand alone if it is a try-with-resources block (which implicitly handles closing resources).');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Exception Handling in Java', 'If a try block has multiple catch blocks, what is the strict rule regarding the order of those catch blocks?', 'They must be ordered alphabetically by exception name.', 'They must be ordered from the most generic exception class (superclass) to the most specific (subclass).', 'They must be ordered from the most specific exception class (subclass) to the most generic (superclass).', 'The order does not matter; the JVM evaluates them simultaneously.', 'C', 'Java evaluates catch blocks sequentially. If a superclass (like Exception) is placed before a subclass (like IOException), the superclass block will intercept everything, rendering the subclass block unreachable and causing a compilation error.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Exception Handling in Java', 'What component of the JVM takes over if an exception is thrown but never caught anywhere in the application''s call stack?', 'The Garbage Collector.', 'The Default Uncaught Exception Handler.', 'The Security Manager.', 'The ClassLoader.', 'B', 'If an exception propagates all the way up the call stack without being caught, the JVM hands it to the Default Uncaught Exception Handler. This handler prints the exception name, message, and stack trace to the standard error stream, and then terminates that specific thread.');
