INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Multiple Catch Block in Java', 'When defining multiple catch blocks for a single try block, what is the absolute, compiler-enforced rule regarding their order?', 'They must be ordered alphabetically by exception class name.', 'They must be ordered from the most generic exception (superclass) to the most specific exception (subclass).', 'They must be ordered from the most specific exception (subclass) at the top, down to the most generic exception (superclass) at the bottom.', 'The order does not matter; the JVM evaluates all of them simultaneously.', 'C', 'Catch blocks must be ordered from subclass to superclass. Because the JVM evaluates catch blocks sequentially from top to bottom, placing a superclass (like ''Exception'') first would intercept every error, rendering any subsequent subclass blocks unreachable and causing a compilation error.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Multiple Catch Block in Java', 'What compile-time error is generated if you place ''catch(Exception e)'' immediately before ''catch(ArithmeticException e)''?', 'TypeMismatchException', 'Unreachable code', 'Duplicate local variable', 'Illegal modifier', 'B', 'Since ''ArithmeticException'' inherits from ''Exception'', the generic ''catch(Exception e)'' block acts as a universal net, catching the error first. The Java compiler detects that the specific ''catch(ArithmeticException e)'' block can never possibly be executed and flags it as "unreachable code".');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Multiple Catch Block in Java', 'If a try block throws an ''ArrayIndexOutOfBoundsException'', and the catch blocks are ''catch(ArithmeticException e)'', ''catch(NullPointerException e)'', and ''catch(Exception e)'', which block executes?', 'All of them execute sequentially.', 'The program crashes because the exact exception is missing.', 'Only the ''catch(Exception e)'' block executes.', 'The JVM creates a new catch block dynamically.', 'C', 'The JVM checks the catch blocks in order. It skips ''ArithmeticException'' and ''NullPointerException'' because they do not match. It then reaches ''catch(Exception e)''. Since ''ArrayIndexOutOfBoundsException'' is a subclass of ''Exception'', polymorphism allows this block to catch and handle it.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Multiple Catch Block in Java', 'Introduced in Java 7, what is the correct syntax for a "Multi-catch" block that handles multiple distinct exceptions within a single block to reduce code duplication?', 'catch(IOException , SQLException e)', 'catch(IOException & SQLException e)', 'catch(IOException | SQLException e)', 'catch(IOException || SQLException e)', 'C', 'Java 7 introduced the multi-catch feature using the bitwise OR pipe operator (''|''). This allows a single catch block to handle multiple unrelated exceptions (e.g., ''catch(IOException | SQLException e)''), significantly reducing duplicated error-handling code.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Multiple Catch Block in Java', 'When utilizing the Java 7 multi-catch syntax (e.g., ''catch(IOException | SQLException e)''), what implicit modifier is automatically applied to the exception parameter ''e''?', 'volatile', 'static', 'final', 'transient', 'C', 'In a multi-catch block, the catch parameter (''e'') is implicitly declared as ''final''. This is a strict compiler rule preventing you from reassigning the exception variable within the block, ensuring the type analysis remains sound during exception re-throwing.');
