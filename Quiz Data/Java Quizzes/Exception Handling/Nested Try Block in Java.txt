INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Nested Try Block in Java', 'What defines a "Nested Try Block" in Java?', 'A try block that does not have a catch block.', 'A try block that is placed entirely within another try block, catch block, or finally block.', 'A try block that spans multiple methods.', 'A try block specifically used for multithreading.', 'B', 'A nested try block is simply a try block declared inside the scope of another exception-handling block. This is commonly used when a specific line of code within a broader try block requires its own specialized, isolated error handling.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Nested Try Block in Java', 'In a nested try-catch architecture, what happens if the inner ''try'' block throws an exception, but the inner ''catch'' block does NOT match the exception type?', 'The JVM immediately terminates the program.', 'The exception is permanently swallowed.', 'The exception bubbles up and is passed to the outer ''catch'' block of the enclosing ''try'' statement.', 'The inner try block is restarted.', 'C', 'Exception propagation applies to nested blocks as well. If the inner ''catch'' handlers cannot process the exception, it propagates outward to the next enclosing scope. The outer ''catch'' blocks will then be evaluated to see if they can handle it.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Nested Try Block in Java', 'If the inner ''try'' block throws an exception and its corresponding inner ''catch'' block successfully handles it, does the outer ''catch'' block also execute?', 'Yes, exceptions always trigger all available catch blocks.', 'No, once the inner catch block handles the exception, the error state is resolved. Execution simply continues normally to the code following the inner try-catch structure, skipping the outer catch block entirely.', 'Yes, but only if the outer catch block catches the generic Exception class.', 'The outer catch block executes first, then the inner one.', 'B', 'Once an exception is successfully caught and handled by an inner block, it is considered neutralized. The outer catch block acts as a fallback; it will not execute unless the inner block fails to handle the exception or intentionally re-throws it.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Nested Try Block in Java', 'What is a highly common real-world architectural use case for deploying a try block nested inside a ''finally'' block?', 'To purposefully trigger a JVM crash.', 'To catch exceptions that might occur while attempting to close critical resources (like a database connection or a file stream) during the cleanup phase of the outer try block.', 'To restart the main thread if it fails.', 'To bypass security manager checks.', 'B', 'Because the ''finally'' block is used to close resources, the act of closing them (e.g., ''connection.close()'') can itself throw a checked exception (like ''SQLException''). To prevent this secondary exception from swallowing the primary one, it must be wrapped in its own nested try-catch block.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Nested Try Block in Java', 'Assume an outer try block handles ''Exception e'', and an inner nested try block handles ''ArithmeticException e''. What is the compiler''s restriction regarding these variable names?', 'Both variables must be named exactly the same.', 'The inner variable must be named ''e2'' or anything other than ''e'', because local variable declarations in nested scopes cannot shadow or duplicate variables already defined in the enclosing scope.', 'The outer variable must be declared as final.', 'There are no restrictions; variable shadowing is perfectly legal in catch blocks.', 'B', 'Java strictly forbids local variable shadowing within nested blocks. Because the outer catch parameter ''e'' is already in scope, declaring another parameter named ''e'' for the inner catch block results in a "duplicate local variable" compile-time error.');
