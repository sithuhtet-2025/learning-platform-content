INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java instanceof Keyword', 'What is the primary purpose of the ''instanceof'' operator in Java?', 'To instantiate a new object from a class definition.', 'To test whether the object on the left side is an instance of the class, subclass, or interface specified on the right side.', 'To compare the memory addresses of two object references.', 'To check if a primitive variable matches a specific data type.', 'B', 'The ''instanceof'' operator is a type comparison operator. It evaluates to true if the object reference on its left side is an instance of the specific type (class or interface) on its right side, or a subclass of it.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java instanceof Keyword', 'How does the ''instanceof'' operator evaluate if the object reference provided on the left-hand side is ''null''?', 'It throws a NullPointerException at runtime.', 'It evaluates to true, as null is considered a universal instance.', 'It evaluates to false without throwing any exceptions.', 'It causes a compilation error.', 'C', 'The ''instanceof'' operator is designed to be null-safe. If the reference variable being tested is null, the operator simply evaluates to false, avoiding a NullPointerException.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java instanceof Keyword', 'What occurs at compile time if you use ''instanceof'' to compare an object to a class that is entirely unrelated in the class hierarchy (e.g., checking if a ''String'' object is an ''instanceof'' ''Integer'')?', 'The code compiles but always evaluates to false at runtime.', 'The compiler throws an "inconvertible types" compilation error.', 'The compiler automatically casts the object.', 'The JVM throws a ClassCastException.', 'B', 'Java is strongly typed. If the compiler determines there is absolutely no inheritance relationship between the object''s declared type and the type being checked, it will reject the code with a compilation error.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java instanceof Keyword', 'Introduced formally in Java 16, what does "Pattern Matching for instanceof" accomplish?', 'It allows checking multiple types in a single ''instanceof'' statement using logical OR operators.', 'It automatically extracts and casts the tested object into a new locally scoped variable if the condition evaluates to true.', 'It allows ''instanceof'' to check the internal properties of an object using regular expressions.', 'It replaces the ''switch'' statement entirely.', 'B', 'Pattern Matching for ''instanceof'' (e.g., ''if (obj instanceof String s)'') eliminates the need for boilerplate explicit casting. If ''obj'' is a String, it is automatically cast and assigned to the local binding variable ''s''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java instanceof Keyword', 'Can the ''instanceof'' operator be used to check if an object implements a specific interface?', 'Yes, the syntax and behavior are exactly the same as checking against a class.', 'No, it can only be used to check against concrete or abstract classes.', 'Yes, but the interface must be cast first.', 'No, interfaces require the ''implements'' keyword at runtime.', 'A', 'The ''instanceof'' operator works seamlessly with interfaces. If the object''s class (or any of its parent classes) implements the specified interface, the expression evaluates to true.');
