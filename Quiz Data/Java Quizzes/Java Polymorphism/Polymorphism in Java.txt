INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Polymorphism in Java', 'What is the fundamental concept of Polymorphism in Object-Oriented Programming?', 'The ability of a class to inherit from multiple parent classes simultaneously.', 'The ability of a single variable, function, or object to take on multiple forms and behaviors depending on the context.', 'The mechanism used to hide the internal state of an object from the outside world.', 'The process of strictly defining types at compile time.', 'B', 'Polymorphism (from Greek "many forms") allows objects of different types to be treated as objects of a common super type. It enables a single interface or method signature to represent different underlying forms (data types or class implementations).');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Polymorphism in Java', 'When a parent class reference variable points to a child class object (e.g., ''Animal a = new Dog();''), what is this specific process called?', 'Downcasting', 'Method Overloading', 'Upcasting', 'Encapsulation', 'C', 'Upcasting occurs when a reference variable of a parent class points to an object of its child class. It is always safe and happens implicitly in Java, enabling runtime polymorphism.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Polymorphism in Java', 'Which of the following Java features is an example of Compile-Time Polymorphism?', 'Method Overriding', 'Method Overloading', 'Virtual Method Invocation', 'Interface Implementation', 'B', 'Method Overloading represents compile-time (or static) polymorphism. The Java compiler determines exactly which overloaded method to invoke during compilation based on the method signature (number and types of arguments passed).');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Polymorphism in Java', 'Given the statement ''Parent obj = new Child();'', where both classes declare an integer variable named ''value''. If you access ''obj.value'', which value is retrieved?', 'The value from the Child class, because of runtime polymorphism.', 'The value from the Parent class, because instance variables are not polymorphic and are resolved at compile time based on the reference type.', 'A compilation error occurs due to ambiguous variable resolution.', 'An IllegalAccessException is thrown.', 'B', 'In Java, runtime polymorphism strictly applies to instance methods, not instance variables (data members). Variable access is resolved at compile time via static binding based on the reference type (Parent), not the actual object type (Child).');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Polymorphism in Java', 'If you have an upcasted object reference (''Parent p = new Child();'') and you need to call a method that exists ONLY in the ''Child'' class, what must you do?', 'Call the method directly; the JVM will find it dynamically.', 'Change the method in the Child class to ''static''.', 'Perform an explicit downcast (e.g., ''((Child) p).childMethod();'').', 'It is impossible to call the method once the object is upcasted.', 'C', 'Because the reference type is ''Parent'', the compiler only allows access to methods defined in the ''Parent'' class. To access methods specific to the ''Child'' class, you must explicitly downcast the reference back to the ''Child'' type.');
