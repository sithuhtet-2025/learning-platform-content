INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Instance Initializer Block in Java', 'What is an Instance Initializer Block in Java?', 'A static method used to create singletons.', 'A block of code enclosed in curly braces { } defined at the class level, used to initialize instance variables.', 'A constructor that takes no arguments.', 'A block of code used exclusively for initializing static variables.', 'B', 'An instance initializer block is an unnamed block of code within a class used to run initialization logic whenever an object of that class is created, sharing code across multiple constructors.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Instance Initializer Block in Java', 'During the object creation process, when exactly does the JVM execute the Instance Initializer Block?', 'Before the superclass constructor is invoked.', 'After the superclass constructor completes, but immediately before the body of the current class''s constructor executes.', 'After the current class''s constructor has fully completed.', 'Only when the object is explicitly cast.', 'B', 'When ''new'' is called, the JVM first invokes the parent class constructor (via ''super()''). Immediately after the parent constructor completes and before the statements inside the current child constructor execute, the instance initializer blocks are run.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Instance Initializer Block in Java', 'If a class contains multiple Instance Initializer Blocks, in what order are they executed?', 'Alphabetical order of their internal variables.', 'Bottom to top, as they appear in the source file.', 'Top to bottom, in the exact textual order they appear in the source file.', 'They are executed concurrently on separate threads.', 'C', 'If a class defines more than one instance initializer block, the Java compiler executes them sequentially, strictly in the top-to-bottom order they are written in the source code file.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Instance Initializer Block in Java', 'What is a primary architectural reason a developer might choose to use an Instance Initializer Block instead of putting the logic directly into a constructor?', 'To initialize static constants.', 'To execute code strictly before the ''super()'' call.', 'To share common initialization code across all overloaded constructors without requiring them to call each other via ''this()''.', 'To handle checked exceptions that a constructor cannot catch.', 'C', 'If a class has multiple constructors that require the same complex initialization logic (like setting up arrays or loading configurations), placing that logic in an instance block guarantees it runs for every constructor, preventing code duplication.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Instance Initializer Block in Java', 'Can an Instance Initializer Block be used to initialize variables within an Anonymous Inner Class?', 'Yes, it is practically the only way to execute initialization logic in an anonymous class since they cannot have named constructors.', 'No, anonymous classes do not support initialization blocks.', 'Yes, but only if the anonymous class implements an interface, not if it extends a class.', 'No, they must use static initializer blocks.', 'A', 'Because anonymous inner classes inherently lack a name, you cannot write a constructor for them. Instance initializer blocks provide the standard mechanism to run setup code during the instantiation of an anonymous class.');
