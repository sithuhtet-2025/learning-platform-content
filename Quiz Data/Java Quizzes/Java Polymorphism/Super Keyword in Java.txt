INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Super Keyword in Java', 'What is the primary function of the ''super'' keyword in Java?', 'To act as a reference variable pointing to the immediate parent class object.', 'To bypass all access modifiers and access private parent data.', 'To invoke a method from an interface.', 'To call a static method of the current class.', 'A', 'The ''super'' keyword is a reference variable used to refer to the immediate parent class object. It is primarily used to access parent class fields, methods, and constructors that may be hidden or overridden by the subclass.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Super Keyword in Java', 'If a subclass overrides a method named ''display()'' from its parent class, how can the subclass explicitly invoke the parent''s original ''display()'' method?', 'By calling ''parent.display();''', 'By calling ''super.display();''', 'By calling ''this.display();''', 'It is impossible to call the parent''s method once it is overridden.', 'B', 'Using the ''super.methodName()'' syntax allows a subclass to bypass its own overriding implementation and explicitly invoke the method definition provided by its immediate parent class.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Super Keyword in Java', 'What is the strict syntactical rule regarding the use of ''super()'' to invoke a parent class constructor?', 'It can be placed anywhere inside the subclass constructor.', 'It must be the very first statement inside the subclass constructor.', 'It must be the last statement inside the subclass constructor.', 'It can only be used inside static blocks.', 'B', 'When invoking a parent class constructor using ''super()'' or ''super(args)'', it must strictly be the first executable statement within the child class''s constructor body. If not written explicitly, the compiler implicitly inserts a no-arg ''super()'' call as the first line.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Super Keyword in Java', 'Can the ''super'' keyword be used within a ''static'' method, such as ''public static void main(String[] args)''?', 'Yes, it can access static members of the parent class.', 'Yes, but only if the parent class is abstract.', 'No, because ''super'' refers to an instance of the parent class, and static context does not belong to any specific instance.', 'No, it will cause a runtime exception, though it compiles successfully.', 'C', 'The ''super'' keyword relies on an object instance reference. Because static methods belong to the class itself and execute without an object instance, using ''super'' (or ''this'') inside a static context causes a compile-time error.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Super Keyword in Java', 'What happens if a parent class only has a parameterized constructor (e.g., ''Parent(int x)'') and no default constructor, but the child class constructor does not explicitly call ''super(x)''?', 'The compiler automatically provides a default integer value and calls ''super(0)''.', 'The child class inherits the parent''s constructor automatically.', 'A compilation error occurs because the compiler attempts to implicitly insert a no-arg ''super()'' call, which does not exist in the parent class.', 'The code compiles but throws an InstantiationError at runtime.', 'C', 'If you do not explicitly call ''super(args)'' in a child constructor, the compiler silently inserts a call to the no-arg ''super()''. If the parent class lacks a no-arg constructor, this implicit call fails to compile.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Super Keyword in Java', 'If both a parent class and a child class define an instance variable named ''x'', what does the expression ''super.x'' within the child class evaluate to?', 'It causes an ambiguous variable compilation error.', 'It evaluates to the child class''s ''x'' variable.', 'It evaluates to the parent class''s hidden ''x'' variable.', 'It evaluates to null.', 'C', 'When a child class declares a variable with the same name as a parent class variable, it "hides" the parent variable. The child can still access the parent''s specific variable value by explicitly using the ''super.x'' syntax.');
