INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Synchronization in Java', 'What is the primary objective of using synchronization in a multithreaded Java application?', 'To execute multiple threads simultaneously on a single CPU core.', 'To prevent thread interference and memory consistency errors by ensuring that only one thread can access a shared resource at a time.', 'To automatically increase the priority of threads accessing critical data.', 'To prevent the JVM from performing garbage collection.', 'B', 'Synchronization provides mutual exclusion. It acts as a gatekeeper, ensuring that when multiple threads attempt to read and write shared data concurrently, only one thread can execute the critical section of code at any given time, preventing data corruption.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Synchronization in Java', 'When a thread attempts to enter a synchronized method but the intrinsic lock is already held by another thread, into which state does the JVM place the waiting thread?', 'WAITING', 'TERMINATED', 'BLOCKED', 'TIMED_WAITING', 'C', 'If a thread fails to acquire the monitor lock because another thread currently holds it, the JVM transitions the thread into the BLOCKED state. It will remain in this state until the lock is released and it successfully acquires it.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Synchronization in Java', 'What monitor lock is implicitly acquired when a thread executes a non-static ''synchronized'' method?', 'The lock of the Thread object currently executing the method.', 'The lock of the ''java.lang.Class'' object associated with the method.', 'A global application-level lock.', 'The intrinsic lock of the specific object instance (''this'') on which the method is being invoked.', 'D', 'Instance methods marked as synchronized automatically use the current object instance (referred to by ''this'') as their monitor lock. Other synchronized instance methods on the same object will block until the lock is released.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Synchronization in Java', 'Is it syntactically legal to declare a class constructor with the ''synchronized'' keyword?', 'No, it results in a compilation error.', 'Yes, it is common practice for thread-safe singletons.', 'Yes, but it strictly locks the Class object, not the instance.', 'No, but it causes a runtime exception rather than a compilation error.', 'A', 'Constructors cannot be synchronized. It is illogical because only the thread that creates an object should have access to it while it is being constructed. The Java compiler enforces this and will throw a syntax error.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Synchronization in Java', 'What defines a "Race Condition" in the context of Java concurrency?', 'A situation where the CPU executes threads faster than the memory can handle.', 'An anomaly that occurs when multiple threads access shared data and try to change it at the same time, leading to unpredictable and incorrect outcomes dependent on thread scheduling.', 'A condition where two threads wait for each other indefinitely.', 'A specific exception thrown when a thread finishes before its parent thread.', 'B', 'A race condition happens when the timing or sequence of thread execution affects the correctness of the program. For example, if two threads simultaneously read a variable, increment it, and write it back, one increment might be lost without proper synchronization.');
