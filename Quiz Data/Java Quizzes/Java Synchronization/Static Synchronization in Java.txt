INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Static Synchronization in Java', 'When a method is declared as ''public static synchronized void process()'', which specific lock does the executing thread acquire?', 'The intrinsic lock of the specific object instance calling the method.', 'The intrinsic lock of the Thread currently executing the code.', 'The intrinsic lock of the ''java.lang.Class'' object associated with the class defining the method.', 'A global JVM lock that pauses all other classes.', 'C', 'Static methods belong to the class, not an instance. Therefore, static synchronization acquires the monitor lock on the Class object itself (e.g., ''MyClass.class''), ensuring only one thread can execute ANY static synchronized method of that class at a time.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Static Synchronization in Java', 'If Thread A is executing a ''static synchronized'' method, and Thread B attempts to execute a non-static ''synchronized'' instance method on an object of the exact same class, what happens?', 'Thread B blocks until Thread A finishes.', 'Both threads execute concurrently without interfering with each other.', 'Thread A is preempted and paused by Thread B.', 'A Deadlock immediately occurs.', 'B', 'Thread A holds the class-level lock (''MyClass.class''), while Thread B holds an object-level instance lock (''this''). Because they are acquiring two completely different locks, they do not block each other and run concurrently.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Static Synchronization in Java', 'What is the correct syntax to create a synchronized block that perfectly mimics the locking behavior of a ''static synchronized'' method within a class named ''DataProcessor''?', 'synchronized(this) { }', 'synchronized(DataProcessor.class) { }', 'synchronized(static) { }', 'synchronized(new DataProcessor()) { }', 'B', 'To achieve static synchronization within a block, you must explicitly pass the class literal (''DataProcessor.class'') as the monitor object. This locks the exact same Class object that a static synchronized method uses.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Static Synchronization in Java', 'What is the primary architectural purpose of using Static Synchronization in a multithreaded environment?', 'To protect instance variables from being modified concurrently by different threads.', 'To enforce a strict chronological execution order among all active threads.', 'To protect shared, static class variables (global state) from data corruption when accessed by multiple threads simultaneously.', 'To prevent the class from being garbage collected.', 'C', 'Because static variables are shared across all instances of a class, synchronizing on an individual instance (''this'') will not prevent other instances from modifying the static data. Static synchronization locks the Class object, providing a global gatekeeper for that shared static state.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Static Synchronization in Java', 'Assume a class has two different ''static synchronized'' methods: ''method1()'' and ''method2()''. If Thread 1 is executing ''method1()'', can Thread 2 concurrently execute ''method2()''?', 'Yes, because they are different methods.', 'Yes, as long as they access different static variables.', 'No, Thread 2 will block until Thread 1 completes ''method1()''.', 'No, it throws a ConcurrentModificationException.', 'C', 'Both static synchronized methods require the exact same monitor lock—the Class object. Since Thread 1 holds this lock while inside ''method1()'', Thread 2 must wait to acquire the same lock before it can enter ''method2()''.');
