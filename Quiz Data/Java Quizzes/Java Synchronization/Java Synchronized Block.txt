INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Synchronized Block', 'What is the primary architectural advantage of using a ''synchronized'' block over declaring an entire method as ''synchronized''?', 'It completely eliminates the possibility of a deadlock.', 'It locks the entire class rather than just the object instance.', 'It reduces the scope of the lock to only the critical section of code, minimizing thread waiting time and improving overall application performance.', 'It allows primitive data types to be used as monitor locks.', 'C', 'Synchronizing an entire method locks the object for the duration of the method, even during non-critical operations. A synchronized block restricts the lock only to the specific lines of code that access shared resources, maximizing concurrent execution.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Synchronized Block', 'What occurs at runtime if the object reference passed into a synchronized block (e.g., ''synchronized(myObject) { ... }'') evaluates to ''null''?', 'The JVM silently skips the block and continues execution.', 'The block automatically falls back to locking on the ''this'' reference.', 'The compiler catches it and throws a syntax error.', 'The JVM immediately throws a NullPointerException.', 'D', 'A synchronized block requires a valid, instantiated object to act as the monitor lock. Attempting to synchronize on a ''null'' reference will inherently result in a NullPointerException at runtime.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Synchronized Block', 'Can you legally use a primitive variable (like an ''int'' or ''boolean'') as the lock parameter for a synchronized block?', 'Yes, the JVM automatically boxes it into a wrapper class.', 'No, primitive types do not possess intrinsic monitor locks; the compiler will throw an error.', 'Yes, but only if the primitive is declared as ''final''.', 'No, primitive types cause an IllegalMonitorStateException at runtime.', 'B', 'In Java, every object has an intrinsic lock (monitor). Primitive types are not objects and therefore do not have monitors. Passing a primitive to a synchronized block results in a compile-time "unexpected type" error.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Synchronized Block', 'If a thread encounters a RuntimeException while executing code deep inside a synchronized block, what happens to the monitor lock it currently holds?', 'The lock is held indefinitely, causing a deadlock for any waiting threads.', 'The JVM automatically releases the lock as the thread unwinds the call stack out of the synchronized block.', 'The lock is transferred to the thread that catches the exception.', 'The lock must be manually released in a ''finally'' block.', 'B', 'Java guarantees that whether a thread exits a synchronized block normally or abruptly via an unhandled exception, the JVM will automatically and safely release the monitor lock, preventing accidental deadlocks.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Synchronized Block', 'What is the functional difference between writing ''public synchronized void doWork()'' and ''public void doWork() { synchronized(this) { ... } }''?', 'The block version executes faster because it skips JVM lock validation.', 'There is no functional difference; both acquire the intrinsic lock of the current object instance for the duration of the enclosed code.', 'The method version locks the class, while the block version locks the instance.', 'The block version cannot be interrupted by the garbage collector.', 'B', 'Functionally, synchronizing the entire method and synchronizing a block using ''this'' that spans the entire method body achieve the exact same result: both acquire the instance''s intrinsic monitor lock.');
