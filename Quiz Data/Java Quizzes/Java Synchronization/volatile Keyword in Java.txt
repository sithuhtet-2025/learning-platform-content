INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('volatile Keyword in Java', 'What specific multithreading problem does the ''volatile'' keyword resolve?', 'Deadlocks caused by circular dependencies.', 'The visibility problem, where a thread caches a variable''s value in CPU registers and fails to see updates made to that variable by other threads in main memory.', 'Race conditions during compound operations like incrementing (i++).', 'Thread starvation caused by unfair lock allocation.', 'B', 'The ''volatile'' keyword guarantees memory visibility. It instructs the JVM and CPU to never cache the variable locally. Every read of a volatile variable will be fetched directly from main RAM, and every write will be flushed directly to main RAM.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('volatile Keyword in Java', 'Does declaring a variable as ''volatile'' guarantee thread safety for compound operations like ''count++''?', 'Yes, ''volatile'' automatically synchronizes all operations on the variable.', 'Yes, but only for primitive data types.', 'No, ''count++'' is a read-modify-write operation. ''volatile'' guarantees visibility but not atomicity, so race conditions can still occur.', 'No, because ''volatile'' only applies to reference variables.', 'C', 'While ''volatile'' guarantees that threads see the most up-to-date value, it does not provide mutual exclusion. A compound operation like ''count++'' actually involves three steps (read, increment, write). Multiple threads can still interleave these steps, causing data loss.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('volatile Keyword in Java', 'Which of the following can legally be declared as ''volatile'' in Java?', 'Local variables inside a method.', 'Classes and Interfaces.', 'Methods and Constructors.', 'Instance and static variables (fields).', 'D', 'The ''volatile'' keyword is strictly a field modifier. It can only be applied to instance variables or static variables. It cannot be applied to local variables, methods, or classes.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('volatile Keyword in Java', 'What is the "Happens-Before" guarantee provided by the ''volatile'' keyword in the Java Memory Model?', 'A read of a volatile variable always happens before any writes to it.', 'A write to a volatile variable happens-before every subsequent read of that same volatile variable, ensuring the updated value is strictly visible to all other threads.', 'Object instantiation happens before the volatile variable is initialized.', 'The main thread always executes before any volatile thread.', 'B', 'The Java Memory Model establishes a happens-before relationship for volatile fields. It guarantees that any memory writes made by a thread before writing to a volatile variable will be visible to another thread after it reads that same volatile variable.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('volatile Keyword in Java', 'How does the performance impact of ''volatile'' compare to full method ''synchronization''?', '''volatile'' is generally slower because it forces constant RAM access.', '''volatile'' is generally faster and lighter because it does not acquire monitor locks and cannot block or suspend threads.', 'They have the exact same performance overhead.', '''volatile'' causes the garbage collector to run continuously, degrading performance.', 'B', 'Using ''volatile'' is a lighter-weight synchronization mechanism than ''synchronized'' blocks. Because it does not involve acquiring locks or context switching blocked threads, it has less execution overhead, though it is limited strictly to visibility guarantees.');
