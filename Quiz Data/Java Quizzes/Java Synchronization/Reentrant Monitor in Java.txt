INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Reentrant Monitor in Java', 'What does it mean that Java''s intrinsic monitor locks are "reentrant"?', 'A thread can force another thread to release its lock.', 'If a thread currently holds the lock for an object, it can freely enter other synchronized blocks or methods locked on that exact same object without blocking itself.', 'The lock can be shared by multiple threads simultaneously if they are reading data.', 'Once a lock is released, it automatically re-enters the JVM lock pool.', 'B', 'Reentrancy means that locks are assigned on a per-thread basis, not per-invocation. A thread can re-acquire a monitor lock it already owns without deadlocking itself, which is crucial for recursive synchronized methods.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Reentrant Monitor in Java', 'How does the JVM internally track and manage a reentrant lock to know when it should finally be released to other threads?', 'By generating a unique hash for every synchronized block entered.', 'By maintaining an ownership thread ID and a "hold count" variable that increments on entry and decrements on exit.', 'By using a decentralized network of semaphores.', 'By tracking the depth of the Thread Stack execution frames.', 'B', 'When a thread acquires a lock, the JVM records the thread ID and sets the hold count to 1. Each re-entry increments the count. Each exit from a synchronized block decrements it. The lock is only fully released when the hold count reaches 0.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Reentrant Monitor in Java', 'Assume a synchronized instance method ''MethodA()'' calls another synchronized instance method ''MethodB()'' on the exact same object. What happens when Thread 1 executes ''MethodA()''?', 'Thread 1 successfully executes both methods because it already holds the object''s intrinsic lock.', 'Thread 1 throws an IllegalMonitorStateException.', 'Thread 1 blocks forever at MethodB(), causing a self-deadlock.', 'The JVM temporarily releases the lock before entering MethodB().', 'A', 'Because Java monitors are reentrant, Thread 1, having acquired the lock for ''this'' upon entering MethodA, can seamlessly invoke MethodB on ''this'' without any blocking or delay.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Reentrant Monitor in Java', 'What severe architectural flaw would occur in Java if intrinsic locks were NOT reentrant?', 'Synchronized methods would run slower due to excessive context switching.', 'Object-oriented designs involving recursive synchronized methods or overriding synchronized methods that call ''super.method()'' would result in unavoidable self-deadlocks.', 'Threads would bypass locks entirely, causing race conditions.', 'The JVM would run out of memory tracking lock states.', 'B', 'If a thread could not re-acquire a lock it already held, calling a synchronized parent method from a synchronized overridden child method (using ''super'') would instantly cause the thread to wait forever for a lock it is currently holding.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Reentrant Monitor in Java', 'Does the reentrant nature of Java monitors apply to the explicit locks provided in the ''java.util.concurrent.locks'' package?', 'No, explicit locks are strictly non-reentrant.', 'Yes, the standard ''ReentrantLock'' class explicitly mirrors this behavior, allowing the owning thread to acquire the lock multiple times.', 'Yes, but it requires a specialized ''RecursiveLock'' class.', 'Only if the explicit lock is initialized with the ''fairness'' parameter set to true.', 'B', 'The ''java.util.concurrent.locks.ReentrantLock'' class implements the ''Lock'' interface and deliberately provides the exact same reentrant mutual exclusion semantics as the implicit monitor locks used by ''synchronized'' methods.');
