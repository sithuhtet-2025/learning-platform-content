INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Interrupting a Thread', 'What does invoking the ''thread.interrupt()'' method actually do to a running Java thread?', 'It forcefully terminates and destroys the thread immediately.', 'It pauses the thread indefinitely until ''resume()'' is called.', 'It sets an internal boolean "interrupt status" flag to true, politely requesting that the thread stop what it is doing.', 'It throws a ThreadDeath error.', 'C', 'Java''s interruption mechanism is strictly cooperative. Calling ''interrupt()'' does not forcefully kill a thread; it merely sets an internal interrupt flag. It is entirely up to the target thread''s code to check this flag and gracefully terminate itself.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Interrupting a Thread', 'Under what exact circumstances will a thread throw an ''InterruptedException''?', 'When it attempts to divide by zero.', 'When it is interrupted while actively executing a CPU-intensive loop.', 'When it is blocked in methods like ''Thread.sleep()'', ''Object.wait()'', or ''Thread.join()'' and its interrupt flag is set.', 'When it tries to acquire a lock that is already held.', 'C', 'An ''InterruptedException'' is specifically thrown when a thread is interrupted while it is in a blocking state (such as sleeping, waiting, or joining). CPU-bound code that is actively running will not throw this exception when interrupted.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Interrupting a Thread', 'What happens to the internal interrupt status flag of a thread immediately after an ''InterruptedException'' is caught in a catch block?', 'The flag remains set to true.', 'The flag is automatically cleared (reset to false).', 'The thread is terminated.', 'The flag is permanently locked.', 'B', 'When a blocking method like ''sleep()'' or ''wait()'' detects an interrupt and throws an ''InterruptedException'', it automatically clears the thread''s interrupt status flag. If you catch the exception, the thread is no longer considered "interrupted" by the JVM.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Interrupting a Thread', 'What is the critical difference between the static method ''Thread.interrupted()'' and the instance method ''thread.isInterrupted()''?', 'There is no difference; one is just older syntax.', '''interrupted()'' checks the current thread and clears the interrupt flag, while ''isInterrupted()'' checks a specific thread and leaves the flag unchanged.', '''isInterrupted()'' clears the flag, while ''interrupted()'' does not.', '''interrupted()'' throws an exception, while ''isInterrupted()'' returns a boolean.', 'B', 'Both methods return the boolean state of the interrupt flag. However, the static ''Thread.interrupted()'' checks the current thread AND resets the flag to false. The instance method ''isInterrupted()'' only reads the state without modifying it.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Interrupting a Thread', 'If a thread catches an ''InterruptedException'' inside a ''Runnable'' but cannot gracefully stop the task itself, what is the highly recommended best practice?', 'Swallow the exception and do nothing (empty catch block).', 'Print a stack trace and continue execution.', 'Call ''System.exit(0)''.', 'Restore the interrupt status by calling ''Thread.currentThread().interrupt()'' so code higher up the call stack knows the interrupt occurred.', 'D', 'Because catching ''InterruptedException'' clears the flag, ignoring it hides the interrupt request from the rest of the application. Best practice dictates that if you do not throw the exception further, you must re-interrupt the current thread to restore the flag.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Interrupting a Thread', 'Which of the following blocking operations in Java is strictly UNINTERRUPTIBLE (i.e., it ignores ''interrupt()'' calls and does not throw ''InterruptedException'')?', 'Waiting to acquire an intrinsic monitor lock entering a ''synchronized'' block.', 'Waiting on a ''CountDownLatch''.', 'Sleeping via ''Thread.sleep()''.', 'Waiting to acquire a lock via ''ReentrantLock.lockInterruptibly()''.', 'A', 'A thread blocked waiting to enter a standard ''synchronized'' block or method cannot be interrupted. It will wait indefinitely for the intrinsic lock, completely ignoring the interrupt flag. This is why ''java.util.concurrent.locks.Lock'' provides interruptible lock alternatives.');
