INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Serialization and Deserialization in Java', 'What is the primary architectural purpose of Serialization in Java?', 'To convert bytecode back into human-readable Java source code.', 'To encrypt an object''s data to prevent unauthorized access in memory.', 'To convert the state of a Java object into a platform-independent byte stream so it can be saved to a file, database, or transmitted over a network.', 'To automatically generate getter and setter methods for a class.', 'C', 'Serialization flattens a complex object graph in memory into a linear stream of bytes. This byte stream can then be persisted to storage or sent across a network, preserving the object''s state for later reconstruction (deserialization).');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Serialization and Deserialization in Java', 'Which interface must a class implement to explicitly allow the JVM to serialize its instances?', 'java.io.Serializable', 'java.lang.Cloneable', 'java.util.Observable', 'java.io.ExternalizableOnly', 'A', 'A class must implement the ''java.io.Serializable'' interface to opt-in to Java''s standard serialization mechanism. If an object of a class that does not implement this interface is passed to an ObjectOutputStream, a NotSerializableException is thrown.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Serialization and Deserialization in Java', 'What distinguishes the ''Serializable'' interface from a typical functional interface like ''Runnable''?', 'It requires implementing the ''serialize()'' and ''deserialize()'' methods.', 'It is a "Marker Interface", meaning it contains absolutely no methods or fields, acting merely as a flag for the JVM.', 'It can only be implemented by abstract classes.', 'It automatically makes all class variables public.', 'B', 'The ''Serializable'' interface is an empty marker interface. It provides no methods to implement. It simply serves as metadata indicating to the JVM''s serialization engine that instances of this class are permitted to be serialized.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Serialization and Deserialization in Java', 'What is the function of the ''serialVersionUID'' field in a serializable class?', 'It encrypts the byte stream using a unique version key.', 'It serves as a version control ID to ensure that the class definition used during deserialization matches the one used during serialization, preventing InvalidClassExceptions.', 'It determines the maximum size (in bytes) of the serialized object.', 'It automatically assigns a database primary key to the object.', 'B', 'The JVM uses the ''serialVersionUID'' to verify class compatibility. If the class structure changes (e.g., adding a field) after an object is serialized, the IDs will mismatch during deserialization, and the JVM will throw an ''InvalidClassException'' to prevent state corruption.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Serialization and Deserialization in Java', 'Which two IO classes are the standard engines used for the physical process of writing and reading serialized objects in Java?', 'FileWriter and FileReader', 'DataOutputStream and DataInputStream', 'ObjectOutputStream and ObjectInputStream', 'BufferedOutputStream and BufferedInputStream', 'C', 'The ''java.io.ObjectOutputStream'' class contains the ''writeObject()'' method used to serialize an object to a stream, while ''java.io.ObjectInputStream'' contains the ''readObject()'' method used to deserialize the byte stream back into a Java object.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Serialization and Deserialization in Java', 'If a serializable class named ''Department'' contains an instance variable pointing to an ''Employee'' object, what is required for the ''Department'' object to serialize successfully?', 'The Employee variable must be marked as transient.', 'The Employee class must also implement the Serializable interface.', 'The Department class must implement the Externalizable interface instead.', 'The Employee object is automatically skipped during serialization.', 'B', 'Serialization cascades through the entire object graph. If an object contains references to other objects, all of those referenced classes must also implement ''Serializable''. If the engine encounters a non-serializable reference, it throws a ''NotSerializableException''.');
