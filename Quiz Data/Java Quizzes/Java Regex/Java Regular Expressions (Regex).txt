INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Regular Expressions (Regex)', 'Which two core classes are primarily utilized in Java to work with Regular Expressions?', 'java.util.Regex and java.util.Match', 'java.lang.String and java.lang.StringBuilder', 'java.util.regex.Pattern and java.util.regex.Matcher', 'java.io.RegexReader and java.io.RegexWriter', 'C', 'The ''java.util.regex'' package contains the ''Pattern'' class (representing the compiled regex) and the ''Matcher'' class (the engine that performs match operations on a character sequence).');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Regular Expressions (Regex)', 'In a Java string literal, how do you represent the regex predefined character class for a digit (\d)?', '"\d"', '"\\d"', '"/d"', '"//d"', 'B', 'Because the backslash (\) is an escape character in Java string literals, you must use a double backslash (\\) to pass a single backslash to the regex engine, resulting in ''\\d''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Regular Expressions (Regex)', 'What is the difference between the Matcher methods ''matches()'' and ''find()''?', '''matches()'' looks for the pattern anywhere in the string, while ''find()'' requires the entire string to match the pattern.', '''matches()'' attempts to match the entire region against the pattern, while ''find()'' scans the sequence looking for the next subsequence that matches the pattern.', 'They are identical; ''find()'' is just the deprecated legacy method.', '''matches()'' returns a boolean, while ''find()'' returns a String.', 'B', 'The ''matches()'' method strictly evaluates if the exact, entire input sequence matches the regex. The ''find()'' method searches for any occurrence of the pattern within the input sequence.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Regular Expressions (Regex)', 'Which regex quantifier indicates that the preceding element should appear "zero or more times"?', '+', '?', '*', '{0,1}', 'C', 'The asterisk (*) quantifier matches the preceding element zero or more times. The plus (+) matches one or more, and the question mark (?) matches zero or one time.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Regular Expressions (Regex)', 'What does the Matcher method ''group(0)'' return after a successful match operation?', 'The first explicitly captured group enclosed in parentheses.', 'The total number of capturing groups in the pattern.', 'The entire substring that successfully matched the regex pattern.', 'It throws an IndexOutOfBoundsException because groups are 1-indexed.', 'C', 'In Java regex, capturing groups are numbered starting from 1. However, group 0 is a special group that always represents the entire sequence matched by the complete regular expression.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Regular Expressions (Regex)', 'Which boundary matcher ensures that a pattern is matched only at the beginning of a line or string?', '$', '\b', '\A', '^', 'D', 'The caret (^) symbol acts as an anchor or boundary matcher asserting that the subsequent pattern must occur strictly at the very beginning of the string (or line, in multiline mode).');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Regular Expressions (Regex)', 'How can you apply case-insensitivity to a compiled Pattern in Java?', 'Pattern.compile(regex, Pattern.CASE_INSENSITIVE)', 'Pattern.compile(regex, "i")', 'Pattern.compile(regex).setFlags(Pattern.IGNORE_CASE)', 'By prefixing the regex string with "/i/"', 'A', 'When compiling a pattern, you can pass a flag as a second argument to ''Pattern.compile()''. Using ''Pattern.CASE_INSENSITIVE'' instructs the engine to ignore capitalization during matching.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Regular Expressions (Regex)', 'Given the regex "[^abc]", what does it match?', 'Any string containing the literal characters "^abc".', 'Any single character that is an "a", "b", or "c".', 'Any single character EXCEPT "a", "b", or "c".', 'A string starting with "a", "b", or "c".', 'C', 'Inside a character class (square brackets), a caret (^) at the very beginning acts as a negation operator. It matches any one character not explicitly listed in the brackets.');
