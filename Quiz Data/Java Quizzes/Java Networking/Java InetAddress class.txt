INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java InetAddress class', 'Which of the following is the correct way to instantiate an ''InetAddress'' object?', 'InetAddress ip = new InetAddress("127.0.0.1");', 'InetAddress ip = InetAddress.getByName("www.example.com");', 'InetAddress ip = InetAddress.create("192.168.1.1");', 'InetAddress ip = new InetAddress();', 'B', 'The ''InetAddress'' class does not have any public constructors. You must use its static factory methods, such as ''getByName()'', ''getAllByName()'', or ''getLocalHost()'', to obtain an instance.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java InetAddress class', 'What exception is thrown by ''InetAddress.getByName()'' if the provided hostname cannot be resolved to an IP address by the DNS?', 'SocketException', 'NullPointerException', 'UnknownHostException', 'ConnectException', 'C', 'If the DNS resolution fails or the host is completely unreachable/non-existent, the method throws a ''java.net.UnknownHostException''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java InetAddress class', 'Which two subclasses of ''InetAddress'' handle the specific implementations for different IP protocol versions?', 'InetAddressV4 and InetAddressV6', 'Inet4Address and Inet6Address', 'IPv4Address and IPv6Address', 'NetworkAddress4 and NetworkAddress6', 'B', '''Inet4Address'' and ''Inet6Address'' are the two concrete subclasses of ''InetAddress'' that represent IPv4 and IPv6 addresses, respectively.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java InetAddress class', 'What does the ''InetAddress.getLocalHost()'' method return?', 'The loopback address (127.0.0.1) explicitly.', 'An array of all active IP addresses on the machine.', 'An InetAddress object representing the local machine''s primary configured network interface and hostname.', 'The IP address of the default gateway router.', 'C', 'The ''getLocalHost()'' method retrieves the IP address and hostname configured for the local machine on the network. It does not strictly return the localhost loopback address unless the machine has no other network interfaces.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java InetAddress class', 'If a high-traffic domain name resolves to multiple IP addresses for load balancing, which method retrieves all of them?', 'InetAddress.getAllByName(String host)', 'InetAddress.getAddresses(String host)', 'InetAddress.getByName(String host)', 'InetAddress.resolveAll(String host)', 'A', 'The ''getAllByName(String host)'' method returns an array of ''InetAddress'' objects, containing all the IP addresses that the DNS server has registered for that specific hostname.');
