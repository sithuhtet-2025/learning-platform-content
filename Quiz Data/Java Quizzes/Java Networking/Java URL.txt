INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java URL', 'When parsing a string into a ''java.net.URL'' object, what exception is thrown if the protocol identifier (e.g., "http", "ftp") is missing or unrecognized by the JVM?', 'IllegalArgumentException', 'UnknownServiceException', 'MalformedURLException', 'ProtocolException', 'C', 'The ''URL'' constructor immediately throws a ''MalformedURLException'' if the string provided does not specify a valid, supported protocol scheme or if the string cannot be parsed as a URL.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java URL', 'What does the ''URL.getPort()'' method return if a port number is NOT explicitly defined in the URL string (e.g., "http://www.example.com/index.html")?', '80 (the default HTTP port)', '0', 'An exception is thrown.', '-1', 'D', 'If the port is not explicitly stated in the URL string, ''getPort()'' returns -1. To get the default port for the protocol (like 80 for HTTP), you must use the ''getDefaultPort()'' method instead.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java URL', 'Which component of the URL "https://example.com/search?q=java#results" is returned by the ''URL.getRef()'' method?', 'search', 'q=java', 'results', 'https', 'C', 'The ''getRef()'' method returns the "reference" or "anchor" portion of the URL, which is the string appearing after the hash/pound sign (#). In this case, it is "results".');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java URL', 'What is the primary difference between the ''URL.getPath()'' and ''URL.getFile()'' methods?', 'There is no difference; they are aliases.', '''getPath()'' returns the directory structure, while ''getFile()'' strictly returns the filename at the end of the path.', '''getPath()'' returns only the path component, whereas ''getFile()'' returns the path component concatenated with the query string.', '''getFile()'' downloads the resource to the local disk, while ''getPath()'' just reads the string.', 'C', 'For a URL like "http://host/path?query", ''getPath()'' returns "/path", while ''getFile()'' returns "/path?query" (the exact file part the server needs to locate the resource dynamically).');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java URL', 'Why is it considered a bad practice to use the ''equals()'' method of the ''java.net.URL'' class to compare two URLs?', 'It compares the memory addresses of the objects, not their string values.', 'It triggers a blocking DNS lookup to see if both hostnames resolve to the same IP address, causing severe performance issues.', 'It ignores the protocol (HTTP vs HTTPS) during comparison.', 'It always returns false if the query parameters are in a different order.', 'B', 'The ''URL.equals()'' method performs synchronous DNS resolution to compare the IP addresses of the hosts. This is a notorious design flaw that can cause massive thread blocking. The modern recommendation is to use ''java.net.URI'' for URL string manipulation and comparison instead.');
