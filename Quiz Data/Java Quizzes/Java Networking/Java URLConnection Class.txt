INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java URLConnection Class', 'What is the architectural relationship between ''URLConnection'' and ''HttpURLConnection'' in the Java networking API?', '''URLConnection'' is an interface that ''HttpURLConnection'' implements.', '''HttpURLConnection'' is an abstract superclass of ''URLConnection''.', '''URLConnection'' is an abstract base class, and ''HttpURLConnection'' is its abstract subclass tailored for HTTP-specific features.', 'They are unrelated classes used for completely different network layers.', 'C', '''java.net.URLConnection'' is a general-purpose abstract class representing a connection to any URL. ''HttpURLConnection'' extends it to provide methods specific to the HTTP protocol (like ''getResponseCode()'').');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java URLConnection Class', 'At what exact point is the physical network connection established when working with a ''URLConnection'' object?', 'Immediately when ''URL.openConnection()'' is called.', 'When the URLConnection object is instantiated.', 'Only when you explicitly call ''connect()'' or implicitly trigger it by requesting the InputStream, OutputStream, or Header fields.', 'When the ''URL'' object is created.', 'C', 'Calling ''URL.openConnection()'' merely creates the connection object locally. The actual TCP network handshake and data transmission do not occur until you call ''connect()'' or request data (e.g., ''getInputStream()'', ''getHeaderField()'').');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java URLConnection Class', 'If you want to configure a ''URLConnection'' to send a request body (e.g., uploading a file or sending JSON data), which method must be called before opening the streams?', 'setDoInput(true)', 'setDoOutput(true)', 'setUseCaches(false)', 'setAllowUserInteraction(true)', 'B', 'A ''URLConnection'' is configured for input (reading) by default. To allow output (writing data to the server), you must explicitly invoke ''setDoOutput(true)'' before the connection is established.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java URLConnection Class', 'Which method of ''URLConnection'' is used to retrieve metadata about the resource, such as the "Content-Type" or "Content-Length", sent by the server before the actual body data?', 'getMetaData()', 'getServerProperties()', 'getHeaderField(String name)', 'getStreamInfo()', 'C', 'The ''getHeaderField(String name)'' method (along with specialized methods like ''getContentType()'' and ''getContentLength()'') parses the HTTP headers returned by the server to provide metadata about the response.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java URLConnection Class', 'What happens if you attempt to call configuration methods like ''setRequestProperty()'' or ''setDoOutput()'' after the ''URLConnection'' has already been connected?', 'The configuration is silently ignored.', 'An IllegalStateException is thrown.', 'The connection is automatically restarted with the new settings.', 'A ProtocolException is thrown.', 'B', 'Configuration variables must be set before the physical network connection is made. Attempting to modify request headers or connection states after it has connected violates the state machine and throws an ''IllegalStateException''.');
