INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java HttpURLConnection class', 'How do you correctly obtain an instance of ''HttpURLConnection'' in Java?', 'By using the ''new'' keyword: ''new HttpURLConnection("http://example.com")''.', 'By calling the static method ''HttpURLConnection.connect(URL)''.', 'By calling the ''openConnection()'' method on a ''URL'' object and casting the result to ''HttpURLConnection''.', 'By instantiating a ''Socket'' and wrapping it in an ''HttpURLConnection''.', 'C', 'The ''HttpURLConnection'' class is abstract and cannot be instantiated directly. You must create a ''java.net.URL'' object, call its ''openConnection()'' method, and cast the returned ''URLConnection'' object to ''HttpURLConnection''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java HttpURLConnection class', 'What is the default HTTP request method used by an ''HttpURLConnection'' if one is not explicitly set?', 'POST', 'GET', 'HEAD', 'OPTIONS', 'B', 'If you do not explicitly set the request method using ''setRequestMethod()'', the ''HttpURLConnection'' will default to using the HTTP GET method.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java HttpURLConnection class', 'If you need to send a request body (such as form data or a JSON payload) in an HTTP POST request using ''HttpURLConnection'', what mandatory step must you take before writing to the output stream?', 'Call ''connection.setDoInput(true)''.', 'Call ''connection.setDoOutput(true)''.', 'Set the request property "Content-Length" manually.', 'Call ''connection.connect()'' before writing.', 'B', 'By default, ''HttpURLConnection'' is configured only for input (reading a response). To send a request body, you must explicitly enable output by calling ''setDoOutput(true)'' before acquiring the output stream.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java HttpURLConnection class', 'Which method is used to retrieve the HTTP status code (e.g., 200, 404, 500) returned by the server?', 'connection.getStatus()', 'connection.getHTTPCode()', 'connection.getResponseCode()', 'connection.getResponseMessage()', 'C', 'The ''getResponseCode()'' method returns the integer status code from the HTTP response message (e.g., 200 for OK, 404 for Not Found).');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java HttpURLConnection class', 'How does ''HttpURLConnection'' handle HTTP redirects (status codes 3xx) by default?', 'It automatically follows all redirects globally across all protocols.', 'It throws an ''HttpRetryException''.', 'It automatically follows redirects, but only if the protocol remains the same (e.g., HTTP to HTTP, not HTTP to HTTPS).', 'It never follows redirects automatically.', 'C', 'By default, Java''s ''HttpURLConnection'' automatically follows redirects, but for security reasons, it does not automatically follow redirects that change the protocol, such as redirecting from an HTTP URL to an HTTPS URL.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java HttpURLConnection class', 'When should you call the ''disconnect()'' method on an ''HttpURLConnection'' instance?', 'Immediately after setting the request method.', 'After you have finished reading the response, to indicate that the network resources can be freed or the connection can be returned to a pool.', 'Before attempting to read the error stream.', 'You should never call it; the garbage collector handles it automatically.', 'B', 'Calling ''disconnect()'' releases the connection resources. While the underlying TCP connection might be kept alive and pooled by the JVM for future requests to the same server, this indicates to the JVM that your specific use of this connection is complete.');
