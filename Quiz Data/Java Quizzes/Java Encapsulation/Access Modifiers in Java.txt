INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Access Modifiers in Java', 'If a class member (field or method) is declared without any explicit access modifier, what is its visibility level?', 'It is visible only within the same class.', 'It is visible to any class globally.', 'It is visible to all classes within the same package, but not to classes in other packages.', 'It is visible to all classes in the same package and to subclasses in different packages.', 'C', 'When no access modifier is specified, Java applies ''default'' (or package-private) visibility. This restricts access strictly to classes residing in the exact same package.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Access Modifiers in Java', 'Which access modifier strictly restricts visibility of a member to the enclosing class only?', 'protected', 'private', 'default', 'public', 'B', 'The ''private'' access modifier is the most restrictive. A private member can only be accessed by code executing within the body of the class that declares it.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Access Modifiers in Java', 'How does the ''protected'' access modifier differ from package-private (default) access?', '''protected'' members are visible globally, whereas default members are not.', '''protected'' allows access to subclasses located in different packages, whereas default access strictly confines visibility to the same package.', '''protected'' is used for classes, while default is used for interfaces.', 'There is no functional difference; they are aliases.', 'B', 'The ''protected'' modifier extends package-private visibility by also allowing child classes (subclasses) residing in entirely different packages to access the inherited member.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Access Modifiers in Java', 'Which of the following access modifiers are legally permitted for a top-level class declaration?', 'public and default (no modifier)', 'public, protected, and private', 'public and private', 'Only public', 'A', 'Top-level classes can only be declared as ''public'' (accessible anywhere) or default/package-private (accessible only within their package). ''private'' and ''protected'' are only valid for nested/inner classes or class members.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Access Modifiers in Java', 'When overriding a method in a subclass, what rule applies to the access modifier of the overriding method?', 'The access modifier must remain exactly the same as the parent method.', 'The overriding method can have any access modifier, regardless of the parent.', 'The access level cannot be more restrictive than the overridden method''s access level.', 'The access level must be strictly ''public''.', 'C', 'Java enforces that an overriding method cannot reduce the visibility of the overridden method. For example, if a superclass method is ''protected'', the subclass overriding it can be ''protected'' or ''public'', but not ''private'' or default.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Access Modifiers in Java', 'Can an interface in Java 8+ have ''private'' methods?', 'No, all methods in an interface must be implicitly public.', 'Yes, but only if they are declared abstract.', 'Yes, Java 9 introduced private methods in interfaces to share common code between default or static methods.', 'Yes, but they can only be called by classes that implement the interface.', 'C', 'Starting in Java 9, interfaces can declare ''private'' and ''private static'' methods. These are used to encapsulate helper logic shared among the interface''s ''default'' or ''static'' methods without exposing it to implementing classes.');
