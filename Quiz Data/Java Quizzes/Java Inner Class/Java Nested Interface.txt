INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Nested Interface', 'What are the implicit modifiers for an interface that is declared directly inside a class?', 'public and abstract', 'static only', 'public, static, and abstract', 'static and abstract', 'D', 'Any interface declared inside a class or another interface is implicitly ''static''. It is also implicitly ''abstract'' (like all interfaces). It is not implicitly ''public'' when declared inside a class; its access depends on the explicit modifier provided (public, private, protected, or default).');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Nested Interface', 'What are the implicit access modifiers for an interface that is nested directly inside another interface?', 'private static', 'protected static', 'public static', 'default static', 'C', 'Interfaces nested inside other interfaces are always implicitly ''public'' and ''static''. You cannot mark them as private, protected, or default.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Nested Interface', 'Which of the following is a classic example of a nested interface in the standard Java API?', 'java.util.List.Node', 'java.util.Map.Entry', 'java.lang.Thread.Runnable', 'java.io.File.Path', 'B', '''Map.Entry'' is an interface nested inside the ''java.util.Map'' interface. It is used to encapsulate a key-value pair, logically grouping it within the context of a Map where it is exclusively used.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Nested Interface', 'If a class implements an outer interface that contains a nested interface, is the class forced to implement the methods of the nested interface as well?', 'Yes, implementing the outer interface implicitly requires implementing all nested interfaces.', 'No, the nested interface is treated as a completely separate entity. Implementing the outer interface does not require implementing the nested one.', 'Yes, unless the nested interface is marked as ''default''.', 'No, but the class must be declared abstract.', 'B', 'An outer interface and its nested interface are entirely distinct contracts. A class can choose to implement the outer interface, the nested interface (e.g., ''implements Outer.Nested''), or both, independently of each other.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Nested Interface', 'Can a nested interface be declared as ''private''?', 'No, interfaces must always be accessible to be implemented.', 'Yes, but only if it is nested inside a class, restricting its implementation strictly to that enclosing class.', 'Yes, but only if it is nested inside another interface.', 'No, the compiler will throw an illegal modifier error.', 'B', 'While top-level interfaces must be public or default, an interface nested inside a class can be declared ''private''. This means it can only be accessed and implemented by the enclosing class or other inner classes within that same enclosing class.');
