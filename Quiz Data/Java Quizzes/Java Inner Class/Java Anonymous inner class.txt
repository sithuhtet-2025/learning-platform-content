INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Anonymous inner class', 'What is the syntactical defining feature of an Anonymous Inner Class in Java?', 'It is defined using the ''anonymous'' keyword.', 'It must be declared abstract.', 'It is declared and instantiated in a single expression without giving it a name.', 'It can only be used to implement the Runnable interface.', 'C', 'An anonymous inner class is an inner class without a name. It is defined and instantiated simultaneously in a single expression using the ''new'' keyword followed by an interface or superclass name and a class block.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Anonymous inner class', 'Which of the following limitations applies strictly to Anonymous Inner Classes?', 'They can implement multiple interfaces simultaneously.', 'They can explicitly define constructors.', 'They can either extend one class OR implement one interface, but not both simultaneously.', 'They cannot override any methods from their supertype.', 'C', 'Because the syntax for an anonymous class combines declaration and instantiation (''new Type() { ... }''), it is impossible to specify both a class to extend and an interface to implement, nor can it implement multiple interfaces.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Anonymous inner class', 'Since an Anonymous Inner Class has no name, how can you initialize its instance variables if you cannot write a constructor?', 'By using an instance initializer block ( { ... } ) inside the class body.', 'By using a static block.', 'Variables in an anonymous class cannot be initialized; they must remain default.', 'By passing parameters directly to the interface''s implicit constructor.', 'A', 'While anonymous classes cannot have explicitly named constructors, you can use an instance initializer block (a set of curly braces inside the body) to execute setup logic when the anonymous object is instantiated.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Anonymous inner class', 'What type of variables from its enclosing scope (like a method) can an Anonymous Inner Class safely access?', 'Any local variable declared in the enclosing scope.', 'Only static variables from the enclosing class.', 'Only local variables that are explicitly marked ''final'' or are "effectively final".', 'No local variables can be accessed.', 'C', 'An anonymous inner class (like local inner classes) can only access local variables of the enclosing block if they are ''final'' or "effectively final" (meaning their value is never changed after initialization). This ensures consistency since the anonymous object might outlive the method execution.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Anonymous inner class', 'In modern Java, what newer feature is frequently used as a more concise replacement for Anonymous Inner Classes when implementing Single Abstract Method (SAM) interfaces?', 'Records', 'Sealed Classes', 'Lambda Expressions', 'Modules', 'C', 'Lambda expressions, introduced in Java 8, provide a much more concise and readable syntax for instantiating functional interfaces (interfaces with exactly one abstract method), largely replacing boilerplate-heavy anonymous inner classes for this specific use case.');
