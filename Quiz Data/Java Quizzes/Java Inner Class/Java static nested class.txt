INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java static nested class', 'What is the most significant difference between a Static Nested Class and a Member Inner Class?', 'A static nested class cannot contain non-static methods.', 'A static nested class does not hold an implicit reference to an instance of the outer class.', 'A static nested class cannot be instantiated.', 'A static nested class must extend the outer class.', 'B', 'Because it is marked ''static'', a static nested class interacts with the outer class like any other static member. It does not require an instance of the outer class to exist and therefore lacks the implicit ''Outer.this'' reference.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java static nested class', 'Assume class ''Outer'' has a public static nested class ''Nested''. How do you instantiate ''Nested'' from an external class?', 'Outer.Nested obj = new Outer().new Nested();', 'Nested obj = new Outer.Nested();', 'Outer.Nested obj = new Outer.Nested();', 'Outer.Nested obj = Outer.createNested();', 'C', 'Because the nested class is static, you do not need an instance of the outer class. You instantiate it directly using the outer class name as a namespace qualifier: ''new Outer.Nested();''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java static nested class', 'Which members of its enclosing Outer class can a Static Nested Class access directly?', 'All members (static and non-static), including private ones.', 'Only public and protected members.', 'Only the static members of the outer class, including private static members.', 'It cannot access any members of the outer class.', 'C', 'A static nested class behaves like a static method in this regard. Because it has no reference to an outer instance, it cannot access non-static instance variables or methods of the outer class directly. It can, however, access all static members, even private ones.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java static nested class', 'Can a Static Nested Class declare its own instance (non-static) variables and methods?', 'No, all members of a static nested class must be static.', 'Yes, it can declare both static and non-static members.', 'Yes, but only if they are final.', 'No, it can only inherit them from the outer class.', 'B', 'The ''static'' keyword on a nested class simply means it does not need an outer class instance. The nested class itself functions like a normal top-level class, meaning it can freely declare its own instance variables, instance methods, and static members.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java static nested class', 'Why might a software architect choose to use a Static Nested Class instead of a top-level class?', 'To enforce singleton design patterns automatically.', 'To tightly group a helper class with its outer class while explicitly avoiding the memory overhead of a hidden outer instance reference.', 'To allow the nested class to override static methods of the outer class.', 'To bypass package-private access restrictions.', 'B', 'If a nested class does not need to access the instance variables of its outer class, making it ''static'' is a best practice. It logically groups the classes (namespace management) while saving memory and avoiding the overhead of maintaining an implicit reference to the outer object.');
