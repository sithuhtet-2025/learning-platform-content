INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Member Inner class', 'Which of the following best defines a Member Inner Class in Java?', 'A class defined inside a method block.', 'A static class defined at the same level as instance variables.', 'A non-static class declared directly inside another class, outside of any method.', 'An anonymous class that implements an interface.', 'C', 'A member inner class is a non-static nested class defined at the member level of a class (outside of any methods), behaving similarly to an instance variable or instance method.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Member Inner class', 'Assume class ''Outer'' contains a public member inner class ''Inner''. What is the correct syntax to instantiate ''Inner'' from a completely unrelated class?', 'Outer.Inner obj = new Outer.Inner();', 'Outer.Inner obj = new Outer().new Inner();', 'Inner obj = new Outer().Inner();', 'Outer.Inner obj = Outer.new Inner();', 'B', 'Because a member inner class is intimately tied to an instance of the outer class, you must first create an instance of the outer class, and then use that instance to invoke the ''new'' operator for the inner class.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Member Inner class', 'What level of access does a Member Inner Class have to the members of its enclosing Outer class?', 'It can only access public and protected members.', 'It can only access static members.', 'It has full access to all members of the outer class, including private fields and methods.', 'It cannot access any members of the outer class unless they are passed via a constructor.', 'C', 'A member inner class implicitly holds a reference to the outer class instance that created it, granting it direct and unrestricted access to all members (even private ones) of the enclosing class.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Member Inner class', 'If both the Outer class and the Member Inner Class define an instance variable named ''count'', how does the inner class explicitly access the Outer class''s ''count'' variable?', 'super.count', 'Outer.this.count', 'this.count', 'Outer.count', 'B', 'To resolve variable shadowing, Java requires the syntax ''OuterClassName.this.variableName'' from within the inner class to explicitly refer to the instance variable of the enclosing outer class.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Member Inner class', 'Prior to Java 16, what was a strict limitation regarding ''static'' declarations inside a non-static Member Inner Class?', 'It could not contain any static members unless they were final constants (static final).', 'It could contain static methods but not static variables.', 'It could contain static variables but not static methods.', 'There were no limitations; it could contain any static members.', 'A', 'Historically (prior to Java 16), a non-static inner class could not declare static initializers, static methods, or static variables unless those variables were compile-time constants marked ''static final''.');
