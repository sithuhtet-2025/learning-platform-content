INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Inner Classes (Nested Classes) in Java', 'What is the primary defining characteristic of a non-static inner class in Java compared to a static nested class?', 'It can only be instantiated inside the outer class''s constructor.', 'It maintains an implicit reference to the specific instance of the outer class that created it.', 'It cannot access the private members of its enclosing class.', 'It can define its own static methods and fields regardless of the Java version.', 'B', 'A non-static nested class (inner class) is intimately associated with an instance of its enclosing class. It implicitly holds a reference to that outer object, allowing it to directly access the outer object''s instance variables and methods.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Inner Classes (Nested Classes) in Java', 'Assume you have an outer class named ''Car'' and a public non-static inner class named ''Engine''. How do you instantiate the ''Engine'' class from outside the ''Car'' class?', 'Car.Engine engine = new Car.Engine();', 'Car.Engine engine = new Engine();', 'Car myCar = new Car(); Car.Engine engine = myCar.new Engine();', 'Car.Engine engine = myCar.createEngine();', 'C', 'Because a non-static inner class requires an instance of the outer class, you must first create the outer object, and then use that object''s reference to invoke the ''new'' operator for the inner class.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Inner Classes (Nested Classes) in Java', 'If an inner class defines an instance variable ''x'', and its outer class also defines an instance variable ''x'', how does the inner class refer specifically to the outer class''s ''x''?', 'super.x', 'OuterClassName.this.x', 'this.x', 'OuterClassName.x', 'B', 'To resolve shadowing where an inner class variable has the same name as an outer class variable, Java provides the syntax ''OuterClassName.this.variableName'' to explicitly reference the outer instance''s state.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Inner Classes (Nested Classes) in Java', 'Which access modifiers can be applied to a member inner class (a class declared directly inside another class)?', 'Only public and default.', 'Only private and protected.', 'Only default.', 'public, protected, default, and private.', 'D', 'Unlike top-level classes which can only be public or default, member inner classes are considered members of the outer class. Therefore, they can utilize all four access modifiers (public, protected, default, private).');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Inner Classes (Nested Classes) in Java', 'Why might a developer choose to use an inner class instead of a standard top-level class?', 'To bypass Java''s garbage collection mechanism.', 'To logically group classes that are only used in one place and to increase encapsulation by hiding the inner class from the outside world.', 'To allow the class to be executed without a main method.', 'To enable multiple inheritance of state.', 'B', 'Inner classes are primarily used for logical grouping of closely related concepts, improving code readability, and enhancing encapsulation by hiding a helper class deep within the class that exclusively uses it.');
