INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Variables', 'Which of the following describes the fundamental difference between an instance variable and a static variable in Java?', 'Instance variables are immutable, while static variables can be modified.', 'Static variables belong to the class itself and are shared across all instances, whereas each object has its own separate copy of an instance variable.', 'Static variables can only store primitive types, while instance variables can store objects.', 'Instance variables are stored on the stack, while static variables are stored on the heap.', 'B', 'The ''static'' keyword binds a variable to the class rather than any specific object instance. All instantiated objects of that class share a single copy of a static variable in memory.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Variables', 'What happens if you attempt to use a local variable in Java before explicitly initializing it?', 'The JVM assigns it a default value of null or 0.', 'The compiler automatically initializes it based on its data type.', 'The compiler throws a compilation error.', 'A NullPointerException is thrown at runtime.', 'C', 'Unlike instance variables which receive default values, Java mandates that local variables (variables declared inside methods or blocks) must be explicitly initialized before they are used; otherwise, the code will fail to compile.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Variables', 'Which of the following is a completely valid variable identifier name in Java?', '2ndValue', 'user-name', '_temporaryValue', 'class', 'C', 'In Java, variable names can begin with a letter, a dollar sign ($), or an underscore (_). They cannot begin with a number, cannot contain hyphens, and cannot be reserved keywords like ''class''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Variables', 'What does the ''final'' keyword signify when applied to a variable declaration?', 'The variable can be accessed globally from any package.', 'The variable''s reference or primitive value cannot be changed once initialized.', 'The variable will automatically be synchronized across multiple threads.', 'The variable is eligible for immediate garbage collection.', 'B', 'A ''final'' variable acts as a constant. Once it has been assigned a value, any attempt to reassign it will result in a compilation error.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Variables', 'Where are local variables physically stored in the JVM memory architecture during execution?', 'In the Metaspace (formerly PermGen).', 'On the Heap.', 'In the constant pool.', 'On the Thread Stack.', 'D', 'Local variables (including method parameters) are stored in the stack frame created on the Thread Stack for the specific method invocation. They are destroyed as soon as the method execution completes.');
