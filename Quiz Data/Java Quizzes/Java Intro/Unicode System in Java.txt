INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Unicode System in Java', 'Which character encoding standard does Java use internally to represent characters and strings?', 'ASCII', 'UTF-8', 'UTF-16', 'ISO-8859-1', 'C', 'The Java language was designed from the ground up to support internationalization and uses the 16-bit UTF-16 encoding scheme natively for its ''char'' data type and String representation.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Unicode System in Java', 'What is the standard syntax for writing a Unicode escape sequence in a Java source file?', '\xNNNN', '\uNNNN', '\U-NNNN', '&#xNNNN;', 'B', 'Java uses the ''\u'' prefix followed by exactly four hexadecimal digits (e.g., ''\u0041'' for ''A'') to represent a Unicode character literal escape sequence anywhere in the source code.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Unicode System in Java', 'Because Java uses a 16-bit ''char'' type, what is the valid numerical range of a ''char'' variable?', '-32768 to 32767', '0 to 255', '0 to 65535', '0 to 4294967295', 'C', 'The ''char'' data type is a single 16-bit unsigned Unicode character. Its minimum value is ''\u0000'' (or 0) and its maximum value is ''\uffff'' (or 65,535 inclusive).');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Unicode System in Java', 'Is it syntactically legal to use Unicode escape sequences to name variables in a Java program?', 'No, identifiers must strictly use standard ASCII characters.', 'Yes, Unicode escape sequences are processed before the compiler parses the code, so they can be used for variable names.', 'Yes, but only if the variable is declared as a String.', 'No, it will cause a lexical parsing error.', 'B', 'Java processes Unicode escape sequences extremely early in the compilation process. A variable name like ''int \u0061 = 5;'' is perfectly valid and is evaluated identically to ''int a = 5;''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Unicode System in Java', 'How does Java handle Unicode characters that fall outside the Basic Multilingual Plane (BMP) and require more than 16 bits to represent (e.g., certain emojis or historical scripts)?', 'It truncates the character to fit into 16 bits, causing data loss.', 'It dynamically changes the ''char'' size to 32 bits.', 'It throws an UnsupportedEncodingException.', 'It represents them using a "surrogate pair"—two sequential 16-bit ''char'' values.', 'D', 'Because a single 16-bit ''char'' can only represent up to U+FFFF, Java represents supplementary characters (above U+FFFF) using a surrogate pair, which consists of a high surrogate ''char'' and a low surrogate ''char'' encoded together in UTF-16.');
