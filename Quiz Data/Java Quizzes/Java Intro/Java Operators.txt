INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Operators', 'What is the critical difference between the logical AND (&&) operator and the bitwise AND (&) operator when used with boolean expressions?', '&& evaluates both operands regardless of the first, while & short-circuits.', '&& short-circuits (skips evaluating the right operand if the left is false), while & always evaluates both operands.', '&& works only on integers, while & works only on booleans.', 'There is no difference when used with boolean variables.', 'B', 'The ''&&'' operator is a short-circuit operator; if the left-hand side is false, the entire expression is false, and the right side is not evaluated. The ''&'' operator always evaluates both sides before determining the result.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Operators', 'Given the code snippet: ''int x = 5; int y = x++;''. What are the values of x and y after execution?', 'x = 5, y = 5', 'x = 6, y = 6', 'x = 6, y = 5', 'x = 5, y = 6', 'C', 'The postfix increment operator (x++) returns the original value of x (5) to be assigned to y, and then increments x by 1 (becoming 6). Therefore, y is 5 and x is 6.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Operators', 'What is the function of the unsigned right shift operator (>>>) in Java?', 'It shifts bits to the right and fills the leftmost bits with the sign bit (preserving positive/negative).', 'It shifts bits to the right and always fills the leftmost bits with zeros, regardless of the original sign.', 'It performs a logical right shift specifically on floating-point numbers.', 'It rotates bits, moving the rightmost bits to the leftmost positions.', 'B', 'Unlike the signed right shift (>>), which preserves the sign by filling the MSB with the original sign bit, the unsigned right shift (>>>) blindly fills the leftmost shifted positions with zeros, effectively treating the value as an unsigned integer.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Operators', 'What is the output of the following string concatenation expression: ''System.out.println(2 + 2 + "5");''?', '9', '225', '45', 'Compilation error', 'C', 'The ''+'' operator evaluates from left to right. First, the integer addition ''2 + 2'' is performed, resulting in 4. Then, the integer 4 is concatenated with the String "5", resulting in the String "45".');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Operators', 'What is the output of the following string concatenation expression: ''System.out.println("5" + 2 + 2);''?', '9', '54', '522', 'Compilation error', 'C', 'Evaluation is left to right. Because the first operand is a String ("5"), the subsequent additions are treated as string concatenations. "5" + 2 becomes "52", and "52" + 2 becomes "522".');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Operators', 'How does the ''instanceof'' operator behave if the object reference being tested is ''null''?', 'It throws a NullPointerException.', 'It evaluates to true if the target class is Object.', 'It evaluates to true.', 'It evaluates to false safely without throwing an exception.', 'D', 'The ''instanceof'' operator checks if an object is an instance of a specific class or interface. If the reference is null, it gracefully returns false because null is not an instance of anything.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Operators', 'What is the result of the modulo operation: ''-5 % 2'' in Java?', '1', '-1', '2.5', '0', 'B', 'In Java, the modulo operator (%) returns a remainder that takes the sign of the dividend (the left operand). Therefore, -5 divided by 2 leaves a remainder of -1.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Operators', 'Which of the following operators has the highest precedence in Java?', 'Multiplication (*)', 'Assignment (=)', 'Logical AND (&&)', 'Postfix Increment (x++)', 'D', 'Postfix operators (like x++ and x--) have the highest precedence in Java, evaluating before multiplication/division, addition/subtraction, logical operators, and assignment operators.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Operators', 'Given ''int a, b, c; a = b = c = 5;'', how is this multiple assignment evaluated?', 'It throws a compilation error; multiple assignments are not allowed on one line.', 'From left to right: a becomes b (uninitialized), b becomes c, c becomes 5.', 'From right to left: c is assigned 5, the assignment expression returns 5 which is assigned to b, and so on.', 'Simultaneously in parallel.', 'C', 'The assignment operator (=) is right-associative. It evaluates from right to left, and the assignment operation itself returns the assigned value. Thus, c=5 evaluates to 5, which is then assigned to b, and finally to a.');
