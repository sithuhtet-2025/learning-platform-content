INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Reverse an Array in Java', 'What is the optimal space complexity for reversing an array natively in Java using the two-pointer approach (swapping elements from the ends towards the center)?', 'O(N)', 'O(log N)', 'O(1)', 'O(N^2)', 'C', 'The two-pointer approach swaps elements in place using only a single temporary variable to hold the swapped value. Because it does not require allocating a new array, its auxiliary space complexity is O(1).');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Reverse an Array in Java', 'When implementing an in-place array reversal using a standard ''for'' loop and swapping the ''i-th'' element with the ''(length - 1 - i)-th'' element, what is the critical termination condition for the loop?', 'i < array.length', 'i <= array.length', 'i < array.length / 2', 'i != array.length', 'C', 'The loop must terminate at ''array.length / 2''. If it iterates through the entire array (''i < array.length''), it will swap the elements twice, effectively restoring the array to its original, unreversed state.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Reverse an Array in Java', 'Why can''t you directly use ''Collections.reverse()'' to reverse an ''int[]'' primitive array in Java?', 'The method only works on multidimensional arrays.', '''Collections.reverse()'' requires a List containing objects, not an array of primitives.', 'The method name is ''Arrays.reverse()'', not ''Collections.reverse()''.', 'It requires the array to be sorted first.', 'B', 'The ''Collections.reverse()'' utility method requires a ''java.util.List''. While you can wrap an object array using ''Arrays.asList()'', you cannot do this directly with primitive arrays like ''int[]''; it would require boxing the primitives into an ''Integer[]'' first.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Reverse an Array in Java', 'If you choose to reverse an array by creating a new secondary array and copying the elements backwards, what is the time and space complexity?', 'Time: O(N), Space: O(1)', 'Time: O(N), Space: O(N)', 'Time: O(log N), Space: O(N)', 'Time: O(N^2), Space: O(N)', 'B', 'Iterating through the original array takes O(N) time. Allocating a new array of the same size to hold the reversed elements requires O(N) auxiliary space, making it less memory-efficient than an in-place swap.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Reverse an Array in Java', 'What is the correct logic to swap two elements in an integer array ''arr'' at indices ''start'' and ''end''?', 'arr[start] = arr[end]; arr[end] = arr[start];', 'int temp = arr[start]; arr[start] = arr[end]; arr[end] = temp;', 'arr.swap(start, end);', 'temp = arr[end]; arr[end] = arr[start];', 'B', 'To swap two elements, you must temporarily store one of the values before overwriting it. ''int temp = arr[start]'' saves the first value, which is then safely placed into ''arr[end]'' after the overwrite.');
