INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Array length Property', 'What is the correct syntax to find the size of an array named ''numbers'' in Java?', 'numbers.size()', 'numbers.length()', 'numbers.length', 'numbers.capacity', 'C', 'In Java, ''length'' is a final instance variable (property) of an array object, not a method. Therefore, it is accessed without parentheses, unlike the ''length()'' method used for String objects.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Array length Property', 'Can the ''length'' property of an array be modified after the array is created (e.g., ''myArray.length = 10;'')?', 'Yes, which automatically resizes the array in memory.', 'Yes, but it only changes the logical size limit; the physical memory remains the same.', 'No, the compiler will throw an error because the length property is implicitly ''final''.', 'No, it will throw a runtime exception.', 'C', 'Once a Java array is instantiated, its size is immutable. The ''length'' property is a ''final'' variable, and any attempt to reassign a value to it will result in a compile-time error ("cannot assign a value to final variable length").');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Array length Property', 'If you declare an array of objects like ''String[] words = new String[10];'' and only assign strings to the first 3 indices, what will ''words.length'' return?', '3', '10', '0', 'NullPointerException', 'B', 'The ''length'' property returns the total allocated capacity of the array, not the number of non-null elements it currently contains. Since the array was initialized with a size of 10, it will return 10 regardless of its contents.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Array length Property', 'For a multidimensional array initialized as ''double[][] matrix = new double[5][10];'', what is the value of ''matrix.length''?', '50', '10', '5', 'An array containing [5, 10]', 'C', 'A 2D array is an array of arrays. Calling ''length'' on the outer array variable (''matrix'') returns the number of element references it holds, which corresponds to the number of rows (5). To get the columns, you would use ''matrix[0].length''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Array length Property', 'Is it syntactically legal to create an array with a length of zero (e.g., ''int[] emptyArr = new int[0];'')?', 'No, it causes a compilation error.', 'No, it throws a NegativeArraySizeException.', 'Yes, it compiles, but attempting to access ''emptyArr.length'' throws a NullPointerException.', 'Yes, it creates a valid, non-null array object whose ''length'' property is exactly 0.', 'D', 'Zero-length arrays are perfectly legal in Java. They are often used as safe return values for methods that find no results, preventing the need to return ''null'' and avoiding NullPointerExceptions for the caller.');
