INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Array Programs in Java', 'Which mechanism is strictly used to dynamically determine the total number of elements a Java array can accommodate?', 'The size() method.', 'The length() method.', 'The length property.', 'The capacity property.', 'C', 'In Java, arrays are objects, and their size is exposed via a final instance variable (property) named ''length''. Unlike the String class which uses a ''length()'' method, array size is accessed without parentheses (e.g., ''arr.length'').');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Array Programs in Java', 'In a standard ''for'' loop iterating sequentially over an array named ''data'', what is the correct and safest loop condition to prevent an ArrayIndexOutOfBoundsException?', 'for(int i = 0; i <= data.length; i++)', 'for(int i = 1; i < data.length; i++)', 'for(int i = 0; i < data.length; i++)', 'for(int i = 0; i < data.length - 1; i++)', 'C', 'Java arrays are zero-indexed, meaning the first element is at index 0 and the last element is at index ''length - 1''. The condition ''i < data.length'' ensures the loop safely terminates exactly after accessing the final valid index.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Array Programs in Java', 'What is the most efficient, native way to copy a large contiguous block of elements from one array to another in Java?', 'Using a standard ''for'' loop to copy elements one by one.', 'Using the ''java.util.Arrays.copy()'' method.', 'Using the native ''System.arraycopy()'' method.', 'Assigning the original array reference to a new array variable.', 'C', '''System.arraycopy()'' is a native (JNI) method provided by the JVM. It is highly optimized for performance and is the standard, fastest way to perform bulk array copying operations in Java.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Array Programs in Java', 'When instantiating a multidimensional array in Java, such as ''int[][] matrix = new int[3][];'', what does this architectural syntax signify?', 'A compilation error, as both dimensions must be specified.', 'A strictly rectangular grid of 3 rows and an unknown number of columns.', 'An array of arrays, where the outer array has 3 elements, but the inner arrays are currently uninitialized (null) and can eventually have differing lengths (a jagged array).', 'A 3D array missing its third dimension.', 'C', 'Java does not have true multi-dimensional arrays; it has arrays of arrays. You can specify just the first dimension to create an array of references. You can later instantiate each inner array with a different size, creating a "jagged" or "ragged" array.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Array Programs in Java', 'If you call the ''clone()'' method on a one-dimensional array of primitives (e.g., ''int[] copy = original.clone();''), what type of copy is produced?', 'A shallow copy: modifying the copy also modifies the original.', 'A deep copy: a completely new array object is created, and the primitive values are copied into it. Modifying the copy does not affect the original.', 'A compile-time error, as arrays do not implement the Cloneable interface.', 'A soft copy, which is eligible for immediate garbage collection.', 'B', 'For one-dimensional arrays of primitives, ''clone()'' creates a deep copy. A new array is allocated, and the primitive values are duplicated. (Note: For arrays of objects, ''clone()'' produces a shallow copy, copying only the references, not the underlying objects themselves).');
