INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Jagged Array in Java', 'Which of the following statements correctly declares and allocates the outer structure of a 2D jagged array of integers with 3 rows?', 'int[][] arr = new int[3][3];', 'int[][] arr = new int[][3];', 'int[][] arr = new int[3][];', 'int[][] arr = new int[];', 'C', 'To create a jagged array, you must instantiate the primary (outer) array by specifying the number of rows, but leave the column dimension empty (e.g., ''new int[3][]''). The inner arrays can then be instantiated individually with different lengths.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Jagged Array in Java', 'After executing ''int[][] myArr = new int[4][];'', what is the current value of ''myArr[0]''?', '0', 'An empty array of length 0.', 'null', 'It throws an ArrayIndexOutOfBoundsException.', 'C', 'Because a 2D array in Java is simply an array of object references (pointing to 1D arrays), initializing only the outer array leaves all its element references pointing to ''null'' until the inner arrays are explicitly instantiated.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Jagged Array in Java', 'Which of the following is the correct syntax to initialize the second row of an existing jagged array ''arr'' to hold exactly 5 elements?', 'arr[1] = new int[5];', 'arr[2] = new int[5];', 'arr[1][] = new int[5];', 'arr.setRow(1, 5);', 'A', 'Java arrays are zero-indexed. The second row is accessed at index 1. You instantiate the inner array using the ''new'' keyword and assign its reference directly to that row index: ''arr[1] = new int[5];''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Jagged Array in Java', 'When iterating through a jagged array using nested ''for'' loops, how should the termination condition of the inner loop be written to avoid an ArrayIndexOutOfBoundsException? Assume ''i'' is the outer loop index and ''j'' is the inner loop index.', 'j < arr.length', 'j < arr[i].length', 'j <= arr[i].length', 'j < arr.maxLength', 'B', 'Because the rows in a jagged array can have completely different lengths, the inner loop must dynamically check the length of the specific row it is currently iterating over using ''arr[i].length''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Jagged Array in Java', 'Which of the following is a valid inline initialization of a jagged array?', 'int[][] arr = { {1, 2}, {3, 4, 5}, {6} };', 'int[][] arr = new int[3][] { {1, 2}, {3, 4, 5}, {6} };', 'int arr[][] = { (1, 2), (3, 4, 5), (6) };', 'int[][] arr = [ [1, 2], [3, 4, 5], [6] ];', 'A', 'Java permits inline instantiation of multi-dimensional arrays using nested curly braces. The compiler automatically determines the length of the outer array and the varying lengths of the inner arrays based on the provided elements.');
