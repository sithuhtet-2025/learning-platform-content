INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Arrays Class in Java', 'What is the absolute prerequisite for correctly utilizing the ''java.util.Arrays.binarySearch(array, key)'' method?', 'The array must be completely free of null values.', 'The array must be sorted in ascending order prior to the method call.', 'The array must contain only primitive data types.', 'The array must be converted to a List first.', 'B', 'The binary search algorithm relies on the array being sorted to repeatedly divide the search interval in half. If the array is unsorted, the results of ''Arrays.binarySearch()'' are strictly undefined and unpredictable.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Arrays Class in Java', 'Which method in the ''java.util.Arrays'' class is used to perform a deep comparison of two multidimensional arrays, checking if all nested elements are equal?', 'Arrays.equals()', 'Arrays.deepEquals()', 'Arrays.compare()', 'Arrays.match()', 'B', 'While ''Arrays.equals()'' works for single-dimensional arrays, it only compares the nested references for multi-dimensional arrays. ''Arrays.deepEquals()'' recursively traverses the nested arrays to compare the actual underlying data for equality.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Arrays Class in Java', 'What behavior occurs if you invoke ''Arrays.asList(1, 2, 3).add(4);''?', 'The array is dynamically resized, and the number 4 is appended.', 'The compiler throws a TypeMismatchException.', 'The JVM throws an ''UnsupportedOperationException'' at runtime.', 'The number 4 replaces the number 3 at the end of the list.', 'C', 'The ''Arrays.asList()'' method returns a fixed-size list backed directly by the original array. Because arrays cannot be resized, structural modifications like ''add()'' or ''remove()'' are not supported and will throw an ''UnsupportedOperationException''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Arrays Class in Java', 'What is the most concise way to print the contents of a single-dimensional array to the console in a human-readable format?', 'System.out.println(array.toString());', 'System.out.println(Arrays.format(array));', 'System.out.println(Arrays.toString(array));', 'System.out.println(array);', 'C', 'Directly printing an array (or calling its ''toString()'' method) prints its memory hash code signature (e.g., [I@1b6d3586). ''Arrays.toString(array)'' iterates through the elements and formats them into a readable string like "[1, 2, 3]".');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Arrays Class in Java', 'Which underlying sorting algorithm is predominantly utilized by ''Arrays.sort()'' for sorting arrays of Objects in modern Java?', 'QuickSort', 'Bubble Sort', 'TimSort', 'HeapSort', 'C', 'For arrays of reference types (Objects), Java utilizes TimSort, which is a highly optimized, stable hybrid sorting algorithm derived from Merge Sort and Insertion Sort, offering O(n log n) performance even on real-world partially sorted data.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Arrays Class in Java', 'What does the ''Arrays.fill(int[] a, int val)'' method do?', 'It calculates the sum of all elements and assigns it to a variable.', 'It dynamically increases the capacity of the array to hold the specified value.', 'It assigns the specified data value to every element within the array, completely overwriting any existing data.', 'It fills an array with random numbers up to the specified maximum value.', 'C', 'The ''fill()'' method is a utility that rapidly assigns a single specified value to every index of the provided array, effectively initializing or resetting its entire contents.');
