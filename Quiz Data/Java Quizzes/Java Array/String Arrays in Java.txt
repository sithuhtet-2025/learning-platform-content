INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('String Arrays in Java', 'What is the default value of each element in a newly initialized String array, such as ''String[] words = new String[5];''?', 'An empty string ("")', 'null', 'Garbage text', 'A compilation error is thrown.', 'B', 'Because ''String'' is a reference type (an object), instantiating a new String array simply creates an array of memory references. By default, the JVM initializes all object references to ''null''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('String Arrays in Java', 'Given ''String[] arr = {"Java", "Python", "C++"};'', what happens in memory if you execute ''arr[0] = "Ruby";''?', 'The original string "Java" is mutated into "Ruby" in the String Pool.', 'A new String object "Ruby" is created (or retrieved from the pool), and the reference at index 0 is updated to point to it. "Java" remains unchanged in memory.', 'The array is destroyed and recreated.', 'A compilation error occurs because Strings are immutable.', 'B', 'Strings are immutable, meaning the string object "Java" cannot be changed. However, the array simply holds references. Reassigning ''arr[0]'' merely changes the pointer at index 0 to point to a different String object ("Ruby").');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('String Arrays in Java', 'If you attempt to call ''arr[0].length()'' on a String array immediately after declaring ''String[] arr = new String[3];'', what occurs?', 'It returns 0.', 'It returns 3.', 'A NullPointerException is thrown.', 'A compile-time error occurs.', 'C', 'The array elements are initialized to ''null''. Attempting to invoke the instance method ''.length()'' on a null reference immediately triggers a NullPointerException at runtime.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('String Arrays in Java', 'Which utility method provides the most direct way to concatenate all elements of a String array into a single String, separated by a comma?', 'String.concat(arr, ",");', 'Arrays.toString(arr);', 'String.join(",", arr);', 'arr.implode(",");', 'C', 'Introduced in Java 8, the static method ''String.join(CharSequence delimiter, CharSequence... elements)'' efficiently concatenates the elements of an array or iterable, inserting the specified delimiter between each element.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('String Arrays in Java', 'When using ''Arrays.sort(strArray)'' to sort an array of Strings, what is the default sorting order?', 'By string length, shortest to longest.', 'Strictly alphabetical, ignoring capitalization.', 'Lexicographical order based on Unicode values, where uppercase letters sort before lowercase letters.', 'Reverse alphabetical order.', 'C', 'The default ''compareTo()'' method of the String class sorts lexicographically using Unicode integer values. Because uppercase letters (e.g., ''A''=65) have lower Unicode values than lowercase letters (e.g., ''a''=97), all uppercase words will appear before lowercase words.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('String Arrays in Java', 'How do you force ''Arrays.sort()'' to sort a String array alphabetically while ignoring case sensitivity?', 'Arrays.sort(arr, String.CASE_INSENSITIVE_ORDER);', 'Arrays.sortIgnoreCase(arr);', 'Arrays.sort(arr).ignoreCase();', 'By manually converting all array elements to lowercase before sorting.', 'A', 'The ''java.util.Arrays'' class provides overloaded sort methods that accept a custom Comparator. ''String.CASE_INSENSITIVE_ORDER'' is a built-in Comparator specifically designed to sort strings without regard to capitalization.');
