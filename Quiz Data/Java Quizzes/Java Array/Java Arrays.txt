INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Arrays', 'What is the direct superclass of an array type in Java?', 'java.util.Collection', 'java.util.Arrays', 'java.lang.Object', 'Arrays do not have a superclass; they are primitive constructs.', 'C', 'In Java, arrays are dynamically created, first-class objects. Regardless of whether they hold primitives or reference types, every array type directly extends ''java.lang.Object'', inheriting methods like ''clone()'', ''equals()'', and ''toString()''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Arrays', 'What happens if you attempt to instantiate an array with a negative size, such as ''int[] arr = new int[-5];''?', 'The compiler throws a syntax error.', 'The JVM throws a ''NegativeArraySizeException'' at runtime.', 'The JVM creates an array of size 0.', 'The array is created but iterates in reverse.', 'B', 'While the compiler will allow a syntactically valid integer expression in the dimension declaration, the JVM will encounter the negative value during runtime memory allocation and immediately throw a ''NegativeArraySizeException''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Arrays', 'Consider the concept of Array Covariance. If ''Integer'' is a subclass of ''Number'', is it legal to assign an ''Integer[]'' to a ''Number[]'' reference?', 'Yes, Java arrays are covariant.', 'No, Java arrays are invariant to ensure strict type safety.', 'Yes, but only if explicit casting is used.', 'No, arrays of wrapper classes cannot be covariant.', 'A', 'Java arrays are covariant, meaning if subclass B extends superclass A, then an array of B (B[]) can be assigned to a reference variable of type A[]. For example, ''Number[] nums = new Integer[5];'' is completely legal.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Arrays', 'Following the concept of Array Covariance, what runtime exception occurs if you assign a ''Double'' into a ''Number[]'' array that was actually instantiated as an ''Integer[]''?', 'ClassCastException', 'TypeMismatchException', 'ArrayStoreException', 'IllegalArgumentException', 'C', 'While covariance allows ''Number[] nums = new Integer[5];'', the JVM remembers the actual instantiated type. If you attempt to execute ''nums[0] = 3.14;'', the JVM recognizes that a Double cannot be placed into an Integer array and throws an ''ArrayStoreException''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Arrays', 'Which of the following describes the correct syntax for creating an "anonymous array" to pass inline to a method without declaring a local variable?', 'methodCall({1, 2, 3});', 'methodCall(new int[] {1, 2, 3});', 'methodCall(new int[3] {1, 2, 3});', 'methodCall([1, 2, 3]);', 'B', 'An anonymous array is created using the ''new'' operator followed by the type and empty brackets, and then the inline initialization block: ''new int[] {1, 2, 3}''. Specifying the size inside the brackets when using inline initialization causes a compilation error.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Arrays', 'What default values are populated into an array of primitive ''boolean'' elements upon initialization (e.g., ''boolean[] flags = new boolean[10];'')?', 'null', 'true', 'false', '0', 'C', 'When an array is created, the JVM zeros out the allocated memory. For a ''boolean'' array, this means every element is automatically initialized to its default value, which is ''false''.');
