INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Multi-Dimensional Arrays in Java', 'How does the Java Virtual Machine physically structure a two-dimensional array (e.g., ''int[][] matrix'') in memory?', 'As a single, contiguous block of memory containing all elements sequentially.', 'As a matrix object with strict row and column memory boundaries.', 'As an array of reference variables, where each reference points to a separate one-dimensional array object on the heap.', 'As a linked list of arrays.', 'C', 'Java does not have true multi-dimensional arrays. Instead, it uses "arrays of arrays". A 2D array is implemented as a 1D array containing object references, each pointing to another distinct 1D array in heap memory.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Multi-Dimensional Arrays in Java', 'Which of the following instantiation statements for a multi-dimensional array will cause a compilation error?', 'int[][] arr = new int[3][4];', 'int[][] arr = new int[3][];', 'int[][] arr = new int[][4];', 'int[][][] arr = new int[3][4][5];', 'C', 'In Java, when instantiating a multi-dimensional array, you must specify the size of at least the first (leftmost) dimension. Specifying the second dimension while leaving the first empty is illegal and causes a compile-time error.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Multi-Dimensional Arrays in Java', 'Given ''int[][] matrix = new int[5][10];'', what does the expression ''matrix[0].length'' evaluate to?', '5', '10', '50', '0', 'B', 'The variable ''matrix'' refers to the outer array (which has 5 elements). ''matrix[0]'' refers to the first inner array. Calling ''.length'' on that inner array returns its allocated capacity, which is 10.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Multi-Dimensional Arrays in Java', 'What is the correct syntax for using an enhanced ''for'' loop (for-each) to iterate through all the primitive integers in an ''int[][]'' matrix?', 'for(int val : matrix) { System.out.print(val); }', 'for(int[] row : matrix) { for(int val : row) { System.out.print(val); } }', 'for(int val : matrix[][]) { System.out.print(val); }', 'for(int i, j : matrix) { System.out.print(matrix[i][j]); }', 'B', 'Because a 2D array is an array of arrays, the outer for-each loop must extract the inner array objects (''int[] row''). The inner for-each loop then extracts the primitive values (''int val'') from those row arrays.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Multi-Dimensional Arrays in Java', 'Can a 3D array in Java be jagged (irregular)?', 'No, only 2D arrays can be jagged.', 'Yes, but only the innermost array can have varying lengths.', 'Yes, every nested level of the array hierarchy can be independently instantiated with a different length.', 'No, arrays deeper than 2 dimensions must be strictly rectangular.', 'C', 'Because every dimension in a Java multi-dimensional array is simply an array of object references, every single nested array at any depth can be instantiated independently with varying lengths, creating a fully irregular/jagged 3D structure.');
