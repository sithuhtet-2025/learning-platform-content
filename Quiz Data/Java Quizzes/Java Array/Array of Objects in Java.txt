INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Array of Objects in Java', 'When the statement ''Employee[] staff = new Employee[5];'' is executed, what exactly is created in the JVM memory?', '5 fully initialized Employee objects on the heap.', 'An array containing 5 null references, capable of pointing to Employee objects.', 'A single Employee object containing an array of 5 elements.', 'An array of 5 primitive memory addresses initialized to 0.', 'B', 'Instantiating an array of objects does not automatically instantiate the objects themselves. It merely creates an array of reference variables on the heap, all initialized to ''null'' by default.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Array of Objects in Java', 'What occurs if you attempt to access a method on an element of a newly declared object array, such as ''staff[0].getName()'', immediately after declaring ''Employee[] staff = new Employee[5];''?', 'It returns an empty string or default value.', 'The compiler throws an "uninitialized variable" error.', 'The JVM throws a NullPointerException at runtime.', 'It automatically calls the default constructor of Employee to create the object.', 'C', 'Because the array elements are references initialized to ''null'', attempting to dereference them to call an instance method before explicitly assigning an actual object (e.g., ''staff[0] = new Employee();'') will result in a NullPointerException.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Array of Objects in Java', 'Which of the following is the correct syntax to declare, instantiate, and populate an array of objects inline in a single statement?', 'Employee[] staff = { new Employee("Alice"), new Employee("Bob") };', 'Employee[] staff = new Employee[2] { "Alice", "Bob" };', 'Employee staff[] = new Employee("Alice", "Bob");', 'Employee[] staff = [ new Employee("Alice"), new Employee("Bob") ];', 'A', 'Java allows inline array initialization using curly braces {}. When dealing with an array of objects, you must use the ''new'' keyword to instantiate each individual object inside those braces.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Array of Objects in Java', 'How does the memory structure of a ''String[]'' differ from an ''int[]'' in Java?', 'The ''int[]'' is stored on the stack, while the ''String[]'' is stored on the heap.', 'Both array objects are on the heap, but ''int[]'' stores actual integer values directly within the array''s contiguous memory, whereas ''String[]'' stores memory references pointing to scattered String objects on the heap.', 'The ''String[]'' can grow dynamically, while the ''int[]'' has a fixed size.', 'There is no difference in memory architecture between arrays of primitives and arrays of objects.', 'B', 'Arrays of primitives hold their values directly in contiguous heap memory. Arrays of objects hold references (pointers) in contiguous memory, but the actual objects they point to can be scattered anywhere on the heap.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Array of Objects in Java', 'Can an array declared as ''Object[]'' store references to different types of objects, such as a String, an Integer, and a custom Employee object simultaneously?', 'No, Java arrays are strictly homogeneous and can only store one exact type.', 'Yes, because all classes in Java implicitly inherit from java.lang.Object, enabling polymorphism within the array.', 'Yes, but primitive wrapper classes like Integer must be explicitly cast.', 'No, arrays of the Object class are abstract and cannot be instantiated.', 'B', 'Because ''Object'' is the root superclass of all Java classes, an ''Object[]'' can leverage polymorphism to store a reference to any object type in Java, effectively allowing for heterogeneous object collections.');
