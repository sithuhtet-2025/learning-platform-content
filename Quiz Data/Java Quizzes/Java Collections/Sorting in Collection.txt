INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Sorting in Collection', 'Which utility method is the standard mechanism for sorting a ''java.util.List'' into its natural ascending order?', 'Arrays.sort(list)', 'Collections.sort(list)', 'List.order()', 'Collections.order(list)', 'B', 'The ''java.util.Collections.sort(List<T> list)'' static method is the foundational algorithm for sorting collections in Java. It specifically targets Lists because Sets and Maps either maintain their own order or do not support indexed ordering.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Sorting in Collection', 'What architectural requirement must the elements of a List fulfill to be successfully sorted using the single-argument ''Collections.sort(list)'' method?', 'They must implement the java.util.Comparator interface.', 'They must override the equals() and hashCode() methods.', 'They must explicitly implement the ''java.lang.Comparable'' interface to define their natural sorting order, otherwise the JVM throws a ClassCastException at runtime.', 'They must be primitive wrapper classes (like Integer or Double).', 'C', 'The single-argument sort method implicitly relies on the elements knowing how to compare themselves. If the objects inside the List do not implement ''Comparable'', the JVM has no mathematical basis to sort them and crashes during the cast attempt.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Sorting in Collection', 'How can you sort a List of custom objects based on a specific property (like sorting Employees by salary) WITHOUT modifying the original object''s class to implement ''Comparable''?', 'By using the overloaded method: ''Collections.sort(list, new MyCustomComparator())''.', 'By calling ''list.sort()'' and passing an integer index.', 'By temporarily casting the list to a TreeSet.', 'It is architecturally impossible to sort them without modifying the class.', 'A', 'The overloaded ''Collections.sort(List<T> list, Comparator<? super T> c)'' method allows developers to inject external, custom sorting logic. The JVM will completely ignore the objects'' natural ordering and strictly use the provided ''Comparator'' engine.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Sorting in Collection', 'Since Java 8, what is the most direct and modern way to sort a ''List'' without using the ''Collections'' utility class?', 'Using the ''List.sort(Comparator c)'' default method directly on the list instance.', 'Using a parallel stream to rebuild the list.', 'Using the ''Arrays.parallelSort()'' method.', 'Using the ''List.order()'' method.', 'A', 'Java 8 added the default method ''sort(Comparator<? super E> c)'' directly to the ''List'' interface. This allows for highly readable, chained calls like ''myList.sort(Comparator.comparing(Employee::getName))'', delegating the operation to the underlying list implementation for optimized performance.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Sorting in Collection', 'If you attempt to pass a ''java.util.HashSet'' directly into ''Collections.sort()'', what is the result?', 'The set is sorted into natural order.', 'The compiler immediately throws a syntax error because ''Collections.sort()'' strictly accepts only objects implementing the ''List'' interface.', 'The set is converted to an array and sorted.', 'The JVM throws a RuntimeException.', 'B', 'The ''Collections.sort()'' method signature specifically demands a ''List''. A ''Set'' (like HashSet) is fundamentally unordered and lacks the index-based architecture required by the sorting algorithms to swap elements in place.');
