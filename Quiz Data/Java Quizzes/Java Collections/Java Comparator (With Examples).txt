INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Comparator (With Examples)', 'What is the exact method signature mandated by the ''java.util.Comparator<T>'' functional interface to establish a custom sorting strategy between two objects?', 'public int compareTo(T o1, T o2)', 'public boolean compare(T o1, T o2)', 'public int compare(T o1, T o2)', 'public void sort(T o1, T o2)', 'C', 'The ''Comparator'' interface defines the ''compare(T o1, T o2)'' method. It takes two distinct object parameters and returns a negative integer if o1 < o2, zero if they are equal, and a positive integer if o1 > o2.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Comparator (With Examples)', 'Why is the ''Comparator'' interface architecturally vital when working with classes from external, compiled, third-party libraries (e.g., a closed-source ''User'' class)?', 'It automatically decompiles the class to inject sorting logic.', 'It modifies the original class bytecode at runtime to implement Comparable.', 'Because you cannot modify a closed-source class to implement ''Comparable''. ''Comparator'' allows you to build an entirely separate, external sorting engine that evaluates the objects without needing to alter their internal source code.', 'Because third-party classes are always final and cannot be sorted otherwise.', 'C', 'This is the primary strength of the Strategy Pattern. ''Comparator'' separates the sorting logic from the data object itself, making it the only way to sort objects when you do not own or cannot modify the underlying class structure.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Comparator (With Examples)', 'Introduced in Java 8, which highly optimized static factory method completely eliminates the need to write bulky anonymous inner classes when creating a Comparator based on a single object property?', 'Comparator.sort(Employee::getSalary)', 'Comparator.comparing(Employee::getSalary)', 'Comparator.orderBy(Employee::getSalary)', 'Collections.compareBy(Employee::getSalary)', 'B', 'Java 8 leveraged functional interfaces to introduce ''Comparator.comparing(Function keyExtractor)''. By passing a method reference (like ''Employee::getSalary''), the JVM instantly generates a robust Comparator under the hood, vastly improving code readability and maintainability.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Comparator (With Examples)', 'If you need to execute a complex, multi-level sort—such as sorting a list of ''Employees'' by Department, and then sorting by Salary for employees within the same Department—how is this achieved using modern Java Comparator methods?', 'By writing a massive nested if-else statement inside a traditional compare() method.', 'By chaining the methods: ''Comparator.comparing(Employee::getDept).thenComparing(Employee::getSalary)''.', 'By calling Collections.sort() twice on the same list.', 'By using the ''Comparator.chain()'' method.', 'B', 'The default method ''thenComparing()'' allows developers to elegantly chain multiple comparators together. If the primary comparison (Department) evaluates to zero (a tie), the JVM automatically falls back to the secondary comparator (Salary) to break the tie.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Comparator (With Examples)', 'How do you instantly reverse the sorting logic of an existing, complex Comparator instance (e.g., ''Comparator<Employee> myComp'')?', 'By multiplying the return value of its compare() method by -1.', 'By invoking ''myComp.reverseOrder()''.', 'By passing it through ''Collections.descending(myComp)''.', 'By appending the ''.reversed()'' default method, such as ''myComp.reversed()''.', 'D', 'The ''Comparator'' interface provides the ''.reversed()'' default method. It acts as a decorator, wrapping the original comparator and automatically inverting its integer return values (swapping positive and negative), thereby reversing the sort order with zero manual mathematical logic required.');
