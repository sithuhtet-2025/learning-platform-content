INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java LinkedHashMap class', 'What specific architectural feature differentiates a ''LinkedHashMap'' from a standard ''HashMap''?', 'LinkedHashMap is fully synchronized for multi-threading, while HashMap is not.', 'LinkedHashMap only accepts String keys.', 'In addition to the standard hash table array, a LinkedHashMap threads an independent doubly-linked list through all of its entries, perfectly preserving the exact sequence of element insertion.', 'LinkedHashMap does not use hashing at all; it relies entirely on pointers.', 'C', 'A standard ''HashMap'' jumbles entry order based on hash buckets. A ''LinkedHashMap'' extends ''HashMap'' and adds ''before'' and ''after'' pointers to every single entry. This hybrid structure maintains the lightning-fast O(1) lookup of a hash table while providing the predictable iteration sequence of a linked list.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java LinkedHashMap class', 'By default, what is the exact iteration order guaranteed by the ''LinkedHashMap'' when looping through its ''keySet()'' or ''entrySet()''?', 'Alphabetical order based on the keys.', 'Random order based on hash codes.', 'Reverse insertion order (LIFO).', 'Strict Insertion Order: elements are returned in the exact chronological order that the keys were first put into the map.', 'D', 'Because of the internal doubly-linked list, iterating through a ''LinkedHashMap'' simply traverses the linked pointers from the oldest inserted node to the newest. Re-inserting an existing key does not affect its original chronological position in the list.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java LinkedHashMap class', 'A ''LinkedHashMap'' can be configured via its constructor to change its iteration order from "Insertion-Order" to "Access-Order" (by passing ''accessOrder = true''). What is the behavioral result of this switch?', 'It sorts the map dynamically based on memory addresses.', 'Whenever an existing entry is accessed (e.g., via ''get()'' or ''put()''), that specific entry is physically detached from its current linked-list position and relocated to the absolute end (tail) of the list, making the map iterate from least-recently-used (LRU) to most-recently-used.', 'It prevents the map from being accessed concurrently.', 'It orders the entries based on how frequently they are accessed (LFU).', 'B', 'Access-Order fundamentally alters the map''s behavior to act as an LRU (Least Recently Used) cache tracker. Merely reading a value with ''map.get(key)'' updates the linked list, pushing that entry to the "newest" position and leaving the untouched elements to languish at the "oldest" front of the list.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java LinkedHashMap class', 'To effortlessly build an automated LRU (Least Recently Used) Cache using ''LinkedHashMap'', which protected method must a developer override?', 'removeOldest()', 'evict()', 'removeEldestEntry(Map.Entry<K,V> eldest)', 'clearCache()', 'C', 'The ''removeEldestEntry()'' method is invoked internally by the map after every ''put()'' operation. By default, it returns ''false''. By overriding it to return ''true'' when the map exceeds a specific size (e.g., ''return size() > 100;''), the LinkedHashMap will autonomously delete the oldest (or least recently used) entry, functioning as a perfect, self-managing cache limit.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java LinkedHashMap class', 'What is the primary architectural trade-off incurred when choosing a ''LinkedHashMap'' over a standard ''HashMap''?', 'LinkedHashMap strictly prohibits null keys and null values.', 'LinkedHashMap suffers from severe performance degradation, shifting from O(1) to O(N) lookup time.', 'LinkedHashMap demands a larger memory footprint because every single entry requires two additional memory reference pointers (to maintain the doubly-linked list), and incurs a fractional CPU penalty to update those pointers during insertions and deletions.', 'LinkedHashMap cannot be resized dynamically.', 'C', 'The lookup and insertion speed remains O(1), almost identical to a ''HashMap''. The cost of predictability is memory. Every entry in the map carries the dead weight of a ''before'' and ''after'' reference pointer, increasing heap consumption compared to the leaner standard ''HashMap''.');
