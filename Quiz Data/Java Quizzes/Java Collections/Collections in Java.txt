INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Collections in Java', 'Which of the following core Java interfaces is strictly NOT a sub-interface of the root ''java.util.Collection'' interface?', 'java.util.List', 'java.util.Set', 'java.util.Map', 'java.util.Queue', 'C', 'The ''Map'' interface (which stores key-value pairs) is fundamentally architected differently than a standard collection of individual elements. Therefore, it does not inherit from the ''Collection'' interface, though it is still considered part of the broader Java Collections Framework.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Collections in Java', 'What is the primary architectural distinction between a ''List'' and a ''Set'' in the Java Collections Framework?', 'A List can only store Strings, while a Set can store any Object.', 'A List strictly maintains the insertion order of its elements and permits duplicate values, whereas a Set fundamentally prohibits duplicate elements and offers no guarantee of maintaining insertion order (with exceptions like LinkedHashSet).', 'A Set is thread-safe by default, while a List is not.', 'A List has a fixed capacity, whereas a Set grows dynamically.', 'B', 'The core contract of a ''Set'' (like HashSet) is mathematical uniqueness—it rejects duplicates. A ''List'' (like ArrayList) acts as a dynamic array, allowing multiple identical values and strictly preserving the exact sequence in which they were added.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Collections in Java', 'What is the absolute difference between ''java.util.Collection'' and ''java.util.Collections''?', 'They are aliases for the exact same entity.', '''Collection'' is the root interface representing a group of objects, whereas ''Collections'' is a utility class comprised exclusively of static methods (like sort(), reverse(), binarySearch()) that operate on or return collections.', '''Collections'' is an interface, and ''Collection'' is the abstract base class.', '''Collection'' is used for Maps, and ''Collections'' is used for Lists.', 'B', 'The ''Collection'' interface defines the blueprint for data structures (List, Set, Queue). The ''Collections'' class is a helper/utility class (similar to ''Math'' or ''Arrays'') that provides powerful polymorphic algorithms to manipulate those data structures.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Collections in Java', 'Which implementation of the ''List'' interface is structurally optimized for rapid, frequent insertions and deletions of elements at any position, but suffers from slow random access retrieval?', 'java.util.ArrayList', 'java.util.Vector', 'java.util.Stack', 'java.util.LinkedList', 'D', '''LinkedList'' is backed by a doubly-linked list architecture. Inserting or removing an element only requires updating a few reference pointers (O(1) if the node is known). However, retrieving an element at index N requires traversing the list sequentially (O(N)), unlike ''ArrayList'' which provides O(1) random access via its backing array.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Collections in Java', 'What specific runtime exception is thrown if a thread attempts to structurally modify a Collection (e.g., adding or removing an element) while it is actively iterating over it using a standard Iterator?', 'ConcurrentModificationException', 'IllegalStateException', 'UnsupportedOperationException', 'IndexOutOfBoundsException', 'A', 'Standard Java collections utilize "fail-fast" iterators. They maintain an internal ''modCount'' to track structural changes. If an iterator detects that the collection was modified outside of its own ''remove()'' method during iteration, it immediately throws a ''ConcurrentModificationException'' to prevent unpredictable behavior.');
