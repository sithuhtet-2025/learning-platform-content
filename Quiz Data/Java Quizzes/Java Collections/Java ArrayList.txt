INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java ArrayList', 'What is the internal data structure utilized by the JVM to back a ''java.util.ArrayList''?', 'A doubly-linked list of nodes.', 'A balanced binary search tree.', 'A dynamically resizing, contiguous array of object references.', 'A hash table with separate chaining.', 'C', 'An ''ArrayList'' is essentially an object-oriented wrapper around a standard Java array. It stores elements in a single contiguous block of heap memory, which provides exceptionally fast O(1) random access.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java ArrayList', 'When an ''ArrayList'' reaches its current maximum capacity and a new element is added, what is the exact mathematical resizing formula applied by the standard JVM implementation?', 'It increases its capacity by exactly 10 elements.', 'It calculates the new capacity as: oldCapacity + (oldCapacity >> 1), effectively growing by 50%.', 'It doubles its capacity (increases by 100%).', 'It throws an OutOfMemoryError unless manually resized.', 'B', 'When the backing array is full, the ''ArrayList'' dynamically creates a new array that is 50% larger than the old one (using a right bit-shift operation for speed). It then uses the native ''System.arraycopy()'' method to migrate all existing elements to the new, larger array.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java ArrayList', 'Why does the ''ArrayList'' class explicitly implement the ''java.util.RandomAccess'' marker interface?', 'To allow the generation of random numbers within the list.', 'To securely encrypt the memory locations of the elements.', 'To signal to polymorphic algorithms (like ''Collections.binarySearch()'') that this specific list supports instantaneous, constant-time (O(1)) element retrieval by index, allowing them to optimize their execution strategies.', 'To allow elements to be inserted randomly without shifting.', 'C', 'The ''RandomAccess'' interface contains zero methods. It acts as a flag for the JVM and utility classes. Because an ''ArrayList'' uses a contiguous array, retrieving ''list.get(50000)'' takes the exact same microsecond as ''list.get(0)''. Algorithms check for this interface to choose the fastest processing loop.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java ArrayList', 'What is the severe architectural drawback of calling ''list.remove(0)'' on an ''ArrayList'' containing 10 million elements?', 'It leaves a null gap at index 0, corrupting iteration.', 'It is actually an O(1) operation and has no drawback.', 'It forces the JVM to immediately shrink the backing array by 50%.', 'Because the array is contiguous, deleting the first element requires a massive O(N) operation where the JVM must physically shift the remaining 9,999,999 elements exactly one slot to the left to close the gap.', 'D', 'While ''ArrayList'' is incredibly fast for reading data, modifying the middle or beginning of the list is computationally disastrous for large datasets. Every element after the insertion or deletion point must be physically moved in memory using ''System.arraycopy()''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java ArrayList', 'Can a standard ''ArrayList'' directly store primitive data types such as ''int'', ''double'', or ''char''?', 'Yes, by declaring ''ArrayList<int>''.', 'No, Java Generics and Collections strictly operate on Objects (reference types). Primitives must be wrapped in their corresponding Wrapper classes (like ''Integer'' or ''Double''), which the compiler handles seamlessly via Autoboxing.', 'Yes, but only in Java 8 and above.', 'No, primitives must be converted to Strings first.', 'B', 'The Collections Framework relies on ''java.lang.Object''. Primitives do not inherit from ''Object'' and have no memory addresses to store in the backing array. Therefore, ''ArrayList<int>'' is a syntax error; you must use ''ArrayList<Integer>''.');
