INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Queue Interface', 'What is the primary architectural processing order dictated by the standard ''java.util.Queue'' interface?', 'LIFO (Last-In-First-Out)', 'Random Access', 'FIFO (First-In-First-Out)', 'FILO (First-In-Last-Out)', 'C', 'A Queue models a traditional waiting line. Elements are inserted at the end (tail) and are extracted from the beginning (head), strictly enforcing a First-In-First-Out processing pipeline.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Queue Interface', 'Like the Deque, the Queue interface defines two distinct sets of methods for insertion, removal, and inspection. What is the fundamental difference between ''queue.add(e)'' and ''queue.offer(e)'' when dealing with a capacity-restricted queue?', 'Both throw an IllegalStateException if the queue is full.', '''add(e)'' inserts the element but throws an ''IllegalStateException'' if the queue is full, whereas ''offer(e)'' gracefully returns ''false'' if it fails to insert due to capacity limits.', '''add(e)'' resizes the queue, whereas ''offer(e)'' overwrites the oldest element.', '''add(e)'' places elements at the head, whereas ''offer(e)'' places them at the tail.', 'B', 'The ''Queue'' API splits into exception-throwing methods (''add'', ''remove'', ''element'') and special-value-returning methods (''offer'', ''poll'', ''peek''). ''offer()'' is the preferred architectural choice for capacity-constrained queues (like thread-pool task queues) to avoid crashing on overload.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Queue Interface', 'If a queue is completely empty, what happens if you invoke the ''queue.remove()'' method versus the ''queue.poll()'' method to extract the head element?', '''remove()'' throws a NoSuchElementException, while ''poll()'' safely returns ''null''.', 'Both return null.', 'Both throw a NullPointerException.', '''remove()'' returns null, while ''poll()'' throws a NoSuchElementException.', 'A', 'Because ''remove()'' belongs to the strict exception-throwing group of the API contract, it crashes if the queue is empty. ''poll()'' is designed for safe asynchronous polling, returning ''null'' to signal the queue is currently empty without interrupting the thread.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Queue Interface', 'Which Queue implementation completely abandons the FIFO rule and instead orders its elements based on their priority (either natural sorting order or a provided Comparator)?', 'java.util.LinkedList', 'java.util.concurrent.ConcurrentLinkedQueue', 'java.util.PriorityQueue', 'java.util.ArrayDeque', 'C', 'A ''PriorityQueue'' does not respect insertion time. It operates as a priority heap. The "head" of the queue is always the mathematically smallest element (or highest priority according to the Comparator), regardless of when it was added to the structure.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Queue Interface', 'Which two methods allow a developer to inspect the element currently at the head of the queue without actually extracting or removing it from the data structure?', 'get() and view()', 'head() and front()', 'element() and peek()', 'poll() and remove()', 'C', 'The ''element()'' method returns the head but throws a ''NoSuchElementException'' if empty. The ''peek()'' method returns the head but safely returns ''null'' if the queue is empty. Neither method modifies the queue''s contents or shifts the elements.');
