INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Comparable Interface in Java (With Examples)', 'What is the specific method signature that a class must override when it implements the ''java.lang.Comparable<T>'' interface?', 'public boolean compare(T obj1, T obj2)', 'public int compareTo(T o)', 'public void sort()', 'public int equals(T o)', 'B', 'The ''Comparable'' interface defines a single method: ''public int compareTo(T o)''. This method establishes the "natural ordering" of the objects of that class, allowing them to be automatically sorted by utilities like ''Collections.sort()''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Comparable Interface in Java (With Examples)', 'When implementing ''compareTo(T o)'', what integer value must the method return to indicate that the current object (''this'') is strictly LESS THAN the specified object (''o'') in the sorting order?', 'Exactly -1', 'Any negative integer (value < 0)', '0', 'Any positive integer (value > 0)', 'B', 'The contract of ''compareTo'' dictates returning a negative integer if the current object is less than the argument, zero if they are equal, and a positive integer if the current object is greater. It does not strictly have to be -1 or 1; any negative/positive integer fulfills the contract.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Comparable Interface in Java (With Examples)', 'What happens at runtime if you pass a ''List'' of custom objects (like ''List<Employee>'') to ''Collections.sort(list)'' but the ''Employee'' class does NOT implement the ''Comparable'' interface?', 'The compiler throws a syntax error.', 'The JVM sorts the objects based on their memory hash codes.', 'The JVM throws a ClassCastException at runtime.', 'The list remains unsorted without any error.', 'C', 'The single-argument ''Collections.sort(List<T>)'' method inherently assumes that the elements define their own natural ordering. If the JVM attempts to cast the objects to ''Comparable'' during the sorting algorithm and fails, it throws a ''ClassCastException''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Comparable Interface in Java (With Examples)', 'What is the fundamental architectural difference between ''java.lang.Comparable'' and ''java.util.Comparator''?', 'Comparable sorts Strings, Comparator sorts Numbers.', 'Comparable modifies the actual class itself to define its default "natural" sorting logic. Comparator is an external, standalone class used to define multiple, alternative custom sorting strategies without modifying the original object''s source code.', 'Comparable is used for Lists, Comparator is used for Sets.', 'There is no functional difference.', 'B', 'If you own the source code and want a default sort (e.g., Employee sorted by ID), use ''Comparable''. If you cannot modify the class, or you need multiple ways to sort (e.g., sort Employee by Name, then sort by Salary), you build external ''Comparator'' classes.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Comparable Interface in Java (With Examples)', 'Assume an ''Employee'' class implements ''Comparable<Employee>''. If you want to sort a list of employees in REVERSE natural order (descending), what is the most efficient way to achieve this using standard Java utilities?', 'Manually rewrite the ''compareTo()'' method to reverse the logic.', 'Collections.sort(employeeList, Collections.reverseOrder());', 'Collections.descendingSort(employeeList);', 'Loop through the list backwards and copy it to a new array.', 'B', 'While you *could* rewrite the class, it breaks the concept of natural ordering. The cleanest architectural solution is to pass the ''Collections.reverseOrder()'' comparator as the second argument to the sort method, which intelligently inverses the logic of the class''s native ''compareTo'' implementation.');
