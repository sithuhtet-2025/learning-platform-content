INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Working of HashSet in Java', 'What is the precise internal mechanism the JVM utilizes to store elements in a ''HashSet''?', 'It uses a highly optimized boolean array.', 'It uses a generic List and calls contains() before adding.', 'It instantiates a private, hidden ''java.util.HashMap''. The elements added to the HashSet are stored strictly as the "Keys" of this backing HashMap.', 'It utilizes a proprietary hashing algorithm distinct from Maps.', 'C', 'A ''HashSet'' does not reinvent hashing. It acts as a facade. When you instantiate a ''HashSet'', it secretly creates a ''HashMap'' internally. The uniqueness constraints of the Set are achieved entirely by leveraging the uniqueness constraints of the Map''s keys.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Working of HashSet in Java', 'Because a HashMap requires both a Key and a Value, what data does the HashSet insert into the "Value" position of its backing HashMap?', 'It inserts a boolean ''true''.', 'It inserts a reference to the key itself, duplicating the object.', 'It inserts a constant, static dummy Object (typically named ''PRESENT'') to satisfy the Map.Entry requirement without wasting significant memory.', 'It inserts a null pointer.', 'C', 'The HashSet source code defines ''private static final Object PRESENT = new Object();''. Every time you add an element to the set, the JVM executes ''map.put(element, PRESENT)''. All keys map to this exact same, single instance of the dummy object.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Working of HashSet in Java', 'How does the ''set.add(element)'' method mathematically determine if it should return ''true'' (success) or ''false'' (duplicate)?', 'It searches the entire backing array sequentially.', 'It delegates to the backing HashMap''s ''put()'' method. If ''put()'' returns ''null'', the key was new, so ''add()'' returns true. If ''put()'' returns the old dummy ''PRESENT'' object, the key already existed, so ''add()'' returns false.', 'It calls ''equals()'' on every single element in the set.', 'It checks the load factor of the set.', 'B', 'The architecture is incredibly elegant. The Map''s ''put(K, V)'' method returns the previous value associated with the key. If the key is brand new, it returns null. The HashSet simply checks ''return map.put(e, PRESENT) == null;'' to inform the developer if the insertion was completely unique.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Working of HashSet in Java', 'Since a HashSet relies entirely on a HashMap, what is the expected time complexity for a ''set.contains(element)'' operation?', 'O(N) Linear Time', 'O(log N) Logarithmic Time', 'O(1) Constant Time, assuming a well-distributed hash function with minimal collisions.', 'O(N^2) Quadratic Time', 'C', 'Because the lookup simply hashes the element and checks the corresponding bucket in the hidden HashMap, determining if a Set contains an item is an instantaneous O(1) operation, giving HashSet massive performance advantages over ArrayList''s O(N) ''contains()'' method.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Working of HashSet in Java', 'If a developer creates a custom class (e.g., ''Student'') and adds multiple instances to a ''HashSet'', what must be overridden to ensure two logically identical Students are not both added to the set?', 'Only the ''equals()'' method.', 'Only the ''hashCode()'' method.', 'Both the ''hashCode()'' and ''equals()'' methods must be properly overridden to ensure logically identical objects hash to the same bucket and evaluate as equal.', 'The ''compareTo()'' method.', 'C', 'If you do not override both, the JVM uses the default Object implementations (based on memory addresses). Two identical Students created with ''new'' will have different memory addresses, hash to different buckets, and the HashSet will erroneously allow both duplicates into the collection.');
