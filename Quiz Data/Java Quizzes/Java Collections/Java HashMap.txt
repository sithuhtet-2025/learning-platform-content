INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java HashMap', 'What is the foundational data structure architecture backing a ''java.util.HashMap''?', 'A doubly-linked list of keys and values.', 'A pure, contiguous array of Objects.', 'An array of "Buckets" (Nodes), where each bucket stores a Linked List (or a Red-Black Tree in Java 8+) of Map.Entry objects to handle multiple keys that map to the same index.', 'A static hash table with no resizing capability.', 'C', 'A ''HashMap'' uses an array as its primary structure. The hash code of a key determines its index (bucket) in the array. If multiple keys hash to the same bucket (a collision), they are chained together in a linked structure within that specific bucket.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java HashMap', 'What specific architectural scenario is defined as a "Hash Collision" in a HashMap?', 'When a HashMap runs out of memory and crashes.', 'When two mathematically distinct keys produce the exact same array index (bucket location) after their hash codes are calculated and processed by the HashMap''s hashing algorithm.', 'When a thread attempts to write to the map while another is reading.', 'When a null key is inserted into a map that already has a null key.', 'B', 'A Hash Collision occurs when two different objects yield the same destination bucket. The HashMap resolves this by placing the new entry into the Linked List (or Tree) existing at that specific bucket index, rather than overwriting the old data.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java HashMap', 'What critical performance optimization was introduced to ''HashMap'' in Java 8 regarding heavily collided buckets?', 'Buckets with collisions are automatically deleted.', 'If the Linked List inside a single bucket grows beyond a certain threshold (default 8 elements), the HashMap dynamically transforms that specific linked list into a balanced Red-Black Tree, improving worst-case search time from O(N) to O(log N).', 'The HashMap automatically spins up a new thread to search the bucket.', 'The HashMap rejects any further additions to that bucket.', 'B', 'Prior to Java 8, a severe hash collision could degrade a HashMap''s performance to a linear O(N) search. By converting long linked lists into Red-Black Trees, Java 8 mathematically guarantees a much faster O(log N) worst-case retrieval time even under heavy collision load.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java HashMap', 'What is the architectural purpose of the "Load Factor" parameter (default 0.75) in a HashMap?', 'It dictates what percentage of the CPU the HashMap is allowed to consume.', 'It determines exactly how many null values are permitted.', 'It acts as a threshold multiplier. When the number of elements in the map exceeds the current capacity multiplied by the Load Factor (e.g., 75% full), the JVM triggers a massive "Rehash" operation, doubling the array size to prevent performance degradation.', 'It defines the maximum size of a bucket''s linked list.', 'C', 'As a HashMap fills up, the probability of collisions increases exponentially, slowing down retrieval. The Load Factor tells the HashMap to proactively double its internal array size and redistribute all existing elements (Rehashing) before the map becomes too congested.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java HashMap', 'What is the strict rule regarding ''null'' keys and ''null'' values in a standard ''java.util.HashMap''?', 'It completely forbids both null keys and null values.', 'It allows exactly one null key, and an unlimited number of null values.', 'It allows unlimited null keys, but no null values.', 'It allows null values, but throws an exception if a null key is used.', 'B', 'Because a Map inherently relies on unique keys, it can only logically accommodate a single ''null'' key (which it always artificially assigns a hash code of 0 and places in bucket 0). However, it imposes no uniqueness constraints on values, allowing as many ''null'' values as the developer requires.');
