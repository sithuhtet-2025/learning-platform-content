INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Map Interface', 'What is the absolute defining architectural characteristic of the ''java.util.Map'' interface that separates it from the rest of the Collections Framework?', 'It strictly prohibits null values.', 'It implements the java.util.Collection interface but overrides all its methods.', 'It stores data in standalone nodes rather than arrays.', 'It represents a mapping between a Key and a Value, rather than a sequence of single elements, and therefore strictly does NOT inherit from the java.util.Collection interface.', 'D', 'A Map is mathematically a dictionary or associative array. Because its fundamental contract operates on pairs of objects (K, V) rather than single elements (E), it completely bypasses the root ''Collection'' interface.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Map Interface', 'What is the strict mathematical rule enforced by the ''Map'' interface regarding Keys and Values?', 'Both Keys and Values must be completely unique.', 'Keys can be duplicated, but Values must be unique.', 'A Map cannot contain duplicate Keys; each Key can map to at most one Value, though multiple distinct Keys can map to the exact same Value.', 'Neither Keys nor Values have uniqueness constraints.', 'C', 'The Key acts as the unique identifier. If you attempt to map a new value to an already existing key, the Map will mathematically overwrite the old value. However, there is no restriction on two different keys pointing to identical values.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Map Interface', 'If a ''Map'' already contains the entry { "ID_123" : "Alice" }, what is the exact return value and structural result of executing ''map.put("ID_123", "Bob")''?', 'It returns ''false'' and ignores the insertion.', 'It returns "Bob" and stores both values as a list under the same key.', 'It overwrites "Alice" with "Bob" and safely returns the old overridden value ("Alice").', 'It throws a DuplicateKeyException.', 'C', 'The ''put(K key, V value)'' method is an overwrite operation if the key already exists. It strategically returns the previous value associated with that key (or ''null'' if there was no previous mapping) so the developer knows what data was destroyed.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Map Interface', 'Because a ''Map'' is not a ''Collection'', it cannot be iterated over directly using a standard Iterator. Which three "Collection Views" does the Map interface provide to enable iteration?', 'getKeys(), getValues(), and getEntries()', 'keySet() (returns a Set of keys), values() (returns a Collection of values), and entrySet() (returns a Set of Map.Entry Key-Value pairs).', 'keyList(), valueSet(), and mapArray()', 'iterator(), spliterator(), and stream()', 'B', 'To loop through a Map, you must extract a Collection View. ''keySet()'' guarantees uniqueness (hence a Set). ''values()'' allows duplicates (hence a generic Collection). ''entrySet()'' returns a Set of ''Map.Entry<K,V>'' objects, containing both the key and the value linked together.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Map Interface', 'Which implementation of the ''Map'' interface guarantees that the entries will be maintained in ascending key order, sorted either by the keys'' natural ordering or by a provided Comparator?', 'java.util.LinkedHashMap', 'java.util.HashMap', 'java.util.TreeMap', 'java.util.EnumMap', 'C', 'While ''HashMap'' is chaotic and ''LinkedHashMap'' maintains insertion sequence, ''TreeMap'' implements the ''SortedMap'' interface. It is backed by a Red-Black tree structure that dynamically sorts the keys logarithmically every time a new entry is put into the map.');
