INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java EnumSet class', 'What is the underlying, hyper-efficient data structure utilized by the JVM to implement a ''java.util.EnumSet''?', 'A standard array of object references.', 'A highly optimized Bit-Vector (a bitmask typically stored as a single 64-bit ''long'' primitive for enums with up to 64 constants).', 'A dynamically resizing Hash Table.', 'A balanced binary search tree.', 'B', 'An ''EnumSet'' is a masterpiece of efficiency. It doesn''t store actual objects. Instead, it uses a 64-bit ''long'' integer. If the Enum at ordinal 3 is present in the Set, the 3rd bit of the ''long'' is simply flipped to 1. This makes it incredibly compact and lightning fast.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java EnumSet class', 'Because its constructor is intentionally hidden, which static factory method should you invoke to create a new ''EnumSet'' that is initially pre-populated with EVERY constant defined in a specific Enum?', 'EnumSet.createAll(MyEnum.class)', 'EnumSet.fullOf(MyEnum.class)', 'EnumSet.allOf(MyEnum.class)', 'EnumSet.populate(MyEnum.class)', 'C', 'The ''EnumSet'' class provides expressive static factory methods. ''EnumSet.allOf(Class<E> elementType)'' creates a set containing all elements of the specified enum type, internally achieving this by simply turning all the bits in its bit-vector to 1.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java EnumSet class', 'Conversely, which static factory method initializes a completely empty ''EnumSet'' specifically typed for a certain Enum, ready to have elements added to it later?', 'EnumSet.empty(MyEnum.class)', 'EnumSet.noneOf(MyEnum.class)', 'EnumSet.createEmpty(MyEnum.class)', 'EnumSet.zero(MyEnum.class)', 'B', 'The ''EnumSet.noneOf(Class<E> elementType)'' method creates an empty set with the specified element type. Internally, the JVM allocates the bit-vector and initializes all bits strictly to 0.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java EnumSet class', 'What happens under the hood when you perform a mathematical bulk operation like ''setA.addAll(setB)'' (Union) or ''setA.retainAll(setB)'' (Intersection) on two EnumSets?', 'The JVM uses a standard Iterator to loop through every object and compare them one by one.', 'The JVM throws an exception because EnumSets cannot be merged.', 'The JVM executes a single, atomic, low-level bitwise operation (like bitwise OR ''|'' for Union, or bitwise AND ''&'' for Intersection) on the underlying bit-vectors, making it phenomenally faster than standard collections.', 'The JVM copies the sets into a HashSet to perform the calculation.', 'C', 'Because the sets are just bitmasks, complex mathematical set operations are reduced to literal CPU-level bitwise hardware instructions, making ''EnumSet'' operations orders of magnitude faster than a standard ''HashSet''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java EnumSet class', 'What exception is thrown if you attempt to add a ''null'' element to an ''EnumSet''?', 'IllegalArgumentException', 'UnsupportedOperationException', 'NullPointerException', 'No exception; nulls are permitted.', 'C', 'Just like ''EnumMap'', an ''EnumSet'' relies entirely on the ''ordinal()'' value of the object to determine which specific bit to flip in its bit-vector. Since a ''null'' reference has no ordinal, it immediately throws a ''NullPointerException''.');
