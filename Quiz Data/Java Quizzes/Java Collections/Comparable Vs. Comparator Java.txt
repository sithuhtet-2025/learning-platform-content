INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Comparable Vs. Comparator Java', 'What is the most fundamental architectural difference regarding how the ''Comparable'' and ''Comparator'' interfaces establish sorting logic?', '''Comparable'' allows sorting by multiple fields simultaneously, whereas ''Comparator'' strictly sorts by a single field.', '''Comparable'' is implemented directly inside the class whose objects are to be sorted (defining its single "natural" ordering), whereas ''Comparator'' is typically implemented in a separate, external class to define multiple, alternative sorting strategies without modifying the original class.', '''Comparable'' is used for Maps, whereas ''Comparator'' is used for Lists.', '''Comparable'' operates at runtime, whereas ''Comparator'' operates at compile time.', 'B', 'If an ''Employee'' class implements ''Comparable'', the sorting logic (e.g., sort by ID) becomes permanently baked into the class itself. If you later need to sort employees by Name, you cannot use ''Comparable''; you must create external ''Comparator'' classes that take two ''Employee'' objects and compare them.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Comparable Vs. Comparator Java', 'Which specific methods must be overridden to satisfy the contracts of the ''Comparable'' and ''Comparator'' interfaces, respectively?', 'compareTo() and compare()', 'compare() and compareTo()', 'sort() and order()', 'equals() and compare()', 'A', 'The ''java.lang.Comparable'' interface defines a single method: ''public int compareTo(T o)'', which compares the current instance (''this'') with the argument. The ''java.util.Comparator'' interface defines ''public int compare(T o1, T o2)'', which takes two distinct objects and compares them against each other.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Comparable Vs. Comparator Java', 'If you possess an ArrayList of custom objects from a third-party library (meaning you cannot modify their source code to implement interfaces), which approach MUST you use to sort them?', 'Use java.lang.Comparable by wrapping the objects.', 'Create a custom java.util.Comparator and pass it to Collections.sort().', 'Convert the ArrayList to an array and use Arrays.sort().', 'It is impossible to sort third-party objects.', 'B', 'Because ''Comparable'' requires directly modifying the source code of the class to implement the interface and override ''compareTo()'', it is useless for third-party classes you don''t own. A ''Comparator'' sits outside the target class, making it the only valid architectural solution for sorting closed-source objects.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Comparable Vs. Comparator Java', 'How do the method signatures for ''Collections.sort()'' differ when utilizing these two interfaces?', 'Both require a single argument.', '''Collections.sort(list)'' strictly requires the list elements to implement ''Comparable''. To use a ''Comparator'', you must use the overloaded signature: ''Collections.sort(list, comparatorInstance)''.', '''Comparable'' uses Collections.order(), ''Comparator'' uses Collections.sort().', '''Collections.sort()'' only accepts Comparators.', 'B', 'The single-argument ''sort(list)'' relies implicitly on the objects knowing how to sort themselves (natural ordering via ''Comparable''). To override this natural ordering, or to provide an ordering if one doesn''t exist, you must explicitly provide the external ''Comparator'' logic as the second argument.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Comparable Vs. Comparator Java', 'Introduced heavily in Java 8, how did functional programming permanently revolutionize the implementation of the ''Comparator'' interface?', 'It allowed Comparators to sort multidimensional arrays.', 'It deprecated the ''compare()'' method.', 'It transformed ''Comparator'' into a Functional Interface, allowing developers to define complex sorting algorithms instantly using Lambda expressions and chained static methods like ''Comparator.comparing(Employee::getName).thenComparing(Employee::getAge)''.', 'It forced Comparators to run on separate CPU threads.', 'C', 'Because ''Comparator'' has exactly one abstract method (''compare''), it qualifies as a Functional Interface. Java 8 leveraged this by adding powerful default and static methods, allowing developers to write highly readable, chained sorting logic in a single line of code instead of writing bulky anonymous inner classes.');
