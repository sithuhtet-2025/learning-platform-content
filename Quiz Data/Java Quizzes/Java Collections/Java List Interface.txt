INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java List Interface', 'What are the two absolute defining architectural characteristics of the ''java.util.List'' interface that separate it from the ''Set'' interface?', 'It strictly prohibits duplicate elements and uses hash codes for storage.', 'It strictly maintains the exact chronological insertion order of elements, and it explicitly permits the storage of duplicate (identical) elements.', 'It is intrinsically thread-safe and allows multiple inheritance.', 'It only accepts primitive data types and has a fixed maximum capacity.', 'B', 'The ''List'' contract guarantees that the sequence in which you add elements is permanently preserved. Furthermore, it mathematically allows identical elements (e.g., adding the string "Apple" five times results in a list size of 5), completely contrasting the unique, unordered nature of a ''Set''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java List Interface', 'Because elements in a ''List'' have a strict sequential order, what powerful capability does the ''List'' interface introduce that the broader ''Collection'' interface lacks?', 'The ability to iterate using a standard for-each loop.', 'The ability to serialize the collection.', 'The ability to access, insert, modify, or delete elements dynamically based on their precise integer zero-based index position (e.g., ''list.get(5)'', ''list.add(2, "Data")'').', 'The ability to clear all elements simultaneously.', 'C', 'The concept of an "index" does not exist in the root ''Collection'' interface because sets and queues do not guarantee order. The ''List'' interface introduces all index-based methods (''get'', ''set'', ''add(index, element)'', ''remove(index)''), treating the collection like a dynamic array.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java List Interface', 'Which specialized Iterator is returned by the ''list.listIterator()'' method, and what unique architectural capability does it possess?', 'A synchronized iterator that prevents ConcurrentModificationExceptions.', 'An iterator that automatically sorts the list as it traverses.', 'A ''ListIterator'', which allows for strict bidirectional traversal (moving both forward via ''next()'' and backward via ''previous()''), as well as providing the ability to modify or replace elements during traversal.', 'An iterator that bypasses null values.', 'C', 'While a standard ''Iterator'' only marches forward, the ''List'' interface provides a ''ListIterator''. Because lists are ordered, it logically makes sense to move backward. It provides ''hasPrevious()'', ''previous()'', and ''set(E e)'' to overwrite the current element dynamically.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java List Interface', 'If you execute ''List<String> sub = myList.subList(1, 4);'', what happens structurally within the JVM memory?', 'A completely new, independent ArrayList is created containing a deep copy of elements 1 through 3.', 'A completely new ArrayList is created containing elements 1 through 4.', 'No new list is created. The ''subList()'' method returns a structural "view" backed by the original list. Any modifications made to the subList (like adding or removing) will instantly mutate the original ''myList''.', 'The original list is truncated, deleting elements outside the 1-4 range.', 'C', 'The ''subList(fromIndex, toIndex)'' method is highly efficient because it does not copy data. It returns a window (a view) mapping directly to the specified range of the original list. Modifying the view modifies the underlying source list.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java List Interface', 'Which of the following classes is considered a legacy implementation of the ''List'' interface, heavily burdened by synchronized methods, and is generally avoided in modern architecture?', 'java.util.ArrayList', 'java.util.LinkedList', 'java.util.Vector', 'java.util.concurrent.CopyOnWriteArrayList', 'C', 'The ''Vector'' class predates the Java Collections Framework. When the Framework was introduced in Java 1.2, ''Vector'' was retrofitted to implement the ''List'' interface. Because every single method in ''Vector'' is synchronized, it causes massive CPU bottlenecking in single-threaded environments, leading to the creation of the unsynchronized ''ArrayList''.');
