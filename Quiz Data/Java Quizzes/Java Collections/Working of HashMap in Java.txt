INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Working of HashMap in Java', 'What is the absolute first computational step a HashMap takes when ''map.put(key, value)'' is invoked?', 'It iterates through the array to find an empty slot.', 'It dynamically resizes the array to ensure capacity.', 'It calls the ''hashCode()'' method on the provided key object to generate a 32-bit integer hash.', 'It calls the ''equals()'' method to check for duplicates.', 'C', 'The entire HashMap architecture is predicated on the key''s hash code. The JVM must first calculate this mathematical integer representation of the key. It then applies a secondary bitwise hash function to map this integer to a specific, valid index within the bounds of the backing array.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Working of HashMap in Java', 'Once the destination array index (bucket) is calculated, what happens if that specific bucket is already occupied by an existing entry with a DIFFERENT key (a Hash Collision)?', 'The HashMap throws a CollisionException.', 'The HashMap overwrites the old entry to prioritize the new data.', 'The HashMap places the new entry into the next sequentially available empty bucket (Linear Probing).', 'The HashMap chains the new Map.Entry object onto the existing entry, forming a Linked List (or a Red-Black Tree) inside that specific bucket.', 'D', 'Java HashMaps use "Separate Chaining" to resolve collisions. Instead of searching for a new bucket, the map simply adds the new node to a linked list residing at that exact bucket address. In Java 8+, if this list exceeds 8 nodes, it mathematically transforms into a balanced Red-Black tree for faster retrieval.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Working of HashMap in Java', 'During a ''map.get(key)'' operation, if the key hashes to a bucket containing a Linked List of multiple entries, how does the HashMap guarantee it returns the correct value?', 'It returns the most recently added value in the list.', 'It traverses the linked list and uses the ''=='' operator to check for exact memory addresses.', 'It traverses the linked list, utilizing the ''equals()'' method to compare the search key against the key stored in each node until it finds a perfect logical match.', 'It returns the first value in the bucket.', 'C', 'This highlights the critical contract between ''hashCode()'' and ''equals()''. ''hashCode()'' gets the JVM to the correct bucket, but because collisions exist, the JVM must use ''equals()'' to walk through the nodes in that bucket to find the exact, logically identical key.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Working of HashMap in Java', 'What internal threshold dictates when a HashMap must undergo the massive, computationally expensive "Rehashing" process?', 'When the number of entries equals the exact capacity of the array.', 'When the Capacity multiplied by the Load Factor (default 0.75) is reached. The JVM dynamically doubles the array size and recalculates the bucket index for every single existing entry.', 'When a linked list inside a bucket exceeds 8 elements.', 'When memory usage reaches 90%.', 'B', 'If a HashMap has a capacity of 16 and a load factor of 0.75, inserting the 13th element triggers a rehash. The array becomes size 32. Because the array size changed, the mathematical modulo formula for indexing changes, forcing the JVM to painstakingly redistribute every single node to new buckets.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Working of HashMap in Java', 'If two unequal keys purposefully return the exact same integer from their ''hashCode()'' methods, what is the architectural result?', 'The HashMap throws an IllegalArgumentException.', 'The keys will always collide and reside in the exact same bucket, degrading performance to O(N) linear search time for those entries, but the HashMap will still function correctly without data loss.', 'The second key overwrites the first key.', 'The HashMap places them in separate arrays.', 'B', 'Identical hash codes guarantee a collision. While valid Java, it destroys the O(1) efficiency of the map. The map gracefully handles this by chaining them in the same bucket and relying entirely on ''equals()'' to distinguish them during retrieval.');
