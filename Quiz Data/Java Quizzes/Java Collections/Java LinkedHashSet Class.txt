INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java LinkedHashSet Class', 'What is the precise underlying data structure utilized by the JVM to implement a ''java.util.LinkedHashSet''?', 'A dynamically resizing contiguous array.', 'A standard java.util.HashMap with a separate Array keeping track of order.', 'A java.util.LinkedHashMap, where the set elements are stored strictly as the map''s keys, and a constant dummy object is mapped as every value.', 'A pure doubly-linked list with no hashing.', 'C', 'Just as ''HashSet'' is a wrapper around ''HashMap'', a ''LinkedHashSet'' is simply a wrapper around a ''LinkedHashMap''. It utilizes the map''s hash table for O(1) uniqueness checks, while relying on the map''s internal doubly-linked list to preserve the exact insertion order of the elements.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java LinkedHashSet Class', 'What specific architectural guarantee does a ''LinkedHashSet'' provide that a standard ''HashSet'' absolutely does not?', 'It guarantees that elements are sorted in ascending numerical or alphabetical order.', 'It guarantees predictable iteration strictly in the chronological order that elements were inserted into the set.', 'It guarantees thread-safety for concurrent modifications.', 'It guarantees O(log N) search times.', 'B', 'A standard ''HashSet'' iterates in a chaotic, unpredictable sequence based on memory buckets. Because a ''LinkedHashSet'' maintains a doubly-linked list running through all of its entries, an Iterator will simply traverse this list from the oldest inserted element to the newest.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java LinkedHashSet Class', 'How does the execution performance of a ''LinkedHashSet'' generally compare to a standard ''HashSet''?', 'It is significantly faster for insertion because it doesn''t compute hash codes.', 'Insertion and removal are fractionally slower due to the overhead of updating the doubly-linked list pointers, but iteration over the collection can actually be slightly faster.', 'They have the exact same performance metrics across all operations.', 'LinkedHashSet is O(N) for searches, whereas HashSet is O(1).', 'B', 'Maintaining the ''before'' and ''after'' pointers for the linked list adds a slight CPU and memory penalty during ''add()'' and ''remove()''. However, iterating a ''LinkedHashSet'' is proportional to the number of *elements* it holds, whereas iterating a ''HashSet'' is proportional to the *capacity* of its backing array (including empty buckets), making LinkedHashSet iteration highly efficient.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java LinkedHashSet Class', 'What is the rule regarding ''null'' elements in a ''LinkedHashSet''?', 'It completely rejects null elements to maintain pointer integrity.', 'It permits an unlimited number of null elements.', 'It permits exactly one null element, seamlessly preserving its position in the insertion order like any other object.', 'It allows nulls but moves them to the absolute end of the set.', 'C', 'Following the standard contract of the ''Set'' interface and its backing map, ''LinkedHashSet'' accommodates exactly one ''null'' element. The JVM handles it natively without throwing a ''NullPointerException'' and remembers exactly when it was inserted relative to the other elements.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java LinkedHashSet Class', 'If you re-insert an element that already exists within the ''LinkedHashSet'' (e.g., ''set.add("A"); set.add("B"); set.add("A");''), what happens to its position in the iteration order?', 'It is physically moved to the absolute end of the list (acting as an access-order update).', 'The set throws an IllegalArgumentException.', 'The insertion is simply ignored, and the element strictly retains its original, chronological insertion position.', 'The element is removed entirely.', 'C', 'The ''add()'' method evaluates to ''false'' if the element is already present. The JVM does not modify the internal doubly-linked list pointers for that element, meaning it remains exactly where it was first inserted chronologically.');
