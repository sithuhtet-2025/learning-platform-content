INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Difference Between ArrayList and LinkedList in Java', 'What are the fundamentally different underlying data structures powering an ''ArrayList'' versus a ''LinkedList'' in Java?', 'An ArrayList is backed by a hash table, while a LinkedList is backed by a binary tree.', 'An ArrayList uses a dynamically resizing primitive array, whereas a LinkedList uses a doubly-linked list architecture consisting of independent nodes scattered across the heap, connected by memory reference pointers.', 'An ArrayList is a fixed-size array, while a LinkedList is a vector.', 'An ArrayList uses single pointers, while a LinkedList uses an array of pointers.', 'B', 'This structural difference defines their entire performance profiles. An ''ArrayList'' wraps a standard Java array, allocating memory in a single contiguous block. A ''LinkedList'' consists of separate ''Node'' objects (containing data, a ''prev'' pointer, and a ''next'' pointer) dynamically allocated anywhere in memory.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Difference Between ArrayList and LinkedList in Java', 'If an application frequently requires retrieving elements by their specific index (e.g., ''list.get(50000)''), which collection provides massively superior performance and why?', 'LinkedList, because it caches node locations.', 'ArrayList, because its backing array allows for instantaneous O(1) random access by mathematically calculating the exact memory address of the index, whereas LinkedList requires an O(N) sequential traversal from the head node.', 'LinkedList, because pointers are faster than arrays.', 'They provide identical O(1) performance for retrieval.', 'B', 'Arrays are contiguous in memory. If you want index 50,000, the JVM simply multiplies the index by the element size and jumps directly to that memory address. A LinkedList has no indexes; to find the 50,000th element, it must literally follow pointers from node 1, to node 2, to node 3... all the way to 50,000.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Difference Between ArrayList and LinkedList in Java', 'Conversely, if an application requires aggressively inserting and deleting elements from the absolute middle of a massive list, which structure is architecturally superior?', 'ArrayList, because memory shifting is hardware accelerated.', 'LinkedList, because once the position is found, inserting or removing a node is an O(1) operation that simply involves repointing the ''prev'' and ''next'' references of adjacent nodes, whereas ArrayList requires an O(N) operation to physically shift all subsequent elements in memory.', 'Both perform identically well for middle insertions.', 'Neither; a HashSet should be used.', 'B', 'If you insert a value at index 0 of an ArrayList containing 1 million items, the JVM must physically copy and shift all 1 million items exactly one slot to the right to make room. A LinkedList simply changes two pointers to snap a new node into the chain, without moving any other data.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Difference Between ArrayList and LinkedList in Java', 'How do ArrayList and LinkedList compare regarding sheer JVM memory footprint overhead?', 'ArrayList consumes more memory because arrays are larger than objects.', 'LinkedList consumes significantly more memory because every single element requires wrapping the data payload in a Node object that also stores two extra memory references (a ''prev'' and ''next'' pointer).', 'They consume the exact same amount of memory per element.', 'ArrayList consumes more memory due to its capacity multiplier.', 'B', 'An ArrayList stores just the object references in an array. A LinkedList incurs massive overhead. If you store 1 million Strings, a LinkedList creates 1 million extra ''Node'' objects, each requiring additional bytes for object headers and internal navigation pointers, aggressively consuming Heap space.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Difference Between ArrayList and LinkedList in Java', 'Beyond the standard ''List'' interface, what additional core collection interface does ''LinkedList'' implement that ''ArrayList'' strictly does not?', 'java.util.Set', 'java.util.Map', 'java.util.Deque', 'java.util.SortedList', 'C', 'Because of its doubly-linked architecture, ''LinkedList'' natively implements the ''Deque'' (Double Ended Queue) interface. This equips it with specialized methods like ''addFirst()'', ''removeLast()'', ''peek()'', and ''poll()'', making it an excellent candidate for implementing Stack or Queue data structures.');
