INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Deque', 'What does the acronym "Deque" stand for, and what fundamental data structure capability does it represent in Java?', 'Dynamic Enqueue; a queue that resizes automatically.', 'Double Ended Queue; a linear collection that supports element insertion and removal at both extremities (the head and the tail).', 'Dual Element Queue; a queue that processes two elements per CPU cycle.', 'Data Extraction Queue; a specialized queue for database operations.', 'B', 'Pronounced "deck", a Deque (Double Ended Queue) is a highly versatile data structure. Unlike a standard Queue (FIFO) or Stack (LIFO) which are restricted to single-end operations, a Deque allows you to push, pop, enqueue, and dequeue from both the absolute front and absolute back.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Deque', 'Which two primary concrete classes in the Java Collections Framework implement the ''java.util.Deque'' interface?', 'ArrayList and Vector', 'PriorityQueue and HashSet', 'ArrayDeque and LinkedList', 'Stack and Queue', 'C', 'The ''Deque'' interface is implemented by ''LinkedList'' (using a doubly-linked node architecture) and ''ArrayDeque'' (using a resizable circular array architecture). Both provide complete bidirectional access to the collection.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Deque', 'Why does official Java documentation strongly recommend using the ''Deque'' interface (specifically ''ArrayDeque'') instead of the legacy ''java.util.Stack'' class when a LIFO (Last-In-First-Out) stack data structure is required?', 'Because ''Stack'' cannot hold generic objects.', 'Because ''Stack'' extends the obsolete ''Vector'' class, meaning every single operation is burdened by severe, unnecessary synchronization locking overhead, completely ruining performance in single-threaded environments.', 'Because ''Stack'' has a fixed maximum size of 100 elements.', 'Because ''Deque'' automatically sorts elements as they are pushed.', 'B', 'The legacy ''Stack'' class is a design flaw from Java 1.0; it inherits from ''Vector'', making it fully synchronized. An ''ArrayDeque'' operates as an unsynchronized, massive-performance Stack (using ''push()'' and ''pop()'') that vastly outperforms the old ''Stack'' class.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Deque', 'In a high-performance, non-concurrent environment where you need to repeatedly add and remove elements strictly from both ends of the collection, which implementation is generally significantly faster and more memory-efficient?', 'java.util.LinkedList', 'java.util.ArrayList', 'java.util.ArrayDeque', 'java.util.Stack', 'C', 'While ''LinkedList'' technically provides O(1) operations at both ends, ''ArrayDeque'' is universally preferred. ''ArrayDeque'' avoids the massive memory overhead of creating millions of ''Node'' objects and leverages contiguous array caching in the CPU, resulting in significantly faster execution and lower garbage collection impact.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Deque', 'Is it architecturally permissible to insert ''null'' elements into an ''ArrayDeque''?', 'Yes, it perfectly accepts null values like an ArrayList.', 'No, the ''ArrayDeque'' implementation strictly prohibits ''null'' elements and will immediately throw a ''NullPointerException'' if attempted.', 'Yes, but only at the tail end.', 'No, unless the Deque is typed as Deque<Object>.', 'B', 'Unlike ''LinkedList'' (which permits nulls), ''ArrayDeque'' explicitly forbids ''null'' elements. This is because ''ArrayDeque'' internally uses a ''null'' return value from methods like ''poll()'' to definitively indicate that the deque is completely empty.');
