INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java TreeSet Class', 'What is the precise underlying data structure utilized by the JVM to back a ''java.util.TreeSet''?', 'A dynamically resizing array.', 'A standard hash table with linked buckets.', 'A java.util.TreeMap, which internally utilizes a self-balancing Red-Black binary search tree to store the elements as keys.', 'A doubly-linked list.', 'C', 'Similar to how HashSet wraps a HashMap, a ''TreeSet'' is simply a wrapper around a ''TreeMap''. It leverages the TreeMap''s Red-Black tree architecture to mathematically guarantee that all elements inserted into the set remain perfectly sorted.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java TreeSet Class', 'Because of its underlying tree architecture, what is the guaranteed time complexity for basic operations like ''add()'', ''remove()'', and ''contains()'' in a ''TreeSet''?', 'O(1) Constant Time', 'O(log N) Logarithmic Time', 'O(N) Linear Time', 'O(N log N) Linearithmic Time', 'B', 'A Red-Black tree balances itself automatically. Because the algorithm halves the search space at every node (going left or right), finding, inserting, or deleting an element mathematically guarantees an efficient O(log N) worst-case performance, which is slower than HashSet''s O(1) but keeps the data sorted.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java TreeSet Class', 'What happens if you attempt to insert a ''null'' element into a standard ''TreeSet'' (in Java 7 and later)?', 'It places the null element at the absolute beginning of the set.', 'It places the null element at the absolute end of the set.', 'The JVM immediately throws a ''NullPointerException''.', 'It silently ignores the insertion and returns false.', 'C', 'A ''TreeSet'' must compare every new element against existing elements to determine its sorted position in the tree. Because you cannot invoke ''compareTo()'' on a ''null'' reference, the tree instantly fails with a ''NullPointerException''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java TreeSet Class', 'Because ''TreeSet'' implements the ''NavigableSet'' interface, which highly specialized method returns the greatest element in the set that is strictly LESS THAN the given element?', 'lower(E e)', 'floor(E e)', 'ceiling(E e)', 'higher(E e)', 'A', 'The ''NavigableSet'' interface provides powerful search capabilities. ''lower()'' returns the greatest element strictly less than the target. ''floor()'' returns the greatest element less than OR EQUAL to the target.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java TreeSet Class', 'If you instantiate a ''TreeSet'' using its default, no-argument constructor (e.g., ''new TreeSet<Employee>()''), what strict requirement is placed upon the ''Employee'' class?', 'The Employee class must extend the ''java.util.Tree'' class.', 'The Employee class must be declared as ''final''.', 'The Employee class must explicitly implement the ''java.lang.Comparable'' interface and override the ''compareTo()'' method so the tree knows how to sort them.', 'The Employee class must have a public default constructor.', 'C', 'Without an external ''Comparator'' provided to the TreeSet''s constructor, the tree relies entirely on the objects'' "natural ordering". If the objects do not implement ''Comparable'', the JVM has no mathematical way to sort them and will throw a ''ClassCastException''.');
