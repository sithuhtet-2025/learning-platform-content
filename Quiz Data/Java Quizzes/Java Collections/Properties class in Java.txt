INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Properties class in Java', 'What is the specific architectural purpose of the ''java.util.Properties'' class?', 'To securely encrypt application source code.', 'To represent persistent sets of properties, primarily used for reading and writing application configuration settings (like database URLs or credentials) from external .properties text files.', 'To manage the memory properties of the JVM Garbage Collector.', 'To apply CSS styles to JavaFX UI components.', 'B', 'The ''Properties'' class is designed to handle key-value pairs representing application configurations. It natively supports parsing standard .properties files, allowing developers to change application behavior without recompiling the Java source code.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Properties class in Java', 'Which legacy Java class does the ''Properties'' class extend, meaning it inherently utilizes a synchronized, thread-safe hash table architecture?', 'java.util.HashMap', 'java.util.TreeMap', 'java.util.Hashtable', 'java.util.Dictionary', 'C', 'The ''Properties'' class is a direct subclass of the legacy ''java.util.Hashtable''. Because ''Hashtable'' is synchronized, the ''Properties'' object is entirely thread-safe for concurrent configuration lookups across multiple application threads.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Properties class in Java', 'Despite inheriting from a generic map structure that allows ''Object'' keys and values, what is the strict, intended data type for both keys and values within a ''Properties'' object?', 'Object and Object', 'String and Integer', 'String and String', 'Integer and String', 'C', 'Because property files are plain text, the ''Properties'' API is strictly designed to handle ''String'' keys mapping to ''String'' values. While you technically *can* use the inherited ''put(Object, Object)'' method, doing so violates the design contract and will cause the ''save()'' and ''load()'' methods to crash.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Properties class in Java', 'Which specific method must a developer call to extract configuration data from a ''FileInputStream'' pointing to a "config.properties" file and inject it into the ''Properties'' object in memory?', 'properties.read(InputStream)', 'properties.load(InputStream)', 'properties.parse(InputStream)', 'properties.import(InputStream)', 'B', 'The ''load(InputStream inStream)'' (or ''load(Reader reader)'') method instructs the ''Properties'' object to read the text stream, automatically parse the "key=value" string formatting from the file, and populate its internal hash table with the configurations.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Properties class in Java', 'What is the architectural advantage of using the ''getProperty(String key, String defaultValue)'' overloaded method?', 'It automatically updates the external file if the key is missing.', 'If the specified key is not found in the property list, it prevents a NullPointerException by safely returning the provided fallback ''defaultValue'', ensuring the application can continue running with a safe default configuration.', 'It creates a new thread to search for the key.', 'It throws an exception if the default value is invalid.', 'B', 'This is a crucial defensive programming pattern. If a configuration file is missing a critical entry (e.g., "timeout"), calling ''getProperty("timeout", "30")'' guarantees the application will receive the string "30" instead of crashing with a null reference.');
