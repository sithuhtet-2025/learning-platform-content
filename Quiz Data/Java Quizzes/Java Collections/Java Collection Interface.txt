INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Collection Interface', 'In the Java Collections Framework hierarchy, which specific interface sits directly above the ''java.util.Collection'' interface, acting as its superinterface?', 'java.util.List', 'java.lang.Object', 'java.lang.Iterable', 'java.util.Iterator', 'C', 'The ''Collection'' interface extends the ''java.lang.Iterable'' interface. This critical architectural decision ensures that every single class that implements ''Collection'' must provide an ''iterator()'' method, making all collections natively compatible with the enhanced ''for-each'' loop.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Collection Interface', 'Which major data structure interface in the ''java.util'' package intentionally does NOT extend the ''Collection'' interface?', 'java.util.Set', 'java.util.Map', 'java.util.List', 'java.util.Queue', 'B', 'The ''Map'' interface (implemented by HashMap, TreeMap, etc.) does not inherit from ''Collection''. A ''Collection'' represents a single sequence or group of individual elements. A ''Map'' represents a mathematical function mapping unique Keys to Values, requiring a fundamentally different architectural contract (e.g., ''put(K, V)'' instead of ''add(E)'').');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Collection Interface', 'What mathematical set operation is performed by the ''collectionA.retainAll(collectionB)'' method?', 'Union (combines all elements).', 'Difference (removes shared elements).', 'Intersection (removes all elements from collectionA EXCPET those that are also present in collectionB).', 'Cross Product.', 'C', 'The ''retainAll()'' method modifies the target collection to keep only the elements that intersect with the provided argument collection. It deletes everything else from the target, effectively executing a mathematical intersection operation.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Collection Interface', 'Why does the ''Collection'' interface NOT define a ''get(int index)'' method for retrieving elements?', 'Because elements are stored in random memory locations.', 'Because retrieving elements is handled strictly by the Iterator.', 'Because the ''Collection'' interface sits above unordered structures (like ''Set'') and queue structures (like ''Queue''). An index-based retrieval method only makes logical sense for an ordered sequence, which is why it is defined lower down in the specific ''List'' sub-interface.', 'Because it would violate encapsulation.', 'C', 'The ''Collection'' interface defines only the universal behaviors applicable to *all* collections. Since a ''HashSet'' has no concept of an "index" or sequential order, forcing it to implement a ''get(int index)'' method would break the interface segregation principle.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Collection Interface', 'What is the exact return type of the ''toArray()'' method defined in the ''Collection'' interface?', 'String[]', 'Object[]', 'Collection[]', 'T[]', 'B', 'The zero-argument ''toArray()'' method guarantees to safely extract the elements from the collection and return them as a standard, contiguous ''java.lang.Object[]'' array. (Note: A heavily overloaded version ''toArray(T[] a)'' exists to return arrays of a specific generic type).');
