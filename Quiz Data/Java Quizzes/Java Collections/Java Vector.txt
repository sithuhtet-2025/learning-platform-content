INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Vector', 'What is the absolute defining architectural characteristic of the legacy ''java.util.Vector'' class compared to a modern ''ArrayList''?', 'Vector is strictly limited to primitive data types.', 'Almost every single method in the Vector class is heavily synchronized (thread-safe), requiring a thread to acquire an intrinsic monitor lock before performing any operation.', 'Vector uses a doubly-linked list instead of an array.', 'Vector cannot grow dynamically; its size is permanently fixed upon instantiation.', 'B', 'Vector was designed in Java 1.0 before the Collections Framework existed. It was built to be inherently thread-safe by applying the ''synchronized'' keyword to all its core methods (add, get, remove). This causes severe performance bottlenecks in single-threaded environments.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Vector', 'When a ''Vector'' exhausts its current internal array capacity, what is its default mathematical resizing behavior?', 'It increases its capacity by exactly 10 elements.', 'It increases its capacity by 50% (oldCapacity + oldCapacity/2).', 'It creates a new array that is exactly DOUBLE (100% increase) the size of the old array, unless a specific capacity increment was provided in the constructor.', 'It throws an OutOfMemoryError.', 'C', 'Unlike ''ArrayList'' which grows by a conservative 50%, a ''Vector'' aggressively doubles its backing array size by default when it runs out of space. While this reduces the frequency of resize operations, it can waste massive amounts of heap memory if the dataset is large.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Vector', 'Which legacy interface does ''Vector'' uniquely support for iterating over its elements, which predates the modern ''Iterator'' interface?', 'java.util.Scanner', 'java.util.Enumeration', 'java.util.Traverser', 'java.util.Cursor', 'B', 'Because Vector is a legacy class from Java 1.0, it still supports the ''Enumeration'' interface via the ''vector.elements()'' method. The modern ''Iterator'' (which supports removing elements) was introduced later in Java 1.2 to replace ''Enumeration''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Vector', 'Which of the following methods is a legacy method completely unique to the ''Vector'' class (and its subclass ''Stack''), and is NOT part of the standard ''List'' interface contract?', 'add(E e)', 'get(int index)', 'addElement(E obj)', 'remove(int index)', 'C', 'Before ''Vector'' was retrofitted to implement the ''List'' interface in Java 1.2, it used entirely different method names. Methods like ''addElement()'', ''elementAt()'', and ''removeAllElements()'' are legacy methods that perform the exact same functions as the modern ''add()'', ''get()'', and ''clear()'' methods.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Vector', 'If an enterprise application requires a thread-safe, array-based list, what is the explicitly recommended modern architectural alternative to using the obsolete ''Vector'' class?', 'java.util.concurrent.CopyOnWriteArrayList', 'java.util.LinkedList', 'java.util.Collections.unmodifiableList()', 'java.util.concurrent.ConcurrentLinkedQueue', 'A', '''CopyOnWriteArrayList'' is the modern standard for concurrent lists. Instead of brute-force synchronizing every read and write like a Vector, it achieves thread safety by creating a completely fresh copy of the internal array every time a write operation occurs, allowing completely lock-free, instantaneous read operations.');
