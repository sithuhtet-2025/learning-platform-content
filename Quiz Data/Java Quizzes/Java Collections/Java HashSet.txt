INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java HashSet', 'What is the actual, underlying data structure utilized by the JVM to implement a ''java.util.HashSet''?', 'A highly optimized array of distinct objects.', 'A balanced binary search tree.', 'A standard java.util.HashMap, where the HashSet elements are stored strictly as the Map''s "Keys", and a constant dummy object is mapped as every "Value".', 'A doubly-linked list with uniqueness checks.', 'C', 'A ''HashSet'' does not invent a new storage mechanism. It acts as a wrapper around a ''HashMap''. When you call ''set.add(element)'', the JVM actually executes ''map.put(element, PRESENT)'', utilizing the Map''s native hash-collision logic to guarantee key uniqueness.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java HashSet', 'How does a ''HashSet'' respond when a developer attempts to insert a duplicate element that already exists within the set?', 'It throws an ''IllegalArgumentException''.', 'It overwrites the old element and returns true.', 'It silently ignores the addition, leaves the existing element untouched, and returns ''false''.', 'It appends a numerical suffix to the duplicate element.', 'C', 'The ''add(E e)'' method of the ''Set'' interface is a boolean function. If the ''HashSet'' determines (via hash code and equals() evaluation) that the element is already present, it rejects the insertion to maintain mathematical uniqueness and safely returns ''false''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java HashSet', 'What guarantee does a standard ''HashSet'' provide regarding the iteration sequence of its elements?', 'It guarantees elements are iterated in the exact order they were inserted.', 'It guarantees elements are sorted alphabetically or numerically.', 'It provides absolutely zero guarantee regarding the iteration order. The sequence depends entirely on the objects'' hash codes and the internal array''s capacity, which can shift dynamically during rehashing.', 'It guarantees elements are iterated in reverse insertion order.', 'C', 'Because the elements are scattered across an array of buckets based on mathematical hash algorithms, a ''HashSet'' is fundamentally unordered. As the set grows and rehashes its internal array, the apparent sequence of elements can completely change.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java HashSet', 'What is the architectural rule regarding ''null'' elements within a ''HashSet''?', 'Nulls are strictly forbidden and throw a NullPointerException.', 'It permits an unlimited number of null elements.', 'It strictly permits one, and only one, ''null'' element, which is typically routed directly to bucket 0 to bypass hash code calculation.', 'Null elements are permitted but cannot be retrieved.', 'C', 'Like its backing ''HashMap'' which allows one null key, a ''HashSet'' allows exactly one ''null'' element. The JVM recognizes the null reference and natively assigns it to the zero-index bucket without attempting to call ''hashCode()'' on it.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java HashSet', 'Assuming a properly distributed hash function, what is the expected time complexity for the fundamental ''add'', ''remove'', and ''contains'' operations in a ''HashSet''?', 'O(N) Linear Time', 'O(log N) Logarithmic Time', 'O(1) Constant Time', 'O(N^2) Quadratic Time', 'C', 'Because it is backed by a Hash Table, the JVM calculates the memory bucket instantly using the object''s hash code. Assuming minimal hash collisions, locating, inserting, or deleting the object takes a constant amount of time (O(1)), regardless of whether the set contains 10 elements or 10 million.');
