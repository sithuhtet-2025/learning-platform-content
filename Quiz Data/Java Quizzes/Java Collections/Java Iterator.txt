INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Iterator', 'What is the fundamental architectural role of the ''java.util.Iterator'' interface within the Java Collections Framework?', 'To sort the elements of a collection asynchronously.', 'To provide a universal, standardized cursor to traverse sequentially through any collection structure (List, Set, Queue) without exposing the collection''s complex underlying internal architecture.', 'To copy elements from one collection to another.', 'To allow multi-threaded read/write access to a collection without locking.', 'B', 'The ''Iterator'' implements the Iterator Design Pattern. It provides a uniform API (''hasNext()'', ''next()'') to loop through data, whether that data is stored in a contiguous array (ArrayList), scattered linked nodes (LinkedList), or hashed buckets (HashSet).');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Iterator', 'When an ''Iterator'' is first acquired via ''collection.iterator()'', where does its internal cursor initially point?', 'Exactly at the first element (index 0).', 'Exactly at the last element in the collection.', 'Strictly BEFORE the first element in the collection.', 'At a random element.', 'C', 'An Iterator''s cursor does not point *at* an element; it points *between* elements. Upon initialization, the cursor rests before the first element. You must call ''next()'' to force the cursor to jump over the first element and return its value.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Iterator', 'What is the ONLY architecturally safe and legal way to delete an element from a Collection while actively looping through it with an Iterator?', 'By calling ''collection.remove(element)'' directly on the main collection object.', 'By setting the element reference to ''null''.', 'By calling the ''iterator.remove()'' method, which safely deletes the exact element that was most recently returned by the ''next()'' method.', 'It is completely impossible to remove elements during iteration.', 'C', 'If you use the collection''s native ''remove()'' method while iterating, the Iterator''s internal counters fall out of sync with the collection''s state, triggering a ''ConcurrentModificationException''. Calling ''iterator.remove()'' ensures both the collection and the cursor are safely updated.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Iterator', 'What does the term "Fail-Fast" mean regarding standard Java Iterators (like those returned by ArrayList or HashSet)?', 'They execute iteration loops faster than standard ''for'' loops.', 'They bypass garbage collection to preserve memory.', 'If the underlying collection is structurally modified (elements added or removed) by any means other than the Iterator''s own ''remove()'' method after the Iterator is created, the Iterator will immediately throw a ''ConcurrentModificationException'' rather than risking unpredictable behavior.', 'They automatically skip null values to prevent crashing.', 'C', 'Fail-fast iterators monitor a ''modCount'' variable inside the collection. If the collection is modified externally, the ''modCount'' changes. The next time the iterator tries to move, it detects the discrepancy and intentionally "fails fast" by throwing an exception, prioritizing system stability over corrupted data reads.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Iterator', 'What is the strict directional limitation of the standard ''java.util.Iterator''?', 'It can move forward and backward, but cannot remove elements.', 'It is strictly unidirectional; it can only traverse elements moving forward from the beginning to the end.', 'It traverses from the end of the collection to the beginning.', 'It randomly accesses elements until all have been visited.', 'B', 'A standard ''Iterator'' only has a ''next()'' method. It can only march forward. If a developer requires bidirectional traversal (moving forward and backward using ''previous()''), they must upgrade to a ''ListIterator'', which is strictly available only for ''List'' implementations.');
