INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Collections Class Methods', 'What is the absolute defining characteristic of the ''java.util.Collections'' class regarding instantiation?', 'It is an abstract class that must be inherited.', 'It is strictly a utility class. Its constructor is explicitly private to prevent instantiation, and every single one of its algorithmic methods is declared as ''public static''.', 'It requires a Factory pattern to instantiate.', 'It is an interface that classes implement to gain sorting abilities.', 'B', 'Like the ''Math'' or ''Arrays'' classes, creating an object of type ''Collections'' is logically meaningless. It exists purely as a stateless namespace container for highly optimized, polymorphic static methods that operate on collection objects.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Collections Class Methods', 'What is the absolute, non-negotiable prerequisite state that a ''List'' must be in before you can successfully invoke ''Collections.binarySearch(list, key)'' on it?', 'The list must be converted to an array.', 'The list must be strictly sorted into ascending natural order (or sorted according to a provided Comparator). If it is unsorted, the result of the binary search is mathematically undefined and completely unreliable.', 'The list must contain no duplicate values.', 'The list must implement the Set interface.', 'B', 'Binary search algorithms work by repeatedly dividing the search space in half. If the dataset is not perfectly sorted, the algorithm will instantly fail to locate the target, often returning a negative index erroneously indicating the element is missing.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Collections Class Methods', 'What is the specific architectural result of wrapping an ArrayList using ''Collections.unmodifiableList(myList)''?', 'It permanently locks the list in the JVM heap, preventing Garbage Collection.', 'It encrypts the list elements using AES-256.', 'It returns a read-only "view" of the original list. If any thread attempts to call a mutator method (like add(), remove(), or set()) on this returned view, it immediately throws an ''UnsupportedOperationException''.', 'It converts the ArrayList into an immutable primitive array.', 'C', 'This is a crucial defensive programming technique. It allows you to pass a collection to third-party code or untrusted methods, guaranteeing they can read the data but actively preventing them from altering the underlying application state.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Collections Class Methods', 'If an application requires legacy Thread-Safe synchronization for an ''ArrayList'', which specific method provides this capability without switching to the obsolete ''Vector'' class?', 'Collections.lock(list)', 'Collections.secureList(list)', 'Collections.synchronizedList(list)', 'Collections.threadSafe(list)', 'C', 'The ''Collections.synchronizedList(List<T> list)'' method returns a synchronized (thread-safe) wrapper backed by the specified list. It automatically applies a monitor lock to every method call (add, get, remove) to serialize multi-threaded access.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Collections Class Methods', 'What sorting algorithm has traditionally been utilized under the hood by the ''Collections.sort()'' method for generic objects?', 'A pure Quicksort algorithm.', 'A brute-force Bubble Sort algorithm.', 'A highly optimized, stable, adaptive, iterative Mergesort variation known as TimSort, which guarantees O(n log n) performance and preserves the relative order of equal elements.', 'A Radix Sort algorithm.', 'C', 'Introduced in Java 7 (replacing the older modified mergesort), TimSort is exceptionally efficient for real-world data that often contains partially sorted sub-sequences. Most importantly, it is a "stable" sort, meaning if you sort Employees by Department, and then sort by Age, the Department groupings remain perfectly intact for employees of the same age.');
