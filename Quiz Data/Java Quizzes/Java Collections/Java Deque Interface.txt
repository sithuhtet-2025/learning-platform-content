INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Deque Interface', 'The ''Deque'' interface provides two distinct sets of methods for every operation (insertion, removal, examination). What is the fundamental architectural difference between these two sets?', 'One set is for the Head, the other is for the Tail.', 'One set operates synchronously, the other asynchronously.', 'One set immediately throws an exception if the operation fails (e.g., removing from an empty deque or adding to a capacity-restricted deque), whereas the other set safely returns a special value (like ''null'' or ''false'') to indicate failure without crashing.', 'One set is used for Collections, the other for Maps.', 'C', 'This dual-method architecture allows developers to choose their error-handling strategy. If a failure should logically crash the current process, use the exception-throwing methods (add, remove, get). If failure is expected and should be handled via conditional logic, use the special-value methods (offer, poll, peek).');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Deque Interface', 'If a ''Deque'' is currently completely empty, what is the exact execution result of invoking ''deque.removeFirst()'' versus ''deque.pollFirst()''?', 'Both return null.', 'Both throw a NoSuchElementException.', '''removeFirst()'' returns null, whereas ''pollFirst()'' throws a NoSuchElementException.', '''removeFirst()'' throws a NoSuchElementException, whereas ''pollFirst()'' gracefully returns ''null''.', 'D', 'Because ''removeFirst()'' belongs to the exception-throwing group, failing to find an element results in a crash (NoSuchElementException). Because ''pollFirst()'' belongs to the special-value group, it safely returns ''null'', allowing the program to continue executing.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Deque Interface', 'Which pair of methods is used strictly to EXAMINE the element at the Tail (rear) of the ''Deque'' without actually removing or modifying the collection?', 'getLast() and peekLast()', 'examineLast() and viewLast()', 'popLast() and pullLast()', 'tail() and getTail()', 'A', 'The ''getLast()'' method returns the tail element but throws an exception if empty. The ''peekLast()'' method returns the tail element but returns ''null'' if empty. Neither method physically removes the element from the underlying data structure.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Deque Interface', 'When using a ''Deque'' strictly as a standard FIFO (First-In-First-Out) Queue, which ''Deque'' methods perfectly map to the traditional ''enqueue'' and ''dequeue'' operations?', 'push() and pop()', 'addFirst() and removeLast()', 'addLast() (to enqueue at the rear) and removeFirst() (to dequeue from the front).', 'addFirst() and removeFirst()', 'C', 'In a standard FIFO queue line, new elements arrive at the back (Tail) and are serviced from the front (Head). Therefore, ''addLast()'' enqueues the element, and ''removeFirst()'' dequeues it.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Deque Interface', 'When using a ''Deque'' strictly as a LIFO (Last-In-First-Out) Stack, how do the traditional ''Stack'' methods ''push()'' and ''pop()'' map to the specific positional methods of the ''Deque'' interface?', '''push()'' maps to ''addLast()'', and ''pop()'' maps to ''removeFirst()''.', '''push()'' maps to ''addFirst()'', and ''pop()'' maps to ''removeFirst()''.', '''push()'' maps to ''addLast()'', and ''pop()'' maps to ''removeLast()''.', '''push()'' maps to ''offer()'', and ''pop()'' maps to ''poll()''.', 'B', 'In a LIFO stack, you add items to the very top, and remove items from the exact same top location. In the ''Deque'' architecture, the "top" of the stack is universally represented by the Head. Thus, pushing is ''addFirst()'' and popping is ''removeFirst()''.');
