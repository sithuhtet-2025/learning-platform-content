INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Stack class', 'What fundamental processing architecture does the legacy ''java.util.Stack'' class mathematically model?', 'FIFO (First-In-First-Out)', 'FILO (First-In-Last-Out)', 'LIFO (Last-In-First-Out)', 'Both LIFO and FIFO', 'C', 'A Stack represents a physical stack of plates. The last plate you put on the top (push) is the absolute first plate you must take off (pop). This enforces a Last-In-First-Out processing sequence.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Stack class', 'What is the severe architectural flaw regarding the inheritance hierarchy of the ''java.util.Stack'' class?', 'It implements the Queue interface instead of the Set interface.', 'It inherits from the legacy ''java.util.Vector'' class. This means every single Stack operation is aggressively synchronized via intrinsic monitor locks, causing massive performance bottlenecks in single-threaded environments.', 'It does not implement the Collection interface.', 'It inherits from ArrayList but fixes its capacity to 10.', 'B', 'Because ''Stack'' extends ''Vector'' (a relic from Java 1.0), it inherits all of Vector''s synchronized methods. Furthermore, it inherits Vector''s index-based methods like ''insertElementAt()'', which completely violates the strict LIFO contract by allowing elements to be injected into the middle of the stack.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Stack class', 'What does the ''stack.search(Object o)'' method explicitly return if the object is successfully located within the Stack?', 'The 0-based index of the array where the object resides.', 'A boolean value of true.', 'The 1-based position from the TOP of the stack. The top element is at position 1, the element below it is at position 2, etc.', 'A reference to the object itself.', 'C', 'The ''search()'' method is unique to the Stack class. It does not return a standard 0-based array index. It returns the "distance" from the top. If the item you pushed last is searched, it returns 1. If the item is not found, it returns -1.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Stack class', 'What specific runtime exception is thrown if you invoke ''stack.pop()'' or ''stack.peek()'' on a Stack object that contains absolutely zero elements?', 'NoSuchElementException', 'EmptyStackException', 'IndexOutOfBoundsException', 'NullPointerException', 'B', 'Unlike the Queue interface which throws a generic ''NoSuchElementException'', the legacy Stack class throws its own highly specific ''java.util.EmptyStackException'' if a thread attempts to read or extract the top element from an exhausted stack.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Stack class', 'Because the legacy ''Stack'' class is considered obsolete and its use is heavily discouraged, what is the official, modern architectural recommendation for implementing a LIFO stack in Java?', 'Use a java.util.LinkedList and synchronize it.', 'Use the java.util.Deque interface and its ArrayDeque implementation, invoking its native push() and pop() methods for vastly superior, unsynchronized performance.', 'Build a custom stack using primitive arrays.', 'Use java.util.Vector directly.', 'B', 'The official Java documentation explicitly states that a more complete and consistent set of LIFO stack operations is provided by the ''Deque'' interface. Using ''Deque<Integer> stack = new ArrayDeque<>();'' provides a modern, fast, thread-unsafe stack without the legacy baggage of Vector.');
