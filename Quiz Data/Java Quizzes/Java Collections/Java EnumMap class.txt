INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java EnumMap class', 'What is the highly optimized internal data structure utilized by the JVM to back a ''java.util.EnumMap''?', 'A balanced Red-Black Tree.', 'A standard Hash Table with separate chaining.', 'A simple, contiguous array where the internal index directly corresponds to the Enum key''s ordinal() value.', 'A doubly-linked list.', 'C', 'Because the exact number of Enum constants and their ordinal values are known strictly at compile time, an ''EnumMap'' does not need to compute hash codes. It simply creates an array equal to the size of the Enum, using the keys'' ordinal values (0, 1, 2...) as direct, instantaneous array indices.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java EnumMap class', 'Due to its architectural design, how does the performance of ''EnumMap'' compare to a standard ''HashMap''?', 'It is significantly slower because it has to iterate through the Enum values.', 'It is vastly superior in performance and memory efficiency because it completely eliminates the computational overhead of calculating hash codes and managing hash collisions.', 'They have the exact same performance characteristics.', 'It is faster for reading, but much slower for writing.', 'B', 'An ''EnumMap'' is the fastest Map implementation in Java for enum keys. By bypassing the hashing algorithm entirely and relying on direct array access, it provides guaranteed, constant-time O(1) performance without the memory footprint of Map.Entry wrapper objects.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java EnumMap class', 'What is the strict requirement when instantiating a new ''EnumMap'' object using its constructor?', 'You must provide a custom Comparator.', 'You must explicitly pass the ''Class'' object of the specific Enum type it will hold (e.g., ''new EnumMap<>(DayOfWeek.class)'').', 'You must specify the initial capacity as an integer.', 'You must pass an existing HashMap to clone.', 'B', 'Unlike a HashMap, an EnumMap needs to know exactly how large its internal array should be at the exact moment of creation. Passing the Enum''s ''Class'' object allows the JVM to extract the Enum''s universe size and pre-allocate the perfectly sized array.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java EnumMap class', 'How does an ''EnumMap'' handle the concept of ordering when iterating through its entries (e.g., via ''keySet()'' or ''entrySet()'')?', 'It iterates in a completely random, unpredictable order.', 'It iterates strictly in the order the elements were inserted into the map.', 'It naturally and strictly iterates in the exact sequential order in which the Enum constants were originally declared in their source file (based on their ordinal values).', 'It sorts the keys alphabetically by their string names.', 'C', 'Because the internal backing array is indexed by the Enum''s ordinal values, iterating through an ''EnumMap'' simply walks the array from index 0 upwards. This inherently guarantees that the iteration order perfectly matches the Enum declaration order.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java EnumMap class', 'Is it architecturally permissible to insert a ''null'' key into an ''EnumMap''?', 'Yes, it is stored at index 0.', 'No, attempting to insert a ''null'' key will immediately result in a ''NullPointerException''.', 'Yes, but only if the value is also null.', 'No, it will silently ignore the insertion.', 'B', 'An ''EnumMap'' relies entirely on calling the ''ordinal()'' method on the provided key to determine its array index. If the key is ''null'', calling ''null.ordinal()'' triggers an immediate ''NullPointerException''.');
