INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java LinkedList Class', 'What is the primary architectural memory model of a ''java.util.LinkedList''?', 'A single, contiguous block of heap memory.', 'A circular array that automatically overwrites old data.', 'A collection of discrete ''Node'' objects scattered non-contiguously across the heap, where each Node stores the element data along with two memory reference pointers mapping to the previous and next Nodes in the sequence.', 'A balanced tree structure.', 'C', 'A ''LinkedList'' does not use arrays. It dynamically allocates a new wrapper object (a ''Node'') for every single element added. The collection is held together entirely by the ''prev'' and ''next'' reference pointers connecting these scattered nodes into a logical chain.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java LinkedList Class', 'Beyond the ''List'' interface, which other critical core interface does ''LinkedList'' natively implement, massively expanding its architectural utility?', 'java.util.Set', 'java.util.Deque', 'java.util.Map', 'java.util.SortedList', 'B', 'Because of its bi-directional pointer architecture, ''LinkedList'' implements the ''Deque'' (Double Ended Queue) interface. This natively equips it with methods like ''addFirst()'', ''removeLast()'', ''push()'', and ''pop()'', making it capable of acting as both a Stack and a Queue.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java LinkedList Class', 'What is the severe computational penalty of invoking ''linkedList.get(50000)'' on a list containing 100,000 elements?', 'It requires an instantaneous O(1) mathematical memory jump.', 'It throws an IndexOutOfBoundsException because LinkedLists do not support indexing.', 'Because it lacks a contiguous array, the JVM must start at the head (or tail) node and sequentially traverse exactly 50,000 reference pointers (O(N) time) just to locate the target node.', 'It forces the JVM to garbage collect the first 49,999 elements.', 'C', 'This is the absolute weakness of a ''LinkedList''. It does not support random access. To find an element in the middle, the JVM literally has to follow the chain of pointers one by one, making index-based retrieval disastrously slow for large datasets.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java LinkedList Class', 'Why is ''LinkedList'' generally considered architecturally superior to ''ArrayList'' when an application requires aggressive, continuous insertion and deletion of elements at the absolute beginning (Head) of a massive list?', 'Because LinkedList automatically sorts elements upon insertion.', 'Because an ArrayList must physically execute an O(N) memory shift of millions of elements every time an item is inserted at index 0, whereas a LinkedList performs an O(1) operation by simply repointing two reference variables to snap a new Node onto the front.', 'Because LinkedList uses less heap memory.', 'Because LinkedList is intrinsically thread-safe.', 'B', 'Inserting at index 0 of an ''ArrayList'' is computationally devastating as it requires shifting the entire array using ''System.arraycopy()''. A ''LinkedList'' simply creates a new Node, points its ''next'' reference to the old Head, and updates the Head pointer—a virtually instantaneous operation.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java LinkedList Class', 'What is the primary trade-off regarding the memory footprint of a ''LinkedList''?', 'It consumes significantly less memory than ArrayList because it does not allocate empty buffer capacity.', 'It consumes significantly more memory per element because every primitive or object must be wrapped in a ''Node'' object, which introduces massive overhead for object headers and the two 64-bit reference pointers (prev and next).', 'It caches elements on the hard drive, freeing up RAM.', 'It shares memory pointers with other lists.', 'B', 'While an ''ArrayList'' just stores an array of references, a ''LinkedList'' suffers from severe object bloat. Storing 1,000,000 Integers requires allocating 1,000,000 ''Node'' objects on the heap, massively increasing memory consumption and putting severe pressure on the Garbage Collector.');
