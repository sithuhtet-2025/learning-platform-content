INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Difference Between ArrayList and Vector in Java', 'What is the absolute defining architectural distinction between an ''ArrayList'' and a ''Vector'' regarding concurrency?', 'ArrayList is thread-safe, while Vector is not.', 'Vector internally utilizes the ''volatile'' keyword for all variables, while ArrayList does not.', 'Almost every method in the ''Vector'' class is explicitly marked with the ''synchronized'' keyword making it inherently thread-safe, whereas ''ArrayList'' is completely non-synchronized and strictly not thread-safe.', 'Vector runs on a dedicated CPU thread.', 'C', 'If multiple threads attempt to modify an ''ArrayList'' simultaneously, race conditions and ''ConcurrentModificationExceptions'' will destroy the data. A ''Vector'' applies a monitor lock to the entire object for every single operation (add, get, remove), guaranteeing thread safety at the OS level.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Difference Between ArrayList and Vector in Java', 'Due to this architectural distinction, how do they compare in terms of execution performance in a standard, single-threaded application?', 'Vector is significantly faster due to memory caching.', 'ArrayList is vastly superior in performance because it completely avoids the severe computational overhead of repeatedly acquiring and releasing intrinsic monitor locks that Vector incurs on every single method call.', 'They have exactly the same performance in a single-threaded environment.', 'Vector is faster because it uses primitive arrays.', 'B', 'Synchronization is expensive. Even if a ''Vector'' is only being used by one thread, the JVM still forces that thread to negotiate a lock for every operation. ''ArrayList'' eliminates this overhead, making it the absolute standard for single-threaded or externally-synchronized data lists.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Difference Between ArrayList and Vector in Java', 'When their internal backing arrays run out of capacity, how do the automatic resizing algorithms differ between ArrayList and Vector?', 'ArrayList doubles in size (100%), while Vector increases by exactly 10 elements.', 'ArrayList increases its capacity by 50%, whereas Vector defaults to doubling its capacity (100% increase), resulting in a much larger memory footprint for massive datasets.', 'Vector throws an exception instead of resizing.', 'Both increase their capacity by 50%.', 'B', 'When an ''ArrayList'' reaches its limit, the JVM creates a new array that is 1.5 times the size of the old one. A ''Vector'', by default, creates a new array that is 2.0 times the size (doubling it). Vector does allow you to specify a custom capacity increment in its constructor, but its default is highly aggressive.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Difference Between ArrayList and Vector in Java', 'Where do these two classes originate in the history of the Java JDK?', 'Both were introduced simultaneously in Java 8.', 'ArrayList was introduced in Java 1.0, while Vector is a modern addition from Java 5.', 'Vector is a legacy class from Java 1.0 that was later retrofitted to implement the List interface, whereas ArrayList was specifically designed from scratch as the cornerstone of the new Java Collections Framework introduced in Java 1.2.', 'Vector is a C++ library ported to Java.', 'C', 'Because ''Vector'' existed before the Collections Framework was invented, it contains many redundant, legacy methods (like ''addElement()'' instead of ''add()''). It is considered largely obsolete in modern Java engineering.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Difference Between ArrayList and Vector in Java', 'If you require a thread-safe List in a modern, highly concurrent Java application, what is the explicitly recommended architectural alternative to using the legacy ''Vector'' class?', 'Wrap an ArrayList in a ''Collections.synchronizedList()'' or use the highly concurrent ''java.util.concurrent.CopyOnWriteArrayList''.', 'Use a standard LinkedList and mark it as volatile.', 'Write a custom synchronization wrapper around an array.', 'Use a HashSet instead.', 'A', 'While ''Collections.synchronizedList(new ArrayList<>())'' acts similarly to a Vector, the modern standard for concurrent read-heavy list operations is ''CopyOnWriteArrayList''. It completely avoids locking during read operations by creating a fresh copy of the internal array upon every write, vastly outperforming Vector''s brute-force locking mechanism.');
