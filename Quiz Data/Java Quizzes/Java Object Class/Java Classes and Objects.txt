INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Classes and Objects', 'In the context of Object-Oriented Programming in Java, what is the precise relationship between a Class and an Object?', 'A class is a physical entity in memory, while an object is a logical template.', 'A class is a logical template or blueprint, while an object is a physical instance of that blueprint that consumes memory.', 'They are synonymous terms for the exact same concept.', 'A class holds data, while an object only holds methods.', 'B', 'A class is a blueprint defining the state (fields) and behavior (methods) of a concept. An object is a concrete instance of that class, occupying physical memory in the JVM at runtime.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Classes and Objects', 'When the ''new'' keyword is used to instantiate an object, where is the memory for that specific object data allocated within the JVM?', 'The Thread Stack', 'The Metaspace (PermGen)', 'The Heap', 'The CPU Cache', 'C', 'In Java, all objects and their corresponding instance variables are dynamically allocated on the Heap memory area when the ''new'' operator is executed.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Classes and Objects', 'What happens to the instance variables of an object immediately after the object is allocated on the heap, but before the explicit constructor body executes?', 'They hold unpredictable garbage values.', 'They are explicitly set to null, causing a NullPointerException if accessed.', 'They are automatically initialized to their default values (e.g., 0 for int, false for boolean, null for object references).', 'They trigger a compilation error if not initialized inline.', 'C', 'Before the constructor code runs, Java guarantees memory safety by zeroing out the allocated heap space, initializing all instance variables to their standard default values based on their type.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Classes and Objects', 'What defines an "anonymous object" in Java?', 'An object whose class is marked as ''private''.', 'An object instantiated without being assigned to any reference variable, often used for immediate, single-use method calls.', 'An object that implements no interfaces.', 'An object whose memory is immediately freed by the OS.', 'B', 'An anonymous object is created using ''new ClassName()'' but is not assigned to a variable (e.g., ''new Car().startEngine();''). It is typically used when the object is only needed for a single operation, after which it becomes immediately eligible for garbage collection.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Classes and Objects', 'Given the declaration ''Car myCar = new Car();'', what exactly does the variable ''myCar'' hold in memory?', 'The actual data fields and methods of the Car object.', 'The entire compiled bytecode of the Car class.', 'A memory address reference pointing to where the Car object is physically located on the heap.', 'A primitive copy of the object.', 'C', 'In Java, object variables are reference variables. ''myCar'' does not hold the object itself; it is stored on the stack and holds a reference (pointer) to the memory location on the heap where the ''Car'' object resides.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Classes and Objects', 'If you execute the code ''Car car1 = new Car(); Car car2 = car1;'', how many objects are created in memory?', 'Two separate objects with identical data.', 'One object, but the compiler generates a warning.', 'Exactly one object is created on the heap, and both reference variables point to that single object.', 'Zero objects are created until a method is called.', 'C', 'The ''new'' keyword is only used once, so only one object is instantiated. The assignment ''car2 = car1'' simply copies the memory reference, meaning both variables now point to the exact same object instance in the heap.');
