INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('this Keyword in Java', 'What is the most common and practical use case for the ''this'' keyword regarding instance variables?', 'To declare a variable as a constant.', 'To resolve shadowing when a local variable or method parameter has the exact same name as an instance variable.', 'To allow a static method to access non-static fields.', 'To cast an object to its parent class type.', 'B', 'When a method parameter shares the same name as an instance variable, the local parameter "shadows" the instance variable. Using ''this.variableName'' explicitly tells the compiler to refer to the object''s instance variable rather than the local parameter.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('this Keyword in Java', 'What does the statement ''this();'' accomplish when placed inside a class constructor?', 'It creates a new, separate instance of the current class.', 'It invokes the superclass''s default constructor.', 'It invokes another overloaded constructor within the same class that takes no arguments.', 'It forcefully triggers the garbage collector on the current object.', 'C', 'The ''this()'' syntax is used for constructor chaining. It allows one constructor to call another overloaded constructor within the exact same class, promoting code reuse and centralized initialization logic.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('this Keyword in Java', 'Which of the following contexts strictly prohibits the use of the ''this'' keyword?', 'Inside an instance initializer block.', 'Inside a constructor body.', 'Inside a ''public static void main'' method.', 'Inside a private instance method.', 'C', 'The ''this'' keyword acts as a reference to the current object instance. Because ''static'' methods (like ''main'') belong to the class itself and execute without any object context, attempting to use ''this'' inside them results in a compilation error.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('this Keyword in Java', 'How is the ''this'' keyword utilized to implement the "Method Chaining" or "Builder" design pattern?', 'By passing ''this'' as an argument to a static factory method.', 'By returning ''this'' at the end of a method, allowing subsequent method calls to be chained sequentially on the same object reference.', 'By assigning ''this'' to a new local variable in every method.', 'Method chaining uses ''super'', not ''this''.', 'B', 'If an instance method specifies the class type as its return type and ends with ''return this;'', it returns the current object reference. This allows the caller to chain method invocations directly (e.g., ''obj.setX(5).setY(10).build();'').');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('this Keyword in Java', 'Is it legal to pass the ''this'' keyword as an argument to a method of another class (e.g., ''eventManager.register(this);'')?', 'No, ''this'' can only be used to access variables and methods within its own class.', 'Yes, passing ''this'' acts as passing a reference of the currently executing object to the external method.', 'No, it causes an IllegalReferenceException at runtime.', 'Yes, but the receiving method must be static.', 'B', 'Passing ''this'' as an argument is a fundamental technique in event-driven programming and callbacks. It provides the receiving method with a direct reference to the object that initiated the call.');
