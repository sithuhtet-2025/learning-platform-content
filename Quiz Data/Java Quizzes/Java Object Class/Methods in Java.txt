INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Methods in Java', 'What components strictly constitute the "Method Signature" in Java, which the compiler uses to resolve overloaded methods?', 'The method name, return type, and access modifier.', 'The method name and the return type.', 'The method name and the number, type, and order of its parameters.', 'The parameter list and the throws clause.', 'C', 'According to the Java Language Specification, a method signature consists exclusively of the method''s name and the types of its parameters in their exact order. Return types and exceptions are ignored when differentiating overloaded methods.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Methods in Java', 'How does Java handle passing arguments to methods?', 'Primitive types are passed by value, and objects are passed by reference.', 'Everything in Java is strictly passed by value.', 'Everything is passed by reference to optimize memory.', 'It depends on whether the method is static or an instance method.', 'B', 'Java is strictly "pass-by-value". For primitives, a copy of the actual value is passed. For objects, a copy of the memory reference (the pointer) is passed, not the object itself. You cannot change which object the original reference points to from within the method.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Methods in Java', 'What is the strict syntactical rule when utilizing varargs (variable-length arguments) in a method definition (e.g., ''void log(String context, int... data)'')?', 'A method can have multiple varargs parameters as long as they are different types.', 'The varargs parameter must be the absolute last parameter in the method''s parameter list.', 'Varargs can only be used with primitive data types.', 'The varargs parameter must be declared before any standard parameters.', 'B', 'To prevent ambiguous method calls, Java mandates that a method can have at most one varargs parameter, and it must be the very last parameter declared in the method signature.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Methods in Java', 'If a method defines a non-void return type (e.g., ''public int calculate()''), what is the compiler''s requirement regarding its execution paths?', 'It must return a value only if an exception is not thrown.', 'It is optional to return a value; it defaults to 0 or null if omitted.', 'Every possible execution path (including all if/else branches) must definitively result in a ''return'' statement of the correct type or throw an exception.', 'It must have exactly one ''return'' statement located at the very end of the method block.', 'C', 'The Java compiler performs strict flow analysis. If a method declares a return type, the compiler verifies that it is impossible for the method to complete normally without executing a valid ''return'' statement.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Methods in Java', 'What distinguishes an ''abstract'' method from a standard method?', 'It can only return primitive types.', 'It is defined without a method body (implementation) and requires subclasses to provide the implementation.', 'It executes significantly faster than standard methods.', 'It is implicitly static and final.', 'B', 'An abstract method acts as a strict contract. It declares the method signature but ends with a semicolon instead of a code block. Concrete subclasses inheriting the abstract class or interface are forced to override and implement the method.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Methods in Java', 'Which implicit objects does the Java compiler pass into an instance method (non-static) that are NOT passed into a static method?', 'The ''String[] args'' array.', 'The ''this'' reference pointing to the calling object.', 'A Class object representing the metadata.', 'The Garbage Collector thread reference.', 'B', 'When an instance method is called, the JVM implicitly passes a reference to the current object executing the method, accessible via the ''this'' keyword. Static methods do not receive this reference.');
