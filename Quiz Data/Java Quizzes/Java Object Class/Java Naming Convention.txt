INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Naming Convention', 'Are standard Java naming conventions (like camelCase for methods) enforced by the Java compiler?', 'Yes, failing to follow them results in a compilation error.', 'Yes, but only in enterprise editions of Java.', 'No, they are universally accepted community standards for readability and maintainability, but the compiler only enforces basic syntax rules.', 'Yes, they are strictly enforced by the JVM at runtime.', 'C', 'Naming conventions are not language rules enforced by the compiler; they are stylistic guidelines established by Sun Microsystems (and the community) to ensure code is readable and uniform across different development teams.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Naming Convention', 'According to standard Java naming conventions, how should the identifier for a Class or Interface be formatted?', 'camelCase (e.g., myCustomClass)', 'PascalCase (e.g., MyCustomClass)', 'snake_case (e.g., my_custom_class)', 'UPPER_SNAKE_CASE (e.g., MY_CUSTOM_CLASS)', 'B', 'Classes and Interfaces should be named using nouns (or adjectives for interfaces) in PascalCase, meaning every word in the identifier begins with a capital letter, including the first word.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Naming Convention', 'What is the universally recognized naming convention for Java packages to ensure global namespace uniqueness?', 'CamelCase starting with the project name.', 'All uppercase letters to distinguish them from classes.', 'All lowercase letters, typically structured starting with a reversed top-level domain name (e.g., com.company.project).', 'Prefixing the package name with "pkg_".', 'C', 'Package names should be written in all lowercase to avoid conflict with class names. The standard convention to ensure uniqueness is to reverse the creator''s internet domain name (e.g., ''org.apache.commons'').');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Naming Convention', 'How should a variable declared as ''public static final'' (a constant) be named according to Java conventions?', 'camelCase', 'PascalCase', 'UPPER_SNAKE_CASE (e.g., MAX_THREAD_COUNT)', 'lowercase_with_underscores', 'C', 'Constants (variables marked static and final) should be written in all uppercase letters. If the name consists of multiple words, they should be separated by an underscore character to ensure readability.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Naming Convention', 'Which of the following is the standard convention for naming methods in Java?', 'They should be nouns in PascalCase.', 'They should be verbs in camelCase, starting with a lowercase letter (e.g., calculateTotal).', 'They should begin with an underscore.', 'They should use UPPER_SNAKE_CASE.', 'B', 'Methods represent actions or behaviors. Therefore, they should be named using verbs in camelCase. The first letter is lowercase, and the first letter of each subsequent concatenated word is capitalized.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Naming Convention', 'Which of the following variable names correctly adheres to Java''s standard naming conventions for instance variables?', 'CustomerFirstName', 'CUSTOMER_FIRST_NAME', 'customerFirstName', 'customer_first_name', 'C', 'Instance variables (and local variables) should follow the camelCase convention, starting with a lowercase letter. ''CustomerFirstName'' is PascalCase (used for classes), ''CUSTOMER_FIRST_NAME'' is for constants, and ''customer_first_name'' is snake_case (common in Python/C, but not standard for Java variables).');
