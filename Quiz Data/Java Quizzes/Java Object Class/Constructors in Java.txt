INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Constructors in Java', 'Which of the following accurately describes the return type of a Java constructor?', 'It must return an object of the class type.', 'It implicitly returns ''void''.', 'It has no return type whatsoever, not even ''void''.', 'It returns an integer representing the memory address.', 'C', 'Unlike standard methods, a constructor in Java strictly has no return type in its declaration. If you add a return type (even ''void''), the compiler treats it as a standard method that just happens to share the class name.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Constructors in Java', 'When does the Java compiler automatically generate and insert a default no-argument constructor into a class?', 'Always, regardless of other constructors.', 'Only if the class contains absolutely no explicit constructor declarations.', 'Only when the class is declared as ''public''.', 'Only if the class extends ''Object'' directly.', 'B', 'The compiler provides a default no-argument constructor (which initializes variables to their default values and calls ''super()'') strictly if, and only if, the developer has not explicitly defined any constructors in the class.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Constructors in Java', 'Which of the following modifiers is strictly illegal when declaring a constructor in Java?', 'private', 'protected', 'public', 'static', 'D', 'Constructors cannot be ''static'', ''abstract'', ''final'', or ''synchronized''. They are intrinsically tied to object instantiation, while ''static'' belongs to the class itself, making the two concepts mutually exclusive.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Constructors in Java', 'What is the strict syntactical rule when using the ''this()'' keyword to invoke another overloaded constructor within the same class?', 'It must be the very last statement in the constructor block.', 'It can be placed anywhere, as long as it is within a constructor.', 'It must be the absolute first executable statement within the calling constructor''s body.', 'It must be placed inside a static initialization block.', 'C', 'To prevent an object from being partially initialized or initialized multiple times, Java mandates that a constructor call using ''this()'' (or ''super()'') must be the first statement in the constructor.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Constructors in Java', 'What is the primary architectural use case for explicitly declaring a constructor as ''private''?', 'To prevent the JVM garbage collector from destroying the object.', 'To restrict instantiation to within the class itself, often used for the Singleton design pattern or utility classes containing only static methods.', 'To allow only subclasses to instantiate the class.', 'To force the compiler to use the default constructor.', 'B', 'A ''private'' constructor prevents external classes from using the ''new'' keyword to create instances of the class. This is the foundational technique for implementing the Singleton pattern or preventing instantiation of static-only utility classes (like ''java.lang.Math'').');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Constructors in Java', 'Does Java automatically provide a default copy constructor for objects, similar to C++?', 'Yes, every class inherits a default copy constructor from ''java.lang.Object''.', 'Yes, but only if the class implements the ''Serializable'' interface.', 'No, Java does not provide a default copy constructor; developers must explicitly write one if needed.', 'No, copy constructors are completely illegal in Java.', 'C', 'Java does not automatically generate copy constructors (e.g., ''public MyClass(MyClass original)''). If you want to create a new object as a copy of an existing one, you must explicitly define the copy constructor yourself or use the ''Cloneable'' interface.');
