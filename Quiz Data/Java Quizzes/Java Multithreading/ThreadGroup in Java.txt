INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('ThreadGroup in Java', 'What is the primary architectural purpose of the ''java.lang.ThreadGroup'' class?', 'To combine multiple threads into a single CPU process.', 'To provide a hierarchical, tree-like structure that groups multiple threads and sub-groups together, allowing bulk operations (like interrupting or querying) to be performed on all of them simultaneously.', 'To automatically synchronize all threads within the group.', 'To prevent threads from accessing shared memory.', 'B', 'A ''ThreadGroup'' allows developers to organize threads into functional clusters. Instead of manually iterating through an array of threads to call ''interrupt()'', you can simply call ''threadGroup.interrupt()'', which cascades the interruption down the entire hierarchy.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('ThreadGroup in Java', 'Every Java thread belongs to a ''ThreadGroup''. What is the implicit, default ''ThreadGroup'' assigned to the main application thread executing ''public static void main''?', 'The "system" group.', 'The "main" group, which is a child of the root "system" group.', 'The "root" group.', 'It has no group until explicitly assigned.', 'B', 'The JVM sets up a hierarchical tree upon launch. At the root is the "system" group (handling JVM internal threads like the Garbage Collector). Below that is the "main" group, which automatically houses the main application thread.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('ThreadGroup in Java', 'If you instantiate a new thread via ''new Thread(myRunnable)'' without explicitly passing a ''ThreadGroup'' to the constructor, which group does it automatically join?', 'It joins a newly created group with a random name.', 'It joins the root "system" group.', 'It automatically joins the exact same ThreadGroup as the parent thread that instantiated it.', 'It joins a generic "default" group.', 'C', 'If no group is specified, a new thread inherits the ''ThreadGroup'' of the thread that created it. If the main thread creates a worker thread, that worker thread is automatically placed inside the "main" ThreadGroup.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('ThreadGroup in Java', 'Which method is used to retrieve a conservative estimate of the total number of active, executing threads currently residing within a specific ''ThreadGroup'' and all of its descendant sub-groups?', 'group.size()', 'group.activeCount()', 'group.threadCount()', 'group.count()', 'B', 'The ''activeCount()'' method recursively traverses the current group and all nested sub-groups to estimate the number of live threads. It is an estimate because threads can dynamically start or die while the method is actively calculating the sum.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('ThreadGroup in Java', 'Why is the explicit use of the ''ThreadGroup'' class considered largely obsolete and strongly discouraged in modern Java enterprise engineering?', 'Because it restricts threads to a single CPU core.', 'Because it causes severe memory leaks.', 'Because its API is fundamentally flawed (e.g., the dangerous ''suspend()'' and ''stop()'' methods), and its intended role of managing collections of threads has been vastly superseded by the much safer and more robust ''java.util.concurrent.ExecutorService'' Thread Pool framework.', 'Because it is not supported in Java 8 and above.', 'C', 'The ''ThreadGroup'' class is a relic from Java 1.0. Its bulk control methods are either deprecated or unsafe. Modern concurrency architecture strictly dictates the use of Thread Pools (Executors) to manage, group, and control the lifecycle of concurrent tasks.');
