INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Daemon Thread in Java', 'What is the primary defining characteristic of a Daemon Thread in Java?', 'It is a high-priority thread that preempts all other execution.', 'It is a thread designed exclusively for network I/O operations.', 'It is a low-priority background service thread that does not prevent the JVM from exiting when all user (non-daemon) threads have finished execution.', 'It is a thread that cannot be killed or interrupted.', 'C', 'Daemon threads (like the Garbage Collector) provide background support tasks. The JVM lifecycle dictates that once all standard user threads terminate, the JVM immediately shuts down, abruptly terminating any remaining daemon threads without waiting for them to finish.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Daemon Thread in Java', 'Which method is used to designate a Java thread as a daemon thread?', 'thread.makeDaemon()', 'thread.setDaemon(true)', 'thread.setPriority(Thread.MIN_PRIORITY)', 'Thread.daemonize(thread)', 'B', 'The ''setDaemon(boolean on)'' method in the ''Thread'' class is used to mark a thread as either a daemon thread or a user thread. Passing ''true'' designates it as a daemon.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Daemon Thread in Java', 'At what specific point in a thread''s lifecycle MUST you invoke the ''setDaemon(true)'' method?', 'Any time before the thread enters the TERMINATED state.', 'Immediately after calling ''thread.start()''.', 'Strictly before invoking the ''thread.start()'' method.', 'Inside the thread''s ''run()'' method.', 'C', 'You can only change the daemon status of a thread before it has been started. Attempting to call ''setDaemon(true)'' on a thread that is already running or alive will immediately trigger an ''IllegalThreadStateException''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Daemon Thread in Java', 'If a Daemon Thread spawns a new child thread during its execution, what is the default status of that newly created child thread?', 'It defaults to a User Thread.', 'It defaults to a Daemon Thread.', 'It inherits the default priority of the main thread but becomes a User Thread.', 'It throws a runtime exception because Daemon threads cannot spawn children.', 'B', 'In Java, a newly created thread inherently inherits the daemon status of its parent thread. Therefore, any thread created inside a Daemon Thread''s ''run()'' method will automatically be a Daemon Thread itself unless explicitly changed before starting.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Daemon Thread in Java', 'Why is it considered a severe architectural anti-pattern to perform critical I/O operations (like writing to a database or saving a file) inside a Daemon Thread?', 'Because Daemon threads cannot access the java.io package.', 'Because Daemon threads execute too slowly to handle I/O efficiently.', 'Because the JVM will abruptly terminate Daemon threads the moment the last user thread dies, potentially abandoning the I/O operation mid-write and severely corrupting the data.', 'Because it causes deadlocks with the Garbage Collector.', 'C', 'Daemon threads have no guaranteed execution completion. If the main application finishes, the JVM halts without executing ''finally'' blocks or allowing Daemon threads to cleanly close streams, leading to data corruption if they were performing I/O.');
