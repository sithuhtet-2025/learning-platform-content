INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Thread Priority in Java', 'What are the valid integer boundaries for setting a thread''s execution priority in Java?', '0 to 100', '-1 to 1', '1 to 10', '1 to 5', 'C', 'The java.lang.Thread class defines priority using an integer scale from 1 to 10. The constants are Thread.MIN_PRIORITY (1), Thread.NORM_PRIORITY (5), and Thread.MAX_PRIORITY (10).');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Thread Priority in Java', 'If a developer creates a new thread (e.g., ''Thread child = new Thread();'') inside the ''main'' method, what priority is automatically assigned to the ''child'' thread?', 'Thread.MIN_PRIORITY (1)', 'Thread.NORM_PRIORITY (5)', 'Thread.MAX_PRIORITY (10)', '0', 'B', 'By default, a child thread inherits the exact priority of its parent thread at the moment of creation. Since the JVM assigns the ''main'' thread a default priority of 5 (NORM_PRIORITY), the child thread inherits this value.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Thread Priority in Java', 'Does assigning ''Thread.MAX_PRIORITY'' (10) to a thread absolutely guarantee that it will execute and complete before a thread with ''Thread.MIN_PRIORITY'' (1)?', 'Yes, the JVM enforces strict chronological execution based on priority.', 'Yes, but only on multi-core processors.', 'No, Java thread priorities are merely advisory hints to the underlying Operating System''s thread scheduler. The OS retains absolute authority and may choose to ignore these hints entirely to prevent thread starvation.', 'No, but it guarantees it will get twice as much CPU time.', 'C', 'Java does not dictate thread scheduling. It maps its 1-10 priority scale to the native OS priorities. Depending on the OS architecture (Windows vs. Linux), the scheduler might execute a lower priority thread first, making priority-dependent logic highly unpredictable and an architectural anti-pattern.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Thread Priority in Java', 'What exception is thrown if you attempt to execute ''thread.setPriority(15)''?', 'IllegalThreadStateException', 'IllegalArgumentException', 'SecurityException', 'NullPointerException', 'B', 'The ''setPriority(int newPriority)'' method strictly validates the input parameter. If the requested priority is less than MIN_PRIORITY (1) or greater than MAX_PRIORITY (10), the JVM immediately throws an ''IllegalArgumentException''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Thread Priority in Java', 'If a thread belongs to a specific ''ThreadGroup'', how does the group''s maximum priority affect the individual thread''s priority?', 'It has no effect; individual threads override group settings.', 'The thread''s priority cannot exceed the maximum priority defined by its enclosing ThreadGroup. Attempting to set it higher will silently cap it at the group''s maximum.', 'The thread is terminated if its priority differs from the group.', 'The group automatically averages the priorities of all its threads.', 'B', 'A ''ThreadGroup'' imposes a structural ceiling on thread priority. If a group has a max priority of 7, and you call ''thread.setPriority(10)'' on a thread within that group, the JVM will silently restrict the thread''s priority to 7 without throwing an exception.');
