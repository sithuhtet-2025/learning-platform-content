INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Shutdown Hook', 'What exactly is a Shutdown Hook in the Java runtime architecture?', 'A method that forces the operating system to shut down.', 'A specialized, un-started Thread that the JVM guarantees to execute concurrently with other hooks during its final termination sequence, used primarily for graceful resource cleanup.', 'An exception thrown when the JVM runs out of memory.', 'A background daemon thread that monitors for infinite loops.', 'B', 'A shutdown hook is an initialized but un-started Thread. When the JVM begins its shutdown process (either normally or via user interrupt like Ctrl+C), it starts all registered shutdown hooks concurrently to allow the application to cleanly close files, database connections, or network sockets before the process dies.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Shutdown Hook', 'Which method is used to register a new Shutdown Hook with the Java Virtual Machine?', 'System.setShutdownHook(Thread hook)', 'Thread.registerHook(Runnable r)', 'Runtime.getRuntime().addShutdownHook(Thread hook)', 'JVM.addHook(Thread hook)', 'C', 'Because shutdown hooks interact directly with the JVM''s execution environment, they must be registered via the singleton ''Runtime'' class using ''Runtime.getRuntime().addShutdownHook(Thread hook)''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Shutdown Hook', 'Under which of the following extreme conditions will a registered Java Shutdown Hook definitively FAIL to execute?', 'When the user presses Ctrl+C in the console.', 'When the application calls ''System.exit(0)''.', 'When the JVM process is forcibly killed by the operating system (e.g., SIGKILL on Linux or terminating the process tree in Task Manager), or if the JVM crashes due to a severe internal error.', 'When an OutOfMemoryError is thrown.', 'C', 'Shutdown hooks are designed to handle normal and graceful terminations. If the OS abruptly kills the JVM process (SIGKILL) or if the JVM suffers a native crash or hardware power loss, there is no time or capacity to execute the hooks.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Shutdown Hook', 'What happens if you attempt to register a Thread as a Shutdown Hook that has already been started (e.g., ''hook.start(); Runtime.getRuntime().addShutdownHook(hook);'')?', 'The JVM registers it successfully and waits for it to finish.', 'The JVM immediately throws an ''IllegalArgumentException''.', 'The JVM throws an ''IllegalThreadStateException''.', 'The thread is stopped and registered.', 'B', 'The JVM strictly requires that the Thread object passed to ''addShutdownHook()'' is in the ''NEW'' state (initialized but not started). If the thread is already running or has terminated, the JVM rejects it by throwing an ''IllegalArgumentException''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Shutdown Hook', 'What severe architectural anti-pattern should be strictly avoided inside the ''run()'' method of a Shutdown Hook?', 'Writing data to a log file.', 'Closing a database connection.', 'Invoking ''Runtime.getRuntime().halt()'' or ''System.exit()'', or creating intentional deadlocks/infinite loops.', 'Interrupting other user threads.', 'C', 'Calling ''System.exit()'' inside a shutdown hook will cause the JVM to hang or block indefinitely because the shutdown sequence is already in progress. Similarly, infinite loops or complex deadlocking logic inside a hook will prevent the JVM from ever successfully terminating, requiring an OS-level force kill.');
