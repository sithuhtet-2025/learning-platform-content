INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Thread Pool in Java', 'What is the primary architectural justification for implementing a Thread Pool via ''ExecutorService'' rather than manually instantiating new ''Thread'' objects for every task?', 'To bypass the Java Security Manager.', 'Thread creation and destruction are highly expensive OS-level operations. A Thread Pool recycles a fixed number of worker threads, drastically reducing overhead and preventing OutOfMemoryErrors caused by uncontrolled thread spawning.', 'To force tasks to execute sequentially.', 'To automatically convert Runnable tasks into Callable tasks.', 'B', 'If a server receives 10,000 requests, creating 10,000 new threads will exhaust system memory and crash the OS via context-switching overhead. A Thread Pool restricts the system to, for example, 50 threads, which pull the 10,000 tasks from a queue one by one.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Thread Pool in Java', 'Which factory class in the ''java.util.concurrent'' package provides the standard static methods to easily create various configurations of thread pools?', 'ThreadPoolManager', 'Executors', 'ThreadFactory', 'PoolBuilder', 'B', 'The ''Executors'' utility class provides factory methods like ''Executors.newFixedThreadPool(n)'', ''Executors.newCachedThreadPool()'', and ''Executors.newSingleThreadExecutor()'' to instantiate heavily optimized ''ExecutorService'' implementations with a single line of code.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Thread Pool in Java', 'If you submit a task to an ''ExecutorService'' that needs to return a computed result or throw a checked exception, which interface must your task implement instead of ''Runnable''?', 'Returnable', 'Task', 'Callable', 'Future', 'C', 'The ''Runnable'' interface''s ''run()'' method has a ''void'' return type and cannot throw checked exceptions. The ''Callable<V>'' interface''s ''call()'' method is designed specifically to return a generic value of type ''V'' and is permitted to throw checked exceptions.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Thread Pool in Java', 'When you submit a ''Callable'' task to an ''ExecutorService'', what object does the pool immediately return to the calling thread?', 'The computed result itself.', 'A ''Future'' object, which acts as a proxy or placeholder that the calling thread can later query (using ''future.get()'') to retrieve the actual result once the background task finishes.', 'A new Thread object.', 'A boolean indicating success.', 'B', 'Because the task runs asynchronously, the pool cannot return the result immediately. It returns a ''Future'' object. Calling ''get()'' on this Future will block the main thread until the worker thread has finished processing the ''Callable'' and populated the result.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Thread Pool in Java', 'What is the exact functional difference between calling ''shutdown()'' and ''shutdownNow()'' on an ''ExecutorService''?', 'There is no difference; they are aliases.', '''shutdown()'' instantly kills all threads. ''shutdownNow()'' waits indefinitely.', '''shutdown()'' stops accepting new tasks but allows all currently queued and actively executing tasks to finish. ''shutdownNow()'' attempts to forcibly interrupt active threads and halts the processing of queued tasks immediately, returning a list of the tasks that were never started.', '''shutdownNow()'' throws an exception, ''shutdown()'' does not.', 'C', 'Proper resource management requires cleanly shutting down a pool. ''shutdown()'' provides a graceful exit, guaranteeing queued work is completed. ''shutdownNow()'' is aggressive, issuing ''interrupt()'' signals to running threads and abandoning the queue.');
