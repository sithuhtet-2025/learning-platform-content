INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Multithreading in Java', 'What is the primary architectural advantage of Multithreading over Multiprocessing in a Java environment?', 'Multithreading completely eliminates the possibility of deadlocks.', 'Threads within the same process share the exact same Heap memory and resources, making communication faster and context switching significantly lighter than heavy OS-level process context switching.', 'Multithreading allows Java to execute C++ code natively.', 'Multithreading bypasses the JVM and runs directly on the CPU cache.', 'B', 'Unlike distinct OS processes which require isolated, heavy memory allocations, threads are "lightweight processes." They share the parent process''s memory space (Heap), making the creation, destruction, and switching between threads significantly faster and less resource-intensive.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Multithreading in Java', 'In the context of multithreading on a single-core CPU, what is "Context Switching"?', 'The process of translating Java bytecode to machine code.', 'The physical switching of a memory module.', 'The process where the OS saves the exact state (registers, program counter) of the currently running thread and loads the saved state of the next thread in the queue, giving the illusion of simultaneous execution.', 'The process of a thread terminating itself.', 'C', 'On a single core, true parallelism is impossible. The CPU achieves concurrency by rapidly switching between threads. Context switching is the heavy OS-level operation of saving where one thread left off and restoring where another thread needs to resume.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Multithreading in Java', 'What are the boundaries of Thread Priority levels in Java, as defined by the ''Thread'' class constants?', 'From 0 to 100.', 'From 1 to 10, with 5 being the default (NORM_PRIORITY).', 'From -1 to 1.', 'From A to Z.', 'B', 'Java defines priority using integers. ''Thread.MIN_PRIORITY'' is 1, ''Thread.MAX_PRIORITY'' is 10, and ''Thread.NORM_PRIORITY'' is 5. Note that thread priorities are merely hints to the OS thread scheduler, which may choose to ignore them entirely.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Multithreading in Java', 'What is the absolute, most critical risk introduced when multiple threads read and write to shared instance variables simultaneously without proper synchronization?', 'StackOverflowError', 'Memory Leaks', 'Race Conditions, leading to lost updates and corrupted data states.', 'The JVM will automatically convert the variables to local scope.', 'C', 'A race condition occurs when two or more threads attempt to modify shared data at the exact same time. Because operations like ''x++'' are not atomic (they require a read, increment, and write), thread interleaving can cause one thread to overwrite another''s calculation.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Multithreading in Java', 'What is the strict difference between "Concurrency" and "Parallelism" in Java multithreading?', 'They are exact synonyms in computer science.', 'Concurrency is executing multiple threads simultaneously on multiple CPU cores. Parallelism is managing multiple threads on a single core via time-slicing.', 'Concurrency is about dealing with multiple tasks at once (managing interleaving execution), whereas Parallelism is about physically executing multiple tasks at the exact same instant (requires multi-core hardware).', 'Concurrency applies to software, Parallelism applies to networks.', 'C', 'Concurrency is a structural program design (handling multiple tasks making progress over overlapping time periods). Parallelism is a hardware-dependent execution state (literally doing two calculations at the exact same picosecond on different physical cores).');
