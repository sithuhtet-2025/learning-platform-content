INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('What if We Call run() Method Directly Instead of start() in Java', 'What is the fundamental architectural outcome of invoking ''thread.run()'' directly instead of ''thread.start()''?', 'The JVM throws an IllegalThreadStateException.', 'The ''run()'' method executes perfectly normally, but it executes synchronously within the current, calling thread''s execution stack rather than spawning a new, independent parallel thread.', 'A new thread is created, but it executes in the background without priority.', 'The JVM completely ignores the method call.', 'B', 'The ''run()'' method is just a standard Java instance method. Calling it directly simply executes its instructions sequentially on the current thread (usually the ''main'' thread), entirely defeating the purpose of multithreading by failing to create a new call stack.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('What if We Call run() Method Directly Instead of start() in Java', 'Which internal JNI (Java Native Interface) mechanism is completely bypassed when ''run()'' is called directly?', 'The Garbage Collector.', 'The ClassLoader.', 'The native OS-level thread provisioning and registration hooks required to physically allocate a separate execution path and memory stack for the new thread.', 'The Security Manager.', 'C', 'The ''start()'' method contains native code that interfaces directly with the host Operating System to provision a new physical thread. Calling ''run()'' directly bypasses this critical OS interaction entirely.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('What if We Call run() Method Directly Instead of start() in Java', 'If you execute ''Thread t1 = new Thread(myTask); t1.run();'' inside the main method, what will ''Thread.currentThread().getName()'' return if called from inside the task''s logic?', 'Thread-0', 't1', 'main', 'null', 'C', 'Because ''start()'' was never called, no new thread was ever spawned. The instructions inside the task are executed directly by the thread that made the method call, which in this scenario is the primary ''main'' thread.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('What if We Call run() Method Directly Instead of start() in Java', 'Can you legally call ''thread.start()'' after having already called ''thread.run()'' directly?', 'Yes, the thread will finally be spawned and execute the task concurrently.', 'No, calling ''run()'' consumes the thread object, throwing an IllegalThreadStateException if ''start()'' is called later.', 'Yes, but the thread will skip the ''run()'' method since it was already executed.', 'Yes, because calling ''run()'' leaves the thread in the NEW state, meaning ''start()'' can still be legally invoked to spawn the parallel execution.', 'D', 'Calling ''run()'' directly is just a standard method call. It does not alter the JVM lifecycle state of the ''Thread'' object. The object remains in the ''NEW'' state, meaning a subsequent call to ''start()'' is perfectly legal and will successfully spawn the new thread.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('What if We Call run() Method Directly Instead of start() in Java', 'What happens to the application''s control flow if the direct ''run()'' method call contains a massive, time-consuming loop?', 'The JVM automatically moves it to a background thread to prevent freezing.', 'The calling thread (e.g., the main UI thread) becomes completely blocked and unresponsive until the loop finishes, because the execution is strictly synchronous.', 'The loop executes twice as fast due to lack of context switching.', 'It throws a TimeoutException.', 'B', 'Since direct invocation executes synchronously on the calling thread, it behaves like any other blocking method call. The calling thread cannot progress to its next line of code until the ''run()'' method completely finishes returning.');
