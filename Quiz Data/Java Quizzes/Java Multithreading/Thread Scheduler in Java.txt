INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Thread Scheduler in Java', 'What is the exact role of the Thread Scheduler in the context of a Java application?', 'It is a pure JVM component that translates bytecode to machine code.', 'It is the mechanism (typically part of the underlying Operating System) that dictates which thread in the RUNNABLE state is granted physical CPU time to execute, and for how long.', 'It allocates memory on the Heap for new threads.', 'It schedules tasks to run at specific dates and times.', 'B', 'The Thread Scheduler decides the execution order. When multiple threads are in the RUNNABLE state (ready to execute), the scheduler selects one to move to the RUNNING state based on complex OS-level algorithms.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Thread Scheduler in Java', 'Which primary scheduling algorithm is utilized by modern Operating Systems running the JVM to manage threads of equal priority?', 'First-Come, First-Served (FCFS) non-preemptive scheduling.', 'Preemptive Time-Slicing, where each thread is granted a small quantum of CPU time before being forcefully paused to allow the next thread to execute.', 'Shortest Job First (SJF).', 'Randomized Execution.', 'B', 'Modern JVMs rely on the OS''s preemptive time-slicing. The scheduler rapidly cycles through all RUNNABLE threads of equal priority, giving each a tiny fraction of a second (a time slice) on the CPU. This creates the illusion of simultaneous parallel execution on a single core.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Thread Scheduler in Java', 'Under strict Preemptive Scheduling, what occurs if a thread with priority 10 becomes RUNNABLE while a thread with priority 5 is currently executing on the CPU?', 'The priority 10 thread waits until the priority 5 thread yields.', 'The Thread Scheduler preempts (suspends) the priority 5 thread immediately and allocates the CPU to the priority 10 thread.', 'Both threads execute simultaneously on the same core.', 'The JVM crashes due to a priority collision.', 'B', 'Preemptive scheduling ensures that the highest-priority RUNNABLE thread always gets the CPU. If a higher-priority thread wakes up (e.g., from an I/O block), the scheduler forcefully interrupts the lower-priority thread currently running and swaps them.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Thread Scheduler in Java', 'What explicit signal does the static ''Thread.yield()'' method send to the Thread Scheduler?', 'It forces the current thread to terminate.', 'It instructs the current thread to temporarily pause and release the CPU, signaling to the scheduler that it is willing to let other RUNNABLE threads of the exact same (or higher) priority execute.', 'It puts the current thread to sleep for 1 millisecond.', 'It increases the priority of the current thread.', 'B', 'Calling ''yield()'' is a cooperative gesture. It moves the thread from the RUNNING state back to the RUNNABLE queue. However, if there are no other threads of equal or higher priority waiting, the scheduler might immediately re-select the yielding thread to continue running.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Thread Scheduler in Java', 'Can a Java programmer write deterministic code that absolutely guarantees a specific execution order among multiple RUNNABLE threads?', 'Yes, by assigning unique priorities from 1 to 10.', 'Yes, by using the Thread.setExecutionOrder() method.', 'No, because the underlying Thread Scheduler is inherently platform-dependent and its precise algorithmic decisions cannot be perfectly predicted or strictly controlled by the JVM.', 'Yes, by using daemon threads.', 'C', 'Relying on thread scheduling for program correctness is a critical anti-pattern. Because the JVM maps threads to native OS threads, execution order varies wildly between Windows, macOS, and Linux. Developers must use explicit synchronization constructs (like ''CountDownLatch'' or ''join()'') to guarantee execution sequences.');
