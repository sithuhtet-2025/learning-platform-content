INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Thread Life Cycle in Java', 'According to the ''java.lang.Thread.State'' enum, what are the exactly defined lifecycle states a Java thread can inhabit?', 'START, RUN, WAIT, STOP', 'NEW, RUNNABLE, BLOCKED, WAITING, TIMED_WAITING, TERMINATED', 'CREATED, EXECUTING, PAUSED, KILLED', 'INIT, READY, RUNNING, DEAD', 'B', 'Modern Java rigidly defines six specific thread states. A thread can be queried for its current state at any time using the ''thread.getState()'' method, which returns one of these six enum values.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Thread Life Cycle in Java', 'When a thread is actively executing its ''run()'' method on the CPU, what state does the JVM consider it to be in?', 'EXECUTING', 'RUNNING', 'RUNNABLE', 'ACTIVE', 'C', 'Unlike some OS models, Java does not distinguish between a thread that is "ready to run but waiting for CPU time" and a thread that is "currently executing on the CPU". Both states are officially categorized simply as ''RUNNABLE'' by the JVM.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Thread Life Cycle in Java', 'What specific architectural event strictly causes a thread to transition into the ''BLOCKED'' state?', 'Calling ''Thread.sleep()''.', 'Calling ''Object.wait()''.', 'Attempting to enter a ''synchronized'' block or method while the required object monitor lock is currently held by another thread.', 'Waiting for network I/O.', 'C', 'The ''BLOCKED'' state is exclusively reserved for monitor lock contention. A thread enters this state when it reaches a ''synchronized'' block and is denied entry because another thread has not yet released the lock for that specific object.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Thread Life Cycle in Java', 'What is the fundamental difference between the ''WAITING'' state and the ''TIMED_WAITING'' state?', '''WAITING'' is for I/O operations, ''TIMED_WAITING'' is for CPU operations.', 'A thread in ''WAITING'' relies on another thread to explicitly wake it up (e.g., via ''notify()''). A thread in ''TIMED_WAITING'' will wake up automatically when a specified millisecond timeout expires, or if woken up explicitly.', '''WAITING'' throws an exception, ''TIMED_WAITING'' does not.', 'There is no difference.', 'B', 'Calling ''Object.wait()'' or ''Thread.join()'' without arguments puts a thread into indefinite ''WAITING''. Calling them with millisecond arguments (or calling ''Thread.sleep(ms)'') puts the thread into ''TIMED_WAITING'', creating a guaranteed maximum pause duration.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Thread Life Cycle in Java', 'Once a thread''s ''run()'' method completes execution or throws an unhandled exception, it enters the ''TERMINATED'' state. Can it be brought back to the ''RUNNABLE'' state?', 'Yes, by calling ''start()'' again.', 'Yes, by calling ''notify()''.', 'No, the Java Thread lifecycle is strictly one-way. A terminated thread is dead and its OS-level resources are reclaimed. Calling ''start()'' again will throw an ''IllegalThreadStateException''.', 'Yes, but only if it was a Daemon thread.', 'C', 'The termination of a thread is absolute. Once the state machine reaches ''TERMINATED'', no method exists to resurrect that specific ''Thread'' object instance. You must instantiate a completely new ''Thread'' object to execute the task again.');
