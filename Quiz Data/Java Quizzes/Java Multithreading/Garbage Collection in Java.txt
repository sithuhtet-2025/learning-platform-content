INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Garbage Collection in Java', 'What is the primary architectural objective of the Java Garbage Collector (GC)?', 'To automatically defragment the hard drive during application downtime.', 'To dynamically translate Java bytecode into native machine code.', 'To autonomously identify and destroy objects residing in the Heap memory that are no longer reachable by any active thread, thereby reclaiming memory resources.', 'To monitor and terminate threads that are stuck in a deadlock.', 'C', 'The Garbage Collector acts as an automatic memory manager. It scans the Heap, identifies objects that have lost all reference pointers from the application (making them "unreachable"), and safely deletes them to prevent OutOfMemoryErrors.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Garbage Collection in Java', 'How can a Java developer forcefully and deterministically instruct the JVM to execute the Garbage Collector immediately?', 'By invoking ''Runtime.getRuntime().gc()''.', 'By invoking ''System.gc()''.', 'By calling the ''finalize()'' method directly on an object.', 'It is architecturally impossible to force the JVM to run the Garbage Collector at a specific, exact moment.', 'D', 'Methods like ''System.gc()'' and ''Runtime.getRuntime().gc()'' only act as a request or suggestion to the JVM. The JVM''s internal execution engine retains absolute autonomy over when to actually pause the application and perform a collection cycle based on memory pressure.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Garbage Collection in Java', 'What is the "Generational Hypothesis" that governs the architecture of modern Java Garbage Collectors?', 'The assumption that larger objects live longer than smaller objects.', 'The statistical observation that the vast majority of newly created objects become unreachable (die) very quickly, while objects that survive a few collections tend to live for a very long time.', 'The rule that threads from older generations have priority over newer ones.', 'The theory that garbage collection should only happen when the CPU generation is idle.', 'B', 'Because most objects (like local variables in a method) die young, the JVM divides the Heap into "Young" and "Old" (Tenured) generations. The GC frequently and efficiently cleans the Young generation (Minor GC), and only rarely cleans the Old generation (Major GC), vastly improving application throughput.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Garbage Collection in Java', 'Which of the following actions will definitively make an object eligible for Garbage Collection?', 'Setting all of its instance variables to zero.', 'Casting it to an Object reference.', 'Reassigning its only reference variable to point to ''null'', or allowing the reference variable to fall out of scope.', 'Calling the ''wait()'' method on it.', 'C', 'An object becomes eligible for garbage collection the moment it becomes completely unreachable. This occurs when all variables pointing to it are reassigned to something else, set to ''null'', or when the method containing the local reference variables finishes executing and pops off the stack.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Garbage Collection in Java', 'What is an "Island of Isolation" in the context of Java Garbage Collection?', 'A dedicated section of the Heap reserved for massive objects.', 'A thread that has been blocked indefinitely.', 'A scenario where two or more objects reference each other cyclically, but no active thread in the application holds a reference to any of them.', 'A security protocol that isolates compromised objects.', 'C', 'If Object A points to Object B, and Object B points to Object A, but the main application no longer holds a reference to either A or B, they form an Island of Isolation. Modern tracing garbage collectors easily identify that the entire island is unreachable from the root and collect them all.');
