INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java - Performing a Single Task by Multiple Threads', 'What is the most architecturally efficient way to have three separate threads execute the exact same task logic while sharing the same underlying state?', 'Create three separate classes that extend the Thread class, and copy-paste the task logic into each run() method.', 'Instantiate a single object from a class that implements the Runnable interface, and pass that exact same object reference into the constructors of three distinct Thread objects.', 'Call the run() method three times sequentially from the main thread.', 'Use the Thread.clone() method to duplicate a running thread.', 'B', 'By instantiating a single ''Runnable'' object and passing it to multiple ''Thread'' instances (e.g., ''Thread t1 = new Thread(myRunnable); Thread t2 = new Thread(myRunnable);''), all threads will execute the exact same ''run()'' method and share the exact same instance variables present in that ''Runnable'' object.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java - Performing a Single Task by Multiple Threads', 'If multiple threads are executing a single, shared ''Runnable'' instance, what severe architectural risk must be immediately addressed if the ''run()'' method modifies instance variables?', 'StackOverflowError', 'Deadlock', 'Race Conditions leading to data corruption and inconsistent state.', 'Thread Starvation', 'C', 'Because the threads share the exact same ''Runnable'' object on the heap, any instance variables within that ''Runnable'' act as shared memory. If multiple threads attempt to read and write to those variables simultaneously without synchronization, it will inevitably cause race conditions and data corruption.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java - Performing a Single Task by Multiple Threads', 'Why is achieving shared state between multiple threads significantly more difficult if you create threads by extending the ''Thread'' class rather than implementing ''Runnable''?', 'Because extended Thread classes cannot have instance variables.', 'Because every time you instantiate a new subclass of Thread, it possesses its own completely isolated copy of all instance variables, preventing native state sharing unless static variables are used.', 'Because the JVM strictly isolates Thread subclasses in memory.', 'Because extending the Thread class makes the objects immutable.', 'B', 'When you extend ''Thread'', each thread is a physically distinct object with its own instance variables (e.g., ''new MyThread(), new MyThread()''). To share state, you are forced to use ''static'' variables or pass shared object references manually. A shared ''Runnable'' inherently shares its instance variables.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java - Performing a Single Task by Multiple Threads', 'If a single ''Runnable'' task utilizes local variables declared entirely within the scope of its ''run()'' method, are those variables shared among the multiple threads executing it?', 'Yes, because they belong to the same Runnable instance.', 'No, local variables are stored on each thread''s independent execution stack. Every thread gets its own completely isolated copy of the local variables.', 'Yes, but only if they are declared as volatile.', 'No, the compiler throws an error preventing local variables in shared Runnables.', 'B', 'Java memory architecture guarantees thread safety for local variables. When a thread executes a method, it pushes a new frame onto its private Thread Stack. Therefore, even if 10 threads execute the exact same ''run()'' method, they each have 10 separate, isolated copies of any local variables defined inside that method.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java - Performing a Single Task by Multiple Threads', 'What is the modern, scalable JDK architecture designed to handle a scenario where you have a massive volume of identical tasks that need to be processed concurrently, rather than manually instantiating raw Thread objects?', 'java.util.Timer', 'The java.util.concurrent.ExecutorService framework (Thread Pools).', 'java.lang.ThreadGroup', 'The volatile keyword.', 'B', 'Creating thousands of raw ''Thread'' objects will crash the OS. The ''ExecutorService'' establishes a fixed-size Thread Pool. You simply submit your identical ''Runnable'' tasks to the pool, and the finite worker threads will continuously pull and execute tasks from the queue in a highly optimized manner.');
