INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('How to Create a Thread in Java', 'What are the two primary, foundational mechanisms for creating a new execution thread in Java?', 'Using the ''new Thread()'' keyword and using the ''java.util.concurrent'' package.', 'Extending the ''java.lang.Thread'' class OR implementing the ''java.lang.Runnable'' interface.', 'Implementing the ''Callable'' interface OR extending the ''Process'' class.', 'Using the ''ExecutorService'' interface OR the ''ForkJoinPool'' class.', 'B', 'At the lowest architectural level, a Java thread is created either by creating a custom class that extends the standard ''Thread'' class, or by creating a class that implements the ''Runnable'' interface and passing an instance of it into a ''Thread'' constructor.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('How to Create a Thread in Java', 'Why is implementing the ''Runnable'' interface generally considered a vastly superior architectural design compared to extending the ''Thread'' class?', 'Because ''Runnable'' automatically handles thread synchronization.', 'Because implementing ''Runnable'' bypasses the JVM''s thread limit.', 'Because Java strictly forbids multiple class inheritance; extending ''Thread'' consumes the class''s single superclass slot, whereas implementing ''Runnable'' leaves the class free to extend another domain-specific parent class.', 'Because ''Runnable'' executes significantly faster than ''Thread''.', 'C', 'Inheritance should be reserved for true "IS-A" relationships. A task "IS NOT" a thread; it is executed BY a thread. Implementing ''Runnable'' keeps your task logic decoupled from the thread infrastructure and preserves your ability to extend other classes.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('How to Create a Thread in Java', 'If you create a class ''MyTask implements Runnable'', what is the exact, correct sequence of code required to actually start it on a new parallel thread?', 'MyTask task = new MyTask(); task.start();', 'Runnable r = new MyTask(); r.run();', 'MyTask task = new MyTask(); Thread t = new Thread(task); t.start();', 'Thread.start(new MyTask());', 'C', 'Because a ''Runnable'' is just an interface defining a task, it possesses no native multithreading capabilities itself. You must instantiate your ''Runnable'' task, pass it into the constructor of a brand new ''Thread'' object, and then invoke ''start()'' on that ''Thread'' object to instruct the OS to spawn the parallel execution stack.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('How to Create a Thread in Java', 'Which specific method signature must be overridden to define the actual logic that the new thread will execute, regardless of whether you extend ''Thread'' or implement ''Runnable''?', 'public void start()', 'public static void main(String[] args)', 'public void execute()', 'public void run()', 'D', 'Both the ''Thread'' class and the ''Runnable'' interface define the ''public void run()'' method. This method acts as the entry point and the execution body for the new thread, exactly like the ''main()'' method does for the primary application thread.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('How to Create a Thread in Java', 'What occurs architecturally if a developer accidentally invokes ''task.run()'' directly instead of ''thread.start()''?', 'The compiler throws a syntax error.', 'The JVM throws an IllegalThreadStateException.', 'The code executes perfectly normally, but it runs synchronously on the current, calling thread instead of spawning a new, independent call stack, entirely defeating the purpose of multithreading.', 'The program crashes immediately.', 'C', 'Calling ''run()'' is no different from calling any other standard instance method in Java. It simply executes the code sequentially in the current thread. Only the native ''start()'' method contains the OS-level hooks required to actually provision a separate execution thread and stack.');
