INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Joining Threads in Java', 'What is the primary architectural purpose of the ''thread.join()'' method in Java concurrency?', 'To merge the memory spaces of two distinct threads.', 'To force the currently executing thread to pause and wait synchronously until the specific thread on which ''join()'' was called has fully terminated (died).', 'To copy the state of one thread into another.', 'To allow two threads to share the same CPU core simultaneously.', 'B', 'The ''join()'' method is a fundamental synchronization tool used for thread sequencing. If Thread A calls ''ThreadB.join()'', Thread A enters a WAITING state and halts execution until Thread B finishes its task and dies, ensuring Thread A only proceeds when B''s results are ready.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Joining Threads in Java', 'If the main thread calls ''workerThread.join(5000)'', what is the precise behavior of the main thread?', 'It waits exactly 5000 milliseconds, then terminates the workerThread.', 'It waits for the workerThread to die, but will stop waiting and resume its own execution after exactly 5000 milliseconds even if the workerThread is still running.', 'It pauses the workerThread for 5000 milliseconds.', 'It throws a TimeoutException after 5000 milliseconds.', 'B', 'The overloaded ''join(long millis)'' method provides a timeout mechanism. The calling thread will block until either the target thread dies OR the specified timeout period expires—whichever occurs first. Once the timeout is hit, the calling thread resumes execution regardless of the target thread''s status.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Joining Threads in Java', 'Like ''Thread.sleep()'', what mandatory checked exception must be handled when invoking the ''join()'' method?', 'IllegalThreadStateException', 'TimeoutException', 'InterruptedException', 'ExecutionException', 'C', 'Because ''join()'' places the calling thread into a blocking state (WAITING or TIMED_WAITING), it is susceptible to being interrupted by another thread calling ''interrupt()'' on it. Therefore, the compiler enforces handling of the ''InterruptedException''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Joining Threads in Java', 'What happens if you invoke ''workerThread.join()'' on a thread object that has not yet been started (i.e., is still in the NEW state)?', 'The JVM throws an IllegalThreadStateException.', 'The calling thread blocks indefinitely because the target thread will never die.', 'The ''join()'' method simply returns immediately without blocking, because an un-started thread is not considered "alive".', 'The JVM automatically calls ''start()'' on the workerThread.', 'C', 'The ''join()'' method internally checks if the target thread ''isAlive()''. Since a thread in the NEW state (not yet started) is not alive, the ''join()'' call evaluates to false immediately, and the calling thread continues execution without pausing at all.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Joining Threads in Java', 'Internally, how does the ''join()'' method in the ''Thread'' class actually achieve its blocking behavior within the JVM architecture?', 'By using an infinite while-loop checking the thread state (Busy Waiting).', 'By placing a lock on the CPU.', 'By utilizing the ''Object.wait()'' mechanism. It synchronizes on the target thread object itself and calls ''wait()'' in a loop as long as the target thread ''isAlive()''.', 'By using the native ''Thread.sleep()'' in 1-millisecond increments.', 'C', 'If you examine the JDK source code, ''join()'' is implemented using standard Object monitor synchronization. It synchronizes on the target ''Thread'' instance and calls ''wait(0)''. When the target thread finally terminates, the JVM natively calls ''notifyAll()'' on that thread object, waking up the joining thread.');
