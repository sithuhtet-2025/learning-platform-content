INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Can we start a thread twice', 'If you instantiate a ''Thread'' object and call ''thread.start()'', what happens if you attempt to call ''thread.start()'' a second time on the exact same object?', 'The thread is paused and restarted from the beginning.', 'A second, parallel thread is spawned executing the same run() method.', 'The JVM throws an IllegalThreadStateException at runtime.', 'The method is ignored and the thread continues running.', 'C', 'In Java, it is strictly forbidden to start the same thread instance more than once. The ''start()'' method checks the internal state of the thread. If it is not in the ''NEW'' state, it immediately throws an ''IllegalThreadStateException''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Can we start a thread twice', 'Can a thread be restarted by calling ''start()'' again after its ''run()'' method has completely finished executing and the thread has entered the TERMINATED state?', 'Yes, a terminated thread automatically resets its state to NEW.', 'No, once a thread completes execution and dies, its lifecycle is permanently over and it cannot be resurrected.', 'Yes, but only if you call interrupt() first.', 'No, but you can call resume() to wake it up.', 'B', 'The Java thread lifecycle is strictly unidirectional. Once a thread has finished execution and transitioned into the TERMINATED (dead) state, it cannot be transitioned back to NEW or RUNNABLE under any circumstances. A brand new ''Thread'' instance must be created.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Can we start a thread twice', 'What actually happens under the hood when you invoke the ''start()'' method on a new ''Thread'' object?', 'It directly and synchronously calls the run() method on the current thread.', 'It registers the thread with the JVM, allocates a new independent call stack, and instructs the OS thread scheduler to eventually invoke the run() method concurrently.', 'It creates an entirely new process in the operating system.', 'It compiles the thread''s bytecode.', 'B', 'The ''start()'' method is a native JNI call that interfaces with the underlying OS. It provisions a completely new execution stack and registers the thread with the scheduler. Calling ''run()'' directly instead of ''start()'' would just execute the code synchronously on the existing main thread, defeating multithreading.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Can we start a thread twice', 'If you want to execute the exact same task multiple times concurrently, what is the correct architectural approach instead of trying to restart the same thread?', 'Use a while(true) loop inside the thread''s run method.', 'Instantiate multiple distinct Thread objects passing them the exact same Runnable instance, and call start() on each new Thread object.', 'Call the run() method multiple times manually.', 'Use the Thread.restart() method.', 'B', 'Because ''Thread'' objects cannot be reused, the standard pattern is to encapsulate the business logic within a single ''Runnable'' object. You can then pass that shared ''Runnable'' to as many newly instantiated ''Thread'' objects as you need, safely calling ''start()'' on each.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Can we start a thread twice', 'Which of the following is considered the modern best practice for managing and reusing threads for multiple tasks, rather than manually creating and starting new thread objects each time?', 'Manually resetting the thread''s internal state using Java Reflection.', 'Using a Thread Pool via the java.util.concurrent.ExecutorService architecture to recycle worker threads indefinitely.', 'Utilizing the deprecated Thread.suspend() and Thread.resume() methods.', 'Storing threads in a standard ArrayList and looping through them.', 'B', 'Thread creation is an expensive OS-level operation. Instead of trying to restart dead threads or spawning millions of new ones, modern Java uses an ''ExecutorService''. It maintains a pool of active worker threads that continuously pull and execute new ''Runnable'' tasks from a queue, bypassing the strict lifecycle limitations of individual ''Thread'' objects.');
