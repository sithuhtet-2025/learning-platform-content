INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Naming Thread and Current Thread', 'Which static method is utilized to obtain a direct reference to the Thread object that is currently executing the exact line of code where the method is called?', 'Thread.getActive()', 'Thread.getCurrent()', 'Thread.currentThread()', 'Thread.thisThread()', 'C', 'The native ''Thread.currentThread()'' method returns a reference to the thread currently executing the statement. This is crucial for inspecting or modifying thread properties (like name or priority) from within a standard ''Runnable'' or utility method that doesn''t otherwise possess a thread reference.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Naming Thread and Current Thread', 'If a developer instantiates a new thread via ''new Thread()'' without explicitly providing a custom name, what default naming convention does the JVM automatically apply?', 'Anonymous-Thread-0, Anonymous-Thread-1, etc.', 'Thread-0, Thread-1, Thread-2, etc.', 'Worker-1, Worker-2, Worker-3, etc.', 'The JVM leaves the name strictly null.', 'B', 'To ensure every thread can be identified in logs and debuggers, the JVM automatically assigns sequential default names using the format "Thread-" followed by an incrementing integer, starting at 0.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Naming Thread and Current Thread', 'How can a developer explicitly assign a custom name to a thread at the exact moment of its instantiation?', 'By calling ''thread.setName("Name")'' before calling ''start()''.', 'By passing the string name as an argument to the Thread constructor, such as ''new Thread(myRunnable, "DataProcessorThread")''.', 'By using the ''@ThreadName("Name")'' annotation.', 'By overriding the ''getName()'' method.', 'B', 'The ''Thread'' class provides overloaded constructors specifically designed for naming. Passing the ''Runnable'' target and a String (e.g., ''new Thread(Runnable target, String name)'') guarantees the thread is named immediately upon creation in memory.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Naming Thread and Current Thread', 'Can the name of a Java thread be dynamically changed after the thread has already started executing?', 'No, the thread name is inherently final once execution begins.', 'Yes, by invoking the ''setName(String name)'' method on the thread reference at any time during its lifecycle.', 'Yes, but it requires pausing the thread first using ''wait()''.', 'No, it throws an IllegalThreadStateException.', 'B', 'Thread names are not immutable. A thread''s name can be updated dynamically during runtime using the ''setName()'' method. This is often used in thread pools to rename generic worker threads to reflect the specific task they are currently processing for easier debugging.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Naming Thread and Current Thread', 'What is the default string name automatically assigned by the JVM to the primary thread that executes the ''public static void main(String[] args)'' method?', 'System', 'Root', 'main', 'Thread-0', 'C', 'When a Java application launches, the JVM automatically spins up a primary non-daemon user thread to execute the entry point of the application. This foundational thread is explicitly named "main".');
