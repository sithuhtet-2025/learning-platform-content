INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Runtime class', 'How does a developer properly acquire an instance of the ''java.lang.Runtime'' class?', 'By using the constructor: ''new Runtime()''.', 'By extending the Runtime class.', 'By invoking the static factory method: ''Runtime.getRuntime()''.', 'By casting the System object: ''(Runtime) System.out''.', 'C', 'The ''Runtime'' class strictly follows the Singleton design pattern. Its constructor is private to ensure that only a single instance exists per Java application, representing the single runtime environment. It must be accessed via ''Runtime.getRuntime()''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Runtime class', 'Which method of the ''Runtime'' class is utilized to calculate the exact amount of memory currently available for future object allocation within the JVM heap?', 'Runtime.getRuntime().availableMemory()', 'Runtime.getRuntime().freeMemory()', 'Runtime.getRuntime().maxMemory()', 'Runtime.getRuntime().totalMemory()', 'B', 'The ''freeMemory()'' method returns an approximation of the total amount of memory currently available in the JVM for future allocated objects, measured in bytes. (Note: ''totalMemory()'' returns the current total heap size, and ''maxMemory()'' returns the absolute maximum heap size the JVM will attempt to use).');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Runtime class', 'What powerful, OS-level capability is provided by the ''Runtime.getRuntime().exec(String command)'' method?', 'It allows Java to execute C++ code directly in memory.', 'It allows the Java application to dynamically compile new Java classes at runtime.', 'It instructs the underlying operating system to spawn a completely new, independent process and execute the specified system command or executable file (e.g., "notepad.exe").', 'It forcefully terminates the JVM process.', 'C', 'The ''exec()'' method is a native bridge to the OS command line. It launches a separate OS process to run system-level commands or external applications, returning a ''Process'' object to allow the Java application to interact with its input/output streams.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Runtime class', 'What is the specific architectural purpose of the ''Runtime.getRuntime().addShutdownHook(Thread hook)'' method?', 'To prevent the user from closing the application window.', 'To register a specialized, un-started Thread that the JVM is guaranteed to execute immediately before it begins its final termination sequence, allowing for crucial, graceful resource cleanup.', 'To monitor the application for infinite loops.', 'To hook into the OS and prevent system reboots.', 'B', 'A shutdown hook is a defensive programming mechanism. It allows developers to register a thread containing critical cleanup logic (closing databases, saving state). The JVM ensures this thread runs when the program shuts down normally, or even if it is killed by an OS signal (like Ctrl+C).');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Runtime class', 'What is the functional difference between calling ''System.exit(0)'' and ''Runtime.getRuntime().exit(0)''?', '''System.exit(0)'' shuts down the JVM, while ''Runtime.exit(0)'' only kills the current thread.', '''System.exit(0)'' throws an exception, while ''Runtime.exit(0)'' executes silently.', 'There is absolutely zero functional difference; the ''System.exit()'' method is simply a convenient wrapper that internally calls ''Runtime.getRuntime().exit()''.', '''Runtime.exit(0)'' bypasses shutdown hooks, while ''System.exit(0)'' triggers them.', 'C', 'Architecturally, they are identical. The ''System.java'' source code explicitly demonstrates that calling ''System.exit(status)'' directly invokes ''Runtime.getRuntime().exit(status)''. Both methods abruptly terminate the JVM with the provided status code.');
