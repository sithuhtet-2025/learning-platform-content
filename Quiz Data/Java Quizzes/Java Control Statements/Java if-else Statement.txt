INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java if-else Statement', 'Unlike C or C++, what type of expression is strictly required inside the parentheses of a Java ''if'' statement?', 'An integer expression where 0 is false and non-zero is true.', 'Any object reference where null is false.', 'A strictly boolean expression that evaluates to true or false.', 'A comparison operator.', 'C', 'Java is strongly typed and requires the condition in an ''if'' statement to evaluate strictly to a ''boolean'' primitive. It does not implicitly convert integers or objects to booleans.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java if-else Statement', 'What is the output or result of the following condition if ''b'' is a boolean variable initially set to false: ''if (b = true) { System.out.print("A"); } else { System.out.print("B"); }''?', 'It prints "B" because b was false.', 'It prints "A" because the assignment operator returns the assigned value (true).', 'It causes a compilation error because = is not a comparison operator.', 'It throws a runtime exception.', 'B', 'The expression ''b = true'' is an assignment, not an equality check (==). It assigns true to ''b'' and evaluates to true, causing the ''if'' block to execute.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java if-else Statement', 'In an ''if-else if-else'' ladder, what happens once a true condition is encountered and its block is executed?', 'All subsequent ''else if'' conditions are still evaluated for potential execution.', 'Control flow skips the rest of the ladder and continues after the final ''else'' block.', 'The program terminates automatically.', 'The final ''else'' block is executed as a mandatory fallback.', 'B', 'Once a condition in an ''if-else if'' ladder evaluates to true, its corresponding block executes, and the remainder of the ladder is entirely bypassed (short-circuited).');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java if-else Statement', 'What is the ternary operator, and how does it relate to ''if-else'' statements?', 'It is a looping mechanism that replaces ''if-else''.', 'It is a shorthand expression that evaluates a boolean condition and returns one of two values, often replacing simple ''if-else'' assignments.', 'It allows an ''if'' statement to evaluate three conditions simultaneously.', 'It is an operator used specifically to combine multiple ''else if'' statements.', 'B', 'The ternary operator (condition ? trueValue : falseValue) is a compact expression used to return a value based on a boolean condition, functionally similar to a simple if-else assignment.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java if-else Statement', 'If you omit the curly braces {} after an ''if'' statement, what is the scope of that conditional execution?', 'The entire method.', 'The next block of code bounded by empty lines.', 'Only the single, immediate statement directly following the ''if'' condition.', 'It results in a compilation error; braces are always mandatory.', 'C', 'When curly braces are omitted, an ''if'' or ''else'' statement only applies to the single executable statement immediately following it.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java if-else Statement', 'Can an ''else'' block exist without a preceding ''if'' statement?', 'Yes, it will default to executing unconditionally.', 'Yes, if it is placed inside a switch statement.', 'No, an ''else'' must strictly correspond to an immediately preceding ''if'' or ''else if'' block.', 'Yes, but it causes a runtime warning.', 'C', 'An ''else'' block represents the fallback path of a conditional and is syntactically tied to a preceding ''if''. An orphaned ''else'' will cause a compilation error.');
