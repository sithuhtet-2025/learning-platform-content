INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Break Statement', 'Which of the following statements accurately describes the primary function of an unlabeled ''break'' statement inside a loop?', 'It skips the current iteration and evaluates the loop condition.', 'It terminates the innermost loop in which it is executed.', 'It terminates the program execution immediately.', 'It pauses the loop execution until a ''resume'' command is issued.', 'B', 'An unlabeled ''break'' statement immediately terminates the innermost enclosing loop or ''switch'' statement, passing control to the next statement following the block.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Break Statement', 'How can you terminate an outer loop from within a nested inner loop in Java?', 'By using the ''exit'' keyword.', 'By using a ''break'' statement followed by the number of loops to exit (e.g., ''break 2'').', 'By using a labeled ''break'' statement.', 'Outer loops cannot be terminated from within an inner loop.', 'C', 'Java provides labeled blocks. By placing a label before the outer loop and using ''break labelName;'', you can terminate the outer loop directly from the inner loop.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Break Statement', 'Where is it syntactically invalid to place a ''break'' statement?', 'Inside a ''while'' loop.', 'Inside a ''for'' loop.', 'Inside a ''switch'' statement.', 'Inside an ''if'' statement that is not enclosed within a loop or ''switch''.', 'D', 'The ''break'' statement is only legal inside loops (''for'', ''while'', ''do-while'') and ''switch'' statements. Using it standalone inside an ''if'' block causes a compilation error.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Break Statement', 'What happens if a ''break'' statement is executed inside a ''try'' block that has a ''finally'' block?', 'The ''finally'' block is skipped and the loop terminates immediately.', 'The program throws an IllegalStateException.', 'The ''finally'' block executes before the loop actually terminates.', 'The ''break'' statement is ignored.', 'C', 'Even when a ''break'' statement is encountered, if it is inside a ''try'' block, the associated ''finally'' block is guaranteed to execute before the control flow leaves the enclosing loop.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Break Statement', 'Consider a labeled ''break'' statement: ''break myLabel;''. What must ''myLabel'' refer to?', 'A variable declared in the current scope.', 'A class or method name.', 'A valid identifier preceding an enclosing statement block.', 'A numeric constant indicating the line number.', 'C', 'A label is a valid Java identifier followed by a colon, placed immediately before an enclosing statement block (typically a loop). The labeled ''break'' transfers control to the end of that specific block.');
