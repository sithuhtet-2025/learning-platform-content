INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java do-while Loop', 'What is the most significant difference between a ''while'' loop and a ''do-while'' loop in Java?', 'A ''do-while'' loop can only evaluate integer conditions.', 'A ''while'' loop evaluates its condition at the bottom of the loop body.', 'A ''do-while'' loop guarantees that the loop body will execute at least once.', 'A ''do-while'' loop cannot be infinite.', 'C', 'Unlike a ''while'' loop which checks the condition before the first iteration, a ''do-while'' loop executes its body first and then evaluates the condition, guaranteeing at least one execution.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java do-while Loop', 'Which of the following represents the correct syntax for a ''do-while'' loop in Java?', 'do { // code } while (condition)', 'do { // code } while (condition);', 'do (condition) { // code } while;', 'while (condition) do { // code };', 'B', 'A ''do-while'' loop must end with a semicolon after the closing parenthesis of the while condition. Omitting this semicolon results in a compilation error.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java do-while Loop', 'If a variable is declared inside the ''do'' block of a ''do-while'' loop, what is its scope?', 'It is accessible throughout the entire method.', 'It is accessible within the ''do'' block and the ''while'' condition.', 'It is accessible only within the ''do'' block, and cannot be referenced in the ''while'' condition.', 'It is a global variable.', 'C', 'Variables declared inside the curly braces of the ''do'' block are strictly scoped to that block. They fall out of scope before the ''while'' condition is evaluated, so they cannot be used there.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java do-while Loop', 'What will be the output of a ''do-while'' loop if its boolean condition is initially ''false''?', 'The loop body will not execute at all.', 'The loop body will execute exactly once, and then the loop will terminate.', 'The program will throw a RuntimeException.', 'The loop will execute infinitely.', 'B', 'Because the condition is evaluated after the loop body executes, a ''do-while'' loop with an initially false condition will still run its body exactly one time before terminating.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java do-while Loop', 'Is it legal to use a ''break'' or ''continue'' statement inside a ''do-while'' loop?', 'Yes, both ''break'' and ''continue'' function identically as they do in a standard ''while'' loop.', 'No, ''do-while'' loops do not support branching statements.', 'Only ''break'' is allowed; ''continue'' causes a compilation error.', 'Only ''continue'' is allowed; ''break'' causes a compilation error.', 'A', 'Branching statements like ''break'' (to exit the loop entirely) and ''continue'' (to skip to the condition evaluation) are perfectly legal and common inside ''do-while'' loops.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java do-while Loop', 'Consider the statement: ''do { } while (true);''. What does this represent?', 'A syntax error due to an empty body.', 'A loop that executes exactly once.', 'A perfectly valid infinite loop.', 'Unreachable code.', 'C', 'This is a valid construct for an infinite loop. The empty body executes, the condition evaluates to true, and the cycle repeats indefinitely until an external termination or internal ''break'' occurs.');
