INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java for Loop', 'Which of the following describes the correct execution order of the components in a traditional Java ''for'' loop?', 'Initialization, Update, Condition, Body', 'Initialization, Condition, Body, Update', 'Condition, Initialization, Body, Update', 'Initialization, Body, Update, Condition', 'B', 'A ''for'' loop executes the initialization expression once. Then it evaluates the condition. If true, it executes the loop body, followed by the update expression. It then repeats the condition check.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java for Loop', 'What is the result of omitting all three expressions in a ''for'' loop header, as in ''for(;;) { }''?', 'A compilation error due to missing loop parameters.', 'A runtime exception when the loop is first reached.', 'The loop executes exactly once and terminates.', 'An infinite loop is created.', 'D', 'In Java, if the condition expression is omitted from a ''for'' loop, it implicitly evaluates to true, resulting in an infinite loop unless broken internally.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java for Loop', 'Which of the following is a valid syntax for declaring multiple variables in the initialization block of a ''for'' loop?', 'for(int i = 0; int j = 10; i < j; i++)', 'for(int i = 0, j = 10; i < j; i++, j--)', 'for(int i = 0 AND int j = 10; i < j; i++)', 'for(int i = 0, double j = 10.0; i < j; i++)', 'B', 'Multiple variables can be declared and initialized in the first block of a ''for'' loop provided they are of the same type and separated by commas. The update block can also contain multiple comma-separated statements.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java for Loop', 'Consider a variable ''x'' declared in the initialization section of a ''for'' loop. Where is ''x'' accessible?', 'Anywhere within the class declaring the loop.', 'Anywhere within the method after the loop executes.', 'Only within the loop body, condition, and update expressions.', 'It is globally accessible if declared public.', 'C', 'Variables declared inside the initialization section of a ''for'' loop possess block scope, strictly limiting their accessibility to the loop structure itself.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java for Loop', 'What happens if the condition of a ''for'' loop evaluates to ''false'' on the very first check?', 'The loop body executes exactly once before terminating.', 'The loop body is skipped entirely, and control passes to the next statement after the loop.', 'A compilation error is triggered indicating unreachable code.', 'The update statement executes, and then the loop terminates.', 'B', 'Because the condition is evaluated before the body is executed, a ''false'' condition on the first pass prevents the body and the update statement from ever executing.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java for Loop', 'Can the initialization or update statements of a ''for'' loop contain method calls?', 'No, only assignment and increment/decrement operators are allowed.', 'Yes, but only in the initialization block.', 'Yes, but only in the update block.', 'Yes, both blocks can contain method calls as long as they constitute valid statement expressions.', 'D', 'Java allows any valid statement expression (including method calls, object creation, or assignments) in the initialization and update blocks of a ''for'' loop.');
