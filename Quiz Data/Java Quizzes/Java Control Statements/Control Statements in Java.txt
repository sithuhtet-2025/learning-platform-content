INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Control Statements in Java', 'Which of the following data types CANNOT be used as the control variable in a traditional ''switch'' statement in Java 17?', 'String', 'int', 'enum', 'float', 'D', 'Java ''switch'' statements do not support floating-point types (float, double) or boolean. They support byte, short, char, int, their wrappers, enums, and Strings.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Control Statements in Java', 'What is the ''dangling else'' problem in Java?', 'An ''else'' statement without a preceding ''if''.', 'The ambiguity of which ''if'' statement an ''else'' block belongs to when multiple nested ''if'' statements lack braces.', 'A compilation error caused by using ''else if'' instead of ''else''.', 'An unreachable ''else'' block.', 'B', 'In Java, an ''else'' always associates with the closest preceding ''if'' in the same block that does not have an ''else''. Omitting braces can obscure this logic.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Control Statements in Java', 'What is a defining characteristic of a ''do-while'' loop compared to a ''while'' loop?', 'The ''do-while'' loop cannot use the ''break'' statement.', 'The ''do-while'' loop condition is evaluated before the loop body executes.', 'The ''do-while'' loop guarantees that its body will be executed at least once.', 'The ''do-while'' loop can only iterate over collections.', 'C', 'Because the condition is evaluated at the bottom of the loop, the block of code inside a ''do-while'' loop is executed once before the condition is checked.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Control Statements in Java', 'Which of the following control flow transitions will cause an ''unreachable code'' compilation error in Java?', 'Code placed immediately after an unconditional ''break'' statement inside a loop block.', 'An ''else'' block following an ''if(true)'' statement.', 'Code placed after a ''switch'' statement that contains a ''default'' case.', 'A ''continue'' statement followed by an ''if'' statement in a sibling scope.', 'A', 'Any statement immediately following an unconditional branching statement like ''break'', ''continue'', or ''return'' in the same block is deemed unreachable, causing a compile-time error.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Control Statements in Java', 'In Java 14+, what keyword is used within a ''switch'' expression block to return a value from a case branch without using the arrow syntax?', 'return', 'yield', 'break', 'output', 'B', 'The ''yield'' keyword was introduced for switch expressions to return a value from a case block, acting similarly to a return statement but scoped strictly to the switch expression.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Control Statements in Java', 'When does a traditional ''for'' loop terminate?', 'When the initialization expression evaluates to false.', 'When the boolean condition expression evaluates to false.', 'When the update expression equals zero.', 'After it completes the number of iterations specified in its definition.', 'B', 'A traditional ''for'' loop executes as long as its condition expression evaluates to true. It terminates immediately once the condition evaluates to false.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Control Statements in Java', 'What happens if you omit the ''break'' statement at the end of a ''case'' block in a traditional ''switch'' statement?', 'The code will fail to compile.', 'The switch statement will terminate immediately after the case block finishes.', 'Execution will fall through to the next case block regardless of its matched value.', 'A runtime exception is thrown.', 'C', 'Omitting the ''break'' statement causes ''fall-through'', where the program continues executing the statements of subsequent cases until a ''break'' is reached or the switch ends.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Control Statements in Java', 'What is the scope of a variable declared within the initialization part of a traditional ''for'' loop?', 'It is accessible anywhere within the method containing the loop.', 'It is accessible only within the ''for'' loop body and its condition/update expressions.', 'It is accessible only within the loop body.', 'It has class-level scope.', 'B', 'Variables declared in the initialization section of a ''for'' loop are scoped to the loop itself, meaning they cannot be accessed outside the loop structure.');
