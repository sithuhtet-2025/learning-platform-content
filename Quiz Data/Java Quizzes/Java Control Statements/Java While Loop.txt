INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java While Loop', 'When does a ''while'' loop evaluate its boolean condition?', 'After executing the loop body.', 'Before executing the loop body.', 'Simultaneously while executing the loop body.', 'Only when a ''continue'' statement is encountered.', 'B', 'A ''while'' loop is a pre-test loop. It evaluates the boolean condition first; if the condition is true, it executes the body. If false initially, the body never executes.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java While Loop', 'What compilation error occurs if you write a loop like ''while (false) { System.out.print("Hi"); }'' in Java?', 'Incompatible types error.', 'Syntax error.', 'Unreachable code error.', 'No error; it compiles but does not print anything.', 'C', 'The Java compiler detects that the boolean literal ''false'' is constant. Because the condition is strictly false, the loop body can never execute, triggering an ''unreachable code'' compilation error.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java While Loop', 'What is the scope of a variable declared completely inside the body of a ''while'' loop?', 'It is scoped to the entire method containing the loop.', 'It is accessible anywhere below the loop in the same class.', 'It is created anew for each iteration and ceases to exist when the iteration finishes.', 'It retains its value and scope across all iterations until the method terminates.', 'C', 'Variables declared inside a block (like a ''while'' loop body) have block scope. They are initialized when their declaration is reached during an iteration and destroyed when that specific iteration ends.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java While Loop', 'Which of the following scenarios is most appropriate for utilizing a ''while'' loop instead of a ''for'' loop?', 'Iterating through exactly 10 elements of an array.', 'Executing code an unknown number of times until a specific event or condition occurs (e.g., reading a file until EOF).', 'When you need to initialize multiple variables of different types.', 'When you need guaranteed execution of the loop body at least once.', 'B', 'A ''while'' loop excels in situations where the number of iterations is not known beforehand, and execution depends entirely on a dynamic condition being met.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java While Loop', 'If a ''while'' loop''s condition relies on an integer variable ''counter'', what is a common cause of an infinite loop?', 'Initializing the counter to zero.', 'Declaring the counter outside the loop.', 'Failing to increment, decrement, or otherwise alter the counter variable inside the loop body.', 'Using a postfix increment operator instead of a prefix increment operator.', 'C', 'If the variables involved in the boolean condition are never modified within the loop body to eventually evaluate to ''false'', the loop will execute infinitely.');
