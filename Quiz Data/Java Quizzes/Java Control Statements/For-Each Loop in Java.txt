INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('For-Each Loop in Java', 'Which of the following is required for an object to be the target of a ''for-each'' loop?', 'The object must be an array or implement the ''Iterable'' interface.', 'The object must implement the ''Collection'' interface.', 'The object must implement the ''Iterator'' interface.', 'The object must be a generic class.', 'A', 'The enhanced ''for'' loop (for-each) can only iterate over arrays or instances of classes that implement the ''java.lang.Iterable'' interface.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('For-Each Loop in Java', 'What happens if you reassign the loop variable in a ''for-each'' loop while iterating over an array of primitives?', 'The original array element is modified.', 'A compilation error occurs.', 'A runtime exception is thrown.', 'The original array element remains unchanged.', 'D', 'The loop variable in a ''for-each'' loop is a copy of the value (for primitives). Reassigning it only changes the local variable, not the underlying array.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('For-Each Loop in Java', 'Which of the following scenarios is NOT suitable for a ''for-each'' loop?', 'Iterating over all elements in an ArrayList.', 'Iterating through an array to calculate the sum of its elements.', 'Removing specific elements from a collection during iteration.', 'Printing every character in a char array.', 'C', 'Attempting to remove elements from a collection directly during a ''for-each'' loop will typically throw a ''ConcurrentModificationException''. An explicit ''Iterator'' must be used instead.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('For-Each Loop in Java', 'What is the correct syntax for a ''for-each'' loop iterating over an array of integers named ''numbers''?', 'for (int n in numbers) { }', 'for (int n : numbers) { }', 'for (numbers : int n) { }', 'foreach (int n : numbers) { }', 'B', 'The correct Java syntax for the enhanced for loop uses a colon separating the declared element variable and the iterable collection/array.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('For-Each Loop in Java', 'How does a ''for-each'' loop behave when iterating over a multidimensional array (e.g., int[][] matrix)?', 'It iterates through all primitive elements in a single flattened pass.', 'It iterates through the inner arrays as objects, requiring nested ''for-each'' loops to reach the primitives.', 'It causes a compilation error; ''for-each'' loops only support 1D arrays.', 'It automatically transposes the matrix during iteration.', 'B', 'In Java, a multidimensional array is an array of arrays. The outer ''for-each'' loop will iterate over the nested array objects (e.g., int[]), requiring a nested loop for the primitive elements.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('For-Each Loop in Java', 'Can a ''for-each'' loop iterate over an array in reverse order?', 'Yes, by using the ''Collections.reverse()'' method inline.', 'Yes, by specifying the ''reverse'' keyword in the loop declaration.', 'No, the ''for-each'' loop only iterates strictly forward from index 0 to length-1.', 'No, but it can start from a specific index using the ''start'' parameter.', 'C', 'The enhanced for loop obscures the index and strictly iterates forward from the first element to the last. Reverse iteration requires a traditional ''for'' loop or explicitly reversing the collection first.');

