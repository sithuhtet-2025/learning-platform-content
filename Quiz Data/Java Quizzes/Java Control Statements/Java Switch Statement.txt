INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Switch Statement', 'Which of the following primitive data types (or their wrapper classes) is NOT supported as a selector variable in a standard Java ''switch'' statement?', 'char', 'byte', 'long', 'int', 'C', 'Java ''switch'' statements do not support ''long'', ''float'', ''double'', or ''boolean'' data types. They are limited to ''byte'', ''short'', ''char'', ''int'', ''String'', ''enum'', and their respective wrapper classes.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Switch Statement', 'What occurs at runtime if the variable passed into a traditional ''switch'' statement evaluates to ''null''?', 'The ''default'' case is executed.', 'A NullPointerException is thrown.', 'The switch block is silently bypassed.', 'The first ''case'' block is executed.', 'B', 'If the selector expression of a ''switch'' statement evaluates to ''null'', the JVM immediately throws a ''NullPointerException''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Switch Statement', 'Where must the ''default'' case be positioned within a ''switch'' block?', 'It must be the absolute last case in the block.', 'It must be the first case in the block.', 'It can be placed anywhere within the switch block.', 'It must be placed immediately after the case it serves as a fallback for.', 'C', 'The ''default'' case can be positioned anywhere inside the ''switch'' block. However, if it is not placed at the end, it still requires a ''break'' statement to prevent falling through to subsequent cases.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Switch Statement', 'What is the primary advantage of using the arrow (->) syntax in Java 14+ switch expressions compared to the traditional colon (:) syntax?', 'It allows floating-point numbers to be used in cases.', 'It automatically prevents ''fall-through'', eliminating the need for explicit ''break'' statements.', 'It allows multiple ''default'' cases.', 'It compiles faster than traditional syntax.', 'B', 'The arrow (->) syntax in modern Java switch rules dictates that only the block or expression to the right of the arrow is executed. This natively eliminates fall-through bugs without requiring ''break''.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Switch Statement', 'Can multiple case labels be combined to execute the same block of code in a single line using modern Java (14+) syntax?', 'Yes, by separating the values with a comma (e.g., ''case 1, 2, 3 ->'').', 'Yes, by using the logical OR operator (e.g., ''case 1 || 2 || 3 ->'').', 'No, each case must be written on a separate line.', 'Yes, by using an array (e.g., ''case [1, 2, 3] ->'').', 'A', 'Java 14 introduced the ability to list multiple comma-separated labels in a single ''case'' clause, greatly reducing verbosity when multiple inputs map to the same outcome.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Java Switch Statement', 'In a switch expression (where the switch evaluates to a value), what keyword is used to return a value from a multi-line code block inside a case?', 'return', 'continue', 'yield', 'export', 'C', 'The ''yield'' keyword is used specifically within a block inside a switch expression to return a value and terminate the switch expression execution.');
