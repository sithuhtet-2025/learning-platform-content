INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Passing an Array to a Function', 'When an array is passed to a method in C# without any modifiers (like ref or out), how is it passed by default?', 'By value, meaning a completely new physical copy of the array is created in memory.', 'By reference, allowing the method to change the original reference to point to a new array.', 'By value, meaning a copy of the reference is passed, allowing modification of existing elements but not the array reference itself.', 'It cannot be passed without the params keyword.', 'C', 'Arrays are reference types in C#. When passed by value (the default), a copy of the reference is passed to the method. This means the method can modify the internal elements of the original array, but reassigning the parameter to a new array won''t affect the original reference in the calling code.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Passing an Array to a Function', 'Given the method `void Modify(int[] arr) { arr[0] = 99; }` and the calling code `int[] numbers = { 1, 2, 3 }; Modify(numbers);`, what is the value of `numbers[0]` after the method call executes?', '1', '99', '0', 'A compile-time error occurs.', 'B', 'Because the method receives a copy of the reference that points to the exact same array object in the heap, modifying an element at a specific index directly alters the original array''s contents visible to the caller.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Passing an Array to a Function', 'Consider the method `void Reset(int[] arr) { arr = new int[] { 0, 0 }; }`. If you pass an existing array `int[] data = { 1, 2 };` to this method, what will the `data` array contain after the method returns?', '{ 0, 0 }', 'null', '{ 1, 2 }', 'An InvalidOperationException is thrown.', 'C', 'The method parameter `arr` is a local copy of the reference. Reassigning `arr` to a newly instantiated array simply points that local variable to a new memory location, leaving the original `data` array in the calling method completely untouched.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Passing an Array to a Function', 'How must you declare a method signature and invoke it if you want the method to completely replace the caller''s original array with a newly instantiated array?', '`void Replace(in int[] arr)` and `Replace(in myArray);`', '`void Replace(int[] arr)` and `Replace(myArray);`', '`void Replace(ref int[] arr)` and `Replace(ref myArray);`', '`void Replace(out int[] arr)` and `Replace(myArray);`', 'C', 'To modify the actual reference held by the caller (e.g., pointing it to a completely new array instance), the array must be passed explicitly by reference using the `ref` (or `out`) keyword in both the method signature and the invocation.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Passing an Array to a Function', 'Which of the following is the correct method signature to accept a two-dimensional rectangular array of strings?', '`void Process(string[][] matrix)`', '`void Process(string[,] matrix)`', '`void Process(string[2] matrix)`', '`void Process(ref string[][] matrix)`', 'B', 'A two-dimensional rectangular array is denoted by `[,]`. Option A (`[][]`) represents a jagged array (an array of arrays), which is a fundamentally different data structure in C#.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Passing an Array to a Function', 'If a method expects an `object[]` parameter, can you pass a `string[]` argument into it?', 'No, it will cause a compile-time type mismatch error.', 'Yes, due to array covariance, since `string` derives from `object`.', 'Yes, but only if you use the `ref` keyword.', 'No, unless you explicitly cast each element individually to an object.', 'B', 'C# supports array covariance for reference types. Because `string` inherits from `object`, a `string[]` can be implicitly converted and passed to a method expecting an `object[]`. However, this can throw an ArrayTypeMismatchException at runtime if the method attempts to insert a non-string object into it.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Passing an Array to a Function', 'When a method is defined to return an array (e.g., `public int[] GetNumbers()`), what is actually being returned to the calling code?', 'A deep copy of the entire array and its elements.', 'A pointer to the first element''s memory address on the stack.', 'A reference to the array object located on the managed heap.', 'A read-only immutable span of the array.', 'C', 'Because arrays are reference types, returning an array from a function simply returns a reference (the memory address) to the array object on the managed heap, not a full duplicate of the data block.');