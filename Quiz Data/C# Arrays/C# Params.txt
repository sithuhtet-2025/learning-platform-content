INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Params', 'What is the primary purpose of the `params` keyword in a C# method signature?', 'To pass arguments by reference rather than by value.', 'To allow a method to accept a variable number of arguments of a specific type.', 'To specify optional parameters that have default compiler values.', 'To restrict a method so it can only be called using an array.', 'B', 'The `params` keyword allows you to specify a method parameter that takes a variable number of arguments. The arguments can be passed as a comma-separated list or as an array of the specified type.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Params', 'Which of the following is a strict architectural constraint when using the `params` keyword in C#?', 'It must be the first parameter in the method signature.', 'It can be used multiple times across different parameters in a single method signature.', 'It must be the very last parameter in the method signature.', 'It can only be used with value types, not reference types.', 'C', 'The `params` parameter must be the last parameter in the method''s formal parameter list. This allows the compiler to resolve all preceding fixed parameters first before grouping the remaining arguments into the `params` array. Only one `params` keyword is allowed per method.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Params', 'In standard C# (prior to C# 13 enhancements), which data structure must immediately follow the `params` keyword in a method declaration?', 'A generic `List<T>`', 'An `IEnumerable<T>` interface', 'A single-dimensional array', 'A multidimensional array', 'C', 'Historically and standardly in C#, the `params` keyword must be applied to a single-dimensional array (e.g., `params int[] numbers`). While C# 13 introduces `params` collections like `ReadOnlySpan<T>`, the single-dimensional array remains the foundational requirement.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Params', 'Given the method signature `public void PrintNumbers(params int[] numbers)`, what happens if you invoke `PrintNumbers();` with no arguments provided?', 'A compile-time error occurs because at least one argument is required.', 'A `NullReferenceException` is thrown at runtime when trying to access the array.', 'The `numbers` parameter is passed as `null`.', 'The `numbers` parameter receives an initialized, empty array (length of 0).', 'D', 'If no arguments are passed for a `params` parameter, the C# compiler automatically creates and passes an empty array of the specified type. It does not pass `null`, making it safe to iterate over without null-checking.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Params', 'Consider two overloaded methods: `void Display(int x, int y)` and `void Display(params int[] numbers)`. If you execute the code `Display(1, 2);`, how does the C# compiler resolve this invocation?', 'It invokes `Display(params int[] numbers)` because `params` is inherently more flexible.', 'It invokes `Display(int x, int y)` because a method with an exact parameter count match takes precedence over a `params` method.', 'It results in a compile-time error due to ambiguous method invocation.', 'It executes both methods sequentially in the order they are defined.', 'B', 'During overload resolution, the C# compiler always prefers a method without a `params` array if an applicable method exists where the provided arguments exactly match the explicitly declared parameters. `params` acts as a fallback mechanism.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Params', 'If a method is defined as `public void Log(params string[] messages)`, which of the following is NOT a syntactically valid way to invoke it?', '`Log("Error 1", "Error 2", "Error 3");`', '`Log(new string[] { "Error 1", "Error 2" });`', '`Log(new List<string> { "Error 1", "Error 2" });`', '`Log();`', 'C', 'You cannot pass a `List<T>` directly into a `params T[]` parameter. You must pass either a comma-separated list of items, an explicit array of that type, or nothing at all. To pass a List, you would first need to call `.ToArray()` on it.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Params', 'How can you utilize the `params` keyword to accept a variable number of arguments of differing, mixed data types (e.g., an `int`, a `string`, and a `bool`) in a single method call?', 'By defining multiple `params` arrays in the signature.', 'By using `params object[] args`.', 'By using the `params var[] args` syntax.', 'It is completely impossible; `params` requires a single, strict primitive data type.', 'B', 'Because all types in C# inherit from `System.Object`, defining the parameter as `params object[]` allows you to pass a comma-separated list of any mixture of types. The CLR will automatically box value types as needed.');