INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# this keyword', 'What does the `this` keyword fundamentally represent within a C# class or struct?', 'The base class that the current class inherits from.', 'A reference pointer to the current, specific instance of the class or struct in which the code is executing.', 'A static reference to the overall class blueprint.', 'The thread currently executing the code block.', 'B', 'The `this` keyword acts as a hidden pointer. It refers specifically to the current object instance on which the method or property is being invoked, allowing access to its specific instance members.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# this keyword', 'What is one of the most common, practical architectural uses for the `this` keyword in a C# constructor or method?', 'To explicitly delete the object from memory.', 'To securely encrypt the object''s fields.', 'To resolve naming collisions when a local method parameter has the exact same name as a class-level instance field.', 'To bypass standard access modifiers like `private`.', 'C', 'If a constructor has a parameter named `age` and a class field named `age`, writing `age = age` is ambiguous. Using `this.age = age;` explicitly tells the compiler to assign the parameter''s value to the object''s instance field.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# this keyword', 'How is the `this` keyword used to facilitate constructor chaining within the exact same class?', 'public MyClass() => this.Init();', 'public MyClass() { this(); }', 'public MyClass() inherits this { }', 'public MyClass() : this(5, "Default") { }', 'D', 'Using `: this(...)` in the constructor declaration line instructs the compiler to execute another constructor within the same class before executing the body of the current constructor, ensuring DRY (Don''t Repeat Yourself) initialization logic.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# this keyword', 'Why does the C# compiler throw an immediate error if you attempt to use the `this` keyword inside a `static` method?', 'Because static methods execute at the Type level, completely independent of any instantiated object, meaning an instance context (`this`) mathematically does not exist.', 'Because `this` is exclusively reserved for the Main method.', 'Because static methods are structurally read-only.', 'Because `this` points to the Application Domain in a static context.', 'A', 'The `this` pointer specifically requires a physical object instance in memory to point to. Since static methods belong to the class blueprint itself and are called without instantiating an object, there is no instance reference available.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# this keyword', 'In C# architecture, what highly specialized role does the `this` keyword play when declaring an Extension Method?', 'It is used as the return type to enable fluent method chaining.', 'It is placed before the first parameter of a static method to indicate that the method virtually "extends" the type of that parameter.', 'It is used to lock the method for thread safety.', 'It allows the static method to access private fields of the extended class.', 'B', 'Extension methods are static methods, but by prefixing the first parameter with `this` (e.g., `public static int WordCount(this string text)`), the compiler allows the method to be called as if it were a native instance method on that specific type (e.g., `myString.WordCount()`).');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# this keyword', 'Which specific C# class feature is structurally defined by utilizing the `this` keyword followed by square brackets (e.g., `public int this[int index] { get; set; }`)?', 'An Iterator', 'A Matrix', 'An Indexer', 'A Pointer', 'C', 'An indexer allows an object instance to be indexed and accessed exactly like an array. The `this` keyword is strictly required to declare an indexer, defining how the object behaves when the `[]` operator is applied to it.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# this keyword', 'If an object needs to pass a reference of itself to another external method (e.g., registering itself with a global `EventManager`), how is this safely achieved using the `this` keyword?', 'EventManager.Register(typeof(this));', 'EventManager.Register(ref this);', 'EventManager.Register(this.Instance);', 'EventManager.Register(this);', 'D', 'The `this` keyword evaluates to the object reference of the current instance. Passing `this` as an argument safely hands that reference over to another method, allowing external components to interact with the originating object.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# this keyword', 'What is a subtle, low-level architectural difference in how the `this` keyword behaves inside a `struct` (Value Type) compared to a `class` (Reference Type)?', 'In a struct, `this` cannot be used at all.', 'In a struct, `this` acts as a hidden `ref` parameter, technically allowing the entire struct to be reassigned to a new value (e.g., `this = new MyStruct();`), which is strictly forbidden in a class.', 'In a struct, `this` points to the heap, whereas in a class it points to the stack.', 'In a struct, `this` is strictly read-only and its properties cannot be modified.', 'B', 'Because structs are value types, `this` is treated as a reference variable (`ref T`) pointing directly to the stack memory block. You can actually reassign the entire struct instance from within its own method by assigning a new value to `this`, an operation completely banned for class reference pointers.');