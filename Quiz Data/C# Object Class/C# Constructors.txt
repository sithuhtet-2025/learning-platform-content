INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Constructors', 'What is the primary architectural purpose of a constructor in a C# class?', 'To destroy the object and safely free up managed memory.', 'To define the static, shared methods of the overall class.', 'To execute a specialized block of code that initializes the state of a newly instantiated object before it is ever used by the application.', 'To mathematically compute and return a specific data type to the caller.', 'C', 'A constructor is automatically invoked by the CLR the exact moment an object is created using the `new` keyword. Its primary responsibility is to set initial, safe values for the object''s internal fields and properties, ensuring it boots up in a valid, usable state.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Constructors', 'Which of the following strict syntactic rules applies when defining a standard instance constructor in C#?', 'It must be marked with the `async` keyword.', 'It must return an instance of the class.', 'It must have the exact same name as the class it resides in, and it must declare absolutely no return type, not even `void`.', 'It must begin with the `init` keyword.', 'C', 'Constructors are highly specialized methods. The C# compiler strictly mandates that they share the exact identifier name of their parent class. Furthermore, unlike standard methods, constructors are expressly forbidden from possessing any return type signature.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Constructors', 'What happens if a developer defines a custom C# class but completely omits writing any constructors for it?', 'The code fails to compile.', 'The object can only be instantiated using reflection.', 'The class is automatically marked as `static`.', 'The C# compiler automatically injects a hidden, public parameterless constructor (a default constructor) behind the scenes.', 'D', 'The CLR absolutely requires a constructor to instantiate an object. If you don''t write one, the compiler silently synthesizes a public, parameterless "default" constructor to satisfy this rule. However, if you explicitly write *any* custom constructor, the compiler will NOT generate this default one.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Constructors', 'Can a single C# class contain multiple constructors?', 'No, a class is strictly limited to one constructor.', 'Yes, but only if one is public and the rest are private.', 'Yes, through constructor overloading, provided each constructor has a unique signature (a different number, type, or order of parameters).', 'Yes, but they must all have different names.', 'C', 'Just like standard methods, constructors can be heavily overloaded. This allows architects to provide multiple ways to instantiate the same object (e.g., `new User()` versus `new User("Alice")`), making the class highly flexible for different consumer scenarios.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Constructors', 'How can one constructor in a class explicitly call another constructor within the exact same class to avoid duplicating initialization logic?', 'By calling `base(...)` in the body of the constructor.', 'By using the constructor initializer syntax `: this(...)` immediately after the constructor signature.', 'By invoking `this.Constructor(...)` inside the method body.', 'It is structurally impossible; constructors cannot call each other.', 'B', 'Constructor chaining allows a constructor to route its initialization logic through another constructor in the same class using the `: this(args)` syntax. The target constructor executes completely before the body of the calling constructor ever begins, ensuring DRY (Don''t Repeat Yourself) principles.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Constructors', 'From a software architecture perspective, what is the most common and powerful use case for declaring a constructor as `private`?', 'To allow only derived child classes to instantiate it.', 'To hide the constructor from the Garbage Collector.', 'To implement the Singleton design pattern or to prevent a utility class containing only static methods from ever being instantiated.', 'To allow the object to be instantiated seamlessly across network boundaries.', 'C', 'A `private` constructor creates an absolute barrier, preventing any external code from using the `new` keyword to create an instance. This is foundational for the Singleton pattern (where the class manages its own single instance internally) or for pure static utility classes (like `System.Math`).');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Constructors', 'What is the defining execution guarantee of a `static` constructor in C#?', 'It executes every single time the `new` keyword is used.', 'It executes automatically, exactly once per application domain, immediately before the first instance is created or any static members are referenced.', 'It only executes when explicitly called by the Main method.', 'It executes continuously on a background thread.', 'B', 'A static constructor initializes shared, class-level static data. The CLR guarantees it will run exactly once, in a highly thread-safe manner, right before the type is first accessed. It cannot take parameters and cannot have access modifiers because the CLR calls it automatically.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Constructors', 'When an object of a derived class is instantiated, how does the C# compiler ensure the base class is properly initialized?', 'It calls the derived constructor first, and then the base constructor.', 'It ignores the base constructor completely unless manually invoked.', 'It requires the developer to call `base.Init()` manually as the first line of code.', 'It automatically injects a call to the base class''s parameterless constructor (using `: base()`) right before executing the derived constructor, unless the developer explicitly chains a different base constructor.', 'D', 'Inheritance strictly requires a top-down initialization flow. The base class must be fully constructed before the derived class. The compiler implicitly adds `: base()` to derived constructors. If the base class lacks a parameterless constructor, the developer is forced to explicitly use `: base(args)` to resolve the compilation error.');