INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Static Constructor', 'What is the primary architectural purpose of a `static` constructor in C#?', 'To initialize standard instance variables for a newly created object.', 'To initialize any static data, or to perform a particular foundational action that needs to be performed exactly once per Type.', 'To physically prevent a class from being instantiated.', 'To safely dispose of static members when the application closes.', 'B', 'A static constructor is used to set up the initial global state of a class. It is specifically designed to initialize static fields or properties, ensuring the shared state is ready before any code interacts with the class.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Static Constructor', 'Which of the following strict syntactic rules applies when defining a `static` constructor in C#?', 'It must be explicitly marked `public` to be accessible by the CLR.', 'It must take at least one parameter to pass in configuration data.', 'It strictly cannot take any access modifiers (like `public` or `private`) and it cannot possess any parameters.', 'It must return a boolean indicating successful initialization.', 'C', 'Because the Common Language Runtime (CLR) invokes the static constructor automatically, it cannot be called by developer code. Therefore, access modifiers are completely meaningless, and because no one explicitly calls it, parameters cannot be passed to it.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Static Constructor', 'When does the Common Language Runtime (CLR) automatically invoke a class''s `static` constructor?', 'At the exact millisecond the application process boots up.', 'Every single time an object of the class is instantiated.', 'Exactly once, automatically, immediately before the first instance is created or any static members are referenced.', 'When the Garbage Collector runs its first cycle.', 'C', 'The CLR implements highly lazy initialization for static constructors. It waits until the absolute last possible moment—right before your code creates the first instance or touches a static field—to execute the static constructor, and it guarantees it will only ever run once.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Static Constructor', 'How does a developer explicitly invoke a `static` constructor in their C# code (e.g., `MyClass.StaticConstructor()`)?', 'By calling `base()` inside the instance constructor.', 'By using the `new` keyword on the static class.', 'It is structurally impossible; a static constructor is called exclusively by the CLR and can never be invoked directly by developer code.', 'By executing `typeof(MyClass).Init();`.', 'C', 'Developer code has absolutely no mechanism to call a static constructor. It is a strictly automated architectural feature managed entirely by the .NET runtime environment to ensure thread safety and exact-once execution.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Static Constructor', 'What happens if an unhandled exception is thrown inside a `static` constructor during its execution?', 'The CLR silently swallows the error and leaves static fields uninitialized.', 'The CLR throws a `TypeInitializationException`, and the entire class remains permanently locked and unusable for the entire lifetime of the Application Domain.', 'The CLR automatically retries executing the static constructor on the next instantiation.', 'The application immediately blue-screens the operating system.', 'B', 'Throwing an exception in a static constructor is catastrophic. The CLR wraps the error in a `TypeInitializationException`. Because the class failed to initialize its global state, the CLR permanently marks the Type as broken, and any future attempt to use that class in the application will instantly fail.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Static Constructor', 'How do you ensure thread safety when multiple threads simultaneously attempt to access a type with a `static` constructor for the very first time?', 'You must manually wrap the entire contents of the static constructor in a `lock` statement.', 'You must use the `Interlocked` class for all assignments.', 'The CLR automatically guarantees thread safety natively, ensuring the static constructor executes exactly once and safely blocking all other threads until it finishes.', 'Static constructors are inherently not thread-safe and must be avoided in multithreaded architecture.', 'C', 'The .NET execution engine handles all synchronization internally. If ten threads hit the class simultaneously, the CLR freezes nine of them, allows one to execute the static constructor, and then releases the rest once the shared state is safely initialized.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Static Constructor', 'Can you overload a `static` constructor in C# to provide different ways of initializing global state based on external configuration?', 'Yes, by providing different parameter types.', 'Yes, by using different access modifiers.', 'No, because static constructors cannot take parameters, a class is structurally limited to having exactly one single static constructor.', 'Yes, but only in static classes.', 'C', 'Method overloading relies entirely on having distinct parameter signatures. Since the C# compiler strictly dictates that a static constructor must be parameterless, it is mathematically impossible to have more than one static constructor per class.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Static Constructor', 'Why does the C# compiler throw an error if you attempt to use the `this` keyword inside a `static` constructor?', 'Because `this` is reserved for standard static methods.', 'Because the static constructor executes globally at the Type metadata level, completely independent of any instantiated object, meaning an instance context (`this`) simply does not exist.', 'Because static constructors are strictly read-only scopes.', 'Because `this` points to the Application Domain in a static context.', 'B', 'The `this` keyword is a reference pointer strictly tied to a physically instantiated object on the heap. Because a static constructor initializes the global class blueprint before objects even exist, an instance context (`this`) is impossible.');