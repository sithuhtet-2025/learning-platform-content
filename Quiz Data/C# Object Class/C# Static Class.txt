INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Static Class', 'What is the most defining architectural restriction of a `static class` in C#?', 'It must implement at least one interface.', 'It cannot be instantiated using the `new` keyword, and it can strictly only contain static members.', 'It executes continuously on a background thread.', 'It cannot contain any variables, only methods.', 'B', 'A static class acts as a global container, typically for utility or helper methods. Because it represents global state and behavior rather than an object blueprint, the C# compiler strictly prevents its instantiation and forbids any instance-level members.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Static Class', 'From an object-oriented design perspective, what is the strict inheritance rule for a `static class` in C#?', 'It can inherit from any other static class.', 'It is implicitly sealed; it cannot inherit from any class (except `System.Object`), and no class can inherit from it.', 'It must explicitly inherit from `System.ValueType`.', 'It can inherit from standard classes, but cannot be inherited itself.', 'B', 'Static classes sit completely outside the traditional OOP inheritance hierarchy. Because they cannot be instantiated, the compiler implicitly marks them as `sealed` and abstract, preventing them from serving as base classes or inheriting behavior from other classes.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Static Class', 'Which of the following constructor configurations is legally permitted inside a `static class`?', 'A single `private` instance constructor to prevent instantiation.', 'Multiple overloaded static constructors.', 'A single, parameterless `static` constructor.', 'A public instance constructor.', 'C', 'Because a static class cannot be instantiated, it cannot possess any instance constructors (not even private ones). It is strictly limited to a single, parameterless static constructor used by the CLR to initialize its static fields right before its first use.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Static Class', 'Can a `static class` implement an interface in C#?', 'Yes, provided all interface methods are implemented as static.', 'Yes, but only if the interface is also marked static.', 'No, interfaces define a contract for object instances, and since static classes cannot be instantiated, they cannot implement interfaces.', 'Yes, but it must explicitly implement them.', 'C', 'Interfaces dictate the behavior of an object instance for polymorphic interactions. Because a static class can never be instantiated into an object, it mathematically cannot fulfill an interface contract.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Static Class', 'Which specific class structure is strictly mandated by the C# compiler when you want to create an Extension Method?', 'A generic instance class.', 'A sealed base class.', 'A non-generic, top-level `static class`.', 'A partial class.', 'C', 'Extension methods are syntactic sugar that allow you to "add" methods to existing types. The compiler strictly enforces that these methods must be `static` and must be housed within a top-level, non-generic `static class`.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Static Class', 'Why is utilizing a `static class` to store massive amounts of mutable, global application state generally considered a severe architectural anti-pattern?', 'It causes the application to crash on startup.', 'Because global mutable state creates tight coupling, makes unit testing virtually impossible, and introduces catastrophic race conditions in multithreaded environments.', 'It bypasses the Garbage Collector entirely, causing immediate memory leaks.', 'It forces the CLR to compile the class into unmanaged C++ code.', 'B', 'A static class shares its state globally across the entire Application Domain. If multiple threads mutate this data simultaneously without extreme synchronization, it causes data corruption. It also breaks dependency injection patterns, effectively destroying testability.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Static Class', 'Can a standard, instantiable class be defined as a nested type *inside* a `static class`?', 'No, static classes cannot contain any nested types.', 'Yes, but the nested class must also be marked as static.', 'Yes, a static class can contain nested non-static classes, and those nested classes can be instantiated normally.', 'No, unless the nested class inherits from the static parent.', 'C', 'While a static class can only contain static members, a nested class definition itself is considered a static member of its parent type. Therefore, you can nest a standard, instantiable class inside a static class, though this is an uncommon design pattern.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Static Class', 'Aside from declaring the first parameter of an Extension Method, what happens if you attempt to use the `this` keyword inside a method belonging to a `static class`?', 'The compiler throws a syntax error because there is no object instance context for `this` to point to.', 'It points to the static class''s Type object in memory.', 'It points to the current Application Domain.', 'It automatically references the method that called it.', 'A', 'The `this` keyword is a reference pointer strictly tied to a physically instantiated object. Because static class methods operate globally without an object instance, a `this` context simply does not exist.');