INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# static', 'What is the primary architectural restriction imposed by the C# compiler when a class is declared with the `static` modifier?', 'It forces the class to execute synchronously.', 'It prevents the class from being instantiated with the `new` keyword and dictates that it can only contain static members.', 'It requires the class to implement the `IDisposable` interface.', 'It makes the class entirely immutable.', 'B', 'A `static` class acts as a global container for utility methods and fields. Because it cannot be instantiated, there is no object state; all members must be static and are accessed directly via the class name itself.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# static', 'From a memory management perspective, how does a `static` field behave differently than a standard instance field?', 'A static field is stored on the stack rather than the heap.', 'A static field is automatically encrypted by the CLR.', 'A static field is shared globally across all instances of the class, meaning if one instance modifies it, the change is instantly reflected for all other instances.', 'A static field is reset to its default value every time a new object is created.', 'C', 'Unlike instance fields where every object gets its own independent, isolated copy, a static field represents a single, shared memory location attached to the Type itself, not to any specific object instance.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# static', 'What is the strict execution guarantee provided by the Common Language Runtime (CLR) regarding a `static` constructor?', 'It executes every time a new instance of the class is created.', 'It executes continuously on a background thread.', 'It executes exactly once per Application Domain, immediately before the first instance is created or the first static member is referenced.', 'It only executes when explicitly called by the Main method.', 'C', 'The CLR guarantees that a static constructor will run exactly once in a highly thread-safe manner before the type is ever used by the application. It cannot take parameters or have access modifiers because it is invoked automatically by the system.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# static', 'Why does the C# compiler throw an error if you attempt to use the `this` keyword inside a `static` method?', 'Because `this` is a reserved keyword strictly for the Main method.', 'Because static methods execute completely independent of any specific object instance, meaning there is no current object reference (`this`) to point to.', 'Because static methods are structurally read-only.', 'Because the `this` keyword must be passed as an explicit parameter to static methods.', 'B', 'The `this` keyword represents a pointer to the current, physically instantiated object. Since static methods belong to the class blueprint itself and are called without instantiating an object, an instance context (`this`) mathematically does not exist.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# static', 'In C# architecture, if you want to create an "Extension Method" to add new functionality to an existing type (like adding `.WordCount()` to the standard `string` class), what role does the `static` keyword play?', 'Extension methods do not use the `static` keyword.', 'The extension method must be marked `static`, and it must be housed inside a top-level `static` class.', 'Only the parent class needs to be `static`, not the method itself.', 'Only the first parameter needs to be marked `static`.', 'B', 'Extension methods are syntactic sugar that allows you to "add" methods to existing types without modifying their original source code. Structurally, the C# compiler strictly requires them to be defined as `static` methods residing inside a `static` class, with the first parameter prefixed by the `this` keyword.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# static', 'What is a severe architectural vulnerability when multiple threads concurrently read and write to a shared `static` field or property?', 'The compiler automatically locks the field, causing massive performance bottlenecks.', 'Static fields are inherently thread-safe by default, so there is no vulnerability.', 'Because the static state is globally shared across the AppDomain, concurrent mutations without explicit synchronization (like a `lock` statement) will result in immediate race conditions and data corruption.', 'The CLR throws an `InvalidOperationException` instantly.', 'C', 'Static state is global state. If Thread A and Thread B attempt to modify a static list or numeric counter at the exact same millisecond, the memory becomes hopelessly corrupted. Developers must implement strict synchronization mechanisms to protect mutable static data.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# static', 'Where are static variables physically allocated in the .NET managed memory architecture?', 'The Execution Stack', 'The Large Object Heap (LOH)', 'The High Frequency Heap (associated with the Type object in the AppDomain)', 'Unmanaged OS Memory', 'C', 'Static variables are not stored on the stack, nor are they placed on the normal Garbage Collected heap alongside standard object payloads. They are stored in a highly specialized area called the High Frequency Heap, attached directly to the Type metadata, and they persist for the entire lifetime of the Application Domain.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# static', 'How do you correctly invoke a `static` method named `CalculateTax()` that resides inside a standard class named `Invoice`?', '`new Invoice().CalculateTax();`', '`Invoice.CalculateTax();`', '`this.CalculateTax();`', '`Invoice->CalculateTax();`', 'B', 'Because static methods belong to the Type itself rather than an instantiated object, you cannot call them through an object instance variable (like `myInvoice.CalculateTax()`). You must access them directly using the class name as the qualifier.');