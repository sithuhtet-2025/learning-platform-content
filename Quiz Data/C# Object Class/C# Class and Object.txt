INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Class and Object', 'In object-oriented C# programming, what is the fundamental architectural difference between a Class and an Object?', 'A class is a variable, and an object is a function.', 'A class is a concrete instance in memory, while an object is the theoretical blueprint.', 'A class is the theoretical blueprint or template that defines structure and behavior, while an object is a concrete, physical instance of that blueprint residing in memory.', 'They are interchangeable terms for the exact same concept.', 'C', 'A class defines the architectural rules (the blueprint for a house). An object is the actual entity created using those rules (the physical house built from the blueprint). You can create many objects from a single class.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Class and Object', 'Because classes in C# are strictly "reference types," what happens in memory when you instantiate a new object?', 'The entire object payload is allocated directly on the fast execution stack.', 'The object''s payload is allocated on the managed heap, and the variable itself only holds a lightweight memory address (reference) pointing to that payload.', 'The object is stored on the physical hard drive until accessed.', 'The object is split equally between the stack and the heap.', 'B', 'When a class is instantiated, the CLR allocates the heavy data payload on the managed heap. The variable you declare in your code is merely a pointer (reference) located on the stack that stores the memory address of the heap payload.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Class and Object', 'What two critical structural operations does the `new` keyword perform when creating an object (e.g., `Car myCar = new Car();`)?', 'It compiles the class and registers it with the operating system.', 'It requests memory allocation from the Common Language Runtime (CLR) on the managed heap, and then immediately invokes the class constructor to initialize that memory space.', 'It deletes any old versions of the object and allocates a new one.', 'It creates a static instance of the class that all threads can share.', 'B', 'The `new` keyword is a low-level allocation command. It calculates the memory required for the object, reserves that space on the managed heap, and then fires the constructor method to setup the object''s initial state before returning the reference pointer.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Class and Object', 'From a strict architectural perspective, how should the internal state (data) of an object be managed within a class?', 'Using public fields so external code can read and write data as fast as possible.', 'Using private fields to store the data (encapsulation), exposed to the outside world only through controlled, public Properties (get/set accessors) or methods.', 'Using global variables defined outside the class scope.', 'Data should not be stored in objects; they should strictly contain methods.', 'B', 'Encapsulation is a pillar of OOP. Internal data (fields) must be kept private to prevent external code from corrupting the object''s state. Properties act as gatekeepers, allowing the class to validate data before it modifies the private fields.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Class and Object', 'What is the specific role of a "Method" defined within a C# class blueprint?', 'To hold static data that is shared across all instances of the class.', 'To define the object''s behaviors, actions, and the specific operations it can perform on its internal state.', 'To provide a template for inheriting classes to bypass security checks.', 'To automatically destroy the object when it is no longer needed.', 'B', 'If properties and fields represent what an object *knows* (its state), methods represent what an object can *do* (its behavior). Methods contain the executable code that operates on the object''s encapsulated data.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Class and Object', 'What happens if you define a custom class but do NOT explicitly write a constructor for it?', 'The code will fail to compile.', 'The object can never be instantiated using the `new` keyword.', 'The C# compiler automatically generates a hidden, public parameterless constructor (default constructor) behind the scenes so the object can still be instantiated.', 'You must inherit a constructor from the `System.Object` base class manually.', 'C', 'Every class requires a constructor to be instantiated. If a developer does not provide one, the C# compiler steps in and creates an implicit, empty default constructor (`public MyClass() { }`) to satisfy the CLR''s instantiation requirements.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Class and Object', 'Inside an instance method or constructor, what exactly does the `this` keyword reference?', 'The base class that the current class inherits from.', 'The specific, current instance of the object that the method is actively executing against, which is heavily used for resolving naming collisions between class fields and method parameters.', 'The thread currently executing the object''s code.', 'The static, shared state of the overall class.', 'B', 'The `this` keyword is a reference pointer to the current object itself. If a constructor parameter is named `age` and the class also has a field named `age`, you must use `this.age = age;` to explicitly tell the compiler which one is the object''s field.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Class and Object', 'Unlike C++, C# developers do not explicitly destroy objects to free memory (e.g., using a `delete` keyword). How are objects structurally removed from memory in C#?', 'They are deleted automatically the exact millisecond they go out of scope.', 'The .NET Garbage Collector (GC) runs periodically on a background thread, tracking object references. When an object has zero active references pointing to it, the GC safely reclaims its heap memory.', 'The operating system deletes them when the physical RAM is 100% full.', 'Objects are never removed; they persist until the computer reboots.', 'B', 'C# is a managed language. The Garbage Collector automates memory management by building a graph of all active references. If an object is "orphaned" (no variables point to it anymore), the GC flags it as garbage and clears it from the heap during its next cycle.');