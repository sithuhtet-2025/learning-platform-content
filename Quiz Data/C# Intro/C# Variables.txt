INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Variables', 'What is the fundamental definition of a variable in C#?', 'A keyword used to execute loops.', 'A dynamically expanding array.', 'A named memory location used to store data that can be modified during program execution.', 'A method that returns a specific data type.', 'C', 'A variable acts as a labeled container in the computer''s memory. It holds a value of a specific data type, which can be accessed and modified throughout the lifecycle of the application.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Variables', 'Which of the following is a completely valid variable name in C#?', '1stPlayerScore', 'player_Score_1', 'player-score', 'string', 'B', 'In C#, variable names can contain letters, digits, and underscores, but they strictly CANNOT begin with a digit. Additionally, they cannot contain hyphens or be reserved keywords (like ''string'') unless prefixed with an @ symbol.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Variables', 'When using the implicitly typed ''var'' keyword to declare a local variable, what strict rule must be followed?', 'The variable must be initialized with a value at the exact time of declaration.', 'The variable can be left uninitialized and assigned later.', 'The variable automatically defaults to null.', 'The variable can later change its data type.', 'A', 'The C# compiler requires the variable to be initialized on the same line it is declared (e.g., `var count = 10;`). It uses this initial assignment to definitively lock in the variable''s static type at compile-time.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Variables', 'What happens if you attempt to use a locally scoped variable inside a method before explicitly assigning a value to it?', 'The compiler automatically assigns it a default value (like 0 or null).', 'The application compiles but crashes at runtime.', 'The C# compiler throws a "use of unassigned local variable" error.', 'It inherits the value of the nearest global variable.', 'C', 'Unlike class-level fields (which receive default values), local variables inside methods must be "definitely assigned" before their value is read. The compiler strictly enforces this to prevent unpredictable memory behavior.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Variables', 'Which keyword is used to declare a variable whose value is locked at compile-time and can absolutely never be changed throughout the life of the application?', 'static', 'readonly', 'const', 'sealed', 'C', 'The ''const'' keyword defines a compile-time constant. Its value must be hardcoded at the time of declaration, and any attempt to modify it later will result in a strict compile-time error.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Variables', 'What is the crucial architectural difference between a ''const'' variable and a ''readonly'' variable in C#?', '''const'' applies to value types, while ''readonly'' applies to reference types.', '''const'' variables are evaluated at compile-time, while ''readonly'' variables can be evaluated at runtime and assigned dynamically inside a constructor.', '''readonly'' variables can be changed multiple times within the same method.', 'There is no difference; they are interchangeable aliases.', 'B', 'A ''const'' must be a hardcoded value available when the code is compiled. A ''readonly'' variable is much more flexible—it can be calculated at runtime (e.g., reading from a database) and assigned inside the class''s constructor, becoming immutable only after instantiation is complete.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Variables', 'Which of the following is the correct syntax to declare multiple variables of the same data type on a single line?', 'int x = 5; int y = 10;', 'int x = 5, y = 10;', 'int x, int y = 5, 10;', 'int x = 5 and y = 10;', 'B', 'C# allows multiple variables of the exact same type to be declared and initialized on a single line by separating them with a comma (e.g., `int x = 5, y = 10;`).');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Variables', 'What is the "scope" of a local variable declared inside a specific block of code, such as an `if` statement or a `for` loop?', 'It is accessible anywhere within the entire class.', 'It is accessible anywhere within the method that contains the block.', 'It is accessible only within the specific curly braces `{}` where it was declared.', 'It is accessible globally across the entire namespace.', 'C', 'C# utilizes block-level scope. A variable declared inside a block of code (enclosed by `{}`) only exists within that block. Once the execution exits those braces, the variable is immediately destroyed and out of scope.');