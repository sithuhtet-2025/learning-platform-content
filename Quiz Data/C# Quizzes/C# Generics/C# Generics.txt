INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Generics', 'What are the two primary benefits of using Generics in C# collections compared to the older, non-generic collections like ArrayList?', 'Smaller memory footprint on the hard drive and faster compilation times.', 'Compile-time type safety and improved performance by avoiding boxing/unboxing for value types.', 'Automatic memory management and prevention of memory leaks.', 'The ability to store unmanaged code pointers and execute them safely.', 'B', 'Generics provide strict compile-time type checking, preventing runtime invalid cast exceptions. Furthermore, by specifying the exact type (like int), they completely eliminate the massive performance overhead of boxing and unboxing value types that occurs when using collections that store objects.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Generics', 'Which of the following syntaxes correctly defines a generic method in C#?', 'public T void Process(T item) { }', 'public void Process<T>(T item) { }', 'public void<T> Process(T item) { }', 'public void Process(T item)<T> { }', 'B', 'A generic method is defined by placing the type parameter enclosed in angle brackets (<T>) immediately after the method name and before the parameter list.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Generics', 'In a generic class definition `public class Repository<T> where T : class`, what does the `where T : class` constraint enforce?', 'T must be a primitive value type.', 'T must be an interface.', 'T must be a reference type (like a class, interface, delegate, or array).', 'T must have a parameterless constructor.', 'C', 'The `where T : class` constraint restricts the generic type parameter to be a reference type. Value types (like int, bool, or custom structs) cannot be used as the type argument when this constraint is applied.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Generics', 'What does the generic constraint `where T : new()` require of the type argument?', 'The type must be instantiated inside the generic class using the new keyword.', 'The type must be a newly created class in the current assembly.', 'The type must be a struct (value type).', 'The type must expose a public parameterless constructor.', 'D', 'The `new()` constraint ensures that the compiler allows the generic class to instantiate objects of type T using `new T()`. To guarantee this works, the type argument provided must have a public, parameterless constructor.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Generics', 'If you have a generic class `public class Cache<T> { public static int Count; }`, what happens if you increment `Count` in `Cache<int>` and then read `Count` from `Cache<string>`?', 'They both read the exact same static memory address, so reading from Cache<string> reflects the increment.', 'A compile-time error occurs because generic classes cannot have static fields.', 'Each closed constructed type (Cache<int> and Cache<string>) gets its own completely separate static field.', 'An InvalidCastException is thrown at runtime.', 'C', 'In .NET, static members of generic classes are shared only among instances of the exact same closed constructed type. `Cache<int>` and `Cache<string>` are treated as two entirely different types by the CLR, meaning they have separate `Count` fields in memory.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Generics', 'How do you safely assign the default value of a generic type `T` to a variable when you don''t know if `T` is a reference type or a value type?', 'T myVar = null;', 'T myVar = 0;', 'T myVar = new T();', 'T myVar = default(T);', 'D', 'Because `T` could be a reference type (where the default is null) or a value type (where the default is zero-initialized memory), you must use `default(T)` or simply `default` (in modern C#) to instruct the compiler to assign the correct default value automatically.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Generics', 'Which keyword is used on a generic type parameter in an interface (e.g., `IEnumerable<out T>`) to specify covariance, allowing you to assign an `IEnumerable<string>` to an `IEnumerable<object>` variable?', 'in', 'out', 'ref', 'covariant', 'B', 'The `out` keyword marks a generic type parameter as covariant. Covariance allows a more derived type (like string) to be used where a less derived type (like object) is expected, but it strictly restricts the generic type parameter to only be used in output positions (like return values) to ensure type safety.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Generics', 'Which of the following statements about generic types is true?', 'You can overload a class name by having multiple generic classes with the same name but different numbers of type parameters (e.g., Tuple<T> and Tuple<T1, T2>).', 'Generic type parameters cannot be used as return types for methods.', 'Enums in C# can be generic.', 'Generics were introduced in C# 1.0.', 'A', 'In C#, you can overload types based on their arity (the number of generic type parameters). Therefore, `class MyClass<T>` and `class MyClass<T1, T2>` can perfectly coexist in the same namespace as distinct types.');
