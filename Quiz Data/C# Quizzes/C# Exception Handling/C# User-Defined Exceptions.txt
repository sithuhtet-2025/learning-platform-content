INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# User-Defined Exceptions', 'According to modern C# architectural best practices, which class must you inherit from when creating a custom, user-defined exception?', 'System.SystemException', 'System.ApplicationException', 'System.Exception', 'System.Error', 'C', 'Modern .NET guidelines explicitly dictate that all custom exceptions should inherit directly from the base `System.Exception` class (or a more specific derivative like `InvalidOperationException`). The legacy `ApplicationException` is officially deprecated as a base class.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# User-Defined Exceptions', 'What is the universally accepted, standard naming convention for custom exception classes in the .NET ecosystem?', 'They must start with the prefix "Error".', 'They must end with the suffix "Exception".', 'They must be named in all uppercase letters.', 'They must begin with an underscore.', 'B', 'To maintain structural consistency and immediate discoverability across the entire .NET ecosystem, all custom exception classes should end with the "Exception" suffix (e.g., `InvalidTransactionException`).');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# User-Defined Exceptions', 'When properly implementing a custom exception, which of the following is considered an architectural requirement regarding constructors?', 'Provide exactly one constructor that takes a string message.', 'Provide at least three standard constructors: parameterless, one taking a string message, and one taking a string message and an inner exception.', 'Never provide a parameterless constructor to force developers to write explicit error messages.', 'Constructors are not allowed in custom exception classes.', 'B', 'A robust, production-ready custom exception should always implement the three standard base constructors. This ensures it integrates perfectly with the CLR''s exception handling, debugging infrastructure, and logging frameworks.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# User-Defined Exceptions', 'What is the primary architectural advantage of creating a user-defined exception instead of simply throwing a generic `System.Exception` with a custom string message?', 'Custom exceptions execute much faster in the CLR than base exceptions.', 'Custom exceptions automatically bypass local `catch` blocks and go straight to global handlers.', 'Custom exceptions allow you to strongly type the error and include specialized data properties (like an `ErrorCode` or `TransactionId`) for programmatic error handling.', 'Generic exceptions cannot be caught or logged in modern C#.', 'C', 'While you can put anything in a string message, string parsing is highly brittle. Custom exceptions allow you to add strongly-typed properties (e.g., `ex.TransactionId`), allowing the `catch` block to make precise, programmatic decisions based on that specific data.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# User-Defined Exceptions', 'If your custom business exception is wrapping a lower-level database error, how do you pass that original error into your custom exception to preserve the stack trace?', 'Assign the database error to a custom string property.', 'Pass the database error into the custom exception''s constructor so it can be passed down to the base class''s `InnerException` parameter.', 'Use the `throw;` keyword inside the custom exception''s constructor.', 'It is impossible to preserve the original error when creating a custom exception.', 'B', 'When wrapping exceptions to hide implementation details from higher layers, you should catch the low-level error (e.g., `SqlException`), instantiate your custom exception, and pass the `SqlException` into your constructor as the `innerException`, linking the two stack traces together.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# User-Defined Exceptions', 'Which keyword is strictly required to trigger and propagate a newly instantiated custom exception in C#?', 'raise', 'yield', 'return', 'throw', 'D', 'In C#, the `throw` keyword is used to signal the occurrence of an anomalous situation (an exception) during program execution. For a custom exception, the syntax is typically `throw new MyCustomException("Error message");`.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# User-Defined Exceptions', 'If you have a `try` block and you need to catch both your custom `InvalidTransactionException` and a general `System.Exception`, in what order must the `catch` blocks be arranged?', 'The general `Exception` must be caught first, followed by `InvalidTransactionException`.', 'The specific `InvalidTransactionException` must be caught first, followed by the general `System.Exception`.', 'The order does not matter; the CLR routes the exception dynamically.', 'You cannot catch a custom exception and a general exception originating from the same `try` block.', 'B', 'Catch blocks must always be ordered from most specific to least specific. If the general `Exception` block is placed first, it will indiscriminately catch everything, making the specific `InvalidTransactionException` block permanently unreachable (which results in a compiler error).');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# User-Defined Exceptions', 'When is it architecturally appropriate to create a User-Defined Exception in a large-scale project?', 'For every single method to indicate successful execution flow.', 'To control the normal logical flow of the application (like checking if a user exists in the database).', 'Only when the error represents a specific business logic violation that requires specialized handling or data that standard .NET exceptions cannot provide.', 'To replace `NullReferenceException` globally.', 'C', 'Creating custom exceptions adds maintenance overhead. They should be reserved exclusively for specific, domain-level error conditions (like an `InsufficientFundsException`) that higher-level application code needs to explicitly catch and handle differently than standard system-level crashes.');