INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Jagged Array in C#', 'Which of the following is the correct syntax to declare a jagged array of integers in C#?', 'int[,] jaggedArray;', 'int[][] jaggedArray;', 'int[2][] jaggedArray;', 'int[][] jaggedArray = new int[,];', 'B', 'A jagged array, which is an array of arrays, is declared using multiple sets of empty square brackets (e.g., int[][]). Option A declares a multidimensional rectangular array.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Jagged Array in C#', 'When initially instantiating the top level of a jagged array, which dimensions must be specified?', 'Both the number of rows and the number of columns (e.g., new int[3][4]).', 'Only the second dimension must be specified (e.g., new int[][4]).', 'Only the first dimension, representing the number of sub-arrays, must be specified (e.g., new int[3][]).', 'No dimensions need to be specified; it scales dynamically.', 'C', 'When instantiating a jagged array, you must specify the size of the top-level array (the number of rows/sub-arrays). The inner arrays must be instantiated separately because they can have varying lengths.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Jagged Array in C#', 'Given the jagged array initialization: `int[][] scores = new int[2][]; scores[0] = new int[] { 90, 85 }; scores[1] = new int[] { 80, 75, 70 };`, how do you access the value 75?', 'scores[1, 1]', 'scores[1][1]', 'scores[2][2]', 'scores[0][1]', 'B', 'Jagged arrays are accessed sequentially using separate bracket pairs. `scores[1]` accesses the second inner array, and the subsequent `[1]` accesses the second element within that specific inner array, which is 75.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Jagged Array in C#', 'Internally, how is a jagged array structured in C# memory?', 'As a single contiguous block of memory on the stack.', 'As a single contiguous block of memory on the managed heap.', 'As a one-dimensional array of references, where each reference points to a separate array object on the heap.', 'As a linked list of array objects.', 'C', 'A jagged array is an array of arrays. The top-level array is simply an array of references. Each element points to a completely separate array object allocated elsewhere on the managed heap, which is why inner arrays can have different lengths.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Jagged Array in C#', 'Consider `int[][] arr = new int[3][];` where the sub-arrays are later initialized with varying lengths of 2, 5, and 1. What does `arr.Length` return?', '8', '3', '5', 'An exception is thrown because the sub-array lengths are inconsistent.', 'B', 'Calling `.Length` on the top-level jagged array variable returns the number of elements in that specific array (the number of sub-arrays), which is 3. To get the length of the individual rows, you must query them directly, like `arr[0].Length`.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Jagged Array in C#', 'What is a primary difference between a jagged array (`int[][]`) and a multidimensional rectangular array (`int[,]`) in C#?', 'Jagged arrays can only store strings, while rectangular arrays store value types.', 'Jagged arrays force all inner arrays to have the same length, while rectangular arrays do not.', 'Rectangular arrays allocate a single continuous memory block, while jagged arrays are arrays of references to other arrays.', 'There is no memory or structural difference; they are just different syntaxes for the same underlying IL type.', 'C', 'A rectangular array (`[,]`) allocates one single, contiguous block of memory for all elements. A jagged array (`[][]`) allocates an array of pointers, each pointing to a separate, independently allocated array memory block.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Jagged Array in C#', 'If you instantiate a jagged array as `int[][] matrix = new int[5][];`, what is the default value of `matrix[0]` before it is explicitly initialized?', '0', 'null', 'An empty array (new int[0])', 'Undefined', 'B', 'Because a jagged array is an array of arrays, and arrays are reference types in C#, the elements of the top-level array default to `null` until they are explicitly assigned to a new array instance.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Jagged Array in C#', 'When using a nested `for` loop to iterate through a jagged array `arr`, which property should be used in the inner loop''s condition to prevent an `IndexOutOfRangeException`?', 'arr.GetLength(1)', 'arr.Length', 'arr[i].Length (where i is the outer loop index)', 'arr.Rank', 'C', 'Because each inner array in a jagged array can have a different length, you must query the `Length` property of the specific inner array currently being iterated over (e.g., `arr[i].Length`). `GetLength(1)` is used for rectangular arrays.');