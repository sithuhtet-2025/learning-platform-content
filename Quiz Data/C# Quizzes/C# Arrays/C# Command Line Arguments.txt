INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Command Line Arguments', 'Which of the following is a standard, valid signature for the Main method to accept command-line arguments in a C# console application?', 'public void Main(List<string> args)', 'static void Main(string[] args)', 'static int Main(string args)', 'public static void Main(CommandArgs args)', 'B', 'In a standard C# application, the entry point Main method accepts command-line arguments as an array of strings (string[] args). The method must also be static.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Command Line Arguments', 'If a C# console application is executed from the terminal using the command `MyApp.exe -v config.json`, what is the value of `args[0]` inside the `static void Main(string[] args)` method?', 'MyApp.exe', '-v', 'config.json', 'null', 'B', 'Unlike C or C++, the args array passed directly to the Main method in C# does not include the executable name. The first element (args[0]) is the first argument passed after the executable name.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Command Line Arguments', 'When using top-level statements in modern C# (C# 9.0 and later) where the explicit Main method is omitted, how can you access command-line arguments?', 'By calling Console.GetArguments().', 'By using the implicitly available `args` variable.', 'Command-line arguments are not supported in top-level statements.', 'By explicitly declaring `string[] args;` at the top of the file.', 'B', 'When using top-level statements, the C# compiler automatically generates a Main method behind the scenes and provides an implicitly typed `args` (string array) variable that you can use directly in your global code.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Command Line Arguments', 'What is the primary difference between accessing arguments via the `args` parameter in the Main method and using `Environment.GetCommandLineArgs()`?', 'Environment.GetCommandLineArgs() returns a single space-separated string, while args is an array.', 'The first element returned by Environment.GetCommandLineArgs() is the executable file path, whereas the args array in Main only contains the passed arguments.', 'Environment.GetCommandLineArgs() can only be called from within the Main method.', 'There is no difference; they return the exact same array in memory.', 'B', 'Environment.GetCommandLineArgs() returns an array where the zeroth element (index 0) is the path and name of the executing assembly, and subsequent elements are the actual arguments. The args parameter in Main strictly contains the arguments.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Command Line Arguments', 'Command-line arguments are passed into a C# application as an array of strings. If you pass the number 42 as an argument, how must you process it to use it in mathematical operations?', 'It is automatically unboxed to an integer by the CLR.', 'You must cast it directly using (int)args[0].', 'You must parse it using methods like int.Parse(args[0]) or Convert.ToInt32(args[0]).', 'You cannot pass numeric values; command-line arguments only support text manipulation.', 'C', 'Because all command-line arguments are received natively as strings, any numeric input must be explicitly parsed or converted into a numeric data type (e.g., using int.Parse or int.TryParse) before performing any mathematical operations.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Command Line Arguments', 'How do you pass a single command-line argument that contains spaces (e.g., a file path like `C:\My Documents\file.txt`) to a C# application so it is not split into multiple array elements?', 'Replace spaces with underscores before passing the argument.', 'Enclose the entire argument in double quotes (").', 'Enclose the entire argument in single quotes ('').', 'Escape each space using a backslash (\ ).', 'B', 'The operating system''s command-line parser uses spaces as delimiters to separate arguments. To pass a string containing spaces as a single argument, it must be enclosed in double quotes (e.g., "C:\My Documents\file.txt").');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Command Line Arguments', 'What is the most robust way to ensure that at least one command-line argument has been provided before attempting to access `args[0]`, avoiding a runtime crash?', 'Check if `args == null`.', 'Check if `args[0] != ""`.', 'Check if `args.Length > 0`.', 'Use a try-catch block expecting a NullReferenceException.', 'C', 'The args array is never null in C#, even if no arguments are passed; it simply initializes with a length of 0. Checking `args.Length > 0` is the standard, safest way to verify that arguments exist before accessing array indices, preventing an IndexOutOfRangeException.');