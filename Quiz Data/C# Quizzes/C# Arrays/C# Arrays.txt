INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Arrays', 'Which of the following statements correctly declares and initializes a single-dimensional integer array in C#?', 'int[] numbers = new int(3) { 1, 2, 3 };', 'int numbers[] = new int[3] { 1, 2, 3 };', 'int[] numbers = new int[] { 1, 2, 3 };', 'int[3] numbers = { 1, 2, 3 };', 'C', 'In C#, the array brackets go after the type, not the variable name. The new int[] syntax correctly infers the size from the initializer list, or allows explicit sizing like new int[3].');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Arrays', 'What are the default values of the elements when an array is instantiated as `bool[] flags = new bool[3];`?', 'null', 'false', 'true', 'Undefined (Compilation error if accessed before assignment)', 'B', 'When an array is instantiated using the new keyword, its elements are automatically initialized to the default value of the element type. For boolean types, the default value is false.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Arrays', 'How do you correctly declare a jagged array (an array of arrays) of integers in C#?', 'int[,] jaggedArray;', 'int[][] jaggedArray;', 'int[A,B] jaggedArray;', 'Array<Array<int>> jaggedArray;', 'B', 'A jagged array is declared using multiple sets of brackets [][]. Option A ([,]) declares a two-dimensional rectangular array, which allocates a single contiguous block of memory.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Arrays', 'Which of the following correctly instantiates a jagged array where the first row has 2 elements and the second row has 3 elements?', 'int[][] arr = new int[2, 3];', 'int[][] arr = new int[2][]; arr[0] = new int[2]; arr[1] = new int[3];', 'int[][] arr = new int[][2, 3];', 'int[][] arr = new int[2][3];', 'B', 'Jagged arrays must have their top-level array instantiated first (new int[2][]), and then each inner array must be instantiated individually, as they are independent arrays that can have varying lengths.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Arrays', 'Given the 2D rectangular array `int[,] grid = { { 1, 2 }, { 3, 4 }, { 5, 6 } };`, what is the value of `grid[1, 1]`?', '2', '3', '4', '6', 'C', 'C# arrays are zero-indexed. The expression grid[1, 1] accesses the second row (index 1) and the second column (index 1) of that row, which corresponds to the value 4.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Arrays', 'Which of the following implicitly typed array declarations is valid in C#?', 'var arr = { 1, 2, 3 };', 'var arr = new[] { 1, 2.5, "three" };', 'var arr = new[] { 1, 2, 3 };', 'var[] arr = new int[] { 1, 2, 3 };', 'C', 'To implicitly type an array, you must use the new[] syntax. The compiler must be able to infer a single best type from the initializer list, making Option B invalid because there is no implicit conversion between int, double, and string.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Arrays', 'What happens if you attempt to modify the iteration variable inside a foreach loop iterating over an `int[]` array (e.g., `foreach(int item in arr) { item = item * 2; }`)?', 'The original array elements are multiplied by 2.', 'A runtime exception is thrown.', 'A compile-time error occurs.', 'The loop executes, but the array remains unchanged because item is a copy.', 'C', 'In C#, the iteration variable in a foreach loop over an array or standard collection is fundamentally read-only. Attempting to reassign or modify it directly results in a compile-time error.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Arrays', 'What exception is thrown when attempting to access an array element with an index that is less than zero or greater than or equal to the array''s length?', 'NullReferenceException', 'ArgumentOutOfRangeException', 'IndexOutOfRangeException', 'InvalidOperationException', 'C', 'Attempting to access an index outside the bounds of an array''s allocated memory throws an IndexOutOfRangeException at runtime. The CLR enforces bounds checking on all array accesses to ensure type and memory safety.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Arrays', 'Which of the following statements accurately describes memory allocation for arrays in C#?', 'Arrays of value types are stored on the stack, while arrays of reference types are stored on the heap.', 'All arrays are reference types and their memory blocks are allocated on the managed heap, regardless of the element type.', 'Arrays are value types and are always allocated on the stack.', 'Arrays are allocated dynamically in unmanaged memory to ensure contiguous storage.', 'B', 'In C#, an array is inherently an object and therefore a reference type. The array instance itself is always allocated on the managed heap. If it is an array of value types (like int[]), the actual values are stored contiguously directly within that heap object.');