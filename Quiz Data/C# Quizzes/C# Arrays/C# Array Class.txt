INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Array Class', 'In C#, all standard arrays implicitly inherit from which fundamental base class?', 'System.Collections.Generic.List', 'System.Array', 'System.Object (directly)', 'System.Collections.ArrayList', 'B', 'In C#, every array type implicitly inherits from the abstract base class System.Array, which provides all arrays with properties like Length and methods like Sort() and Copy().');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Array Class', 'What does the Rank property of the C# Array class return?', 'The total number of elements across all dimensions of the array.', 'The number of dimensions of the array.', 'The highest numerical value stored within the array.', 'The memory address rank of the array''s first element.', 'B', 'The Array.Rank property returns an integer representing the number of dimensions of the array. For a single-dimensional array, it returns 1; for a 2D array (e.g., int[,]), it returns 2.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Array Class', 'What is the primary difference between the Array.Clone() and Array.Copy() methods in C#?', 'Clone() creates a deep copy of the elements, while Copy() creates a shallow copy.', 'Clone() creates a new array object containing a shallow copy, while Copy() copies elements into an existing, pre-allocated array.', 'Copy() can only copy elements of the same type, while Clone() can cast types automatically.', 'There is no difference; they are synonymous methods for duplicating arrays.', 'B', 'Array.Clone() returns a newly allocated object representing a shallow copy of the original array. Array.Copy() requires you to pass in a pre-existing destination array to copy a specified number of elements into it.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Array Class', 'Which interface must the elements of an array implement for the static Array.Sort() method to successfully sort them without throwing a runtime exception?', 'ICloneable', 'IEnumerable', 'IComparable', 'IQueryable', 'C', 'The default Array.Sort() method relies on the array''s elements implementing the IComparable interface to determine the relative order of the items. If the types do not implement it and no custom IComparer is provided, an InvalidOperationException is thrown.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Array Class', 'What happens to the elements of an integer array when the Array.Clear() method is called on it?', 'The array is resized to a length of 0.', 'The elements are removed from memory and the array reference becomes null.', 'The elements within the specified range are set to 0, the default value for integers.', 'An exception is thrown because arrays are of fixed size and cannot be cleared.', 'C', 'Array.Clear() does not change the size of the array or destroy it. Instead, it resets the elements within a specified range to the default value of their type (e.g., 0 for integers, false for booleans, null for references).');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Array Class', 'What does the static method Array.IndexOf(Array, Object) return if the specified object is not found within the array?', 'null', '0', '-1', 'An IndexOutOfRangeException is thrown.', 'C', 'Array.IndexOf() returns the zero-based index of the first occurrence of a value in a one-dimensional array. If the value is not found, it returns the lower bound of the array minus 1, which is always -1 for standard zero-indexed C# arrays.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Array Class', 'Consider a two-dimensional array initialized as `int[,] matrix = new int[3, 4];`. What will `matrix.Length` and `matrix.GetLength(0)` return, respectively?', '3 and 4', '12 and 3', '7 and 3', '12 and 4', 'B', 'The Length property returns the total number of elements across all dimensions (3 * 4 = 12). The GetLength(int dimension) method returns the number of elements in a specific dimension (dimension 0 has 3 elements).');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Array Class', 'How can you dynamically resize a standard C# array after it has been instantiated?', 'By directly modifying the Array.Length property.', 'By using the Array.Resize(ref array, newSize) method, which creates a new array and updates the reference.', 'By using the Array.Append() method to add elements to the end of the memory block.', 'Standard C# arrays cannot be resized; you must use a List<T> instead.', 'B', 'While an array instance itself has a strictly fixed size in memory, the Array.Resize() method allocates a completely new array of the specified size, copies the elements from the old array, and updates the reference to point to the new array.');