INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Multidimensional Arrays in C#', 'Which of the following correctly declares and initializes a 2D rectangular array of integers in C#?', 'int[][] matrix = new int[3, 3];', 'int[,] matrix = new int[3, 3];', 'int[2] matrix = new int[3][3];', 'int[3, 3] matrix = new int[,];', 'B', 'In C#, a multidimensional rectangular array is declared using a comma within a single set of brackets [,]. Option A uses the syntax for a jagged array, which is an array of arrays.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Multidimensional Arrays in C#', 'Given the 2D array declaration `int[,] grid = { { 1, 2, 3 }, { 4, 5, 6 } };`, what is the value of `grid[1, 2]`?', '2', '6', '5', 'An IndexOutOfRangeException is thrown.', 'B', 'Multidimensional arrays are zero-indexed. The expression `grid[1, 2]` accesses the second row (index 1) and the third column (index 2) of that row, which corresponds to the value 6.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Multidimensional Arrays in C#', 'If you declare a 3D array as `int[,,] cube = new int[3, 4, 5];`, what will the `cube.Rank` property evaluate to?', '60', '3', '12', '1', 'B', 'The `Rank` property of the `System.Array` base class returns an integer representing the number of dimensions in the array. For a 3D array denoted by [,,], the rank is 3.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Multidimensional Arrays in C#', 'For a multidimensional array `int[,] matrix = new int[4, 5];`, which method should be used to dynamically retrieve the number of columns (the size of the second dimension)?', 'matrix.Length', 'matrix.GetLength(1)', 'matrix.GetLength(2)', 'matrix.GetUpperBound(0)', 'B', 'The `GetLength(int dimension)` method returns the number of elements in the specified dimension. Since dimensions are zero-indexed, `GetLength(1)` returns the size of the second dimension, which is 5.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Multidimensional Arrays in C#', 'How are multidimensional (rectangular) arrays laid out in memory in C# compared to jagged arrays?', 'As an array of pointers, where each pointer references a separate array on the heap.', 'As a single, contiguous block of memory allocated on the managed heap.', 'As a linked list of arrays to allow dynamic resizing of individual rows.', 'They are always allocated directly on the stack for performance.', 'B', 'Unlike jagged arrays (which allocate an array of references pointing to separate memory blocks), multidimensional rectangular arrays in C# allocate a single, continuous block of memory for all elements, improving spatial locality.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Multidimensional Arrays in C#', 'What happens when you use a `foreach` loop to iterate over a 2D array `int[,] grid = new int[2, 2] { { 1, 2 }, { 3, 4 } };`?', 'The compiler throws an error because `foreach` only supports single-dimensional arrays.', 'It iterates through the array returning 1D arrays representing each row.', 'It iterates through every element in a single flat sequence (1, 2, 3, 4) in row-major order.', 'It iterates through every element in column-major order (1, 3, 2, 4).', 'C', 'The C# `foreach` statement fully supports multidimensional arrays. It flattens the traversal, iterating through every single element in row-major order (reading across the first row, then the second row, etc.).');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Multidimensional Arrays in C#', 'If you have a multidimensional array initialized as `string[,,] data = new string[2, 3, 4];`, what integer value will the `data.Length` property return?', '3', '9', '24', '4', 'C', 'The `Length` property of an array evaluates to the total number of elements contained across all dimensions of the array. For a 2 x 3 x 4 array, the total number of elements is 24.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Multidimensional Arrays in C#', 'Is it possible to implicitly type a multidimensional array using the `var` keyword in C#?', 'Yes, using syntax like `var matrix = new[,] { { 1, 2 }, { 3, 4 } };`.', 'Yes, but only if it is a single-dimensional array; 2D arrays require explicit typing.', 'No, the `var` keyword cannot be used with arrays of any kind.', 'Yes, using syntax like `var matrix = { { 1, 2 }, { 3, 4 } };`.', 'A', 'Implicitly typed multidimensional arrays are allowed in C# using the `new[,]` syntax. The compiler analyzes the initializer block to infer the underlying data type and the lengths of each dimension automatically.');