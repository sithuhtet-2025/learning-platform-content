INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Enum', 'What is the primary architectural purpose of an `enum` (enumeration) in C#?', 'To create a collection of immutable string variables.', 'To define a distinct value type consisting of a strongly typed set of named integral constants.', 'To create an array of dynamically typed objects.', 'To define a class that cannot be inherited.', 'B', 'An `enum` provides a way to define a set of named integral constants. This dramatically improves code readability and type safety by replacing arbitrary "magic numbers" with descriptive, strongly typed names.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Enum', 'By default, what is the underlying numeric data type used by the C# compiler to represent enum values?', 'byte', 'short', 'int', 'long', 'C', 'Unless explicitly specified otherwise by the developer, the default underlying integral type for every enum in C# is the 32-bit signed integer (`int`).');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Enum', 'If you need to optimize memory and strictly restrict an enum to hold a maximum of 255 distinct values, how do you change its underlying type in the declaration?', 'public enum Status(byte) { }', 'public enum Status <byte> { }', 'public enum Status : byte { }', 'public byte enum Status { }', 'C', 'You can explicitly declare the underlying integral type of an enum using the colon syntax (e.g., `enum Status : byte { }`). You can use any integral type except `char` (e.g., byte, sbyte, short, ushort, int, uint, long, ulong).');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Enum', 'If you define `enum Days { Sun, Mon, Tue }` without assigning explicit numerical values to the members, what integer value does `Mon` represent by default?', '0', '1', '2', '-1', 'B', 'By default, the Common Language Runtime assigns the first enumerator the value 0, and the value of each successive enumerator is implicitly increased by 1. Therefore, Sun is 0, Mon is 1, and Tue is 2.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Enum', 'What is the primary architectural benefit of applying the `[Flags]` attribute to an enum definition?', 'It encrypts the enum values securely in memory.', 'It allows the enum to be serialized into a JSON string automatically.', 'It indicates that the enum can be treated as a bit field, allowing multiple discrete values to be combined using bitwise operators.', 'It forces the enum values to start at 1 instead of 0.', 'C', 'The `[Flags]` attribute signals that the enum represents a collection of flags rather than a single mutually exclusive value. This allows developers to combine values (e.g., `Permissions.Read | Permissions.Write`) and evaluate them cleanly using bitwise logic.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Enum', 'Which method is the safest, most modern architectural approach to convert a string value (like "Active") into its corresponding enum object without throwing an exception if the string is invalid?', 'Enum.Parse()', 'Enum.Convert()', 'Enum.TryParse()', 'Convert.ToEnum()', 'C', '`Enum.TryParse()` attempts to parse the string into the enum type. It returns a boolean indicating success or failure and provides the result via an `out` parameter, completely avoiding the heavy performance penalty of catching an exception for invalid inputs.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Enum', 'How do you correctly convert an instantiated enum value back into its underlying raw numeric integer value?', 'By calling `enumValue.ToInt32()`.', 'By explicitly casting the enum value to the numeric type (e.g., `(int)myEnum`).', 'By calling `enumValue.Parse()`.', 'The C# compiler performs this conversion implicitly without any explicit syntax.', 'B', 'Because enums are strongly typed to prevent accidental mathematics, implicit conversions to integers are strictly forbidden. You must explicitly cast the enum to its underlying type to extract the raw numeric payload.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Enum', 'Why is using an enum architecturally superior to using a static class filled with `public const int` variables for defining discrete states (like Pending, Approved, Rejected)?', 'Enums are stored on the managed heap, while consts are stored on the stack.', 'Enums execute faster than constant integers at runtime.', 'Enums provide strong compile-time type safety, physically preventing developers from passing invalid integer values to methods that expect a specific state.', 'Enums can store complex objects, while const integers cannot.', 'C', 'If a method expects a raw integer, a developer could accidentally pass `999` instead of a valid state like `1`. By forcing the method to expect a strongly typed `Status` enum, the compiler strictly enforces that only valid `Status` members are passed, eliminating a massive class of runtime errors.');