INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Out Parameter', 'What is the primary purpose of the ''out'' parameter modifier in C#?', 'To pass an initialized variable by value.', 'To pass an argument by reference, strictly requiring the called method to assign a value to it before returning.', 'To make a parameter strictly read-only inside the method.', 'To return multiple values from an async method.', 'B', 'The ''out'' keyword is used to pass arguments by reference. Unlike ''ref'', the variable does not need to be initialized before being passed, but the method is strictly required to assign it a valid value before it exits.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Out Parameter', 'What happens if a method defines an ''out'' parameter but fails to assign a value to it along one of its execution paths?', 'The variable retains its original value.', 'The compiler assigns a default value automatically.', 'A compile-time error is generated.', 'A runtime NullReferenceException is thrown.', 'C', 'The C# compiler enforces strict definite assignment rules for ''out'' parameters. If there is any code path in the method that reaches a return statement without assigning a value to the ''out'' parameter, the code will not compile.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Out Parameter', 'Which of the following accurately describes the difference in initialization requirements between ''ref'' and ''out'' parameters in C#?', '''ref'' requires the variable to be initialized before the method call; ''out'' does not.', '''out'' requires the variable to be initialized before the method call; ''ref'' does not.', 'Both require initialization before the call.', 'Neither requires initialization before the call.', 'A', 'A variable passed as ''ref'' must be initialized by the caller before it is passed to the method. A variable passed as ''out'' does not need prior initialization, as the method itself takes full responsibility for initializing it.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Out Parameter', 'Introduced in C# 7.0, which syntax allows you to declare an ''out'' variable inline at the exact point of the method call?', 'int.TryParse("123", int result out);', 'int.TryParse("123", out int result);', 'int.TryParse("123", new out int result);', 'var result = int.TryParse("123", out);', 'B', 'C# 7.0 introduced inline ''out'' variable declarations. You can declare the variable directly inside the argument list (e.g., ''out int result''), eliminating the need to declare it on a separate line beforehand, leading to cleaner code.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Out Parameter', 'Can you use the ''var'' keyword when declaring an inline ''out'' variable?', 'Yes, the compiler accurately infers the type based on the method signature.', 'No, you must explicitly state the type to ensure strict type safety.', 'Yes, but only if the ''dynamic'' keyword is also used.', 'No, ''var'' can only be used for local variables assigned via the ''='' operator.', 'A', 'You can absolutely use the implicitly typed ''var'' keyword with inline ''out'' declarations (e.g., ''int.TryParse("123", out var result);''). The compiler perfectly infers the correct type from the parameter definition in the method signature.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Out Parameter', 'What is the "TryParse" pattern, which heavily utilizes ''out'' parameters in the .NET framework?', 'A pattern where a method returns a string and outputs a boolean indicating success.', 'A pattern where a method returns a boolean indicating success or failure, and provides the actual parsed result via an ''out'' parameter.', 'A pattern used exclusively for database connection retries.', 'A pattern that forces a method to attempt execution three times before throwing an exception.', 'B', 'Methods like int.TryParse() or DateTime.TryParse() return a boolean (true if parsing succeeded, false if it failed) and use an ''out'' parameter to supply the successfully parsed value. This elegantly avoids the heavy performance cost of throwing exceptions on bad user input.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Out Parameter', 'Introduced in C# 7.0, what syntax should you use if you must call a method with an ''out'' parameter, but you intentionally want to ignore the returned value?', 'out null', 'out void', 'out ignore', 'out _ (a discard)', 'D', 'If you do not need the value produced by an ''out'' parameter, you can use a discard represented by an underscore (''out _''). This tells the compiler to intentionally ignore the assignment, saving memory and suppressing "unused variable" warnings.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Out Parameter', 'Are you permitted to use ''out'' parameters in methods marked with the ''async'' modifier?', 'Yes, asynchronous methods fully support ''out'' parameters.', 'Yes, but only if the ''out'' parameter is a reference type.', 'No, compiler errors will occur because asynchronous methods cannot have by-reference parameters.', 'Yes, but you must await the ''out'' parameter specifically.', 'C', 'Async methods and iterator blocks (using yield return) are compiled into state machines. Local variables might be hoisted to the heap to survive across await boundaries. Because ''out'' parameters require rigid pointers to the stack, they are strictly prohibited in async methods.');