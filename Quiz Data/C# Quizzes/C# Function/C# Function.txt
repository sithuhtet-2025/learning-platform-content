INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Function', 'In C# terminology, what is a function that is defined as a member of a class or struct called?', 'A routine', 'A procedure', 'A method', 'A delegate', 'C', 'In object-oriented programming with C#, functions that belong to a class or struct and operate on its data are officially referred to as methods.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Function', 'Which keyword must be used as the return type to specify that a C# method performs an action but does not return any data to the caller?', 'null', 'void', 'empty', 'none', 'B', 'The ''void'' keyword indicates that a method does not return a value. The method can still use a bare ''return;'' statement to exit early, but it cannot return a payload.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Function', 'When overloading methods in C#, which elements strictly constitute the method''s "signature" that the compiler uses to differentiate them?', 'The method name and its return type.', 'The method name and the number, types, and modifiers (ref/out) of its parameters.', 'The parameter names and their default values.', 'The method name, parameter types, and the access modifier (public/private).', 'B', 'In C#, a method signature consists only of its name and the number, types, and order of its parameters (including ref, in, and out modifiers). The return type and access modifiers are explicitly NOT part of the signature for overloading purposes.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Function', 'How do you correctly define an optional parameter in a C# method declaration so the caller can omit it?', 'By prefixing the parameter with the [Optional] attribute.', 'By placing the parameter at the beginning of the parameter list.', 'By assigning a compile-time constant default value to the parameter in the method signature (e.g., int delay = 1000).', 'By using the ''params'' keyword.', 'C', 'Optional parameters are created by assigning a default value directly in the parameter list. They must appear after all required parameters in the method signature to avoid ambiguity during method calls.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Function', 'What is the purpose of the ''params'' keyword when used in a method parameter list?', 'It allows the method to return multiple values simultaneously.', 'It allows the caller to pass a variable number of arguments of the specified type as a single array, or as a comma-separated list of values.', 'It forces all arguments to be passed by reference.', 'It securely encrypts the parameters in memory.', 'B', 'The ''params'' keyword lets a method accept a variable number of arguments. It must be a single-dimensional array and must be the absolute last parameter in the method declaration.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Function', 'Introduced in C# 6.0, what is the correct syntax for creating a concise, expression-bodied method that returns the multiplication of two integers?', 'public int Multiply(int x, int y) { => x * y; }', 'public int Multiply(int x, int y) => x * y;', 'public int Multiply(int x, int y) -> x * y;', 'public => int Multiply(int x, int y) return x * y;', 'B', 'Expression-bodied methods use the lambda arrow (=>) to define a method consisting of a single expression. It removes the need for curly braces {} and the explicit ''return'' keyword.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Function', 'If you have a method defined as `void Register(string username, int age, bool isAdmin)`, how can you use "Named Arguments" to call it while ignoring the parameter order?', 'Register("username" : "Alice", "age" : 30, "isAdmin" : true);', 'Register(age=30, isAdmin=true, username="Alice");', 'Register(age: 30, isAdmin: true, username: "Alice");', 'Register({age: 30, isAdmin: true, username: "Alice"});', 'C', 'Named arguments allow you to specify the value for a parameter by associating it with the parameter''s name followed by a colon (e.g., `age: 30`), freeing you from having to remember the exact order of parameters in the method signature.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Function', 'Introduced in C# 7.0, what is a "Local Function" in C#?', 'A function that is only accessible within the same physical .cs file.', 'A private function declared directly inside the body of another method, scoped exclusively to that parent method.', 'A function that executes entirely on the local CPU cache.', 'A global function that ignores namespace boundaries.', 'B', 'Local functions are private methods nested inside another member (like a method or property). They are extremely useful for organizing helper logic that is only relevant to the containing method, keeping the broader class namespace clean and encapsulated.');