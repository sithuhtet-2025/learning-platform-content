INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Switch Statement in C#', 'What is the primary architectural purpose of a `switch` statement in C#?', 'To loop through an array.', 'To replace all `if-else` statements.', 'To evaluate a single expression and branch execution to a specific `case` block that matches the expression''s value.', 'To handle runtime exceptions.', 'C', 'The `switch` statement is a control flow structure that evaluates a single match expression and transfers control to the `case` label that matches the evaluated value, offering a cleaner, more readable alternative to complex `if-else if` chains.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Switch Statement in C#', 'Unlike C or C++, what strict architectural rule does the C# compiler enforce regarding "fall-through" in `switch` statements?', 'C# requires the `fallthrough` keyword.', 'C# strictly forbids execution from implicitly falling through from one populated `case` block to the next; a jump statement like `break`, `return`, or `goto` is mandatory.', 'C# automatically falls through all cases until a `break` is found.', 'C# does not support `switch` statements.', 'B', 'To prevent common and dangerous logical bugs, C# prohibits implicit fall-through. If a `case` block contains executable code, it must explicitly terminate (usually with a `break` statement) before the next `case` begins.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Switch Statement in C#', 'How does C# handle multiple `case` labels stacked together with no code between them (e.g., `case 1: case 2: case 3: DoWork(); break;`)?', 'It throws a compile-time error.', 'It only executes `DoWork()` for `case 3`.', 'It allows the empty cases to implicitly fall through, effectively sharing the exact same execution block for all three values.', 'It executes `DoWork()` three times.', 'C', 'While C# strictly forbids fall-through for populated cases, it perfectly allows stacking completely empty `case` labels. This acts as a logical OR, routing multiple matching values to the exact same block of code without duplicating logic.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Switch Statement in C#', 'Introduced in C# 7.0, what powerful feature fundamentally transformed the `switch` statement beyond simple constant value matching?', 'Asynchronous cases.', 'Pattern Matching (allowing matching against types, properties, and structural shapes).', 'LINQ integration.', 'Lambda switch blocks.', 'B', 'Pattern matching revolutionized the `switch` statement. Instead of being restricted to simple primitive constants (like checking if an integer equals 5), developers can now switch on object types (e.g., `case string s:`), null values, and complex object shapes.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Switch Statement in C#', 'When using modern Pattern Matching in a `switch` statement, what keyword is used to add a supplementary boolean "guard clause" to a specific `case`?', 'if', 'where', 'when', 'and', 'C', 'The `when` keyword acts as a guard clause (e.g., `case int x when x > 0:`). The case will only be considered a successful match if the pattern type matches AND the boolean `when` condition evaluates to true.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Switch Statement in C#', 'Introduced in C# 8.0, what is a "switch expression"?', 'A switch statement used exclusively inside a while loop.', 'A highly concise, functional-style syntax that evaluates an expression and directly returns a value, rather than executing blocks of statements.', 'A switch statement that only accepts boolean expressions.', 'A compiler directive for switch optimization.', 'B', 'Switch expressions (`variable switch { pattern => result }`) provide a lightweight, expression-bodied alternative to traditional switch statements. They are designed to compute and return a single value based on pattern matching with minimal boilerplate code.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Switch Statement in C#', 'In a modern C# 8.0+ "switch expression", what character is used to represent the default or "discard" arm (the exact equivalent of the traditional `default:` case)?', 'default', '*', '_ (underscore)', 'null', 'C', 'In functional-style switch expressions, the underscore `_` acts as the universal discard pattern. It behaves exactly like the traditional `default:` case, catching any values that did not match any of the preceding patterns.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('Switch Statement in C#', 'What is the purpose of the `default` case in a traditional C# `switch` statement?', 'It executes first to set default variables.', 'It acts as a fallback block that executes only if the evaluated expression does not match any of the specified `case` labels.', 'It resets the switch expression to its default memory state.', 'It is mandatory and must be the first case.', 'B', 'The `default` case gracefully handles unmatched scenarios. It is entirely optional, and while it is traditionally placed at the very bottom of the switch block for readability, the C# compiler actually allows it to be placed anywhere within the block.');