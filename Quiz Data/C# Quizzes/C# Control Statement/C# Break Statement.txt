INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Break Statement', 'What is the primary function of the ''break'' statement in C#?', 'To pause the program execution for debugging.', 'To skip the current iteration of a loop and move to the next.', 'To immediately terminate the closest enclosing loop or switch statement and transfer control to the next statement following it.', 'To exit the entire method and return to the caller.', 'C', 'The ''break'' statement is used to abruptly exit the innermost loop (for, foreach, while, do) or switch block it is currently executing inside. Control is immediately transferred to the line of code directly following the terminated block.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Break Statement', 'In the context of a C# ''switch'' statement, why is the ''break'' keyword generally required at the end of every non-empty ''case'' block?', 'To speed up the evaluation of the switch expression.', 'Because C# strictly prohibits implicit "fall-through" from one case block to the next.', 'To return a value from the switch statement.', 'It is not required; cases fall through automatically by default.', 'B', 'Unlike C or C++, C# explicitly forbids execution from falling through from one case label to another if the case contains code. You must use a jump statement like ''break'', ''return'', or ''goto'' to explicitly exit the switch block to prevent compile-time errors.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Break Statement', 'If a ''break'' statement is executed inside a nested loop (a loop inside another loop), what is the immediate result?', 'It terminates both the inner and outer loops simultaneously.', 'It terminates only the inner loop, and execution resumes with the next iteration of the outer loop.', 'It causes a runtime exception.', 'It restarts the inner loop from the beginning.', 'B', 'The ''break'' statement is strictly context-aware. It only terminates the closest, innermost enclosing loop. The outer loop will continue its execution cycle normally.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Break Statement', 'What is the fundamental architectural difference between the ''break'' statement and the ''continue'' statement in a loop?', '''break'' exits the entire method, while ''continue'' exits the loop.', '''break'' skips the current iteration, while ''continue'' stops the loop completely.', '''break'' immediately terminates the entire loop, whereas ''continue'' only terminates the current iteration and forces the loop to evaluate its condition for the next cycle.', 'There is no difference; they can be used interchangeably.', 'C', 'While ''break'' completely destroys the loop''s execution cycle and moves past it, ''continue'' simply bypasses the remaining code inside the loop body for that specific iteration and jumps straight to the next evaluation phase of the loop.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Break Statement', 'Inside which of the following C# code structures is it syntactically INVALID to use a standalone ''break'' statement?', 'A ''foreach'' loop iterating over a List.', 'A standalone ''if'' statement that is not enclosed within any loop or switch block.', 'A ''while(true)'' infinite loop.', 'A ''switch'' statement evaluating a string.', 'B', 'The ''break'' statement can only be used inside iterative loops (for, foreach, while, do-while) or switch statements. Using it inside a standalone ''if'' statement results in a compiler error: "No enclosing loop out of which to break or continue."');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Break Statement', 'How is the ''break'' statement most commonly utilized in conjunction with a `while (true)` loop pattern?', 'To ensure the loop runs exactly once.', 'To provide a dynamically evaluated, conditional exit point from an otherwise infinite loop.', 'To bypass compile-time syntax checks.', 'To reset the loop''s internal counter.', 'B', 'A `while (true)` loop creates an intentional infinite loop. A ''break'' statement wrapped inside an `if` condition within the loop body is the standard architectural pattern to safely exit the loop once a specific runtime business condition is met.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Break Statement', 'What happens if you use a ''return'' statement instead of a ''break'' statement inside a loop?', 'The loop terminates, and the code immediately following the loop executes.', 'The compiler throws an error because ''return'' cannot be used in loops.', 'The entire method containing the loop immediately terminates, passing control (and optionally a value) back to the calling method.', 'The loop acts as if ''continue'' was called.', 'C', 'The ''return'' statement is much more drastic than ''break''. While ''break'' merely escapes the current loop block, ''return'' instantly aborts the entire method execution, skipping any remaining code in the method (except for ''finally'' blocks) and returning to the caller.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Break Statement', 'Unlike Java, C# does not support "labeled breaks" (e.g., `break outerLoop;`). If you are deeply nested inside three loops and need to immediately break out of all of them at once, which C# keyword is typically used as a workaround?', 'continue', 'goto', 'exit', 'yield', 'B', 'Because C# does not support labeled breaks, breaking out of multiple nested loops simultaneously often requires using a ''goto'' statement pointing to a label placed immediately after the outermost loop, or refactoring the complex loops into a separate method and using ''return''.');