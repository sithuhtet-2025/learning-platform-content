INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Continue Statement', 'What is the primary function of the ''continue'' statement in C#?', 'To pause the loop execution for a specified duration.', 'To immediately terminate the entire loop and transfer control to the next statement.', 'To bypass the remaining code in the current iteration of a loop and immediately proceed to the next iteration.', 'To exit the current method and return a value.', 'C', 'The ''continue'' statement halts the execution of the current loop iteration. It skips any remaining code inside the loop body for that specific cycle and immediately begins the next iteration of the loop.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Continue Statement', 'Inside which of the following C# structures is the use of the ''continue'' statement strictly considered valid?', 'Inside any block of code enclosed by curly braces.', 'Only within iteration statements (loops) such as ''for'', ''foreach'', ''while'', and ''do-while''.', 'Inside both loops and ''switch'' statements.', 'Inside standalone ''if'' statements.', 'B', 'The C# compiler restricts the ''continue'' keyword exclusively to iterative loops. Attempting to use it outside of a loop, such as in a standalone ''switch'' or ''if'' block, results in a compile-time error.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Continue Statement', 'What is the strict architectural difference between the ''continue'' statement and the ''break'' statement?', '''continue'' exits the entire method, while ''break'' exits the loop.', '''continue'' skips the current iteration and resumes the loop, whereas ''break'' completely terminates the loop and moves past it.', '''continue'' requires a boolean condition to execute, while ''break'' does not.', 'There is no difference; they compile to the exact same IL instructions.', 'B', 'While ''break'' destroys the loop''s execution cycle entirely, ''continue'' acts as an early reset for just the current iteration. The loop remains active and will proceed to evaluate its condition for the next cycle.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Continue Statement', 'In a standard ''for'' loop (e.g., `for (int i = 0; i < 10; i++)`), what happens immediately after a ''continue'' statement is executed?', 'Control jumps to the beginning of the loop body without incrementing ''i''.', 'The loop terminates completely.', 'Control jumps directly to the iterator statement (`i++`), executes it, and then evaluates the condition.', 'Control jumps directly to the condition (`i < 10`) without executing the iterator.', 'C', 'In a ''for'' loop, the ''continue'' statement transfers control straight to the loop''s iterator block (the increment/decrement step). This ensures that the loop counter is still updated properly before the condition is checked for the next cycle.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Continue Statement', 'If a ''continue'' statement is executed inside a deeply nested inner loop, how does it affect the outer enclosing loops?', 'It skips the current iteration of all enclosing loops.', 'It terminates the inner loop and continues the outer loop.', 'It has absolutely no effect on the outer loops; it strictly skips the current iteration of the closest enclosing inner loop only.', 'It restarts the outermost loop from the beginning.', 'C', 'Like the ''break'' statement, ''continue'' is strictly context-aware. It only applies to the closest, innermost iterative block that encloses it, leaving the execution flow of any outer loops completely undisturbed.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Continue Statement', 'Where does the control flow jump immediately after hitting a ''continue'' statement inside a ''while'' or ''do-while'' loop?', 'To the top of the loop body.', 'To the first line of code following the loop.', 'Directly to the loop''s boolean condition evaluation.', 'To the closest ''break'' statement.', 'C', 'Unlike a ''for'' loop which has a dedicated iterator block, a ''continue'' statement inside a ''while'' or ''do-while'' loop transfers execution control directly to the loop''s conditional expression to determine if the next iteration should proceed.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Continue Statement', 'What is a common risk or infinite loop trap when using the ''continue'' statement inside a ''while'' loop?', 'The ''continue'' statement automatically forces the condition to evaluate to true permanently.', 'Placing the ''continue'' statement before the manual increment/iterator variable update, causing the loop to evaluate the exact same state endlessly.', 'The ''continue'' statement causes the garbage collector to suspend the thread.', 'The compiler ignores ''continue'' statements in ''while'' loops during Release builds.', 'B', 'In a ''while'' loop, the developer is manually responsible for updating the iterator variable. If a ''continue'' statement triggers before the variable is updated, the loop skips the update and re-evaluates the same condition, instantly creating an infinite loop.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Continue Statement', 'According to Clean Code principles, why is the ''continue'' statement often utilized as a "guard clause" inside a loop?', 'To prevent the loop from ever executing more than once.', 'To drastically reduce code indentation and cyclomatic complexity by filtering out unwanted items early, avoiding massive nested ''if'' blocks.', 'To ensure variables are properly garbage collected.', 'To lock the loop iteration for thread safety.', 'B', 'Using ''continue'' at the top of a loop to filter out items that don''t require processing acts as a guard clause. It keeps the main "happy path" logic at a lower indentation level, making the loop significantly easier to read and maintain.');