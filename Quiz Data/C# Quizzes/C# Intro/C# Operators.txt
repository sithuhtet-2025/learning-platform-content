INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Operators', 'Which arithmetic operator is used in C# to return the remainder of an integer division operation?', '/', '*', '%', '\\', 'C', 'The modulo (or remainder) operator is represented by the percent sign (%). For example, 10 % 3 evaluates to 1, because 10 divided by 3 leaves a remainder of 1.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Operators', 'What is the critical performance difference between the conditional logical AND operator (&&) and the standard logical AND operator (&) in C#?', '&& only works with booleans, while & only works with integers.', '&& performs "short-circuit" evaluation, whereas & evaluates both operands regardless of the first operand''s value.', '&& is evaluated after &, due to operator precedence.', 'There is no difference; they compile to the exact same IL code.', 'B', 'The && operator short-circuits: if the left operand evaluates to false, the overall expression can never be true, so the right operand is completely skipped. The & operator always evaluates both sides, which wastes CPU cycles and can cause runtime exceptions if the right side relies on the left side being true (like a null check).');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Operators', 'What is the purpose of the null-coalescing operator (??) in C#?', 'It forces a nullable variable to become null.', 'It throws a NullReferenceException if the operand is null.', 'It returns the value of its left-hand operand if it isn''t null; otherwise, it evaluates the right-hand operand and returns its result.', 'It compares two objects to see if they both point to null.', 'C', 'The ?? operator is used to provide a default fallback value. For example, `string name = inputName ?? "Unknown";` will assign "Unknown" to the variable only if `inputName` is currently null.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Operators', 'Introduced in C# 6.0, how does the null-conditional operator (?.) prevent runtime application crashes?', 'It catches exceptions globally before they terminate the application.', 'It safely accesses members (properties or methods) of an object, immediately returning null if the object itself is null, instead of throwing a NullReferenceException.', 'It automatically instantiates a new object if the referenced object is null.', 'It acts as a ternary operator specifically for booleans.', 'B', 'The ?. operator (e.g., `customer?.Address?.City`) allows you to chain property accesses gracefully. If any object in the chain is null, the entire expression safely short-circuits and evaluates to null without throwing an exception.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Operators', 'Which of the following correctly demonstrates the syntax for the ternary (conditional) operator in C#?', 'condition ? true_value : false_value', 'condition : true_value ? false_value', 'condition => true_value | false_value', 'if condition then true_value else false_value', 'A', 'The ternary operator takes three operands. It evaluates a boolean condition; if true, it returns the expression immediately following the question mark (?). If false, it returns the expression following the colon (:).');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Operators', 'What behavior does the compound assignment operator (+=) exhibit when applied to a variable of type ''string''?', 'It adds the ASCII values of the characters together.', 'It attempts to parse the strings into integers and sum them.', 'It concatenates the right-hand string to the end of the left-hand string.', 'It throws a compile-time error because arithmetic operators cannot be used on reference types.', 'C', 'While += is used for mathematical addition with numeric types, it is overloaded for the `string` class to perform efficient text concatenation, appending the new string onto the existing one.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Operators', 'Introduced in C# 8.0, what does the null-coalescing assignment operator (??=) do?', 'It sets both operands to null simultaneously.', 'It strictly compares two variables, returning true if either is null.', 'It assigns the right-hand value to the left-hand variable ONLY if the left-hand variable is currently null.', 'It throws an exception if the right-hand value is null.', 'C', 'The ??= operator is a highly concise way to initialize objects or fields lazily. `myList ??= new List<int>();` will only allocate a new List in memory if `myList` hasn''t already been initialized.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Operators', 'Which operator is used to safely attempt to cast an object to a specific reference type, returning `null` if the cast fails rather than throwing an InvalidCastException?', 'is', 'as', 'typeof', 'cast', 'B', 'The `as` operator is used for safe type conversions among reference types or nullable types. If the object cannot be cast to the requested type, it gracefully returns `null`, whereas using a direct prefix cast (e.g., `(MyType)obj`) will violently throw an exception if it fails.');