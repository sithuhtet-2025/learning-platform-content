INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Hello World Program', 'What is the standard, traditional entry point for a C# console application where execution begins?', 'The Start() method', 'The Init() method', 'The Main() method', 'The Run() method', 'C', 'In traditional C# applications, the static Main() method serves as the official entry point where the Common Language Runtime (CLR) begins executing the program.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Hello World Program', 'Which namespace must be imported or explicitly referenced to use the `Console.WriteLine` method?', 'System.IO', 'System', 'System.Console', 'System.Core', 'B', 'The Console class is located within the base `System` namespace. You must either include `using System;` at the top of your file or write the fully qualified name `System.Console.WriteLine()`.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Hello World Program', 'What is the difference between `Console.Write` and `Console.WriteLine`?', 'Write outputs text to a file, while WriteLine outputs to the screen.', 'Write prints text and keeps the cursor on the same line, while WriteLine prints text and appends a newline character.', 'There is no difference; they are aliases.', 'Write only accepts strings, while WriteLine accepts any data type.', 'B', '`Console.WriteLine` automatically appends a line terminator (newline) after the specified data is printed, moving the cursor to the next line. `Console.Write` leaves the cursor at the end of the printed text.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Hello World Program', 'Introduced in C# 9.0, which feature allows you to write a complete "Hello World" program in just one line of code (e.g., `Console.WriteLine("Hello World");`) without explicitly defining a class or Main method?', 'Implicit Main', 'Top-level statements', 'Global functions', 'Inline execution', 'B', 'Top-level statements drastically reduce boilerplate code for simple programs and microservices. The C# compiler automatically generates the hidden Program class and Main method behind the scenes.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Hello World Program', 'What is the correct traditional signature for the `Main` method if the program needs to accept input arguments from the command line?', 'public void Main(arguments[] args)', 'static int Main(List<string> args)', 'static void Main(string[] args)', 'public static Main(String args)', 'C', 'The traditional Main method must be static. If it needs to process command-line arguments, it must accept a single parameter: an array of strings (`string[] args`).');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Hello World Program', 'Why is the traditional `Main` method required to be marked with the `static` modifier?', 'To prevent the method from being modified at runtime.', 'To make it execute faster.', 'To allow the Common Language Runtime (CLR) to call the entry point without needing to instantiate an object of the containing class first.', 'To ensure variables inside it are stored globally.', 'C', 'Because `Main` is the very first method executed, no objects exist yet. Making it `static` allows the CLR to invoke it directly on the class level to bootstrap the application.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Hello World Program', 'When running a Hello World console application outside of an IDE, which command is often added at the end of the `Main` method to prevent the command prompt window from closing immediately?', 'Console.Wait();', 'Console.ReadLine();', 'System.Pause();', 'Thread.Sleep(-1);', 'B', '`Console.ReadLine()` or `Console.ReadKey()` pauses the application''s execution, waiting for the user to press Enter or a key. This keeps the console window open so the output can be viewed before the program terminates.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Hello World Program', 'If you do not include the `using System;` directive at the top of your file, how must you write the command to print "Hello World"?', 'System.Console.WriteLine("Hello World");', 'Console.WriteLine("Hello World", System);', 'WriteLine("Hello World");', 'It is impossible to print without the using directive.', 'A', 'Without the `using System;` directive to import the namespace into the current file''s scope, you must use the fully qualified name of the class, specifying both its namespace and class name (`System.Console`).');