INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Strings', 'Which of the following statements about the ''string'' keyword in C# is true?', 'Strings are mutable value types allocated on the stack.', 'Strings are immutable reference types allocated on the managed heap.', 'Strings are dynamic types that change size in memory when modified.', 'Strings are character arrays that must be manually disposed of.', 'B', 'In C#, strings are reference types. Once a string object is created, its value cannot be modified (it is immutable). Any operation that appears to modify a string actually creates and returns a completely new string object in memory.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Strings', 'What is the technical difference between the lowercase ''string'' keyword and the uppercase ''System.String'' class in C#?', 'The ''string'' keyword is used for ASCII text, while ''System.String'' is used for Unicode text.', 'The ''string'' keyword is a value type, while ''System.String'' is a reference type.', 'There is no difference; ''string'' is simply a C# language alias for the .NET framework''s ''System.String'' class.', '''System.String'' provides manipulation methods, whereas ''string'' is just a raw data container.', 'C', 'The ''string'' keyword is built into the C# compiler as a direct alias for the base class library''s ''System.String'' type. They compile to the exact same Intermediate Language (IL) code and can be used interchangeably.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Strings', 'How do you declare a verbatim string literal in C# to prevent escape characters (like \n or \t) from being processed by the compiler?', 'By prefixing the string with a dollar sign ($).', 'By prefixing the string with an at symbol (@).', 'By enclosing the string in single quotes instead of double quotes.', 'By prefixing the string with a hash symbol (#).', 'B', 'Prefixing a string with the @ symbol creates a verbatim string literal. This tells the C# compiler to treat backslashes as literal characters rather than escape sequences, which is highly useful for hardcoding file paths or regular expressions.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Strings', 'Introduced in C# 6.0, which syntax correctly utilizes string interpolation to directly embed the variable ''name'' into a formatted string?', '$"Hello, {name}!"', '@"Hello, {name}!"', '"Hello, " + name + "!"', 'String.Format("Hello, {name}!")', 'A', 'String interpolation is indicated by prefixing the string literal with a dollar sign ($). Variables and inline expressions can then be evaluated directly within the string by wrapping them in curly braces {}.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Strings', 'When using the equality operator (==) to compare two separate string variables that happen to hold the exact same text, how does C# evaluate them?', 'It compares their memory addresses (reference equality), returning false.', 'It performs a case-sensitive value comparison of the actual text contents, returning true.', 'It throws a compilation error because the .Equals() method must be used for objects.', 'It performs a case-insensitive comparison automatically.', 'B', 'Even though strings are reference types, the equality operator (==) is explicitly overloaded for the ''string'' class in C# to perform a value comparison. It checks if the underlying character sequences are identical, not if they point to the same memory location.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Strings', 'If you need to perform thousands of append or modify operations on a text variable inside a loop, which class should you use to avoid severe performance degradation and memory overhead?', 'System.String', 'System.Text.StringWriter', 'System.Text.StringBuilder', 'System.Collections.Generic.List<char>', 'C', 'Because standard strings are immutable, modifying them inside a loop creates thousands of discarded string objects, causing heavy Garbage Collection pressure. ''StringBuilder'' maintains a mutable memory buffer, allowing highly efficient, in-place string manipulation.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Strings', 'Given the string declaration `string text = "HelloWorld";`, what does the method call `text.Substring(5, 3)` return?', '"Wor"', '"World"', '"oWo"', '"orl"', 'A', 'The Substring(int startIndex, int length) method begins at the specified zero-based index (5, which is the letter ''W'') and retrieves the specified number of characters (3 characters), resulting in the string "Wor".');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Strings', 'What is the most robust, exception-safe, and memory-efficient way to check if a string variable ''str'' is null, completely empty, or consists only of blank spaces?', 'if (str == null || str == "" || str == " ")', 'if (String.IsNullOrEmpty(str))', 'if (String.IsNullOrWhiteSpace(str))', 'if (str.Length == 0)', 'C', 'String.IsNullOrWhiteSpace(str) is the most comprehensive method. It securely checks if the string reference is null, if its length is zero, or if it contains nothing but whitespace characters (spaces, tabs, newlines) without throwing NullReference exceptions.');