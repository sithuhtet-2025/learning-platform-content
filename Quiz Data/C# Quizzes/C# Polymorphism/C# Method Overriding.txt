INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Method Overriding', 'What is the primary purpose of method overriding in C#?', 'To create multiple methods with the same name in the same class.', 'To provide a new, specific implementation in a derived class for a method already defined in its base class.', 'To hide a base class method so it can never be called.', 'To allow a method to accept a variable number of arguments.', 'B', 'Method overriding allows a subclass to provide a specific, customized implementation of a method that is already provided by its parent class, facilitating runtime polymorphism.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Method Overriding', 'Which two C# keywords are strictly required to successfully implement method overriding?', 'base and derived', 'virtual (or abstract) in the base class, and override in the derived class', 'new in the base class, and virtual in the derived class', 'static and override', 'B', 'To override a method in C#, the base class method must be explicitly marked as virtual, abstract, or override. The derived class method must then explicitly use the override keyword to replace the implementation.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Method Overriding', 'What happens if you define a method in a derived class with the exact same signature as a base class method, but you use the ''new'' keyword instead of ''override''?', 'The method overrides the base class method normally.', 'A compilation error is thrown.', 'The derived method "shadows" or "hides" the base method, meaning the base method will still be called if the object is accessed via a base class reference.', 'The base class method is deleted from memory.', 'C', 'The ''new'' keyword hides the base class member. Unlike ''override'', it does not participate in polymorphic dispatch. If a derived object is cast to its base type, calling the method will execute the original base class implementation, not the new one.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Method Overriding', 'Can you change the access modifier (e.g., from protected to public) when overriding a virtual method in a derived class?', 'Yes, you can make it more accessible.', 'Yes, you can make it less accessible.', 'No, the access level must perfectly match the overridden base method.', 'Yes, but only if the base method is abstract.', 'C', 'When overriding a method in C#, the overriding method cannot alter the access modifier. It must maintain the exact same accessibility level as the virtual method it is overriding to fulfill the established polymorphic contract.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Method Overriding', 'Which of the following statements about overriding abstract methods is true?', 'Abstract methods cannot be overridden; they can only be hidden.', 'Overriding an abstract method is optional for a non-abstract derived class.', 'Abstract methods are implicitly virtual and must be overridden by any non-abstract derived class.', 'You must use the ''virtual'' keyword alongside ''abstract'' in the base class.', 'C', 'Abstract methods contain no implementation and serve purely as a strict contract. They are implicitly virtual, and any concrete (non-abstract) class that inherits them is strictly required by the compiler to override and implement them.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Method Overriding', 'What type of polymorphism does method overriding represent?', 'Compile-time (Static) Polymorphism', 'Runtime (Dynamic) Polymorphism', 'Interface Polymorphism', 'Structural Polymorphism', 'B', 'Method overriding is resolved dynamically at runtime (late binding). The Common Language Runtime (CLR) checks the actual instantiated object type in memory to determine which overridden version of the method to execute, regardless of the reference variable''s type.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Method Overriding', 'How can you prevent a method that you have overridden from being overridden any further by classes that inherit from your derived class?', 'Remove the override keyword.', 'Mark the overridden method with the ''static'' keyword.', 'Mark the overridden method with the ''sealed'' keyword (e.g., public sealed override void DoWork()).', 'Change its access modifier to private.', 'C', 'Applying the ''sealed'' keyword to an overridden method prevents any further derived classes from overriding it again. This locks the implementation in place for all subsequent subclasses in the hierarchy while still participating in the initial override.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Method Overriding', 'Within an overridden method in a derived class, how can you execute the original implementation of the method from the base class?', 'By calling super.MethodName();', 'By calling base.MethodName();', 'By instantiating the base class and calling the method.', 'It is impossible; overriding completely destroys the base implementation.', 'B', 'The ''base'' keyword allows you to explicitly call into the parent class''s implementation of a method. This is a common architectural pattern when you want to augment or extend the base behavior rather than completely discarding and replacing it.');