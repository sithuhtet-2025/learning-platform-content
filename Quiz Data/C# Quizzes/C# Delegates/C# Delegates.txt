INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Delegates', 'Which of the following best describes a delegate in C#?', 'A class that defines custom exception types.', 'A type-safe object that encapsulates a reference to a method.', 'A mechanism to restrict inheritance in derived classes.', 'A keyword used to implement multiple inheritance.', 'B', 'A delegate in C# is conceptually similar to a type-safe function pointer in C or C++. It is an object that holds a reference to a method with a matching signature and return type.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Delegates', 'Which of the following is the correct syntax for declaring a custom delegate that takes a string parameter and returns an integer?', 'public delegate int StringToInt(string value);', 'public int delegate StringToInt(string value);', 'delegate(string value) => int;', 'public Action<string, int> StringToInt;', 'A', 'A custom delegate is declared using the ''delegate'' keyword, followed by the return type, the delegate name, and the required parameter list, matching the exact signature of the methods it will point to.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Delegates', 'How do you add multiple methods to a single delegate instance (creating a multicast delegate)?', 'By using the Add() method.', 'By using the .Concat() LINQ extension.', 'By using the += operator.', 'By passing an array of methods to the delegate constructor.', 'C', 'In C#, you can use the += operator to add additional methods to a delegate''s invocation list. When the delegate is invoked, all methods attached via += will be called synchronously in the order they were added.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Delegates', 'If a multicast delegate has a non-void return type (e.g., returns an int) and invokes three separate methods, what value does the caller receive?', 'An array of integers containing the results from all three methods.', 'The return value of the first method executed.', 'The return value of the last method executed.', 'A runtime exception is thrown because multicast delegates must return void.', 'C', 'If a multicast delegate has a return type, the caller only receives the return value of the very last method executed in the invocation list. The return values of the preceding methods are generated but effectively discarded.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Delegates', 'Which built-in generic delegate type should you use to point to a method that takes two integers and returns void?', 'Func<int, int>', 'Action<int, int>', 'Predicate<int, int>', 'EventHandler<int, int>', 'B', 'The Action<> delegate family is used exclusively for methods that do not return a value (void). Func<> is used for methods that return a value, and Predicate<> strictly returns a boolean.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Delegates', 'Given the declaration `Func<string, int, bool> myFunc;`, what is the exact signature of the method this delegate can point to?', 'A method that takes a string, an int, and a bool, and returns void.', 'A method that takes a bool, and returns a string and an int.', 'A method that takes a string and an int, and returns a bool.', 'A method that takes an int and a bool, and returns a string.', 'C', 'In the generic Func<> delegate, the final type parameter always represents the method''s return type. All preceding type parameters represent the input arguments in order. Therefore, it takes a string and an int, and returns a bool.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Delegates', 'Which built-in delegate is specifically designed to represent a method that defines a set of criteria and determines whether the specified object meets those criteria?', 'Predicate<T>', 'Action<T>', 'Func<T, bool>', 'Comparison<T>', 'A', 'While Func<T, bool> operates identically at a technical level, Predicate<T> is explicitly designed semantically for evaluating conditions (returning true or false). It is heavily used in methods like List<T>.FindAll().');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Delegates', 'What is the safest, most modern way to invoke a delegate named `OnCompleted` to ensure it doesn''t throw a NullReferenceException if no methods are currently attached?', 'try { OnCompleted(); } catch { }', 'if (OnCompleted != null) OnCompleted();', 'OnCompleted?.Invoke();', 'OnCompleted.SafeInvoke();', 'C', 'Using the null-conditional operator (?.) combined with the explicit Invoke() method is the modern, thread-safe way to invoke delegates in C#. It evaluates the delegate, and if it is null, it gracefully bypasses the invocation without throwing an exception.');