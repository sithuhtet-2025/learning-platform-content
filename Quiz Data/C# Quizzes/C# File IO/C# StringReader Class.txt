INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# StringReader Class', 'What is the primary architectural purpose of the `StringReader` class in C#?', 'To read binary data directly from the RAM.', 'To read characters sequentially from an in-memory string as if it were an I/O stream.', 'To serialize complex objects into JSON strings.', 'To read text directly from a physical hard drive.', 'B', 'The `StringReader` class provides a stream-like interface for reading characters directly from an existing, in-memory string. It allows you to treat a string payload exactly like you would a file or network stream.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# StringReader Class', 'Which abstract base class does the `StringReader` class inherit from?', 'System.IO.Stream', 'System.IO.StreamReader', 'System.IO.TextReader', 'System.IO.BinaryReader', 'C', '`StringReader` inherits from the abstract `TextReader` class. This ensures it shares the exact same core method signatures (`ReadLine`, `ReadToEnd`, `Peek`) as classes that read from physical files (like `StreamReader`).');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# StringReader Class', 'From an architectural perspective, why would a developer wrap an existing string in a `StringReader` instead of just using standard string manipulation methods like `.Split()`?', '`StringReader` automatically decrypts the string payload.', 'To pass the string into a third-party API or existing method that strictly demands a `TextReader` parameter (like an XML or JSON parser).', '`StringReader` consumes significantly less memory than a standard string.', 'It allows the original string to be modified in place.', 'B', 'The primary power of `StringReader` is polymorphism. If an architectural component (like `XmlReader.Create()`) strictly requires a `TextReader` to parse data, `StringReader` allows you to seamlessly feed it an in-memory string without writing that string to a physical hard drive first.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# StringReader Class', 'When calling `StringReader.ReadLine()`, how does the class accurately determine the end of a line within the underlying string?', 'It only recognizes the `\n` (Line Feed) character.', 'It strictly expects a null-terminator `\0`.', 'It automatically recognizes `\r` (Carriage Return), `\n` (Line Feed), or a combination of `\r\n` as a valid line terminator.', 'It automatically reads exactly 80 characters per line.', 'C', 'The `ReadLine()` method is highly robust and cross-platform compatible. It advances the internal character pointer until it detects any standard line-break sequence (`\r`, `\n`, or `\r\n`), extracts the characters up to that point as a string, and positions the pointer at the start of the next line.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# StringReader Class', 'What does the `StringReader.Peek()` method return if the internal pointer has reached the absolute end of the underlying string?', 'An empty string `""`', 'A null reference', 'An `EndOfStreamException`', 'The integer value -1', 'D', 'The `Peek()` method looks at the next available character without actually consuming it or advancing the pointer. If there are no more characters left to read, it safely returns the integer -1, which is the standard pattern for checking if a reader is exhausted.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# StringReader Class', 'Although `StringReader` implements `IDisposable` (via its base class), why is failing to call `.Dispose()` or omitting a `using` block generally NOT considered a critical application failure?', 'Because `StringReader` is a value type (struct).', 'Because the garbage collector triggers `.Dispose()` instantly upon going out of scope.', 'Because `StringReader` only manipulates managed memory (a string) and does not hold onto unmanaged OS resources like physical file locks or network sockets.', 'Because the C# compiler automatically inserts the `Dispose` call for all readers at compile-time.', 'C', 'While it is an established best practice to dispose of all `IDisposable` objects, `StringReader` operates entirely on an in-memory string. Unlike `FileStream` or `StreamReader`, it doesn''t lock external OS files, meaning forgetting to dispose of it will not cause catastrophic "File in Use" access errors.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# StringReader Class', 'What happens if you call `StringReader.ReadToEnd()` when the internal read pointer is already halfway through the underlying string?', 'It automatically resets the pointer and reads the entire string from the beginning.', 'It reads all remaining characters strictly from the current pointer position down to the absolute end of the string.', 'It throws an `InvalidOperationException`.', 'It returns the entire string but flags it as partially read.', 'B', 'The `ReadToEnd()` method is strictly context-aware. It does not blindly return the entire original string payload; instead, it extracts a new string containing everything from the current read pointer''s position down to the final character.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# StringReader Class', 'Unlike `StreamReader`, which typically wraps an underlying `Stream` (like a `FileStream`), what specific data type is passed directly into the constructor of a `StringReader`?', 'A `MemoryStream` object', 'A standard C# `string` variable', 'A raw character array `char[]`', 'An absolute file path', 'B', 'The `StringReader` constructor takes a standard `string` as its sole required argument (e.g., `new StringReader(myTextPayload)`). This string becomes the permanent, immutable data source for all subsequent read operations performed by that specific reader instance.');