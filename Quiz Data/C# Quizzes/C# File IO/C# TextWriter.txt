INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# TextWriter', 'What is the primary architectural role of the `TextWriter` class in the `System.IO` namespace?', 'To write raw binary bytes directly to memory.', 'To serve as an abstract base class that defines the fundamental contract for writing a sequential series of characters.', 'To serialize complex objects into XML.', 'To exclusively handle network stream transmissions.', 'B', 'The `TextWriter` class is an abstract base class. It does not implement the physical writing mechanism itself, but rather establishes the required architectural blueprint (methods like Write, WriteLine, and Flush) that all character-writing classes (like StreamWriter and StringWriter) must implement.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# TextWriter', 'Which two primary concrete classes in the .NET framework directly inherit from the abstract `TextWriter` class?', 'BinaryWriter and XmlWriter', 'CharWriter and ByteWriter', 'StreamWriter and StringWriter', 'FileWriter and MemoryWriter', 'C', '`StreamWriter` (used for writing characters to a physical byte stream like a file) and `StringWriter` (used for writing characters to an in-memory `StringBuilder`) are the two core concrete implementations that leverage the `TextWriter` polymorphic contract.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# TextWriter', 'What is the exact functional difference between the `TextWriter.Write()` and `TextWriter.WriteLine()` methods?', '`Write()` writes to RAM, while `WriteLine()` writes to the disk.', '`Write()` only accepts strings, while `WriteLine()` accepts objects.', '`WriteLine()` appends the environment-specific line terminator string to the end of the output, whereas `Write()` appends only the provided text.', '`WriteLine()` automatically calls `Flush()` after executing.', 'C', '`WriteLine()` ensures that the next write operation will begin on a new line. It automatically appends the characters defined by the `TextWriter.NewLine` property (which defaults to `Environment.NewLine`, such as `\r\n` on Windows) immediately after the supplied text.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# TextWriter', 'Because `TextWriter` implementations often buffer data in memory to optimize performance, which method must be called to force that buffered data to be physically pushed to the underlying stream or device?', 'Push()', 'Commit()', 'Save()', 'Flush()', 'D', 'Disk and network I/O operations are expensive, so derived classes like `StreamWriter` buffer characters in RAM. The `Flush()` method forcefully drains this internal buffer, ensuring all pending characters are actually written to the destination without closing the writer.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# TextWriter', 'What occurs sequentially when the `Dispose()` (or `Close()`) method is invoked on a `TextWriter` instance?', 'It permanently deletes the underlying file.', 'It automatically calls `Flush()` to write any remaining buffered data, and then gracefully releases the underlying unmanaged resources (like OS file handles).', 'It throws an exception if there is unwritten data in the buffer.', 'It resets the internal memory buffer to zero but leaves the stream open.', 'B', 'Like `TextReader`, `TextWriter` implements `IDisposable`. Calling `Dispose()` (usually via a `using` block) is a critical architectural requirement. It guarantees that any lingering data in the buffer is flushed to the stream before the underlying OS locks are safely released.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# TextWriter', 'How does the `TextWriter` class architecture natively support culture-specific formatting (such as date and currency formats) when writing data?', 'By utilizing its `FormatProvider` property, which defaults to the current thread''s culture unless explicitly set.', 'It does not support formatting; all data must be pre-formatted as strings before passing to `Write()`.', 'By inheriting from the `System.Globalization` namespace.', 'By strictly forcing all output to invariant culture (ASCII).', 'A', '`TextWriter` has a `FormatProvider` property (of type `IFormatProvider`). When you pass objects directly to methods like `Write(object)` or use composite formatting like `WriteLine("Total: {0:C}", price)`, it uses this provider to ensure culturally accurate formatting for dates, numbers, and currencies.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# TextWriter', 'If you are building a highly scalable ASP.NET Core web application that generates massive text files, which `TextWriter` method should you utilize to prevent blocking the CPU thread?', 'WriteSynchronized()', 'WriteLineAsync()', 'BeginWrite()', 'ThreadedWrite()', 'B', 'Modern .NET architecture heavily relies on asynchronous I/O to maximize server scalability and prevent thread starvation. Using `WriteAsync()` or `WriteLineAsync()` offloads the blocking disk or network write operation, allowing the CPU thread to process other web requests simultaneously.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# TextWriter', 'By default, are instances of `TextWriter` and its derivatives thread-safe for concurrent write operations from multiple threads?', 'Yes, writing is inherently thread-safe.', 'Yes, it automatically locks the underlying stream on every call.', 'No, instance members are NOT guaranteed to be thread-safe, and concurrent writes will corrupt the output or throw exceptions unless synchronized.', 'Yes, but only if writing to an in-memory `StringWriter`.', 'C', 'Standard instances of `TextWriter` are strictly not thread-safe. If multiple threads attempt to write simultaneously without a lock, the internal buffers will become hopelessly corrupted, leading to mangled text or application crashes. Developers must implement custom `lock` blocks or use the `TextWriter.Synchronized()` wrapper.');