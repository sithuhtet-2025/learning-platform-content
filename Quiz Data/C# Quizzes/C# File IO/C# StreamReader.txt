INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# StreamReader', 'What is the primary architectural role of the `StreamReader` class in C#?', 'To read raw binary data (bytes) directly from a physical hard drive.', 'To serialize C# objects into XML format.', 'To read characters from a byte stream in a specific encoding, abstracting away the complex low-level byte-to-character translation.', 'To continuously monitor a directory for new text files.', 'C', 'The `StreamReader` class is a high-level wrapper around a raw byte stream (like a `FileStream`). It automatically handles decoding raw bytes into human-readable characters (strings) using a specified text encoding format.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# StreamReader', 'When parsing a file sequentially inside a `while` loop, which `StreamReader` property is typically evaluated to safely determine if the end of the file has been reached?', 'IsFinished', 'EndOfStream', 'HasNext', 'Length', 'B', 'The `EndOfStream` property returns a boolean `true` when the current stream position has reached the absolute end of the underlying stream, making it the standard condition for `while (!reader.EndOfStream)` loops.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# StreamReader', 'If you need to process a text file line-by-line, which method extracts the next available sequence of characters until it hits a carriage return or newline character?', 'ReadNext()', 'ReadString()', 'ReadLine()', 'ParseLine()', 'C', 'The `ReadLine()` method reads characters from the current stream pointer until it encounters a standard line terminator (like `\n` or `\r\n`), returns that line as a string, and advances the pointer to the next line.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# StreamReader', 'From an architectural and performance perspective, why is using `StreamReader.ReadLine()` in a loop superior to using `File.ReadAllText()` when processing a massive 10GB log file?', '`ReadAllText` automatically deletes the file after reading it.', '`StreamReader` reads the data sequentially into a small, fixed-size memory buffer, keeping RAM utilization extremely low, whereas `ReadAllText` attempts to load the entire 10GB payload into RAM at once, causing an `OutOfMemoryException`.', '`StreamReader` executes asynchronously by default, preventing CPU locking.', '`ReadAllText` cannot read log files.', 'B', '`StreamReader` is designed for streaming. It only holds a small chunk of the file in memory at any given time. Methods like `File.ReadAllText()` or `File.ReadAllLines()` materialize the entire file contents in the managed heap, which is catastrophic for large files.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# StreamReader', 'What happens if you fail to wrap a `StreamReader` in a `using` block or explicitly call its `.Dispose()` method after you finish reading a file?', 'The CLR automatically closes the file within 3 seconds.', 'The underlying `FileStream` and operating system file handles remain locked, severely restricting or completely preventing other applications from modifying or deleting that file until the Garbage Collector eventually runs.', 'The `StreamReader` throws a `MemoryLeakException`.', 'The file contents are permanently erased.', 'B', 'Like all I/O classes, `StreamReader` ties up unmanaged OS resources (file handles). Failing to dispose of it explicitly leaves the file locked ("File is in use by another process") and creates unpredictable resource leaks.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# StreamReader', 'If no explicit text encoding is provided to the `StreamReader` constructor, which standard encoding does it assume by default?', 'ASCII', 'UTF-16', 'UTF-8', 'ANSI', 'C', 'By default, the `StreamReader` class targets UTF-8 encoding. It also automatically attempts to detect Byte Order Marks (BOMs) at the beginning of the stream to seamlessly adjust its decoding if necessary.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# StreamReader', 'What is the specific function of the `StreamReader.Peek()` method?', 'It temporarily opens the file to check its overall size in bytes.', 'It returns the next available character as an integer without actually consuming it or advancing the stream''s position pointer.', 'It skips the current line and moves the pointer to the next line.', 'It forcefully flushes the internal buffer.', 'B', 'The `Peek()` method allows you to "look ahead" at the next character in the buffer. If it returns -1, it guarantees there are no more characters to be read. Because it doesn''t advance the pointer, the next `Read()` operation will still capture that character.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# StreamReader', 'Which method should you call on a `StreamReader` if you simply want to extract all remaining text from the current position pointer to the absolute end of the stream in one massive string block?', 'ReadEverything()', 'ReadToEnd()', 'Flush()', 'GetAllText()', 'B', 'The `ReadToEnd()` method consumes the entirety of the remaining stream from the current pointer position. While convenient, it should be used with extreme caution on large files to avoid massive heap allocations and memory spikes.');