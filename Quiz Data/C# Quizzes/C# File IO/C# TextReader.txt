INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# TextReader', 'What is the primary architectural role of the `TextReader` class in the `System.IO` namespace?', 'To parse and execute SQL commands against a text-based database.', 'To serve as an abstract base class that defines the fundamental contract for reading a sequential series of characters.', 'To read raw binary bytes from an unmanaged memory stream.', 'To strictly read text specifically from physical hard drive files.', 'B', 'The `TextReader` class is an abstract base class. It does not implement the reading itself, but rather dictates the architectural blueprint (methods like Read, ReadLine, and Peek) that all character-reading classes (like StreamReader and StringReader) must follow.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# TextReader', 'Which two primary concrete classes in the .NET framework directly inherit from the abstract `TextReader` class?', 'FileReader and MemoryReader', 'StreamReader and StringReader', 'BinaryReader and XmlReader', 'CharReader and ByteReader', 'B', '`StreamReader` (for reading characters from a physical byte stream) and `StringReader` (for reading characters from an in-memory string) are the two primary concrete implementations of the `TextReader` abstract class, leveraging its polymorphic contract.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# TextReader', 'Why does the `TextReader.Read()` method return an `int` data type instead of a `char`?', 'Because characters in C# are strictly 32-bit integers.', 'To return the memory address of the character.', 'To safely return the integer -1 when the end of the stream is reached, distinguishing it from any valid Unicode character.', 'Because it reads multiple characters at once and returns their mathematical sum.', 'C', 'The `Read()` method extracts the next available character. It returns an `int` so it can explicitly return -1 when the reader is exhausted. If it returned a `char`, there would be no safe way to indicate the end of the stream without throwing an exception or conflicting with a valid character.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# TextReader', 'What is the strict behavioral difference between `TextReader.Read()` and `TextReader.Peek()`?', '`Read()` works asynchronously, while `Peek()` is synchronous.', '`Read()` retrieves and consumes the character (advancing the internal pointer), while `Peek()` merely looks at the next character without advancing the pointer.', '`Peek()` reads an entire line, while `Read()` reads a single character.', '`Peek()` bypasses the internal memory buffer completely.', 'B', 'The `Peek()` method is used for lookahead operations. It returns the next character (-1 if at the end) but leaves the internal position pointer exactly where it is, meaning the next `Read()` call will retrieve that exact same character again.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# TextReader', 'When calling `TextReader.ReadLine()`, how does the method determine what constitutes a complete "line"?', 'It strictly looks for a null-terminator (0x00).', 'It reads exactly 256 characters regardless of content.', 'It reads sequentially until it encounters a standard line terminator (`\n`, `\r`, or `\r\n`), consuming the terminator but NOT including it in the returned string.', 'It stops reading when it encounters a space or tab.', 'C', '`ReadLine()` is highly cross-platform. It automatically recognizes carriage returns and line feeds (Windows vs. Unix style) as line boundaries. It extracts the text before the boundary and cleanly discards the hidden line terminator characters.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# TextReader', 'Because implementations of `TextReader` often handle underlying unmanaged resources, what method must be called (or invoked via a `using` block) to prevent memory and file lock leaks?', 'Clear()', 'Flush()', 'Dispose()', 'Finalize()', 'C', 'Like its base implementations, `TextReader` implements `IDisposable`. Calling `Dispose()` (or its alias `Close()`) is absolutely critical to gracefully release any underlying unmanaged resources (like OS file handles or network sockets) locked by the concrete implementation.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# TextReader', 'If you are building a high-performance web application that reads thousands of text streams simultaneously, which `TextReader` method should you utilize to prevent thread starvation?', 'ReadToEnd() inside a `lock` statement', 'ReadLineAsync()', 'ReadSynchronized()', 'BeginRead()', 'B', 'Modern .NET architecture heavily relies on asynchronous I/O to maximize server scalability. Using `ReadLineAsync()` or `ReadToEndAsync()` offloads the blocking read operation from the CPU thread, allowing the application to process other web requests while waiting for the I/O payload.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# TextReader', 'By default, is a `TextReader` instance thread-safe for concurrent read operations from multiple threads?', 'Yes, it automatically locks the underlying stream.', 'No, instance members of `TextReader` are NOT guaranteed to be thread-safe.', 'Yes, but only if it is wrapped in a `StringReader`.', 'Yes, because reading does not mutate data.', 'B', 'Standard instances of `TextReader` (and its derivatives) are strictly not thread-safe. If multiple threads attempt to read from the same `TextReader` simultaneously, the internal position pointer will corrupt. You must implement your own synchronization (`lock`) or use the `TextReader.Synchronized()` wrapper method.');