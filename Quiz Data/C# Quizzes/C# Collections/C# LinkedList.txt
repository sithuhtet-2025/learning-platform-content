INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# LinkedList', 'What specific type of data structure is the System.Collections.Generic.LinkedList<T> class in C#?', 'A singly linked list where nodes only point forward.', 'A circular linked list where the tail points to the head.', 'A doubly linked list where each node contains references to both the previous and next nodes.', 'An array-backed list that dynamically resizes memory.', 'C', 'The C# LinkedList<T> is implemented as a general-purpose doubly linked list. This allows for bidirectional traversal and efficient O(1) insertions or deletions at both ends, as well as before or after any known node.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# LinkedList', 'Which object type represents the individual elements stored within a C# LinkedList<T>?', 'Node<T>', 'LinkedListElement<T>', 'LinkedListNode<T>', 'ListNode<T>', 'C', 'Elements in a LinkedList<T> are encapsulated inside LinkedListNode<T> objects. These node objects expose the actual data via the ''Value'' property, along with ''Next'', ''Previous'', and ''List'' properties for structural navigation.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# LinkedList', 'How do you access the 5th element in a LinkedList<string> using zero-based indexing?', 'string val = list[4];', 'string val = list.ElementAt(4);', 'string val = list.Get(4);', 'LinkedList<T> does not support direct index-based access.', 'D', 'Because a LinkedList<T> allocates nodes in non-contiguous memory locations, it does not support the indexer operator ([]). To find an element by its numerical position, you must manually traverse the list from the First or Last node, resulting in an O(n) operation.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# LinkedList', 'What is the time complexity (Big O notation) of inserting a new node into the middle of a LinkedList<T> using the AddBefore() or AddAfter() methods, assuming you already hold the reference to the target LinkedListNode<T>?', 'O(1)', 'O(log n)', 'O(n)', 'O(n^2)', 'A', 'If you already hold the reference to a specific LinkedListNode<T>, inserting a new node adjacent to it is an O(1) operation. It only requires updating the ''Next'' and ''Previous'' reference pointers of the surrounding nodes, unlike a List<T> which must physically shift subsequent elements in memory.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# LinkedList', 'What happens if you attempt to take a LinkedListNode<T> that already belongs to `listA` and pass it directly into `listB.AddLast(node);`?', 'The node is seamlessly moved from listA to listB.', 'A shallow copy of the node is added to listB.', 'An InvalidOperationException is thrown at runtime.', 'A compile-time error occurs because nodes are immutable.', 'C', 'A LinkedListNode<T> is strictly tied to the specific LinkedList<T> instance it was created in via its ''List'' property. Attempting to add a node to a list when it already belongs to another list (or adding it multiple times to the same list) throws an InvalidOperationException.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# LinkedList', 'Which methods are provided by LinkedList<T> to search for a specific value within the collection?', 'Search() and SearchLast()', 'Find() and FindLast()', 'IndexOf() and LastIndexOf()', 'ContainsNode() and ContainsValue()', 'B', 'The LinkedList<T> class provides the Find(T value) and FindLast(T value) methods. Find() performs a linear search starting from the Head (First node), while FindLast() starts from the Tail (Last node). Both return the first matching LinkedListNode<T> they encounter, or null if no match is found.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# LinkedList', 'If you need to safely remove elements from a LinkedList<T> while iterating through it, which approach avoids throwing an InvalidOperationException (Collection Modified)?', 'Using a standard `foreach` loop and calling `list.Remove(item)`.', 'Using a `for` loop with an index counter.', 'Using a `while` loop, capturing the `node.Next` reference into a temporary variable before calling `list.Remove(node)`, and then advancing to the temporary variable.', 'It is impossible to remove items from a LinkedList<T> while iterating; you must copy it to an array first.', 'C', 'Modifying a collection inside a foreach loop always throws an exception. Because LinkedList<T> lacks indexers, a for loop is inefficient. The correct pattern is to manually traverse using a while loop, safely caching the next node''s reference before deleting the current node, and then continuing the loop using the cached reference.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# LinkedList', 'Why might a developer choose to use a standard List<T> instead of a LinkedList<T> even if their application requires frequent insertions?', 'List<T> uses less overall memory per element and benefits heavily from CPU cache spatial locality due to contiguous memory allocation.', 'LinkedList<T> requires elements to implement the IComparable interface.', 'List<T> automatically sorts elements during insertion.', 'LinkedList<T> is strictly limited to storing reference types.', 'A', 'While LinkedList<T> offers O(1) insertions mathematically, List<T> often outperforms it in real-world scenarios due to contiguous memory allocation. Arrays (backing List<T>) are highly cache-friendly for modern CPUs. Furthermore, LinkedList<T> allocates a separate object (LinkedListNode) for every element, causing memory overhead and increasing Garbage Collection pressure.');