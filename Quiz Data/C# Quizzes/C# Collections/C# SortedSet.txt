INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# SortedSet', 'How is the System.Collections.Generic.SortedSet<T> class internally implemented in the .NET framework?', 'As a dynamically resizing array.', 'As a hash table.', 'As a binary search tree (specifically a Red-Black tree).', 'As a doubly linked list.', 'C', 'Like SortedDictionary, SortedSet<T> is implemented using a Red-Black tree. This allows it to dynamically maintain elements in a sorted order while providing O(log n) performance for insertions, deletions, and lookups.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# SortedSet', 'What is the primary structural difference between SortedSet<T> and a standard List<T> that has been sorted?', 'SortedSet<T> allows duplicate elements, while List<T> does not.', 'SortedSet<T> automatically maintains the sorted order upon every insertion and guarantees all elements are strictly unique.', 'SortedSet<T> provides O(1) index-based access, whereas List<T> requires O(n) traversal.', 'SortedSet<T> is limited to storing primitive value types only.', 'B', 'SortedSet<T> inherently enforces mathematical set rules (all elements must be unique) and automatically places new elements in their correct sorted position within the tree. A List<T> allows duplicates and requires manual re-sorting after new insertions.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# SortedSet', 'What does the SortedSet<T>.Add(T item) method return, and how does it handle duplicate items?', 'It returns void and throws an ArgumentException if the item already exists.', 'It returns a bool (true if the item was added, false if the item was already present in the set).', 'It returns the newly added item and silently overwrites any existing duplicate.', 'It returns an int representing the new index of the item, allowing duplicates.', 'B', 'Unlike collections like SortedList or Dictionary which throw exceptions on duplicate keys, the SortedSet<T>.Add method gracefully handles duplicates by simply returning false without modifying the collection.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# SortedSet', 'If you have a SortedSet<int> and you want to extract a sub-collection of all values between 10 and 50 (inclusive), which method provides the most efficient, allocation-free way to do this?', 'Iterate with a foreach loop and copy matching items to a new List<int>.', 'Use LINQ: set.Where(x => x >= 10 && x <= 50).', 'Use the GetViewBetween(10, 50) method.', 'Use the IntersectWith(new[] { 10, 50 }) method.', 'C', 'GetViewBetween(lower, upper) returns a lightweight view of the subset within the original tree without allocating new memory or copying elements. Changes made to this view will directly affect the underlying SortedSet<T>.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# SortedSet', 'Which of the following methods performs a mathematical set operation that modifies the current SortedSet<T> to contain only elements that are present in both the current set and a specified collection?', 'UnionWith(IEnumerable<T>)', 'IntersectWith(IEnumerable<T>)', 'ExceptWith(IEnumerable<T>)', 'SymmetricExceptWith(IEnumerable<T>)', 'B', 'The IntersectWith method modifies the current set so that it only retains elements that also exist in the provided IEnumerable<T>. This is a highly optimized mathematical intersection operation.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# SortedSet', 'What is the time complexity for retrieving the minimum or maximum value from a SortedSet<T> using the .Min or .Max properties?', 'O(1)', 'O(log n)', 'O(n)', 'O(n log n)', 'B', 'Because SortedSet<T> is a binary search tree, finding the minimum or maximum value requires traversing down the leftmost or rightmost branches of the tree, resulting in an O(log n) time complexity. It does not cache these values for immediate O(1) retrieval.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# SortedSet', 'If you create a SortedSet<string>, how can you enforce that the strings are sorted by their length (shortest to longest) rather than alphabetically?', 'Pass a custom IComparer<string> to the SortedSet constructor.', 'Call the set.Sort((a, b) => a.Length.CompareTo(b.Length)) method after adding elements.', 'Override the ToString() method on the strings.', 'SortedSet<T> can only sort primitive types using their default comparers; custom sorting is not supported.', 'A', 'The sorting behavior of a SortedSet<T> is permanently established at instantiation. By passing an object that implements IComparer<T> to the constructor, you completely control the comparison logic used by the underlying Red-Black tree to order the elements.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# SortedSet', 'What occurs if you call SymmetricExceptWith(otherCollection) on a SortedSet<T>?', 'It removes all elements from the current set that are also present in otherCollection.', 'It modifies the current set to contain only the elements that are present either in the current set or in otherCollection, but not in both.', 'It adds all elements from otherCollection to the current set, silently ignoring duplicates.', 'It strictly evaluates if the current set and otherCollection contain the exact same elements, returning a boolean.', 'B', 'SymmetricExceptWith performs a mathematical symmetric difference operation (equivalent to an XOR). It modifies the target set to contain only the elements that are strictly unique to each individual collection, removing any elements they share in common.');