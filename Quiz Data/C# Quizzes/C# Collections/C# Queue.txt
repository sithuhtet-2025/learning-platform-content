INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Queue', 'Which primary data processing principle does the System.Collections.Generic.Queue<T> class enforce in C#?', 'Last-In, First-Out (LIFO)', 'First-In, First-Out (FIFO)', 'First-In, Last-Out (FILO)', 'Random Access Memory (RAM)', 'B', 'A Queue operates on a First-In, First-Out (FIFO) basis. This means the first element added to the collection will be the first element available to be removed and processed.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Queue', 'Which method is used to add an object to the end (tail) of a Queue<T>?', 'Add()', 'Push()', 'Enqueue()', 'Insert()', 'C', 'The Enqueue() method adds an element to the tail end of the Queue. The Push() method is used for Stacks (LIFO), and Add() is standard for Lists and general ICollection implementations.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Queue', 'What is the precise behavior of the Dequeue() method when called on a Queue<T>?', 'It returns the object at the beginning of the queue without removing it.', 'It removes and returns the object at the beginning (head) of the queue.', 'It removes and returns the object at the end (tail) of the queue.', 'It clears all items from the queue and returns the count.', 'B', 'The Dequeue() method removes the oldest element from the head of the queue and returns it to the caller. To read the element without removing it, the Peek() method should be used instead.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Queue', 'What happens if you invoke the Dequeue() or Peek() methods on an empty Queue<T>?', 'It returns null or the default value of T.', 'It throws an IndexOutOfRangeException.', 'It throws an InvalidOperationException.', 'It blocks the current thread until an item is enqueued.', 'C', 'Calling Dequeue() or Peek() on an empty Queue immediately throws an InvalidOperationException. It is best practice to either check the Count property first or use the TryDequeue()/TryPeek() methods to safely handle empty states.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Queue', 'Which modern C# method safely attempts to remove and return the object at the beginning of the queue, avoiding exceptions if the queue is empty?', 'TryDequeue(out T result)', 'DequeueOrDefault()', 'SafeDequeue()', 'Pop(out T result)', 'A', 'The TryDequeue(out T result) method returns true if an element was successfully removed and returned in the out parameter, and false if the queue was empty, entirely avoiding the overhead of throwing an InvalidOperationException.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Queue', 'How is the generic Queue<T> class internally implemented in the .NET framework to ensure optimal performance?', 'As a singly linked list where nodes only point to the next item.', 'As a doubly linked list to allow rapid traversal in both directions.', 'As a dynamically resizing circular array.', 'As a hash table mapped by insertion timestamps.', 'C', 'Queue<T> is internally implemented as a dynamically resizing circular array. It maintains internal head and tail index pointers, allowing both Enqueue and Dequeue operations to run in highly efficient O(1) time without requiring memory shifts.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Queue', 'What happens to the elements within a Queue<T> when you iterate over it using a standard foreach loop?', 'The elements are permanently Dequeued (removed) one by one during iteration.', 'The elements are enumerated from newest (tail) to oldest (head) without being removed.', 'The elements are enumerated from oldest (head) to newest (tail) without being removed.', 'A compile-time error occurs because Queues do not implement IEnumerable.', 'C', 'Iterating over a Queue<T> using a foreach loop strictly enumerates the elements in standard FIFO order (from head to tail). It reads the data without modifying the queue or removing any elements from the underlying collection.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Queue', 'If you need a queue structure where multiple threads can safely add and remove items concurrently without explicit locking, which class should you use?', 'System.Collections.Generic.Queue<T>', 'System.Collections.Concurrent.ConcurrentQueue<T>', 'System.Collections.SynchronizedQueue<T>', 'System.Collections.Generic.PriorityQueue<TElement, TPriority>', 'B', 'System.Collections.Concurrent.ConcurrentQueue<T> is a thread-safe, lock-free implementation of a FIFO data structure. It is specifically optimized for highly concurrent multithreaded scenarios where standard Queue<T> would cause race conditions or require heavy lock contention.');