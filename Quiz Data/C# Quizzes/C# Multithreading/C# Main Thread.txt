INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Main Thread', 'From an architectural standpoint, what exactly is the "Main Thread" in a standard C# application?', 'A background thread managed by the Garbage Collector.', 'The primary thread automatically created by the operating system to execute the application''s `Main()` method entry point.', 'A specific thread pool dedicated exclusively to network I/O operations.', 'A virtual thread used to simulate parallel processing.', 'B', 'When a C# application is launched, the operating system allocates a primary thread of execution. This "Main Thread" is responsible for bootstrapping the application and executing the code sequentially starting from the static `Main()` method.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Main Thread', 'In desktop application frameworks like WPF or Windows Forms, what critical secondary role does the Main Thread assume?', 'It becomes the exclusive thread for all database connections.', 'It transforms into a background thread.', 'It becomes the "UI Thread", taking exclusive responsibility for rendering the user interface and processing input events (like clicks and keystrokes).', 'It delegates UI rendering to the GPU and goes to sleep.', 'C', 'In UI frameworks, the Main Thread enters a message loop (dispatcher) and becomes the UI Thread. It is strictly responsible for handling user interactions and updating visual elements. No other thread is allowed to directly modify UI components.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Main Thread', 'Regarding application lifecycle, what is the default behavior of the Main Thread in relation to application termination?', 'It is a background thread; the application will close even if the Main Thread is still processing.', 'It is a foreground thread; the application process will remain alive and executing as long as the Main Thread is running.', 'It terminates automatically after 60 seconds of inactivity.', 'It relies on the Garbage Collector to determine when to terminate.', 'B', 'The Main Thread is, by definition, a foreground thread. The .NET Common Language Runtime (CLR) will not terminate the application process until all foreground threads, including the Main Thread, have finished executing their work.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Main Thread', 'Which static property allows you to programmatically inspect the properties of the thread that is currently executing your code block?', 'System.Environment.CurrentThread', 'System.Threading.Thread.Main', 'System.Threading.Thread.CurrentThread', 'System.Diagnostics.Process.ActiveThread', 'C', 'The `Thread.CurrentThread` property returns a reference to the currently executing thread. Developers heavily use this to check properties like `ManagedThreadId` or `IsBackground` to verify if their code is currently running on the Main Thread or a worker thread.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Main Thread', 'Why is calling `Thread.Sleep()` or executing a massive, synchronous database query directly on the Main Thread considered a severe architectural anti-pattern in a desktop or mobile app?', 'It automatically causes a memory leak.', 'It freezes the UI Thread, preventing the application from redrawing the screen or responding to user input, resulting in an "Application Not Responding" (ANR) state.', 'It forces the operating system to reboot the application.', 'It encrypts the memory stack, preventing debugging.', 'B', 'Because the Main Thread acts as the UI Thread, any long-running, synchronous operation "blocks" it. While blocked, the thread cannot process the UI message queue, causing the entire application interface to freeze and become unresponsive to the user.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Main Thread', 'If a background worker thread attempts to directly update a TextBox (e.g., `myTextBox.Text = "Done";`) that was created by the Main Thread, what happens?', 'The TextBox updates successfully but with a slight delay.', 'The compiler automatically marshals the call to the Main Thread.', 'The CLR immediately throws an `InvalidOperationException` (Cross-thread operation not valid).', 'The background thread crashes silently.', 'C', 'UI components exhibit "thread affinity"; they can only be accessed or modified by the specific thread that created them (the Main/UI Thread). Attempting to mutate them from a background thread throws a strict cross-thread violation exception.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Main Thread', 'In an asynchronous UI event handler (e.g., `async void Button_Click`), what happens immediately after an `await` operator yields control during a long-running Task?', 'Control is returned to the Main Thread''s message pump, allowing the UI to remain responsive while the Task runs in the background.', 'The Main Thread goes to sleep until the Task finishes.', 'A new Main Thread is spawned by the operating system.', 'The application closes.', 'A', 'This is the core power of `async/await` in UI apps. The `await` keyword yields execution back to the caller (the UI message loop). The Main Thread is freed to handle other clicks and repaints while the awaited work executes asynchronously.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Main Thread', 'How does the standard `SynchronizationContext` assist the Main Thread when an `await` operation finishes in a UI application (like WPF or WinForms)?', 'It forces the continuation code to run on a random ThreadPool thread.', 'It automatically posts the continuation of the `async` method back onto the Main (UI) Thread, ensuring that any subsequent UI updates are performed safely and legally.', 'It securely synchronizes data with an external SQL database.', 'It clears the thread''s memory cache.', 'B', 'When you `await` a task in a UI app, the default behavior captures the UI''s SynchronizationContext. When the background task finishes, the context automatically routes the rest of the method back to the Main Thread, allowing you to update UI controls legally without manual marshaling (like `Dispatcher.Invoke`).');