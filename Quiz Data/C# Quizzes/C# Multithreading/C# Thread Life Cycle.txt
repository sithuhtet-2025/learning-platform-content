INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Thread Life Cycle', 'What is the specific enumeration provided by the .NET framework to track the current execution phase of a thread?', 'System.Threading.ThreadPhase', 'System.Threading.ThreadStatus', 'System.Threading.ThreadState', 'System.Threading.ExecutionState', 'C', 'The `ThreadState` enumeration defines a set of bitwise flags that indicate the exact current execution status of a managed thread within the CLR, such as Unstarted, Running, or Stopped.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Thread Life Cycle', 'Immediately after a new `Thread` object is instantiated in memory, but before the `Start()` method is explicitly invoked, what is its exact `ThreadState`?', 'Suspended', 'Unstarted', 'Idle', 'Waiting', 'B', 'When a thread is instantiated, it is purely a managed object resting in memory. It strictly remains in the `Unstarted` state until the `Start()` method is called to request actual execution resources from the operating system.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Thread Life Cycle', 'When a thread is actively executing its assigned instruction set on the CPU, what state is it in?', 'Executing', 'Active', 'Running', 'Started', 'C', 'Once the operating system scheduler allocates physical CPU time slice to the thread and it begins executing its code block, its internal state transitions to `Running`.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Thread Life Cycle', 'If a running thread explicitly calls `Thread.Sleep()`, invokes `Join()` on another thread, or waits to acquire a `lock` (Monitor), what state does it transition into?', 'Suspended', 'WaitSleepJoin', 'Background', 'Blocked', 'B', 'Any operation that intentionally blocks the thread''s execution (like sleeping, joining, or waiting on a synchronization lock) causes the CLR to transition the thread into the highly efficient `WaitSleepJoin` state, ensuring it consumes zero CPU cycles while waiting.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Thread Life Cycle', 'From an architectural perspective, why is the `ThreadState` enumeration defined with the `[Flags]` attribute?', 'To allow the CLR to flag the thread for aggressive garbage collection.', 'Because it is a legacy enumeration imported directly from C++.', 'Because a thread can theoretically exist in multiple states simultaneously (e.g., `WaitSleepJoin` and `AbortRequested` at the exact same time).', 'To allow the state to be serialized directly into a string format.', 'C', '`ThreadState` is a bitmask. Thread states are not always mutually exclusive. For instance, a thread can be fast asleep (`WaitSleepJoin`) while simultaneously having an external abort requested against it (`AbortRequested`), requiring bitwise operators (`&`, `|`) to evaluate accurately.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Thread Life Cycle', 'Which simple boolean property should be evaluated to quickly determine if a thread has been started and has not yet terminated, effectively bypassing complex bitwise state checks?', 'IsActive', 'IsRunning', 'IsExecuting', 'IsAlive', 'D', 'Instead of manually performing complex bitwise logical checks against the `ThreadState` enum to see if a thread is actively participating in the application (i.e., not Unstarted, Stopped, or Aborted), developers heavily rely on the convenient, built-in `IsAlive` property.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Thread Life Cycle', 'Once a thread completely finishes executing its assigned method and transitions into the `Stopped` state, how can it be restarted to run the method again?', 'By calling `Thread.Reset()`.', 'By calling `Thread.Start()` again.', 'By changing its `ThreadState` property back to `Unstarted`.', 'It is strictly impossible; a thread''s lifecycle is one-way, and a stopped thread can never be restarted.', 'D', 'Managed threads are strictly single-use entities. Once a thread reaches the `Stopped` state, its underlying OS-level resources are permanently destroyed. Attempting to call `Start()` on a stopped thread violently throws a `ThreadStateException`.');

INSERT INTO Quizzes (LessonTitle, QuestionText, OptionA, OptionB, OptionC, OptionD, CorrectAnswer, Explanation) VALUES 
('C# Thread Life Cycle', 'In the legacy .NET Framework, if `Thread.Abort()` is called on a running thread, what temporary transition state does it enter before it finally dies?', 'AbortRequested', 'Terminating', 'Faulted', 'Stopping', 'A', 'When a forced `Abort()` is invoked, the thread transitions to `AbortRequested` while the CLR attempts to safely inject the `ThreadAbortException` and unwind the stack. Once the stack unwinds completely, it permanently enters the `Aborted` state. (Note: Forced abortion is heavily deprecated in modern .NET).');